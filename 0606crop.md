# 0606 作物状态图片接入计划

## 本轮实现状态（2026-06-06）

状态：已接入。

- 设置弹窗「外观」页新增 `原版` / `图片` 显示切换，默认 `原版`，偏好写入 `useSettingsStore.farmPlotDisplayMode` 并随设置存档。
- 个人田庄网格和温室网格在图片模式下显示 `CropImage`，原有缺水、洒水器、肥料、虫害、杂草角标继续保留。
- 田庄 / 温室小格的 `tile` 模式外层占地块 98%，内部图片保持原始比例，不额外放大或裁切；详情大图仍保持完整预览。
- 个人地块详情和温室地块详情新增作物大图与 `01` / `02` 变体选择器，选择按 `cropId` 保存，并通过本地存储与 `/api/taoyuan/crop-image-preferences` 同步。
- `resolveCropVisualState` 已按优先级解析：虫害 > 杂草 > 缺水 > 已施肥 > 已浇水 > 生长阶段 > 成熟。
- `prepare:crop-assets` 已生成 manifest / QA report：`23752` 个图片文件，`447/447` 个作物映射，基础状态、变体和特殊状态均无缺失。
- 服务端已挂载 `/crop`，支持 `TAOYUAN_CROP_ASSET_DIR`，manifest / QA report 使用短缓存，图片使用长期缓存。
- `Dockerfile` 和 `Dockerfile.repack` 已复制 `taoyuan-main/public/crop` 到 `/opt/taoyuan/crop`，重打包后容器可直接访问作物素材。

本轮验证：

```powershell
npm --prefix taoyuan-main run prepare:crop-assets
npm --prefix taoyuan-main run qa:crop-assets
npm --prefix taoyuan-main run check
node --check server\src\index.js
node --check server\src\routes\api.js
```

验证结果：`qa:crop-assets` 通过；`check` 通过且仅剩既有 lint warnings；两个后端文件语法检查通过。

## 变更类型

Addition：在现有田庄和温室地块显示上新增一套作物状态图片表现，并保留原有图标 / 文字表现供玩家选择。

## 当前结论

- `taoyuan-main/public/crop/` 已经是运行时素材目录，不需要二次压缩。
- 目录包含 `128/` 和 `256/` 两档 `.webp`。
- 文件命名格式为 `作物名·状态__变体.webp`，例如 `青菜·幼苗__01.webp`。
- 作物素材和 `taoyuan-main/src/data/crops.ts` 的 447 个作物名称可以完全对齐。
- 每个作物都有基础状态图，特殊状态图与作物配置一致：
  - `deepWatering` 作物有 `深灌水泽`
  - `regrowth` 作物有 `再生期`
  - `giantCropEligible` 作物有 `巨型成熟`

## 目标

让玩家在田庄里选择两种显示模式：

- 原版：继续使用当前的颜色、图标、作物名文字和角标。
- 图片：地块主体显示 `crop` 目录中的作物状态图片，角标继续保留关键信息。

默认建议保留原版显示，避免上线后突然改变老玩家界面。玩家切换为图片模式后，选择应随设置保存。

## 用户设置

在 `useSettingsStore` 增加显示偏好：

```ts
type FarmPlotDisplayMode = 'classic' | 'image'
farmPlotDisplayMode: FarmPlotDisplayMode
```

序列化 / 反序列化规则：

- 默认值：`classic`
- 只接受 `classic` 或 `image`
- 写入 `settings.serialize()`
- 从旧存档读取时缺字段自动回落 `classic`

入口放在 `SettingsDialog.vue` 的「外观」页，用分段控件：

- `原版`
- `图片`

不要做成新页面，不占用田庄页批量操作入口。

## 图片状态优先级

图片模式下，地块状态按以下优先级决定图片：

1. 虫害
2. 杂草
3. 缺水
4. 已施肥
5. 已浇水
6. 生长阶段
7. 成熟

具体解析规则：

```text
if plot.infested:
  state = '虫害'
else if plot.weedy:
  state = '杂草'
else if plot.state in ['planted', 'growing'] && !plot.watered:
  state = '缺水'
else if plot.fertilizer:
  state = '已施肥'
else if plot.state in ['planted', 'growing', 'harvestable'] && plot.watered:
  state = crop.deepWatering ? '深灌水泽' : '已浇水'
else if plot.state === 'planted':
  state = '播种'
else if plot.state === 'growing':
  state = resolveGrowthStage(plot, crop)
else if plot.state === 'harvestable':
  state = plot.giantCropGroup !== null ? '巨型成熟' : '成熟可收获'
else:
  no crop image, use original display
```

`resolveGrowthStage(plot, crop)` 建议按成长进度分段：

```text
progress < 0.20: '发芽'
progress < 0.45: '幼苗'
progress < 0.70: '生长期'
progress < 0.90: '花苞抽穗'
otherwise: '结果结实'
```

如果作物是多茬作物，并且 `plot.harvestCount > 0` 且状态为 `growing`，优先用 `再生期`。该规则属于“生长阶段”内部，不覆盖虫害、杂草、缺水、施肥和浇水。

## 变体选择

`__01` / `__02` 参照物品图标和 NPC 头像偏好处理：

- 默认使用 `01`
- 在地块详情弹窗的作物大图下方显示两个小预览按钮
- 玩家点开详情图后可以切换 `01` / `02`
- 选择按 `cropId` 保存，不按单个地块保存
- 同一种作物在田庄、温室和详情里统一使用同一个变体

建议新增：

```text
taoyuan-main/src/composables/useCropImagePreferences.ts
taoyuan-main/src/components/game/CropImageVariantPicker.vue
```

本地存储 key 建议：

```text
taoyuanxiang_crop_image_preferences_v1
```

如果后端已有偏好接口扩展空间，可以仿照 `/api/taoyuan/item-icon-preferences` 做 `/api/taoyuan/crop-image-preferences`；第一版也可以先本地持久化，离线可用。

## 资源管线

新增脚本：

```text
taoyuan-main/scripts/prepare-crop-assets.cjs
```

新增 npm script：

```json
"prepare:crop-assets": "node scripts/prepare-crop-assets.cjs"
```

生成文件：

```text
taoyuan-main/public/crop/crop-asset-manifest.json
taoyuan-main/public/crop/crop-asset-qa-report.json
```

manifest 建议字段：

```ts
interface CropAssetManifest {
  version: string
  generatedAt: string
  basePath: '/crop'
  defaultVariant: '01'
  sizes: [128, 256]
  states: string[]
  byCropId: Record<string, CropAssetEntry>
  byName: Record<string, CropAssetEntry>
}

interface CropAssetEntry {
  cropId: string
  name: string
  seedId: string
  deepWatering: boolean
  regrowth: boolean
  giantCropEligible: boolean
  states: Record<string, Partial<Record<'01' | '02', { 128?: string; 256?: string }>>>
}
```

解析文件名时不要直接用简单字符串切第一个 `·`。应按已知状态后缀匹配，或从最后一个 `·状态__变体` 拆分，避免作物名本身包含相似字符时误拆。

QA report 至少包含：

- 总文件数
- 尺寸目录覆盖
- 作物名覆盖：`CROPS` 中 447 个作物必须全部映射
- 每个作物是否有 `01` / `02`
- 每个作物基础状态覆盖
- `deepWatering` / `regrowth` / `giantCropEligible` 与特殊状态图是否一致
- 未匹配、重复、非法命名文件清单

## 前端接入

新增：

```text
taoyuan-main/src/composables/useCropAssetManifest.ts
taoyuan-main/src/components/game/CropImage.vue
```

`CropImage.vue` props 建议：

```ts
cropId?: string | null
cropName?: string | null
plot?: FarmPlot | null
state?: CropVisualState
variant?: '01' | '02'
resolution?: 128 | 256
size?: 'xs' | 'sm' | 'md' | 'lg'
fallbackMode?: 'glyph' | 'classic'
```

本轮实现中额外使用了 `tile` 尺寸，用于农田 / 温室地块小格显示。

接入位置：

- `FarmView.vue` 个人田庄网格
- `FarmView.vue` 地块详情弹窗
- `FarmView.vue` 温室网格
- `FarmView.vue` 温室地块详情弹窗

图片模式只替换地块主体视觉，不删除原角标：

- 缺水角标保留
- 洒水器角标保留
- 肥料角标保留
- 虫害 / 杂草角标保留
- 巨型作物提示保留

原因：图片负责表达状态，角标负责快速扫读和兼容小屏。

## 服务端与部署

新增静态挂载：

```text
/crop -> /opt/taoyuan/crop
```

本地 Windows 回退：

```text
taoyuan-main/public/crop
```

新增环境变量：

```text
TAOYUAN_CROP_ASSET_DIR
```

manifest 和 QA report 不应长期缓存；图片文件可以长期缓存。

部署时同步：

```text
taoyuan-main/public/crop
-> /opt/taoyuan/crop
```

同时需要重新部署前端构建产物和服务端代码，否则旧页面不会读取新 manifest，服务端也不会挂载 `/crop`。

## 验收标准

- 玩家可以在田庄页切换 `原版` / `图片` 显示。
- 默认新旧存档均为 `经典`。
- 图片模式下，田庄和温室地块显示作物状态图。
- 状态优先级严格为：虫害 > 杂草 > 缺水 > 已施肥 > 已浇水 > 生长阶段 > 成熟。
- 地块详情中可以切换同一作物的 `01` / `02` 变体。
- 变体选择按作物保存，同作物所有地块同步变化。
- 缺 manifest、缺图、加载失败时不会报错，回退到原版显示或文字 fallback。
- `crop-asset-qa-report.json` 显示 447 个作物全部覆盖。
- `/crop/crop-asset-manifest.json` 在本地和生产均可访问。

## 验证命令

```powershell
npm --prefix taoyuan-main run prepare:crop-assets
npm --prefix taoyuan-main run type-check
npm --prefix taoyuan-main run lint
```

建议补充一个轻量 QA：

```powershell
npm --prefix taoyuan-main run qa:crop-assets
```

该 QA 检查 manifest 覆盖、状态优先级 helper、设置序列化，以及 `SettingsDialog.vue` 中显示模式入口和 `CropImageVariantPicker` 是否接入。

## 不做范围

- 不改作物成长、虫害、杂草、浇水、施肥、收获逻辑。
- 不改变作物数值、掉落、售价或品质。
- 不把 `crop` 图片合并进 `/item` 图标 manifest。
- 第一版不接共同农田地图；共同农田有联机读写边界，建议后续单独接。
