# 0521 玩法 bug 全量审查（进行中）

> 续 `0520全量审查.md` 的联机线，本份专门针对单机玩法系统（时间 / 田庄 / 牧场 / 钓鱼 / 鱼塘 / 矿洞 / 烹饪 / 加工 / 育种 / 钱包 / 仓库 / 商店 / 任务 / 成就 / 技能 / 装饰 / 行旅图 等）做一轮 bug 复核。
> 完成时间：2026-05-21（首轮提交）。后续轮次会继续覆盖未审到的玩法 store。

## 0. 复核范围

第一轮重点（已读源码）：
- `taoyuan-main/src/stores/useGameStore.ts`（时间 / 季节 / 天气 / 旅行 / 序列化）
- `taoyuan-main/src/stores/useFarmStore.ts`（地块 / 种植 / 浇水 / 收获 / 多茬作物）
- `taoyuan-main/src/stores/useAnimalStore.ts`（喂食 / 抚摸 / 心情 / 产品 / 死亡）
- `taoyuan-main/src/stores/useFishingStore.ts`（钓鱼 / 鱼饵 / 鱼竿 / 体力 / 小游戏参数）
- `taoyuan-main/src/stores/useBreedingStore.ts`（首段）
- `taoyuan-main/src/stores/useMiningStore.ts`（首段 + 战斗入口）
- `taoyuan-main/src/stores/useWalletStore.ts`（首段）
- `taoyuan-main/src/composables/useFarmActions.ts`（advanceTime 调用方）

下一轮将继续：`useFishPondStore.ts`、`useBreedingStore.ts` 中段、`useMiningStore.ts` 战斗 / 探险、`useCookingStore.ts`、`useProcessingStore.ts`、`useShopStore.ts`、`useInventoryStore.ts`、`useQuestStore.ts`、`useGoalStore.ts`、`useAchievementStore.ts`、`useSkillStore.ts`、`useHiddenNpcStore.ts`、`useDecorationStore.ts`、`useRegionMapStore.ts`、`useGuildStore.ts`、`useMuseumStore.ts`、`useHanhaiStore.ts`。

## 1. 已识别问题清单（首轮）

### [P1] 钓鱼 junkChance 没有上限夹钳

- 位置：`taoyuan-main/src/stores/useFishingStore.ts:273-274`
  ```ts
  const junkBase = 0.12 - skillStore.fishingLevel * 0.01
  const junkChance = Math.max(0, (baitDef ? junkBase * 0.5 : junkBase) + environmentWindow.value.fishing.junkChanceDelta)
  ```
- 触发条件：环境窗口 `junkChanceDelta` 较大正值时，或多重叠加正向 delta。
- 现象：`junkChance` 可超过 1.0；`Math.random() < junkChance` 恒为真，抛竿一律得到垃圾。
- 影响范围：钓鱼线长期手感劣化、玩家无法理解 “一直钓不到鱼”。
- 证据：仅做了 `Math.max(0, ...)` 下钳，未做 `Math.min(0.95, ...)` 上钳。
- 建议修法：改为 `Math.min(0.95, Math.max(0, ...))`，留 5% 给真正鱼。
- 验证方式：人为把 `junkChanceDelta=1.5` 后开钓，期望 ~5% 概率仍能见到鱼。

### [P2] 天气滚动里 `rainBoost` 不夹钳，极端值会吞掉晴天

- 位置：`taoyuan-main/src/stores/useGameStore.ts:130-141`
  ```ts
  const rainBoost = useHiddenNpcStore().getAbilityValue('long_ling_2') / 100
  // ...
  return roll < 0.5 - rainBoost ? 'sunny' : ...
  ```
- 触发条件：仙缘 `long_ling_2` 数值若 ≥50（成长后 / 文档预期 15%，但实际未在 store 内做硬上限）。
- 现象：`0.5 - rainBoost <= 0`，`Math.random()` 永远不小于负数，晴天概率归零。
- 影响范围：天气体感与预期严重不一致。
- 建议修法：在调用处或 `getAbilityValue('long_ling_2')` 内强制 `clamp(0, 30)` 之类的上限，并把 0.5/0.42/0.45 三处统一过 `Math.max(0.05, base - rainBoost)`。
- 验证方式：手动把仙缘等级拉满，跑 100 天天气日志，期望晴天 ≥10% 保底。

### [P2] 兔子幸运兔脚不要求 `wasFed`

- 位置：`taoyuan-main/src/stores/useAnimalStore.ts:794`
  ```ts
  if (animal.type === 'rabbit' && animal.friendship >= 600 && !animal.sick && Math.random() < 0.04) {
    products.push({ itemId: 'rabbit_foot', quality: getAnimalProductQuality(animal.friendship) })
  }
  ```
- 触发条件：兔子 ≥600 好感但今日未喂食。
- 现象：饥饿一天的兔子仍可掉幸运兔脚；与主产出 `wasFed && !sick` 的口径不一致。
- 影响范围：玩家可以躺平养鸡式收兔脚，破坏产出节奏。
- 建议修法：补 `animal.wasFed` 条件，与主产出一致；或在 design intent 文件里明确允许 “饥饿兔仍掉兔脚”。
- 验证方式：构造 “饥饿兔 + 高好感” 场景跑 50 天，验证兔脚不再被掉落。

### [P2] `advanceTime` 在 “停在 PASSOUT_HOUR” 后由调用方决定收尾，部分调用方只看 `passedOut` 不收尾

- 位置：`taoyuan-main/src/stores/useGameStore.ts:152-176`（advanceTime 实现） + 调用方多处。
- 现象：`advanceTime` 在 `newHour >= PASSOUT_HOUR` 时把 `hour.value` 锁到 `PASSOUT_HOUR` 并返回 `passedOut:true`，**不自动 nextDay**。`useFarmActions.ts` 多数行如 `handleEndDay()` 兜底，但 `useFarmActions.ts:95/150/190/259` 仅 `if (tr.passedOut) { handleEndDay(); return }`，看起来 OK；而 `useNavigation.ts:158` 是 `if (travelResult.passedOut)` 后续段需要复核是否实际推进了一天。
- 影响范围：若任一动作分支漏调 `handleEndDay()`，玩家时间会卡死在 PASSOUT_HOUR，体力 / 行动均不能继续。
- 建议修法：把 “passOut → 强制 endDay” 收成 `useGameStore.advanceTime` 内部 side-effect，或在 `gameStore` 上集中 watch `hour === PASSOUT_HOUR` 自动 endDay。
- 验证方式：grep 所有 `advanceTime(` 调用方，逐条对照是否有 `handleEndDay`；下一轮报告会逐条标注。

### [P2] `deserialize` 不校验非法值

- 位置：`taoyuan-main/src/stores/useGameStore.ts:352-372`
  ```ts
  season.value = data.season ?? 'spring'
  weather.value = data.weather ?? 'sunny'
  currentLocation.value = data.currentLocation ?? 'farm'
  ```
- 触发条件：旧档 / 损坏档 / 跨版本回档；存档迁移流程里某字段被改坏。
- 现象：直接赋值，没有 `(SEASON_ORDER as readonly Season[]).includes(data.season)` 等白名单。
- 影响范围：非法 season → 派生 computed（如 `seasonIndex` 返回 -1）后续公式连环异常。
- 建议修法：每个枚举字段做一次白名单校验，命中即用，否则回 fallback。
- 验证方式：构造 `data.season='dragon_season'` 的人工 raw，期望加载后 fallback 到 spring 而不是 propagate。

### [P3] `plantCrop` 与 `plantGeneticSeed` 大段字段复制粘贴

- 位置：`useFarmStore.ts:84-124`。
- 现象：两个函数除 cropId / seedGenetics 来源不同，其它字段重置完全一致。任一字段未来新加，需同步两处。
- 建议修法：抽出 `resetPlotForPlanting(plot, cropId, genetics)` 私有函数；外层只做合法性检查。

### [P3] 多文件存在 UTF-8 / GBK 混合编码注释

- 位置：`useGameStore.ts:25-26 / 36 / 46 / 52 / 130-150 / 270-290`、`useFarmStore.ts` 中段、`useFishingStore.ts` 顶端、`useAnimalStore.ts` 个别行等。
- 现象：同一文件内同时存在正确中文注释（“季节中文名”）与乱码注释（“瀛ｈ妭椤哄簭”）。
- 影响：构建 / 运行不受影响（JS 注释会被 esbuild 丢掉）；但 git diff / IDE / 玩家二次开发阅读体验劣化。
- 建议修法：统一全部源文件 BOM-less UTF-8 重存；并在 `.editorconfig` 添加 `charset = utf-8` 约束、`pre-commit` 增加非 UTF-8 检测。
- 验证方式：`file` 命令逐项识别字符集；CI 添加 `encoding` lint。

### [P3] `nextDay` 内 `dailyLuck` 立即用 `Math.random()`，无确定性回放

- 位置：`useGameStore.ts:295`。
- 现象：跨设备同步存档 + 重放无法复现同一日运势；但 dailyLuck 持久化后跨开关机正确。属于设计约束而非 bug，但若未来引入 “重玩本周” 之类玩法需要确定性，会成阻碍。
- 建议修法：引入种子化 RNG（每周种子 + 偏置）；非紧急。

### [P3] `processHiddenNpcDiscovery` 在每次 `advanceTime` 触发

- 位置：`useGameStore.ts:167`。
- 现象：哪怕 0.5h 的小动作（喂动物、浇水）也会触发一次 hidden NPC 全表扫描。规模化后是热点。
- 建议修法：把检测节流到 “每整点触发一次” 或 “每地点切换时”。

### [P3] `useGameStore.deserialize` 对 `data.hour ?? DAY_START_HOUR >= MIDNIGHT_HOUR` 的写法歧义

- 位置：`useGameStore.ts:357`
  ```ts
  midnightWarned.value = (data.hour ?? DAY_START_HOUR) >= MIDNIGHT_HOUR
  ```
- 现象：若玩家恰好在午夜后存档再读档，会把 `midnightWarned` 视为已警告过；新一天 `nextDay` 时被显式置 false，逻辑正确。但语义上不够清晰。
- 建议修法：直接读 `data.midnightWarned ?? ((data.hour ?? DAY_START_HOUR) >= MIDNIGHT_HOUR)`，避免覆盖玩家真实 warned 历史。

### [P3] 钓鱼 `legendaryBoost = 1 + getAbilityValue('long_ling_3') / 100` 未夹钳负向

- 位置：`useFishingStore.ts:425`
- 现象：若 ability 返回负值，`legendaryBoost < 1` → 反向降低传说鱼概率。
- 建议修法：`Math.max(1, 1 + ability/100)` 显式保底。

## 2. 已确认为正常行为的疑似项（避免误报）

- `useFarmStore.harvestPlot` 多茬作物的 `growthDays = growthDays - regrowthDays` ✅
- `useAnimalStore` 死亡过滤逻辑 `animals.filter(a => a.hunger < HUNGER_DEATH_DAYS && a.sickDays < SICK_DEATH_DAYS)` ✅
- `nextDay()` 把 `tomorrowWeather` 滚到 `weather`、并预滚下下天 ✅
- `useGameStore.startNewGame(mapType)` 重置完整时间 / 天气 / 地点 ✅
- `useWalletStore.normalizeRewardTicketLedger` 对 NaN / Infinity / 负数都做了 `Math.max(0, Math.floor(...))` ✅

## 3. 第二轮新增问题（鱼塘 / 矿洞宝藏 / 仓库 / 技能 / 仙缘）

### [P2] `useSkillStore.addExp` 不夹钳负向加成

- 位置：`taoyuan-main/src/stores/useSkillStore.ts:97-102`
  ```ts
  const ringExpBonus = useInventoryStore().getRingEffectValue('exp_bonus')
  const adjustedAmount = Math.floor(amount * (1 + ringExpBonus))
  skill.exp += adjustedAmount
  ```
- 触发条件：戒指 `exp_bonus` 出现负值（例如未来引入 “消耗经验换稀有产出” 的负面戒指、或配置错误）。
- 现象：`adjustedAmount` 可为负，`skill.exp` 反向回退，可能跌破 EXP_TABLE 阈值；但 `skill.level` 不会回退（while 条件 `skill.exp >= nextLevelExp` 不触发），所以等级保留、经验体验异常。
- 建议修法：`const adjustedAmount = Math.max(0, Math.floor(amount * Math.max(0, 1 + ringExpBonus)))`。
- 验证方式：单元测试 `addExp('farming', 10)` 在 `exp_bonus = -2` 下不让经验回退。

### [P2] `useInventoryStore.addItem` 在 “最终丢失” 仍标记成就发现

- 位置：`taoyuan-main/src/stores/useInventoryStore.ts:254-307`
- 现象：函数入口立即 `useAchievementStore().discoverItem(itemId)`，随后 fallback 路径里如果主背包 + 临时背包都满，物品 `quantity` 被丢弃但 “图鉴解锁” 已写入。
- 影响：背包满时也能解锁稀有物品图鉴，未必符合玩家预期 “没真正捡到”。
- 建议修法：成就 `discoverItem` 只在 `remaining <= 0` 时调用，或在调用前比对 `canAddItem(itemId, quantity)`，仅在确定能完整入包时解锁。
- 验证方式：清空背包到只剩 1 格 + 满临时背包，尝试拾取一个未发现物品 → 期望图鉴不解锁。

### [P2] `useFishPondStore.buildPondFishRatingSnapshot` 的 `displayBonus` 在 bonus=0 时仍给 4 分

- 位置：`taoyuan-main/src/stores/useFishPondStore.ts:283`
  ```ts
  const displayBonus = displayAssigned ? Math.max(4, Math.floor(maintenanceConfig.ornamentalFeedShowBonus / 2)) : 0
  ```
- 触发条件：`ornamentalFeedShowBonus` 被配置为 0（不应给 buff）但 fish 是 displayAssigned。
- 现象：仍返回 `Math.max(4, 0) = 4`，等于硬编码了 “上展示池就 +4”。
- 影响：观赏分系数偏高，难以靠配置真正关闭。
- 建议修法：`displayBonus = displayAssigned ? Math.max(0, Math.floor(maintenanceConfig.ornamentalFeedShowBonus / 2)) : 0`；或将 +4 作为常量并显式注释 “即使配置为 0 也给 4”。

### [P3] 矿洞宝箱掉落对 `tile.data.treasureItems` 进行原地 mutate

- 位置：`taoyuan-main/src/stores/useMiningStore.ts:738 / 758 / 776 / 789`
- 现象：函数把宝箱内 ring/hat/shoe/weapon 的 push 写回 `tile.data.treasureItems`（同一 items 引用）；仅用于本轮日志，但写到了 tile 的数据上。
- 影响：理论上 tile 状态会被改写为 'opened'/'revealed'，不会被二次打开。但单元测试若 reuse tile mock 会出现 “第二次开宝箱多出戒指/帽子” 的混淆。
- 建议修法：在函数顶部 `const itemsForLog = items.slice()`，对 `itemsForLog` push；不要改 `tile.data.treasureItems` 原数组。

### [P3] `useFishPondStore.normalizeMaintenanceState` 不校验枚举

- 位置：`useFishPondStore.ts:67-72`
- 现象：`buffDays / quarantineShieldDays` 通过 `Math.max(0, Number(value?.x) || 0)` 限制非负，但若 future config 引入更细的离散值（例如 `0|3|7|14`）则非整数值会通过。
- 建议修法：补 `Math.floor(...)`，并在 settings 加 “允许的离散值” 校验；目前是 P3 防御性建议。

### [P3] `useHiddenNpcStore.getAbilityValue` 不做能力值边界

- 位置：`taoyuan-main/src/stores/useHiddenNpcStore.ts:509-512`
- 现象：直接返回 `ability.passive.value ?? 0`，依赖 `HIDDEN_NPCS` 静态数据 “数值合理”。未来若数据漂移（PR 改 passive.value 到 60+），消费方（钓鱼 / 天气 / 矿洞）需要自己夹钳。
- 建议修法：在 `getAbilityValue` 内统一夹钳 `Math.max(0, Math.min(50, ...))`，给所有消费方一个安全口径；或者每个消费方显式 clamp（已在阶段一报告里点名钓鱼 / 天气两处）。

### [P3] `dailyBlessingPreview` 在 mastery 解锁中途切换会无声变化

- 位置：`taoyuan-main/src/stores/useSkillStore.ts:68-75`
- 现象：blessing 仅在 mastery `blessing_altar` 已解锁时返回。如果玩家在白天中途解锁 mastery，从这一刻起钓鱼 / 矿洞 / 战斗的 blessing 加成立即生效，没有任何提示。
- 影响：体感隐性、不可复现。
- 建议修法：把 “mastery 解锁刹那” 通过 addLog 提示，并在 UI 主菜单显式展示当日 blessing 文案。

### [P3] `useCookingStore.normalizeActiveBuff` 不做 value 上限

- 位置：`taoyuan-main/src/stores/useCookingStore.ts:30-41`
- 现象：`Number.isFinite(numericValue)` 通过即接受。负数、超大值都能进入 `activeBuff`。后续 `getActiveFishingSkillBonus / getActiveMiningStaminaReduction` 等直接消费。
- 建议修法：根据 buff type 加合理上限（`farming/fishing/mining` 等技能 buff ≤ 5、`speed` ≤ 0.5、`stamina` ≤ 50）。

## 4. 已确认正常的疑似项（避免误报）

- `useInventoryStore.addItem` 主背包 + 临时背包堆叠 + 溢出回退逻辑正确（`items.value.push` 在 `isFull` 守卫下，溢出走 `tempItems`，仍满则 “物品丢失” 提示）✅
- `useSkillStore.addExp` 等级上限 20 + EXP_TABLE 索引边界正确 ✅
- 矿洞 `_handleTreasureTile` 对掉落 ring/hat/shoe/weapon 不消耗主背包格子（走 ownedRings/Hats/Shoes 独立数组）✅
- 鱼塘 `clamp(..., 0, 100)` 对 showValue/foodValue/healthScore/stabilityScore 都加了上下夹钳 ✅
- 钓鱼 `_combatTileIndex.value = tile.index` 与 `floorGrid` 索引一致 ✅

## 5. 第三轮新增问题（育种 / 战斗 / 装饰 / 公会 / 瀚海 / 成就）

### [P3] `useAchievementStore.ts` 残留乱码注释 dead code

- 位置：`taoyuan-main/src/stores/useAchievementStore.ts:65-84`
- 现象：`/* ... */` 块包含旧版本 `discoverItem / buildCurrentDiscoveryTime` 实现，字符串里是 GBK 乱码（`'鏄?', '澶?', '绗?{...}'`）。新实现已用 `'春' / '夏' / '秋' / '冬'` Unicode 转义重写，正确替代乱码版。
- 影响：dead code 不影响运行，但阅读 / 重构时易误读。
- 建议修法：直接删除注释块；UTF-8 重存一次以消除其它注释里的潜在乱码字节。

### [P3] `useBreedingStore.breedSameCrop` 稳定度只夹钳上限不夹下限

- 位置：`taoyuan-main/src/stores/useBreedingStore.ts:981`
  ```ts
  stability: Math.min(Math.round(avgStability) + stabilityGain + researchStabilityBonus, MAX_STABILITY),
  ```
- 触发条件：未来调整 `GENERATIONAL_STABILITY_GAIN` 至 < 5（或新增其它负向 modifier），自交场景 `stabilityGain` 可为负，最终 stability < 0。
- 建议修法：`Math.max(0, Math.min(..., MAX_STABILITY))` 上下夹钳；当前 const 配置下尚不触发，属防御性补强。

### [P3] 多 store 各自重写绝对日 / 周编号

- 位置：
  - `taoyuan-main/src/stores/useGuildStore.ts:440-443`（`getCurrentDay`）
  - `taoyuan-main/src/stores/useShopStore.ts:104`（`getAbsoluteDay`）
  - `taoyuan-main/src/stores/useFishPondStore.ts:136-139`（`getCurrentDayTag`）
- 现象：`weekCycle.getAbsoluteDay` 已存在于 `@/utils/weekCycle`，被 useQuestStore / useGoalStore / useRegionMapStore 使用；上面三处各自重写 `(year-1)*112 + seasonIndex*28 + day` 表达式。
- 影响：任一处计算口径漂移（例如未来加入 “闰月 / 加更天”）都需要同步多处修。
- 建议修法：统一调用 `getAbsoluteDay`；保留 `getCurrentDayTag` 作为对外标签格式器。

## 6. 已确认正常的疑似项（第三轮）

- **战斗 runtime** `taoyuan-main/src/utils/combatRuntime.ts:62-115`：`clampChance`、`multiplyDamageModifiers(每项 Math.max(0))`、`calculateAttackDamage(minimumDamage 1)`、`rollAttackOutcome(critMult Math.max(1, ...))` 全部做了边界守卫 ✅
- **育种核心** `breedSeeds → breedSameCrop / breedDifferentCrop`：avg + fluctuate + clampStat + mutation event + isSelfing detection + lineageParents 记录，整体逻辑健康 ✅
- **装饰**：`grantDecoration` 正确 `Math.min(safeCount, def.maxCount - getOwnedCount(id))`，`hasReachedMaxCount` 命中阻止 ✅
- **瀚海下注重置**：`resetDailyBets` 由 `useEndDay.ts:830` 调用，单一入口的日切换链 ✅
- **公会 / 商店每日限购**：`ensureDailyReset` / `ensureWeeklyReset` 用绝对日 / 周编号 vs `lastResetDay` 比较，并在每次买卖前调用 ✅
- **加工槽位反序列化** `useProcessingStore.sanitizeProcessingSlot:87-100`：machineType / recipeId / quality / daysProcessed / totalDays 全部白名单 + 防御 fallback ✅
- **end-of-day 单一入口**：`useEndDay.ts:907` 是仓库内唯一 `gameStore.nextDay()` 调用点，全套 store 重置都在同一 pipeline 中编排 ✅

## 8. 第四轮新增问题（任务 / 玩家 / NPC / 商店 / 酒窖 / 经济遥测）

### [P2] `usePlayerStore.earnMoney` 用作 “退款” 时仍会污染收入遥测

- 位置：`taoyuan-main/src/stores/usePlayerStore.ts:748-756`、`useShopStore.ts:2390-2402`
  ```ts
  // shop
  if (!inventoryStore.addItemExact(itemId)) {
    playerStore.earnMoney(finalPrice, { countAsEarned: false })
    return false
  }

  // player
  const earnMoney = (amount, options?) => {
    if (normalizedAmount <= 0) return
    money.value += normalizedAmount
    recordEconomyFlow('income', normalizedAmount, options?.system ?? 'system')
    if (options?.countAsEarned ?? true) {
      useAchievementStore().recordMoneyEarned(normalizedAmount)
    }
  }
  ```
- 触发条件：旅商购买商品时背包刚好溢出 → 退款流程。
- 现象：退款依然走 `recordEconomyFlow('income', ...)`，使 `economyTelemetry.lifetimeIncome.total` 与 `bySystem` 被虚增；`countAsEarned: false` 只屏蔽 achievement 计数，未屏蔽遥测。
- 影响：经济回路报表（用于发布前调参的 ECONOMY_AUDIT_CONFIG 与回收平衡）失真；偶发，但叠加规模化购买/退款后偏差累积。
- 建议修法：给 `earnMoney` 增加 `options.recordTelemetry?: boolean`（默认 true），退款场景显式设 false；或为退款单独 helper `refundMoney(amount)` 直接回款不记账。
- 验证方式：在烟测里制造一次 “购入即退款”，断言 `economyTelemetry.lifetimeIncome.total` 没有上升。

### [P3] `usePlayerStore.consumeStamina` 对非法输入返回 `true`（“免费通过”）

- 位置：`usePlayerStore.ts:294-304`
  ```ts
  if (!Number.isFinite(normalizedAmount) || normalizedAmount <= 0) return true
  ```
- 现象：NaN / Infinity / 0 / 负数都被视为 “消耗成功”，调用方继续推进动作但实际未扣体力。
- 影响：若上游 bug 导致传入异常值，错误被吞，不易被发现。
- 建议修法：分流。`0` 返回 true（无消耗）；NaN / Infinity / 负数返回 false 并 `console.warn`（dev only）。

### [P3] `usePlayerStore.dailyReset(late|passout)` 恢复百分比未夹钳下限

- 位置：`usePlayerStore.ts:349-352, 358-361`
  ```ts
  recoveryPct = LATE_NIGHT_RECOVERY_MAX - t * (MAX - MIN) + staminaBonus + villageBonus
  appliedRecoveryPct = Math.min(recoveryPct, 1)
  stamina.value = Math.floor(maxStamina.value * appliedRecoveryPct)
  ```
- 现象：只 `Math.min(..., 1)`，没有 `Math.max(0, ...)`。若 staminaBonus / villageBonus 出现负值并占主导，`recoveryPct` 可为负，`stamina.value` 临时被设为负数，由 `normalizeDerivedState` 兜底回 0。
- 影响：UI 在事件序列中可能瞬间显示 0 体力 + “恢复失败” 误导。
- 建议修法：`appliedRecoveryPct = Math.max(0, Math.min(recoveryPct, 1))`。

### [P3] `useHomeStore.dailyCellarUpdate` 在 supreme 品质时 `daysAging` 持续递增

- 位置：`useHomeStore.ts:255-271`
- 现象：slot 已达 `supreme` 后，`currentIdx + 1 === QUALITY_ORDER.length`，`if (currentIdx < length - 1)` 为 false，但 `slot.daysAging++` 已在 if 之前发生，且没有 reset 路径。
- 影响：序列化时 daysAging 数值持续增长；玩家不主动取出酒，存档体积缓慢膨胀。
- 建议修法：supreme 后 `slot.daysAging = CELLAR_AGING_DAYS`（钳住），避免继续累加。

### [P3] `useHomeStore.deserialize` 不规范化 `cellarSlots`

- 位置：`useHomeStore.ts:300-305`
  ```ts
  cellarSlots.value = data.cellarSlots ?? []
  ```
- 现象：未对每个 slot 做 itemId / quality / daysAging 白名单校验。
- 影响：损坏存档若把 quality 改成非法字符串，后续 `QUALITY_ORDER.indexOf(slot.quality)` 返回 -1，`currentIdx + 1 === 0`，等同于把品质降到 normal。
- 建议修法：补一个 `normalizeCellarSlot(slot)` 函数对每个字段白名单 + fallback。

### [P3] 商店 `refreshMerchantStock` 中 `hu_xian_3` 仙缘加成用 `Math.random()`

- 位置：`useShopStore.ts:2353-2367`
- 现象：当 ability 激活时，每次 stock 重新生成（即每个旅商日首次访问）都 `Math.random()` 选一个稀有品 push 进去。其它子 stock 是按 day 种子化的，这一项不是。
- 影响：跨设备同步存档时，同一日同一旅商可能在不同设备出现不同的稀有品 push。
- 建议修法：把 `hu_xian_3` 的额外随机改为基于 `year_seasonIndex_day` 的确定性 hash 选品。

## 9. 已确认正常的疑似项（第四轮）

- **任务提交锁**：`beginQuestSubmission / finishQuestSubmission` 在 `submitQuest` 中由 `try-finally` 包裹（`useQuestStore.ts:1411-1824`），早返回路径也会清锁 ✅
- **特殊订单重复领奖**：`specialOrderSettlementReceipts` + `duplicateSettlementGuardEnabled` 双保险 ✅
- **NPC 好感度**：`adjustFriendship / talkTo` 均做了 `Math.min(Math.max(0, ...), cap)` 上下夹钳 ✅
- **`talkedToday`**：当天对话只算一次 +20，避免刷好感 ✅
- **takeDamage**：`Math.min(amount, hp.value)` 不让 HP 跌破 0 ✅
- **spendMoney**：`Number.isFinite + non-negative + normalize` 三重防御 ✅
- **setMoney**：`Math.max(0, Math.floor(amount))` 防御 ✅
- **dailyReset(passout) 金钱惩罚**：`Math.min(Math.floor(money*0.1), PASSOUT_MONEY_PENALTY_CAP)` 上限 ✅
- **shop buyFromTraveler 退款**：失败时显式 `earnMoney(finalPrice, countAsEarned:false)` 回款（仅遥测有副作用，见上方 P2）✅ 逻辑正确
- **shop refreshMerchantStock 跨日去重**：`travelingStockKey` 比对避免同日多次 push 重复稀有品 ✅
- **生命周期发现**（lifestyle discovery ledger）：count、firstSeen、lastSeen 三字段，正确的 “只增不减” 模式 ✅

## 10. 累计统计

到第四轮为止：**1 P1（首轮）+ 6 P2 + 14 P3**，共 21 条问题。

| 级别 | 数 | 累计来自 |
| --- | --- | --- |
| P1 | 1 | 钓鱼 junkChance 不夹上限（第一轮） |
| P2 | 6 | 天气 rainBoost 不夹钳；兔子兔脚不需要喂食；advanceTime passedOut 不统一；deserialize 不校验枚举；技能负向 exp 不夹钳；addItem 满包仍解锁图鉴；鱼塘 displayBonus 隐性 +4；earnMoney 退款污染遥测 |
| P3 | 14 | plantCrop/plantGeneticSeed 字段重复；编码混合注释；dailyLuck 非确定性；processHiddenNpcDiscovery 热点；deserialize midnightWarned 写法；legendaryBoost 不夹钳负向；treasure tile 原地 mutate；fishPond normalize 离散值；getAbilityValue 不夹钳；blessing 解锁无声生效；cooking buff value 不夹钳；成就乱码 dead code；breedSameCrop 稳定度不夹下限；多 store 重写绝对日；consumeStamina 非法输入返 true；dailyReset 恢复百分比不夹下限；cellarSlots supreme daysAging 累加；deserialize cellarSlots 不规范化；hu_xian_3 非确定性随机 |

## 12. 第五轮新增问题（送礼 / 周结算 / 瀚海事务回滚）

### [P3] NPC `giftsThisWeek` 周重置仅靠 `day % 7 === 0`，debug / 跳天会让计数器永不归零

- 位置：`taoyuan-main/src/stores/useNpcStore.ts:2394-2397`
  ```ts
  if (gameStore.day % 7 === 0) {
    state.giftsThisWeek = 0
  }
  ```
- 触发条件：
  - 正常玩家：day 7 / 14 / 21 / 28 各跑一次 endOfDay 时归零，行为正确；
  - debug / 跳天 / 跨季节 day = 1：若 endOfDay 不经过 7 / 14 / 21 / 28，`giftsThisWeek` 不归零。
- 现象：在测试 / dev / 跳天场景下，玩家可能看到 `本周已送满 2/2` 持续好几周。
- 影响：仅影响 dev 与 QA；普通玩家路径不触发，所以仅 P3。
- 建议修法：把重置逻辑改为 “跨绝对周” 对比（基于 `getWeekCycleInfo(year, season, day)`），与商店 / 公会的周重置统一。

### [P3] 生日 + 极品 + 增益倍率叠加，hated gift 友好度跳水过大

- 位置：`taoyuan-main/src/stores/useNpcStore.ts:1095-1115`
- 触发条件：玩家在 NPC 生日，使用极品（×2）品质送出 hated 物品；同时 cooking buff 给出 `giftBonusMultiplier > 1`。
- 现象：`gain = -40 × 2 × 4 × giftBonusMultiplier`，可达 `-320 × buff`。一次错送可能瞬间打掉 1/3 心。
- 影响：如果是有意惩罚（“别在生日送讨厌的礼物”），OK；如果是漏算（生日加倍只应该用在正面礼物），属 bug。
- 建议修法：`gain < 0` 时不再乘 birthdayMultiplier，例如 `const effectiveBirthdayMultiplier = gain >= 0 ? birthdayMultiplier : 1`。

## 13. 已确认正常的疑似项（第五轮）

- **瀚海事务回滚** `createHanhaiActionSnapshots / rollbackHanhaiAction` （`useHanhaiStore.ts:591-611`）：snapshot 包含 player + inventory + wallet + hanhai，`hanhai.serialize()` 内含 `casinoBetsToday`，**rollback 路径完整恢复每日下注计数器**。任何 `catch` 路径不会偷扣下注次数。✅
- **周结算幂等** `useGoalStore.settleWeeklyGoals:1520`：`lastSettledWeeklyGoalWeekId === weekInfo.seasonWeekId` 守卫 + `goal.rewarded` 标记防双发奖。✅
- **送礼防刷**：`giftedToday` + `giftsThisWeek >= 2` 双重限制；删物失败不扣礼数。✅
- **giveGift `qualityMultiplier`**：1 / 1.25 / 1.5 / 2.0 与其它系统一致。✅
- **endOfDay 日重置**：`talkedToday / giftedToday` 每天清空 + 婚姻 / 知己未对话扣好感 + 美观度好友加成（最后兜上 friendshipCap）。完整链路无漏。✅
- **startDating / propose 锁 + 快照 + 回滚**：`beginRelationshipAction / rollbackRelationshipAction / finishRelationshipAction` 三件套与 hanhai 模式一致。`silk_ribbon` 删除失败 / 关系不满足 / 异常都不进入 dating 状态。✅
- **roulette / dice / cup**：每次都 `playerStore.spendMoney(betTier)` 前置失败 → 早返回，不进入 `casinoBetsToday++`。✅
- **roulette `betTier` 校验**：`ROULETTE_BET_TIERS.includes(betTier)` 白名单防注入。✅

## 14. 累计统计（截至第五轮）

到第五轮为止：**1 P1 + 6 P2 + 16 P3 = 23 条**。

| 级别 | 第一轮 | 第二轮 | 第三轮 | 第四轮 | 第五轮 | 累计 |
| --- | --- | --- | --- | --- | --- | --- |
| P1 | 1 | 0 | 0 | 0 | 0 | 1 |
| P2 | 4 | 2 | 0 | 1 | 0 | 7（含 deserialize 校验等被多次细化为单条） |
| P3 | 4 | 4 | 3 | 5 | 2 | 18 |

> 上表用于跨轮次追踪；与正文逐条对照可能略有合并。最终统一以正文 P1/P2/P3 区块为准。

## 16. 第六轮新增问题（商店出售 / 博物馆 / 瀚海完整子游戏 / View 退款链）

### [P2] 退款污染遥测的影响范围扩大（不止 buyFromTraveler）

- 位置：
  - `taoyuan-main/src/views/game/AnimalView.vue:1154-1158`（购饲料失败退款）
  - `taoyuan-main/src/stores/useShopStore.ts:2390-2402`（旅商退款，原 P2）
  - 其它 view 内 `spendMoney → ... → earnMoney(amount)` 兜底回退路径同样存在
- 现象：与 §8 已记录的 P2 同根因——`earnMoney` 不区分 “收入” 与 “退款”，所有调用都进 `economyTelemetry.lifetimeIncome.total` + `bySystem`。
- 建议合并修法：在 `usePlayerStore.earnMoney` 加 `options.recordTelemetry?: boolean` 默认 true；所有退款显式 `false`。或新增 `playerStore.refundMoney(amount)` 不走遥测。
- 验证方式：选 “购买后立刻被背包满拒收” 的样本数据，断言 `lifetimeIncome.total` 未上升。

### [P3] `useShopStore.sellItem` 缺数量与品质合法性校验

- 位置：`useShopStore.ts:2325-2331`
  ```ts
  const sellItem = (itemId, quantity = 1, quality = 'normal'): number => {
    if (!inventoryStore.removeItem(itemId, quantity, quality)) return 0
    const totalPrice = calculateSellPrice(itemId, quantity, quality)
    playerStore.earnMoney(totalPrice)
    recordCompletedSale(itemId, quantity, 'direct_shop')
    return totalPrice
  }
  ```
- 现象：未对 `quantity <= 0 / NaN / Infinity / 非整数` 做前置校验。如果上游传入异常 quantity，`removeItem` 行为不定（可能成功移除 0 件），`recordCompletedSale` 会记账非法数量。
- 建议修法：函数入口先 `const safeQuantity = normalizeNonNegativeInteger(quantity); if (safeQuantity <= 0) return 0`。

### [P3] `useMuseumStore.donateItem` 依赖 feature flag 才有锁

- 位置：`useMuseumStore.ts:666-696, 719-728`
- 现象：`beginMuseumAction / finishMuseumAction` 都受 `museumFeatureFlags.museumActionGuardEnabled` 控制。若 flag 关闭，锁被绕过。当前业务全同步且 `canDonate` 在 push 之前检查，单次点击不会重复入；但未来引入异步 dialog 确认 / animation 时缺锁会暴露。
- 建议修法：去掉 feature flag，锁始终开启；或在 flag 关闭时仍保留同步双击防御 `if (donatedItems.value.includes(itemId)) return false`。

## 17. 已确认正常的疑似项（第六轮）

- **瀚海完整子游戏闭环**：`playCricketFight / playCardFlip / startTexas` 与 `playRoulette / playDice / playCup` 用同一套 `beginHanhaiAction → snapshot → spendMoney → casinoBetsToday++ → try-catch-rollback → finally finishHanhaiAction` 模板；任意环节抛错可完整回滚。✅
- **Texas 多局并发防御**：`if (activeTexasSession.value && !activeTexasSession.value.settled)` 拒绝开新局。✅
- **`hanhai.serialize` 包含 `casinoBetsToday`**：rollback 路径恢复每日下注计数器，确保异常不偷扣次数。✅
- **MuseumStore donateItem 快照回滚**：`inventorySnapshot + donatedSnapshot` 双 snapshot；catch 路径恢复后返回 false。✅
- **ShopStore `sellItem` `removeItem` 失败短路**：先扣库存再计价、再入账；扣库存失败直接 0。✅
- **FarmView `plantGeneticSeed` 失败路径**：体力扣后若 plant 失败立即 `restoreStamina(cost)`；并且育种种子保留在 box 里没有被消耗（`removeFromBox` 只在 success 分支调用）。✅
- **AuthView / MainMenu / HallView async**：所有 `await fetch(...)` 调用都被 `try { ... } catch { ... }` 包裹；`res.json().catch(() => null)` 防御 JSON 解析失败；GameLayout 内 mailbox refresh 也用 `.catch(() => {})` 静默兜底。✅

## 18. 最终累计与排序（六轮收官）

到第六轮，本份审查的当前覆盖范围已足以支撑发布前 bug 优先级排期。

### 18.1 P0

> 0 条。本轮单机玩法系统中未发现导致存档损坏 / 数据丢失 / 玩家进度永久错误的 P0 级问题。

### 18.2 P1

```md
### [P1] 钓鱼 junkChance 没有上限夹钳（首轮 §1）
- 位置：useFishingStore.ts:273-274
- 关键风险：环境窗口 junkChanceDelta 累加可使 junkChance > 1，抛竿永远是垃圾
```

### 18.3 P2（按建议修复顺序）

```md
### [P2-1] earnMoney 退款路径污染收入遥测（§8、§16 合并）
- 位置：usePlayerStore.ts:748-756 + 多处 view / store 调用
- 影响：经济回路 / 调参报表失真
- 修法：earnMoney 增加 recordTelemetry:false 路径；改造所有退款点

### [P2-2] 天气 rainBoost 不夹钳，仙缘满级会吞掉晴天概率（首轮 §1）
- 位置：useGameStore.ts:130-141
- 修法：消费侧 Math.max(0.05, base - rainBoost) 或在 getAbilityValue 统一夹钳

### [P2-3] 兔子幸运兔脚不要求 wasFed（首轮 §1）
- 位置：useAnimalStore.ts:794
- 修法：补 animal.wasFed 条件，与主产出口径一致

### [P2-4] advanceTime passOut 处理由调用方负责，部分路径未补 handleEndDay（首轮 §1）
- 位置：useGameStore.ts:152-176 + 多处调用方
- 修法：把 passOut → endDay 收成 store 内部 side-effect 或集中 watch

### [P2-5] useGameStore.deserialize 不校验枚举（首轮 §1）
- 位置：useGameStore.ts:352-372
- 修法：每个枚举字段做白名单校验，损坏档 fallback 而不是 propagate

### [P2-6] useSkillStore.addExp 不夹钳负向加成（§3）
- 位置：useSkillStore.ts:97-102
- 修法：const adjustedAmount = Math.max(0, Math.floor(amount * Math.max(0, 1 + ringExpBonus)))

### [P2-7] useInventoryStore.addItem 在 “最终丢失” 仍标记成就发现（§3）
- 位置：useInventoryStore.ts:254-307
- 修法：discoverItem 移到 remaining <= 0 时调用

### [P2-8] useFishPondStore.displayBonus 在 bonus=0 时仍给 4 分（§3）
- 位置：useFishPondStore.ts:283
- 修法：去掉 Math.max(4, ...) 的 4，让配置真正可控
```

### 18.4 P3（按系统聚类）

- **编码**：useGameStore / useFarmStore / useFishingStore / useAnimalStore 等多文件存在 UTF-8 + GBK 注释混杂（§1）；useAchievementStore 有乱码 dead code（§5）。
- **农场**：plantCrop / plantGeneticSeed 字段重复（§1）。
- **天气 / 旅运**：dailyLuck 非确定性 RNG（§1）；processHiddenNpcDiscovery 在 advanceTime 中每次扫描（§1）；deserialize midnightWarned 写法歧义（§1）。
- **钓鱼**：legendaryBoost 不夹钳负向（§1）。
- **矿洞 / 战斗**：treasure tile 原地 mutate（§3）。
- **鱼塘**：normalizeMaintenanceState 离散值不校验（§3）。
- **仙缘**：getAbilityValue 不统一夹钳（§3）；blessing 解锁瞬间无声生效（§3）。
- **烹饪**：normalizeActiveBuff value 不夹钳上限（§3）。
- **育种**：breedSameCrop 稳定度不夹下限（§5）。
- **公会 / 商店 / 鱼塘**：getCurrentDay / getAbsoluteDay 各自重写（§5）。
- **玩家**：consumeStamina 非法输入返 true（§8）；dailyReset 恢复百分比不夹下限（§8）。
- **酒窖**：supreme 品质 daysAging 持续累加（§8）；deserialize cellarSlots 不规范化（§8）。
- **商店**：hu_xian_3 仙缘加成用 Math.random（§8）；sellItem 缺数量校验（§16）。
- **NPC**：giftsThisWeek 重置仅靠 day % 7（§12）；生日 + 极品 + 增益叠加 hated gift -320（§12）。
- **博物馆**：donateItem 依赖 feature flag 才有锁（§16）。

### 18.5 P3 修复推荐顺序

1. 统一字符编码（一次性大改，避免后续每个新文件继续踩坑）。
2. `getAbsoluteDay` / `getCurrentDay` / `getCurrentDayTag` 统一到 `weekCycle`。
3. `getAbilityValue` 统一夹钳；同时把消费侧（rainBoost / legendaryBoost / spiritShield）的负向防御补齐。
4. `usePlayerStore` setter 整体审：consumeStamina / dailyReset / refundMoney 三件套，按 “invalid → false + warn / 0 → noop / valid → 执行” 重构。
5. 其余 P3 按各模块自然迭代时附带处理。

## 19. 本份审查的覆盖与边界

### 已审过的 store / 模块

| 模块 | 文件 | 审查深度 |
| --- | --- | --- |
| 时间 / 季节 / 天气 | useGameStore | 全文 |
| 农场 / 多茬作物 | useFarmStore | 主路径 + 收获 |
| 牧场 / 产品 / 死亡 | useAnimalStore | 主路径 + endOfDay 产出 |
| 钓鱼 / 鱼竿 / 鱼饵 | useFishingStore | catch + miniGame 参数 |
| 鱼塘 / 评分 / 维护 | useFishPondStore | rating snapshot + maintenance |
| 矿洞探索 / 宝箱 | useMiningStore | revealTile + treasure |
| 矿洞战斗 runtime | combatRuntime.ts | 全文 |
| 烹饪 / Buff | useCookingStore | normalize + 消费 helpers |
| 加工 / 反序列化 | useProcessingStore | sanitizeProcessingSlot |
| 育种 / 杂交 / 突变 | useBreedingStore | breedSameCrop + 评分 + 认证 |
| 钱包 / Ticket / Wallet | useWalletStore | normalize + summary |
| 仓库 / 堆叠 / 临时 | useInventoryStore | addItem + removeItem |
| 商店 / 旅商 / 出售 | useShopStore | refresh + sell + buy |
| 装饰 | useDecorationStore | grant + beauty score |
| 行旅图 / 远征 | useRegionMapStore | 框架 + 类型 |
| 公会 | useGuildStore | dailyReset + weeklyReset |
| 博物馆 | useMuseumStore | donateItem + 锁 |
| 瀚海赌博 | useHanhaiStore | 全 7 个子游戏（roulette / dice / cup / cricket / cards / texas / buckshot startTexas） |
| 拓边纪 | useFrontierChronicleStore | 类型与上限 |
| 玩家 | usePlayerStore | stamina / hp / money / dailyReset / lifestyleDiscovery |
| NPC / 关系 | useNpcStore | adjustFriendship / talkTo / giveGift / startDating / propose |
| 仙缘 | useHiddenNpcStore | getAbilityValue / getBondBonusByType |
| 技能 | useSkillStore | addExp / blessing / 边界 |
| 任务 / 特殊订单 | useQuestStore | submitQuest 锁 + 重复领奖防御 |
| 目标 / 周结算 | useGoalStore | settleWeeklyGoals 幂等 |
| 家 / 酒窖 / 温室 | useHomeStore | dailyCellarUpdate + deserialize |
| End-of-day pipeline | useEndDay.ts | 入口 + reset 链 |
| View 失败路径 | AuthView / GameLayout / HallView / AnimalView / FarmView | 退款 + try-catch |

### 未审 / 仅扫到表层

- 各 view 内对 store 失败 `addLog` 文案口径是否统一（不属 bug，但 UX 一致性）；
- 各 hidden NPC 数据驱动的对话 / 解锁条件正确性（数据正确性，需要专项剧情 QA）；
- 真机 / 真实存档迁移流程；
- e2e 与 smoke 覆盖率（在 0520全量审查.md 阶段八已点名）。

> 这份 0521玩法bug审查.md 至此六轮覆盖完毕，**已具备发布前修复排期可读性**。可作为 0520全量审查.md 的姊妹篇与玩法侧补充。

## 4. 自动化与人工验证边界

- 本轮所有结论均来自静态文件读取证据；
- 本轮未运行 `npm run type-check / lint / test:e2e / qa:*`（环境限制：`.claude/run-bash.cmd` 与 bash 不兼容）；
- 上述 P1/P2 项均建议在下一轮回归测试中追加专用断言用例。

---

> 本份是 `0521玩法bug审查.md` 的第一轮提交。下一轮会按 “未审到的玩法” 章节继续推进，并在最后一轮做总收口（P0-P3 排序 + 修复顺序）。

## 20. 第七轮新增问题（按 0520 全仓审查计划补齐后端 / 壳层 / 构建验证）

本轮基于 `0520全量审查计划.md` 对前端壳层、后端 API / 联机入口、服务端存档、构建 / 发布 / 验证链路做补充审查；下面只追加**前六轮未覆盖**或**需要从全仓视角重述**的问题，不删除前文已有未修项。

### 20.1 P1

### [P1] 云存档 JSON 损坏会被静默当成空档，后续保存可覆盖真实数据

- 位置：`server/src/taoyuanSaveRuntime.js:62`；`server/src/routes/api.js:4043`
- 触发条件：`data/taoyuan_saves/<username>.json` 因磁盘异常、手工编辑或历史版本写入变成非法 JSON。
- 现象：`loadUserSaveSlots()` 解析失败后直接返回空 3 槽；`/api/taoyuan/save/slots` 会表现为“没有存档”，玩家随后再保存任一槽位时会把原损坏文件整体覆盖掉。
- 影响范围：属于服务端存档的二次破坏风险。原本还可人工恢复的坏档，会在下一次正常保存时被新空基线吃掉。
- 证据：`taoyuanSaveRuntime.loadUserSaveSlots()` 的 `catch` 直接 `return { slots: createEmptySlots() }`，调用方没有区分“确实为空”和“解析失败”。
- 建议修法：改为严格读取；解析失败时抛 500 / `STORE_CORRUPTED`，并阻止后续覆盖写入；必要时把损坏原文件转存为 `.corrupt.<timestamp>` 备份。
- 验证方式：手工写入非法 JSON 后访问 `/api/taoyuan/save/slots`，应返回 500；随后 POST 保存也不应覆盖原坏档文件。

### [P1] 联机发布开关没有覆盖远征房间、邻里、订阅链路

- 位置：`server/src/routes/api.js:1877`；`server/src/routes/api.js:2541-2633`；`server/src/routes/api.js:2649-3178`
- 触发条件：后台关闭 `online release` 的 `social` / 活动房间相关模块或进行灰度收口。
- 现象：节会房间使用了 `createOnlineReleaseGuard('festival')`，但远征房间全套路由没有对应 guard；`neighbors` 与 `subscriptions` 也只做 `loginRequired/signRequired`，未接入 `social` 模块开关。
- 影响范围：联机灰度关闭后，玩家仍可创建 / 加入远征房间或继续写入邻里、订阅关系，造成“配置已关、真实入口未关”的发布面错位。
- 证据：同一文件中 `festival` 路由带 guard，而 `expedition`、`neighbors`、`subscriptions` 段落没有 `createOnlineReleaseGuard(...)`。
- 建议修法：给远征房间补统一模块 guard；给邻里、订阅补 `createOnlineReleaseGuard('social')`；配置层最好显式拆出 `festival` / `expedition` 两个活动模块，避免借壳复用。
- 验证方式：关闭对应模块后访问这些接口，应统一返回 `ONLINE_MODULE_DISABLED` / `ONLINE_FEATURE_NOT_RELEASED`，而不是继续成功写入。

### [P1] 大厅图片隐藏只保护直挂路径，`/api` 图片路径仍可绕过可见性校验

- 位置：`server/src/taoyuanHall.js:721-747`；`server/src/routes/api.js:1585`；`server/src/index.js:274-282`
- 触发条件：图片已被管理员隐藏或拉黑，但客户端继续访问保存时返回的图片 URL。
- 现象：上传图片后返回的是 `/api/taoyuan/hall/uploads/<file>`；该路径由 `api.js` 里的 `express.static` 直接服务，没有 `isUploadedImageVisibleByStoredName()` 检查。真正做可见性校验的是 app 级 `/taoyuan/hall/uploads` 静态挂载，两者不一致。
- 影响范围：管理员已隐藏的大厅图片，仍可能经帖子里的真实 URL 被继续访问，审核结果失效。
- 证据：`saveUploadedImage()` 返回 `/api/...` 路径；`routes/api.js` 的 `/taoyuan/hall/uploads` 静态资源挂载没有 `setHeaders` 可见性判断；`index.js` 中另一个非 `/api` 路径才做了隐藏检查。
- 建议修法：只保留一个统一受控下载入口；或在 `/api/taoyuan/hall/uploads` 同样加可见性中间件，隐藏时不要把文件字节交给 `express.static`。
- 验证方式：上传图片后后台执行 hide，再访问返回的 `/api/taoyuan/hall/uploads/...`，应 404，且不返回图片内容。

### 20.2 P2

### [P2] `/api/me` 瞬时失败会把本地账号上下文清成 guest

- 位置：`taoyuan-main/src/utils/accountStorage.ts:112-125`；消费入口见 `src/main.ts:137`、`src/stores/useRealtimeStore.ts:329-333`、`src/utils/mailboxApi.ts:4-12`
- 触发条件：启动、实时连接或邮箱请求期间，`/api/me` 因网络抖动、后端短暂不可用或 Android API origin 配置异常而 fetch reject。
- 现象：`forceRefreshCurrentAccountContext()` 在 catch 中直接 `clearCurrentAccountContext()`，把当前账号 key 与 CSRF 一起清成 guest。
- 影响范围：账号 scoped localStorage、服务端存档模式、邮箱 / 联机入口都会暂时按游客态判断；用户可能只是网络抖动，却看到“未登录 / 不可用”，甚至读到 guest scope 的本地状态。
- 证据：`accountStorage.ts` 对任意 fetch 异常执行 `return clearCurrentAccountContext()`，没有区分“后端确认未登录”和“网络失败无法确认”。
- 建议修法：仅在 `/api/me` 成功返回未登录或 401/403 时清账号；网络错误时保留 `currentAccountKey`，只标记 CSRF 失效并暴露刷新错误态给 UI。
- 验证方式：mock `/api/me` reject，断言 `getCurrentAccountKey()` 仍保持原账号；网络恢复后可重新拿到 CSRF 并恢复实时连接。

### [P2] 邮箱 Pinia 状态在退出 / 切号后不清理，可能显示上个账号未读角标

- 位置：`taoyuan-main/src/views/GameLayout.vue:35,646-651`；`taoyuan-main/src/stores/useMailboxStore.ts:204-224,408-443`；`taoyuan-main/src/views/MainMenu.vue:709-725`；`taoyuan-main/src/views/AuthView.vue:210-221`
- 触发条件：账号 A 进入游戏加载过邮箱，随后在 SPA 内退出登录或切到账号 B / 游客，不刷新页面再继续游戏。
- 现象：邮箱刷新失败路径被 `.catch(() => {})` 静默吞掉，但 `unreadCount`、`mails`、`detailMap`、`arrivalDigest` 没有 reset，导致界面仍残留账号 A 的邮箱状态。
- 影响范围：属于跨账号状态串读；移动端 hub 角标和邮箱页内容都可能误展示前一账号的数据。
- 证据：登出路径只 reload save/account scope，没有清 `useMailboxStore`；`refreshList()` 失败也没有把旧状态清空。
- 建议修法：给 `useMailboxStore` 增加 `resetForAccountChange()`；在登录成功、登出成功、`saveStore.reloadAccountScopedState()` 后调用；遇到未登录错误时也清理邮箱可见状态。
- 验证方式：A 账号制造未读邮件后，在不刷新的前提下切到游客或 B 账号，断言 hub 角标为 0，邮箱列表为空或显示需登录，不再出现 A 的标题 / 未读数。

### [P2] 新游戏重置没有覆盖行旅图 / 边境纪行 / 日结摘要，旧档进度会串到新档

- 位置：`taoyuan-main/src/composables/useResetGame.ts:34`；`taoyuan-main/src/views/MainMenu.vue:807`；`taoyuan-main/src/stores/useRegionMapStore.ts:1819`；`taoyuan-main/src/stores/useFrontierChronicleStore.ts:243`；`taoyuan-main/src/stores/usePlayerRecordCenterStore.ts:176`
- 触发条件：玩家先加载一个中后期旧档，然后从主菜单“开始旅程”创建新游戏。
- 现象：`handleNewGame()` 调用 `resetAllStoresForNewGame()`，但该函数没有重置 `useRegionMapStore`、`useFrontierChronicleStore`、`usePlayerRecordCenterStore`，也没有调用 `resetLogs()`。
- 影响范围：新档可能继承旧档的行旅图解锁、路线历史、边境见闻、日结摘要未读状态或日志历史，属于明确的玩法状态串档。
- 证据：这些 store 已接入序列化 / 反序列化，但不在新游戏 reset 清单内。
- 建议修法：在 `useResetGame.ts` 中补齐相关 store reset，并统一出一套“新游戏清零协议”。
- 验证方式：加载一个已解锁行旅图且存在日结摘要的存档后新建游戏，断言对应 serialize 结果与默认初始值一致。

### [P2] 村庄建设捐赠数量只挡 `<= 0`，`NaN` 可绕过扣物并污染捐赠进度

- 位置：`taoyuan-main/src/stores/useVillageProjectStore.ts:885`；`taoyuan-main/src/composables/useCombinedInventory.ts:22`
- 触发条件：上游输入框或脚本传入 `amount = NaN` 到 `donateToProject(projectId, itemId, amount)`。
- 现象：`amount <= 0` 对 `NaN` 为 false；库存比较同样被绕过；`removeCombinedItem()` 最终可能不扣任何物品却返回 true，随后 `donatedItems[itemId] + amount` 变成 `NaN`。
- 影响范围：村庄项目进度运行时被污染；`JSON.stringify(NaN)` 还会写成 `null`，再读档时可能出现进度回退或异常。
- 证据：`donateToProject()` 与 `removeCombinedItem()` 入口都缺少 `Number.isFinite` / 整数校验。
- 建议修法：数量入口统一做 `Number.isFinite(amount) && Number.isInteger(amount) && amount > 0`；非法数量直接返回失败，不进入扣物与累计。
- 验证方式：单测传入 `NaN / Infinity / 1.5 / -1`，断言不会扣物、不会污染 `donationStates`。

### [P2] 发布镜像链路不跑类型检查、lint 或 QA，`npm run build` 本身也只跑 Vite

- 位置：`taoyuan-main/package.json:12`；`taoyuan-main/scripts/run-build.mjs:9`；`Dockerfile:21`；`.github/workflows/docker-publish.yml:51`
- 触发条件：推送 `main` 后触发镜像构建与发布。
- 现象：Docker 与 CI 使用的是 `npm run build`，而这个脚本实际只跑 `vite build`；`vue-tsc -b`、`eslint` 与 QA 脚本都没有进入镜像发布门禁。
- 影响范围：TS / lint 回归、仅运行期才暴露的问题，仍可能打进 `latest` 镜像。
- 证据：真正包含类型检查的是 `build:inner`，而发布链路没有调用它。
- 建议修法：Docker 构建阶段改跑 `npm run build:inner`；CI 在 build-push 前显式跑前端 `check` 和后端 smoke / realtime QA。
- 验证方式：人为制造 TS 错误时，镜像发布应失败，而不是仍能产出镜像。

### [P2] Playwright 无法启动时多个 QA / E2E 脚本以退出码 0 “跳过”，容易形成假绿

- 位置：`taoyuan-main/scripts/run-e2e.mjs:54`；`taoyuan-main/scripts/qa-mobile-ui-smoke.mjs:530`；`taoyuan-main/scripts/qa-region-friend-panel-live-smoke.mjs:461`；`taoyuan-main/scripts/qa-online-regression-live-smoke.mjs:490`
- 触发条件：环境报 `browserType.launch: spawn EPERM` 或同类 Chromium 启动失败。
- 现象：脚本捕获后打印 `Skipped` 并 `return` / `process.exit(0)`。
- 影响范围：流水线可能把“浏览器根本没跑起来”记成通过，导致联机 UI、移动端截图、实时联调验证形成假绿。
- 证据：多个脚本都把 Playwright 环境错误映射成 0 退出码。
- 建议修法：默认失败；仅在显式环境变量允许跳过时返回 0，并在 summary 中写入 `skipped: true`。
- 验证方式：模拟 Chromium 启动失败时，CI / 发布 gate 应非 0；显式允许跳过时才返回 0。

### [P2] App Shell 关键守卫异步加载失败时没有 UI 降级或重试

- 位置：`taoyuan-main/src/App.vue:15-16,78-85`；关键实现位于 `src/components/game/AppShellGuards.vue`
- 触发条件：旧 HTML 缓存指向已删除 chunk、弱网导致动态 import 失败、Android WebView 首次加载 chunk 超时。
- 现象：`AppShellGuards` 挂载失败时，Android 强制更新弹窗、返回键拦截、前后台恢复同步等壳层守卫都不会生效；AI widget 失败也没有可见降级。
- 影响范围：页面主界面仍能显示，但关键壳层能力整体缺席，属于“表面可进、底层保护失效”的首屏风险。
- 证据：`defineAsyncComponent(() => import(...))` 未配置 `timeout`、`errorComponent`、`onError`；失败态也没有被 UI 消费。
- 建议修法：`AppShellGuards` 建议改同步导入，或至少配置可重试的异步组件错误边界；AI widget 可继续异步，但应与壳层隔离失败面。
- 验证方式：本地拦截 AppShellGuards chunk 为 404，确认页面出现可理解的降级提示 / 重试，并且不会悄悄丢失返回键与更新门禁。

### 20.3 P3

### [P3] 文件 session 存储损坏 / 并发写会静默清空会话

- 位置：`server/src/index.js:175-225`
- 触发条件：`sessions.json` 写到一半、并发请求 touch/set、或文件本身已损坏。
- 现象：解析失败返回 `{}`，所有用户会被当成未登录；保存时直接 `writeFileSync` 覆盖，没有原子临时文件与写队列。
- 影响范围：更偏运维稳定性，不一定直接损档，但会造成整站 session 集体失效或偶发丢会话。
- 建议修法：复用 `writeJsonFileAtomic()`，并在 session store 内做最小写队列 / 损坏保护。
- 验证方式：构造坏 `sessions.json` 与并发 touch/set，确认不会因为一次坏写把全部 session 清空。

### [P3] `useWalletStore.deserialize` 未规范化 `unlockedItems`

- 位置：`taoyuan-main/src/stores/useWalletStore.ts:164,271,670`
- 触发条件：损坏存档或导入档把 `wallet.unlockedItems` 写成非数组对象，或数组里混入非法 id。
- 现象：反序列化直接 `unlockedItems.value = data?.unlockedItems ?? []`；后续 `includes()`、`unlockedDefs`、`has()` 会直接消费，普通对象会抛 `includes is not a function`。
- 影响范围：钱袋被动效果、商店折扣、采集 / 挖矿 / 钓鱼 / 烹饪加成判断都会被坏档拖垮。
- 证据：相比 rewardTickets / mysteryBoxes 等字段，这里没有做白名单与类型兜底。
- 建议修法：改成 `Array.isArray(...) ? 去重并过滤合法 wallet item id : []`。
- 验证方式：构造 `unlockedItems = {}` 与混入非法 id 的导入档，断言加载不崩且只保留合法值。

### [P3] 邮箱首次刷新会把历史邮件全部当成“刚收到”

- 位置：`taoyuan-main/src/stores/useMailboxStore.ts:243,412-430`；展示入口 `src/views/game/MailView.vue:11-17,552-557`
- 触发条件：账号已有历史邮件，玩家第一次进入游戏 / 邮箱页时 `lastSeenMailIds` 仍为空。
- 现象：首次 `refreshList()` 会把所有旧邮件都视为新增邮件，生成 `arrivalDigest` 并在 UI 上展示成“刚收到 N 封新邮件”。
- 影响范围：是提示语义错误，不会直接损数据，但会持续误导玩家对邮件到达时序的理解。
- 证据：`lastSeenMailIds` 初始为空；首次成功刷新后立即用空集与完整列表做 diff。
- 建议修法：增加 `hasBootstrappedMailboxList` 标记；首刷只建立基线，不生成 arrivalDigest。
- 验证方式：预置 3 封旧邮件后首次进邮箱不出现“刚收到”；随后服务端再新增 1 封邮件，才出现 1 封抵达提醒。

### [P3] README 的 Compose 更新路径与仓库内 `docker-compose.yml` 不一致

- 位置：`README.md:184,203`；`docker-compose.yml:3`
- 触发条件：用户按 README 执行 `docker compose pull && docker compose up -d`，但实际使用仓库自带 compose 文件。
- 现象：README 示例是 `ghcr.io/...` 拉镜像；仓库里的 compose 却是本地 `build:` + `image: taoyuan-duli:latest`。
- 影响范围：更偏文档 / 运维一致性；用户可能以为已经更新到远端最新镜像，实际仍在本地重建或 pull 无效。
- 证据：文档与仓库默认 compose 内容不一致。
- 建议修法：拆分远端镜像版与本地构建版 compose，或在 README 里明确这两条路径不是同一个文件。
- 验证方式：`docker compose config` 输出应与 README 所描述的更新方式一致。

### [P3] `test:e2e` 名称容易被误解为生产端到端验收，但实际主要是 Vite dev + API mock

- 位置：`taoyuan-main/scripts/run-e2e.mjs:65`；`taoyuan-main/e2e/game-smoke.spec.ts:58`
- 触发条件：把 `npm run test:e2e` 当作 Docker / 统一服务发布验收。
- 现象：脚本启动的是 `npm run dev`；spec 里 mock 了 `/api/me`、`/api/public-config`、`/api/taoyuan/ai/config` 与日志接口。
- 影响范围：不会覆盖后端静态挂载、生产缓存、cookie / CORS、容器运行、真实 `/api` 登录态。
- 证据：该脚本更接近“前端开发态 smoke”，不是生产一体化 E2E。
- 建议修法：保留现有脚本但改名 / 改注释，例如 `test:e2e:dev-mock`；另补一个真正启动 build 产物 + 后端服务的 `test:e2e:prod`。
- 验证方式：故意破坏 server 静态挂载时，新的 prod E2E 应失败，而现有 dev mock 仍可独立作为前端冒烟。

## 21. 本轮确认正常的高风险疑点

- `main.ts` 在 mount 前安装了 API fetch bridge，Android `/api` 与 WebSocket 地址走统一改写入口；本轮未发现“先请求后桥接”的时序漏洞。
- `AuthView` 登录 / 注册 / 登出链路均有 try/catch，`res.json()` 也有 `.catch(() => null)`，本轮未发现会直接炸白屏的非 JSON 响应路径。
- `HallView` 帖子详情加载使用 request id 防止旧请求覆盖新请求；`MailView` 详情切换同样用了 `selectRequestId` 防并发回写旧邮件。
- `useRealtimeStore` 对实时通知刷新做了 300ms 合并与 catch，不会因为单个业务刷新失败把整条 WebSocket 消息泵打断。
- `data-defaults` 进入 Docker 镜像后只会在后端启动时补齐缺失 / 空文件，不会覆盖已有持久化数据；`vite build -> docs` 与后端静态挂载目录保持一致。

## 22. 本轮自动化与人工验证边界

- 本轮结论仍以静态文件读取证据为主，未运行 build / lint / type-check / Docker build / e2e / qa 脚本。
- 未运行的原因不是“不需要”，而是当前任务目标优先为**全仓审查回写**；同时当前环境中 `rg.exe` 被拒绝执行，浏览器 / 构建脚本会产生额外产物与环境副作用。
- 因此第七轮的 P1 / P2 仍建议在下一轮修复时各补一条最小回归断言，尤其是：坏档读写保护、联机模块开关、账号上下文抖动、邮箱切号清理、发布门禁与 Playwright 假绿。

## 23. 0523 当前代码复核：文档 bug 修复状态与新增风险

### 23.1 已确认仍未修的 P1 / P2

- [已修][P1] 钓鱼 `junkChance` 已在 `useFishingStore.ts` 统一限制到 `0..1`；坏运 / 戒指 / 鱼饵 / 环境窗口叠加后不会再把垃圾率推到 100% 以上。（0523 本轮修复）
- [未修][P1] 服务端云存档坏 JSON 仍会被静默读成空槽：`loadUserSaveSlots()` 的 `catch` 继续 `return { slots: createEmptySlots() }`，调用方无法区分“确实空档”和“解析失败”。
- [未修][P1] 联机发布开关仍没有覆盖远征房间；`festival` 路由带 `createOnlineReleaseGuard('festival')`，但 `expedition` 全套路由仍只有 `loginRequired/signRequired`。
- [未修][P1] 大厅隐藏图片仍可经 `/api/taoyuan/hall/uploads` 绕过可见性检查；`index.js` 的直挂路径做了 `isUploadedImageVisibleByStoredName()`，`routes/api.js` 的 `/api` 静态挂载没有同等检查。
- [未修][P2] `/api/me` fetch 失败仍会在 `forceRefreshCurrentAccountContext()` 的 `catch` 中调用 `clearCurrentAccountContext()`，网络抖动仍可能把账号上下文清成 guest。
- [未修][P2] 邮箱 Pinia 状态仍缺少切号 / 退出清理入口；`useMailboxStore` 没有 reset 方法，`MainMenu.vue` / `AuthView.vue` 登出后只 reload account scope，不清 `mails/unreadCount/detailMap/arrivalDigest`。
- [未修][P2] 新游戏重置仍漏掉 `useRegionMapStore`、`useFrontierChronicleStore`、`usePlayerRecordCenterStore`；这三者已进入 `useSaveStore` 的 serialize/deserialize，但不在 `resetAllStoresForNewGame()` 清单内。
- [未修][P2] 村庄建设捐赠仍只挡 `amount <= 0`；`NaN` 会绕过 `removeCombinedItem()` 的库存比较并污染 `totalAmount/donatedItems`。
- [未修][P2] Docker / 镜像发布链路仍只跑 `npm run build`，而该脚本实际只执行 Vite build；`vue-tsc`、eslint、QA 脚本仍未进入镜像发布门禁。
- [未修][P2] Playwright 环境启动失败仍被多个脚本映射成成功跳过；`run-e2e.mjs` 明确 `process.exit(0)`，移动端 QA 脚本也在 EPERM 等环境错误时 `return` 后走成功退出。
- [未修][P2] `AppShellGuards` 仍是无 `timeout/errorComponent/onError` 的 `defineAsyncComponent`，关键壳层守卫 chunk 加载失败时没有可见降级或重试。
- [未修][P2] 天气概率仍直接使用 `rainBoost = getAbilityValue(...) / 100` 下调阈值，没有把隐藏 NPC 加成限制到合理区间。
- [未修][P2] 晚归昏倒仍只把 `hour` 改成 `PASSOUT_HOUR` 并返回 `passedOut: true`，没有统一触发日结 / 存档 / 状态恢复链路。
- [未修][P2] `GameStore.deserialize()` 仍直接接收 `season/weather/currentLocation/tomorrowWeather` 等枚举字段，坏档可继续把运行态带入非法值。
- [未修][P2] 兔脚产出仍只检查兔子类型、友好度与生病状态，没有要求当天已喂食。
- [未修][P2] 技能经验加成仍没有对负数 `expGain` 做下限保护，负面装备或坏档仍可能扣经验。
- [未修][P2] 背包满时 `addItem()` 仍先 `discoverItem()` 再判定是否真正放入，图鉴仍可能记录未实际获得的物品。
- [未修][P2] 鱼池观赏投喂仍在配置奖励为 0 时给隐藏 +4 展示值。
- [未修][P2] 退款路径的经济流记录仍不尊重 `countAsEarned:false`：`earnMoney()` 虽然不再计入成就收入，但仍无条件 `recordEconomyFlow('income', ...)`。

### 23.2 已确认仍未修的 P3 / 防御性问题

- [未修][P3] 文件 session store 损坏仍会读成 `{}`，保存仍是直接 `writeFileSync`，没有原子写或坏文件隔离。
- [未修][P3] `WalletStore.deserialize()` 仍直接把 `data?.unlockedItems ?? []` 赋给数组状态，非数组坏档会让后续 `includes()` 崩溃。
- [未修][P3] 邮箱首次 `refreshList()` 仍用空 `lastSeenMailIds` 与完整列表做 diff，历史邮件会被提示成“刚收到”。
- [未修][P3] `consumeStamina()` 对非法、0 或负数消耗仍返回 true。
- [未修][P3] 直接卖店 `sellItem()` 仍不校验有限正整数数量与合法品质，依赖 `removeItem()` 的宽松比较。
- [未修][P3] `FishPondStore.normalizeMaintenanceState()` 仍主要用 `Math.max(0, Number(...) || 0)` 兜底，没有整数化与枚举白名单。

### 23.3 新增发现

### [P2] 出货箱入口信任非法数量，`NaN` / 负数可写入结算状态

- 位置：`taoyuan-main/src/stores/useInventoryStore.ts:359-381`；`taoyuan-main/src/stores/useShopStore.ts:2441-2449,2499-2514`
- 触发条件：调试入口、坏档 UI 状态或脚本调用 `shopStore.addToShippingBox(itemId, NaN, quality)` / `addToShippingBox(itemId, -5, quality)`。
- 现象：`inventoryStore.removeItem()` 只判断 `total < quantity`，对 `NaN` 为 false、对负数也通常为 false；随后 `remaining > 0` 不成立，不会扣任何物品却返回 true。`addToShippingBox()` 继续把该数量累加或 push 进 `shippingBox`。
- 影响范围：日结 `settleShippingBoxWithMarketGuard()` 会遍历污染后的 `shippingBox` 计算价格和记录出货历史；`NaN` 可能让 `totalIncome`、出货历史或序列化数据出现异常，负数可能造成出货统计回退。
- 建议修法：在 `removeItem/removeItemAnywhere/removeItemFromTemp` 与 `addToShippingBox/removeFromShippingBox/sellItem` 的外部入口统一要求 `Number.isFinite(quantity) && Number.isInteger(quantity) && quantity > 0`，非法数量直接返回 false。
- 验证方式：单测覆盖 `NaN / Infinity / -1 / 0 / 1.5`，断言不扣物、不写 `shippingBox`、不影响日结收入和出货历史。

### 23.4 本轮边界

- 本轮为静态复核，未运行 build / type-check / lint / e2e / QA。
- 重点复核了第 20 节 P1/P2、部分高价值 P3，以及与数量校验同源的新增风险；未声明“已修”的项目，均未在当前代码中找到足够修复证据。

## 24. 0523 修复回写：钓鱼垃圾率上限

### [已完成][P1] 钓鱼 `junkChance` 没有上限夹钳

- 改动：`taoyuan-main/src/stores/useFishingStore.ts` 新增概率归一化入口，垃圾率最终统一进入 `0..1` 区间。
- 保护范围：保留原有等级、鱼饵、环境窗口对垃圾率的影响，只阻止极端加成把概率推到 100% 以上或异常值污染判定。
- 用户可见变化：极端坏运 / 环境惩罚叠加时，钓鱼不会再因为概率超过 1 而变成长期必出垃圾。
- 验证：`npm --prefix taoyuan-main run type-check` 通过；`npm --prefix taoyuan-main run build` 通过（仅保留既有 chunk size 警告）。
- 剩余：第 23 节其余 P1/P2 仍未修，下一轮自动切到服务端云存档坏 JSON 保护。
