# 0520 联机输入框 UI 修改计划

## 0. 文档定位

这份计划只处理一个问题：

> 目前联机系统相关输入框看起来太挤、太窄、上下高度也偏扁，影响玩家输入用户名、房间标题、邀请对象、求助内容和联机配置时的体验。

本次按 CCGS 相关流程拆分：

- `ccgs-design / ux-review`：先判断现有联机表单的可读性、可点区域、输入摩擦。
- `ccgs-design / ux-design`：定义统一的联机输入控件体验规格。
- `ccgs-planning / dev-story`：把修改拆成可执行文件范围、步骤和验收标准。

这不是重做联机页面，而是做一次低风险 UI 整理：扩大输入控件尺寸、调整按钮排列、让移动端和窄面板下不再挤压。

## 1. 当前问题

### 1.1 输入框横向太窄

主要原因：

- 多处使用 `flex gap-2`，输入框和按钮在同一行抢宽。
- 有些操作行同时放了 2 个按钮，例如“加好友 / 拉黑”，导致输入框被压缩。
- 部分设置区使用固定双列或三列布局，在中等宽度下每列空间不足。

重点位置：

- `taoyuan-main/src/views/game/SocialView.vue`
  - 好友输入：`friendUsernameDraft`
  - 邻里邀请：`neighborInviteUsernameDraft`
  - 名片设置：庄园名、公开称号、邻里身份、展示主题
- `taoyuan-main/src/views/game/ExpeditionRoomView.vue`
  - 远征房间邀请：`draftInviteUsername`
  - 房间标题：`draftTitle`
- `taoyuan-main/src/views/game/FestivalView.vue`
  - 节会房间邀请：`draftInviteUsername`
  - 房间标题：`draftTitle`
- `taoyuan-main/src/views/game/SocietyView.vue`
  - 村社邀请：`draftInviteUsername`
  - 提案标题、归档备注
- `taoyuan-main/src/views/game/QuestView.vue`
  - 在线求助单创建表单
- `taoyuan-main/src/components/game/AdminOnlineGovernancePanel.vue`
  - 联机发布控制里的内测模板输入框

### 1.2 输入框上下高度太窄

主要原因：

- 高频输入框大量使用 `py-1`、`text-xs`，没有统一 `min-height`。
- 按钮和输入框高度不一致，视觉上像被压扁的后台控件。
- textarea 默认行数偏少，中文长句容易贴边。

影响：

- 手机端触控区域偏小。
- 中文输入时上下留白不足。
- 表单看起来密度过高，和游戏内联机入口的情绪不匹配。

### 1.3 页面节奏太像管理后台

联机系统本身是玩家高频社交入口，不应该让玩家感觉在填后台表格。当前很多地方字段、按钮、说明贴得太近，需要增加表单区的呼吸感。

## 2. 修改目标

### 2.1 控件尺寸目标

联机相关文本输入、下拉框、按钮统一到更舒展的尺寸：

- 普通输入框最小高度：`42px`
- 高频操作输入框建议高度：`44px`
- textarea 最小高度：`96px`
- 输入框横向最小可用宽度：桌面端不低于 `240px`
- 移动端操作行自动换行，输入框占满整行

### 2.2 操作行目标

把“输入 + 按钮”从紧凑挤压改成自适应动作区：

- 桌面端：输入框优先占宽，按钮固定宽度。
- 窄屏端：输入框一行，按钮下一行。
- 多按钮场景：主操作优先，次操作换到下一行或变成次级按钮组。

### 2.3 视觉目标

- label 和输入框之间留出稳定间距。
- 卡片内部 `gap-2` 的高频表单区提升到 `gap-3`。
- 联机输入框不再使用过扁的 `py-1` 作为默认样式。
- 维持现有像素风、小圆角、暗色底和金色描边，不引入新的视觉体系。

## 3. 建议实现方案

### 3.1 先补统一样式

建议在 `taoyuan-main/src/app.css` 增加一组联机表单通用类：

- `.online-field`
- `.online-field-label`
- `.online-input`
- `.online-select`
- `.online-textarea`
- `.online-action-row`
- `.online-action-buttons`

建议行为：

- `.online-input` / `.online-select`
  - `width: 100%`
  - `min-height: 42px`
  - `padding: 0.65rem 0.75rem`
  - `font-size: 0.8125rem`
  - `line-height: 1.45`
- `.online-textarea`
  - `min-height: 96px`
  - `padding: 0.75rem`
  - `line-height: 1.55`
- `.online-action-row`
  - 默认使用自适应 grid 或 flex wrap
  - 输入区 `minmax(240px, 1fr)`
  - 按钮区 `auto`
  - 小屏下自动单列

### 3.2 高频玩家输入先改

第一批优先修改玩家最容易感到难受的位置：

1. `SocialView.vue`
   - 好友输入框加高加宽。
   - “加好友 / 拉黑”从同一行挤压改成输入框优先，按钮组换行或右侧固定。
   - 邻里邀请输入框同样改成统一 action row。

2. `ExpeditionRoomView.vue`
   - 房间标题使用统一 `.online-input`。
   - 邀请玩家操作行改为 `.online-action-row`。
   - 操作区间距从 `space-y-2` 提升到 `space-y-3`。

3. `FestivalView.vue`
   - 和远征房间保持同一套房间表单规格。
   - 邀请输入框与按钮高度对齐。

4. `SocietyView.vue`
   - 村社邀请输入框加高。
   - 提案标题、归档备注改用统一输入高度。

### 3.3 中频复杂表单第二批改

第二批处理字段较多、但不是每分钟都用的表单：

1. `QuestView.vue`
   - 在线求助单创建区从固定 `md:grid-cols-2` 调整为 `grid-template-columns: repeat(auto-fit, minmax(260px, 1fr))`。
   - 求助内容 textarea 默认更高。
   - 多段接力阶段里的目标资源、数量、标题输入统一高度。

2. `SocialView.vue`
   - 名片设置四个输入框改成自适应列宽。
   - 一句公开介绍 textarea 加高。

3. `ManorView.vue`
   - 庄园主题名保存操作行改为统一 action row。

### 3.4 管理端联机配置第三批改

`AdminOnlineGovernancePanel.vue` 的联机发布控制不属于玩家高频路径，但当前三列输入框也偏窄。

建议：

- `md:grid-cols-3` 改成 `lg:grid-cols-3 sm:grid-cols-2` 或 `auto-fit minmax(240px, 1fr)`。
- 三个模板输入框使用同样的高度和 padding。
- release notes textarea 也保持更高的默认高度。

## 4. 推荐修改顺序

### Phase 1：统一样式与高频邀请输入

范围：

- `taoyuan-main/src/app.css`
- `taoyuan-main/src/views/game/SocialView.vue`
- `taoyuan-main/src/views/game/ExpeditionRoomView.vue`
- `taoyuan-main/src/views/game/FestivalView.vue`
- `taoyuan-main/src/views/game/SocietyView.vue`

目标：

- 好友、拉黑、邻里邀请、村社邀请、节会邀请、远征邀请都不再挤压。
- 输入框和按钮高度一致。
- 手机端自动上下排列。

### Phase 2：复杂联机表单舒展化

范围：

- `taoyuan-main/src/views/game/QuestView.vue`
- `taoyuan-main/src/views/game/SocialView.vue`
- `taoyuan-main/src/views/game/ManorView.vue`

目标：

- 求助单、名片设置、庄园主题输入不再像压缩表格。
- textarea 的中文输入体验明显变好。

### Phase 3：管理端和发布配置统一收口

范围：

- `taoyuan-main/src/components/game/AdminOnlineGovernancePanel.vue`

目标：

- 联机发布控制输入框不再在中等屏宽下变窄。
- 管理端和玩家端的输入高度保持基本一致。

## 5. 开发 Story

### Story 1：联机高频操作输入框舒展化

作为玩家，我希望在添加好友、邀请成员、邀请房间玩家时，有足够宽和足够高的输入框，这样我输入用户名或 ID 时不会感觉拥挤。

验收条件：

- 好友输入框在桌面端宽度不低于 240px。
- 手机端“输入 + 操作按钮”自动上下排列。
- 输入框和按钮高度一致，不低于 42px。
- `SocialView`、`ExpeditionRoomView`、`FestivalView`、`SocietyView` 中的邀请/好友操作行样式一致。

### Story 2：联机创建类表单增加输入高度

作为玩家，我希望创建房间、填写求助单、编辑公开名片时输入区更高、更清楚，这样我能稳定输入中文内容。

验收条件：

- 房间标题、求助标题、名片字段使用统一输入高度。
- 求助内容、公开介绍、提案说明 textarea 默认高度不低于 96px。
- 表单字段之间不互相贴边，主要表单区间距提升到更舒展的层级。

### Story 3：联机配置表单响应式修正

作为运营或测试人员，我希望联机发布控制里的模板输入框在中等屏幕也有足够空间，这样配置时不会看起来像被压缩。

验收条件：

- 三个内测模板输入框在中等屏幕不会强行三列挤压。
- 输入框高度和玩家端联机输入框保持同级。
- release notes textarea 不再过矮。

## 6. 验证计划

### 6.1 页面检查

至少检查这些页面：

- `SocialView`
- `ExpeditionRoomView`
- `FestivalView`
- `SocietyView`
- `QuestView`
- `ManorView`
- `AdminOnlineGovernancePanel`

### 6.2 视口检查

至少看三档宽度：

- 桌面宽屏：`1280px`
- 中等宽度：`768px`
- 手机宽度：`390px`

### 6.3 体验检查

检查项：

- 输入框文字没有上下贴边。
- 按钮没有挤压输入框。
- 长中文 placeholder 不导致布局变形。
- 手机端没有横向滚动。
- 同类输入框高度一致。
- 高频操作区一眼能看出主按钮。

## 7. 风险与约束

- 不改接口、不改 store、不改联机业务逻辑。
- 不重构整个页面，只处理输入区和布局密度。
- 不引入新的 UI 库。
- 避免大面积改 Tailwind 体系；优先用少量通用 class 收口。
- 如果某些页面已有特殊视觉节奏，先局部套用，不强行全局覆盖。

## 8. 完成判断

这次修改完成后，联机输入框应该达到：

- 横向不挤。
- 上下不扁。
- 按钮不抢空间。
- 手机端可点区域足够大。
- 玩家输入用户名、ID、房间标题、求助内容时不再像填压缩后台表单。

一句话：

> 联机输入框要从“能用”提升到“愿意反复用”。
