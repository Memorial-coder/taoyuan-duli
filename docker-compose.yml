services:
  taoyuan:
    build:
      context: .
      dockerfile: Dockerfile.repack
    image: taoyuan-duli:latest
    container_name: taoyuan
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${HOST_PORT:-4014}:4013"
    volumes:
      - ./data:/app/data
