# 0520 联机小游戏修改计划

## 0. 文档定位

这份计划只处理一个问题：

> 目前联机系统里的小游戏太像“点几个按钮推进数值”，玩家进房后的体验偏平，缺少协作感、判断感和临场变化。

本次按 CCGS 相关流程拆分：

- `ccgs-design / brainstorm`：先判断现有联机小游戏还能往哪些方向变得更有趣。
- `ccgs-design / ux-review`：先看当前房间玩法的操作节奏和反馈是不是太扁。
- `ccgs-planning / dev-story`：把修改拆成可执行的阶段、文件范围和验收标准。

这不是重做联机系统，而是给现有房间玩法做一次竖切升级：先把一个模板改成“回合事件 + 角色分工 + 风险反馈”，再考虑迁移到其他房间。

## 1. 当前问题

### 1.1 玩法太像按钮面板

当前房间玩法的核心流程是：

- 创建房间
- 邀请 / 加入
- ready
- 倒计时
- 进入运行态
- 点几个动作按钮
- 累积进度和分数
- 结算

问题不在房间底座，而在玩法本体太简单：

- 每个动作基本都是 `progress_delta + score_delta`。
- 玩家之间的差异不够大。
- 没有明显的回合事件。
- 没有风险判断。
- 没有“我这一步会影响别人下一步”的感觉。

### 1.2 角色感太弱

现在的动作模板虽然很多，但本质上仍然是统一按钮集：

- 谁点都差不多。
- 什么时候点都差不多。
- 点完之后只看到进度条涨一点。

这会让联机小游戏看起来像在做轻量表单，不像在共同完成一场活动。

### 1.3 反馈太单一

当前结算主要还是：

- 目标进度
- 评分
- 个人贡献
- 结算凭证

这些都必要，但还不够“有戏”。

缺少的部分是：

- 当前回合发生了什么
- 谁承担了什么职责
- 哪个选择带来了风险
- 哪个配合触发了组合收益
- 为什么这一局会和上一局不一样

## 2. 修改目标

### 2.1 体感目标

把联机小游戏从“点按钮”改成“短回合协作”：

- 每局有明确回合。
- 每回合有一个可读事件。
- 每位成员有职责或角色。
- 动作会影响风险、资源、进度和后续动作。
- 结算结果能回看整局过程。

### 2.2 设计目标

- 保留现有房间状态机，不改 `created / inviting / ready_check / countdown / running / settling / closed` 这套底层。
- 先做一个最小竖切模板，不要一上来改全部小游戏。
- 动作不只加分，还要有选择代价。
- 让玩家能在房间里做一次真正的协作判断。

### 2.3 落地目标

- 先把一个模板改出“像小游戏”的感觉。
- 再抽出共通接口。
- 最后迁移到其他节会模板。

## 3. 建议实现方案

### 3.1 第一阶段先改协作矿洞

优先模板建议用 `expedition_cavern`，原因是它天然适合做分工、风险和回合事件。

这一阶段要补的不是更多按钮，而是：

- 回合号
- 当前事件
- 风险值
- 队伍资源
- 角色分配
- 每回合动作记录

玩法例子：

- 探路：看见风险并降低后续代价。
- 采集：推进目标，但可能抬高风险。
- 支护：降低塌方或失误概率。
- 标记：给后续回合加组合收益。
- 撤离：把局面收稳后提前收尾。

### 3.2 再抽共通动作协议

建议给动作模板补这些字段：

- `required_role`
- `once_per_round`
- `risk_delta`
- `resource_delta`
- `combo_tags`
- `round_effect`

这样后面同一套模板可以复用到：

- 补给接力
- 护送抵运
- 节会抢答
- 节奏表演
- 组队采集

### 3.3 前端从按钮列表改成事件面板

`ExpeditionRoomView.vue` 不再只展示动作按钮，而是改成：

- 当前事件卡
- 队伍状态板
- 角色提示
- 回合动作区
- 回合日志
- 最近一次行动反馈

这样玩家一眼就知道：

- 现在发生了什么
- 自己该干什么
- 这一步为什么重要

## 4. 推荐修改顺序

### Phase 1：做一个样板间

范围：

- `server/src/taoyuanActivityRoomRuntime.js`
- `taoyuan-main/src/utils/expeditionRoomApi.ts`
- `taoyuan-main/src/stores/useExpeditionRoomStore.ts`
- `taoyuan-main/src/views/game/ExpeditionRoomView.vue`

目标：

- 把一个远征模板改成回合事件型玩法。
- 让玩家不是只点按钮，而是在局面里做选择。
- 保留现有房间创建、邀请、准备、结算、关闭链路。

### Phase 2：抽共通字段

范围：

- `server/src/taoyuanActivityRoomRuntime.js`
- 对应的 TS 类型和 API 定义

目标：

- 把事件、角色、风险、回合动作抽成共通结构。
- 让其他模板能复用，不再重写一套玩法数据。

### Phase 3：迁移节会模板

范围：

- `FestivalView.vue`
- `useFestivalRoomStore.ts`
- `festivalRoomApi.ts`

目标：

- 把守岁、灯会、赛舟、赏月这些模板也接进回合事件系统。
- 让节会房间从“按钮房间”变成“活动房间”。

## 5. 开发 Story

### Story 1：远征样板间变成回合玩法

作为玩家，我希望在远征房间里看到当前事件、风险和职责分工，而不是只看到几个按钮，这样我能更像是在和队友共同处理一局任务。

验收条件：

- 房间里有明确的回合显示。
- 每回合至少有一个事件提示。
- 动作会影响进度之外的内容，例如风险或资源。
- 玩家之间的选择会互相影响。

### Story 2：联机动作支持角色和回合约束

作为房间成员，我希望有些动作和角色、回合状态绑定，这样我需要先判断局面，再决定怎么出手。

验收条件：

- 动作可以要求特定角色。
- 动作可以限制每回合使用次数。
- 部分动作能触发组合收益或风险变化。

### Story 3：联机房间从按钮面板改成事件面板

作为玩家，我希望房间里能直观看到当前局势和回合日志，这样我能理解为什么这局和上一局不一样。

验收条件：

- 页面能展示当前事件。
- 页面能展示队伍状态和最近行动。
- 按钮区不再是唯一的信息来源。

## 6. 验证计划

### 6.1 功能验证

至少确认这些点：

- 房间仍可正常创建、邀请、加入、准备、倒计时、结算、关闭。
- 样板间能跑出至少 3 轮有差异的回合。
- 玩家动作不再只是单纯加数值。
- 结算凭证仍然能正常写回个人存档。

### 6.2 体验验证

重点看这几件事：

- 玩家是否能说出自己这一轮在做什么。
- 队友之间是否会商量。
- 失败或风险是否会被看见。
- 局面变化是否足够清楚。

## 7. 风险控制

- 不要一次改全部模板。
- 不要把规则全堆到前端。
- 不要让回合变成纯随机。
- 不要破坏现有房间状态机和发奖链。
- 不要为了复杂而复杂，样板间先能玩起来最重要。

## 8. 结论

这次改造的重点不是“再加几个按钮”，而是把联机小游戏从数值推进，改成有回合、有判断、有协作的活动。

先把一个模板做活，后面再谈扩展，会稳很多。
