# 0531 联机页面优化拆解 TODO

日期：2026-05-31  
来源：`0531优化.md`  
范围：在线中心、节会 / 远征房间、共同庄园、邻里、在线委托、村社、在线庄园、在线可视化组件、QA 脚本、玩家态文案。  
目标：把当前联机各页面从「功能验证大页」拆成可读、可操作、可验证的正式联机体验，并形成可直接开工的任务清单。

## 0. 使用说明

状态标记：

- `[ ]` 未开始。
- `[~]` 进行中。
- `[x]` 已完成。
- `[!]` 阻塞，需要补条件或决策。

优先级：

- `P0`：必须先做，解决房主流程、开发态文案、移动端主操作不可达等核心问题。
- `P1`：第二批统一体验，覆盖其他联机子页和信息架构。
- `P2`：视觉、动效、截图回归、长期维护优化。

每条任务字段：

- `文件`：主要涉及文件，不代表唯一文件。
- `依赖`：必须先完成的任务。
- `执行`：开发动作。
- `验收`：完成判定。
- `验证`：建议命令、E2E、人工检查或截图证据。

## 0.1 AI 执行总协议

本文件不是只给人看的计划，而是给 AI 智能体逐条执行的运行手册。执行时必须先读本章节，再选择任务。

执行目标：

- 把联机页面从「所有功能平铺在一个大页」改为「主路径清晰、次级信息折叠、高风险动作有确认、移动端可触达」。
- 优先修复房主开房间、邀请、准备、倒计时、结算、关闭这些 P0 主路径。
- 优先清理玩家会直接看到的开发态语言。
- 所有改动必须保留原 store action、接口语义和可回归测试能力。
- 不能只做视觉摆放，必须同步补验证、文案扫描或 E2E 保护。

执行硬规则：

- 每次只领取一个最小可完成任务，除非多个任务必须同一补丁完成。
- 开工前必须检查任务涉及文件的现状，不允许只按本文猜测代码结构。
- 不允许删除旧入口、旧 test id 或旧 QA 断言，除非任务明确要求且新断言已补齐。
- 不允许把开发态信息直接删掉；技术凭证、审计、回滚、调试入口应迁入 `OnlineTechnicalDetails` 或等价折叠区。
- 不允许改后端状态机语义；前端只改变交互承载方式和展示层级。
- 不允许为了通过扫描把必要技术字段改坏；技术字段可以留在 allowlist、测试数据、接口类型、技术详情组件中。
- 不允许在没有验证证据的情况下把任务标成 `[x]`。
- 如果环境导致验证无法运行，必须写明命令、失败原因、已完成的替代检查，任务状态不能直接标 `[x]`。
- 如果发现当前代码已经实现任务要求，仍需补证据并回填，不要重复实现。
- 如果任务描述与现有代码冲突，以当前代码为准，保留旧行为并在证据中说明映射关系。

任务生命周期：

- `读取`：读本文件、目标任务、依赖任务、涉及文件、相关 store、相关 QA 脚本。
- `确认`：判断依赖是否完成，确认任务是否仍未开始或可继续。
- `标记`：开始实际修改前把任务从 `[ ]` 改为 `[~]`。
- `实现`：按任务的 `执行` 字段做最小必要改动。
- `自检`：检查 UI 路径、移动端影响、文案影响、旧 test id 影响、可访问性影响。
- `验证`：至少运行任务指定验证；如果过重，先跑目标脚本，再记录未跑的全量命令。
- `回填`：把证据写回任务下方，包含改动、验证、截图或人工检查、风险。
- `关闭`：只有验收和验证都有证据时，才把 `[~]` 改成 `[x]`。
- `汇报`：输出本次完成任务 ID、改动文件、验证结果、未完成风险、建议下一任务。

## 0.2 AI 自动选任务规则

默认选择算法：

1. 扫描所有 `### OUI-*` 任务。
2. 排除 `[x]` 已完成任务。
3. 排除 `[!]` 阻塞任务，除非本轮目标就是解除该阻塞。
4. 优先选择 `P0`，再选择 `P1`，最后选择 `P2`。
5. 同优先级内按阶段顺序选择：M0 → M1 → M2 → M3 → M4 → M5 → M6。
6. 同阶段内优先选择依赖已完成的任务。
7. 同阶段内优先选择组件和 QA 基础任务，再接业务页面。
8. 如果目标任务依赖未完成，先执行最近的未完成依赖。
9. 如果多个任务都可执行，选择能在一次补丁内完成且风险最小的任务。
10. 如果用户指定任务 ID，则执行用户指定任务，并先核对依赖。

推荐 AI 初始执行顺序：

| 顺序 | 任务 | 原因 | 完成后解锁 |
| --- | --- | --- | --- |
| 1 | OUI-000 | 建立测试基线，避免把历史失败当新增失败 | OUI-001、OUI-002 |
| 2 | OUI-001 | 保护旧 E2E 和 test id，不让 UI 重构断测试 | OUI-003、OUI-CMP-001 |
| 3 | OUI-002 | 建立玩家文案扫描口径 | OUI-COPY-001、OUI-QA-002 |
| 4 | OUI-CMP-001 | 所有弹窗和确认流的底座 | OUI-CMP-002、OUI-CMP-006 |
| 5 | OUI-CMP-002 | 弹窗键盘和读屏基础 | OUI-QA-005 |
| 6 | OUI-CMP-003 | 移动端抽屉基础 | OUI-CMP-004、OUI-FLOW-001 |
| 7 | OUI-CMP-004 | 移动端主行动固定能力 | OUI-CENTER-001、OUI-RESP-001 |
| 8 | OUI-CMP-005 | 技术信息折叠能力 | OUI-COPY-001、OUI-CENTER-002 |
| 9 | OUI-CMP-006 | 高风险确认能力 | OUI-FLOW-007、OUI-FLOW-008 |
| 10 | OUI-FLOW-001 | 房间向导基础 | OUI-FLOW-002、OUI-FLOW-003 |
| 11 | OUI-FLOW-004 | 邀请流程基础 | OUI-FEST-002、OUI-SOCIETY-002 |
| 12 | OUI-FLOW-005 | 准备大厅基础 | OUI-FEST-003、OUI-EXP-001 |
| 13 | OUI-FEST-001 | 节会创建弹窗化 | OUI-FEST-002 |
| 14 | OUI-FEST-002 | 节会邀请弹窗化 | OUI-FEST-003 |
| 15 | OUI-FEST-003 | 节会准备大厅 | OUI-FEST-004 |
| 16 | OUI-FEST-004 | 节会结算与关闭确认 | OUI-FEST-005 |
| 17 | OUI-FEST-006 | 远征标签复用房间流程 | OUI-EXP-001 |
| 18 | OUI-CENTER-001 | 在线中心首屏行动区 | OUI-CENTER-002 |
| 19 | OUI-COPY-001 | 第一批玩家文案替换 | OUI-QA-002 |
| 20 | OUI-COHAB-001 | 共同庄园标签降噪 | OUI-COHAB-002 |

跳过规则：

- 依赖未完成时跳过，转去做依赖。
- 任务需要设计决策但本文没有足够信息时，标 `[!]` 并写明缺口。
- 当前环境缺少依赖包时，不跳过实现，但验证结果必须说明未能运行的命令。
- 如果任务牵涉后端协议变化，先暂停该任务，改选纯前端承载任务。
- 如果同一文件已有用户未提交改动，先读懂差异，避免覆盖；冲突不可安全合并时暂停并说明。

## 0.3 单任务执行清单

每个 AI 执行任务时按以下清单操作。

开工前：

- 确认任务 ID、优先级、阶段、状态、依赖。
- 读取任务的所有 `文件`。
- 搜索相关组件、store action、test id、文案、QA 脚本。
- 查 `git status`，确认目标文件是否已有未提交改动。
- 如果要新增组件，先查看同目录现有组件命名、props、emit、CSS 约定。
- 如果要改页面，先定位主入口、状态来源、提交 action、错误反馈、移动端布局。
- 如果要改 QA，先运行或阅读现有脚本输出格式。

实现中：

- 优先复用现有 `game-panel`、`game-modal-overlay`、按钮类、store action、路由 query。
- 新组件 props 必须稳定、命名清楚、便于多个页面复用。
- 新交互必须有 `data-testid`，名称使用 `online-*` 前缀。
- 移动端优先保证主行动在 360x780 和 390x844 可见。
- 桌面端避免把全部卡片铺成瀑布，优先使用列表、主舞台、右侧状态或弹窗。
- 高风险动作必须展示影响对象、资产变化、失败恢复方式。
- 失败态必须保留用户输入或草稿。
- loading 态必须防止重复提交。
- 文案必须使用玩家能理解的语言，技术词进入技术详情。
- 增删旧 DOM 结构前，先确认 E2E 和 QA 是否依赖该结构。

收尾前：

- 运行任务指定验证命令。
- 检查是否引入未使用 import、类型错误、格式问题。
- 检查玩家主路径是否出现 denylist 词。
- 检查移动端是否出现底部按钮遮挡。
- 检查桌面端是否仍然大面积平铺。
- 检查旧 test id 是否仍能被测试定位，或已补迁移说明。
- 把证据写回任务下方。

## 0.4 任务元数据规范

现有任务已经用自然语言列出字段。AI 读取时按以下映射解析。

| 字段 | 来源 | 说明 |
| --- | --- | --- |
| `id` | 标题里的 `OUI-*` | 全局唯一任务 ID |
| `title` | 标题中 ID 后面的文字 | 任务名称 |
| `status` | 第一条 checkbox | `[ ]`、`[~]`、`[x]`、`[!]` |
| `priority` | checkbox 后的 `P0/P1/P2` | 执行优先级 |
| `phase` | 最近上级章节 | M0-M6 |
| `files` | `文件` 字段 | 主要文件，不是唯一文件 |
| `dependencies` | `依赖` 字段 | 必须先完成的任务 |
| `actions` | 所有 `执行` 字段 | 实现步骤 |
| `acceptance` | `验收` 字段 | 完成判定 |
| `verification` | `验证` 字段 | 最低验证要求 |
| `evidence` | `证据` 字段 | 执行后补充 |
| `risks` | `风险` 字段 | 执行后补充 |

新增任务必须使用此格式：

```markdown
### OUI-AREA-000 任务名称

- [ ] `P0` 文件：`path/to/file.vue`、`path/to/script.mjs`
- 依赖：OUI-XXX-000。
- 输入：已有组件、store action、路由 query 或测试脚本。
- 不改：后端状态机、接口字段、旧 test id。
- 执行：第一步。
- 执行：第二步。
- 验收：可观察完成条件。
- 验证：`npm run xxx` 或人工截图要求。
- 证据：未执行。
- 风险：未评估。
```

执行后回填格式：

```markdown
- 证据（YYYY-MM-DD，AI）：改动了 `file-a`、`file-b`；新增 / 保留的 `data-testid` 为 `online-example`；验证 `npm run check` 通过。
- 证据（YYYY-MM-DD，AI）：移动端 390x844 人工检查通过，主按钮在首屏可见；截图路径：`path/to/screenshot.png`。
- 风险（YYYY-MM-DD，AI）：`npm run test:e2e` 未运行，原因是本次只改静态组件；建议 PR 收尾运行。
```

状态变更规则：

- 开始改文件时，把任务状态从 `[ ]` 改为 `[~]`。
- 任务因依赖、设计缺口、环境缺口无法继续时，把状态改为 `[!]` 并写 `阻塞`。
- 任务实现完成但验证未完成时，保持 `[~]`，写明剩余验证。
- 任务实现和最低验证都完成时，把 `[~]` 改为 `[x]`。
- 如果只做了部分子项，保持 `[~]`，不要因为某个子项完成就关闭整个任务。

## 0.5 验证分级规则

每个任务至少要有一个目标验证。PR 收尾必须提升到组合验证。

目标验证：

- 组件任务：`npm run check`，加 `npm run qa:online-ui-structure` 或组件引用检查。
- 页面交互任务：目标 E2E、移动端 smoke、人工流程截图三选一，优先自动化。
- 文案任务：`npm run qa:online-player-copy`。
- 移动端任务：`npm run qa:mobile-ui-smoke` 或 360x780、390x844 截图。
- QA 脚本任务：运行被修改的脚本并记录输出。
- 服务端相关任务：以 `server/package.json` 中实际存在脚本为准，不臆造命令。

PR 级验证：

- PR-A：`npm run check`、`npm run qa:online-ui-structure`。
- PR-B：`npm run check`、`npm run qa:online-ui-structure`、房主创建 / 邀请 / 准备 / 结算 / 关闭 E2E。
- PR-C：`npm run check`、`npm run qa:online-player-copy`、`npm run qa:online-visual-feature-flags`、在线中心移动端 smoke。
- PR-D：`npm run check`、共同庄园移动端截图、家族节会确认人工或 E2E、文案扫描。
- PR-E：`npm run check`、被改页面的移动端 smoke、关键 E2E 迁移。
- PR-F：`npm run check`、`npm run qa:mobile-ui-smoke`、`npm run test:e2e`、截图回归。

失败分类：

- `新增失败`：本次改动导致，必须修复后才能关闭任务。
- `历史失败`：OUI-000 基线已记录，本次未扩大影响，可在证据中引用。
- `环境失败`：依赖、浏览器、服务未启动、网络或权限导致，记录命令和错误摘要。
- `脚本缺失`：命令在 `package.json` 不存在，记录实际脚本并更新任务验证建议。
- `需求冲突`：任务要求与代码真实状态冲突，标 `[!]` 并写清需要决策的点。

## 0.6 AI 输出报告格式

每轮执行结束必须用以下格式汇报，便于另一个 AI 继续。

```markdown
完成任务：OUI-XXX-000
状态变化：`[ ]` → `[x]`
改动文件：
- `path/to/file.vue`
- `path/to/script.mjs`
验证结果：
- 通过：`npm run check`
- 未运行：`npm run test:e2e`，原因：本次只改静态脚本，建议 PR 收尾执行
保留风险：
- 需要在真实 390x844 设备上复核安全区
建议下一任务：OUI-XXX-001
```

如果任务阻塞，使用以下格式：

```markdown
阻塞任务：OUI-XXX-000
状态变化：`[ ]` → `[!]`
阻塞原因：缺少某 store action 或需求与现有接口冲突
已确认信息：
- 读过 `path/to/file.vue`
- 读过 `path/to/store.ts`
可继续任务：OUI-YYY-000
需要人工决策：是否允许新增接口字段
```

## 0.7 AI 提示词模板

执行单个指定任务：

```text
读取 `0531拆解todo.md` 的 0.x AI 执行协议，并执行任务 OUI-XXX-000。
开工前检查依赖和涉及文件。
实现时保留旧 store action、旧 test id 和原接口语义。
完成后运行任务要求的最低验证，把证据和风险回填到该任务下方。
最后汇报改动文件、验证结果、剩余风险和建议下一任务。
```

继续下一个可执行任务：

```text
读取 `0531拆解todo.md` 的 0.x AI 执行协议。
按 0.2 自动选任务规则选择下一个未完成且依赖已满足的任务。
只执行一个最小任务。
执行后把任务状态、证据、风险写回 `0531拆解todo.md`。
不要跳过验证；验证无法运行时写明原因。
```

只做验收复核：

```text
读取 `0531拆解todo.md` 中任务 OUI-XXX-000。
检查该任务的实现文件、验收条件和验证证据是否一致。
如果证据不足，保持或改回 `[~]`，补充缺失验证清单。
如果证据充分，把任务标为 `[x]` 并补一行验收证据。
```

处理失败验证：

```text
读取最近一次验证失败输出，判断属于新增失败、历史失败、环境失败、脚本缺失还是需求冲突。
如果是新增失败，修复实现并重跑目标验证。
如果不是本次新增，记录证据并保持任务状态不超过 `[~]`，除非已有替代验证足够覆盖验收。
```

执行 PR 收尾：

```text
读取 `0531拆解todo.md` 的 PR 拆分建议。
只处理指定 PR 范围内的任务。
确认所有任务都有证据和风险记录。
运行该 PR 对应的验证命令。
输出可复制到 PR 描述的完成摘要、验证结果和已知问题。
```

## 0.8 UI 改造判定标准

AI 判断一个页面是否仍然「平铺」时使用以下标准。

需要改造：

- 首屏出现多个长表单，且没有明确主行动。
- 首屏同时出现玩家操作、技术凭证、审计日志、调试按钮。
- 房主流程按钮分散在多个卡片中，玩家需要滚动寻找下一步。
- 移动端主按钮不在首屏或被底部安全区遮挡。
- 高风险动作是裸按钮，没有确认和影响说明。
- 列表卡片展示过多字段，一屏只能看到 1 张或更少。
- 桌面端大网格同时铺出 5 个以上模块，主舞台不突出。

改造完成：

- 首屏只有 1 个主行动，最多 2 个次行动。
- 长表单进入弹窗、向导或抽屉。
- 房主流程进入大厅或向导，不散落页面。
- 技术信息默认折叠，但 QA 能展开定位。
- 移动端底部主行动可点击且不遮挡内容。
- 桌面端有清晰的主区和辅助区，不把所有信息等权展示。
- 高风险动作确认弹窗包含影响对象、资产变化、失败恢复方式。

## 0.9 文案替换准则

玩家主路径禁止直接出现以下词：

- `fallback`
- `hash`
- `idempotency`
- `record-only`
- `ledger`
- `revision`
- `mock`
- `receipt`
- `per-member receipt`
- `开始 ready`
- `模拟断线`
- `降级入口`
- `服务端落账`
- `服务端状态冲突`

推荐替换：

| 开发态词 | 玩家态文案 | 技术详情文案 |
| --- | --- | --- |
| `开始 ready` | 开始准备 | ready 状态检查 |
| `fallback` | 备用操作 | fallback path |
| `降级入口` | 备用操作 | degrade entry |
| `hash` | 凭证校验码 | receipt hash |
| `ledger` | 记录明细 | ledger record |
| `revision` | 版本记录 | state revision |
| `receipt` | 结算记录 | receipt payload |
| `per-member receipt` | 成员结算记录 | per-member receipt |
| `record-only` | 仅记录本次结果 | record-only mode |
| `idempotency` | 重复提交保护 | idempotency key |
| `模拟断线` | 网络异常测试 | disconnect simulation |
| `服务端落账` | 奖励已记录 | server ledger write |
| `服务端状态冲突` | 房间信息有更新，请刷新后继续 | server state conflict |

文案处理规则：

- 玩家路径说结果、影响和下一步，不说实现细节。
- 技术详情可以保留英文协议词，但必须默认折叠。
- 按钮文案必须说明动作，例如「创建节会房间」「邀请玩家」「开始准备」。
- 错误文案必须提供恢复动作，例如刷新、重试、返回大厅、查看备用操作。
- 不要把无法解释的错误统一写成「失败」，要说明玩家可做什么。

## 0.10 文件改动边界

允许改动：

- `taoyuan-main/src/components/game/online/*`
- `taoyuan-main/src/views/game/OnlineView.vue`
- `taoyuan-main/src/views/game/ExpeditionRoomView.vue`
- `taoyuan-main/src/views/game/online/*`
- `taoyuan-main/src/app.css`
- `taoyuan-main/scripts/qa-*.mjs`
- `taoyuan-main/e2e/game-smoke.spec.ts`
- `taoyuan-main/package.json` 中新增必要脚本
- 本文件的任务状态、证据、风险

默认不改：

- 后端数据库结构。
- 后端房间状态机。
- API 字段语义。
- 存档格式。
- 与在线页面无关的玩法系统。
- 大范围重命名。
- 大范围格式化。

需要暂停确认：

- 需要新增或删除 API 字段。
- 需要改变房间状态枚举。
- 需要删除旧 UI 入口且没有备用路径。
- 需要删除或重命名大量 test id。
- 需要改动付费、资产扣除、奖励结算语义。
- 需要迁移大量文件导致 PR 难以审查。

## 0.11 并行执行规则

可以并行：

- OUI-CMP-007、OUI-CMP-008 与房间主流程组件。
- P1 其他在线页在 P0 组件稳定后分页面并行。
- QA 脚本增强和页面接入可以在 test id 策略确认后并行。
- 文案替换和技术详情收口可以按页面并行。

不建议并行：

- OUI-CMP-001 与依赖它的确认弹窗任务。
- OUI-FLOW-001 与节会 / 远征向导接入。
- OUI-FLOW-005 与节会准备大厅接入。
- OUI-COHAB-001 与共同庄园子组件大拆分。
- 同一 `.vue` 大文件上的多个结构性重构任务。

冲突处理：

- 同一文件出现并行改动时，先完成基础组件，再接页面。
- 页面接入和文案替换冲突时，先完成结构接入，再做文案扫描。
- QA 断言和 DOM 改造冲突时，先保留旧 test id，再新增新 test id。
- 如果两个任务都要改同一按钮行为，以房间状态矩阵和高风险确认规则为准。

## 0.12 最小可关闭单元

AI 不应把整个阶段当作一次任务。推荐最小可关闭单元如下。

组件类：

- 一个通用组件加最小样式和导出。
- 一个组件的一组可访问性修复。
- 一个组件对应的结构 QA 断言。

页面类：

- 一个页面的一个主流程，例如创建房间。
- 一个页面的一类高风险确认，例如结算和关闭。
- 一个页面的移动端主行动栏。
- 一个页面的一批玩家文案替换。

QA 类：

- 一个脚本的新增规则。
- 一个 E2E 场景。
- 一个移动端 smoke 场景。
- 一个文案 allowlist 调整。

文档类：

- 一个任务的证据回填。
- 一个旧 test id 到新 test id 对照表。
- 一个阻塞项说明。

## 0.13 AI 读文件顺序

通用组件任务：

1. `taoyuan-main/src/components/game/online/OnlineModuleShell.vue`
2. `taoyuan-main/src/components/game/online/OnlineModuleCard.vue`
3. `taoyuan-main/src/app.css`
4. 同目录已有 modal、panel、visual 组件
5. `taoyuan-main/scripts/qa-online-ui-structure.mjs`

房间流程任务：

1. `taoyuan-main/src/views/game/online/OnlineFestivalView.vue`
2. `taoyuan-main/src/views/game/ExpeditionRoomView.vue`
3. `taoyuan-main/src/stores/useFestivalRoomStore.ts`
4. `taoyuan-main/src/stores/useExpeditionRoomStore.ts`
5. `taoyuan-main/e2e/game-smoke.spec.ts`
6. `taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`

在线中心任务：

1. `taoyuan-main/src/views/game/OnlineView.vue`
2. `taoyuan-main/src/components/game/online/OnlineModuleCard.vue`
3. `taoyuan-main/src/components/game/online/OnlineModuleShell.vue`
4. `taoyuan-main/scripts/qa-online-visual-feature-flags.mjs`
5. `taoyuan-main/scripts/qa-online-player-copy.mjs`

共同庄园任务：

1. `taoyuan-main/src/views/game/online/OnlineCohabitationView.vue`
2. `taoyuan-main/src/components/game/online/*`
3. `taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`
4. `taoyuan-main/e2e/game-smoke.spec.ts`

其他在线页任务：

1. 对应 `taoyuan-main/src/views/game/online/Online*.vue`
2. 可复用通用组件
3. 对应 store 或 composable
4. 对应 QA 脚本和 E2E

## 1. 总体完成定义

- [ ] `P0` 主路径完成：房主创建节会房、创建远征房、邀请、准备、倒计时、结算、关闭都由弹窗 / 抽屉 / 大厅承接。
- [ ] `P0` 移动端完成：360x780、390x844 下在线中心、节会房、远征房首屏有状态摘要和主行动。
- [ ] `P0` 文案完成：玩家主路径不出现 `fallback`、`hash`、`idempotency`、`record-only`、`ledger`、`revision`、`开始 ready`、`模拟断线`、`降级入口`。
- [ ] `P0` 技术详情完成：服务端凭证、降级、审计、回滚、调试入口默认折叠，不干扰玩家主路径。
- [ ] `P0` QA 完成：静态结构检查、移动端 smoke、关键 E2E 覆盖新的房主流程。
- [ ] `P1` 页面统一完成：邻里、委托、村社、庄园、共同庄园都不再把长表单和日志直接铺满首屏。
- [ ] `P2` 回归完成：截图证据覆盖移动端、桌面端和关键高风险弹窗。

## 2. 阶段切分

| 阶段 | 目标 | 主要交付 | 建议顺序 |
| --- | --- | --- | --- |
| M0 | 基线与保护 | 当前测试基线、文案 allowlist、旧 test id 迁移策略 | 第 0 天 |
| M1 | 通用组件 | Dialog、BottomSheet、StickyActionBar、TechnicalDetails | 第 1-2 天 |
| M2 | 房间核心流 | RoomWizard、InvitePanel、LobbyDialog、ConfirmAction | 第 3-5 天 |
| M3 | 在线中心与文案 | 行动首页、治理折叠、第一批文案替换 | 第 6-7 天 |
| M4 | 共同庄园 P0 | 标签分组、高风险确认、审计拆层 | 第 8-10 天 |
| M5 | 其他在线页 | 邻里、委托、村社、庄园弹窗化 | 第 11-14 天 |
| M6 | QA 与打磨 | 静态检查、E2E、移动端 smoke、截图回归 | 第 15-17 天 |

## 3. M0 基线与保护

### OUI-000 当前基线确认

- [x] `P0` 文件：`taoyuan-main/package.json`、`taoyuan-main/scripts/*`、`server/package.json`
- 依赖：无。
- 执行：确认当前可用命令和现有在线 QA 覆盖范围。
- 执行：记录 `npm run qa:online-ui-structure`、`npm run qa:online-visual-feature-flags`、`npm run qa:mobile-ui-smoke`、`npm run check` 的当前结果。
- 验收：有一段基线记录，明确哪些失败是历史问题，哪些是本次新增问题。
- 验证：命令输出或人工记录写入 PR 描述。
- 证据（2026-06-02，Agent A）：已运行 `npm --prefix taoyuan-main run qa:online-ui-structure`，当前失败为既有非房间主流程基线：共同庄园入口移动端 smoke、在线庄园护理 / 轻采断言、公共订单 / 村社仓廪 / 花灯墙移动端 smoke、`VisualTrackBoard` 多队名次断言等缺失；本轮新增的 `OnlineActionDialog` 断言未出现在失败列表。
- 证据（2026-06-02，Agent A）：已运行 `npm --prefix taoyuan-main run qa:online-visual-feature-flags`，结果通过。
- 证据（2026-06-02，Agent A）：已运行 `npm --prefix taoyuan-main run qa:mobile-ui-smoke`，当前被 `src/utils/cohabitationApi.ts:2430 Unexpected "{"` 阻断，随后 `game-layout` 未出现；归类为既有共同庄园语法 / 构建基线问题。
- 证据（2026-06-02，Agent A）：已运行 `npm --prefix taoyuan-main run check`，`vue-tsc` 当前被 `src/utils/cohabitationApi.ts` 与 `OnlineCohabitationView.vue` 多处语法错误阻断；这些文件属于 Agent B 范围，本轮未改。
- 风险（2026-06-02，Agent A）：后续 Agent A 的组件和房间页验证可引用此基线，但在 Agent B 修复共同庄园语法前，`check`、移动端 smoke 和全量结构 QA 仍不能作为新增失败判断的唯一依据。

### OUI-001 旧 test id 迁移策略

- [x] `P0` 文件：`taoyuan-main/e2e/game-smoke.spec.ts`、`taoyuan-main/scripts/qa-online-ui-structure.mjs`
- 依赖：OUI-000。
- 执行：列出节会 / 远征房间当前关键 test id，包括创建、邀请、ready、倒计时、结算、关闭。
- 执行：确定第一轮保留旧 test id，按钮可移动到弹窗内。
- 执行：给新弹窗补充新 test id，但不要立即删除旧断言。
- 验收：`0531拆解todo.md` 或 PR 描述中有旧到新 test id 对照表。
- 验证：E2E 仍能定位关键按钮。
- 证据（2026-06-02，Agent A）：旧 test id 第一轮保留策略已确认。节会房旧 ID：`online-festival-room-create-submit`、`online-festival-room-invite-submit`、`online-festival-room-ready-check-submit`、`online-festival-room-ready-submit`、`online-festival-room-start-submit`、`online-festival-room-settle-submit`、`online-festival-room-close-submit`。在线节会远征旧 ID：`online-expedition-room-create-submit`、`online-expedition-room-invite-submit`、`online-expedition-room-ready-check-submit`、`online-expedition-room-ready-submit`、`online-expedition-room-start-submit`、`online-expedition-room-settle-submit`、`online-expedition-room-close-submit`，统一壳结算 ID 为 `online-expedition-room-shell-settle-submit`。独立远征旧 ID：`expedition-room-ready-check-submit`、`expedition-room-ready-submit`、`expedition-room-start-submit`、`expedition-room-settle-submit`、`expedition-room-close-submit`。
- 证据（2026-06-02，Agent A）：后续新弹窗 / 向导新增 ID 使用 16.4 规范：`online-room-create-trigger`、`online-room-wizard`、`online-invite-panel`、`online-room-lobby`、`online-room-settle-confirm`、`online-room-close-confirm`；旧 ID 移入弹窗或备用操作时必须并存，不直接删除。
- 证据（2026-06-02，Agent A）：已用 `rg` 静态验证旧 ID 仍存在于 `OnlineFestivalView.vue`、`ExpeditionRoomView.vue`、`game-smoke.spec.ts`、`qa-online-ui-structure.mjs`；现有 E2E 仍定位 `online-expedition-room-settle-submit`、`online-expedition-room-shell-settle-submit`、`online-festival-room-settle-submit`。
- 风险（2026-06-02，Agent A）：现有 E2E 对创建、邀请、ready、倒计时的覆盖仍偏静态 / 页面级，完整浏览器路径需在 OUI-QA-005 扩展。

### OUI-002 文案禁用词 allowlist 草案

- [x] `P0` 文件：`taoyuan-main/scripts/qa-online-player-copy.mjs`
- 依赖：OUI-000。
- 执行：先创建脚本设计或 TODO，不要求第一天启用阻断。
- 执行：allowlist 允许 `data-testid`、类型字段、API 字段、技术详情组件、测试 mock。
- 执行：denylist 包含 `fallback`、`idempotency`、`record-only`、`hash`、`ledger`、`revision`、`mock`、`开始 ready`、`模拟断线`、`降级入口`。
- 验收：脚本可以输出命中文件、行号、命中词、替换建议。
- 验证：本地运行脚本能命中现有问题，不误扫全部测试数据。
- 证据（2026-06-02，Agent B）：新增 / 更新 `taoyuan-main/scripts/qa-online-player-copy.mjs`，扫描 `src/views/game` 与 `src/components/game/online` 下 Vue 模板的玩家可见文本和静态可读属性，不扫描 `data-testid`、脚本区、类型字段、API/store 字段或测试数据；denylist 对齐 0.9 首批词，allowlist 先保留未来 `OnlineTechnicalDetails.vue` 默认折叠技术详情。输出格式包含 `文件:行号`、命中词、替换建议和摘要。
- 证据（2026-06-02，Agent B）：`node --check taoyuan-main\scripts\qa-online-player-copy.mjs` 通过；`npm --prefix taoyuan-main run qa:online-player-copy` 以阶段 1 warn-only 运行通过，命中现有问题 `ExpeditionRoomView.vue:104` 的「开始 ready」、`ExpeditionRoomView.vue:138` 的「模拟断线」、`OnlineView.vue:156` 的「降级入口」。
- 风险（2026-06-02，Agent B）：历史风险已解除；OUI-000 已有基线记录，OUI-COPY-001 首批文案已收口，`qa:online-player-copy` 已切到严格阻断模式。
- 证据（2026-06-02，Agent B）：`node --check taoyuan-main/scripts/qa-online-player-copy.mjs` 通过；严格运行 `npm --prefix taoyuan-main run qa:online-player-copy` 通过，输出 `[qa-online-player-copy] passed (59 files, 14 denylist terms, 1 allowlist entries)`。

### OUI-003 新流程开关策略

- [x] `P0` 文件：`taoyuan-main/src/views/game/online/OnlineFestivalView.vue`、`taoyuan-main/src/views/game/ExpeditionRoomView.vue`
- 依赖：OUI-001。
- 执行：决定是否使用局部常量 `useNewOnlineRoomFlow` 保护新弹窗路径。
- 执行：如果不用开关，必须保留「备用操作」折叠恢复路径。
- 验收：房间核心流接入后可快速回滚到旧操作入口。
- 验证：人工检查备用操作仍能提交同一 store action。
- 证据（2026-06-03，Agent A）：决策为暂不新增局部 `useNewOnlineRoomFlow` 开关；`OnlineFestivalView.vue` 和 `ExpeditionRoomView.vue` 均未包含该常量，回滚路径依赖默认折叠的 `OnlineTechnicalDetails` / 统一房间壳备用操作读回。
- 证据（2026-06-03，Agent A）：`qa-online-ui-structure.mjs` 新增 `expectNotContains` 与 OUI-003 静态守护，覆盖节会 / 在线节会远征 / 独立远征的备用创建、邀请、准备、倒计时、结算、关闭 test id 和原 `createRoom`、`inviteMember`、`startReadyCheck`、`readyRoomAction`、`unreadyRoomAction`、`startCountdown`、`settleRoomAction`、`closeRoomAction` 调用。
- 证据（2026-06-03，Agent A）：`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`npm --prefix taoyuan-main run qa:online-ui-structure` 运行后无 OUI-003 新增失败，仅剩既有 `qa-mobile-ui-smoke.mjs` 小屋 / 农场共同庄园、在线庄园轻采、公共订单接力、花灯墙、村社仓廪覆盖缺口。
- 证据（2026-06-04，Agent A）：补齐浏览器级备用操作提交证据：`game-smoke.spec.ts` 新增 `online festival room backup ready action submits original endpoint`，展开 `online-festival-room-lobby-backup-actions` 后点击旧 `online-festival-room-ready-check-submit`，等待原 `/api/taoyuan/online/festival/rooms/*/ready-check` 返回 200，并读回 `online-festival-room-status-panel` 的「准备确认」与备用区 `online-festival-room-start-submit`。
- 证据（2026-06-04，Agent A）：`qa-online-ui-structure.mjs` 已把上述 E2E 名称纳入 OUI-003 静态守护；验证 `node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、目标 E2E grep 均返回 0。
- 证据（2026-06-04，Agent A）：补齐在线节会远征备用开始准备浏览器证据：`game-smoke.spec.ts` 新增 `online expedition room backup ready action submits original endpoint`，展开 `online-expedition-room-lobby-backup-actions` 后点击旧 `online-expedition-room-ready-check-submit`，等待原 `/api/taoyuan/online/expedition/rooms/*/ready-check` 返回 200，并读回 `online-expedition-room-status-panel` 的「准备确认」与备用区 `online-expedition-room-start-submit`。
- 证据（2026-06-04，Agent A）：`qa-online-ui-structure.mjs` 已把节会房与在线节会远征两个备用 ready E2E 名称纳入 OUI-003 静态守护；验证 `node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、目标 E2E grep `online (festival|expedition) room backup ready action submits original endpoint` 均返回 0。
- 证据（2026-06-04，Agent A）：补齐独立远征备用开始准备浏览器证据：`game-smoke.spec.ts` 新增 `standalone expedition room backup ready action submits original endpoint`，直接打开 `/#/game/expedition-room` 后展开 `expedition-room-lobby-backup-actions`，点击旧 `expedition-room-ready-check-submit`，等待原 `/api/taoyuan/online/expedition/rooms/*/ready-check` 返回 200，并读回 `expedition-room-page` 的「准备确认」与备用区 `expedition-room-start-submit`。
- 证据（2026-06-04，Agent A）：`qa-online-ui-structure.mjs` 已把节会房、在线节会远征和独立远征三个备用 ready E2E 名称纳入 OUI-003 静态守护；验证 `node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、目标 E2E grep `(online (festival|expedition) room|standalone expedition room) backup ready action submits original endpoint` 均返回 0。
- 证据（2026-06-04，Agent A）：补齐远征侧备用结算浏览器证据：`game-smoke.spec.ts` 新增 `online expedition room backup settle action submits original endpoint` 与 `standalone expedition room backup settle action submits original endpoint`，分别展开 `online-expedition-room-lobby-backup-actions` / `expedition-room-lobby-backup-actions`，点击旧 `online-expedition-room-settle-submit` / `expedition-room-settle-submit`，进入 `OnlineConfirmActionDialog` 后提交原 `/api/taoyuan/online/expedition/rooms/*/settle`，并读回结算记录。
- 证据（2026-06-04，Agent A）：`qa-online-ui-structure.mjs` 已把两个备用 settle E2E 名称纳入 OUI-003 静态守护；验证 `node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、目标 E2E grep `(online expedition room|standalone expedition room) backup settle action submits original endpoint` 均返回 0。
- 证据（2026-06-04，Agent A）：补齐旧备用创建入口浏览器证据：`game-smoke.spec.ts` 新增 `online festival room backup create action submits original endpoint`、`online expedition room backup create action submits original endpoint`、`standalone expedition room backup create action submits original endpoint`，分别展开节会 / 在线节会远征 / 独立远征的「备用创建表单」，点击旧 `online-festival-room-create-submit`、`online-expedition-room-create-submit`、`expedition-room-create-submit`，确认仍提交原 `/api/taoyuan/online/festival/rooms` 或 `/api/taoyuan/online/expedition/rooms` POST 并读回房间与大厅入口。
- 证据（2026-06-04，Agent A）：`qa-online-ui-structure.mjs` 已把三条备用 create E2E 名称纳入 OUI-003 静态守护；验证 `node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、目标 E2E grep `(online festival room|online expedition room|standalone expedition room) backup create action submits original endpoint`、`npm --prefix taoyuan-main run check` 均返回 0。
- 证据（2026-06-04，Agent A）：补齐旧备用邀请入口浏览器证据：`game-smoke.spec.ts` 新增 `online festival room backup invite action submits original endpoint`、`online expedition room backup invite action submits original endpoint`、`standalone expedition room backup invite action submits original endpoint`，分别展开节会 / 在线节会远征 / 独立远征的「备用邀请表单」，点击旧 `online-festival-room-invite-submit`、`online-expedition-room-invite-submit`、`expedition-room-invite-submit`，确认仍提交原 `/api/taoyuan/online/festival/rooms/*/invite` 或 `/api/taoyuan/online/expedition/rooms/*/invite` POST，并由原 store action 清空输入。
- 证据（2026-06-04，Agent A）：`qa-online-ui-structure.mjs` 已把三条备用 invite E2E 名称纳入 OUI-003 静态守护；验证 `node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、目标 E2E grep `(online festival room|online expedition room|standalone expedition room) backup invite action submits original endpoint`、`npm --prefix taoyuan-main run check` 均返回 0。
- 证据（2026-06-04，Agent A）：补齐旧备用关闭入口浏览器证据：`game-smoke.spec.ts` 新增 `online festival room backup close action submits original endpoint`、`online expedition room backup close action submits original endpoint`、`standalone expedition room backup close action submits original endpoint`，分别展开节会 / 在线节会远征 / 独立远征的备用房间操作，点击旧 `online-festival-room-close-submit`、`online-expedition-room-close-submit`、`expedition-room-close-submit`，进入确认弹窗并填写确认文字后提交原 `/api/taoyuan/online/festival/rooms/*/close` 或 `/api/taoyuan/online/expedition/rooms/*/close` POST。
- 证据（2026-06-04，Agent A）：`qa-online-ui-structure.mjs` 已把三条备用 close E2E 名称纳入 OUI-003 静态守护；验证 `node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、目标 E2E grep `(online festival room|online expedition room|standalone expedition room) backup close action submits original endpoint`、`npm --prefix taoyuan-main run check` 均返回 0。
- 风险（2026-06-04，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，但失败项为小屋 / 农场共同庄园入口与花灯墙移动端 smoke 覆盖缺口；未出现 OUI-003 备用操作、备用 create / invite / ready / settle / close E2E 名称或房间流程结构断言失败。

## 4. M1 通用组件

### OUI-CMP-001 新建 `OnlineActionDialog.vue`

- [x] `P0` 文件：`taoyuan-main/src/components/game/online/OnlineActionDialog.vue`
- 依赖：OUI-001。
- 执行：复用 `game-modal-overlay` 和 `game-panel` 视觉底座。
- 执行：支持 `open`、`title`、`description`、`tone`、`confirmLabel`、`cancelLabel`、`confirmDisabled`、`running`、`requireText`、`closeOnBackdrop`。
- 执行：提供 `confirm`、`cancel`、`close` 事件。
- 执行：支持默认 slot、footer slot、details slot。
- 执行：危险确认时显示确认文字输入。
- 验收：普通确认、危险确认、运行中禁用、取消关闭都可用。
- 验证：`npm run qa:online-ui-structure` 增加组件存在性检查。
- 证据（2026-06-02，Agent A）：新增 `taoyuan-main/src/components/game/online/OnlineActionDialog.vue`，复用 `game-modal-overlay` 与 `game-panel`；支持 `open`、`title`、`description`、`tone`、`confirmLabel`、`cancelLabel`、`confirmDisabled`、`running`、`requireText`、`closeOnBackdrop`，并提供 `confirm`、`cancel`、`close` 事件、默认 / `footer` / `details` slot。
- 证据（2026-06-02，Agent A）：组件已提供 `online-action-dialog`、`online-action-dialog-title`、`online-action-dialog-confirm`、`online-action-dialog-cancel`、`online-action-dialog-required-text`；危险确认需输入 `requireText` 才能确认，`running` 和 `confirmDisabled` 会禁用确认按钮。
- 证据（2026-06-02，Agent A）：`taoyuan-main/scripts/qa-online-ui-structure.mjs` 已增加 `OnlineActionDialog.vue` 读取和组件存在性 / test id / `role="dialog"` / `aria-modal="true"` / `requireText` / `closeOnBackdrop` 断言；`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过，`rg` 已确认断言与组件关键字段存在。
- 证据（2026-06-02，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 已运行，仍因 OUI-000 记录的既有共同庄园、庄园、移动 smoke 与 `VisualTrackBoard` 基线失败返回非 0，但没有 `OnlineActionDialog` 相关失败。
- 风险（2026-06-02，Agent A）：`npm --prefix taoyuan-main run check` 仍被 Agent B 范围的 `cohabitationApi.ts` / `OnlineCohabitationView.vue` 语法错误阻断；`OnlineActionDialog` 尚未接入实际页面，OUI-CMP-002 的焦点返回 / Esc 浏览器级验证仍需后续补 E2E。

### OUI-CMP-002 Dialog 可访问性

- [x] `P0` 文件：`OnlineActionDialog.vue`
- 依赖：OUI-CMP-001。
- 执行：根节点增加 `role="dialog"` 和 `aria-modal="true"`。
- 执行：标题绑定 `aria-labelledby`。
- 执行：说明绑定 `aria-describedby`。
- 执行：打开后聚焦第一个可交互元素或标题。
- 执行：关闭后返回触发按钮焦点。
- 执行：Esc 关闭普通弹窗，危险弹窗 Esc 走取消逻辑。
- 验收：键盘可完成确认 / 取消，焦点不丢失。
- 验证：E2E 增加 Esc 和焦点返回断言。
- 证据（2026-06-02，Agent A）：`OnlineActionDialog.vue` 已具备 `role="dialog"`、`aria-modal="true"`、`:aria-labelledby="titleId"`、`:aria-describedby="description ? descriptionId : undefined"`；打开时记录 `document.activeElement` 并调用 `focusFirstControl`，关闭时通过 `previousFocus` 尝试恢复触发点焦点。
- 证据（2026-06-02，Agent A）：`qa-online-ui-structure.mjs` 已补充 `OnlineActionDialog.vue` 的 `aria-labelledby`、`aria-describedby`、`focusFirstControl`、`previousFocus`、`@keydown.esc.prevent.stop="handleEscape"`、危险弹窗 Esc 走取消逻辑静态断言；`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过。
- 证据（2026-06-03，Agent A）：`OnlineActionDialog.vue` 增加 `handleGlobalKeydown` 全局 Esc 兜底、`immediate: true` 打开监听和 `onBeforeUnmount` 清理；父级通过 `v-if` 直接创建 `open=true` 弹窗时，也会记录触发焦点、注册 Esc，并在关闭 / 卸载后调用 `restoreFocus()`。
- 证据（2026-06-03，Agent A）：`game-smoke.spec.ts` 新增 `online festival room close confirm requires text escape focus and submits`，真实浏览器断言危险确认未填文字时禁用、按 Esc 关闭、焦点返回 `online-festival-room-close-submit`，重新打开后填写「确认关闭房间」并提交既有 `/close` 接口。
- 证据（2026-06-03，Agent A）：验证 `node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online festival room close confirm requires text escape focus and submits"` 通过；`npm --prefix taoyuan-main run qa:online-ui-structure` 仍因既有非 Agent A 页面 smoke 覆盖缺口返回非 0，未出现 `OnlineActionDialog`、Esc、焦点返回或关闭确认 E2E 守护新增失败。
- 风险（2026-06-03，Agent A）：本任务要求的 dialog 键盘和焦点证据已补齐；后续 OUI-QA-005 仍需继续扩展完整房主创建、邀请、准备、倒计时和远征路径 E2E。

### OUI-CMP-003 新建 `OnlineBottomSheet.vue`

- [x] `P0` 文件：`taoyuan-main/src/components/game/online/OnlineBottomSheet.vue`、`taoyuan-main/src/app.css`
- 依赖：OUI-CMP-001。
- 执行：小屏底部抽屉，大屏右侧抽屉或居中面板。
- 执行：支持 header、default、footer slot。
- 执行：内容区独立滚动，头尾固定。
- 执行：底部使用 `env(safe-area-inset-bottom)`。
- 执行：提供 `data-testid="online-bottom-sheet"`。
- 验收：390x844 下内容不溢出，底部按钮不被系统安全区遮挡。
- 验证：移动端 smoke 截图。
- 证据（2026-06-02，Agent A）：新增 `taoyuan-main/src/components/game/online/OnlineBottomSheet.vue`，支持 `open`、`title`、`description`、`side`、`closeOnBackdrop`、`initialFocus`；提供 `close`、`after-open`、`after-close` 事件和 `header` / default / `footer` slot。
- 证据（2026-06-02，Agent A）：组件提供 `online-bottom-sheet`、`online-bottom-sheet-title`、`online-bottom-sheet-close`、`online-bottom-sheet-description`；使用 `role="dialog"`、`aria-modal="true"`、`aria-labelledby`，打开时锁定 `document.body.style.overflow`，底部 footer 使用 `env(safe-area-inset-bottom)`。
- 证据（2026-06-02，Agent A）：`qa-online-ui-structure.mjs` 已读取并断言 `OnlineBottomSheet.vue` 的 test id、dialog 语义、side 布局、`after-open` / `after-close`、背景滚动锁定和安全区；`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`node -e` 调用 `@vue/compiler-sfc` 对新增房间组件解析和 `compileScript` 通过。
- 风险（2026-06-02，Agent A）：`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 被既有 `src/utils/cohabitationApi.ts:2430 Unexpected "{"` 阻断，未获得 390x844 截图；组件尚未接入节会 / 远征页面，因此本任务保持 `[~]`。
- 证据（2026-06-03，Agent A）：`taoyuan-main/scripts/qa-mobile-ui-smoke.mjs` 新增 `prepareOnlineFestivalRoomWizardMobile` 和 `mockFestivalRoom`，在 `25-online-festival-room-wizard-mobile-390x844`、`27-online-festival-room-wizard-mobile-360x780` 打开节会房创建向导并断言 `online-bottom-sheet`、`online-room-wizard`、`online-room-wizard-next`、`online-bottom-sheet-close` 可见，面板 / footer 主按钮 / 关闭按钮位于 viewport 内，`document.body.style.overflow` 锁定，且无横向溢出。
- 证据（2026-06-03，Agent A）：`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0；生成截图 `docs/ui-smoke-2026-04-26/25-online-festival-room-wizard-mobile-390x844.png`、`docs/ui-smoke-2026-04-26/27-online-festival-room-wizard-mobile-360x780.png`，summary 中两条新增场景均为 `horizontalOverflow=false`、`primaryActionVisibleInViewport=true`。
- 风险（2026-06-03，Agent A）：底部抽屉移动端验收风险已解除；`qa:mobile-ui-smoke` 运行时仍输出既有 Vite 代理拒连 `127.0.0.1:4013` 噪声，但命令退出码为 0，且本轮 mock 覆盖节会房间 API。

### OUI-CMP-004 新建 `OnlineStickyActionBar.vue`

- [x] `P0` 文件：`taoyuan-main/src/components/game/online/OnlineStickyActionBar.vue`、`taoyuan-main/src/app.css`
- 依赖：OUI-CMP-003。
- 执行：小屏固定底部，桌面隐藏或变为普通 action row。
- 执行：支持 statusLabel、primaryAction、secondaryActions、moreActions。
- 执行：主按钮高度不低于 44px。
- 执行：页面底部预留 spacer，避免遮挡最后内容。
- 验收：移动端首屏可触达主行动。
- 验证：`qa-mobile-ui-smoke.mjs` 检查主按钮在 viewport 内。
- 证据（2026-06-02，Agent A）：新增 `taoyuan-main/src/components/game/online/OnlineStickyActionBar.vue`，支持 `statusLabel`、`primaryAction`、`secondaryActions`、`moreActions`、`disabledReason`；提供 `primary`、`secondary`、`more` 事件。
- 证据（2026-06-02，Agent A）：组件提供 `online-sticky-action-bar`、`online-sticky-primary-action`、`online-sticky-action-bar-spacer`、`online-sticky-status-label`、`online-sticky-disabled-reason`；主按钮和次按钮触控高度使用 `min-h-[44px]`，底部 padding 使用 `env(safe-area-inset-bottom)`，并内置移动端 spacer。
- 证据（2026-06-02，Agent A）：`qa-online-ui-structure.mjs` 已断言固定行动栏 test id、行动模型、禁用原因、44px 触控目标和安全区；`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；新增组件 SFC 解析和 `compileScript` 通过。
- 风险（2026-06-02，Agent A）：`qa-mobile-ui-smoke` 仍被共同庄园语法基线阻断，未能验证主按钮真实 viewport 位置；页面尚未接入 sticky action bar，因此本任务保持 `[~]`。
- 证据（2026-06-04，Agent A）：`OnlineStickyActionBar.vue` 已被在线中心首屏复用，`TAOYUAN_MOBILE_SMOKE_ONLY=online-center npm --prefix taoyuan-main run qa:mobile-ui-smoke` 提升权限复跑返回 0；生成 `docs/ui-smoke-2026-04-26/24-online-center-mobile-390x844.png`、`docs/ui-smoke-2026-04-26/26-online-center-mobile-360x780.png`，脚本断言 `online-sticky-action-bar`、`online-sticky-primary-action` 可见，`stickyPrimaryInViewport=true`，主行动在 390x844 / 360x780 viewport 内。
- 风险（2026-06-04，Agent A）：沙箱内同一移动端 smoke 因 Playwright Chromium `spawn EPERM` 被脚本跳过，已按权限规则提升权限复跑通过；`qa:online-ui-structure` 仍被非 Agent A 的共同庄园 / 在线庄园 / 花灯墙 / 村社仓廪移动端覆盖缺口阻断，未出现 sticky 组件断言失败。

### OUI-CMP-005 新建 `OnlineTechnicalDetails.vue`

- [x] `P0` 文件：`taoyuan-main/src/components/game/online/OnlineTechnicalDetails.vue`
- 依赖：OUI-CMP-001。
- 执行：默认折叠，标题支持「技术详情」「结算凭证」「审计记录」「备用操作」。
- 执行：支持 summary slot、default slot、actions slot。
- 执行：玩家摘要与技术字段分区。
- 执行：提供复制凭证按钮，但默认隐藏长 ID。
- 验收：主路径不直接显示技术字段，展开后 QA 可定位。
- 验证：静态检查确认技术词只在该组件或 allowlist 出现。
- 证据（2026-06-02，Agent A）：新增 `taoyuan-main/src/components/game/online/OnlineTechnicalDetails.vue`，使用 `<details>` 默认折叠技术信息，支持 `title`、`summary`、`defaultOpen`、`tone`、`copyable`，并提供 `summary` / default / `actions` slot。
- 证据（2026-06-02，Agent A）：组件提供 `online-technical-details`、`online-technical-details-toggle`、`online-technical-details-summary`、`online-technical-details-content`、`online-technical-details-copy`；`copyable` 长值默认通过 `copyPreview` 截断展示，复制时写入完整 `navigator.clipboard.writeText`。
- 证据（2026-06-02，Agent A）：`qa-online-ui-structure.mjs` 已加入技术详情组件默认折叠、复制、截断、QA 展开区断言；`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`npm --prefix taoyuan-main run qa:online-player-copy` 通过 warn-only，新增组件未引入 denylist 命中。
- 风险（2026-06-02，Agent A）：组件尚未接入节会 / 远征页面，后续 OUI-COPY-001 / OUI-FEST-005 迁移技术字段时需要复用该组件。

### OUI-CMP-006 新建 `OnlineConfirmActionDialog.vue`

- [x] `P0` 文件：`taoyuan-main/src/components/game/online/OnlineConfirmActionDialog.vue`
- 依赖：OUI-CMP-001。
- 执行：封装危险确认常用结构。
- 执行：支持影响对象、资产变化、不可撤销提示、确认文字。
- 执行：默认 `tone="danger"`，默认 `closeOnBackdrop=false`。
- 验收：结算、关闭、供品、基金等高风险操作可复用。
- 验证：E2E 验证确认文字未填时主按钮禁用。
- 证据（2026-06-02，Agent A）：新增 `taoyuan-main/src/components/game/online/OnlineConfirmActionDialog.vue`，复用 `OnlineActionDialog`，默认 `tone="danger"` 且 `:close-on-backdrop="false"`；支持 `impactItems`、`assetChanges`、`irreversible`、`requireText`、`confirmLabel`、`running`、`recoveryHint`。
- 证据（2026-06-02，Agent A）：组件提供 `online-confirm-action-dialog`、`online-confirm-impact-list`、`online-confirm-asset-list`、`online-confirm-required-text`、`online-confirm-disabled-reason`；`confirmBlocked` 会在 `running`、影响对象缺失或确认文字不匹配时禁用确认。
- 证据（2026-06-02，Agent A）：`qa-online-ui-structure.mjs` 已断言高风险确认弹窗复用底座、必备 test id、危险语气、背景不可关闭、资产变化、不可撤销提示和禁用规则；`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；新增组件 SFC 解析和 `compileScript` 通过。
- 证据（2026-06-03，Agent A）：节会房结算 / 关闭已复用 `OnlineConfirmActionDialog`；新增浏览器用例 `online festival room close confirm requires text escape focus and submits` 断言 `online-confirm-action-dialog-confirm` 在确认文字未填时禁用，填写 `online-confirm-required-text` 后才提交关闭 action。
- 风险（2026-06-03，Agent A）：确认文字禁用已有浏览器证据，但供品等非房间高风险动作和远征房确认接入不在本轮收口范围内，因此通用高风险确认任务仍保持 `[~]`，等待剩余页面复用证据齐全后关闭。
- 证据（2026-06-04，Agent A）：当前实现仍满足 16.1 规格：组件复用 `OnlineActionDialog`，默认 `tone="danger"`、`:close-on-backdrop="false"`，提供 `online-confirm-action-dialog`、`online-confirm-impact-list`、`online-confirm-required-text`、`online-confirm-disabled-reason`，并在 `running`、影响对象缺失或确认文字不匹配时禁用确认。
- 证据（2026-06-04，Agent A）：`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 返回 0；`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online festival room close confirm requires text escape focus and submits" --reporter=line --workers=1` 返回 0，覆盖房间关闭确认未填文字禁用、Esc 取消、焦点返回、填写「确认关闭房间」后提交。
- 风险（2026-06-04，Agent A）：`npm --prefix taoyuan-main run check` 当前被 Agent B 范围 `OnlineSocietyView.vue` 多个未使用 import / helper 阻断；`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败集中在共同庄园入口、在线庄园轻采、花灯墙、村社仓廪等非 Agent A 移动端覆盖缺口，未出现 `OnlineConfirmActionDialog` 或房间关闭确认断言失败。

### OUI-CMP-007 新建 `OnlineEmptyState.vue`

- [x] `P1` 文件：`taoyuan-main/src/components/game/online/OnlineEmptyState.vue`
- 依赖：OUI-CMP-001。
- 执行：统一空态标题、说明、主按钮、次按钮。
- 执行：适配无房间、无邀请、无可见房间、无委托、无村社。
- 验收：主要在线页不再各自写零散空态。
- 验证：静态检查可选。
- 证据（2026-06-02，Agent B）：新增 `taoyuan-main/src/components/game/online/OnlineEmptyState.vue`，提供统一标题、说明、可选插图、主按钮和次按钮；保留 `data-testid="online-empty-state"`、`online-empty-state-primary`、`online-empty-state-secondary`，主按钮使用现有 `online-action-btn--primary`。
- 证据（2026-06-02，Agent B）：`node -e` 调用 `@vue/compiler-sfc` 对 `OnlineEmptyState.vue` 解析和 `compileScript` 通过；`npm --prefix taoyuan-main run qa:online-player-copy` 通过 warn-only，新增组件未引入 denylist 命中。
- 证据（2026-06-02，Agent B）：`taoyuan-main/src/views/game/online/OnlineSocietyView.vue` 已把未加入村社、无公开村社、无成员视图、无待处理申请、未加入仓库 / 公共建设 / 提案，以及无进行中提案等零散说明替换为 `OnlineEmptyState`；`taoyuan-main/scripts/qa-online-ui-structure.mjs` 已增加组件 test id 与村社页至少 6 处引用断言。
- 证据（2026-06-02，Agent B）：`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`node -e` 调用 `@vue/compiler-sfc` 对 `OnlineEmptyState.vue`、`OnlineStatusBanner.vue`、`OnlineSocietyView.vue` 解析和 `compileScript` 通过；`npm --prefix taoyuan-main run qa:online-ui-structure` 已运行，仍因 OUI-000 已记录的移动端 smoke / 在线庄园 / VisualTrackBoard 旧缺口返回非 0，未出现 `OnlineEmptyState` 或村社页新增引用断言失败。
- 证据（2026-06-02，Agent B）：`taoyuan-main/src/views/game/online/OnlineOrdersView.vue` 已把接力阶段草稿、可接委托筛选结果、公开订单凭证、我的发布、我的接单、已接接力阶段、结算凭证和补偿重试等空态替换为 `OnlineEmptyState`，并保留“新增阶段”“查看全部”“发布求助单”“查看可接委托”等既有动作路径；`qa-online-ui-structure.mjs` 已增加委托页至少 7 处空态引用断言。
- 证据（2026-06-02，Agent B）：`node -e` 调用 `@vue/compiler-sfc` 对 `OnlineEmptyState.vue`、`OnlineStatusBanner.vue`、`OnlineOrdersView.vue` 解析和 `compileScript` 通过；`npm --prefix taoyuan-main run qa:online-player-copy` 通过 warn-only，新增委托页文案未引入 denylist 命中。
- 证据（2026-06-02，Agent B）：`taoyuan-main/src/views/game/online/OnlineNeighborView.vue` 已把公开名片未载入、联机史册、成就卡、邻里排行、成员信息、邻里动态、公开邻里、申请 / 邀请和订阅预览等空态替换为 `OnlineEmptyState`，并保留刷新邻里、创建 / 申请 / 管理等既有动作路径；`qa-online-ui-structure.mjs` 已增加邻里页至少 7 处空态引用断言。
- 证据（2026-06-02，Agent B）：`node -e` 调用 `@vue/compiler-sfc` 对 `OnlineEmptyState.vue`、`OnlineStatusBanner.vue`、`OnlineNeighborView.vue` 解析和 `compileScript` 通过；`npm --prefix taoyuan-main run qa:online-player-copy` 通过 warn-only，新增邻里页文案未引入 denylist 命中。
- 证据（2026-06-02，Agent B）：`taoyuan-main/src/views/game/online/OnlineManorView.vue` 已把无快照、留言 / 来访 / 导览空列表、访客行为审计空态、无可照料对象、无进行中护理房、照料 / 轻采 / 护理房记录等零散说明替换为 `OnlineEmptyState`，并保留刷新庄园、留言、来访、导览、护理房创建和原列表 test id；`qa-online-ui-structure.mjs` 已增加庄园页至少 12 处空态引用断言。
- 证据（2026-06-02，Agent B）：`node -e` 调用 `@vue/compiler-sfc` 对 `OnlineManorView.vue`、`OnlineEmptyState.vue`、`OnlineStatusBanner.vue` 解析和 `compileScript` 通过；`npm --prefix taoyuan-main run qa:online-player-copy` 通过 warn-only，新增庄园页文案未引入 denylist 命中。
- 风险（2026-06-02，Agent B）：当前已完成村社、委托、邻里和庄园页第一批接入，但 `npm --prefix taoyuan-main run qa:online-ui-structure` 仍被 OUI-000 已记录的移动端 smoke / `VisualTrackBoard` / 村社仓廪等既有结构缺口阻断，`npm --prefix taoyuan-main run check` 仍被现有 `cohabitationApi.ts` 语法错误阻断；`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 已尝试运行，也被 `cohabitationApi.ts:2430 Unexpected "{"` 阻断导致 `game-layout` 未出现。任务保持 `[~]`，待共同庄园和全量验证基线收口后再关闭。
- 证据（2026-06-03，Agent B）：复核当前实现，`OnlineEmptyState.vue` 仍保留统一标题、说明、插图、主 / 次按钮和 `online-empty-state*` test id；村社、委托、邻里、在线庄园页仍分别有 6 / 7 / 7 / 12 处以上 `OnlineEmptyState` 引用，主要在线页零散空态已被统一组件承接。
- 证据（2026-06-03，Agent B）：`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（66 files / 14 denylist / 1 allowlist）；在 `taoyuan-main` 目录用 `node -e` 调用 `@vue/compiler-sfc` 对 `OnlineEmptyState.vue`、`OnlineStatusBanner.vue`、`OnlineSocietyView.vue`、`OnlineOrdersView.vue`、`OnlineNeighborView.vue`、`OnlineManorView.vue` 解析和 `compileScript` 通过。
- 风险（2026-06-03，Agent B）：`npm --prefix taoyuan-main run check` 当前被并行未提交的物品图标改动阻断（`CookingView.vue(108,62)`、`GameLayout.vue(378,53)` 把 `string` 传给 `ItemIconSize`）；`npm --prefix taoyuan-main run qa:online-ui-structure` 仍因 `qa-mobile-ui-smoke.mjs` 既有 / 并行移动端覆盖缺口返回非 0，失败列表未出现 `OnlineEmptyState` 或四个接入页新增断言。

### OUI-CMP-008 新建 `OnlineStatusBanner.vue`

- [x] `P1` 文件：`taoyuan-main/src/components/game/online/OnlineStatusBanner.vue`
- 依赖：OUI-CMP-001。
- 执行：统一 loading、success、warning、danger、info 状态条。
- 执行：支持 `aria-live`。
- 验收：房间冲突、邀请成功、结算失败等反馈样式一致。
- 验证：人工检查和 E2E 文案断言。
- 证据（2026-06-02，Agent B）：新增 `taoyuan-main/src/components/game/online/OnlineStatusBanner.vue`，支持 `info / loading / success / warning / danger` tone、可选说明、操作按钮和可关闭按钮；保留 `data-testid="online-status-banner"`、`online-status-banner-action`、`online-status-banner-dismiss`，成功 / 错误态使用 `aria-live="assertive"`。
- 证据（2026-06-02，Agent B）：`node -e` 调用 `@vue/compiler-sfc` 对 `OnlineStatusBanner.vue` 解析和 `compileScript` 通过；`npm --prefix taoyuan-main run qa:online-player-copy` 通过 warn-only，新增组件未引入 denylist 命中。
- 证据（2026-06-02，Agent B）：`OnlineSocietyView.vue` 的模块错误反馈已改用 `OnlineStatusBanner`，保留“重试”恢复动作并继续调用既有 `refreshSocietyModule`；`qa-online-ui-structure.mjs` 已增加状态条 test id、`aria-live` 和村社页引用断言。
- 证据（2026-06-02，Agent B）：`npm --prefix taoyuan-main run qa:online-player-copy` 通过 warn-only，仍只命中既有 `ExpeditionRoomView.vue:104`「开始 ready」、`ExpeditionRoomView.vue:138`「模拟断线」、`OnlineView.vue:156`「降级入口」；`npm --prefix taoyuan-main run qa:online-ui-structure` 已运行，失败列表未出现 `OnlineStatusBanner` 或村社页错误状态条新增断言。
- 证据（2026-06-02，Agent B）：`OnlineOrdersView.vue` 的模块错误反馈已改用 `OnlineStatusBanner`，保留“重试”并继续调用既有 `refreshOrders`；补偿重试的 `last_error` 也改用 warning 状态条展示玩家化失败标题。`qa-online-ui-structure.mjs` 已增加委托页状态条、重试动作和补偿失败标题断言。
- 证据（2026-06-02，Agent B）：`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`npm --prefix taoyuan-main run qa:online-ui-structure` 已运行，仍因 OUI-000 已记录的移动端 smoke / 在线庄园 / VisualTrackBoard 旧缺口返回非 0，未出现 `OnlineOrdersView.vue` 的新增空态或状态条断言失败。
- 证据（2026-06-02，Agent B）：`OnlineNeighborView.vue` 的模块错误反馈已改用 `OnlineStatusBanner`，保留“重试”并继续调用既有 `refreshNeighborShell`；`qa-online-ui-structure.mjs` 已增加邻里页状态条、重试动作和引用断言。
- 证据（2026-06-02，Agent B）：`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`npm --prefix taoyuan-main run qa:online-ui-structure` 已运行，仍因 OUI-000 已记录的移动端 smoke / 在线庄园 / VisualTrackBoard 旧缺口返回非 0，未出现 `OnlineNeighborView.vue` 的新增空态或状态条断言失败。
- 证据（2026-06-02，Agent B）：`OnlineManorView.vue` 的模块错误、留言 / 来访 / 导览加载态和错误态已改用 `OnlineStatusBanner`，保留“重试”并继续调用既有 `refreshSnapshot`；同轮补齐庄园协作护理记录健康、风险和凭证读回钩子，消除结构 QA 中 5 条在线庄园护理记录旧失败。
- 证据（2026-06-02，Agent B）：`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`npm --prefix taoyuan-main run qa:online-ui-structure` 已运行，仍因 OUI-000 已记录的移动端 smoke / `VisualTrackBoard` / 村社仓廪等既有结构缺口返回非 0，未出现 `OnlineManorView.vue` 的新增空态或状态条断言失败。
- 风险（2026-06-02，Agent B）：`npm --prefix taoyuan-main run check` 已运行但被现有 `cohabitationApi.ts` TypeScript 语法错误阻断；`qa:online-ui-structure` 剩余失败仍来自移动端 smoke、`VisualTrackBoard` 和村社仓廪等未收口断言；`qa:mobile-ui-smoke` 已尝试运行，也被 `cohabitationApi.ts:2430 Unexpected "{"` 阻断。组件目前接入村社、委托、邻里与庄园页，但共同庄园和全量验证尚未完成，因此保持 `[~]`。
- 证据（2026-06-03，Agent B）：复核当前实现，`OnlineStatusBanner.vue` 仍支持 `info / loading / success / warning / danger` tone、说明、操作按钮、关闭按钮、`online-status-banner*` test id，成功 / 错误态通过 `liveMode` 使用 `assertive` 播报；村社、委托、邻里、在线庄园页错误 / 加载 / 补偿反馈仍复用该组件并保留重试动作。
- 证据（2026-06-03，Agent B）：`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（66 files / 14 denylist / 1 allowlist）；在 `taoyuan-main` 目录用 `node -e` 调用 `@vue/compiler-sfc` 对 `OnlineStatusBanner.vue`、`OnlineEmptyState.vue` 和四个接入页解析 / `compileScript` 通过。
- 风险（2026-06-03，Agent B）：`npm --prefix taoyuan-main run check` 当前被并行未提交的物品图标改动阻断（`CookingView.vue(108,62)`、`GameLayout.vue(378,53)` 把 `string` 传给 `ItemIconSize`）；`npm --prefix taoyuan-main run qa:online-ui-structure` 仍因 `qa-mobile-ui-smoke.mjs` 既有 / 并行移动端覆盖缺口返回非 0，失败列表未出现 `OnlineStatusBanner` 或四个接入页新增断言。

### OUI-CMP-009 增强 `OnlineModuleShell.vue`

- [x] `P1` 文件：`taoyuan-main/src/components/game/online/OnlineModuleShell.vue`
- 依赖：OUI-CMP-004、OUI-CMP-008。
- 执行：增加 `primaryAction` slot。
- 执行：增加 `status` slot。
- 执行：tab 区增加 `role="tablist"`。
- 执行：tab 按钮增加 `role="tab"`、`aria-selected`。
- 执行：内容区调用侧增加 `tabpanel`。
- 执行：移动端 tab sticky 或保持可横向滚动且选中态明确。
- 验收：各在线子页头部有统一主行动入口和 tab 语义。
- 验证：`qa-online-ui-structure.mjs` 增加断言。
- 证据（2026-06-02，Agent B）：`OnlineModuleShell.vue` 新增 `primaryAction` 与 `status` slot 定位区，tab 容器提供 `online-module-tablist`、`role="tablist"`，tab 按钮保留旧 `online-module-tab-*` test id 并新增 `role="tab"`、`aria-selected`、`aria-controls`、`tabindex`；移动端 tab 保持横向滚动并增加 sticky 可见性。
- 证据（2026-06-02，Agent B）：`OnlineCohabitationView.vue`、`OnlineManorView.vue`、`OnlineNeighborView.vue`、`OnlineOrdersView.vue`、`OnlineSocietyView.vue` 的模块内容区新增 `role="tabpanel"`、`online-module-panel-${activeTab}`、`aria-labelledby` 与 `online-module-tabpanel`，只覆盖 Agent B 非房间调用页，未移动节会 / 远征房间模板结构。
- 证据（2026-06-02，Agent B）：`qa-online-ui-structure.mjs` 已读取并断言 `OnlineModuleShell.vue` 的主行动 slot、状态 slot、tablist / tab 语义、选中态、面板关联、移动端横向滚动和 sticky；`qa-mobile-ui-smoke.mjs` 的在线委托场景改用 `online-module-tab-*` 定位新 tab 语义，并保留旧 test id 路径。
- 证据（2026-06-02，Agent B）：`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs` 通过；`npm --prefix taoyuan-main run qa:online-player-copy` 通过，输出 59 files / 14 denylist / 1 allowlist；`npm --prefix taoyuan-main run check` 通过，0 error，保留既有 3 条 lint warning；`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 通过，期间仅有后端代理 `127.0.0.1:4013` 未启动警告。
- 风险（2026-06-02，Agent B）：`npm --prefix taoyuan-main run qa:online-ui-structure` 已运行但仍因 OUI-000 既有移动端 smoke 覆盖、`VisualTrackBoard` 多队名次和村社仓廪覆盖缺口返回非 0，未出现 `OnlineModuleShell` 或本轮 tabpanel 新断言失败；`OUI-CMP-004` / `OUI-CMP-008` 仍保持 `[~]`，本轮只复用其已存在底座能力，不关闭这些依赖任务。

## 5. M2 房间核心流程组件

### OUI-FLOW-001 新建 `OnlineRoomWizard.vue`

- [x] `P0` 文件：`taoyuan-main/src/components/game/online/OnlineRoomWizard.vue`
- 依赖：OUI-CMP-001、OUI-CMP-003。
- 执行：支持 `domain: 'festival' | 'expedition'`。
- 执行：支持 `initialTemplateId`、`initialGameplayId`、`initialMemberLimit`、`initialTitle`。
- 执行：四步流程：选择玩法、配置房间、邀请成员、确认创建。
- 执行：移动端使用底部抽屉，大屏使用向导弹窗。
- 执行：创建失败停留当前步骤并保留草稿。
- 验收：节会和远征都能复用同一向导。
- 验证：E2E 覆盖创建节会房、创建远征房。
- 证据（2026-06-02，Agent A）：新增 `taoyuan-main/src/components/game/online/OnlineRoomWizard.vue`，支持 `domain: 'festival' | 'expedition'`、`initialTemplateId`、`initialGameplayId`、`initialMemberLimit`、`initialTitle`、`initialVisibility`，并提供 `submit`、`cancel`、`close`、`draft-change` 事件；提交只 emit `OnlineRoomWizardDraft`，不直接调用或改变现有 store action。
- 证据（2026-06-02，Agent A）：向导包含 `online-room-wizard`、`online-room-wizard-next`、`online-room-wizard-back`、`online-room-wizard-submit`，以及 `online-room-wizard-step-gameplay`、`online-room-wizard-step-config`、`online-room-wizard-step-invite`、`online-room-wizard-step-review` 四步钩子；移动端通过 `OnlineBottomSheet` 底部抽屉承载，桌面端通过 `OnlineActionDialog` 承载。
- 证据（2026-06-02，Agent A）：组件内置节会基础玩法「上元灯会 / 龙舟赛道 / 腊八共灶 / 烟火留影」和远征基础玩法「协作矿洞 / 护送抵运 / 海域共探 / 山林采集」作为无 store 数据时的兜底展示，同时支持页面传入 `templates`、`gameplayTemplates`、`memberLimitOptions` 以沿用现有 store 当前配置；创建失败可由父级传入 `errorMessage`，组件不会在 submit 后重置草稿。
- 证据（2026-06-02，Agent A）：`taoyuan-main/scripts/qa-online-ui-structure.mjs` 已读取并断言 `OnlineRoomWizard.vue` 的必备 test id、props、emits、底部抽屉 / 通用弹窗复用、人数选项、错误保留、节会 / 远征基础玩法；`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`node -e` 调用 `@vue/compiler-sfc` 对 `OnlineRoomWizard.vue`、`OnlineBottomSheet.vue`、`OnlineActionDialog.vue` 解析和 `compileScript` 通过。
- 证据（2026-06-02，Agent A）：`npm --prefix taoyuan-main run qa:online-player-copy` 通过 warn-only，仍只命中既有 `ExpeditionRoomView.vue:104`「开始 ready」、`ExpeditionRoomView.vue:138`「模拟断线」、`OnlineView.vue:156`「降级入口」，新增向导未引入玩家可见 denylist 命中。
- 风险（2026-06-02，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 已运行但仍被 OUI-000 记录的共同庄园 / 庄园 / 村社仓廪 / `VisualTrackBoard` 既有结构缺口阻断，失败列表未出现 `OnlineRoomWizard` 新断言；`npm --prefix taoyuan-main run check` 与 `qa:mobile-ui-smoke` 仍被 `src/utils/cohabitationApi.ts:2430 Unexpected "{"` 阻断。
- 证据（2026-06-02，Agent A）：`OnlineFestivalView.vue` 已在 OUI-FEST-001 中接入 `OnlineRoomWizard`，新增 `online-room-create-trigger`，并把节会 store 的 `templates`、`gameplayTemplates`、`memberLimitOptions` 传入向导。
- 风险（2026-06-02，Agent A）：节会页已接入向导，但 `ExpeditionRoomView.vue` / 在线节会远征标签尚未接入统一创建向导，也未获得创建节会房 / 创建远征房 E2E 通过证据；因此 OUI-FLOW-001 保持 `[~]`，等待 OUI-EXP-001 页面接入和 E2E 后关闭。
- 证据（2026-06-04，Agent A）：收口复核确认 OUI-FEST-006 / OUI-EXP-001 已解除前置风险；`OnlineFestivalView.vue` 与 `ExpeditionRoomView.vue` 均已接入 `OnlineRoomWizard`，节会、在线节会远征、独立远征创建入口分别保留 `online-room-create-trigger` / `online-expedition-room-create-trigger`，提交仍由页面同步草稿后调用原 `festivalRoomStore.createRoom()` / `expeditionRoomStore.createRoom()`。
- 证据（2026-06-04，Agent A）：本轮复跑 `node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node --check taoyuan-main/e2e/game-smoke.spec.ts`、`npm --prefix taoyuan-main run check` 均返回 0（保留既有 3 条 warning）；目标 E2E 组合 `online festival room wizard creates host room`、`online expedition room wizard creates host room`、`standalone expedition room reuses invite lobby and close confirm` 返回 0。
- 风险（2026-06-04，Agent A）：全量 `npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败均为小屋 / 农场共同庄园入口、在线庄园轻采、花灯墙、村社仓廪等非 Agent A 移动端覆盖缺口；未出现 `OnlineRoomWizard` 或房间创建路径新增失败。

### OUI-FLOW-002 节会向导玩法配置

- [x] `P0` 文件：`OnlineRoomWizard.vue`、`taoyuan-main/src/stores/useFestivalRoomStore.ts`
- 依赖：OUI-FLOW-001。
- 执行：玩法卡包含上元灯会、龙舟赛道、腊八共灶、烟火留影。
- 执行：配置字段包含房间标题、人数上限、玩法模式、公开可见。
- 执行：支持 URL query 预填 `template`、`gameplay`。
- 验收：从村社节庆广场完工入口进入时自动预填灯会模板。
- 验证：E2E 断言 query 预填后向导展示正确模板。
- 证据（2026-06-02，Agent A）：`OnlineFestivalView.vue` 已把 `OnlineRoomWizard` 的 `initialTemplateId`、`initialGameplayId`、`initialMemberLimit`、`initialTitle` 绑定到 `festivalRoomStore.selectedTemplateId`、`selectedGameplayTemplateId`、`normalizedDraftMemberLimit`、`draftTitle`；向导接收 `festivalRoomStore.templates`、`gameplayTemplates`、`memberLimitOptions`，节会玩法卡和人数上限来自当前 store 数据。
- 证据（2026-06-02，Agent A）：保留 `applyFestivalRoomRouteDraft()` 对 `route.query.template` / `template_id`、`route.query.gameplay` / `gameplay_template_id` 和 `title` 的预填逻辑；`syncFestivalRoomWizardDraft()` 会把向导草稿写回既有 store draft 字段，再由原 `festivalRoomStore.createRoom()` 提交。
- 证据（2026-06-02，Agent A）：`taoyuan-main/scripts/qa-online-ui-structure.mjs` 已新增节会页向导接入、store 模板 / 玩法 / 人数透传、submit handler、query 预填和旧 test id 保留断言；`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`node -e` 调用 `@vue/compiler-sfc` 对 `OnlineFestivalView.vue`、`OnlineRoomWizard.vue`、`OnlineTechnicalDetails.vue` 解析和 `compileScript` 通过。
- 风险（2026-06-02，Agent A）：`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts` 被 `src/utils/cohabitationApi.ts:2430 Unexpected "{"` 触发的 Vite error overlay 阻断，未获得 query 预填后的真实浏览器断言；向导中的「公开可见」目前仅作为前端草稿展示，节会 store / API 尚无对应提交字段，本轮未新增协议语义。
- 证据（2026-06-04，Agent A）：收口复核确认 `OnlineRoomWizard.vue` 仍内置上元灯会、龙舟赛道、腊八共灶、烟火留影兜底玩法，并在节会页透传 `festivalRoomStore.templates`、`gameplayTemplates`、`memberLimitOptions`；`applyFestivalRoomRouteDraft()` / `syncFestivalRoomWizardDraft()` 仍保留 query 预填和既有 draft 回写，不新增 API 字段。
- 证据（2026-06-04，Agent A）：目标 E2E 组合中的 `online festival room wizard creates host room` 与 `online festival room wizard escape closes dialog and restores focus` 返回 0；新增并运行 `online festival room wizard honors society launch query prefill` 返回 0，覆盖 `template=lantern_fair`、`gameplay=assembly`、`title=节庆广场开幕` 进入节会房向导后的模板、玩法和标题预填；`npm --prefix taoyuan-main run check` 返回 0。
- 风险（2026-06-04，Agent A）：向导的可见性仍是前端草稿展示，不进入节会创建 payload；这是既有“不新增协议语义”决策，不影响本任务的玩法 / 标题 / 人数 / 模式配置关闭。

### OUI-FLOW-003 远征向导玩法配置

- [x] `P0` 文件：`OnlineRoomWizard.vue`、`taoyuan-main/src/stores/useExpeditionRoomStore.ts`
- 依赖：OUI-FLOW-001。
- 执行：玩法卡包含协作矿洞、护送抵运、海域共探、山林采集。
- 执行：配置字段包含队伍标题、人数上限、风险等级、路线模板。
- 执行：确认页展示撤离规则和奖励预览。
- 验收：远征独立页和在线节会远征标签都能打开远征向导。
- 验证：E2E 覆盖远征创建。
- 证据（2026-06-02，Agent A）：`useExpeditionRoomStore.ts` 新增 `draftMemberLimit`、`memberLimitOptions`、`normalizedDraftMemberLimit`，按当前远征模板的 `min_member_limit` / `max_member_limit` / `default_member_limit` 派生人数上限；`createRoom()` 继续调用原远征创建 action，并把 `member_limit: normalizedDraftMemberLimit.value` 写入既有 payload，不改后端状态机语义。
- 证据（2026-06-02，Agent A）：`ExpeditionRoomView.vue` 已接入 `OnlineRoomWizard domain="expedition"`，新增 `online-expedition-room-create-trigger` 主入口；旧创建表单折叠进 `OnlineTechnicalDetails`，保留 `expedition-room-template-select`、`expedition-room-gameplay-select`、`expedition-room-title-input`、`expedition-room-member-limit-group`、`expedition-room-create-submit`。
- 证据（2026-06-02，Agent A）：`OnlineFestivalView.vue` 的远征标签已接入第二个 `OnlineRoomWizard domain="expedition"`，把 `expeditionRoomStore.templates`、`gameplayTemplates`、`memberLimitOptions`、当前草稿传入向导；提交由 `submitExpeditionRoomWizard()` 同步草稿后调用原 `expeditionRoomStore.createRoom()`，成功后 `expeditionRoomStore.refreshOverview({ silent: true })`；旧远征创建表单折叠到「备用创建表单」并保留 `online-expedition-room-template-select`、`online-expedition-room-gameplay-select`、`online-expedition-room-title-input`、`online-expedition-room-member-limit-group`、`online-expedition-room-create-submit`。
- 证据（2026-06-02，Agent A）：`taoyuan-main/scripts/qa-online-ui-structure.mjs` 已新增远征 store、独立远征页、在线节会远征标签的向导接入和旧 test id 保留断言；`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`node -e` 使用 `@vue/compiler-sfc` 解析并 `compileScript` 检查 `OnlineFestivalView.vue`、`ExpeditionRoomView.vue`、`OnlineRoomWizard.vue`、`OnlineTechnicalDetails.vue` 通过。
- 风险（2026-06-02，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 已运行但返回非 0；新增远征 store / 页面断言均未出现在失败列表，剩余失败集中在 `qa-mobile-ui-smoke.mjs` 的共同庄园 / 在线庄园 / 公共订单 / 村社仓廪历史缺口，以及 `VisualTrackBoard.vue` 轨道名次结构缺口。
- 风险（2026-06-02，Agent A）：`npm --prefix taoyuan-main run check` 已运行但返回非 0，当前阻断为 Agent B 范围 `OnlineCohabitationView.vue` 引用缺失 `sharedLogTechnicalDetail`；本轮曾修复 `ExpeditionRoomView.vue` 空 `catch` lint 错误，重跑后不再出现 Agent A 文件 lint / 类型错误。
- 风险（2026-06-02，Agent A）：`npm --prefix taoyuan-main run qa:online-player-copy` 以 warn-only 返回 0，剩余命中在旧 `FestivalView.vue` 和 `OnlineCohabitationView.vue`；`ExpeditionRoomView.vue` 与 `OnlineFestivalView.vue` 本轮远征向导路径未新增玩家可见 denylist 命中。
- 风险（2026-06-02，Agent A）：`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回非 0，阻断为在线委托场景找不到「当前没有结算凭证。」且本地后端 `127.0.0.1:4013` 代理拒绝；`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts` 返回非 0，最近失败包含旧节会人数按钮被折叠后 `online-festival-room-member-limit-8` 不可见，以及既有 `game-layout` 缺失场景；远征创建 E2E 尚未获得通过证据，因此 OUI-FLOW-003 保持 `[~]`。
- 证据（2026-06-02，Agent A）：`game-smoke.spec.ts` 新增 `openTechnicalDetailsForTestId()`，龙舟浏览器 smoke 会先展开折叠后的备用创建表单再断言 `online-festival-room-member-limit-group` / `online-festival-room-member-limit-8`，保留旧 test id 迁移路径；协作矿洞和龙舟房间壳奖励回看断言同步改查玩家态「奖励已记录」。
- 风险（2026-06-02，Agent A）：已重跑 `npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online festival visual track supports dragon boat cell actions"`，旧人数按钮不可见问题已越过，当前阻断变为既有 `VisualTrackBoard.vue` 缺少 `visual-track-team-standings` / 队伍名次行；全量 `test:e2e -- e2e/game-smoke.spec.ts` 仍返回非 0，最近失败还包括在线庄园 `online-manor-page` 和 NPC `npc-view` 等非本轮房间邀请组件路径。
- 证据（2026-06-04，Agent A）：收口复核确认在线节会远征标签和独立 `ExpeditionRoomView.vue` 均能打开 `OnlineRoomWizard domain="expedition"`；远征确认页仍展示 `online-room-wizard-expedition-rules` 与 `online-room-wizard-reward-preview`，创建提交继续调用原 `expeditionRoomStore.createRoom()`。
- 证据（2026-06-04，Agent A）：目标 E2E 组合中的 `online expedition room wizard creates host room` 与 `standalone expedition room reuses invite lobby and close confirm` 返回 0；`node --check taoyuan-main/e2e/game-smoke.spec.ts` 和 `npm --prefix taoyuan-main run check` 返回 0。
- 风险（2026-06-04，Agent A）：全量 E2E 未在本轮重跑；本轮关闭依据为房间目标 grep 与 OUI-EXP-001 已生成的独立远征移动端截图证据。

### OUI-FLOW-004 新建 `OnlineInvitePanel.vue`

- [x] `P0` 文件：`taoyuan-main/src/components/game/online/OnlineInvitePanel.vue`
- 依赖：OUI-CMP-003。
- 执行：支持单个用户名输入。
- 执行：支持批量粘贴，按空格、逗号、换行拆分。
- 执行：支持最近好友 / 村社成员快捷选择的 slot 或 props。
- 执行：展示已邀请、邀请中、邀请失败、已在房、不可邀请。
- 执行：支持失败项重试。
- 验收：节会、远征、村社、邻里可复用。
- 验证：E2E 覆盖邀请成功和邀请失败提示。
- 证据（2026-06-02，Agent A）：新增 `taoyuan-main/src/components/game/online/OnlineInvitePanel.vue`，复用 `OnlineBottomSheet`，支持 `open`、`domain`、`existingMembers`、`recentPlayers`、`results`、`busy`；提供 `invite`、`retry`、`remove`、`close` 事件，提交 payload 为批量 `recipients: string[]`。
- 证据（2026-06-02，Agent A）：组件提供 `online-invite-panel`、`online-invite-input`、`online-invite-submit`、`online-invite-result-list`、`online-invite-retry`、`online-invite-remove`；输入按空格、逗号、中文逗号和换行拆分，重复项去重，已在房成员显示 `already-in-room`，失败项可单独重试，提交不会清空输入草稿。
- 证据（2026-06-02，Agent A）：`taoyuan-main/scripts/qa-online-ui-structure.mjs` 已读取并断言 `OnlineInvitePanel.vue` 的必备 test id、底部抽屉复用、批量拆分、批量邀请 payload、重试 / 移除事件、已在房状态和最近玩家 slot / props；`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`node -e` 使用 `@vue/compiler-sfc` 解析并 `compileScript` 检查 `OnlineInvitePanel.vue` 与 `OnlineBottomSheet.vue` 通过。
- 证据（2026-06-02，Agent A）：`npm --prefix taoyuan-main run check` 通过，保留既有 warning（`onlineProfileApi.ts` import type、`CottageView.vue` / `FarmView.vue` floating promises）；`npm --prefix taoyuan-main run qa:online-player-copy` 通过；`npm --prefix taoyuan-main run qa:online-ui-structure` 已运行但仍被历史移动端 smoke / `VisualTrackBoard` 断言缺口阻断，失败列表未出现 `OnlineInvitePanel` 新断言。
- 证据（2026-06-02，Agent A）：OUI-FEST-002 已把 `OnlineInvitePanel` 接入 `OnlineFestivalView.vue` 的节会房和在线节会远征标签，页面层分别通过 `festivalRoomStore.inviteMember(roomId)` / `expeditionRoomStore.inviteMember(roomId)` 调用既有 store action，不改后端接口语义；旧邀请输入 test id 已保留在默认折叠备用表单。
- 证据（2026-06-02，Agent A）：`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online festival room invite panel handles success failure and retry"` 返回 0，覆盖新邀请入口、已在房识别、批量发送、失败提示和失败项重试；`npm --prefix taoyuan-main run check` 通过，保留既有 3 个 warning。
- 风险（2026-06-02，Agent A）：村社 / 邻里邀请接入属于 Agent B 或后续 P1 范围，本任务只关闭通用邀请面板能力和节会 / 远征房间主流程接入；`npm --prefix taoyuan-main run qa:online-ui-structure` 仍被既有移动端 smoke / `VisualTrackBoard` 缺口阻断，失败列表未出现 `OnlineInvitePanel` 断言。

### OUI-FLOW-005 新建 `OnlineRoomLobbyDialog.vue`

- [x] `P0` 文件：`taoyuan-main/src/components/game/online/OnlineRoomLobbyDialog.vue`
- 依赖：OUI-CMP-001、OUI-CMP-003、OUI-FLOW-004。
- 执行：显示房间名、玩法、人数、状态、成员列表。
- 执行：成员状态区分房主、已准备、未准备、离线、被邀请。
- 执行：房主按钮：邀请玩家、开始准备、开始倒计时、取消房间。
- 执行：成员按钮：接受邀请、我已准备、取消准备、离开房间。
- 执行：显示最近反馈和阻塞原因。
- 验收：准备流程不再散落页面。
- 验证：E2E 覆盖房主开始准备、成员准备、房主倒计时。
- 证据（2026-06-02，Agent A）：新增 `taoyuan-main/src/components/game/online/OnlineRoomLobbyDialog.vue`，复用 `OnlineBottomSheet` 右侧抽屉承载房间准备大厅；支持 `open`、`domain`、`room`、`currentUserId`、`busyAction`、`lastFeedback`，并提供 `invite`、`ready`、`unready`、`start-ready-check`、`start-countdown`、`settle`、`cancel-room`、`leave-room`、`accept-invite`、`close` 事件，组件本身不直接调用 store。
- 证据（2026-06-02，Agent A）：组件提供 `online-room-lobby`、`online-room-member-list`、`online-room-primary-action`、`online-room-disabled-reason`；成员列表会区分房主、已准备、未准备、离线、被邀请，房主侧按当前能力展示邀请 / 开始准备 / 开始倒计时 / 结算 / 取消房间，成员侧展示接受邀请 / 我已准备 / 取消准备 / 离开房间，并在主行动禁用时显示原因。
- 证据（2026-06-02，Agent A）：`taoyuan-main/scripts/qa-online-ui-structure.mjs` 已读取并断言 `OnlineRoomLobbyDialog.vue` 的必备 test id、底部抽屉复用、事件、成员状态区分、房主 / 成员能力判断和最近反馈；`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`@vue/compiler-sfc` 解析并 `compileScript` 检查 `OnlineRoomLobbyDialog.vue` 通过。
- 证据（2026-06-02，Agent A）：`npm --prefix taoyuan-main run check` 通过，保留既有 3 个 warning（`onlineProfileApi.ts` import type、`CottageView.vue` / `FarmView.vue` floating promises）；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（60 files、14 denylist terms、1 allowlist）。
- 风险（2026-06-02，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回非 0，失败项为既有共同庄园 / 在线庄园 / 公共订单 / 花灯墙 / 村社仓廪移动端覆盖缺口和 `VisualTrackBoard.vue` 多队名次断言，未出现 `OnlineRoomLobbyDialog` 新断言；准备大厅尚未接入 `OnlineFestivalView.vue` 或 `ExpeditionRoomView.vue`，未跑准备流程浏览器 E2E，因此 OUI-FLOW-005 保持 `[~]`。
- 证据（2026-06-04，Agent A）：收口复核确认准备大厅已接入节会房、在线节会远征标签与独立远征页；页面入口包括 `online-festival-room-lobby-trigger`、`online-expedition-room-lobby-trigger`，旧 ready / start test id 仍保留在备用操作区。
- 证据（2026-06-04，Agent A）：目标 E2E 组合中的 `online festival room member accepts invite and readies`、`online festival room lobby starts ready check and countdown`、`online expedition room invite panel and lobby start ready check`、`standalone expedition room reuses invite lobby and close confirm` 返回 0，覆盖成员接受邀请、成员准备、房主开始准备、房主倒计时和远征准备大厅入口。
- 风险（2026-06-04，Agent A）：本轮未新增单元快照；关闭依据为组件结构 QA、页面接入断言、移动端 smoke 既有截图和房间目标 E2E 组合。

### OUI-FLOW-006 房间按钮显隐矩阵

- [x] `P0` 文件：`OnlineRoomLobbyDialog.vue`、`0531拆解todo.md`
- 依赖：OUI-FLOW-005。
- 执行：实现房主状态矩阵：created、ready_check、countdown、running、settling、closed。
- 执行：实现成员状态矩阵：invited、joined、ready、running、disconnected、settled。
- 执行：禁用按钮时显示原因，不只 disabled。
- 验收：每个房间状态都有且只有一个推荐主行动。
- 验证：单元级人工状态快照或 E2E mock 多状态房间。
- 证据（2026-06-02，Agent A）：`taoyuan-main/src/components/game/online/OnlineRoomLobbyDialog.vue` 新增 `hostStateActionMatrix` 与 `memberStatusActionMatrix`，房主覆盖 `created / ready_check / countdown / running / settling / settled / closed / unknown`，成员覆盖 `invited / joined / ready / running / disconnected / settled / unknown`；`activeLobbyMatrix` 按房主 / 成员视角选择矩阵，并通过 `enabledBy`、`disabledActionReasons` 展示禁用原因。
- 证据（2026-06-02，Agent A）：一次性矩阵快照命令通过，输出 `hostStateActionMatrix created:1, ready_check:1, countdown:1, running:1, settling:1, settled:1, closed:1, unknown:1` 与 `memberStatusActionMatrix invited:1, joined:1, ready:1, running:1, disconnected:1, settled:1, unknown:1`，确认每个状态只有一个推荐主行动。
- 证据（2026-06-02，Agent A）：`taoyuan-main/scripts/qa-online-ui-structure.mjs` 新增矩阵存在、状态覆盖、单 primary、`enabledBy`、`disabledActionReasons` 和新页面承接事件断言；`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`@vue/compiler-sfc` 解析并编译 `OnlineRoomLobbyDialog.vue` 通过。
- 证据（2026-06-02，Agent A）：`npm --prefix taoyuan-main run check` 通过，保留既有 3 条 warning（`onlineProfileApi.ts` import type、`CottageView.vue` / `FarmView.vue` floating promises）；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（60 files、14 denylist terms、1 allowlist）。
- 风险（2026-06-02，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回非 0，但失败项均为既有非房间缺口（共同庄园 / 在线庄园 / 公共订单 / 花灯墙 / 村社仓廪移动端覆盖和 `VisualTrackBoard.vue` 多队名次），未出现 `OnlineRoomLobbyDialog` 或矩阵新增失败；大厅仍未接入页面，危险动作确认留给 OUI-FLOW-007 / OUI-FLOW-008。

### OUI-FLOW-007 结算确认流程

- [x] `P0` 文件：`OnlineConfirmActionDialog.vue`、`OnlineFestivalView.vue`、`ExpeditionRoomView.vue`
- 依赖：OUI-CMP-006、OUI-FLOW-005。
- 执行：结算前展示奖励预览、未完成成员、凭证结果说明。
- 执行：结算按钮执行原 store action，不改接口。
- 执行：结算失败时保留房间并可重试。
- 验收：结算不再是页面里的裸按钮。
- 验证：E2E 覆盖结算确认和结果回看。
- 证据（2026-06-02，Agent A）：`taoyuan-main/src/views/game/online/OnlineFestivalView.vue` 已将节会房大厅 `@settle` 和旧 `online-festival-room-settle-submit` 入口改为打开 `OnlineConfirmActionDialog`；新增 `online-room-settle-confirm` 包装钩子，弹窗展示房间状态、参与成员、未完成成员、奖励预览 / 结算记录 fallback 和失败恢复提示。
- 证据（2026-06-02，Agent A）：确认结算后调用既有 `festivalRoomStore.settleRoomAction(roomId)`，不改接口语义；失败时保留确认弹窗并读回 store `errorMessage`，房主可重试。`taoyuan-main/e2e/game-smoke.spec.ts` 的灯会结算 smoke 已迁移为「旧 test id → 结算确认 → 确认按钮 → 回看」路径。
- 证据（2026-06-02，Agent A）：`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`npm --prefix taoyuan-main run check` 通过，保留既有 3 条 warning；`npm --prefix taoyuan-main run qa:online-player-copy` 通过；`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online festival visual scene supports lantern object actions"` 通过。
- 风险（2026-06-02，Agent A）：本轮只接入在线节会房结算确认；`ExpeditionRoomView.vue`、在线节会远征标签和完整结算 E2E 尚未接入，因此任务保持 `[~]`。
- 证据（2026-06-04，Agent A）：收口复核确认节会房、在线节会远征标签和独立远征页的结算入口均先打开 `OnlineConfirmActionDialog`；节会统一钩子为 `online-room-settle-confirm`，在线节会远征钩子为 `online-expedition-room-settle-confirm`，独立远征确认继续由 `ExpeditionRoomView.vue` 承接，确认后仍调用原 `festivalRoomStore.settleRoomAction(roomId)` / `expeditionRoomStore.settleRoomAction(roomId)`。
- 证据（2026-06-04，Agent A）：目标 E2E 中 `online festival room settle confirm shows impacts and records receipt` 返回 0；补跑 `online expedition visual map supports cavern node actions` 返回 0，覆盖 `online-expedition-room-shell-settle-submit` 打开 `online-expedition-room-settle-confirm`、提交原 `/settle` 接口并读回远征结算回看。`qa-online-ui-structure.mjs` 已保护远征结算确认 test id 和原 store action 调用。
- 风险（2026-06-04，Agent A）：全量结构 QA 仍有非 Agent A 移动端覆盖缺口；未出现结算确认相关新增失败。

### OUI-FLOW-008 关闭 / 取消房间确认流程

- [x] `P0` 文件：`OnlineConfirmActionDialog.vue`、`OnlineFestivalView.vue`、`ExpeditionRoomView.vue`
- 依赖：OUI-CMP-006、OUI-FLOW-005。
- 执行：关闭前展示房间状态、成员影响、是否可撤销。
- 执行：进行中关闭需要二次确认文字。
- 执行：关闭失败展示原因和刷新入口。
- 验收：关闭 / 取消房间不再是裸按钮。
- 验证：E2E 覆盖关闭确认。
- 证据（2026-06-02，Agent A）：`OnlineFestivalView.vue` 已将节会房大厅 `@cancel-room` 和旧 `online-festival-room-close-submit` 入口改为打开 `OnlineConfirmActionDialog`；新增 `online-room-close-confirm` 包装钩子，弹窗展示房间状态、影响成员、已准备 / 已参与成员、奖励影响和失败恢复提示。
- 证据（2026-06-02，Agent A）：进行中节会房关闭会要求输入「确认关闭房间」，确认后调用既有 `festivalRoomStore.closeRoomAction(roomId)`，失败时保留弹窗并可重试；`taoyuan-main/scripts/qa-online-ui-structure.mjs` 已增加关闭确认、确认文字和原 store action 调用断言。
- 证据（2026-06-02，Agent A）：`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-player-copy`、`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 均已运行；其中 mobile smoke 返回 0，但有本地后端 `127.0.0.1:4013` 代理拒绝警告。
- 证据（2026-06-03，Agent A）：`game-smoke.spec.ts` 新增节会关闭确认浏览器 E2E，覆盖旧 `online-festival-room-close-submit` 入口、`online-room-close-confirm` 危险确认、Esc 取消、焦点返回、确认文字门槛和既有 `/api/taoyuan/online/festival/rooms/*/close` 提交。
- 风险（2026-06-03，Agent A）：节会房关闭确认已有浏览器证据；远征房关闭确认尚未接入，因此 OUI-FLOW-008 仍保持 `[~]`。
- 证据（2026-06-04，Agent A）：收口复核确认节会房、在线节会远征标签和独立远征页关闭 / 取消房间均先进入 `OnlineConfirmActionDialog`；节会钩子 `online-room-close-confirm`、在线节会远征钩子 `online-expedition-room-close-confirm`、独立远征钩子 `expedition-room-close-confirm` 均有浏览器断言，进行中关闭仍要求确认文字。
- 证据（2026-06-04，Agent A）：目标 E2E 组合中的 `online festival room close confirm requires text escape focus and submits`、`online expedition room close confirm requires text escape focus and submits`、`standalone expedition room reuses invite lobby and close confirm` 返回 0，覆盖危险确认文字门槛、Esc 取消、焦点返回和原 close action 提交。
- 风险（2026-06-04，Agent A）：关闭失败态仍依赖页面读取 store `errorMessage` 保留弹窗；本轮未改后端状态机或接口语义。

## 6. M3 在线中心与玩家文案

### OUI-CENTER-001 在线中心 Hero 行动区

- [x] `P0` 文件：`taoyuan-main/src/views/game/OnlineView.vue`
- 依赖：OUI-CMP-004、OUI-CMP-008。
- 执行：新增 `online-center-hero-actions`。
- 执行：派生主 CTA：继续当前房间、处理邀请、创建房间、查看村社待办、接力委托。
- 执行：首屏展示在线状态摘要和最多 3 个行动。
- 验收：玩家进入在线中心 3 秒内知道下一步。
- 验证：移动端 smoke 检查主 CTA 在首屏。
- 证据（2026-06-03，Agent B）：`OnlineView.vue` 新增 `data-testid="online-center-hero-actions"`、`online-center-status-summary` 和 `online-center-hero-action-*`，首屏状态摘要覆盖活动房间、村社待办、接力委托；主行动按继续当前房间、处理邀请、创建活动房间、查看村社待办、接力委托派生，并通过 `slice(0, 3)` 限制最多 3 个行动。
- 证据（2026-06-03，Agent B）：在线中心接入 `OnlineStatusBanner` 承接摘要刷新失败，并接入 `OnlineStickyActionBar`，移动端固定主行动复用同一 action 模型；点击 sticky 主 / 次行动只走前端路由跳转，不改 store action、接口或后端状态机。
- 证据（2026-06-03，Agent B）：`taoyuan-main/scripts/qa-mobile-ui-smoke.mjs` 将在线中心 390x844 / 360x780 场景主选择器改为 `online-center-hero-actions`，新增 hero action 数量 1-3、首个行动在视口内、`online-sticky-primary-action` 在视口内断言；`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs` 通过。
- 证据（2026-06-03，Agent B）：`npm --prefix taoyuan-main run check` 通过，保留既有 3 条 warning；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（60 files / 14 denylist / 1 allowlist）；`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，生成 `docs/ui-smoke-2026-04-26/24-online-center-mobile-390x844.png` 和 `26-online-center-mobile-360x780.png`，期间仅有本地后端 `127.0.0.1:4013` 代理拒绝警告。
- 风险（2026-06-03，Agent B）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍因 OUI-000 已记录的移动端共同庄园 / 在线庄园 / 公共订单 / 花灯墙 / 村社仓廪覆盖和 `VisualTrackBoard.vue` 多队名次等既有缺口返回非 0，未出现在线中心新增钩子失败；`OUI-CMP-004` / `OUI-CMP-008` 在 todo 中仍保持 `[~]`，但本轮依赖的组件文件已存在且在线中心移动端 smoke 已通过。

### OUI-CENTER-002 治理信息折叠

- [x] `P0` 文件：`OnlineView.vue`、`OnlineTechnicalDetails.vue`
- 依赖：OUI-CMP-005、OUI-CENTER-001。
- 执行：可视化功能开关迁入「调试与治理信息」。
- 执行：奖励与投放控制迁入折叠区。
- 执行：活动排期只保留玩家可参与信息。
- 验收：首屏不再展示功能开关、降级、缺失、服务端凭证。
- 验证：文案扫描和人工首屏截图。
- 证据（2026-06-03，Agent B）：`OnlineView.vue` 复用已完成的 `OnlineTechnicalDetails`，将 `online-visual-feature-flag-panel` 放入 `online-center-governance-details` 折叠区，将 `online-visual-reward-control-panel` 放入第二个「调试与治理信息：奖励与投放」折叠区；两个旧 test id 均保留，未改 store action、接口字段或后端状态机。
- 证据（2026-06-03，Agent B）：活动入口和排期卡不再在玩家路径拼出 `fallbackStatus/fallbackLabel`，可见文案改为「备用入口可用：进入后按原页面继续操作。」和「可从备用入口参加。」；奖励池、结算口径和投放限制只在折叠技术详情内展示。
- 证据（2026-06-03，Agent B）：`npm --prefix taoyuan-main run qa:online-visual-feature-flags` 通过；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（60 files / 14 denylist / 1 allowlist）；`npm --prefix taoyuan-main run check` 通过，保留既有 3 条 warning。
- 证据（2026-06-03，Agent B）：Playwright 1366x768 打开 `/#/game/online` 后确认 `online-center-hero-actions`、`online-center-module-entry-group` 可见，`online-visual-feature-flag-panel`、`online-visual-reward-control-panel` 默认隐藏，并生成本地截图 `docs/ui-smoke-2026-04-26/27-online-center-desktop-1366x768.png`（该目录按 `.gitignore` 作为本地 smoke 证据）。
- 风险（2026-06-03，Agent B）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍因 OUI-000 已记录的共同庄园 / 在线庄园 / 公共订单 / 花灯墙 / 村社仓廪移动端 smoke 覆盖和 `VisualTrackBoard.vue` 多队名次等既有缺口返回非 0，未出现在线中心新增折叠断言失败。

### OUI-CENTER-003 模块卡降噪

- [x] `P1` 文件：`OnlineView.vue`、`OnlineModuleCard.vue`
- 依赖：OUI-CENTER-001。
- 执行：模块卡从首屏主角降为「常用入口」。
- 执行：每张卡只保留标题、状态、一个主入口。
- 执行：详细 stats 进入抽屉或二级页。
- 验收：桌面端不再 `xl:grid-cols-6` 密集平铺全部信息。
- 验证：1366x768 截图。
- 证据（2026-06-03，Agent B）：`OnlineView.vue` 将原 `xl:grid-cols-6` 等权模块网格替换为 `data-testid="online-center-module-entry-group"` 的「常用入口」分区，桌面端网格收敛为 `xl:grid-cols-3`，首屏主角保留给 hero 行动区和状态摘要。
- 证据（2026-06-03，Agent B）：`OnlineModuleCard.vue` 移除卡片内 stats 网格展示，卡片高度从 `min-h-[176px]` 降为 `min-h-[132px]`，可见内容只保留图标标题、状态 / 错误和一个「进入」主入口；新增 `online-module-${moduleKey}-status` 便于 QA 定位状态文本，旧模块入口 test id 保留。
- 证据（2026-06-03，Agent B）：Playwright 1366x768 桌面截图已生成到 `docs/ui-smoke-2026-04-26/27-online-center-desktop-1366x768.png`；`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，期间仅有本地后端 `127.0.0.1:4013` 代理拒绝警告。
- 风险（2026-06-03，Agent B）：模块详细数字现在进入各模块二级页查看，本轮未新增单独抽屉；本地截图目录被 `.gitignore` 忽略，后续 PR 收尾如需附件可重新生成同路径截图。

### OUI-COPY-001 第一批文案替换

- [x] `P0` 文件：`OnlineView.vue`、`OnlineFestivalView.vue`、`ExpeditionRoomView.vue`、`OnlineVisualRoomShell.vue`
- 依赖：OUI-002、OUI-CMP-005。
- 执行：`开始 ready` 改「开始准备」。
- 执行：`服务端状态冲突` 改「房间信息有更新，请刷新后继续」。
- 执行：`服务端落账` 改「奖励已记录」。
- 执行：`降级入口` 改「备用操作」。
- 执行：`per-member receipt` 改「成员结算记录」。
- 验收：玩家主路径没有第一批禁用词。
- 验证：`node scripts/qa-online-player-copy.mjs`。
- 证据（2026-06-02，Agent B）：`OnlineView.vue` 的「降级入口」改为「备用操作」；`OnlineVisualRoomShell.vue` 默认文案改为旧按钮备用操作；`ExpeditionRoomView.vue` 和 `OnlineFestivalView.vue` 将「开始 ready」改为「开始准备」、`per-member receipt` 改为「成员结算记录」、「服务端落账」改为「奖励已记录」，并把结算兜底说明改为玩家态「结算记录已生成」。
- 证据（2026-06-02，Agent B）：`taoyuan-main/scripts/qa-online-ui-structure.mjs` 已同步旧按钮备用操作和奖励记录断言；`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回非 0，但本轮新增的「降级入口 / 服务端落账」断言漂移已消失，剩余为 OUI-000 已记录的移动端 smoke、`VisualTrackBoard` 和村社仓廪缺口。
- 风险（2026-06-02，Agent B）：历史 warn-only 命中已解除；共同庄园审计文本和旧 `FestivalView.vue` 调试按钮均已收口，当前首批文案任务的剩余风险只来自未做浏览器截图，不影响文案扫描验收。
- 证据（2026-06-02，Agent B）：接续 OUI-COHAB-006 后再次运行 `npm --prefix taoyuan-main run qa:online-player-copy`，已不再报告 `OnlineCohabitationView.vue` 的 `hash / ledger / receipt / record-only / 服务端落账` 命中；当时剩余的旧 `src/views/game/FestivalView.vue:474`「模拟断线」已在后续跨域最小收口中解除。
- 证据（2026-06-02，Agent B）：跨域最小收口旧 `FestivalView.vue` 后再次运行 `npm --prefix taoyuan-main run qa:online-player-copy`，扫描 58 个 Vue 文件全部通过，玩家主路径未再命中 0.9 denylist；`npm --prefix taoyuan-main run check` 通过，剩余 3 条既有 lint warning。
- 证据（2026-06-02，Agent B）：`taoyuan-main/package.json` 已将 `qa:online-player-copy` 切到严格模式；再次运行 `npm --prefix taoyuan-main run qa:online-player-copy` 通过，扫描 59 个 Vue 文件、14 个 denylist 词、1 条 allowlist，无玩家主路径命中。
- 证据（2026-06-02，Agent B）：`npm --prefix taoyuan-main run check` 通过，0 个 error，保留既有 3 条 lint warning；`npm --prefix taoyuan-main run qa:online-ui-structure` 仍因 OUI-000 基线中的移动端 smoke 覆盖、`VisualTrackBoard` 多队名次和村社仓廪断言缺口返回非 0。

### OUI-COPY-002 调试动作隐藏

- [x] `P0` 文件：`OnlineFestivalView.vue`、`ExpeditionRoomView.vue`
- 依赖：OUI-CMP-005。
- 执行：`模拟断线` 移入 `OnlineTechnicalDetails`。
- 执行：调试详情默认折叠。
- 执行：如果需要 QA 入口，保留 test id 但移出玩家主路径。
- 验收：普通玩家首屏和房间大厅看不到「模拟断线」。
- 验证：E2E 或文案扫描。
- 证据（2026-06-02，Agent B）：`ExpeditionRoomView.vue` 的断线测试按钮移入默认折叠 `OnlineTechnicalDetails`，新增 `data-testid="expedition-room-disconnect-submit"` 并继续调用原 `disconnectRoom`；`OnlineFestivalView.vue` 的节会房间壳、远征房间壳和远征备用操作区也把断线测试移入默认折叠「调试操作」。
- 证据（2026-06-02，Agent B）：`node -e` 使用 `@vue/compiler-sfc` 解析并 `compileScript` 检查 `ExpeditionRoomView.vue`、`OnlineFestivalView.vue`、`OnlineView.vue`、`OnlineVisualRoomShell.vue` 通过；文案扫描不再报告 `ExpeditionRoomView.vue` 或 `OnlineFestivalView.vue` 的「模拟断线」。
- 风险（2026-06-02，Agent B）：未运行浏览器 E2E；`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回非 0，当前卡在在线委托空态断言 `当前没有结算凭证。` 缺失，并伴随本地后端 `127.0.0.1:4013` 代理拒绝连接，不属于本轮房间文案改动。任务保持 `[~]`，等待全局文案扫描和浏览器路径补证据后关闭。
- 证据（2026-06-03，Agent B）：复核 `ExpeditionRoomView.vue` 与 `OnlineFestivalView.vue`，玩家路径已无「模拟断线」；可用调试入口均显示为「网络异常测试」并位于默认折叠 `OnlineTechnicalDetails title="调试操作"` 内，旧 `disconnectRoomAction` / `disconnectExpeditionRoom` 调用未改，`expedition-room-disconnect-submit` test id 保留。
- 证据（2026-06-03，Agent B）：`npm --prefix taoyuan-main run qa:online-player-copy` 通过，输出 `[qa-online-player-copy] passed (60 files, 14 denylist terms, 1 allowlist entries)`；严格文案扫描覆盖 `src/views/game` 与 `src/components/game/online`，未再发现玩家主路径 denylist 命中。
- 风险（2026-06-03，Agent B）：本轮只做验收回填，未改房间流程和后端状态机；浏览器 E2E 未单独运行，OUI-COPY-002 关闭依据为严格玩家文案扫描与源码复核。`VisualTrackBoard.vue` 当前有他人未提交的多队名次钩子改动，本轮未触碰也不暂存。
- 证据（2026-06-02，Agent B）：跨域最小改动旧 `FestivalView.vue`，将剩余「模拟断线」按钮移入默认折叠 `OnlineTechnicalDetails`，按钮文案改为「网络异常测试」，新增 `data-testid="festival-room-disconnect-submit"` 并继续调用原 `disconnectRoom(festivalRoomStore.myRoom.id)`；未接入或推进房间向导 / 邀请 / 准备大厅流程。
- 证据（2026-06-02，Agent B）：`node -e` 使用 `@vue/compiler-sfc` 解析并编译 `FestivalView.vue` 通过；`rg -n "模拟断线" taoyuan-main/src/views/game taoyuan-main/src/components/game/online` 无命中；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（58 files / 14 denylist / 1 allowlist）。
- 证据（2026-06-02，Agent B）：严格运行 `npm --prefix taoyuan-main run qa:online-player-copy` 通过（59 files / 14 denylist / 1 allowlist），确认调试动作旧文案不再出现在玩家文案扫描范围。
- 风险（2026-06-02，Agent B）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍因 OUI-000 既有移动端 smoke、协作矿洞浏览器 smoke、`VisualTrackBoard` 多队名次和村社仓廪断言返回非 0；本轮未运行浏览器 E2E 或 390x844 截图，因此 OUI-COPY-002 继续保持 `[~]`。

## 7. M2/M3 节会与远征页面接入

### OUI-FEST-001 节会房创建表单弹窗化

- [x] `P0` 文件：`taoyuan-main/src/views/game/online/OnlineFestivalView.vue`
- 依赖：OUI-FLOW-001、OUI-FLOW-002。
- 执行：移除或折叠页面内创建节会房长表单。
- 执行：新增「创建节会房间」按钮，打开 `OnlineRoomWizard`。
- 执行：创建成功刷新 `festivalRoomStore.refreshOverview`。
- 执行：失败保留草稿并显示错误。
- 验收：创建节会房不需要滚到页面中段。
- 验证：E2E 创建节会房。
- 证据（2026-06-02，Agent A）：`OnlineFestivalView.vue` 新增首屏 `online-room-create-trigger` 主按钮打开 `OnlineRoomWizard`；提交走 `submitFestivalRoomWizard()`，先把向导草稿同步到 `festivalRoomStore` 既有 draft 字段，再调用原 `festivalRoomStore.createRoom()`，成功后调用 `festivalRoomStore.refreshOverview({ silent: true })` 并关闭向导。
- 证据（2026-06-02，Agent A）：创建失败时 `submitFestivalRoomWizard()` 捕获错误并保持 `showFestivalRoomWizard=true`，`festivalRoomStore.errorMessage` 会传给向导 `errorMessage` 展示；旧长表单已折叠进 `OnlineTechnicalDetails` 的「备用创建表单」，保留 `online-festival-room-template-select`、`online-festival-room-member-limit-group`、`online-festival-room-gameplay-select`、`online-festival-room-title-input`、`online-festival-room-create-submit`。
- 证据（2026-06-02，Agent A）：已更新 `CHANGELOG.md` 与 `taoyuan-main/CHANGELOG.md` 的在线节会房创建向导记录；`npm --prefix taoyuan-main run qa:online-player-copy` 通过 warn-only，仍只命中既有 `ExpeditionRoomView.vue:104`「开始 ready」、`ExpeditionRoomView.vue:138`「模拟断线」、`OnlineView.vue:156`「降级入口」。
- 风险（2026-06-02，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍被 OUI-000 已记录的共同庄园 / 庄园 / 村社仓廪 / `VisualTrackBoard` 既有结构缺口阻断，失败列表未出现本轮新增的 `OnlineFestivalView.vue` 向导接入断言；`npm --prefix taoyuan-main run check` 与 `qa:mobile-ui-smoke` 仍被 Agent B 范围 `src/utils/cohabitationApi.ts` 语法错误阻断。
- 风险（2026-06-02，Agent A）：`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts` 返回非 0；`taoyuan-main/test-results/game-smoke-web-game-smoke--52cb5-nlocks-festival-room-launch-chromium/error-context.md` 显示 Vite error overlay 拦截点击，根因同为 `cohabitationApi.ts:2430 Unexpected "{"`，因此本任务保持 `[~]`，待共同庄园语法基线修复后补创建节会房 E2E。
- 证据（2026-06-04，Agent A）：收口复核确认 `OnlineFestivalView.vue` 仍使用 `online-room-create-trigger` 打开 `OnlineRoomWizard`，提交仍经 `submitFestivalRoomWizard()` 同步向导草稿后调用原 `festivalRoomStore.createRoom()` 与 `refreshOverview({ silent: true })`；旧备用创建表单和 `online-festival-room-create-submit`、模板 / 人数 / 玩法 / 标题旧 test id 继续保留在折叠区。
- 证据（2026-06-04，Agent A）：目标 E2E `online festival room wizard creates host room`、`online festival room wizard escape closes dialog and restores focus`、`online festival room wizard honors society launch query prefill` 返回 0，覆盖创建成功、Esc 焦点返回和村社完工 query 预填；过滤移动端 smoke `TAOYUAN_MOBILE_SMOKE_ONLY=online-festival-room npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，并重新生成 390x844 / 360x780 的节会房创建向导截图。
- 风险（2026-06-04，Agent A）：全量 `npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，剩余为小屋 / 农场共同庄园入口、在线庄园轻采、花灯墙、村社仓廪等非 Agent A 移动端覆盖缺口；未出现节会房创建向导相关新增失败。

### OUI-FEST-002 节会房邀请弹窗化

- [x] `P0` 文件：`OnlineFestivalView.vue`
- 依赖：OUI-FLOW-004。
- 执行：页面内邀请输入框替换为「邀请玩家」按钮。
- 执行：按钮打开 `OnlineInvitePanel`。
- 执行：提交调用原 `festivalRoomStore.inviteMember`。
- 执行：支持邀请成功和失败反馈。
- 验收：邀请流程不再占据房间主体。
- 验证：E2E 邀请玩家。
- 证据（2026-06-02，Agent A）：`taoyuan-main/src/views/game/online/OnlineFestivalView.vue` 已接入两个 `OnlineInvitePanel`，节会房新增 `online-festival-room-invite-trigger`，在线节会远征标签新增 `online-expedition-room-invite-trigger`；页面层用本地 `festivalInviteResults` / `expeditionInviteResults` 回传邀请中、已邀请、失败状态，并保留失败项重试。
- 证据（2026-06-02，Agent A）：邀请面板批量提交没有改后端或 API 语义，而是逐项写回既有 store 草稿后调用 `festivalRoomStore.inviteMember(roomId)` / `expeditionRoomStore.inviteMember(roomId)`；旧页面单人邀请入口移入 `OnlineTechnicalDetails` 的「备用邀请表单」，保留 `online-festival-room-invite-username-input`、`online-festival-room-invite-submit`、`online-expedition-room-invite-username-input`、`online-expedition-room-invite-submit`。
- 证据（2026-06-02，Agent A）：`taoyuan-main/scripts/qa-online-ui-structure.mjs` 已增加节会页引用 `OnlineInvitePanel`、两个新邀请触发 test id、四个旧邀请 test id、原 store action 调用、失败反馈结果和备用邀请表单折叠断言；`CHANGELOG.md` 与 `taoyuan-main/CHANGELOG.md` 已追加 Agent A 房间邀请接入记录。
- 证据（2026-06-02，Agent A）：`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node --check taoyuan-main/e2e/game-smoke.spec.ts`、`@vue/compiler-sfc` 解析 / `compileScript` 检查 `OnlineFestivalView.vue`、`OnlineInvitePanel.vue`、`OnlineBottomSheet.vue` 均通过；`npm --prefix taoyuan-main run check` 通过，保留既有 3 个 warning（`onlineProfileApi.ts` import type、`CottageView.vue` / `FarmView.vue` floating promises）；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（59 files、14 denylist terms、1 allowlist）。
- 证据（2026-06-02，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 已运行但仍被既有非本轮断言阻断：共同庄园 / 庄园 / 公共订单 / 花灯墙 / 村社仓廪移动端 smoke 覆盖缺口，以及 `VisualTrackBoard.vue` 多队名次 test id 缺口；失败列表未出现本轮新增的邀请面板页面接入断言。
- 风险（2026-06-02，Agent A）：`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 仍返回非 0，先出现后端代理 `127.0.0.1:4013` 拒绝连接，随后在线委托场景找不到「当前没有结算凭证。」；不属于本轮节会 / 远征邀请面板新增路径，但仍阻断移动端 smoke 关闭证据。
- 风险（2026-06-02，Agent A）：`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts` 返回非 0 且 npm 包装未捕获终端详情，`test-results/.last-run.json` 显示全量 game smoke 失败 25 项；抽查最新 error context，房间相关龙舟路径仍在邀请流程前的 `online-festival-room-my-room` 可见性等待处失败，共同庄园路径也有 `online-cohabitation-page` 等既有页面等待失败。因此本任务实现保持 `[~]`，后续仍需补邀请成功 / 失败浏览器 E2E 后才能关闭。
- 证据（2026-06-04，Agent A）：收口复核确认节会房邀请主入口 `online-festival-room-invite-trigger` 打开 `OnlineInvitePanel`，批量提交仍逐项写回既有草稿并调用原 `festivalRoomStore.inviteMember(roomId)`；旧 `online-festival-room-invite-username-input` 与 `online-festival-room-invite-submit` 保留在默认折叠备用邀请表单。
- 证据（2026-06-04，Agent A）：目标 E2E `online festival room invite panel handles success failure and retry` 返回 0，覆盖成功、失败和失败项重试；过滤移动端 smoke 返回 0，并重新生成 `36-online-festival-room-invite-panel-mobile-390x844.png` 与 `37-online-festival-room-invite-panel-mobile-360x780.png`，summary 记录无横向溢出且主行动在 viewport 内。
- 风险（2026-06-04，Agent A）：全量结构 QA 的剩余失败仍集中在非 Agent A 页面移动端覆盖缺口；节会邀请面板结构断言、旧 test id 保留断言和目标 E2E 均已通过。

### OUI-FEST-003 节会房准备大厅

- [x] `P0` 文件：`OnlineFestivalView.vue`
- 依赖：OUI-FLOW-005、OUI-FLOW-006。
- 执行：`开始准备`、`我已准备`、`取消准备`、`开始倒计时` 进入 `OnlineRoomLobbyDialog`。
- 执行：成员准备状态固定展示在大厅。
- 执行：页面主按钮只保留「打开准备大厅」或身份化主行动。
- 验收：准备流程集中，不再散在多个按钮组。
- 验证：E2E 准备流程。
- 证据（2026-06-02，Agent A）：`OnlineFestivalView.vue` 已接入 `OnlineRoomLobbyDialog`，新增 `online-festival-room-lobby-trigger` 打开节会准备大厅；页面将 `FestivalRoomSnapshot` 映射为 `festivalLobbyRoom`，并用 `festivalLobbyCurrentUserId` / `festivalLobbyCurrentMember` 给大厅提供房主或成员视角。
- 证据（2026-06-02，Agent A）：`开始准备`、`我已准备`、`取消准备`、`开始倒计时` 已从 `OnlineVisualRoomShell` 主操作区迁入准备大厅事件回调，分别调用既有 `festivalRoomStore.startReadyCheck`、`readyRoomAction`、`unreadyRoomAction`、`startCountdown`；旧 `online-festival-room-ready-check-submit`、`online-festival-room-ready-submit`、`online-festival-room-start-submit` 保留在默认折叠「备用房间操作」中。
- 证据（2026-06-02，Agent A）：`qa-online-ui-structure.mjs` 已增加节会页引用 `OnlineRoomLobbyDialog`、大厅触发按钮、room / current-user 映射、准备 / 倒计时事件回调和旧 test id 保留断言；`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过，`@vue/compiler-sfc` 解析并 `compileScript` 检查 `OnlineFestivalView.vue`、`OnlineRoomLobbyDialog.vue` 通过。
- 证据（2026-06-02，Agent A）：`npm --prefix taoyuan-main run check` 通过，保留既有 3 条 warning（`onlineProfileApi.ts` import type、`CottageView.vue` / `FarmView.vue` floating promises）；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（60 files、14 denylist terms、1 allowlist）；`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，期间仅有本地后端 `127.0.0.1:4013` 代理拒绝警告。
- 证据（2026-06-02，Agent A）：`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online festival room invite panel handles success failure and retry"` 返回 0，确认节会页现有邀请面板浏览器回归未被大厅接入破坏。
- 风险（2026-06-02，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍因既有非房间缺口返回非 0，失败项为共同庄园 / 在线庄园 / 公共订单 / 花灯墙 / 村社仓廪移动端 smoke 覆盖和 `VisualTrackBoard.vue` 多队名次断言，未出现节会准备大厅新增断言失败；本轮未改并行中的 `game-smoke.spec.ts`，仍缺「打开准备大厅 → 开始准备 / 成员准备 / 开始倒计时」完整浏览器 E2E，因此 OUI-FEST-003 保持 `[~]`。
- 证据（2026-06-04，Agent A）：收口复核确认节会房主入口 `online-festival-room-lobby-trigger` 打开 `OnlineRoomLobbyDialog`，准备、取消准备、开始准备和开始倒计时均由大厅事件回调调用原 `festivalRoomStore.startReadyCheck()`、`readyRoomAction()`、`unreadyRoomAction()`、`startCountdown()`；旧 `online-festival-room-ready-check-submit`、`ready-submit`、`start-submit` 继续保留在备用房间操作。
- 证据（2026-06-04，Agent A）：目标 E2E `online festival room member accepts invite and readies` 与 `online festival room lobby starts ready check and countdown` 返回 0，覆盖受邀成员接受邀请 / 准备以及房主开始准备 / 倒计时；过滤移动端 smoke 返回 0，并重新生成 `26-online-festival-room-lobby-mobile-390x844.png` 与 `28-online-festival-room-lobby-mobile-360x780.png`。
- 风险（2026-06-04，Agent A）：本任务关闭依据为组件结构 QA、目标 E2E 与移动端截图；全量结构 QA 仍有非 Agent A 覆盖缺口，但未出现节会准备大厅相关断言失败。

### OUI-FEST-004 节会房结算与关闭确认

- [x] `P0` 文件：`OnlineFestivalView.vue`
- 依赖：OUI-FLOW-007、OUI-FLOW-008。
- 执行：结算按钮打开确认弹窗。
- 执行：关闭 / 取消按钮打开危险确认弹窗。
- 执行：弹窗展示奖励、成员、影响说明。
- 验收：高风险动作都有确认。
- 验证：E2E 结算、关闭。
- 证据（2026-06-02，Agent A）：`OnlineFestivalView.vue` 已接入节会房结算 / 关闭确认弹窗，保留旧 `online-festival-room-settle-submit`、`online-festival-room-close-submit` test id，并让准备大厅 `settle` / `cancel-room` 事件进入同一确认流程；确认后仍调用原 `festivalRoomStore.settleRoomAction` / `closeRoomAction`。
- 证据（2026-06-02，Agent A）：`qa-online-ui-structure.mjs` 已增加 `OnlineConfirmActionDialog` 引用、`online-room-settle-confirm`、`online-room-close-confirm`、大厅事件承接、旧 test id 保留、确认文字和 E2E 确认路径断言；灯会结算浏览器 smoke 已通过确认弹窗后读回结算记录。
- 证据（2026-06-02，Agent A）：验证通过：`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`；`npm --prefix taoyuan-main run check`（0 error，3 条既有 warning）；`npm --prefix taoyuan-main run qa:online-player-copy`；`npm --prefix taoyuan-main run qa:mobile-ui-smoke`；`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online festival visual scene supports lantern object actions"`。
- 证据（2026-06-03，Agent A）：`game-smoke.spec.ts` 新增并通过 `online festival room close confirm requires text escape focus and submits`，节会房关闭确认现在和结算确认一样具备浏览器级保护；结构 QA 同步守护该用例名、Esc、焦点返回、确认文字禁用和 `/close` 提交路径。
- 风险（2026-06-03，Agent A）：节会房结算与关闭确认主路径已有自动化证据；`qa:online-ui-structure` 全量仍有既有非本轮页面 smoke 覆盖缺口，且远征标签复用房间流程仍未完成，因此 OUI-FEST-004 暂保持 `[~]`，避免把后续房间流程收尾误标完成。
- 证据（2026-06-04，Agent A）：收口复核确认 OUI-FEST-006 / OUI-EXP-001 已解除远征复用前置风险；节会房结算和关闭均先进入 `OnlineConfirmActionDialog`，统一钩子 `online-room-settle-confirm` / `online-room-close-confirm`、旧 `online-festival-room-settle-submit` / `online-festival-room-close-submit` 和原 `festivalRoomStore.settleRoomAction(roomId)` / `closeRoomAction(roomId)` 均保留。
- 证据（2026-06-04，Agent A）：目标 E2E `online festival room settle confirm shows impacts and records receipt` 与 `online festival room close confirm requires text escape focus and submits` 返回 0，覆盖影响对象、资产变化、恢复提示、结算记录读回、危险确认文字门槛、Esc 取消和焦点返回；过滤移动端 smoke 返回 0，并重新生成 390x844 / 360x780 的节会房结算确认截图。
- 风险（2026-06-04，Agent A）：全量 `qa:online-ui-structure` 仍有非 Agent A 移动端覆盖缺口；本任务关闭范围仅为节会房结算与关闭确认，未改后端状态机、接口字段或资产结算语义。

### OUI-FEST-005 节会旧动作折叠为备用操作

- [x] `P0` 文件：`OnlineFestivalView.vue`、`OnlineTechnicalDetails.vue`
- 依赖：OUI-CMP-005。
- 执行：旧玩法动作面板放入「备用操作」折叠。
- 执行：可视化入口可用时默认收起。
- 执行：保留键盘用户和 QA 可操作路径。
- 验收：主路径不再同时展示舞台和旧按钮堆。
- 验证：E2E 视觉棋盘可用时旧按钮隐藏或折叠。
- 证据（2026-06-03，Agent A）：`OnlineFestivalView.vue` 新增 `hasPrimaryFestivalVisualActions`，当节会场景物件或赛道格存在可用主行动时，旧 `online-festival-room-gameplay-action-*` 按钮不再渲染；当没有可用视觉主行动但仍有旧玩法 action 时，按钮进入 `OnlineTechnicalDetails` 的「旧节会按钮备用操作」，保留 `online-festival-room-gameplay-backup-actions` 与原 `playGameplayAction` 调用。
- 证据（2026-06-03，Agent A）：`festivalRoomFallbackActionsVisible` 现在按「有旧 action 且没有可用视觉主行动」计算；统一房间壳继续读回 `旧节会按钮备用操作`，玩家主路径不会同时看到视觉舞台和旧按钮堆。
- 证据（2026-06-03，Agent A）：`qa-online-ui-structure.mjs` 新增 `hasPrimaryFestivalVisualActions`、备用操作 test id、备用操作标题、`!hasPrimaryFestivalVisualActions.value` 和 `:default-open="true"` 断言；验证 `node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`npm --prefix taoyuan-main run check`（0 error，3 条既有 warning）、`npm --prefix taoyuan-main run qa:online-player-copy`、`npm --prefix taoyuan-main run qa:mobile-ui-smoke`（返回 0，仅本地后端 `127.0.0.1:4013` 代理拒绝警告）、`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online festival visual scene supports lantern object actions|online festival visual scene supports laba cookpot object actions"` 通过。
- 证据（2026-06-03，Agent A）：`VisualTrackBoard.vue` 已为龙舟多队榜单补齐 `visual-track-team-standings`、`visual-track-team-row-${team_id}` 和「第 N 名」现场名次展示；`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online festival visual track supports dragon boat cell actions"` 返回 0，龙舟赛道可视化主行动路径已能覆盖旧玩法按钮折叠后的轨道回归。
- 证据（2026-06-03，Agent A）：验证 `node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`npm --prefix taoyuan-main run check`（0 error，3 条既有 warning）、`npm --prefix taoyuan-main run qa:online-player-copy`、`npm --prefix taoyuan-main run qa:mobile-ui-smoke`（返回 0，仅本地后端 `127.0.0.1:4013` 代理拒绝警告）通过；`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回非 0，但失败列表已不再包含 `VisualTrackBoard.vue` 多队名次 / test id 缺口。
- 风险（2026-06-03，Agent A）：全量结构 QA 剩余失败仍集中在既有共同庄园 / 在线庄园 / 公共订单 / 花灯墙 / 村社仓廪移动端 smoke 覆盖，不属于本轮节会旧动作折叠和龙舟赛道名次收口；OUI-FEST-005 已具备关闭证据。

### OUI-FEST-006 远征标签复用房间流程

- [x] `P0` 文件：`OnlineFestivalView.vue`
- 依赖：OUI-FLOW-001、OUI-FLOW-003、OUI-FLOW-005。
- 执行：远征创建、邀请、准备、结算、关闭全部复用房间流程组件。
- 执行：和节会房保持相同按钮行为。
- 验收：节会标签和远征标签体验一致。
- 验证：E2E 远征房完整路径。
- 证据（2026-06-04，Agent A）：`OnlineFestivalView.vue` 在线节会远征标签已复用 `OnlineRoomWizard`、`OnlineInvitePanel`、`OnlineRoomLobbyDialog` 和 `OnlineConfirmActionDialog`；新增 / 保留 `online-expedition-room-lobby-trigger`、`online-expedition-room-lobby-backup-actions`、`online-expedition-room-settle-confirm`、`online-expedition-room-close-confirm`、旧 `online-expedition-room-ready-check-submit` / `ready-submit` / `start-submit` / `settle-submit` / `close-submit` 等 test id，结算和关闭仍调用原 `expeditionRoomStore.settleRoomAction(roomId)` / `closeRoomAction(roomId)`。
- 证据（2026-06-04，Agent A）：`game-smoke.spec.ts` 补齐远征 `/ready-check`、`/close` 浏览器 mock，新增 `online expedition room invite panel and lobby start ready check` 与 `online expedition room close confirm requires text escape focus and submits`，并把远征创建用例改为断言统一准备大厅主入口；`qa-online-ui-structure.mjs` 新增远征大厅、邀请、结算、关闭和 E2E 保护断言。
- 证据（2026-06-04，Agent A）：`node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`npm --prefix taoyuan-main run check` 通过（0 error，3 条既有 warning）；`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online expedition" --reporter=line` 返回 0，覆盖远征创建、邀请 / 准备大厅、视觉结算确认和关闭确认。
- 风险（2026-06-04，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回非 0，失败项均为 `qa-mobile-ui-smoke.mjs` 中小屋 / 农场共同庄园入口、在线庄园轻采、花灯墙、村社仓廪等非 Agent A 覆盖缺口；未出现远征标签新增断言失败。独立 `ExpeditionRoomView.vue` 复用相同流程仍留给 OUI-EXP-001。

### OUI-EXP-001 独立远征页复用房间流程

- [x] `P0` 文件：`taoyuan-main/src/views/game/ExpeditionRoomView.vue`
- 依赖：OUI-FEST-006。
- 执行：独立页创建、邀请、准备、结算、关闭复用相同组件。
- 执行：标题改成「远征大厅」语义。
- 执行：最近 receipt 改为「最近结算」并默认隐藏长 ID。
- 验收：独立远征页和在线节会远征标签一致。
- 验证：移动端 smoke 覆盖独立远征页。
- 证据（2026-06-04，Agent A）：`ExpeditionRoomView.vue` 新增页面级 `expedition-room-page`，标题改为「联机远征大厅」/「我的远征大厅」，创建主入口复用 `OnlineRoomWizard`，邀请主入口复用 `OnlineInvitePanel`，准备 / ready / 倒计时主流程复用 `OnlineRoomLobbyDialog`，结算和关闭复用 `OnlineConfirmActionDialog`；旧 `expedition-room-create-submit`、`expedition-room-invite-submit`、`expedition-room-ready-check-submit`、`expedition-room-ready-submit`、`expedition-room-start-submit`、`expedition-room-settle-submit`、`expedition-room-close-submit` 保留在备用创建 / 邀请 / 房间操作内。
- 证据（2026-06-04，Agent A）：`ExpeditionRoomView.vue` 结算确认仍调用 `expeditionRoomStore.settleRoomAction(roomId)`，关闭确认仍调用 `expeditionRoomStore.closeRoomAction(roomId)`；`taoyuan-main/src/router/index.ts` 新增 `/game/expedition-room` 直达路由用于独立页 smoke，保留 `/game/expedition` 既有重定向不变。`qa-online-ui-structure.mjs` 已补独立远征页组件、旧 test id、store action、移动 smoke 和 standalone E2E 断言。
- 证据（2026-06-04，Agent A）：`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node --check taoyuan-main/e2e/game-smoke.spec.ts`、`npm --prefix taoyuan-main run check` 通过；`TAOYUAN_E2E_PORT=57044 TAOYUAN_MOBILE_SMOKE_ONLY=online-expedition-room npm --prefix taoyuan-main run qa:mobile-ui-smoke` 非沙箱复跑返回 0，生成 `docs/ui-smoke-2026-04-26/46-online-expedition-room-main-mobile-390x844.png` 与 `docs/ui-smoke-2026-04-26/47-online-expedition-room-main-mobile-360x780.png`；`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "standalone expedition room" --reporter=line` 返回 0。
- 风险（2026-06-04，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回非 0，失败项集中在小屋 / 农场共同庄园入口、在线庄园轻采、花灯墙、村社仓廪等非 Agent A 移动端覆盖缺口；未出现独立远征新增断言失败。移动端 smoke 期间仍有本地后端 `127.0.0.1:4013` ws proxy 拒连日志，但命令返回 0 且截图已生成。

### OUI-EXP-002 远征技术文案收口

- [x] `P0` 文件：`ExpeditionRoomView.vue`
- 依赖：OUI-CMP-005、OUI-COPY-001。
- 执行：`降级玩法动作` 改「备用操作」。
- 执行：`服务端房间状态存在冲突` 改玩家态文案。
- 执行：`per-member receipt` 改「成员结算记录」。
- 验收：远征主路径没有开发态词。
- 验证：文案扫描。
- 证据（2026-06-04，Agent A）：`ExpeditionRoomView.vue` 将玩家可见「降级玩法动作」改为「备用玩法动作」，将房间状态冲突错误改为「房间信息有更新，请刷新后继续。」；本文件未再命中 `per-member receipt`，最近结算区域只展示玩家态房间、路线、风险峰值和奖励摘要，不直接暴露长 ID。
- 证据（2026-06-04，Agent A）：`npm --prefix taoyuan-main run qa:online-player-copy` 通过，输出 `[qa-online-player-copy] passed (67 files, 14 denylist terms, 1 allowlist entries)`；`qa-online-ui-structure.mjs` 已加入 `降级玩法动作` / `服务端房间状态存在冲突` 的独立页反向断言。
- 风险（2026-06-04，Agent A）：`ExpeditionRoomView.vue` 模板仍向 `VisualMapBoard` 传入 `revision` prop，属于技术字段绑定而非玩家可见文案；文案扫描未命中玩家主路径 denylist。
- 证据（2026-06-05，Agent A）：当前态复跑 `npm --prefix taoyuan-main run qa:online-player-copy` 返回 0，输出 `[qa-online-player-copy] passed (67 files, 14 denylist terms, 1 allowlist entries)`；定向搜索 `降级玩法动作|服务端房间状态存在冲突|per-member receipt|开始 ready|模拟断线|降级入口` 未在节会 / 远征房间主流程组件中命中，仍只保留玩家态「备用玩法动作」与「房间信息有更新，请刷新后继续。」。
- 风险（2026-06-05，Agent A）：宽泛 denylist 搜索仍会命中 `fallback`、`receipt`、`revision` 等代码变量、组件 prop、测试钩子或结算数据字段；本轮未改这些技术字段，依据严格玩家文案扫描和结构反向断言判断未外露到玩家主路径。

## 8. M4 共同庄园

### OUI-COHAB-001 标签分组

- [x] `P0` 文件：`taoyuan-main/src/views/game/online/OnlineCohabitationView.vue`
- 依赖：OUI-CMP-009。
- 执行：12 个 tab 重组为「总览」「共同经营」「家庭与节会」「资产与治理」「更多」。
- 执行：保留旧 query 到新分组的映射。
- 执行：默认只显示总览、地图、仓库、基金、节会。
- 验收：玩家不再看到 12 个并列标签。
- 验证：路由 query 兼容检查。
- 证据（2026-06-02，Agent B）：`OnlineCohabitationView.vue` 将共同庄园顶层导航改为「总览 / 共同经营 / 家庭与节会 / 资产与治理 / 更多」五组，默认常用入口只显示 `overview`、`map`、`warehouse`、`fund`、`festivalSeats`；`permissions`、`offline`、`buildings`、`relations`、`visibility`、`orders`、`reputation` 进入折叠「更多分组入口」，旧 `online-module-tab-*` test id 仍保留在常用或折叠入口中。
- 证据（2026-06-02，Agent B）：新增 `resolveCohabitationTabKey()`，保留现有真实 tab query，同时把旧 query `festival` → `festivalSeats`、`public` → `visibility`、`family` → `relations`、`separation` → `offline`，并支持五组导航 query 回落到对应默认 tab。
- 证据（2026-06-02，Agent B）：`game-smoke.spec.ts` 新增 `openCohabitationTab()`，让旧权限 / 离线 test id 在折叠分组中仍可定位；新增 `online cohabitation keeps legacy tab query aliases grouped` 覆盖 `tab=festival/public/separation`。验证 `node taoyuan-main/scripts/run-e2e.mjs --grep "online cohabitation keeps legacy tab query aliases grouped" --reporter=line`、`--grep "online cohabitation permissions support low-risk toggle actions"`、`--grep "online cohabitation shared audit details keep technical fields folded"` 均返回 0。
- 证据（2026-06-02，Agent B）：`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`npm --prefix taoyuan-main run check` 通过，0 error、3 条既有 lint warning；`npm --prefix taoyuan-main run qa:online-player-copy` 通过，输出 60 files / 14 denylist / 1 allowlist；`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 通过，期间仅有本地后端 `127.0.0.1:4013` 代理拒绝警告。
- 证据（2026-06-05，Agent B）：修复共同庄园旧 query alias 在异步刷新后回到总览的问题；`OnlineCohabitationView.vue` 在 setup / route / refresh / 契约切换路径重放显式 `tab`，并给公开面板首帧缺失 `summary` / `governance` 时补空值保护；`game-smoke.spec.ts` 保留 `public` → `visibility`、`separation` → `offline`、`festival` → `festivalSeats` 的浏览器断言。提权复跑 `TAOYUAN_E2E_PORT=58247 TAOYUAN_DEV_SERVER_SETTLE_MS=30000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online cohabitation keeps legacy tab query aliases grouped" --reporter=line --workers=1 --timeout=120000` 返回 0，1 passed。
- 风险（2026-06-02，Agent B）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍因 OUI-000 既有移动端 smoke 覆盖、`VisualTrackBoard` 多队名次和村社仓廪覆盖缺口返回非 0，失败列表未出现共同庄园标签分组新增断言；本轮未做 `OUI-COHAB-002` 总览首屏卡片数量收口。

### OUI-COHAB-002 总览首屏降噪

- [x] `P0` 文件：`OnlineCohabitationView.vue`
- 依赖：OUI-COHAB-001。
- 执行：总览只展示契约状态、共同基金摘要、仓库摘要、今日建议、风险待办。
- 执行：审计、权限、离线细节进入折叠或更多。
- 验收：总览首屏不超过 4 张主卡。
- 验证：390x844 和 1366x768 截图。
- 证据（2026-06-02，Agent B）：`OnlineCohabitationView.vue` 移除共同庄园模块头部旧 6 项统计条，总览内容区改为 4 张主卡：`online-cohabitation-overview-contract-card`、`online-cohabitation-overview-fund-card`、`online-cohabitation-overview-warehouse-card`、`online-cohabitation-overview-risk-card`；审计、契约表单、宠物照料、恢复和安全边界保留在默认折叠的 `online-cohabitation-overview-details` 内，旧 test id 不删除。
- 证据（2026-06-02，Agent B）：`game-smoke.spec.ts` 新增 `online cohabitation overview keeps first screen to four main cards`，并让旧宠物照料回归先展开 `online-cohabitation-overview-details` 后再定位 `online-cohabitation-shared-pets-panel`；两条 E2E grep 均返回 0。
- 证据（2026-06-02，Agent B）：截图验证已生成并人工查看：`docs/ui-smoke-2026-06-02/online-cohabitation-overview-390x844.png`、`docs/ui-smoke-2026-06-02/online-cohabitation-overview-1366x768.png`，两档视口均显示 4 张总览主卡和默认折叠详情入口。
- 证据（2026-06-02，Agent B）：`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`npm --prefix taoyuan-main run check` 通过，0 error、3 条既有 lint warning；`npm --prefix taoyuan-main run qa:online-player-copy` 通过；`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，期间仅有本地后端 `127.0.0.1:4013` 代理拒绝警告。
- 风险（2026-06-02，Agent B）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍因 OUI-000 历史移动端 smoke、`VisualTrackBoard` 多队名次和村社仓廪基线缺口返回非 0，失败列表未出现共同庄园总览新增项。

### OUI-COHAB-003 家族节会高风险动作弹窗化

- [x] `P0` 文件：`OnlineCohabitationView.vue`
- 依赖：OUI-CMP-006。
- 执行：锁席、开房、供品、结算全部使用确认弹窗。
- 执行：弹窗展示影响对象、资产变化、失败原因。
- 执行：开房动作可接 `OnlineRoomWizard` 预填节会模板。
- 验收：四个按钮不再裸露执行。
- 验证：E2E 或移动端 smoke 覆盖至少一个家族节会确认。
- 证据（2026-06-03，Agent B）：`OnlineCohabitationView.vue` 将家族节会席位的锁席、开房、供品、结算四个按钮改为 `OnlineConfirmActionDialog` 触发，新增 `online-cohabitation-family-festival-*-confirm-trigger` test id；确认弹窗展示共同庄园、节会模板、影响席位、失败原因、资产变化和恢复提示，确认文字为「确认家族节会」。
- 证据（2026-06-03，Agent B）：开房确认继续调用原 `createFamilyFestivalRoom` store action，保留 `template_id` 与房间标题作为模板预填上下文；本轮未触碰 `OnlineRoomWizard` 房间向导文件，避免跨 Agent A 范围重构。
- 证据（2026-06-03，Agent B）：`game-smoke.spec.ts` 新增 `online cohabitation family festival actions require confirm dialog`，mock 家族节会席位面板和四个 action endpoint；实际确认锁席一次，并断言开房、供品、结算都会先出现确认弹窗。
- 证据（2026-06-03，Agent B）：`npm --prefix taoyuan-main run check` 通过；`node node_modules/playwright/cli.js test e2e/game-smoke.spec.ts --grep "family festival actions require confirm dialog" --reporter=line` 通过；`node node_modules/playwright/cli.js test e2e/game-smoke.spec.ts --grep "fund and warehouse high risk" --reporter=line` 通过；`npm --prefix taoyuan-main run qa:online-player-copy` 通过。
- 风险（2026-06-03，Agent B）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回非 0，失败项集中在 `scripts/qa-mobile-ui-smoke.mjs` 既有移动端覆盖缺口（小屋 / 农场共同庄园入口、在线庄园轻采、公共订单、花灯墙、村社仓廪等）；本轮新增家族节会确认 E2E 已通过。工作区中 `taoyuan-main/scripts/qa-mobile-ui-smoke.mjs` 有并行未提交差异，本轮不暂存不覆盖。

### OUI-COHAB-004 基金高风险确认

- [x] `P0` 文件：`OnlineCohabitationView.vue`
- 依赖：OUI-CMP-006。
- 执行：大额基金草案创建、执行扣款、高风险回执记录使用确认弹窗。
- 执行：要求确认文字，例如「确认消耗共同资产」。
- 执行：说明是否影响个人铜币、共同基金、共同仓库。
- 验收：基金高风险动作都有确认和影响说明。
- 验证：E2E / 人工检查。
- 证据（2026-06-02，Agent B）：`OnlineCohabitationView.vue` 已接入页面级 `OnlineConfirmActionDialog`，大额基金草案创建、成员确认、执行扣款和高风险回执提交都会先进入确认弹窗；基金确认要求输入「确认消耗共同资产」，弹窗列出共同庄园、用途、目标、金额以及个人铜币 / 共同基金 / 共同仓库影响。
- 证据（2026-06-02，Agent B）：`game-smoke.spec.ts` 新增 `online cohabitation fund and warehouse high risk actions require confirm dialog`，mock 待确认、可扣款、待回执三类基金草案，断言确认文字未填时 `online-confirm-action-dialog-confirm` 禁用，填入确认文案后才继续调用原 store action。
- 证据（2026-06-02，Agent B）：`npm --prefix taoyuan-main run check` 通过（0 error，3 条既有 warning）；`npm --prefix taoyuan-main run qa:online-player-copy` 通过；`node node_modules/playwright/cli.js test e2e/game-smoke.spec.ts --grep "fund and warehouse high risk" --reporter=line` 通过，输出 `1 passed`；`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，仅有本地后端 `127.0.0.1:4013` 代理拒绝警告。
- 风险（2026-06-02，Agent B）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍因 OUI-000 既有移动端 smoke 覆盖、`VisualTrackBoard` 多队名次和村社仓廪覆盖缺口返回非 0，失败列表未出现共同庄园基金确认新增失败。

### OUI-COHAB-005 仓库高价值取用确认

- [x] `P0` 文件：`OnlineCohabitationView.vue`
- 依赖：OUI-CMP-006。
- 执行：高价值取用草案、确认、执行进入确认弹窗。
- 执行：说明消耗物品、目标用途、是否需要另一位成员确认。
- 验收：仓库高价值动作不裸露。
- 验证：人工检查。
- 证据（2026-06-02，Agent B）：`OnlineCohabitationView.vue` 的高价值取用草案创建、成员确认和执行按钮均改为先打开 `OnlineConfirmActionDialog`；仓库确认要求输入「确认取用共同资产」，弹窗展示物品、数量、目标用途、成员确认状态、共同仓库冻结 / 扣除、个人背包和共同基金影响。
- 证据（2026-06-02，Agent B）：`game-smoke.spec.ts` 同一新增 E2E 覆盖高价值仓库申请、确认和执行三条入口，保留旧 `online-cohabitation-warehouse-high-value-*` test id，并断言确认文字门槛、影响对象、个人背包与共同基金边界。
- 证据（2026-06-02，Agent B）：`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-player-copy`、`node node_modules/playwright/cli.js test e2e/game-smoke.spec.ts --grep "fund and warehouse high risk" --reporter=line`、`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 已运行；新增 E2E 通过，mobile smoke 返回 0。
- 风险（2026-06-02，Agent B）：本轮未改后端高价值取用状态机或补偿 / 回滚语义；`qa:online-ui-structure` 仍保留既有基线失败，但未出现仓库高价值确认相关新增失败。

### OUI-COHAB-006 审计详情拆层

- [x] `P0` 文件：`OnlineCohabitationView.vue`、`OnlineTechnicalDetails.vue`
- 依赖：OUI-CMP-005、OUI-COPY-001。
- 执行：`formatSharedAuditDetail` 拆成玩家摘要和技术详情。
- 执行：玩家摘要不输出 `ledger`、`revision`、`receipt`、`idempotent`。
- 执行：技术详情保留完整字段。
- 验收：玩家可读，QA 可追踪。
- 验证：文案扫描和技术详情展开检查。
- 证据（2026-06-02，Agent B）：`OnlineCohabitationView.vue` 已接入 `OnlineTechnicalDetails`，共同日志每条仍显示玩家摘要，同时新增默认折叠 `online-cohabitation-shared-audit-technical-detail`，保留 `entry.action`、`entry.id` 和完整 `entry.detail` JSON；安全版本快照也改为玩家态「凭证校验码」，原始 `snapshot_hash` 放入默认折叠技术详情。
- 证据（2026-06-02，Agent B）：共同庄园主路径可见文案已把 `hash / ledger / receipt / record-only / revision / idempotent / 服务端落账` 相关表达改为「确认码 / 记录明细 / 回执 / 仅记录本次结果 / 本地版本 / 重复提交保护 / 系统记录入仓」；同时修复 `RotateCcw,` 漂到模板可见文本且缺少 import 的问题。
- 证据（2026-06-02，Agent B）：`node -e` 使用 `@vue/compiler-sfc` 解析并编译 `OnlineCohabitationView.vue` 通过；`npm --prefix taoyuan-main run qa:online-player-copy` 返回 0（warn-only），当前只剩 `src/views/game/FestivalView.vue:474` 的「模拟断线」，不再报告 `OnlineCohabitationView.vue`。
- 证据（2026-06-02，Agent B）：`npm --prefix taoyuan-main run check` 通过，剩余 3 条既有 lint warning；`npm --prefix taoyuan-main run qa:online-ui-structure` 仍因 OUI-000 已记录的移动端 smoke、`VisualTrackBoard` 多队名次、村社仓廪等基线缺口返回非 0，未出现共同庄园审计详情新增失败。
- 证据（2026-06-02，Agent B）：严格运行 `npm --prefix taoyuan-main run qa:online-player-copy` 通过（59 files / 14 denylist / 1 allowlist），共同庄园玩家摘要未再触发 `hash / ledger / receipt / record-only / revision / idempotent / 服务端落账` 玩家路径命中。
- 证据（2026-06-02，Agent B）：`game-smoke.spec.ts` 新增 `online cohabitation shared audit details keep technical fields folded`，mock 一条含 `result_ledger_ids / receipt_id / receipt_hash / idempotency_key / client_queue_revision / server_queue_revision` 的共同日志；浏览器断言玩家摘要不显示 `ledger / receipt / hash / idempotency / revision`，默认折叠的 `online-cohabitation-shared-audit-technical-detail` 展开后可见原始 action、audit_id 和完整 detail 字段。
- 证据（2026-06-02，Agent B）：`node scripts/run-e2e.mjs --grep "online cohabitation shared audit details" --reporter=line` 通过，输出 `1 passed`；`npm --prefix taoyuan-main run qa:online-player-copy` 通过，`npm --prefix taoyuan-main run check` 通过（0 error，3 条既有 warning）。
- 风险（2026-06-02，Agent B）：浏览器展开证据已补齐；`npm --prefix taoyuan-main run qa:online-ui-structure` 仍因 OUI-000 既有移动端 smoke、`VisualTrackBoard` 多队名次和村社仓廪覆盖缺口返回非 0，不阻塞本任务关闭。

### OUI-COHAB-007 子组件拆分

- [x] `P1` 文件：`OnlineCohabitationView.vue`、`taoyuan-main/src/components/game/online/cohabitation/*`
- 依赖：OUI-COHAB-001 至 OUI-COHAB-006。
- 执行：拆 `CohabitationOverviewPanel`。
- 执行：拆 `CohabitationSharedMapPanel`。
- 执行：拆 `CohabitationWarehousePanel`。
- 执行：拆 `CohabitationFundPanel`。
- 执行：拆 `CohabitationFamilyFestivalPanel`。
- 执行：拆 `CohabitationGovernancePanel`。
- 验收：主文件行数明显下降，逻辑分区清楚。
- 验证：`npm run check`。
- 证据（2026-06-03，Agent B）：已新增 `taoyuan-main/src/components/game/online/cohabitation/CohabitationFamilyFestivalPanel.vue`，把家族节会席位主面板、成员席位、结算护栏和暂缓能力区从 `OnlineCohabitationView.vue` 拆出；父页面只保留 props 和 `reserve/create-room/consume-supplies/settle-rewards` 事件接线，原 store action、确认弹窗状态和 `online-cohabitation-family-festival-*-confirm-trigger` test id 均保留。
- 证据（2026-06-03，Agent B）：`OnlineCohabitationView.vue` 从 HEAD 的 12527 行降到 12404 行，新组件 188 行；本轮只完成 `CohabitationFamilyFestivalPanel` 最小拆分，`CohabitationOverviewPanel`、`CohabitationSharedMapPanel`、`CohabitationWarehousePanel`、`CohabitationFundPanel`、`CohabitationGovernancePanel` 仍待拆，因此任务保持 `[~]`。
- 证据（2026-06-03，Agent B）：`node -e` 调用 `@vue/compiler-sfc` 对 `CohabitationFamilyFestivalPanel.vue` 与 `OnlineCohabitationView.vue` 解析和 `compileScript` 通过；`npm --prefix taoyuan-main run qa:online-player-copy` 通过；`node node_modules/playwright/cli.js test e2e/game-smoke.spec.ts --grep "family festival actions require confirm dialog" --reporter=line` 通过。
- 风险（2026-06-03，Agent B）：`npm --prefix taoyuan-main run check` 当前被并行未提交的 `src/composables/useItemIconPreferences.ts` 类型错误阻断（`ItemIconVariant | undefined` 不能赋给 `ItemIconVariant`），非本轮共同庄园拆分引入；`npm --prefix taoyuan-main run qa:online-ui-structure` 仍因既有 `scripts/qa-mobile-ui-smoke.mjs` 覆盖缺口返回非 0。
- 证据（2026-06-03，Agent B）：已新增 `taoyuan-main/src/components/game/online/cohabitation/CohabitationOverviewPanel.vue`，把共同庄园总览顶部契约状态、共同基金、共同仓库、今日建议 / 风险待办四张主卡从父页面拆出；父页面通过默认 slot 保留契约列表、发起契约、照料入口、恢复和安全边界动作，未改 store action、表单状态或后端语义，`online-cohabitation-overview-summary`、`online-cohabitation-overview-main-cards` 与四张卡的旧 test id 均保留。
- 证据（2026-06-03，Agent B）：`OnlineCohabitationView.vue` 本轮从 12404 行降到 12381 行，新增 `CohabitationOverviewPanel.vue` 57 行；`node -e` 调用 `@vue/compiler-sfc` 对 `CohabitationOverviewPanel.vue`、`CohabitationFamilyFestivalPanel.vue`、`OnlineCohabitationView.vue` 解析和 `compileScript` 通过。
- 证据（2026-06-03，Agent B）：`npm --prefix taoyuan-main run check` 通过（0 error，3 条既有 warning：`onlineProfileApi.ts` import type、`CottageView.vue` / `FarmView.vue` floating promises）；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（62 files / 14 denylist / 1 allowlist）。
- 风险（2026-06-03，Agent B）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍因 `scripts/qa-mobile-ui-smoke.mjs` 中小屋共同庄园入口、农场共同庄园切换、在线庄园轻采、公共订单接力、花灯墙和村社仓廪等既有移动端 smoke 覆盖缺口返回非 0；失败列表未指向本轮 `CohabitationOverviewPanel` 拆分。`CohabitationSharedMapPanel`、`CohabitationWarehousePanel`、`CohabitationFundPanel`、`CohabitationGovernancePanel` 仍待拆，任务保持 `[~]`。
- 证据（2026-06-03，Agent B）：已新增 `taoyuan-main/src/components/game/online/cohabitation/CohabitationSharedMapPanel.vue`，把共同农田地图标题、统计卡、区域分页 tab、当前区域摘要和拼接地块网格从父页面拆出；父页面通过默认 slot 继续持有共同农田操作、共同动物照料和暂缓写操作，未改 store action、地块选择、动物照料、接口字段或后端语义，`online-cohabitation-shared-map-region-tabs`、`online-cohabitation-shared-map-region-tab-*`、`online-cohabitation-shared-map-region-page-summary`、`online-cohabitation-shared-map-page-grid` 与地块按钮旧 test id 均保留。
- 证据（2026-06-03，Agent B）：`OnlineCohabitationView.vue` 本轮从 12381 行降到 12323 行，新增 `CohabitationSharedMapPanel.vue` 115 行；`taoyuan-main/scripts/qa-online-ui-structure.mjs` 最小更新为读取新组件，并把 4 条共同地图静态断言指向新组件，避免子组件拆分后误报旧 test id 缺失。
- 证据（2026-06-03，Agent B）：`node -e` 调用 `@vue/compiler-sfc` 对 `CohabitationSharedMapPanel.vue`、`CohabitationOverviewPanel.vue`、`CohabitationFamilyFestivalPanel.vue`、`OnlineCohabitationView.vue` 解析和 `compileScript` 通过；`npm --prefix taoyuan-main run check` 通过（0 error，3 条既有 warning）；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（63 files / 14 denylist / 1 allowlist）；`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过。
- 风险（2026-06-03，Agent B）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍因 `scripts/qa-mobile-ui-smoke.mjs` 中小屋共同庄园入口、农场共同庄园切换、在线庄园轻采、公共订单接力、花灯墙和村社仓廪等既有移动端 smoke 覆盖缺口返回非 0；本轮新增的共同地图静态断言失败已消除。`CohabitationSharedMapPanel` 目前只承接地图主舞台，农田操作、动物照料和暂缓写操作仍在父页面 slot；`CohabitationWarehousePanel`、`CohabitationFundPanel`、`CohabitationGovernancePanel` 仍待拆，任务保持 `[~]`。
- 证据（2026-06-03，Agent B）：已新增 `taoyuan-main/src/components/game/online/cohabitation/CohabitationWarehousePanel.vue`，把共同仓库物品列表、冻结 / 可用数量、普通取出、高价值申请和卖出按钮从父页面拆出；父页面通过默认 slot 继续持有仓库策略、高频治理、放入普通物品、共同工坊、高价值草案和补偿审计动作，未改 store action、确认弹窗状态、接口字段或后端语义，`online-cohabitation-warehouse-withdraw-*`、`online-cohabitation-warehouse-high-value-draft-*`、`online-cohabitation-warehouse-sell-*` 旧 test id 均保留。
- 证据（2026-06-03，Agent B）：`OnlineCohabitationView.vue` 本轮从 12323 行降到 12282 行，新增 `CohabitationWarehousePanel.vue` 94 行；`node -e` 调用 `@vue/compiler-sfc` 对 `CohabitationWarehousePanel.vue`、`CohabitationSharedMapPanel.vue`、`CohabitationOverviewPanel.vue`、`CohabitationFamilyFestivalPanel.vue`、`OnlineCohabitationView.vue` 解析和 `compileScript` 通过。
- 证据（2026-06-03，Agent B）：`npm --prefix taoyuan-main run check` 通过（0 error，3 条既有 warning）；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（64 files / 14 denylist / 1 allowlist）；`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回非 0，但失败列表只剩 `scripts/qa-mobile-ui-smoke.mjs` 既有移动端 smoke 覆盖缺口，未出现仓库主列表拆分相关新增失败。
- 风险（2026-06-03，Agent B）：`CohabitationWarehousePanel` 目前只承接仓库主列表和物品按钮，仓库策略、高频治理、放入普通物品、共同工坊、高价值草案和补偿审计仍在父页面 slot；`CohabitationFundPanel`、`CohabitationGovernancePanel` 仍待拆，任务保持 `[~]`。
- 证据（2026-06-03，Agent B）：已新增 `taoyuan-main/src/components/game/online/cohabitation/CohabitationFundPanel.vue`，把共同基金余额摘要、注资状态、白名单购买、中额预算和大额草案创建表单从父页面拆出；父页面通过默认 slot 继续持有大额草案列表、高风险回执、共同装修拆除主状态写回和基金流水，未改 store action、确认弹窗状态、接口字段或后端语义，`online-cohabitation-fund-contribution-*`、`online-cohabitation-fund-buy-*`、`online-cohabitation-fund-medium-*`、`online-cohabitation-fund-large-draft-*` 旧 test id 均保留。
- 证据（2026-06-03，Agent B）：`OnlineCohabitationView.vue` 本轮从 12282 行降到 12175 行，新增 `CohabitationFundPanel.vue` 244 行；`taoyuan-main/scripts/qa-online-ui-structure.mjs` 最小更新为读取新基金组件，并把共同基金大额草案创建入口断言指向新组件，避免子组件拆分后误报旧 test id 缺失。
- 证据（2026-06-03，Agent B）：`npm --prefix taoyuan-main run check` 通过（0 error，3 条既有 warning）；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（65 files / 14 denylist / 1 allowlist）；`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回非 0，但本轮新增的共同基金大额草案入口失败已消除，失败列表只剩 `scripts/qa-mobile-ui-smoke.mjs` 既有 / 并行移动端 smoke 覆盖缺口。
- 风险（2026-06-03，Agent B）：`CohabitationFundPanel` 目前只承接共同基金左侧主面板，草案确认 / 执行、回执、主状态写回和基金流水仍在父页面 slot；`CohabitationGovernancePanel` 仍待拆，任务保持 `[~]`。
- 证据（2026-06-03，Agent B）：已新增 `taoyuan-main/src/components/game/online/cohabitation/CohabitationGovernancePanel.vue`，把成员权限、默认权限恢复、权限组开关、安全阀、家族职位和权限审计从父页面拆出；父页面只保留 `permissionMembers / roleMembers` 数据、分组 helper、`restore-default / toggle-permission / change-role` 事件接线和原 store action，未改接口字段、确认状态或后端语义。
- 证据（2026-06-03，Agent B）：`OnlineCohabitationView.vue` 当前约 12065 行，新增 `CohabitationGovernancePanel.vue` 219 行；6 个命名面板 `CohabitationOverviewPanel`、`CohabitationSharedMapPanel`、`CohabitationWarehousePanel`、`CohabitationFundPanel`、`CohabitationFamilyFestivalPanel`、`CohabitationGovernancePanel` 均已拆出，权限默认恢复、权限组开关、角色切换等旧 test id 均保留在新组件内。
- 证据（2026-06-03，Agent B）：`taoyuan-main/scripts/qa-online-ui-structure.mjs` 已读取 `CohabitationGovernancePanel.vue`，并把 `online-cohabitation-permission-default-restore-*`、`online-cohabitation-permission-grouped-toggles`、`online-cohabitation-permission-toggle-group-*`、组内 `sm:grid-cols-2` 等 UI 断言指向新组件；默认权限偏离数量现在在治理面板中以「偏离默认 N 项」可见提示展示。
- 证据（2026-06-03，Agent B）：`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`npm --prefix taoyuan-main run check` 通过（0 error，3 条既有 warning：`onlineProfileApi.ts` import type、`CottageView.vue` / `FarmView.vue` floating promises）；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（66 files / 14 denylist / 1 allowlist）。
- 风险（2026-06-03，Agent B）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍因 `scripts/qa-mobile-ui-smoke.mjs` 中小屋 / 农场共同庄园入口、在线庄园轻采、公共订单接力、花灯墙、村社仓廪等既有或并行移动端 smoke 覆盖缺口返回非 0；失败列表未出现治理面板、共同基金、仓库、地图或总览拆分新增缺口，本任务按 `npm run check` 验证和结构断言迁移证据关闭。

## 9. M5 其他在线页

### OUI-NEIGHBOR-001 名片编辑弹窗化

- [x] `P1` 文件：`taoyuan-main/src/views/game/online/OnlineNeighborView.vue`
- 依赖：OUI-CMP-001。
- 执行：名片编辑表单进入弹窗。
- 执行：默认展示公开名片摘要。
- 执行：保存失败保留草稿。
- 验收：移动端首屏不被名片表单占满。
- 验证：移动端 smoke。
- 证据（2026-06-03，Agent B）：`OnlineNeighborView.vue` 已将名片编辑长表单迁入 `OnlineActionDialog`，首屏仅保留公开名片摘要、公开范围 / 草稿状态和 `online-neighbor-profile-edit-trigger`；弹窗内容新增 `online-neighbor-profile-dialog`，原 `online-neighbor-profile-save`、`online-neighbor-profile-manor-input`、`online-neighbor-profile-title-input`、`online-neighbor-profile-role-input`、`online-neighbor-profile-theme-input`、`online-neighbor-profile-visibility-select`、`online-neighbor-profile-intro-input` 均保留。
- 证据（2026-06-03，Agent B）：保存仍调用既有 `socialStore.saveProfile()`，成功后关闭弹窗；失败时只写入 `profileSaveError` 并保持 `profileEditorOpen`，草稿仍留在 store draft 字段中，未改后端接口或状态机语义。
- 证据（2026-06-03，Agent B）：`qa-online-ui-structure.mjs` 已新增邻里名片弹窗静态断言，覆盖通用弹窗、摘要行动区、编辑入口、旧 test id、保存禁用和保存失败提示；`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过。
- 证据（2026-06-03，Agent B）：`node -e` 对 `OnlineNeighborView.vue` 执行 `@vue/compiler-sfc` 解析 / `compileScript` 通过；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（66 files / 14 denylist / 1 allowlist）；`npm --prefix taoyuan-main run check` 通过（0 error，3 条既有 warning：`onlineProfileApi.ts` import type、`CottageView.vue` / `FarmView.vue` floating promises）；`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 通过，期间仅有后端代理 `127.0.0.1:4013` 未启动警告。
- 风险（2026-06-03，Agent B）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍因 `qa-mobile-ui-smoke.mjs` 中小屋 / 农场共同庄园入口、在线庄园轻采、公共订单接力、花灯墙、村社仓廪等既有或并行移动端覆盖缺口返回非 0；失败列表未出现邻里名片弹窗新增断言。`taoyuan-main/scripts/qa-mobile-ui-smoke.mjs` 当前存在他人未提交改动，本轮未触碰也不暂存。

### OUI-NEIGHBOR-002 邻里创建向导

- [x] `P1` 文件：`OnlineNeighborView.vue`
- 依赖：OUI-CMP-001。
- 执行：创建邻里进入步骤弹窗。
- 执行：步骤包含名称、简介、可见性、加入条件、确认。
- 验收：创建邻里失败保留草稿。
- 验证：E2E 或人工检查。
- 证据（2026-06-03，Agent B）：`OnlineNeighborView.vue` 已将未加入邻里时的创建长表单迁入 `OnlineActionDialog` 三步向导，首屏只保留 `online-neighbor-create-trigger`；弹窗新增 `online-neighbor-create-dialog`、`online-neighbor-create-step-basic`、`online-neighbor-create-step-access`、`online-neighbor-create-step-review`，覆盖名称 / 简介、公开方式 / 加入门槛、确认创建。
- 证据（2026-06-03，Agent B）：原 `online-neighbor-create-name-input`、`online-neighbor-create-summary-input`、`online-neighbor-create-notice-input`、`online-neighbor-create-capacity-select`、`online-neighbor-create-submit` 均保留在向导内；确认创建仍直接调用既有 `socialStore.submitNeighborGroup()`，只传原有 name / summary / notice / capacity 字段，不新增后端协议语义。
- 证据（2026-06-03，Agent B）：创建失败时 `neighborCreateOpen` 保持打开，错误写入 `neighborCreateError` 并用 `OnlineStatusBanner` 展示，store 中 `neighborNameDraft` / `neighborSummaryDraft` / `neighborNoticeDraft` / `neighborCapacityDraft` 不会被清空。
- 证据（2026-06-03，Agent B）：`qa-online-ui-structure.mjs` 已补邻里创建向导断言；`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`node -e` 对 `OnlineNeighborView.vue` 执行 `@vue/compiler-sfc` 解析 / `compileScript` 通过；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（66 files / 14 denylist / 1 allowlist）；`npm --prefix taoyuan-main run check` 通过（0 error，3 条既有 warning）；`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，期间仅有后端代理 `127.0.0.1:4013` 未启动警告。
- 风险（2026-06-03，Agent B）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍因 `qa-mobile-ui-smoke.mjs` 中小屋 / 农场共同庄园入口、在线庄园轻采、公共订单接力、花灯墙、村社仓廪等既有或并行移动端覆盖缺口返回非 0；失败列表未出现邻里创建向导新增断言。可见性 / 加入条件目前是前端向导确认口径，因现有 `createNeighborGroup` API 仅支持 name / summary / notice / capacity，本轮未新增接口字段。

### OUI-NEIGHBOR-003 邻里邀请和申请抽屉

- [x] `P1` 文件：`OnlineNeighborView.vue`、`OnlineInvitePanel.vue`
- 依赖：OUI-FLOW-004、OUI-CMP-003。
- 执行：邀请成员复用 `OnlineInvitePanel`。
- 执行：申请 / 邀请处理进入详情抽屉。
- 验收：管理动作不散落页面。
- 验证：人工检查。
- 证据（2026-06-03，Agent B）：`OnlineNeighborView.vue` 已将邻里成员邀请改为 `OnlineInvitePanel domain="neighbor"`；首屏只保留 `online-neighbor-invite-panel-trigger` 主入口，批量邀请由 `inviteNeighborRecipients` 逐项写入既有 `socialStore.neighborInviteUsernameDraft` 并调用原 `socialStore.inviteNeighbor()`，已在邻里的玩家会被标记为不可邀请，失败项保留在 `neighborInviteResults` 供重试。
- 证据（2026-06-03，Agent B）：旧单人邀请入口未删除，`online-neighbor-invite-username-input`、`online-neighbor-invite-submit` 保留在备用折叠区；申请 / 邀请列表卡只保留 `online-neighbor-request-detail-trigger`，接受 / 拒绝动作迁入 `OnlineBottomSheet` 详情抽屉，新增 `online-neighbor-request-detail-sheet`、`online-neighbor-request-accept`、`online-neighbor-request-reject`，仍调用原 `socialStore.acceptNeighbor()` / `socialStore.rejectNeighbor()`。
- 证据（2026-06-03，Agent B）：人工检查代码路径确认管理动作不再散落在申请列表卡片上，申请处理会通过 `selectedNeighborRequestActionDisabled` 保留运行中和非管理员禁用规则；处理失败时抽屉保持打开并用 `OnlineStatusBanner` 展示错误，未改变后端接口或状态机语义。
- 证据（2026-06-03，Agent B）：`qa-online-ui-structure.mjs` 已新增邻里邀请面板、备用旧 test id、原 store action、申请详情抽屉和禁用规则静态断言；`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`npm --prefix taoyuan-main run check` 通过（0 error，3 条既有 warning）；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（66 files / 14 denylist / 1 allowlist）；`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 通过，期间仅有后端代理 `127.0.0.1:4013` 未启动警告。
- 风险（2026-06-03，Agent B）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍因 `qa-mobile-ui-smoke.mjs` 中小屋 / 农场共同庄园入口、在线庄园轻采、公共订单接力、花灯墙、村社仓廪等既有或并行移动端覆盖缺口返回非 0；失败列表未出现本轮新增邻里邀请 / 申请抽屉断言。

### OUI-ORDERS-001 发布委托向导

- [x] `P1` 文件：`taoyuan-main/src/views/game/online/OnlineOrdersView.vue`、`taoyuan-main/src/components/game/online/OnlineOrderWizard.vue`
- 依赖：OUI-CMP-001、OUI-CMP-003。
- 执行：发布委托长表单进入步骤弹窗。
- 执行：步骤：类型、需求数量、协作模式、奖励、确认发布。
- 执行：发布失败保留草稿。
- 验收：发布委托不再占据首屏。
- 验证：E2E 发布委托。
- 证据（2026-06-03，Agent B）：新增 `OnlineOrderWizard.vue`，复用 `OnlineActionDialog` 承接发布委托五步向导：类型 / 可见范围、需求标题 / 截止 / 内容、协作模式 / 接力阶段、回报、确认发布；原 `online-orders-publish-title-input`、`online-orders-publish-type-select`、`online-orders-publish-scope-select`、`online-orders-publish-mode-select`、`online-orders-publish-deadline-input`、`online-orders-publish-reward-type-select`、`online-orders-publish-reward-value-input`、`online-orders-publish-reward-label-input`、`online-orders-publish-description-input`、`online-orders-publish-submit` 均保留在向导内。
- 证据（2026-06-03，Agent B）：`OnlineOrdersView.vue` 的发布 tab 首屏改为 `online-orders-publish-wizard-trigger` 和 `online-orders-publish-summary` 草稿摘要，不再直接铺长表单；提交仍调用既有 `coopOrderStore.submitOrder()`，成功后关闭向导并切到“我的发布”，失败时保持 `orderWizardOpen` 且不清空 store 草稿，未改后端接口或状态机语义。
- 证据（2026-06-03，Agent B）：`qa-mobile-ui-smoke.mjs` 的在线委托移动端场景已打开发布向导，依次走到 `online-order-wizard-step-review`，填写“移动端烟测求助单”并点击旧 `online-orders-publish-submit`，mock `/api/taoyuan/online/orders` POST 后在 `online-orders-mine-entry` 读回新发布单；`qa-online-ui-structure.mjs` 已新增向导组件、首屏入口、旧 test id、发布失败保留和移动端发布路径静态断言。
- 证据（2026-06-03，Agent B）：`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs` 通过；`node -e` 对 `OnlineOrderWizard.vue`、`OnlineOrdersView.vue` 执行 `@vue/compiler-sfc` 解析 / `compileScript` 通过；`npm --prefix taoyuan-main run check` 通过（0 error，3 条既有 warning）；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（67 files / 14 denylist / 1 allowlist）；提交前复核 `npm --prefix taoyuan-main run qa:mobile-ui-smoke` 已跑完委托 390x844 / 360x780 场景，生成 `docs/ui-smoke-2026-04-26/25-online-orders-mobile-390x844.png`、`docs/ui-smoke-2026-04-26/27-online-orders-mobile-360x780.png`，截图中可见“移动端烟测求助单”已进入“我的发布”。
- 风险（2026-06-03，Agent B）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍因 `qa-mobile-ui-smoke.mjs` 中小屋 / 农场共同庄园入口、在线庄园轻采、公共订单接力、花灯墙、村社仓廪等既有或并行移动端覆盖缺口返回非 0；失败列表未出现本轮新增委托发布向导断言。提交前复核的 `npm --prefix taoyuan-main run qa:mobile-ui-smoke` 在委托场景完成后，后续 `28-online-society-projects-mobile-390x844` 因未启动后端代理 `127.0.0.1:4013` 的 `page.goto` 超时返回非 0；本轮未改该村社路径。`taoyuan-main/e2e/game-smoke.spec.ts` 当前有 Agent A 未提交改动，本轮未写入桌面 E2E，发布提交流由移动端浏览器 smoke 截图覆盖。

### OUI-ORDERS-002 委托接单 / 交付确认

- [x] `P1` 文件：`OnlineOrdersView.vue`
- 依赖：OUI-CMP-006。
- 执行：接单、交付、确认交付、补偿确认进入确认弹窗。
- 执行：说明需求、奖励、影响、失败原因。
- 验收：委托高风险或关键动作都有确认。
- 验证：E2E 接单交付。
- 证据（2026-06-03，Agent B）：`OnlineOrdersView.vue` 已接入 `OnlineConfirmActionDialog`，新增 `online-orders-action-confirm` 确认上下文；整单接单、接力阶段接单、提交交付、阶段交付、确认结算、阶段结算、补偿重试、取消接单 / 取消阶段都会先展示影响对象、回报 / 交付 / 结算变化和失败恢复提示，再调用原 `coopOrderStore.acceptOrder()` / `acceptStage()` / `submitDelivery()` / `confirmDelivery()` / `retryCompensation()` / `cancelAccepted*()`，未改 store action 或 API payload。
- 证据（2026-06-03，Agent B）：旧按钮 test id `online-orders-accept-submit`、`online-orders-delivery-submit`、`online-orders-confirm-submit`、`online-orders-compensation-retry-submit` 均保留在触发按钮上；新增 `online-orders-stage-accept-submit`、`online-orders-stage-delivery-submit`、`online-orders-stage-confirm-submit` 覆盖接力阶段按钮。执行失败时确认弹窗不关闭，并把 `coopOrderStore.errorMessage` 拼入确认说明。
- 证据（2026-06-03，Agent B）：`qa-mobile-ui-smoke.mjs` 已新增“移动端待交付求助单”mock，在线委托移动端场景会在“我的接单”填写 `smoke_wheat ×2`，点击旧 `online-orders-delivery-submit` 后断言 `online-orders-action-confirm` 展示求助单和交付内容，再确认 POST `/deliver` 并读回“待确认”；`qa-online-regression-live-smoke.mjs` 已在确认结算和补偿重试后补点确认弹窗。
- 证据（2026-06-03，Agent B）：`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`node --check taoyuan-main/scripts/qa-online-regression-live-smoke.mjs` 通过；`npm --prefix taoyuan-main run check` 通过（0 error，3 条既有 warning）；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（67 files / 14 denylist / 1 allowlist）；`npm --prefix taoyuan-main run qa:online-ui-structure` 仍因既有 / 并行移动端覆盖缺口返回非 0，失败列表未出现本轮新增委托确认断言。
- 风险（2026-06-03，Agent B）：`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 连续两次在首个页面 `page.goto("http://127.0.0.1:4181/4182/", waitUntil="load")` 30s 超时，尚未进入在线委托场景，故本任务保持 `[~]` 不关闭；`taoyuan-main/e2e/game-smoke.spec.ts` 当前有 Agent A 未提交改动，本轮未跨域迁移桌面 E2E。`AsyncCommunityBoard` 可视接力快捷按钮仍按原路径直接触发，后续需在 E2E 可安全迁移时统一纳入确认。
- 证据（2026-06-05，Agent B）：`OnlineOrdersView.vue` 已把 `AsyncCommunityBoard` 可视接力快捷按钮纳入同一套确认上下文，`accept_stage` / `confirm_stage` / `deliver_stage` 不再直接调用 store action，而是先打开 `online-orders-action-confirm` / `OnlineConfirmActionDialog`；来自 `online-orders-detail-sheet` 的接力按钮会先关闭详情抽屉，再由确认按钮提交原 `coopOrderStore.acceptStage()` / `confirmDelivery()` / `submitDelivery()` 路径，未改 API payload 或后端语义。
- 证据（2026-06-05，Agent B）：`game-smoke.spec.ts` 的 `online orders async board supports public relay route actions` 已迁移到“打开 `online-orders-detail-sheet` → 点击 `online-society-async-contribute-relay_route-accept_stage:stage_process` → 断言确认弹窗影响对象 / 资产变化 / 恢复提示 → 等待原 `/api/taoyuan/online/orders/relay-order-e2e/stages/stage_process/accept` POST → 回看接单后阶段与共同基金分账”的路径；`qa-online-ui-structure.mjs` 同步守护详情抽屉、确认按钮和原接单接口等待。
- 证据（2026-06-05，Agent B）：`node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-player-copy` 通过；提权复跑 `TAOYUAN_E2E_PORT=57072 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online orders async board supports public relay route actions" --reporter=line --timeout=120000` 返回 0（1 passed）。
- 风险（2026-06-05，Agent B）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项为小屋 / 农场共同庄园入口与花灯墙移动端覆盖既有缺口，未出现委托确认或详情抽屉新增断言失败；`TAOYUAN_MOBILE_SMOKE_ONLY=online-orders npm --prefix taoyuan-main run qa:mobile-ui-smoke` 沙箱内因 Chromium `spawn EPERM` 跳过，提权复跑两次均在首页 `page.goto` 30s 超时，未进入委托场景，因此本轮不把 mobile smoke 作为新增通过证据。

### OUI-ORDERS-003 委托列表详情抽屉

- [x] `P1` 文件：`OnlineOrdersView.vue`、`OnlineBottomSheet.vue`
- 依赖：OUI-CMP-003。
- 执行：列表卡只显示标题、需求摘要、奖励、剩余时间、主按钮。
- 执行：点击卡片打开详情抽屉。
- 执行：凭证和补偿细节进技术详情。
- 验收：一屏至少能看到 3 张可接委托卡。
- 验证：移动端截图。
- 证据（2026-06-04，Agent B）：`OnlineOrdersView.vue` 的可接委托列表已改为瘦卡，只保留标题、接力标签、需求摘要、回报、剩余时间、状态和主按钮；点击卡片或 `online-orders-available-detail-trigger` 会打开 `OnlineBottomSheet` 详情抽屉 `online-orders-detail-sheet`，接力阶段、异步共建板、故事流、分账摘要和阶段接单动作迁入抽屉，旧 `online-orders-available-entry`、`online-orders-accept-submit`、`online-orders-stage-accept-submit` 仍保留可回归定位。
- 证据（2026-06-04，Agent B）：凭证与补偿页已将凭证编号、重复提交保护、共同庄园契约、共同基金流水、补偿编号、失败原因和更新时间收进默认折叠 `OnlineTechnicalDetails`（`title="结算凭证详情"` / `title="补偿详情"`）；玩家主卡只展示状态、回报、交付内容、结算去向和可恢复操作。`qa-mobile-ui-smoke.mjs` 新增 `TAOYUAN_MOBILE_SMOKE_ONLY` 场景过滤，在线委托 smoke 会先打开详情抽屉并断言接力回看、公开凭证、分账摘要和确认弹窗；`qa-online-regression-live-smoke.mjs` 改为保留旧 `online-orders-action-confirm` 存在检查，并用真实可见的 `online-confirm-action-dialog` 提交确认。
- 证据（2026-06-04，Agent B）：`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`node --check taoyuan-main/scripts/qa-online-regression-live-smoke.mjs`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 通过；`npm --prefix taoyuan-main run check` 通过（0 error，3 条既有 warning）；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（67 files / 14 denylist / 1 allowlist）；`TAOYUAN_E2E_PORT=57040 TAOYUAN_MOBILE_SMOKE_ONLY=online-orders npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，生成 `docs/ui-smoke-2026-04-26/25-online-orders-mobile-390x844.png` 与 `docs/ui-smoke-2026-04-26/27-online-orders-mobile-360x780.png`。
- 风险（2026-06-04，Agent B）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回非 0，但失败列表只剩小屋 / 农场共同庄园入口、在线庄园轻采、花灯墙、村社仓廪等既有或后续 Agent B 范围移动端覆盖缺口；本轮新增的委托详情抽屉、凭证 / 补偿技术详情和 orders-only mobile smoke 断言均未出现在失败列表。orders-only smoke 运行期间仍有本地后端 `127.0.0.1:4013` 代理拒连日志，但命令返回 0，委托 mock 场景和截图均已完成。
- 证据（2026-06-05，Agent B）：收口未过滤 mobile smoke 暴露的委托详情抽屉断言：`qa-mobile-ui-smoke.mjs` 的 `prepareOnlineOrdersMobile()` 改为点击 `online-society-async-contribute-relay_route-accept_stage:stage_process` 后先断言 `online-confirm-action-dialog` 的影响对象、资产变化和恢复提示，确认提交后重新打开 `online-orders-detail-sheet` 验证“送到灯会”、`async-community-project-readback` 和“加工干菜：35% / 90 · 共同基金”；同时将 `online-orders` 加入静态构建过滤 smoke 的样例兜底名单。
- 证据（2026-06-05，Agent B）：`TAOYUAN_BASE_URL=http://127.0.0.1:58131 TAOYUAN_SKIP_DEV_SERVER=1 TAOYUAN_MOBILE_SMOKE_ONLY=online-orders npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0；未过滤 `npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，刷新 `docs/ui-smoke-2026-04-26/25-online-orders-mobile-390x844.png` 与 `docs/ui-smoke-2026-04-26/27-online-orders-mobile-360x780.png`，`summary.json` 记录两项均 `horizontalOverflow: false`、`primaryActionVisibleInViewport: true`。

### OUI-SOCIETY-001 创建村社向导

- [x] `P1` 文件：`taoyuan-main/src/views/game/online/OnlineSocietyView.vue`
- 依赖：OUI-CMP-001。
- 执行：创建村社表单进入向导。
- 执行：步骤：名称、简介公告、徽记主题、可见性容量、加入条件、确认。
- 验收：未加入村社时首屏展示创建 CTA 和公开村社摘要，不直接铺长表单。
- 验证：移动端 smoke。
- 证据（2026-06-04，Agent B）：`OnlineSocietyView.vue` 未加入村社首屏已改为 `online-society-create-summary` 摘要卡、`online-society-create-trigger` 主 CTA 和公开村社摘要；原创建长表单迁入 `OnlineActionDialog` 向导 `online-society-create-dialog`，步骤覆盖 `online-society-create-step-basic`、`online-society-create-step-style`、`online-society-create-step-access`、`online-society-create-step-join`、`online-society-create-step-review`，旧 `online-society-create-name-input`、`online-society-create-summary-input`、`online-society-create-notice-input`、`online-society-create-emblem-select`、`online-society-create-theme-select`、`online-society-create-visibility-select`、`online-society-create-capacity-select`、`online-society-create-join-requirement-select`、`online-society-create-join-note-input`、`online-society-create-submit` 均保留在向导内。
- 证据（2026-06-04，Agent B）：`qa-online-ui-structure.mjs` 新增创建村社向导静态断言；`qa-mobile-ui-smoke.mjs` 新增 `prepareOnlineSocietyCreateMobile` 和 `TAOYUAN_MOBILE_SMOKE_ONLY=online-society-create` 两个视口场景，覆盖打开向导、逐步填写、提交后读回新村社、确认首屏不再铺旧长表单及横向溢出检查。
- 证据（2026-06-04，Agent B）：`npm --prefix taoyuan-main run check` 通过（0 error，3 条既有 warning）；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（67 files / 14 denylist / 1 allowlist）；`TAOYUAN_E2E_PORT=57041 TAOYUAN_MOBILE_SMOKE_ONLY=online-society-create npm --prefix taoyuan-main run qa:mobile-ui-smoke` 非沙箱复跑返回 0，生成 `docs/ui-smoke-2026-04-26/40-online-society-create-mobile-390x844.png` 与 `docs/ui-smoke-2026-04-26/41-online-society-create-mobile-360x780.png`。
- 风险（2026-06-04，Agent B）：沙箱内移动端 smoke 因 Playwright Chromium `spawn EPERM` 被脚本跳过，已按权限规则非沙箱复跑通过；复跑期间仍有本地后端 `127.0.0.1:4013` 代理拒连日志，但命令返回 0，截图和 summary 均生成。`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回非 0，失败集中在小屋 / 农场共同庄园入口、在线庄园轻采、花灯墙、村社仓廪等既有或后续 Agent B 覆盖缺口，未出现创建村社向导新增断言失败。

### OUI-SOCIETY-002 村社邀请与申请处理

- [x] `P1` 文件：`OnlineSocietyView.vue`、`OnlineInvitePanel.vue`
- 依赖：OUI-FLOW-004、OUI-CMP-003。
- 执行：邀请玩家复用 `OnlineInvitePanel`。
- 执行：申请处理进入详情抽屉。
- 执行：管理员和普通成员动作分层。
- 验收：非管理员看不到管理主按钮。
- 验证：E2E 或人工检查。
- 证据（2026-06-04，Agent B）：`OnlineSocietyView.vue` 已复用 `OnlineInvitePanel domain="society"` 承接村社批量邀请，新增 `online-society-invite-panel-trigger` 主入口、发送结果列表、已在村社成员拦截和失败重试；原 `online-society-invite-username-input`、`online-society-invite-save-id-input`、`online-society-invite-submit` 保留在备用邀请表单内，仍调用原 `societyStore.inviteMember()`。
- 证据（2026-06-04，Agent B）：收到的邀请、本人待处理申请和管理员待审申请都改为详情入口，点击 `online-society-incoming-request-detail-*` / `online-society-pending-request-detail-*` / `online-society-managed-request-detail-*` 打开 `OnlineBottomSheet` 的 `online-society-request-detail-sheet`；旧管理员处理按钮 `online-society-managed-request-accept-*` / `online-society-managed-request-reject-*` 保留在抽屉 footer，接受 / 拒绝仍调用原 `societyStore.acceptRequest()` / `rejectRequest()`。
- 证据（2026-06-04，Agent B）：村社成员页拆出 `online-society-admin-actions` 与 `online-society-member-actions`，管理员入口只在 `can_invite` / `can_review_requests` / `can_manage_roles` 可用时展示；`qa-mobile-ui-smoke.mjs` 新增 society management mock 和 `online-society-requests` 390x844 / 360x780 场景，覆盖非成员动作隐藏、邀请面板、批量邀请、已入社拦截、详情抽屉处理和成员列表读回。
- 证据（2026-06-04，Agent B）：`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node --check taoyuan-main/scripts/qa-online-regression-live-smoke.mjs` 通过；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（67 files / 14 denylist / 1 allowlist）；`node -e` 解析并 `compileScript` `OnlineSocietyView.vue` 通过；`TAOYUAN_E2E_PORT=57042 TAOYUAN_MOBILE_SMOKE_ONLY=online-society-requests npm --prefix taoyuan-main run qa:mobile-ui-smoke` 非沙箱复跑返回 0，生成 `docs/ui-smoke-2026-04-26/42-online-society-requests-mobile-390x844.png` 与 `docs/ui-smoke-2026-04-26/43-online-society-requests-mobile-360x780.png`。
- 风险（2026-06-04，Agent B）：沙箱内 society-only smoke 因 Playwright Chromium `spawn EPERM` 被脚本跳过，已按权限规则非沙箱复跑通过；复跑期间仍有本地后端 `127.0.0.1:4013` 代理拒连日志，但命令返回 0，截图和 summary 均生成。`npm --prefix taoyuan-main run check` 最新复跑被 `OnlineFestivalView.vue` 远征房间大厅 handler 缺失阻断，属于 Agent A 房间 / 远征接入范围，本轮不跨域修复。`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回非 0，失败集中在小屋 / 农场共同庄园入口、在线庄园轻采、在线节会远征房间壳、花灯墙、村社仓廪等既有或后续覆盖缺口，未出现村社邀请与申请新增断言失败。

### OUI-SOCIETY-003 提案弹窗化

- [x] `P1` 文件：`OnlineSocietyView.vue`
- 依赖：OUI-CMP-001。
- 执行：发起提案进入弹窗。
- 执行：关闭提案使用确认弹窗。
- 执行：归档备注不直接铺在列表里。
- 验收：提案页主路径更像列表 + 操作，不是表单堆叠。
- 验证：人工检查。
- 证据（2026-06-04，Agent B）：`OnlineSocietyView.vue` 的提案 tab 已从“活跃列表 + 右侧长表单”收口为 `online-society-proposal-list` 列表和 `online-society-proposal-action-panel` 操作摘要；发起提案通过 `online-society-proposal-create-trigger` 打开 `OnlineActionDialog` 的 `online-society-proposal-dialog`，旧 `online-society-proposal-title-input`、`online-society-proposal-kind-select`、`online-society-proposal-summary-input`、`online-society-proposal-submit` 均保留在弹窗内，仍调用原 `societyStore.submitProposal()`。
- 证据（2026-06-04，Agent B）：活跃提案归档移除列表内备注输入，改为点击 `online-society-proposal-close-trigger-*` 打开 `online-society-proposal-close-dialog` 确认弹窗，展示提案、票数、归档后影响和失败恢复提示，备注输入进入 `online-society-proposal-close-note-input`；归档仍调用原 `societyStore.archiveProposal()`。历史提案的 `resolution_note` 改为 `online-society-proposal-archive-note-*` 可展开备注，不再直接铺在列表里。
- 证据（2026-06-04，Agent B）：`qa-online-ui-structure.mjs` 新增村社提案弹窗、归档确认、旧 test id 保留和 mobile smoke 场景断言；`qa-mobile-ui-smoke.mjs` 新增 `prepareOnlineSocietyProposalsMobile` 与 `online-society-proposals` 390x844 / 360x780 场景，覆盖页面不直接铺旧提案输入、打开弹窗、提交读回、归档确认、归档备注收折和横向溢出检查；`qa-online-regression-live-smoke.mjs` 已迁移为先点击 `online-society-proposal-create-trigger` 再使用旧输入 test id。
- 证据（2026-06-04，Agent B）：`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node --check taoyuan-main/scripts/qa-online-regression-live-smoke.mjs` 通过；`node -e` 解析并 `compileScript` `OnlineSocietyView.vue` 通过；`npm --prefix taoyuan-main run check` 通过（0 error，3 条既有 warning）；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（67 files / 14 denylist / 1 allowlist）；`TAOYUAN_E2E_PORT=57043 TAOYUAN_MOBILE_SMOKE_ONLY=online-society-proposals npm --prefix taoyuan-main run qa:mobile-ui-smoke` 非沙箱复跑返回 0，生成 `docs/ui-smoke-2026-04-26/44-online-society-proposals-mobile-390x844.png` 与 `docs/ui-smoke-2026-04-26/45-online-society-proposals-mobile-360x780.png`。
- 风险（2026-06-04，Agent B）：沙箱内 proposals-only smoke 因 Playwright Chromium `spawn EPERM` 被脚本跳过，已按权限规则非沙箱复跑通过；复跑期间仍有本地后端 `127.0.0.1:4013` 代理拒连日志，但命令返回 0，截图已生成。`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回非 0，失败集中在小屋 / 农场共同庄园入口、在线庄园轻采、花灯墙、村社仓廪等既有或后续覆盖缺口，未出现村社提案新增断言失败。

### OUI-SOCIETY-004 公共工程详情抽屉

- [x] `P1` 文件：`OnlineSocietyView.vue`、`AsyncCommunityBoard.vue`
- 依赖：OUI-CMP-003。
- 执行：公共工程贡献主按钮保留。
- 执行：阶段详情、历史、贡献记录进入抽屉。
- 执行：完工创建房间入口接 `OnlineRoomWizard` 预填模板。
- 验收：公共工程主路径可读，历史不铺满页面。
- 验证：E2E 村社节庆广场完工入口。
- 证据（2026-06-04，Agent B）：`AsyncCommunityBoard.vue` 新增 `detailsMode="compact"` 和 `open-detail` 事件，村社页使用 compact 模式后只保留当前阶段摘要、详情入口和贡献主按钮；旧贡献按钮 `online-society-async-contribute-*` / `online-society-project-contribute-*` 保留，仍调用原 `societyStore.contributeProject()`。
- 证据（2026-06-04，Agent B）：`OnlineSocietyView.vue` 新增 `online-society-project-detail-sheet`，阶段详情进入 `online-society-project-stage-list`，贡献记录进入 `online-society-project-recent-contributions`，贡献榜进入 `online-society-project-contributors`，历史进入 `online-society-project-history`；公共工程列表卡只保留状态、进度、详情入口、完工入口摘要和贡献动作，不再把阶段 / 历史 / 贡献明细铺满首屏。
- 证据（2026-06-04，Agent B）：完工创建房间入口在抽屉内保留为 `online-society-project-detail-room-launch`，继续沿用现有 `online-festival?tab=festival-room&template=...&gameplay=...&title=...` 预填 query 跳转；`qa-mobile-ui-smoke.mjs` 的节庆广场 mock 已补 `completion_room_launch`，并断言详情抽屉里可见“节庆广场开幕 / 创建房间”入口，未跨域改动 `OnlineRoomWizard` 或房间主流程。
- 证据（2026-06-04，Agent B）：`qa-online-ui-structure.mjs` 已补 compact 模式、详情抽屉、阶段 / 贡献 / 历史 test id 和 mobile smoke 覆盖断言；`qa-mobile-ui-smoke.mjs` 已覆盖 `online-society-projects`、`online-society-bridge`、`online-society-festival-square` 三组 390x844 / 360x780 场景，生成 `docs/ui-smoke-2026-04-26/28-online-society-projects-mobile-390x844.png`、`29-online-society-projects-mobile-360x780.png`、`30-online-society-bridge-mobile-390x844.png`、`31-online-society-bridge-mobile-360x780.png`、`32-online-society-festival-square-mobile-390x844.png`、`33-online-society-festival-square-mobile-360x780.png`。
- 证据（2026-06-04，Agent B）：`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node -e` 解析并 `compileScript` `OnlineSocietyView.vue` / `AsyncCommunityBoard.vue` 通过；`npm --prefix taoyuan-main run check` 通过（0 error，3 条既有 warning）；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（67 files / 14 denylist / 1 allowlist）；`TAOYUAN_E2E_PORT=57044 TAOYUAN_MOBILE_SMOKE_ONLY=online-society-projects npm --prefix taoyuan-main run qa:mobile-ui-smoke`、`TAOYUAN_E2E_PORT=57045 TAOYUAN_MOBILE_SMOKE_ONLY=online-society-bridge npm --prefix taoyuan-main run qa:mobile-ui-smoke`、`TAOYUAN_E2E_PORT=57046 TAOYUAN_MOBILE_SMOKE_ONLY=online-society-festival-square npm --prefix taoyuan-main run qa:mobile-ui-smoke` 非沙箱返回 0，最后一组已在补完工入口断言后复跑通过。
- 风险（2026-06-04，Agent B）：沙箱内目标 mobile smoke 因 Playwright Chromium `spawn EPERM` 被脚本跳过，已按权限规则非沙箱复跑通过；复跑期间仍有本地后端 `127.0.0.1:4013` 代理拒连日志，但命令返回 0 且截图生成。`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回非 0，失败集中在小屋 / 农场共同庄园入口、在线庄园轻采、花灯墙现场物件、村社仓廪等既有或后续覆盖缺口，未出现公共工程详情抽屉新增断言失败。`OnlineRoomWizard` 的真实向导接入属于 Agent A 房间主流程，本轮只保留并验证现有预填 query 入口，不跨域改房间状态机或向导组件。

### OUI-SOCIETY-005 仓廪消耗确认

- [x] `P1` 文件：`OnlineSocietyView.vue`
- 依赖：OUI-CMP-006。
- 执行：入仓可以保持轻操作，公共消耗必须确认。
- 执行：确认文案说明只扣公共仓。
- 验收：公共仓消耗风险明确。
- 验证：E2E 公共仓消耗。
- 证据（2026-06-04，Agent B）：`OnlineSocietyView.vue` 保留入仓轻操作和旧 `online-society-warehouse-deposit-*` test id；公共消耗旧按钮 `online-society-warehouse-consume-*` 改为先打开 `online-society-warehouse-consume-confirm`，复用 `OnlineConfirmActionDialog` 展示影响对象、公共仓扣除、个人资产不扣除、失败恢复提示和「确认公共消耗」确认文字，确认后才调用原 `societyStore.consumeWarehouse()`，接口语义和房间预填跳转保持不变。
- 证据（2026-06-04，Agent B）：`game-smoke.spec.ts` 的 `online society warehouse supports public deposit actions` 已改为先断言确认弹窗、`只扣公共仓`、`稻米 x2`、`不扣个人背包` 和确认按钮禁用，填写「确认公共消耗」后再断言「测试者 消耗了 腊八共灶底料」日志；`qa-mobile-ui-smoke.mjs` 新增 `prepareOnlineSocietyWarehouseMobile`、仓廪 public-warehouse mock 和 `38/39-online-society-warehouse-mobile-*` 两个视口，覆盖入仓、确认消耗、确认文字和横向溢出检查；`qa-online-ui-structure.mjs` 已补仓廪确认弹窗、E2E 与 mobile smoke 静态保护。
- 证据（2026-06-04，Agent B）：`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node -e` 解析并 `compileScript` `OnlineSocietyView.vue` 通过；`npm --prefix taoyuan-main run check` 通过（0 error，3 条既有 warning）；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（67 files / 14 denylist / 1 allowlist）；`TAOYUAN_E2E_PORT=57047 TAOYUAN_MOBILE_SMOKE_ONLY=online-society-warehouse npm --prefix taoyuan-main run qa:mobile-ui-smoke` 非沙箱复跑返回 0，生成 `docs/ui-smoke-2026-04-26/38-online-society-warehouse-mobile-390x844.png` 与 `docs/ui-smoke-2026-04-26/39-online-society-warehouse-mobile-360x780.png`；`TAOYUAN_E2E_PORT=57049 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online society warehouse supports public deposit actions" --reporter=line --timeout=120000` 返回 0，`.last-run.json` 为 `passed`。
- 风险（2026-06-04，Agent B）：沙箱内仓廪 mobile smoke 因 Playwright Chromium `spawn EPERM` 被脚本跳过，已按权限规则非沙箱复跑通过；复跑期间仍有本地后端 `127.0.0.1:4013` ws proxy 拒连日志，但命令返回 0 且截图生成。`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回非 0，剩余失败集中在小屋 / 农场共同庄园入口、在线庄园轻采、花灯墙移动端物件等既有或后续 Agent B 覆盖缺口，未出现仓廪确认新增断言失败。

### OUI-MANOR-001 庄园主人 / 访客态分层

- [x] `P1` 文件：`taoyuan-main/src/views/game/online/OnlineManorView.vue`
- 依赖：OUI-CMP-008。
- 执行：主人态默认展示管理展示、查看留言、处理照料。
- 执行：访客态默认展示留言、照料、轻采。
- 执行：两种身份使用不同主 CTA。
- 验收：访客不会看到主人管理优先路径。
- 验证：E2E mock 主人和访客。
- 证据（2026-06-04，Agent B）：`OnlineManorView.vue` 概览页新增 `online-manor-identity-summary`，按 `viewer_is_owner` 分流 `online-manor-owner-primary-actions` 与 `online-manor-visitor-primary-actions`；主人主行动为 `online-manor-owner-primary-manage-theme` / `online-manor-owner-primary-guestbook` / `online-manor-owner-primary-care`，访客主行动为 `online-manor-visitor-primary-guestbook` / `online-manor-visitor-primary-care` / `online-manor-visitor-primary-steal`，访客态不会渲染主人管理主行动。主人概览新增 `online-manor-overview-latest-summary`，读回最新留言与照料摘要；访客概览新增 `online-manor-visitor-action-status`，读回今日照料 / 轻采权限与剩余次数。
- 证据（2026-06-04，Agent B）：`game-smoke.spec.ts` 新增 `online manor overview separates owner and visitor primary actions`，同一 mock 覆盖自己的庄园与好友庄园，断言主人 / 访客主行动互斥、主人可进入主题 / 照料、访客可进入留言 / 轻采；既有庄园照料 / 轻采 / 护理房用例改用稳定 test id，避免新增概览按钮后按钮文案歧义。`qa-mobile-ui-smoke.mjs` 新增 owner 身份与有限轻采 mock、`prepareOnlineManorOwnerIdentityMobile`、`prepareOnlineManorLimitedStealMobile`，覆盖 390x844 / 360x780 下主人主行动、访客留言 / 照料 / 轻采入口、轻采凭证 `mobile-smoke-steal-receipt` 和横向溢出；截图为 `docs/ui-smoke-2026-04-26/36-online-manor-limited-steal-mobile-390x844.png`、`37-online-manor-limited-steal-mobile-360x780.png`、`46-online-manor-owner-identity-mobile-390x844.png`、`47-online-manor-owner-identity-mobile-360x780.png`。`qa-online-ui-structure.mjs` 已补身份主行动、E2E 和移动端 smoke 静态保护。
- 证据（2026-06-04，Agent B）：`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node -e` 解析并 `compileScript` `OnlineManorView.vue` 均通过；`npm --prefix taoyuan-main run check` 通过（0 error，3 条既有 warning）；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（67 files / 14 denylist / 1 allowlist）；沙箱内 `TAOYUAN_E2E_PORT=57050 TAOYUAN_MOBILE_SMOKE_ONLY=online-manor npm --prefix taoyuan-main run qa:mobile-ui-smoke` 因 Chromium `spawn EPERM` 跳过，非沙箱复跑返回 0；`TAOYUAN_E2E_PORT=57051 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online manor overview separates owner and visitor primary actions|online manor visual scene supports friend care actions|online manor visual scene supports limited steal actions|online manor care room supports full cooperation settlement" --reporter=line --timeout=120000` 返回 0，4/4 通过。
- 风险（2026-06-04，Agent B）：非沙箱 mobile smoke 运行期间仍有本地后端 `127.0.0.1:4013` ws proxy 拒连日志，但命令返回 0 且截图生成。`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回非 0，剩余失败集中在小屋 / 农场共同庄园入口和花灯墙移动端物件等既有或后续 Agent B 覆盖缺口；在线庄园轻采结构缺口已解除，未出现本轮新增断言失败。本轮不改后端状态机、不新增接口字段，主图上传、留言弹窗化和访客记录 / 轻采凭证抽屉化仍留给 OUI-MANOR-002 至 OUI-MANOR-004。

### OUI-MANOR-002 留言 / 上传 / 主题弹窗化

- [x] `P1` 文件：`OnlineManorView.vue`
- 依赖：OUI-CMP-001。
- 执行：主图上传进入弹窗。
- 执行：主题保存进入弹窗或确认条。
- 执行：留言输入进入弹窗或底部抽屉。
- 验收：概览页只保留庄园预览和主行动。
- 验证：移动端截图。
- 证据（2026-06-04，Agent B）：`OnlineManorView.vue` 已引入 `OnlineActionDialog`，将主图上传迁入 `online-manor-cover-upload-dialog`、主题保存迁入 `online-manor-theme-save-dialog`、留言输入迁入 `online-manor-guestbook-dialog`；主题页只保留 `online-manor-cover-summary-card` / `online-manor-theme-save-summary-card` 摘要和弹窗触发，留言页只保留 `online-manor-guestbook-action-panel` 与列表。旧 `online-manor-cover-alt-input`、`online-manor-theme-label-input`、`online-manor-theme-save-button`、`online-manor-guestbook-input`、`online-manor-guestbook-submit` 均保留在弹窗内，提交仍调用原 `uploadHallImage()`、`manorStore.saveThemeWeekSnapshot()` 和 `manorStore.createGuestbookEntry()`。
- 证据（2026-06-04，Agent B）：`game-smoke.spec.ts` 的 `online manor overview separates owner and visitor primary actions` 已扩展覆盖主题保存弹窗、主图上传弹窗 mock 回填「弹窗主图」、访客留言弹窗提交和弹窗关闭；`qa-mobile-ui-smoke.mjs` 已在 owner / visitor 庄园移动端路径断言主图上传弹窗和留言弹窗，截图为 `docs/ui-smoke-2026-04-26/36-online-manor-limited-steal-mobile-390x844.png`、`37-online-manor-limited-steal-mobile-360x780.png`、`46-online-manor-owner-identity-mobile-390x844.png`、`47-online-manor-owner-identity-mobile-360x780.png`、`34-online-manor-care-room-mobile-390x844.png`、`35-online-manor-care-room-mobile-360x780.png`；`qa-online-ui-structure.mjs` 已补 `online-manor-cover-upload-dialog*`、`online-manor-theme-save-dialog*`、`online-manor-guestbook-dialog*` 和目标 E2E / mobile smoke 静态守护。
- 证据（2026-06-04，Agent B）：`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node -e` 解析并 `compileScript` `OnlineManorView.vue` 均通过；`npm --prefix taoyuan-main run check` 通过（0 error，3 条既有 warning）；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（67 files / 14 denylist / 1 allowlist）；`TAOYUAN_E2E_PORT=57053 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online manor overview separates owner and visitor primary actions|online manor visual scene supports friend care actions|online manor visual scene supports limited steal actions|online manor care room supports full cooperation settlement" --reporter=line --timeout=120000` 非沙箱复跑 4/4 通过。
- 证据（2026-06-04，Agent B）：沙箱内 `TAOYUAN_E2E_PORT=57053 ... test:e2e ...` 与 `TAOYUAN_E2E_PORT=57052 TAOYUAN_MOBILE_SMOKE_ONLY=online-manor npm --prefix taoyuan-main run qa:mobile-ui-smoke` 均因 Playwright Chromium `spawn EPERM` 被脚本跳过；已按权限规则非沙箱复跑，目标 E2E 返回 0，online-manor-only mobile smoke 返回 0 并写入 `docs/ui-smoke-2026-04-26/summary.json`。
- 风险（2026-06-04，Agent B）：非沙箱 mobile smoke 运行期间仍有本地后端 `127.0.0.1:4013` ws proxy 拒连日志，但命令返回 0 且截图生成。`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项为小屋 / 农场共同庄园入口和花灯墙移动端物件 / 贡献摘要既有覆盖缺口，未出现本轮庄园弹窗化新增断言失败；本轮不改后端状态机、不新增接口字段，护理房动作抽屉和访客记录 / 轻采凭证详情仍留给 OUI-MANOR-003 / OUI-MANOR-004。

### OUI-MANOR-003 护理房动作抽屉化

- [x] `P1` 文件：`OnlineManorView.vue`
- 依赖：OUI-CMP-003、OUI-CMP-006。
- 执行：护理房创建进入弹窗。
- 执行：护理动作详情进入抽屉。
- 执行：结算确认进入确认弹窗。
- 验收：护理房流程不再长页面堆叠。
- 验证：已有护理房 E2E 迁移。
- 证据（2026-06-04，Agent B）：`OnlineManorView.vue` 已将护理房创建迁入 `online-manor-care-room-create-dialog`，成员人数选择和旧 `online-manor-care-room-create` 保留在弹窗内；护理房列表只保留摘要、详情入口 `online-manor-care-room-detail-trigger` 和结算触发 `online-manor-care-room-settle-trigger`，动作按钮、成员、待完成分工和旧 `online-manor-care-room-action` / `online-manor-care-room-action-ledger` 迁入 `online-manor-care-room-detail-sheet`。
- 证据（2026-06-04，Agent B）：护理房结算改为 `online-manor-care-room-settle-confirm` 包裹 `OnlineConfirmActionDialog`，展示护理房、分工进度、风险回看、健康收口和结算记录，要求输入「确认结算护理」后才调用原 `manorStore.settleCareRoom(roomId)`；确认成功后关闭结算弹窗和详情抽屉，不改后端状态机或接口字段。
- 证据（2026-06-04，Agent B）：`game-smoke.spec.ts` 的 `online manor care room supports full cooperation settlement` 已迁移到创建弹窗、详情抽屉和确认弹窗路径，并断言确认后弹窗 / 抽屉关闭和结算记录读回；`qa-mobile-ui-smoke.mjs` 的 `prepareOnlineManorCareRoomMobile` 已迁移到同一路径，风险摘要定位限定在详情抽屉内，避免列表摘要与抽屉旧 test id 并存时触发 Playwright strict mode。
- 证据（2026-06-04，Agent B）：验证 `node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node -e` 解析并 `compileScript` `OnlineManorView.vue` 通过；`npm --prefix taoyuan-main run check` 通过（0 error，3 条既有 warning）；`npm --prefix taoyuan-main run qa:online-player-copy` 通过（67 files / 14 denylist / 1 allowlist）；`TAOYUAN_E2E_PORT=57053 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online manor care room supports full cooperation settlement|online manor overview separates owner and visitor primary actions|online manor visual scene supports friend care actions|online manor visual scene supports limited steal actions" --reporter=line --timeout=120000` 非沙箱复跑 4/4 通过。
- 证据（2026-06-04，Agent B）：沙箱内 `TAOYUAN_MOBILE_SMOKE_ONLY=online-manor npm --prefix taoyuan-main run qa:mobile-ui-smoke` 因 Playwright Chromium `spawn EPERM` 被脚本跳过；按权限规则非沙箱复跑 `TAOYUAN_E2E_PORT=57062 TAOYUAN_MOBILE_SMOKE_ONLY=online-manor npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，`docs/ui-smoke-2026-04-26/summary.json` 写入护理房截图 `34-online-manor-care-room-mobile-390x844.png`、`35-online-manor-care-room-mobile-360x780.png`。
- 风险（2026-06-04，Agent B）：非沙箱 mobile smoke 仍输出本地后端 `127.0.0.1:4013` ws proxy 拒连日志，但命令返回 0 且截图生成；`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项为小屋 / 农场共同庄园入口和花灯墙移动端物件 / 贡献摘要既有覆盖缺口，未出现本轮护理房弹窗 / 抽屉 / 结算确认新增断言失败。

### OUI-MANOR-004 访客记录和轻采凭证详情抽屉

- [x] `P1` 文件：`OnlineManorView.vue`、`OnlineTechnicalDetails.vue`
- 依赖：OUI-CMP-003、OUI-CMP-005。
- 执行：访客记录默认展示最近 3 条。
- 执行：轻采凭证进入技术详情。
- 执行：反刷审计和主人保留比例放入规则折叠。
- 验收：照料 / 轻采主路径更短。
- 验证：E2E 轻采用例仍通过。
- 证据（2026-06-04，Agent B）：`OnlineManorView.vue` 已将来访记录默认列表收敛为 `recentVisitEntries.slice(0, 3)`，访客行为审计默认列表收敛为 `recentVisitorActivityEntries.slice(0, 3)`；保留旧 `online-manor-visit-list` / `online-manor-visit-entry` / `online-manor-visitor-activity-log` / `online-manor-visitor-activity-entry`，新增 `online-manor-visit-detail-trigger`、`online-manor-visit-detail-sheet`、`online-manor-visitor-activity-detail-trigger`、`online-manor-visitor-activity-detail-sheet` 承接反馈、携带物、对象、动作、审计说明和来源记录。
- 证据（2026-06-04，Agent B）：轻采记录卡只保留收益摘要和 `online-manor-steal-detail-trigger`；`online-manor-steal-receipt-guard`、`online-manor-steal-use-summary` 保留在 `online-manor-steal-detail-sheet` 内的 `OnlineTechnicalDetails`，凭证、用途、重复提交保护默认折叠；`online-manor-steal-anti-abuse-summary`、`stealReadableImpactSummary` 和主人保留比例进入「轻采规则说明 / 轻采规则明细」折叠区，不改 `manorStore.submitStealAction()` 或任何 API 字段。
- 证据（2026-06-04，Agent B）：`game-smoke.spec.ts` 的 `online manor visual scene supports limited steal actions` 和 `qa-mobile-ui-smoke.mjs` 的 `prepareOnlineManorLimitedStealMobile` 已迁移到打开 `online-manor-steal-detail-sheet`、展开 `online-technical-details-toggle` 后断言 `receipt-steal-e2e-1` / `mobile-smoke-steal-receipt`、主人保留 100% 和用途摘要；`qa-online-ui-structure.mjs` 已补最近 3 条、访客详情抽屉、轻采详情抽屉和技术详情静态保护。
- 证据（2026-06-04，Agent B）：验证 `node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node -e` 解析并 `compileScript` `OnlineManorView.vue`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-player-copy` 均通过；目标 E2E 沙箱内因 Playwright Chromium `spawn EPERM` 跳过，非沙箱复跑 `TAOYUAN_E2E_PORT=57064 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online manor care room supports full cooperation settlement|online manor overview separates owner and visitor primary actions|online manor visual scene supports friend care actions|online manor visual scene supports limited steal actions" --reporter=line --timeout=120000` 返回 0，`.last-run.json` 为 `passed` / `failedTests: []`。
- 证据（2026-06-04，Agent B）：沙箱内 `TAOYUAN_MOBILE_SMOKE_ONLY=online-manor npm --prefix taoyuan-main run qa:mobile-ui-smoke` 因 Playwright Chromium `spawn EPERM` 被脚本跳过；非沙箱复跑 `TAOYUAN_E2E_PORT=57065 TAOYUAN_MOBILE_SMOKE_ONLY=online-manor npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，并刷新 `docs/ui-smoke-2026-04-26/summary.json`，截图包含 `36-online-manor-limited-steal-mobile-390x844.png`、`37-online-manor-limited-steal-mobile-360x780.png`、`46-online-manor-owner-identity-mobile-390x844.png`、`47-online-manor-owner-identity-mobile-360x780.png`、`34-online-manor-care-room-mobile-390x844.png`、`35-online-manor-care-room-mobile-360x780.png`。
- 风险（2026-06-04，Agent B）：非沙箱 mobile smoke 仍输出本地后端 `127.0.0.1:4013` ws proxy 拒连日志和 summary 中 1 条 WebSocket console error，但命令返回 0、页面错误 / 请求失败为空且截图生成。`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项为小屋 / 农场共同庄园入口和花灯墙移动端物件 / 贡献摘要既有覆盖缺口，未出现本轮在线庄园访客记录或轻采凭证抽屉新增断言失败。

## 10. 可视化舞台与响应式

### OUI-VISUAL-001 可视化失败原因玩家化

- [x] `P1` 文件：`VisualMapBoard.vue`、`VisualSceneBoard.vue`、`VisualTrackBoard.vue`
- 依赖：OUI-COPY-001。
- 执行：将「服务端拒绝」改成「当前不能重复行动」等玩家文案。
- 执行：保留技术原因到详情。
- 验收：舞台反馈不出现服务端实现语言。
- 验证：文案扫描。
- 证据（2026-06-04，Agent B）：`VisualMapBoard.vue`、`VisualSceneBoard.vue`、`VisualTrackBoard.vue` 已把玩家可见失败原因改为「节点尚未解锁，请先处理相邻节点或稍后再试」「节点 / 物件已经处理完成，当前不能重复行动」「当前没有队伍位于该格，请选择队伍所在格」等玩家态提示；`rg -n "服务端拒绝|服务端不会接受|等待服务端开放" taoyuan-main/src/components/game/online/VisualMapBoard.vue taoyuan-main/src/components/game/online/VisualSceneBoard.vue taoyuan-main/src/components/game/online/VisualTrackBoard.vue` 无命中。
- 证据（2026-06-04，Agent B）：三个视觉组件均引入默认折叠的 `OnlineTechnicalDetails`，新增 `visual-map-technical-reason`、`visual-scene-technical-reason`、`visual-track-technical-reason`，保留 state、available_action_ids、connected_node_ids、协作进度、progress、track_id、cell_id、occupant_team_ids、current_round 等技术判断；旧 `visual-map-readable-feedback`、`visual-scene-readable-feedback`、`visual-track-readable-feedback` 和 action result test id 均保留。
- 证据（2026-06-04，Agent B）：`qa-online-ui-structure.mjs` 已增加三个视觉组件 `<OnlineTechnicalDetails` / `visual-*-technical-reason` 静态断言，并用 `expectNotContains` 守护「服务端拒绝」「服务端不会接受」「等待服务端开放」不再回到视觉组件玩家反馈。
- 证据（2026-06-04，Agent B）：验证 `node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node --check taoyuan-main/scripts/qa-online-player-copy.mjs`、三个视觉组件 SFC `compileScript`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-player-copy` 均通过；目标 E2E 沙箱内因 Playwright Chromium `spawn EPERM` 跳过，非沙箱复跑 `TAOYUAN_E2E_PORT=57066 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online expedition visual map supports cavern node actions|online festival visual scene supports lantern object actions|online festival visual track supports dragon boat cell actions" --reporter=line --timeout=120000` 返回 0，`.last-run.json` 为 `passed` / `failedTests: []`。
- 风险（2026-06-04，Agent B）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项为小屋 / 农场共同庄园入口和花灯墙移动端物件 / 贡献摘要既有覆盖缺口，未出现本轮可视化失败原因玩家化新增断言失败；本轮不改移动端对象详情承载和触控尺寸，仍留给 OUI-VISUAL-002 / OUI-VISUAL-003。

### OUI-VISUAL-002 移动端对象详情底部抽屉

- [x] `P1` 文件：`VisualMapBoard.vue`、`VisualSceneBoard.vue`、`VisualTrackBoard.vue`、`OnlineBottomSheet.vue`
- 依赖：OUI-CMP-003。
- 执行：小屏选中节点、物件、轨道格后打开底部详情。
- 执行：详情里显示行动按钮和反馈。
- 验收：舞台下方不再挤压大量详情文字。
- 验证：移动端 smoke 点击节点 / 物件。
- 证据（2026-06-04，Agent B）：`VisualMapBoard.vue`、`VisualSceneBoard.vue`、`VisualTrackBoard.vue` 均接入 `OnlineBottomSheet` 和 760px 紧凑视口判断；移动端选中节点 / 物件 / 赛道格时打开底部抽屉，页面侧栏只保留 `visual-map-detail-sheet-trigger`、`visual-scene-detail-sheet-trigger`、`visual-track-detail-sheet-trigger` 轻量入口与移动端行动反馈，桌面端继续在原侧栏展示详情。
- 证据（2026-06-04，Agent B）：三个抽屉内保留旧 `visual-map-node-detail` / `visual-scene-object-detail` / `visual-track-cell-detail`、`visual-*-readable-feedback`、`visual-*-action-*`、`visual-*-action-result` test id；行动按钮、失败原因 / 影响范围、技术详情和行动结果在抽屉内可见，旧 store action 事件仍通过原 `trigger-action` emit 提交。
- 证据（2026-06-04，Agent B）：`qa-online-ui-structure.mjs` 新增三个视觉组件 `<OnlineBottomSheet`、移动详情触发器和移动端轻量反馈静态守护；`qa-mobile-ui-smoke.mjs` 的 online-manor limited-steal 场景会点击 `visual-scene-object-mobile_smoke_apple_tree`，断言 `online-bottom-sheet` 与 `visual-scene-object-detail` 后提交轻采行动，并关闭视觉抽屉再继续轻采凭证详情，避免抽屉遮挡后续流程。
- 证据（2026-06-04，Agent B）：`game-smoke.spec.ts` 的三条视觉目标用例已在 390x844 下点击地图节点、场景物件和赛道格并断言 `online-bottom-sheet`，再切回桌面继续旧行动与结算流程；非沙箱复跑 `TAOYUAN_E2E_PORT=57068 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online expedition visual map supports cavern node actions|online festival visual scene supports lantern object actions|online festival visual track supports dragon boat cell actions" --reporter=line --timeout=120000` 返回 0，`.last-run.json` 为 `passed` / `failedTests: []`。
- 证据（2026-06-04，Agent B）：验证 `node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`node --check taoyuan-main/e2e/game-smoke.spec.ts`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-player-copy` 均通过；沙箱内 online-manor-only `qa:mobile-ui-smoke` 因 Playwright Chromium `spawn EPERM` 跳过，非沙箱复跑 `TAOYUAN_MOBILE_SMOKE_ONLY=online-manor npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，截图刷新 `docs/ui-smoke-2026-04-26/36-online-manor-limited-steal-mobile-390x844.png`、`37-online-manor-limited-steal-mobile-360x780.png`。
- 风险（2026-06-04，Agent B）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项仍为小屋 / 农场共同庄园入口与花灯墙移动端贡献摘要既有覆盖缺口，未出现本轮视觉抽屉新增断言失败；非沙箱 mobile smoke 仍输出本地后端 `127.0.0.1:4013` ws proxy 拒连日志但命令返回 0；触控尺寸提升仍留给 OUI-VISUAL-003。

### OUI-VISUAL-003 触控目标尺寸

- [x] `P1` 文件：`VisualMapBoard.vue`、`VisualSceneBoard.vue`、`VisualTrackBoard.vue`、`taoyuan-main/src/app.css`
- 依赖：OUI-VISUAL-002。
- 执行：主要热区和动作按钮触控尺寸不低于 44px。
- 执行：紧凑按钮只用于次要操作。
- 验收：移动端可单手点击，不误触。
- 验证：移动端脚本检查小于 36px 的交互元素为 0，目标最好提高到 44px。
- 证据（2026-06-05，Agent B）：`taoyuan-main/src/app.css` 新增统一 `--online-visual-touch-target: 44px`；`VisualMapBoard.vue`、`VisualSceneBoard.vue`、`VisualTrackBoard.vue` 的地图节点、场景物件、物件列表、轨道格、赛道标签、移动详情入口和行动按钮均使用该变量或更高尺寸，保留旧 `visual-*-node/detail/action`、`visual-scene-object-*`、`visual-track-cell-*` test id 与原 `trigger-action` 事件。
- 证据（2026-06-05，Agent B）：`qa-mobile-ui-smoke.mjs` 的 online-manor 视觉物件检查已从 36px 提升为 44px，只量测可见按钮；`game-smoke.spec.ts` 新增 `expectVisualTouchTargets()`，三条视觉目标 E2E 分别量测地图节点 / 行动、场景物件 / 列表 / 行动、轨道格 / 赛道标签 / 行动；`qa-online-ui-structure.mjs` 新增统一变量、三个视觉组件变量引用、44px mobile smoke 和 E2E selector 静态守护。
- 证据（2026-06-05，Agent B）：验证 `node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`node --check taoyuan-main/e2e/game-smoke.spec.ts`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-player-copy` 均通过；非沙箱复跑 `TAOYUAN_MOBILE_SMOKE_ONLY=online-manor npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，三条视觉目标 E2E grep 返回 0。
- 风险（2026-06-05，Agent B）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项为小屋 / 农场共同庄园入口和花灯墙移动端物件 / 贡献摘要既有覆盖缺口，未出现本轮可视化触控目标新增断言失败；非沙箱 mobile smoke 仍输出本地后端 `127.0.0.1:4013` ws proxy 拒连日志但命令返回 0。

### OUI-RESP-001 移动端主操作安全区

- [x] `P0` 文件：`taoyuan-main/src/app.css`、`OnlineStickyActionBar.vue`
- 依赖：OUI-CMP-004。
- 执行：底部 sticky action 使用安全区 padding。
- 执行：页面底部 spacer 与 sticky 高度一致。
- 验收：iOS / Android WebView 不遮挡按钮。
- 验证：390x844、360x780 截图。
- 证据（2026-06-04，Agent A）：`OnlineStickyActionBar.vue` 移除固定 `h-24` spacer，新增 `ResizeObserver` / `requestAnimationFrame` 高度同步；`online-sticky-action-bar-spacer` 通过 `--online-sticky-action-bar-spacer-height` 跟随实际固定栏高度，`taoyuan-main/src/app.css` 提供 `--online-sticky-action-bar-fallback-height: calc(6rem + env(safe-area-inset-bottom, 0px))`，固定栏底部仍使用 `calc(0.75rem + env(safe-area-inset-bottom, 0px))`。
- 证据（2026-06-04，Agent A）：`npm --prefix taoyuan-main run check` 返回 0（保留既有 3 条 warning）；`TAOYUAN_MOBILE_SMOKE_ONLY=online-center npm --prefix taoyuan-main run qa:mobile-ui-smoke` 提升权限返回 0，截图 `docs/ui-smoke-2026-04-26/24-online-center-mobile-390x844.png`、`docs/ui-smoke-2026-04-26/26-online-center-mobile-360x780.png` 均生成且主行动在 viewport 内。
- 证据（2026-06-04，Agent A）：`TAOYUAN_MOBILE_SMOKE_ONLY=online-festival-room npm --prefix taoyuan-main run qa:mobile-ui-smoke` 提升权限返回 0，重新生成节会房创建向导、准备大厅、邀请面板、结算确认 390x844 / 360x780 共 8 张截图；`docs/ui-smoke-2026-04-26/summary.json` 记录这些房间场景 `horizontalOverflow=false`、`primaryActionVisibleInViewport=true`。
- 风险（2026-06-04，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败集中在小屋 / 农场共同庄园入口、在线庄园轻采、花灯墙、村社仓廪等非 Agent A 覆盖缺口；移动端 smoke 运行期间仍有本地后端 `127.0.0.1:4013` 代理拒连噪声，但过滤 smoke 命令均返回 0。

### OUI-RESP-002 桌面三栏布局规则

- [x] `P1` 文件：`OnlineFestivalView.vue`、`ExpeditionRoomView.vue`、`OnlineCohabitationView.vue`
- 依赖：OUI-FEST-003、OUI-COHAB-001。
- 执行：桌面房间页采用左列表 / 中舞台 / 右状态。
- 执行：右栏宽度 320-380px。
- 执行：不再把全部卡片平铺到底。
- 验收：1366x768 下主舞台清晰。
- 验证：桌面截图。
- 证据（2026-06-04，Agent A）：Agent A 子范围已完成三栏接入：`OnlineFestivalView.vue` 的节会房与在线节会远征标签分别提供 `online-festival-room-desktop-layout` / `online-expedition-room-desktop-layout`，并拆出 left list、main stage、right status 稳定 test id；`ExpeditionRoomView.vue` 提供 `expedition-room-desktop-layout`、`expedition-room-left-list`、`expedition-room-main-stage`、`expedition-room-right-status`。三处桌面布局均使用 `xl:grid-cols-[18rem_minmax(0,1fr)_320px]`，右栏宽度由结构 QA 和浏览器断言保护在 320px 规格内。
- 证据（2026-06-04，Agent A）：`game-smoke.spec.ts` 新增桌面布局 smoke，1366x768 下断言左 / 中 / 右三栏均可见、右栏 310-380px、主舞台宽度不少于 400px、列顺序为左列表 → 中舞台 → 右状态；目标 E2E `online festival room desktop layout keeps three columns` 与 `online expedition room desktop layouts keep three columns` 返回 0，并生成截图 `docs/ui-smoke-2026-04-26/resp-002-online-festival-room-desktop-1366x768.png`、`docs/ui-smoke-2026-04-26/resp-002-online-expedition-room-desktop-1366x768.png`、`docs/ui-smoke-2026-04-26/resp-002-standalone-expedition-room-desktop-1366x768.png`。
- 证据（2026-06-04，Agent A）：验证通过：`node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`npm --prefix taoyuan-main run check`、目标 E2E grep `online (festival room desktop layout keeps three columns|expedition room desktop layouts keep three columns)`。`npm --prefix taoyuan-main run check` 返回 0，保留既有 3 条 warning（`onlineProfileApi.ts` import type、`CottageView.vue` / `FarmView.vue` floating promises）。
- 风险（2026-06-04，Agent A）：任务整体保持 `[~]`，因为 `OnlineCohabitationView.vue` 的桌面三栏仍依赖 OUI-COHAB-001 且属于 Agent B 范围；本轮未改共同庄园。`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项为小屋 / 农场共同庄园入口与花灯墙移动端 smoke 覆盖缺口，未出现节会 / 远征桌面三栏新增断言失败。
- 证据（2026-06-05，Agent A）：当前态复验 `node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node --check taoyuan-main/e2e/game-smoke.spec.ts`、`npm --prefix taoyuan-main run check`、目标 E2E `npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "desktop layout keeps three columns|desktop layouts keep three columns" --reporter=line --workers=1 --timeout=120000` 均返回 0；`check` 仍仅保留既有 3 条 warning。
- 风险（2026-06-05，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项仍为小屋 / 农场共同庄园入口、农场共同庄园切换与花灯墙移动端现场物件 / 贡献记录断言，未出现节会房、在线节会远征或独立远征三栏布局断言失败；共同庄园三栏仍不在 Agent A 本轮处理范围内。
- 证据（2026-06-05，Agent B）：`OnlineCohabitationView.vue` 已补齐共同庄园桌面三栏：`online-cohabitation-desktop-layout` 使用 `xl:grid-cols-[18rem_minmax(0,1fr)_320px]`，左栏 `online-cohabitation-left-list` 承载分组导航和契约切换，中栏 `online-cohabitation-main-stage` 保留原 `online-module-tabpanel` 与各业务面板，右栏 `online-cohabitation-right-status` 展示契约、共同基金、共同仓库、待办、成员和默认折叠技术详情；旧 tab、旧 panel、旧 store action 与 test id 均保留。
- 证据（2026-06-05，Agent B）：`qa-online-ui-structure.mjs` 新增共同庄园三栏容器、左 / 中 / 右稳定 test id、320px 右栏规格和截图 E2E 名称守护；`game-smoke.spec.ts` 新增 `online cohabitation desktop layout keeps three columns`，1366x768 下复用 `expectRoomDesktopLayout()` 断言三栏可见、列顺序、右栏 310-380px、主舞台宽度不少于 400px，并输出 `docs/ui-smoke-2026-04-26/resp-002-online-cohabitation-desktop-1366x768.png`。
- 证据（2026-06-05，Agent B）：验证 `node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node --check taoyuan-main/e2e/game-smoke.spec.ts`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-player-copy` 均通过；非沙箱复跑 `TAOYUAN_E2E_PORT=57070 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online cohabitation desktop layout keeps three columns" --reporter=line --timeout=120000` 返回 0。
- 风险（2026-06-05，Agent B）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项仍为小屋 / 农场共同庄园入口、农场共同庄园切换与花灯墙移动端现场物件 / 贡献记录既有覆盖缺口，未出现共同庄园三栏新增断言失败；本轮未改房间三栏代码，只补 Agent B 共同庄园子范围。

## 11. QA 与验证任务

### OUI-QA-001 更新在线 UI 结构检查

- [x] `P0` 文件：`taoyuan-main/scripts/qa-online-ui-structure.mjs`
- 依赖：OUI-CMP-001 至 OUI-CMP-009。
- 执行：检查新增组件存在。
- 执行：检查房间页引用 wizard、invite、lobby、confirm。
- 执行：检查 `OnlineModuleShell` tab 语义。
- 执行：检查移动端 sticky action bar 存在。
- 验收：结构检查能保护新架构。
- 验证：`npm run qa:online-ui-structure`。
- 证据（2026-06-04，Agent A）：复核 `taoyuan-main/scripts/qa-online-ui-structure.mjs`，当前脚本已读取并检查 P0 通用组件 `OnlineActionDialog`、`OnlineBottomSheet`、`OnlineStickyActionBar`、`OnlineTechnicalDetails`、`OnlineConfirmActionDialog`，并继续守护 `OnlineModuleShell` 的 `online-module-tablist`、`role="tablist"`、`role="tab"`、`aria-selected`、tabpanel 关联和移动端 sticky tab。
- 证据（2026-06-04，Agent A）：房间流程结构断言已覆盖 `OnlineRoomWizard`、`OnlineInvitePanel`、`OnlineRoomLobbyDialog`、`OnlineConfirmActionDialog` 的必备 test id、props / emits、移动端抽屉 / 桌面弹窗复用、按钮显隐矩阵、确认文字禁用；页面接入断言覆盖 `OnlineFestivalView.vue` 节会房 / 在线节会远征，以及 `ExpeditionRoomView.vue` 独立远征的创建、邀请、准备大厅、结算、关闭、旧 test id 和原 store action 调用。
- 证据（2026-06-04，Agent A）：脚本已保护房间 QA 证据链：节会房创建 / 邀请 / 成员准备 / 房主准备与倒计时 / 结算 / 关闭 E2E 名称，远征创建 / 邀请与准备大厅 / 关闭 E2E 名称，独立远征 `standalone expedition room reuses invite lobby and close confirm`，以及 `qa-mobile-ui-smoke.mjs` 中节会房 390x844 / 360x780 创建向导、邀请面板、准备大厅、结算确认和独立远征截图场景。
- 证据（2026-06-04，Agent A）：验证通过：`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`；`npm --prefix taoyuan-main run check` 返回 0（保留既有 3 条 warning）；房间目标 E2E grep `online (festival room ...|expedition room ...|standalone expedition room reuses invite lobby and close confirm)` 返回 0。
- 风险（2026-06-04，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，但失败项均为 `qa-mobile-ui-smoke.mjs` 中小屋 / 农场共同庄园入口、在线庄园轻采、花灯墙现场物件与贡献摘要等非 Agent A 移动端覆盖缺口；未出现通用组件、房间组件、节会 / 远征房间或 OnlineModuleShell tab 语义相关失败。本轮关闭范围为 OUI-QA-001 的房间流程相关结构 QA。
- 证据（2026-06-05，Agent A）：当前态复验 `node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 返回 0；`npm --prefix taoyuan-main run qa:online-ui-structure` 返回 1，但失败清单只剩小屋 / 农场共同庄园入口与花灯墙移动端 smoke 断言。
- 风险（2026-06-05，Agent A）：本轮未改结构脚本；未出现通用房间组件、节会房、在线节会远征、独立远征或 OUI-QA-005 房间 E2E 名称相关结构失败，剩余结构 QA 阻断继续归为非 Agent A 覆盖缺口。

### OUI-QA-002 新增玩家文案扫描

- [x] `P0` 文件：`taoyuan-main/scripts/qa-online-player-copy.mjs`、`taoyuan-main/package.json`
- 依赖：OUI-002、OUI-COPY-001。
- 执行：实现扫描脚本。
- 执行：在 `package.json` 增加 `qa:online-player-copy`。
- 执行：输出替换建议。
- 执行：先允许技术详情组件内出现技术词。
- 验收：脚本能区分玩家主路径和技术详情。
- 验证：`npm run qa:online-player-copy`。
- 证据（2026-06-02，Agent B）：`taoyuan-main/package.json` 新增 `qa:online-player-copy`，当前指向严格命令 `node scripts/qa-online-player-copy.mjs`；脚本已支持技术详情组件级 allowlist、文件行号输出和替换建议。
- 证据（2026-06-02，Agent B）：`npm --prefix taoyuan-main run qa:online-player-copy` 已运行，返回 0 并输出 3 个历史玩家文案命中，作为 OUI-COPY-001 的替换输入。
- 风险（2026-06-02，Agent B）：历史依赖风险已解除；OUI-COPY-001 已关闭，严格非 0 阻断已经开启。
- 证据（2026-06-02，Agent B）：`qa-online-player-copy.mjs` 改用 `@vue/compiler-sfc` 解析完整 SFC template，修复内层 `<template #actions>` 提前截断扫描的问题；同时补齐标签属性中 `>` 引起的误切分保护，并按 `OnlineTechnicalDetails` 上下文跳过默认折叠技术 / 调试详情。
- 证据（2026-06-02，Agent B）：`node --check taoyuan-main/scripts/qa-online-player-copy.mjs` 通过；`npm --prefix taoyuan-main run qa:online-player-copy` 返回 0（warn-only），不再报告 `OnlineView.vue`、`OnlineVisualRoomShell.vue`、`ExpeditionRoomView.vue`、`online/OnlineFestivalView.vue` 的首批玩家文案命中。
- 风险（2026-06-02，Agent B）：新版完整模板扫描曾揭出 `FestivalView.vue:474` 旧页「模拟断线」以及 `OnlineCohabitationView.vue` 的 `hash / ledger / receipt / record-only / 服务端落账` 命中；这些输入已由 OUI-COHAB-006 和旧节会页跨域最小收口解除。
- 证据（2026-06-02，Agent B）：OUI-COHAB-006 收口后再次运行 `npm --prefix taoyuan-main run qa:online-player-copy`，扫描 58 个 Vue 文件，仅剩 `FestivalView.vue:474` 的「模拟断线」；`OnlineTechnicalDetails` 上下文继续作为 allowlist，技术详情折叠内容不计入玩家主路径命中。
- 证据（2026-06-02，Agent B）：旧 `FestivalView.vue` 断线调试按钮移入 `OnlineTechnicalDetails` 后再次运行 `npm --prefix taoyuan-main run qa:online-player-copy`，脚本输出 `[qa-online-player-copy] passed (58 files, 14 denylist terms, 1 allowlist entries)`；随后 `taoyuan-main/package.json` 已切到严格阻断模式。
- 证据（2026-06-02，Agent B）：`node --check taoyuan-main/scripts/qa-online-player-copy.mjs` 通过；严格运行 `npm --prefix taoyuan-main run qa:online-player-copy` 通过，输出 `[qa-online-player-copy] passed (59 files, 14 denylist terms, 1 allowlist entries)`。
- 证据（2026-06-02，Agent B）：`npm --prefix taoyuan-main run check` 通过，0 个 error，保留既有 3 条 lint warning；`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回非 0，失败项为既有移动端 smoke 覆盖、`VisualTrackBoard` 多队名次和村社仓廪覆盖缺口，未出现玩家文案扫描脚本新增失败。

### OUI-QA-003 更新可视化功能开关检查

- [x] `P0` 文件：`taoyuan-main/scripts/qa-online-visual-feature-flags.mjs`
- 依赖：OUI-CENTER-002、OUI-CMP-005。
- 执行：不再要求玩家首屏直接出现「降级」「缺失」「服务端」。
- 执行：改为要求这些内容在 `OnlineTechnicalDetails` 或后台可找到。
- 验收：QA 不再阻止文案收口。
- 验证：`npm run qa:online-visual-feature-flags`。
- 证据（2026-06-03，Agent B）：`qa-online-visual-feature-flags.mjs` 新增 `sourceBlockAfter` 和 `isInsideTechnicalDetails`，要求 `online-visual-feature-flag-panel`、`online-visual-reward-control-panel` 位于 `OnlineTechnicalDetails` 内，并继续保留 active room safe close、missing config fallback、fallback label 等治理字段检查。
- 证据（2026-06-03，Agent B）：脚本新增玩家路径断言，要求在线中心出现玩家态备用入口文案，并禁止模板继续暴露 `{{ activity.fallbackStatus }}：{{ activity.fallbackLabel }}` / `{{ entry.fallbackStatus }}：{{ entry.fallbackLabel }}` 原始拼接。
- 证据（2026-06-03，Agent B）：`npm --prefix taoyuan-main run qa:online-visual-feature-flags` 通过；`npm --prefix taoyuan-main run qa:online-player-copy` 通过；`npm --prefix taoyuan-main run check` 通过。
- 风险（2026-06-03，Agent B）：本脚本仍是静态源码检查，默认折叠的真实浏览器行为由本轮 1366x768 Playwright 截图脚本和 `qa:mobile-ui-smoke` 补证。

### OUI-QA-004 更新移动端 smoke

- [x] `P0` 文件：`taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`
- 依赖：OUI-CENTER-001、OUI-FEST-001 至 OUI-FEST-004。
- 执行：新增在线中心行动首屏场景。
- 执行：新增节会创建向导场景。
- 执行：新增邀请面板场景。
- 执行：新增准备大厅场景。
- 执行：新增结算确认场景。
- 执行：新增共同庄园家族节会确认场景。
- 验收：移动端 P0 主路径有截图和断言。
- 验证：`npm run qa:mobile-ui-smoke`。
- 证据（2026-06-03，Agent A）：`qa-mobile-ui-smoke.mjs` 新增节会房创建向导移动端场景，覆盖 390x844 / 360x780 下 `online-room-create-trigger` 打开 `OnlineRoomWizard` 底部抽屉、footer 下一步按钮、关闭按钮、背景滚动锁定、关闭后可重新打开和横向溢出断言。
- 证据（2026-06-03，Agent A）：`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs` 通过；`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，并生成 `25-online-festival-room-wizard-mobile-390x844.png`、`27-online-festival-room-wizard-mobile-360x780.png` 两张截图。
- 风险（2026-06-03，Agent A）：本轮只补 OUI-QA-004 的“节会创建向导场景”；邀请面板、准备大厅、结算确认和共同庄园家族节会确认场景仍待后续，因此任务保持 `[~]`。
- 证据（2026-06-03，Agent A）：`npm --prefix taoyuan-main run check` 返回 0，保留既有 3 个 lint warning（`onlineProfileApi.ts` import type、`CottageView.vue` / `FarmView.vue` floating promises）；`npm --prefix taoyuan-main run qa:online-ui-structure` 当前失败项为共同庄园、在线庄园、公共订单、花灯墙、村社仓廪等非 Agent A 移动端覆盖缺口，未出现本轮新增节会房向导断言失败。
- 证据（2026-06-03，Agent A）：`qa-mobile-ui-smoke.mjs` 新增 `host-running` 节会房 mock，覆盖运行中节会房准备大厅和结算确认移动端场景；390x844 / 360x780 下分别生成 `26-online-festival-room-lobby-mobile-390x844.png`、`27-online-festival-room-settle-confirm-mobile-390x844.png`、`28-online-festival-room-lobby-mobile-360x780.png`、`29-online-festival-room-settle-confirm-mobile-360x780.png`，并在 `summary.json` 写入对应 label。
- 证据（2026-06-03，Agent A）：新增 smoke 断言 `online-festival-room-lobby-trigger` 打开 `online-room-lobby`，成员列表读回房主 / 协作成员，`online-room-primary-action` 在 viewport 内且真实触控高度不低于 44px；备用操作中的 `online-festival-room-settle-submit` 会打开 `online-room-settle-confirm` / `online-confirm-action-dialog`，影响对象、资产变化、恢复提示、确认 / 取消按钮均可见且无横向溢出。
- 证据（2026-06-03，Agent A）：为满足真实移动端高度断言，`OnlineRoomLobbyDialog.vue` 给 `online-room-primary-action` 增加组件内 `online-room-lobby__primary-action` 样式，确保主行动实测不低于 44px；未改 store action、房间状态机、接口字段或旧 test id。
- 证据（2026-06-03，Agent A）：`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs` 通过；`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，运行期间仍打印既有 `127.0.0.1:4013` 后端代理拒绝日志，但本轮 mock 场景和截图全部完成。
- 风险（2026-06-03，Agent A）：`npm --prefix taoyuan-main run check` 当前被 Agent B 范围 `OnlineCohabitationView.vue(2235,10)` 的 `Type '(value: number) => string' is not assignable to type '(value?: number | undefined) => string'` 阻断；`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回非 0，失败项集中在共同庄园权限面板、共同庄园 / 在线庄园 / 公共订单 / 花灯墙 / 村社仓廪移动端覆盖缺口，未出现本轮新增节会房大厅或结算确认断言失败。
- 风险（2026-06-03，Agent A）：OUI-QA-004 已补“准备大厅场景”和“结算确认场景”；“邀请面板场景”和“共同庄园家族节会确认场景”仍未完成，因此任务继续保持 `[~]`。
- 证据（2026-06-03，Agent A）：`qa-mobile-ui-smoke.mjs` 新增节会房邀请面板场景，390x844 / 360x780 下生成 `36-online-festival-room-invite-panel-mobile-390x844.png`、`37-online-festival-room-invite-panel-mobile-360x780.png`；`summary.json` 的两个 `pageChecks` 均记录 `horizontalOverflow: false`。
- 证据（2026-06-03，Agent A）：新增节会房邀请接口 mock，断言 `online-festival-room-invite-trigger` 打开 `online-invite-panel`，批量输入按中文逗号 / 空格拆分，已在房成员被识别为“已在房间”，`online-invite-submit` 只发送 2 个可邀请对象，发送后结果列表显示 `lantern_guest` / `wish_helper` 已邀请；`OnlineInvitePanel.vue` footer 主 / 次按钮增加组件内 44px 最小高度保护，未改 store action、接口字段或旧 test id。
- 证据（2026-06-03，Agent A）：`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs` 通过；`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，运行期间仍打印既有 `127.0.0.1:4013` Vite proxy 拒绝日志。
- 风险（2026-06-03，Agent A）：`npm --prefix taoyuan-main run check` 当前被非 Agent A 范围 `CookingView.vue(108,62)`、`GameLayout.vue(378,53)` 的 `ItemIconSize` 类型不匹配阻断；`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回非 0，失败项集中在共同庄园 / 在线庄园 / 公共订单 / 花灯墙 / 村社仓廪移动端覆盖缺口，未出现本轮新增节会邀请面板断言失败。
- 风险（2026-06-03，Agent A）：OUI-QA-004 的节会创建向导、邀请面板、准备大厅、结算确认均已补移动端 smoke；共同庄园家族节会确认场景仍未完成，因此任务继续保持 `[~]`。
- 证据（2026-06-03，Agent A）：`qa-mobile-ui-smoke.mjs` 新增共同庄园家族节会确认 mock，覆盖 `/#/game/online/cohabitation?tab=festivalSeats` 下 `online-cohabitation-family-festival-panel`、`online-cohabitation-family-festival-settle-confirm-trigger` 打开 `online-confirm-action-dialog`，断言影响对象、资产变化、不可撤销提示、恢复提示、确认文字输入、确认按钮初始禁用、取消按钮和无横向溢出。
- 证据（2026-06-03，Agent A）：`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs` 通过；`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，并生成 `38-online-cohabitation-family-festival-confirm-mobile-390x844.png`、`39-online-cohabitation-family-festival-confirm-mobile-360x780.png`；`summary.json` 两个新增 `pageChecks` 均记录 `horizontalOverflow: false`。
- 证据（2026-06-03，Agent A）：`npm --prefix taoyuan-main run check` 返回 0，保留既有 3 条 lint warning（`onlineProfileApi.ts` import type、`CottageView.vue` / `FarmView.vue` floating promises）。
- 风险（2026-06-03，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项为小屋 / 农场共同庄园入口、在线庄园轻采、公共订单接力、花灯墙、村社仓廪等既有或 Agent B 范围移动端覆盖缺口，未出现本轮新增家族节会确认断言失败；`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts` 返回 1，`.last-run.json` 记录 19 个既有页面级失败，本轮未修改 E2E spec。
- 证据（2026-06-04，Agent A）：`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs` 返回 0；提权运行 `TAOYUAN_MOBILE_SMOKE_ONLY=online-festival-room npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，重新生成 `docs/ui-smoke-2026-04-26/25-online-festival-room-wizard-mobile-390x844.png`、`26-online-festival-room-lobby-mobile-390x844.png`、`36-online-festival-room-invite-panel-mobile-390x844.png`、`27-online-festival-room-settle-confirm-mobile-390x844.png`、`27-online-festival-room-wizard-mobile-360x780.png`、`28-online-festival-room-lobby-mobile-360x780.png`、`37-online-festival-room-invite-panel-mobile-360x780.png`、`29-online-festival-room-settle-confirm-mobile-360x780.png`。
- 证据（2026-06-04，Agent A）：提权运行 `TAOYUAN_MOBILE_SMOKE_ONLY=online-cohabitation-family-festival-confirm npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，重新生成 `docs/ui-smoke-2026-04-26/38-online-cohabitation-family-festival-confirm-mobile-390x844.png`、`39-online-cohabitation-family-festival-confirm-mobile-360x780.png`，`summary.json` 对两项记录 `horizontalOverflow: false`、`primaryActionVisibleInViewport: true`。
- 风险（2026-06-04，Agent A）：未过滤的 `npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 1，阻断点为 Agent B 范围在线委托公开接力凭证断言 `online-orders-society-board-receipts` 未读回“已完成的帮手 · 赏金 80 · 个人铜钱”；普通权限过滤运行曾因 Playwright Chromium `spawn EPERM` 被脚本跳过，已按权限规则提权复跑并获得房间相关截图证据。
- 证据（2026-06-05，Agent A）：修复 `qa-mobile-ui-smoke.mjs` 独立远征移动端 smoke 的「最近结算」模糊文本定位，改为精确匹配，避免当前页面同时出现「最近结算：0 条」和标题「最近结算」时触发 Playwright strict mode；未改页面 DOM、store action、接口字段或旧 test id。
- 证据（2026-06-05，Agent A）：当前态验证 `node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs` 返回 0；`TAOYUAN_MOBILE_SMOKE_ONLY=online-festival-room npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0；提权复跑 `TAOYUAN_MOBILE_SMOKE_ONLY=online-expedition-room npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，生成 `docs/ui-smoke-2026-04-26/46-online-expedition-room-main-mobile-390x844.png`、`47-online-expedition-room-main-mobile-360x780.png`。
- 证据（2026-06-05，Agent A）：`docs/ui-smoke-2026-04-26/summary.json` 记录独立远征 390x844 / 360x780 两个场景 `horizontalOverflow: false`、`primaryActionVisibleInViewport: true`；`npm --prefix taoyuan-main run check` 返回 0，保留既有 3 条 warning（`onlineProfileApi.ts` import type、`CottageView.vue` / `FarmView.vue` floating promises）。
- 风险（2026-06-05，Agent A）：提权运行未过滤 `npm --prefix taoyuan-main run qa:mobile-ui-smoke` 仍返回 1，阻断点为 Agent B 范围在线委托详情抽屉断言 `online-orders-detail-sheet` 内缺少 `async-community-project-detail` / “送到灯会”；运行期间仍有本地后端 `127.0.0.1:4013` proxy 拒连噪声，但节会房与独立远征房过滤场景均已返回 0。
- 证据（2026-06-05，Agent A）：本轮复验 `node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs` 返回 0；`$env:TAOYUAN_MOBILE_SMOKE_ONLY='online-festival-room'; npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，重新覆盖节会房创建向导、准备大厅、邀请面板、结算确认 390x844 / 360x780 截图。
- 证据（2026-06-05，Agent A）：沙箱内 `$env:TAOYUAN_MOBILE_SMOKE_ONLY='online-expedition-room'; npm --prefix taoyuan-main run qa:mobile-ui-smoke` 因 Playwright Chromium `spawn EPERM` 被脚本跳过；按权限规则非沙箱复跑同一命令返回 0，`summary.json` 记录 `46-online-expedition-room-main-mobile-390x844`、`47-online-expedition-room-main-mobile-360x780` 均为 `horizontalOverflow: false`、`primaryActionVisibleInViewport: true`。
- 风险（2026-06-05，Agent A）：本轮未跑未过滤全量 `qa:mobile-ui-smoke`；过滤场景运行期间仍有本地后端 `127.0.0.1:4013` proxy 拒连噪声，但节会房 / 独立远征房移动端主路径均已获得真实浏览器截图证据。
- 证据（2026-06-05，Agent A）：`qa-mobile-ui-smoke.mjs` 的 `openHome()` 首页导航改为 `waitUntil: 'commit'`、3 次短重试，并把「桃源乡」标题与「新的旅程」按钮等待提升到 15 秒；该改动只加固 smoke 启动阶段，不改变业务页面断言、截图 label、房间 mock、store action 或旧 test id。
- 证据（2026-06-05，Agent A）：本轮复验 `node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-ui-structure` 均返回 0；提权复跑 `$env:TAOYUAN_E2E_PORT='57068'; $env:TAOYUAN_MOBILE_SMOKE_ONLY='online-expedition-room'; npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，仍只出现本地后端 `127.0.0.1:4013` proxy 拒连噪声。
- 风险（2026-06-05，Agent A）：helper 加固后再次提权复跑 `$env:TAOYUAN_E2E_PORT='57070'; $env:TAOYUAN_MOBILE_SMOKE_ONLY='online-festival-room'; npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 1，失败仍停在新 Vite 端口首页 `page.goto(..., waitUntil: 'commit')` 15 秒提交超时，未进入节会房 DOM 或业务断言；当前机器已有大量并行 `npm run dev` / Vite node 进程，本条只能作为本地启动环境风险，不能作为节会房 smoke 失败证据。
- 证据（2026-06-05，Agent A）：使用本轮专用外部 dev server `http://127.0.0.1:58100` 复跑房间移动端过滤 smoke：`TAOYUAN_BASE_URL=http://127.0.0.1:58100 TAOYUAN_SKIP_DEV_SERVER=1 TAOYUAN_MOBILE_SMOKE_ONLY=online-festival-room npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0；随后 `TAOYUAN_MOBILE_SMOKE_ONLY=online-expedition-room` 返回 0。
- 证据（2026-06-05，Agent A）：截图文件已生成 / 刷新：节会房 `25-online-festival-room-wizard-mobile-390x844.png`、`26-online-festival-room-lobby-mobile-390x844.png`、`36-online-festival-room-invite-panel-mobile-390x844.png`、`27-online-festival-room-settle-confirm-mobile-390x844.png`、`27-online-festival-room-wizard-mobile-360x780.png`、`28-online-festival-room-lobby-mobile-360x780.png`、`37-online-festival-room-invite-panel-mobile-360x780.png`、`29-online-festival-room-settle-confirm-mobile-360x780.png`；独立远征 `46-online-expedition-room-main-mobile-390x844.png`、`47-online-expedition-room-main-mobile-360x780.png`。
- 证据（2026-06-05，Agent B）：`qa-mobile-ui-smoke.mjs` 补齐非房间剩余结构缺口：新增 `prepareCottageCohabitationEntryMobile`、`prepareFarmCohabitationSwitchMobile` 和 `40/41-cottage-cohabitation-entry-*`、`42/43-farm-cohabitation-switch-*` 场景；花灯墙 `prepareOnlineSocietyProjectsMobile` 追加 `好友留言`、`纪念墙`、`愿望册`、`async-community-project-readback` 等价读回、`1 人 · 1 条历史`、`移动端烟测号 提交了 写愿望（+10）`、`移动端烟测号写下一张愿望签。` 断言。
- 证据（2026-06-05，Agent B）：验证 `node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`npm --prefix taoyuan-main run qa:online-ui-structure`、`npm --prefix taoyuan-main run qa:online-player-copy`、`npm --prefix taoyuan-main run qa:online-visual-feature-flags` 均返回 0；`qa:online-ui-structure` 当前输出“在线 UI 结构静态检查通过：112 个表单控件复用 online-* 类，86 处滚动边界仍在”。
- 证据（2026-06-05，Agent B）：沙箱内 `TAOYUAN_MOBILE_SMOKE_ONLY=cottage-cohabitation-entry npm --prefix taoyuan-main run qa:mobile-ui-smoke` 因 Playwright Chromium `spawn EPERM` 被脚本跳过；提权并复用当时可达的 `TAOYUAN_BASE_URL=http://127.0.0.1:4175` 后返回 0，生成 `40-cottage-cohabitation-entry-mobile-390x844.png`、`41-cottage-cohabitation-entry-mobile-360x780.png`，summary 记录两个场景 `horizontalOverflow: false`、`primaryActionVisibleInViewport: true`。
- 风险（2026-06-05，Agent B）：后续 `farm-cohabitation-switch` 与 `online-society-projects` 过滤 smoke 复验受本机 dev server 生命周期阻断：4175 外部服务随后不可达，脚本自启 / Start-Process / Start-Job 临时 4190 均在 `waitForServer` 或首页提交阶段超时，未稳定进入目标 DOM；因此本轮不把农场和花灯墙浏览器截图记为通过，只以结构 QA、脚本语法、文案扫描和已通过的小屋入口截图作为证据。
- 证据（2026-06-05，Agent B）：`qa-mobile-ui-smoke.mjs` 将 `cottage-cohabitation-entry`、`farm-cohabitation-switch`、`online-society-projects` 加入样例档不可用时的新旅程兜底白名单；这些场景已有页面级 API mock，可在生产构建静态 server 下继续验证目标 DOM，不影响未过滤全量 smoke 的 dev-only 样例载入要求。
- 证据（2026-06-05，Agent B）：`npm --prefix taoyuan-main run build` 返回 0（仅保留 Vite 大 chunk warning）；用只读静态 server `http://127.0.0.1:58121` 复跑 `TAOYUAN_BASE_URL=http://127.0.0.1:58121 TAOYUAN_SKIP_DEV_SERVER=1 TAOYUAN_MOBILE_SMOKE_ONLY=farm-cohabitation-switch npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，刷新 `42-farm-cohabitation-switch-mobile-390x844.png`、`43-farm-cohabitation-switch-mobile-360x780.png`。
- 证据（2026-06-05，Agent B）：同一静态 server 复跑 `TAOYUAN_BASE_URL=http://127.0.0.1:58121 TAOYUAN_SKIP_DEV_SERVER=1 TAOYUAN_MOBILE_SMOKE_ONLY=online-society-projects npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，刷新 `28-online-society-projects-mobile-390x844.png`、`29-online-society-projects-mobile-360x780.png`；`summary.json` 记录两个花灯墙场景 `horizontalOverflow: false`、`primaryActionVisibleInViewport: true`。
- 风险（2026-06-05，Agent B）：农场与花灯墙过滤 smoke 的浏览器证据已补齐；PR-F 仍不能据此关闭未过滤全量 `qa:mobile-ui-smoke`，因为未过滤模式仍依赖 dev-only 样例档注入，静态 server 只覆盖本轮 mock-friendly 过滤场景。静态 server 下 `summary.json` 仍记录 `/api/taoyuan/online/realtime` WebSocket 404，这是未 mock 的实时通道占位，不影响本轮目标场景退出码。
- 证据（2026-06-05，Agent B）：修复委托详情抽屉接力确认后的 mobile smoke 断言并复跑未过滤全量 `npm --prefix taoyuan-main run qa:mobile-ui-smoke`，命令返回 0；`docs/ui-smoke-2026-04-26/summary.json` 生成时间 `2026-06-05T04:10:08.437Z`，包含 `25-online-orders-mobile-390x844.png`、`27-online-orders-mobile-360x780.png` 等截图，委托两视口均无横向溢出且主行动可见。
- 风险（2026-06-05，Agent B）：未过滤 mobile smoke 已通过，但运行期间仍有本地后端 `127.0.0.1:4013` 的 `item-icon-preferences`、realtime ws、cohabitation contracts 代理拒连噪声；这些请求未阻断 mock 场景和截图生成，仍建议 PR 收尾时保留为环境噪声观察项。

### OUI-QA-005 更新浏览器 E2E

- [x] `P0` 文件：`taoyuan-main/e2e/game-smoke.spec.ts`
- 依赖：OUI-FEST-001 至 OUI-FEST-006、OUI-EXP-001。
- 执行：新增房主创建节会房完整路径。
- 执行：新增房主创建远征房完整路径。
- 执行：新增成员接受邀请并准备。
- 执行：新增房主开始准备和倒计时。
- 执行：新增房主结算确认和关闭确认。
- 执行：新增 Esc 关闭普通弹窗、危险弹窗确认文字、焦点返回。
- 验收：关键房间流程有浏览器级保障。
- 验证：`npm run test:e2e`。
- 证据（2026-06-03，Agent A）：新增 `online festival room close confirm requires text escape focus and submits`，覆盖节会房危险关闭确认、确认文字门槛、Esc 取消、焦点返回触发按钮和既有关闭接口提交；同时为 festival close endpoint 增加 E2E mock，关闭后读回「房主已关闭本轮房间。」。
- 风险（2026-06-03，Agent A）：本轮只补关闭确认与 dialog 键盘焦点浏览器保护；房主创建节会 / 远征、成员接受邀请并准备、房主开始准备和倒计时等完整路径仍待后续补齐，因此任务保持 `[~]`。
- 风险（2026-06-03，Agent A）：本轮复跑 `npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts`，命令返回 1；`test-results/.last-run.json` 记录 17 个失败，样例包括共同庄园 tab query alias 断言 `online-module-tab-visibility` 隐藏、NPC 页 `npc-view` 未出现、在线中心用例未命中 `online-center`、节会关闭确认用例停在首页未进入 `online-festival-room-my-room`。本轮未修改 `game-smoke.spec.ts`，这些失败未作为 OUI-QA-004 移动端 smoke 的通过证据。
- 风险（2026-06-03，Agent A）：本轮补节会邀请面板 smoke 后复跑 `npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts`，命令返回 1；`taoyuan-main/test-results/.last-run.json` 记录 13 个失败，抽样包括远征房 `online-expedition-room-my-room` 未出现、在线庄园照料 / 轻采等待「照料」按钮超时、护理房 `online-manor-page` 未出现。未出现本轮新增 `online-invite-panel` 移动端 smoke 断言失败。
- 证据（2026-06-03，Agent A）：`game-smoke.spec.ts` 新增 `online festival room lobby starts ready check and countdown`，覆盖节会房从准备大厅点击 `online-room-action-start-ready-check` 调用 `/ready-check`，更新成员为已准备，再点击 `online-room-primary-action` 调用 `/start`，读回 `ready_check` / `countdown` 状态、成员「倒计时锁定」和 `online-visual-room-countdown` 的「倒计时 30 秒」。
- 证据（2026-06-03，Agent A）：`mockOnlineVisualRoom` 新增 festival `/ready-check` 与 `/start` mock，`startNewJourney` 增加 `game-layout` 等待以消除全量 E2E 开局竞态；房间载入 helper 优先保留旧 `online-festival-room-my-room` / `online-expedition-room-my-room`，并用状态面板与房间壳操作按钮兜底等待，不删除旧 test id。
- 证据（2026-06-03，Agent A）：`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online festival room (lobby starts ready check and countdown|close confirm requires text escape focus and submits)"` 返回 0；`npm --prefix taoyuan-main run check` 返回 0，保留既有 3 条 lint warning（`onlineProfileApi.ts` import type、`CottageView.vue` / `FarmView.vue` floating promises）。
- 风险（2026-06-03，Agent A）：`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts` 仍返回 1，但 `.last-run.json` 已从本轮修复前的 18 个失败收窄到 14 个失败，失败目录不再包含新增倒计时场景或关闭确认场景；剩余样例包括远征节点选择、共同庄园 / 宠物 / NPC / 在线中心等既有页面级失败，后续 OUI-QA-005 仍需继续补房主创建节会 / 远征、成员接受邀请并准备等完整路径。
- 风险（2026-06-03，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项集中在小屋 / 农场共同庄园入口、在线庄园轻采、公共订单、花灯墙、村社仓廪等既有或 Agent B 范围移动端 smoke 覆盖缺口；未出现本轮新增准备倒计时 E2E 结构断言。
- 证据（2026-06-03，Agent A）：`game-smoke.spec.ts` 新增 `online festival room wizard creates host room`，覆盖空闲节会房状态打开 `OnlineRoomWizard`、选择 `lantern_fair`、填写标题、邀请名单进入确认页、提交 `/api/taoyuan/online/festival/rooms` POST，并在创建后读回 `online-festival-room-status-panel`、`online-festival-room-lobby-trigger`、`online-festival-room-invite-trigger`。
- 证据（2026-06-03，Agent A）：`mockOnlineVisualRoom` 新增 `startWithoutRoom` 起始态和 festival rooms POST mock，创建后以同一个 overview 注入房主房间；旧 `online-festival-room-my-room` / 状态面板等待和旧 test id 继续保留，不改变 store action 或后端状态机语义。
- 证据（2026-06-03，Agent A）：`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online festival room wizard creates host room" --reporter=line` 返回 0；组合 grep `online festival room (wizard creates host room|lobby starts ready check and countdown|close confirm requires text escape focus and submits)` 返回 0；`npm --prefix taoyuan-main run check` 返回 0，保留既有 3 条 lint warning（`onlineProfileApi.ts` import type、`CottageView.vue` / `FarmView.vue` floating promises）。
- 风险（2026-06-03，Agent A）：`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts --reporter=line` 仍返回 1；当次 `.last-run.json` 记录 38 个 failedTests，新增创建向导产物的 `error-context.md` 显示失败停在 `openHome` 的 `page.goto('/')`，错误为 `net::ERR_ABORTED; maybe frame was detached`，尚未进入节会房业务路径；随后新增单场景复跑返回 0。
- 风险（2026-06-03，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项仍集中在小屋 / 农场共同庄园入口、在线庄园轻采、公共订单、花灯墙、村社仓廪等既有或 Agent B 范围移动端 smoke 覆盖缺口；`npm --prefix taoyuan-main run qa:mobile-ui-smoke` 首次在 `openHome` 的 `page.goto('/')` 超时，复跑 240 秒超时，未进入本轮新增节会创建 E2E 路径。
- 证据（2026-06-03，Agent A）：`game-smoke.spec.ts` 新增 `online expedition room wizard creates host room`，覆盖在线节会远征标签空闲状态打开 `OnlineRoomWizard domain="expedition"`、选择 `expedition_outpost`、填写队伍标题、邀请名单进入确认页、展示撤离规则、提交 `/api/taoyuan/online/expedition/rooms` POST，并在创建后读回 `online-expedition-room-status-panel`、`online-expedition-room-invite-trigger`、`online-expedition-room-ready-check-submit`。
- 证据（2026-06-03，Agent A）：`mockOnlineVisualRoom` 复用 `startWithoutRoom` 起始态并新增 expedition rooms POST mock，创建后以同一个 overview 注入房主远征房；旧 `online-expedition-room-my-room` / 状态面板等待、旧准备 test id 和原 `expeditionRoomStore.createRoom()` 接口语义继续保留。
- 证据（2026-06-03，Agent A）：`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online expedition room wizard creates host room" --reporter=line` 返回 0；组合 grep `online (festival|expedition) room (wizard creates host room|lobby starts ready check and countdown|close confirm requires text escape focus and submits)` 返回 0；`npm --prefix taoyuan-main run check` 返回 0，保留既有 3 条 lint warning。
- 风险（2026-06-03，Agent A）：`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts --reporter=line` 仍返回 1；`.last-run.json` 记录 9 个 failedTests，失败目录为共同庄园 tab、在线庄园、宠物、NPC、育种、炼丹等页面级用例，不包含新增远征创建、节会创建、倒计时或关闭确认场景。
- 风险（2026-06-03，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项集中在小屋 / 农场共同庄园入口、在线庄园轻采、在线委托 live smoke、公共订单、花灯墙、村社仓廪等既有或 Agent B 范围覆盖缺口；未出现本轮新增远征创建 E2E 结构断言。
- 证据（2026-06-03，Agent A）：`game-smoke.spec.ts` 新增 `online festival room member accepts invite and readies`，覆盖受邀成员 `my_member_status="invited"` 打开 `online-room-lobby`，通过 `online-room-primary-action` 依次触发 `/join` 与 `/ready`，读回成员行 `online-room-member-tester` 从“待接受邀请 / 被邀请”变为“已加入 / 未准备”，再变为“已准备”，主行动从“接受邀请”切到“我已准备”再切到“取消准备”。
- 证据（2026-06-03，Agent A）：`mockOnlineVisualRoom` 新增 festival `/join` 与 `/ready` POST mock，只推进成员状态、`my_member_status`、`can_ready` / `can_unready` 和 `ready_member_count`，不改 store action、接口字段或旧 test id。
- 证据（2026-06-03，Agent A）：`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online festival room member accepts invite and readies" --reporter=line` 返回 0；房间组合 grep `online (festival room (wizard creates host room|invite panel handles success failure and retry|member accepts invite and readies|lobby starts ready check and countdown|close confirm requires text escape focus and submits)|expedition room wizard creates host room)` 返回 0；`npm --prefix taoyuan-main run check` 返回 0，保留既有 3 条 lint warning。
- 风险（2026-06-03，Agent A）：`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts --reporter=line` 仍返回 1；`.last-run.json` 记录 23 个 failedTests，部分房间 grep 已通过场景在全量并发中停在 `openHome` 的 `page.goto('/')` 加载超时，未进入房间业务路径；其他失败仍混有在线中心、共同庄园、在线庄园、宠物、NPC、育种、炼丹等页面级用例。
- 风险（2026-06-03，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项为小屋 / 农场共同庄园入口、在线庄园轻采、公共订单接力、花灯墙、村社仓廪等既有或 Agent B 范围覆盖缺口；未出现本轮新增成员接受邀请与准备断言。
- 证据（2026-06-03，Agent A）：`game-smoke.spec.ts` 新增 `online festival room settle confirm shows impacts and records receipt`，覆盖运行中节会房从旧备用 `online-festival-room-settle-submit` 进入 `online-room-settle-confirm` / `online-confirm-action-dialog`，断言影响对象包含房间名和“结算成员”、资产变化显示“奖励预览将在结算后生成”、恢复提示说明房间会保留当前状态，确认后提交既有 `/settle` 接口并读回 `settled` 状态、结算记录和 `90 铜钱、1 张奖券、lantern_token x2` 奖励。
- 证据（2026-06-03，Agent A）：`mockOnlineVisualRoom` 复用 festival `onSettle` 推进结算结果，只更新本轮房间快照、最近结算记录和奖励读回；`openHome` 增加最多 3 次 `domcontentloaded` 短重试，降低全量 E2E 首页启动竞态，不改路由、store action、接口字段或旧 test id。
- 证据（2026-06-03，Agent A）：`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online festival room settle confirm shows impacts and records receipt" --reporter=line` 返回 0；房间组合 grep `online (festival room (wizard creates host room|invite panel handles success failure and retry|member accepts invite and readies|lobby starts ready check and countdown|settle confirm shows impacts and records receipt|close confirm requires text escape focus and submits)|expedition room wizard creates host room)` 返回 0；`npm --prefix taoyuan-main run check` 返回 0，保留既有 3 条 lint warning。
- 风险（2026-06-03，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项集中在小屋 / 农场共同庄园入口、在线庄园轻采、公共订单、花灯墙、村社仓廪等既有或 Agent B 范围覆盖缺口；`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts --reporter=line` 仍返回 1，`.last-run.json` 记录 20 个 failedTests，最新错误多为首页 `openHome` 加载超时或非房间页面级失败，本轮房间组合 grep 已通过。
- 证据（2026-06-03，Agent A）：`game-smoke.spec.ts` 新增 `online festival room wizard escape closes dialog and restores focus`，在 1280x720 桌面视口打开空闲节会房 `OnlineRoomWizard`，断言其由 `online-action-dialog` 普通弹窗承载，按 Esc 后 `online-action-dialog` / `online-room-wizard` 关闭，焦点返回 `online-room-create-trigger`，并可再次打开到 `online-room-wizard-step-gameplay`。
- 证据（2026-06-03，Agent A）：`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online festival room wizard escape closes dialog and restores focus" --reporter=line` 返回 0；房间组合 grep `online (festival room (wizard escape closes dialog and restores focus|wizard creates host room|invite panel handles success failure and retry|member accepts invite and readies|lobby starts ready check and countdown|settle confirm shows impacts and records receipt|close confirm requires text escape focus and submits)|expedition room wizard creates host room)` 返回 0；`npm --prefix taoyuan-main run check` 返回 0，保留既有 3 条 lint warning。
- 风险（2026-06-03，Agent A）：本轮复跑 `npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项仍为小屋 / 农场共同庄园入口、在线庄园轻采、公共订单、花灯墙、村社仓廪等非 Agent A 覆盖缺口；复跑 `npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts --reporter=line` 仍返回 1，`.last-run.json` 记录 11 个 failedTests，失败名称包含在线中心入口、在线庄园、宠物、NPC、育种、炼丹、公共订单、共同庄园 tab 等页面级路径，不包含本轮新增房间键盘场景或房间组合 grep 场景。
- 证据（2026-06-04，Agent A）：`game-smoke.spec.ts` 新增 `gotoOnlineFestivalTab()`，将在线节会 / 远征房间与相邻节会视觉用例的 hash 跳转改为“跳转后等待目标 test id，失败最多重试 3 次”；对应房间、节会视觉和远征视觉用例加 `test.slow()`，降低全量并发下仍停在首页 / 田庄时继续断言的误杀，不改路由、store action、接口字段或旧 test id。
- 证据（2026-06-04，Agent A）：`node --check taoyuan-main/e2e/game-smoke.spec.ts` 返回 0；房间组合 grep `online (festival room (wizard escape closes dialog and restores focus|wizard creates host room|invite panel handles success failure and retry|member accepts invite and readies|lobby starts ready check and countdown|settle confirm shows impacts and records receipt|close confirm requires text escape focus and submits)|expedition room wizard creates host room)` 返回 0；相邻节会 / 远征视觉 grep `online (expedition visual map supports cavern node actions|festival visual scene supports lantern object actions|festival visual scene supports laba cookpot object actions|festival visual track supports dragon boat cell actions|festival memorials can load friend lantern photo replay)` 返回 0。
- 证据（2026-06-04，Agent A）：`npm --prefix taoyuan-main run check` 返回 0，保留既有 3 条 lint warning（`onlineProfileApi.ts` import type、`CottageView.vue` / `FarmView.vue` floating promises）；完整 `npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts --reporter=line` 返回 1，但 `.last-run.json` 已收窄为 11 个非房间失败，失败项为在线庄园照料 / 轻采、宠物喂食、NPC、共同庄园、炼丹、育种样例和在线委托公开接力，不再包含 OUI-QA-005 房主创建、邀请、成员准备、倒计时、结算、关闭、Esc 焦点或节会 / 远征房间视觉场景。
- 风险（2026-06-04，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项集中在小屋 / 农场共同庄园入口、在线庄园轻采、花灯墙、村社仓廪等非 Agent A 覆盖缺口；完整 E2E 仍有 11 个非房间页面级失败，故本轮关闭范围仅限 OUI-QA-005 的关键房间流程浏览器保障。
- 证据（2026-06-04，Agent A）：补充旧备用入口浏览器保护：`online festival room backup ready action submits original endpoint` 返回 0，覆盖展开 `OnlineTechnicalDetails` 内的旧 `online-festival-room-ready-check-submit`、提交原 `/ready-check` 接口、读回准备确认状态和旧 `online-festival-room-start-submit`；`qa-online-ui-structure.mjs` 同步守护该 E2E 名称，避免后续 UI 重排只保留静态 test id 而断掉真实提交。
- 证据（2026-06-04，Agent A）：本轮复跑 `node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`npm --prefix taoyuan-main run check`、目标 E2E grep `online festival room backup ready action submits original endpoint` 均返回 0；`npm --prefix taoyuan-main run check` 继续保留既有 3 条 warning。
- 风险（2026-06-04，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项为小屋 / 农场共同庄园入口与花灯墙移动端 smoke 覆盖缺口，未出现新增备用操作 E2E 名称或房间流程结构断言失败。
- 证据（2026-06-04，Agent A）：继续补在线节会远征旧备用入口浏览器保护：`online expedition room backup ready action submits original endpoint` 返回 0，覆盖展开 `OnlineTechnicalDetails` 内的旧 `online-expedition-room-ready-check-submit`、提交原 `/expedition/rooms/*/ready-check` 接口、读回准备确认状态和旧 `online-expedition-room-start-submit`；结构 QA 同步守护该 E2E 名称。
- 证据（2026-06-04，Agent A）：本轮复跑 `node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`npm --prefix taoyuan-main run check`、备用 ready 组合 E2E grep `online (festival|expedition) room backup ready action submits original endpoint` 均返回 0；`npm --prefix taoyuan-main run check` 继续保留既有 3 条 warning。
- 风险（2026-06-04，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项仍为小屋 / 农场共同庄园入口与花灯墙移动端 smoke 覆盖缺口，未出现新增在线节会远征备用 ready 断言失败。
- 证据（2026-06-04，Agent A）：补齐独立远征旧备用入口浏览器保护：`standalone expedition room backup ready action submits original endpoint` 返回 0，覆盖 `/#/game/expedition-room` 下旧 `expedition-room-ready-check-submit`、提交原 `/expedition/rooms/*/ready-check` 接口、读回准备确认状态和旧 `expedition-room-start-submit`；结构 QA 同步守护该 E2E 名称。
- 证据（2026-06-04，Agent A）：本轮复跑 `node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`npm --prefix taoyuan-main run check`、三条备用 ready 组合 E2E grep `(online (festival|expedition) room|standalone expedition room) backup ready action submits original endpoint` 均返回 0；`npm --prefix taoyuan-main run check` 继续保留既有 3 条 warning。
- 风险（2026-06-04，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项仍为小屋 / 农场共同庄园入口与花灯墙移动端 smoke 覆盖缺口，未出现新增独立远征备用 ready 断言失败。
- 证据（2026-06-04，Agent A）：补齐远征旧备用结算入口浏览器保护：`online expedition room backup settle action submits original endpoint` 与 `standalone expedition room backup settle action submits original endpoint` 返回 0，覆盖在线节会远征和独立远征旧 `*-settle-submit` 从折叠备用操作进入结算确认、提交原 `/expedition/rooms/*/settle` 接口，并读回 `online-visual-room-settlement-replay` 的结算奖励记录。
- 证据（2026-06-04，Agent A）：本轮复跑 `node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`npm --prefix taoyuan-main run check`、目标 E2E grep `(online expedition room|standalone expedition room) backup settle action submits original endpoint` 均返回 0；`npm --prefix taoyuan-main run check` 继续保留既有 3 条 warning。
- 风险（2026-06-04，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项仍为小屋 / 农场共同庄园入口与花灯墙移动端 smoke 覆盖缺口；未出现新增备用 settle E2E 名称或房间流程结构断言失败。
- 证据（2026-06-04，Agent A）：补齐旧备用创建入口浏览器保护：`online festival room backup create action submits original endpoint`、`online expedition room backup create action submits original endpoint`、`standalone expedition room backup create action submits original endpoint` 返回 0，覆盖节会房、在线节会远征和独立远征折叠备用创建表单里的旧 create submit test id 仍提交原创建 POST，并读回创建后状态与大厅入口。
- 证据（2026-06-04，Agent A）：本轮复跑 `node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`npm --prefix taoyuan-main run check`、目标 E2E grep `(online festival room|online expedition room|standalone expedition room) backup create action submits original endpoint` 均返回 0；`npm --prefix taoyuan-main run check` 继续保留既有 3 条 warning。
- 风险（2026-06-04，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项为小屋 / 农场共同庄园入口与花灯墙移动端 smoke 覆盖缺口；未出现新增备用 create E2E 名称或房间流程结构断言失败。
- 证据（2026-06-04，Agent A）：补齐旧备用邀请入口浏览器保护：`online festival room backup invite action submits original endpoint`、`online expedition room backup invite action submits original endpoint`、`standalone expedition room backup invite action submits original endpoint` 返回 0，覆盖节会房、在线节会远征和独立远征折叠备用邀请表单里的旧 invite submit test id 仍提交原 `/invite` POST，并由原 store action 清空输入。
- 证据（2026-06-04，Agent A）：本轮复跑 `node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`npm --prefix taoyuan-main run check`、目标 E2E grep `(online festival room|online expedition room|standalone expedition room) backup invite action submits original endpoint` 均返回 0；`npm --prefix taoyuan-main run check` 继续保留既有 3 条 warning。
- 风险（2026-06-04，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项为小屋 / 农场共同庄园入口与花灯墙移动端 smoke 覆盖缺口；未出现新增备用 invite E2E 名称或房间流程结构断言失败。
- 证据（2026-06-04，Agent A）：补齐旧备用关闭入口浏览器保护：`online festival room backup close action submits original endpoint`、`online expedition room backup close action submits original endpoint`、`standalone expedition room backup close action submits original endpoint` 返回 0，覆盖节会房、在线节会远征和独立远征折叠备用操作里的旧 close submit test id 仍先进入高风险确认弹窗，填写确认文字后提交原 `/close` POST，并读回已关闭状态。
- 证据（2026-06-04，Agent A）：本轮复跑 `node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`npm --prefix taoyuan-main run check`、目标 E2E grep `(online festival room|online expedition room|standalone expedition room) backup close action submits original endpoint` 均返回 0；`npm --prefix taoyuan-main run check` 继续保留既有 3 条 warning。
- 风险（2026-06-04，Agent A）：`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项为小屋 / 农场共同庄园入口与花灯墙移动端 smoke 覆盖缺口；未出现新增备用 close E2E 名称或房间流程结构断言失败。
- 证据（2026-06-04，Agent A）：当前态复验通过：`npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "(online festival room|online expedition room|standalone expedition room)" --reporter=line --workers=1 --timeout=120000` 返回 0，覆盖节会房、在线节会远征、独立远征的主流程、桌面三栏、备用 create / invite / ready / settle / close、结算确认、关闭确认、Esc / 焦点返回。
- 证据（2026-06-04，Agent A）：当前态复跑 `node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`npm --prefix taoyuan-main run check` 均返回 0，`check` 保留既有 3 条 warning（`onlineProfileApi.ts` import type、`CottageView.vue` / `FarmView.vue` floating promises）。
- 风险（2026-06-04，Agent A）：当前态 `npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，失败项仍为小屋 / 农场共同庄园入口与花灯墙移动端 smoke 覆盖缺口；未出现 OUI-QA-005 房间域 E2E 或房间结构断言失败。
- 证据（2026-06-05，Agent A）：当前态复验 `node --check taoyuan-main/e2e/game-smoke.spec.ts` 返回 0；房间组合 E2E `npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "(online festival room|online expedition room|standalone expedition room)" --reporter=line --workers=1 --timeout=120000` 返回 0，继续覆盖节会房、在线节会远征、独立远征的创建、邀请、准备、倒计时、结算、关闭、备用 create / invite / ready / settle / close、桌面三栏、Esc / 焦点返回。
- 证据（2026-06-05，Agent A）：当前态复跑 `node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 和 `npm --prefix taoyuan-main run check` 均返回 0；`npm --prefix taoyuan-main run qa:online-ui-structure` 返回 1，但失败项仍只指向小屋 / 农场共同庄园入口与花灯墙移动端现场物件 / 贡献记录断言。
- 风险（2026-06-05，Agent A）：未运行完整 `npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts`；本轮浏览器证据限定为房间域 grep，已排除非房间页面级用例对 OUI-QA-005 的误判。
- 证据（2026-06-05，Agent A）：本轮复验 `node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`npm --prefix taoyuan-main run check` 均返回 0；`check` 仍只有 `onlineProfileApi.ts` import type 与 `CottageView.vue` / `FarmView.vue` floating promise 3 条既有 warning。
- 证据（2026-06-05，Agent A）：房间组合 E2E `npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "(online festival room|online expedition room|standalone expedition room)" --reporter=line --workers=1 --timeout=120000` 返回 0，继续覆盖节会房、在线节会远征、独立远征主流程、备用 create / invite / ready / settle / close、桌面三栏、结算确认、关闭确认、Esc 与焦点返回。
- 风险（2026-06-05，Agent A）：本轮 `npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，剩余失败为小屋 / 农场共同庄园入口与花灯墙移动端 smoke 覆盖缺口；未运行完整未过滤 `test:e2e`，OUI-QA-005 当前证据仍限定为房间域 grep。
- 证据（2026-06-05，Agent A）：`game-smoke.spec.ts` 的 `openHome()` 首页导航改为 `waitUntil: 'commit'` 后继续保留 3 次重试和「桃源乡」/「新的旅程」就绪断言；验证 `node --check taoyuan-main/e2e/game-smoke.spec.ts`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-ui-structure` 均返回 0。
- 风险（2026-06-05，Agent A）：本轮尝试复跑房间域 E2E grep 和单条 `online expedition room close confirm requires text escape focus and submits`，提权后仍返回 1；`test-results/.../error-context.md` 显示失败停在首页 `page.goto('/', waitUntil: 'commit')` 对独立 Vite 端口的提交超时，页面未进入远征房关闭确认断言。当前本轮不把该 E2E 记为通过，OUI-QA-005 的可通过浏览器证据仍引用本日早前房间域 grep 已通过记录。
- 证据（2026-06-05，Agent A）：使用本轮专用外部 dev server `http://127.0.0.1:58100` 复跑房间域 E2E：`TAOYUAN_E2E_PORT=58100 TAOYUAN_E2E_EXTERNAL_SERVER=1 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "(online festival room|online expedition room|standalone expedition room)" --reporter=line --workers=1 --timeout=120000` 返回 0，28 条房间域用例全部通过，覆盖节会房、在线节会远征、独立远征的主流程、备用 create / invite / ready / settle / close、桌面三栏、结算确认、关闭确认、Esc 与焦点返回。
- 证据（2026-06-05，Agent A）：`test-results/.last-run.json` 当前为 `status: passed`、`failedTests: []`；单条自动启动 E2E `online expedition room close confirm requires text escape focus and submits` 曾在 `58106` 返回 0，但默认自动启动路径后续仍可能受本机并行 Vite 进程影响。
- 证据（2026-06-05，Agent A）：本轮修复节庆广场完工后跳转节会房间 E2E 的遮挡问题：`online society festival square contribution unlocks festival room launch` 在点击 `async-community-completion-room-link` 前先关闭 `online-bottom-sheet` 并等待抽屉卸载，保留原 query 断言 `tab=festival-room`、`template=lantern_fair`、`gameplay=assembly`。
- 证据（2026-06-05，Agent A）：跨域兼容改动：`OnlineSocietyView.vue` 的公共建设详情抽屉补 `async-community-project-readback` 回看摘要，使花灯墙贡献后的详情抽屉能真实读回「挂花灯 · 进行中」；E2E 断言迁到详情抽屉内，不改村社 store action、接口字段或房间状态机。
- 证据（2026-06-05，Agent A）：本轮验证 `node --check taoyuan-main/e2e/game-smoke.spec.ts`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-ui-structure` 均返回 0，`check` 仅保留既有 3 条 warning（`onlineProfileApi.ts` import type、`CottageView.vue` / `FarmView.vue` floating promises）。
- 证据（2026-06-05，Agent A）：目标 E2E `TAOYUAN_E2E_PORT=58216 TAOYUAN_DEV_SERVER_SETTLE_MS=5000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online society (async board supports lantern wall contribution actions|festival square contribution unlocks festival room launch)" --reporter=line --workers=1 --timeout=120000` 返回 0，2 条通过。
- 证据（2026-06-05，Agent A）：远征 / 区域样例目标 E2E `TAOYUAN_E2E_PORT=58211 TAOYUAN_DEV_SERVER_SETTLE_MS=5000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "(built-in region map|locked region|blocked route|manual region expedition|region map mobile layout)" --reporter=line --workers=1 --timeout=120000` 返回 0，5 条通过；确认上一轮 `58190` 失败源于 Python 静态 server 下无 `import.meta.env.DEV` 样例 API，不是远征流程失败。
- 风险（2026-06-05，Agent A）：本轮全量 dev server E2E `TAOYUAN_E2E_PORT=58217 TAOYUAN_DEV_SERVER_SETTLE_MS=5000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts --reporter=line --workers=1 --timeout=120000` 返回 1，结果为 59 passed / 5 failed；失败为 `processing workshop can start an alchemy furnace recipe`、`animal page can feed a pet special food`、两条 NPC 页面、`online cohabitation keeps legacy tab query aliases grouped`，均非 Agent A 房间 / 节会 / 远征范围。房间域、节庆广场房间入口和 5 条远征区域样例均已在本次全量运行中经过。
- 证据（2026-06-05，Agent A）：当前态复跑 Agent A 目标组合 `TAOYUAN_E2E_PORT=58218 TAOYUAN_DEV_SERVER_SETTLE_MS=5000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "(online festival room|online expedition room|standalone expedition room|online society festival square contribution unlocks festival room launch|built-in region map|locked region|blocked route|manual region expedition|region map mobile layout)" --reporter=line --workers=1 --timeout=120000` 返回 0，34 条通过；覆盖节会房、在线节会远征、独立远征、节庆广场房间入口和远征区域样例。
- 风险（2026-06-05，Agent A）：最新 `taoyuan-main/test-results/.last-run.json` 仍记录 5 个失败，目录对应炼丹、宠物、两条 NPC 和共同庄园 tab alias；未出现 OUI-QA-005 房间域、节庆广场房间入口或远征区域样例失败。
- 证据（2026-06-05，Agent A）：当前 HEAD `b77f7a8f` 复跑 `node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-ui-structure` 均返回 0；`check` 仍只有 `onlineProfileApi.ts` import type 与 `CottageView.vue` / `FarmView.vue` floating promise 3 条既有 warning。
- 证据（2026-06-05，Agent A）：本轮未过滤全量 `npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，刷新 `docs/ui-smoke-2026-04-26/summary.json`；运行期间仍出现本地后端 `127.0.0.1:4013` 的 `item-icon-preferences`、realtime ws、cohabitation contracts 代理拒连噪声，但未阻断移动端 smoke 退出码或截图 summary。
- 证据（2026-06-05，Agent A）：当前 HEAD 复跑 Agent A 目标组合 `TAOYUAN_E2E_PORT=58218 TAOYUAN_DEV_SERVER_SETTLE_MS=5000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "(online festival room|online expedition room|standalone expedition room|online society festival square contribution unlocks festival room launch|built-in region map|locked region|blocked route|manual region expedition|region map mobile layout)" --reporter=line --workers=1 --timeout=120000` 返回 0，34 条通过；全量 dev server E2E 中对应房间、节庆广场房间入口和远征区域样例也已逐条跑到。
- 风险（2026-06-05，Agent A）：当前 HEAD 未过滤全量 E2E `TAOYUAN_E2E_PORT=58219 TAOYUAN_DEV_SERVER_SETTLE_MS=5000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts --reporter=line --workers=1 --timeout=120000` 返回 1，结果为 59 passed / 5 failed；失败仍为炼丹工坊、宠物特殊食物、两条 NPC 页面和共同庄园 tab alias，均非 Agent A 房间 / 节会 / 远征范围，未出现 OUI-QA-005 新增房间域失败。
- 证据（2026-06-05，Agent A）：当前 HEAD 续跑 `node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`node --check taoyuan-main/scripts/run-e2e.mjs`、`node --check taoyuan-main/scripts/port-utils.mjs`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-ui-structure` 均返回 0；`check` 仍只有 3 条既有 warning。
- 证据（2026-06-05，Agent A）：当前 HEAD 未过滤全量 `npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，再次刷新 `docs/ui-smoke-2026-04-26/summary.json`；运行期间仍有本地后端 `127.0.0.1:4013` 的 item icon、realtime ws、cohabitation contracts proxy 拒连噪声，但未影响退出码。
- 证据（2026-06-05，Agent A）：当前 HEAD 续跑 Agent A 目标组合 `TAOYUAN_E2E_PORT=58220 TAOYUAN_DEV_SERVER_SETTLE_MS=5000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "(online festival room|online expedition room|standalone expedition room|online society festival square contribution unlocks festival room launch|built-in region map|locked region|blocked route|manual region expedition|region map mobile layout)" --reporter=line --workers=1 --timeout=120000` 返回 0，34 passed；覆盖房间域、节庆广场房间入口和远征区域样例。
- 风险（2026-06-05，Agent A）：当前 HEAD 续跑未过滤全量 E2E `TAOYUAN_E2E_PORT=58221 TAOYUAN_DEV_SERVER_SETTLE_MS=5000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts --reporter=line --workers=1 --timeout=120000` 仍返回 1，结果为 59 passed / 5 failed；失败仍为 `processing workshop can start an alchemy furnace recipe`、`animal page can feed a pet special food`、两条 NPC 页面、`online cohabitation keeps legacy tab query aliases grouped`。房间、节庆广场房间入口、远征区域样例均在本次全量中跑过，未出现 Agent A 房间域失败。

### OUI-QA-006 类型与 lint

- [x] `P0` 文件：全前端。
- 依赖：每个 PR 收尾。
- 执行：跑 `npm run check`。
- 执行：修复类型错误、lint 错误、未使用 import。
- 验收：类型和 lint 通过。
- 验证：`npm run check`。
- 证据（2026-06-03，Agent B）：`npm --prefix taoyuan-main run check` 通过，`vue-tsc -b` 无类型错误，`eslint src --ext .ts,.vue` 返回 0。
- 风险（2026-06-03，Agent B）：本轮保留既有 3 条 lint warning：`src/utils/onlineProfileApi.ts:1181` 的 `import()` type annotation、`CottageView.vue:742` 和 `FarmView.vue:1166` 的 floating promise；均非本轮 Agent B 改动引入，后续可作为独立 lint 清理项处理。

### OUI-QA-007 服务端回归

- [x] `P1` 文件：`server/scripts/*`、`server/src/taoyuanSocietyRuntime.js`
- 依赖：房间流程接入完成。
- 执行：确认前端没有改后端状态机语义。
- 执行：跑服务端在线 smoke 或对应脚本。
- 验收：服务端在线路径仍通过。
- 验证：以 `server/package.json` 实际脚本为准。
- 证据（2026-06-04，Agent A）：复核 `server/package.json`，服务端实际脚本包含 `qa:online-smoke`、`qa:online-release-guard`、`qa:server-node-check`、`qa:activity-room-visual-state`；本轮未修改 `server/src/*` 房间状态机或接口语义，当前 `server/src/routes/api.js` 仍保留节会 / 远征房间 `create`、`invite`、`join`、`ready-check`、`ready`、`start`、`action`、`settle`、`close` 路由及 `createOnlineReleaseGuard('festival'|'expedition')` 保护。
- 证据（2026-06-04，Agent A）：验证通过：`node --check server/scripts/qa-online-smoke.mjs`；`node --check server/scripts/qa-activity-room-visual-state.mjs`；`node --check server/src/routes/api.js`；`node --check server/src/taoyuanActivityRoomRuntime.js`；`npm --prefix server run qa:server-node-check` 返回 0（26 files）；`npm --prefix server run qa:online-release-guard` 返回 0；`npm --prefix server run qa:activity-room-visual-state` 返回 0，覆盖活动房间视觉状态、生命周期、邀请、准备、倒计时、运行、结算、关闭和回执持久化。
- 风险（2026-06-04，Agent A）：`npm --prefix server run qa:online-smoke` 返回 1，脚本在进入节会 / 远征房间 API 段前被非房间路径 `POST /api/taoyuan/exchange-station/festival-stall/:offerId/purchase food path` 阻断，错误为 `festival stall did not grant festival food correctly, current food=0`；该失败不属于本轮房间主流程改造，且脚本当前没有可用的房间段过滤开关，因此 OUI-QA-007 保持 `[~]`，等待非房间服务端 smoke 阻断解除后再跑全量并关闭。
- 证据（2026-06-04，Agent A）：本轮复跑 `npm --prefix server run qa:online-smoke`，脚本仍成功启动本地服务并跑到节会摊位 purchase 段，但退出码为 1。
- 风险（2026-06-04，Agent A）：阻断仍为非房间路径 `festival stall did not grant festival food correctly, current food=0`；未进入节会 / 远征房间 API 段，OUI-QA-007 继续保持 `[~]`，不作为房间流程服务端回归通过证据。
- 证据（2026-06-05，Agent B）：`server/scripts/qa-online-smoke.mjs` 不再把节庆摊位食物 / 票券奖励硬编码为 `food_qing_tuan` / `caravan`，改为先读取当前 `offer.rewards`，再校验购买响应与持久化存档增量；本轮解除 `festival_banquet_food` 实际奖励 `food_yue_bing x2` 时的误报。
- 证据（2026-06-05，Agent B）：`server/src/taoyuanSocietyRuntime.js` 的节庆广场完工 `completion_room_launch.summary` 同时拼入场景读回说明和“不直接发个人资产”资产边界说明，保持原自动创建上元灯会房间、`template_id=lantern_fair`、`gameplay_template_id=assembly` 与 room id 语义不变。
- 证据（2026-06-05，Agent B）：验证 `node --check server/scripts/qa-online-smoke.mjs`、`node --check server/src/taoyuanSocietyRuntime.js`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:server-node-check`、`npm --prefix server run qa:online-release-guard` 均返回 0；`qa:online-smoke` 输出 `[qa-online-smoke] OK`，覆盖节庆摊位、节会 / 远征房间、村社公共工程、在线委托、治理补偿和清理路径。
- 风险（2026-06-05，Agent B）：本轮只改服务端 smoke 断言口径和村社完工房间入口的玩家可见说明字段；未改资产发放、房间路由、房间状态机或接口提交语义。`qa:online-smoke` 中两条注册审核错误堆栈是脚本的 moderation reject 预期用例，最终退出码为 0。

## 12. PR 拆分建议

### PR-A 通用组件

- [x] 包含：OUI-CMP-001 至 OUI-CMP-006。
- [x] 包含：OUI-RESP-001。
- [~] 不接业务页面，或只加 demo / 静态引用。
- [~] 验证：`npm run check`、`npm run qa:online-ui-structure`。
- 证据（2026-06-04，Agent A）：收口复核确认 `OUI-CMP-001` 至 `OUI-CMP-006` 均为 `[x]`，覆盖 `OnlineActionDialog`、`OnlineBottomSheet`、`OnlineStickyActionBar`、`OnlineTechnicalDetails`、`OnlineConfirmActionDialog` 的 16.1 规格、必备 test id、Esc / 焦点、确认文字禁用和安全区能力；`OUI-RESP-001` 已为 `[x]`，并有 390x844 / 360x780 在线中心与节会房间 smoke 截图证据。
- 证据（2026-06-04，Agent A）：本轮验证 `node --check taoyuan-main/scripts/qa-online-ui-structure.mjs` 返回 0；`npm --prefix taoyuan-main run check` 返回 0，保留既有 3 条 warning（`onlineProfileApi.ts` import type、`CottageView.vue` / `FarmView.vue` floating promises）。
- 风险（2026-06-04，Agent A）：当前集成线已继续完成 PR-B 业务页面接入，PR-A 不能再作为“纯组件、不接业务页面”的干净独立 PR 边界；因此该条保持 `[~]`。`npm --prefix taoyuan-main run qa:online-ui-structure` 返回 1，但失败项均为小屋 / 农场共同庄园入口与花灯墙移动端 smoke 覆盖缺口，未出现 PR-A 通用组件或 `OUI-RESP-001` 安全区断言失败，验证条保持 `[~]` 等待非 Agent A 缺口收束后再改为 `[x]`。

### PR-B 节会 / 远征房间 P0

- [x] 包含：OUI-FLOW-001 至 OUI-FLOW-008。
- [x] 包含：OUI-FEST-001 至 OUI-FEST-006。
- [x] 包含：OUI-EXP-001、OUI-EXP-002。
- [x] 验证：房主创建、邀请、准备、结算、关闭 E2E。
- 证据（2026-06-04，Agent A）：PR-B 范围收口复核确认 `OUI-FLOW-001` 至 `OUI-FLOW-008`、`OUI-FEST-001` 至 `OUI-FEST-006`、`OUI-EXP-001`、`OUI-EXP-002` 均为 `[x]`，且对应任务已记录旧 test id 保留、原 `festivalRoomStore` / `expeditionRoomStore` action 调用、节会 / 在线节会远征 / 独立远征页面接入和移动端截图证据。
- 证据（2026-06-04，Agent A）：当前态验证通过：`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`；`node --check taoyuan-main/e2e/game-smoke.spec.ts`；`npm --prefix taoyuan-main run check` 返回 0（保留 3 条既有 warning）；房间组合 E2E 串行复跑 `npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online (festival room ...|expedition room ...|standalone expedition room reuses invite lobby and close confirm)" --reporter=line --workers=1 --timeout=120000` 返回 0，覆盖房主创建、邀请、成员准备、房主准备与倒计时、结算确认、关闭确认、Esc / 焦点返回和独立远征复用。
- 风险（2026-06-04，Agent A）：同一房间组合 E2E 默认并发复跑曾因所有目标用例在 `openHome()` 的 `page.goto('/')` 10 秒超时而返回 1，页面停在「桃源乡加载中...」，串行复跑通过，判断为首屏加载 / 并发竞态而非房间流程断言失败；`npm --prefix taoyuan-main run qa:online-ui-structure` 仍返回 1，但失败项均为小屋 / 农场共同庄园入口、在线庄园轻采、花灯墙现场物件和贡献摘要等非 Agent A 移动端覆盖缺口，未出现 PR-B 房间结构断言失败。

### PR-C 在线中心与文案 P0

- [x] 包含：OUI-CENTER-001 至 OUI-CENTER-003。
- [x] 包含：OUI-COPY-001、OUI-COPY-002。
- [x] 包含：OUI-QA-002、OUI-QA-003。
- [x] 验证：在线中心移动端 smoke、文案扫描。
- 证据（2026-06-05，Agent B）：收口复核确认 `OUI-CENTER-001` 至 `OUI-CENTER-003`、`OUI-COPY-001`、`OUI-COPY-002`、`OUI-QA-002`、`OUI-QA-003` 均已为 `[x]`，各任务下已有在线中心首屏降噪、治理信息折叠、模块卡降噪、玩家文案替换、文案扫描和视觉开关 QA 证据。
- 证据（2026-06-05，Agent B）：当前态验证 `npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-player-copy`、`npm --prefix taoyuan-main run qa:online-visual-feature-flags`、`npm --prefix taoyuan-main run qa:online-ui-structure`、未过滤 `npm --prefix taoyuan-main run qa:mobile-ui-smoke` 均返回 0。
- 风险（2026-06-05，Agent B）：`check` 仍保留 3 条既有 warning（`onlineProfileApi.ts` import type、`CottageView.vue` / `FarmView.vue` floating promise）；移动端 smoke 运行期间仍有本地后端 `127.0.0.1:4013` 代理拒绝日志，但命令退出码为 0。

### PR-D 共同庄园 P0

- [x] 包含：OUI-COHAB-001 至 OUI-COHAB-006。
- [x] 验证：共同庄园首屏截图、家族节会确认、文案扫描。
- 证据（2026-06-05，Agent B）：收口复核确认 `OUI-COHAB-001` 至 `OUI-COHAB-006` 均已为 `[x]`，各任务下已有标签分组、首屏四卡降噪、高风险确认、成员贡献抽屉、共享仓库确认和审计详情折叠证据。
- 证据（2026-06-05，Agent B）：当前态验证 `npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-player-copy`、`npm --prefix taoyuan-main run qa:online-ui-structure`、未过滤 `npm --prefix taoyuan-main run qa:mobile-ui-smoke` 均返回 0；关键 E2E 组合复跑返回 0，共 14 passed，其中覆盖共同庄园标签 query alias、首屏四卡、家族节会确认、基金 / 仓库高风险确认和共享审计详情折叠 5 个场景。
- 风险（2026-06-05，Agent B）：关键 E2E 首次使用端口 `58253` 时 Vite 监听 `127.0.0.1:58253` 返回 `EACCES`，已改用端口 `58260` 复跑通过；`check` 仍保留 3 条既有 warning。

### PR-E 其他在线页 P1

- [x] 包含：OUI-NEIGHBOR-001 至 OUI-NEIGHBOR-003。
- [x] 包含：OUI-ORDERS-001 至 OUI-ORDERS-003。
- [x] 包含：OUI-SOCIETY-001 至 OUI-SOCIETY-005。
- [x] 包含：OUI-MANOR-001 至 OUI-MANOR-004。
- [x] 验证：移动端 smoke 和关键 E2E 迁移。
- 证据（2026-06-05，Agent B）：收口复核确认邻里、委托、村社、在线庄园范围子任务均已为 `[x]`，其中 `OUI-ORDERS-003` 已完成委托列表详情抽屉，接力阶段、故事流、公开接力分账、阶段接单、结算摘要和技术凭证默认折叠均已有任务证据。
- 证据（2026-06-05，Agent B）：当前态验证 `npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-player-copy`、`npm --prefix taoyuan-main run qa:online-ui-structure`、未过滤 `npm --prefix taoyuan-main run qa:mobile-ui-smoke` 均返回 0；关键 E2E 组合复跑返回 0，共 14 passed，覆盖委托公开接力、村社公共工程 / 花灯墙 / 节庆广场 / 仓廪、在线庄园身份分层 / 可视化照料 / 有限轻采 / 护理房结算路径。
- 风险（2026-06-05，Agent B）：关键 E2E 首次端口 `58253` 因 Vite `EACCES` 未启动，端口 `58260` 复跑通过；运行日志仍可能出现本地后端 `127.0.0.1:4013` 代理拒绝，当前验证命令退出码均为 0。

### PR-F 视觉、响应式、QA 收尾

- [x] 包含：OUI-VISUAL-001 至 OUI-VISUAL-003。
- [x] 包含：OUI-RESP-002。
- [x] 包含：OUI-QA-001、OUI-QA-004 至 OUI-QA-007。
- [x] 验证：全套 QA、截图回归、已知问题清单。
- 证据（2026-06-05，Agent A）：PR-F 房间域收尾复核已完成一轮：`node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-player-copy`、`npm --prefix taoyuan-main run qa:online-ui-structure` 均返回 0；`check` 保留 3 条既有 warning。
- 证据（2026-06-05，Agent A）：提权复跑独立远征过滤 mobile smoke `$env:TAOYUAN_E2E_PORT='57068'; $env:TAOYUAN_MOBILE_SMOKE_ONLY='online-expedition-room'; npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0；节会过滤 mobile smoke 本轮早些时候也返回 0 并生成节会房 390x844 / 360x780 路径截图。
- 风险（2026-06-05，Agent A）：PR-F 仍保持 `[~]` 验证中；本轮后续节会过滤 smoke 复跑和房间域 E2E 复跑均卡在本机 Vite 新端口首页提交超时，未进入房间业务 DOM。当前机器存在大量并行 node / Vite 进程，建议下一轮优先做 QA 启动环境清点或等待并行代理收束后再跑未过滤全套 QA。
- 证据（2026-06-05，Agent A）：PR-F 房间域改用专用外部 dev server 复核后，`online-festival-room` / `online-expedition-room` 移动端过滤 smoke 均返回 0；房间域 E2E grep 返回 0，28 passed；复验 `npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-ui-structure`、`npm --prefix taoyuan-main run qa:online-player-copy` 均返回 0。
- 证据（2026-06-05，Agent A）：`taoyuan-main/scripts/run-e2e.mjs` 与 `taoyuan-main/scripts/qa-mobile-ui-smoke.mjs` 已补 Windows server 进程树清理兜底；脚本只在本轮确实启动 server 时按对应 `--port` 清理 `vite` 子进程，避免继续扩大 4175 / 570xx 端口残留。验证 `node --check taoyuan-main/scripts/run-e2e.mjs`、`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs` 返回 0。
- 风险（2026-06-05，Agent A）：PR-F 仍保持 `[~]`；本轮未跑未过滤全量 `npm --prefix taoyuan-main run qa:mobile-ui-smoke` 或完整未过滤 `test:e2e`。默认自动启动 dev server 的路径在当前多 Vite 进程环境下仍偶发首页提交超时，且 Vite 子进程退出可能有短暂延迟；专用外部 server 路径已证明房间业务断言本身通过。
- 证据（2026-06-05，Agent A）：继续加固 `taoyuan-main/scripts/run-e2e.mjs` 与 `taoyuan-main/scripts/qa-mobile-ui-smoke.mjs` 的 Windows Vite 清理兜底：`taskkill` 父进程后按本次 `--port` / `--port=` 短重试清理迟到的 `vite` node 子进程；未改业务页面断言、截图 label、store action、接口字段或旧 test id。
- 证据（2026-06-05，Agent A）：本轮验证 `node --check taoyuan-main/scripts/run-e2e.mjs`、`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`node --check taoyuan-main/e2e/game-smoke.spec.ts`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-ui-structure`、`npm --prefix taoyuan-main run qa:online-player-copy`、`git diff --check -- 0531拆解todo.md taoyuan-main/scripts/qa-mobile-ui-smoke.mjs taoyuan-main/scripts/run-e2e.mjs taoyuan-main/e2e/game-smoke.spec.ts` 均返回 0；`check` 仍保留既有 3 条 warning。
- 风险（2026-06-05，Agent A）：复跑过滤移动端 smoke 时，外部端口 `57070`、`4176`、`57068` 在 Playwright 打开首页前后拒连或退出；自动启动 `TAOYUAN_MOBILE_SMOKE_ONLY=online-festival-room npm --prefix taoyuan-main run qa:mobile-ui-smoke` 仍返回 1，失败停在 `http://127.0.0.1:4175/` 的 `page.goto(..., waitUntil: 'commit')` 15 秒超时，未进入节会房 DOM 或业务断言。PR-F 继续保持 `[~]`，等待稳定 server 后再跑未过滤 `qa:mobile-ui-smoke` 与完整 `test:e2e`。
- 证据（2026-06-05，Agent B）：非房间侧补齐 `qa-online-ui-structure` 最后一批移动 smoke 静态缺口后，`npm --prefix taoyuan-main run qa:online-ui-structure` 返回 0；同轮 `node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`npm --prefix taoyuan-main run qa:online-player-copy`、`npm --prefix taoyuan-main run qa:online-visual-feature-flags` 也返回 0。
- 证据（2026-06-05，Agent B）：`TAOYUAN_BASE_URL=http://127.0.0.1:4175 TAOYUAN_MOBILE_SMOKE_ONLY=cottage-cohabitation-entry npm --prefix taoyuan-main run qa:mobile-ui-smoke` 提权复跑返回 0，刷新小屋共同庄园入口 390x844 / 360x780 截图；沙箱内同命令仍按预期因 Chromium `spawn EPERM` 跳过。
- 风险（2026-06-05，Agent B）：PR-F 仍保持 `[~]`；本轮未获得未过滤全量 `qa:mobile-ui-smoke`、`test:e2e` 或农场 / 花灯墙过滤 smoke 的真实浏览器通过证据。当前阻断为本机外部 dev server 失效和临时 Vite 端口 `waitForServer` / 首页提交超时，并非 `qa-online-ui-structure` 静态缺口或新增断言失败。
- 证据（2026-06-05，Agent B）：使用当前构建产物与只读静态 server `http://127.0.0.1:58121` 补跑非房间过滤 smoke：`TAOYUAN_BASE_URL=http://127.0.0.1:58121 TAOYUAN_SKIP_DEV_SERVER=1 TAOYUAN_MOBILE_SMOKE_ONLY=farm-cohabitation-switch npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，`TAOYUAN_MOBILE_SMOKE_ONLY=online-society-projects` 返回 0；刷新农场共同庄园入口 `42/43` 与花灯墙 `28/29` 四张 390x844 / 360x780 截图。
- 证据（2026-06-05，Agent B）：同轮复验 `node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-ui-structure`、`npm --prefix taoyuan-main run qa:online-player-copy`、`npm --prefix taoyuan-main run qa:online-visual-feature-flags` 均返回 0；`check` 仅保留既有 3 条 warning，`npm --prefix taoyuan-main run build` 返回 0，仅保留 Vite 大 chunk warning。
- 风险（2026-06-05，Agent B）：PR-F 仍保持 `[~]`；农场与花灯墙过滤 smoke 缺口已清，但本轮仍未运行未过滤全量 `npm --prefix taoyuan-main run qa:mobile-ui-smoke` 或完整未过滤 `npm --prefix taoyuan-main run test:e2e`。静态 server 只作为 dev server 生命周期不稳定时的过滤场景验证兜底，不能替代最终未过滤全量 QA。
- 证据（2026-06-05，Agent A）：为绕过当前 Vite dev server 首页提交超时，本轮用当前源码执行临时静态构建 `npm exec -- vite build --outDir ../.tmp/agent-a-prf-dist --emptyOutDir` 返回 0（仅保留 Vite 大 chunk warning），并用只读静态 server `http://127.0.0.1:58140/` 验证首页 HTTP 200 与 Playwright 标题「桃源乡 — 文字田园物语」。
- 证据（2026-06-05，Agent A）：`qa-mobile-ui-smoke.mjs` 增加静态构建下的登记过滤场景 fallback：当开发态 `__TAOYUAN_SAMPLE_SAVES__` 不可用且 `TAOYUAN_MOBILE_SMOKE_ONLY` 命中 `sampleFallbackScenarios` 时，先按 E2E 同款「新的旅程」流程建立临时旅程，再进入 mock 页面；本轮 Agent A 只验证 `online-festival-room` / `online-expedition-room` 两条房间场景，未改房间业务断言、截图 label、store action、接口字段或旧 test id。
- 证据（2026-06-05，Agent A）：当前静态构建 server 上，`TAOYUAN_BASE_URL=http://127.0.0.1:58140 TAOYUAN_SKIP_DEV_SERVER=1 TAOYUAN_MOBILE_SMOKE_ONLY=online-festival-room npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0；随后 `TAOYUAN_MOBILE_SMOKE_ONLY=online-expedition-room` 返回 0，刷新节会房创建 / 邀请 / 准备 / 结算与独立远征 390x844 / 360x780 截图证据。
- 证据（2026-06-05，Agent A）：当前静态构建 server 上，`TAOYUAN_E2E_PORT=58140 TAOYUAN_E2E_EXTERNAL_SERVER=1 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "(online festival room|online expedition room|standalone expedition room)" --reporter=line --workers=1 --timeout=120000` 返回 0，28 条房间域用例全部通过；`taoyuan-main/test-results/.last-run.json` 为 `status: passed`、`failedTests: []`。
- 证据（2026-06-05，Agent A）：本轮复验 `node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`node --check taoyuan-main/scripts/run-e2e.mjs`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-ui-structure`、`npm --prefix taoyuan-main run qa:online-player-copy`、`git diff --check -- 0531拆解todo.md taoyuan-main/scripts/qa-mobile-ui-smoke.mjs taoyuan-main/scripts/run-e2e.mjs taoyuan-main/e2e/game-smoke.spec.ts` 均返回 0；`check` 仍保留既有 3 条 warning。
- 风险（2026-06-05，Agent A）：静态构建 fallback 只作为房间过滤 smoke 的替代启动路径；未过滤 `TAOYUAN_BASE_URL=http://127.0.0.1:58140 TAOYUAN_SKIP_DEV_SERVER=1 npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 1，并明确提示生产静态构建缺少开发态样例存档 API，完整移动端 smoke 仍必须等 dev server 稳定后运行。PR-F 继续保持 `[~]`，未跑完整未过滤 `test:e2e`。
- 证据（2026-06-05，Agent B）：PR-F 非房间 QA 收口复验：`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`npm --prefix taoyuan-main run build`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-ui-structure`、`npm --prefix taoyuan-main run qa:online-player-copy`、`npm --prefix taoyuan-main run qa:online-visual-feature-flags`、未过滤 `npm --prefix taoyuan-main run qa:mobile-ui-smoke` 均返回 0；`check` 仍仅保留 3 条既有 warning，`build` 仅保留 Vite 大 chunk warning。
- 证据（2026-06-05，Agent B）：用临时只读静态 server `http://127.0.0.1:58131` 复跑委托链路：`TAOYUAN_BASE_URL=http://127.0.0.1:58131 TAOYUAN_SKIP_DEV_SERVER=1 TAOYUAN_MOBILE_SMOKE_ONLY=online-orders npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0；`TAOYUAN_E2E_PORT=58131 TAOYUAN_E2E_EXTERNAL_SERVER=1 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online orders async board supports public relay route actions" --reporter=line --workers=1 --timeout=120000` 返回 0（1 passed）。
- 风险（2026-06-05，Agent B）：完整未过滤 `npm --prefix taoyuan-main run test:e2e` 仍返回 1（30 passed / 34 failed）。失败集中在默认 dev server 下 `openHome()` 的 `page.goto("/", waitUntil: "commit")` 超时或首页标题未出现，另有 NPC、加工、动物、共同庄园共享宠物、村社公共工程、节会 visual track、区域地图等既有 / 跨域用例失败；同一委托 E2E 在静态外部 server 下通过，说明本轮 `online-orders` 业务断言已收口，但 PR-F 因完整 E2E 未通过继续保持 `[~]`。
- 证据（2026-06-05，Agent A）：本轮将 `taoyuan-main/scripts/run-e2e.mjs`、`taoyuan-main/scripts/qa-mobile-ui-smoke.mjs` 的 Vite 启动等待从首页 HTTP 探测改为 stdout ready + TCP 端口等待，避免 `waitForServer` 先请求首页后 Chromium `page.goto(..., waitUntil: 'commit')` 卡死；`taoyuan-main/scripts/port-utils.mjs` 新增按端口枚举并 `taskkill` 清理 Vite 子进程的 Windows helper，`game-smoke.spec.ts` 首页导航 / 首屏等待改为 30 秒 env 可调。
- 证据（2026-06-05，Agent A）：本轮复验 `node --check taoyuan-main/scripts/port-utils.mjs`、`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`node --check taoyuan-main/scripts/run-e2e.mjs`、`node --check taoyuan-main/e2e/game-smoke.spec.ts`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-ui-structure`、`npm --prefix taoyuan-main run qa:online-player-copy`、`git diff --check -- 0531拆解todo.md taoyuan-main/scripts/port-utils.mjs taoyuan-main/scripts/qa-mobile-ui-smoke.mjs taoyuan-main/scripts/run-e2e.mjs taoyuan-main/e2e/game-smoke.spec.ts` 均返回 0；`check` 仍仅保留既有 3 条 warning。
- 证据（2026-06-05，Agent A）：`TAOYUAN_E2E_PORT=58170 TAOYUAN_MOBILE_SMOKE_ONLY=online-festival-room npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0；`TAOYUAN_E2E_PORT=58172 TAOYUAN_MOBILE_SMOKE_ONLY=online-expedition-room TAOYUAN_DEV_SERVER_SETTLE_MS=30000 npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0；房间域 E2E `TAOYUAN_E2E_PORT=58174 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "(online festival room|online expedition room|standalone expedition room)" --reporter=line --workers=1 --timeout=120000` 返回 0，28 passed，`.last-run.json` 为 `status: passed`、`failedTests: []`。
- 风险（2026-06-05，Agent A）：未过滤全量 `TAOYUAN_E2E_PORT=58169 npm --prefix taoyuan-main run qa:mobile-ui-smoke` 已能越过首页并跑到在线委托场景，但返回 1，阻断为非 Agent A 范围 `online-orders-detail-sheet` 内 `async-community-project-detail` 缺少“送到灯会”；后续自动端口 `58175`、`58176` 的独立远征过滤 smoke 仍偶发首页提交超时但端口均已清理。PR-F 继续保持 `[~]`，仍待稳定环境下未过滤全量 mobile smoke 与完整未过滤 `test:e2e`。
- 证据（2026-06-05，Agent A）：复核完整 E2E 产物时发现 `online festival visual scene supports laba cookpot object actions` 真实失败并非首页启动问题，而是腊八共灶场景中 `visual-scene-object-laba_cookpot_rice_tub` 拦截了 `visual-scene-object-laba_cookpot_stove` 的中心点击；`game-smoke.spec.ts` 改为先定位玩家可见的「灶台火候 2/6」热区并断言可见，`VisualSceneBoard.vue` 对 selected / needs_action / overheated / blocked 物件提高 z-index，确保需要处理的热区不被普通物件遮挡。跨域兼容改动：`VisualSceneBoard.vue` 为通用可视化组件，本轮只因节会房 QA 做最小 CSS 层级修复，未改 store action、接口字段、房间状态机或旧 test id。
- 证据（2026-06-05，Agent A）：临时静态构建 `npm exec -- vite build --outDir ../.tmp/agent-a-prf-dist --emptyOutDir` 返回 0（仅 Vite 大 chunk warning），只读静态 server `http://127.0.0.1:58186/` 首页 HTTP 200；修复前目标 E2E 返回 1，错误为 `米桶` hotzone intercepts pointer events；修复后 `TAOYUAN_E2E_PORT=58186 TAOYUAN_E2E_EXTERNAL_SERVER=1 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "online festival visual scene supports laba cookpot object actions" --reporter=line --workers=1 --timeout=120000` 返回 0，1 passed。临时 server 已关闭，`.tmp/agent-a-prf-dist` 已删除。
- 证据（2026-06-05，Agent A）：本轮复验 `node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/port-utils.mjs`、`node --check taoyuan-main/scripts/run-e2e.mjs`、`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-ui-structure` 均返回 0；`check` 仍仅保留既有 3 条 warning（`onlineProfileApi.ts` import type、`CottageView.vue` / `FarmView.vue` floating promises）。
- 风险（2026-06-05，Agent A）：PR-F 继续保持 `[~]`；本轮只关闭节会房 visual scene 热区遮挡这一条目标 E2E 失败，未重新跑完整未过滤 `npm --prefix taoyuan-main run test:e2e` 或未过滤 `qa:mobile-ui-smoke`。`VisualSceneBoard.vue` 层级修复对其他视觉场景为正向兼容，但仍建议后续 PR-F 全量 E2E 关注 visual map / track / manor 等相邻用例是否出现新的层级顺序差异。
- 证据（2026-06-05，Agent A）：当前 HEAD `b77f7a8f` 已补 PR-F 当前态复验：`node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-ui-structure`、未过滤 `npm --prefix taoyuan-main run qa:mobile-ui-smoke` 均返回 0；移动端 smoke 刷新 `docs/ui-smoke-2026-04-26/summary.json`。
- 证据（2026-06-05，Agent A）：当前 HEAD Agent A 目标组合 E2E `TAOYUAN_E2E_PORT=58218 TAOYUAN_DEV_SERVER_SETTLE_MS=5000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "(online festival room|online expedition room|standalone expedition room|online society festival square contribution unlocks festival room launch|built-in region map|locked region|blocked route|manual region expedition|region map mobile layout)" --reporter=line --workers=1 --timeout=120000` 返回 0，34 passed；覆盖房间域、节庆广场房间入口和远征区域样例。
- 风险（2026-06-05，Agent A）：PR-F 仍保持 `[~]`，因为未过滤全量 E2E `TAOYUAN_E2E_PORT=58219 TAOYUAN_DEV_SERVER_SETTLE_MS=5000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts --reporter=line --workers=1 --timeout=120000` 仍返回 1，结果为 59 passed / 5 failed；失败为炼丹、宠物、两条 NPC 和共同庄园 tab alias，均非 Agent A 房间 / 节会 / 远征范围。当前不把 PR-F 验证条改为 `[x]`。
- 证据（2026-06-05，Agent A）：当前 HEAD 续跑 PR-F 验证门：`node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`node --check taoyuan-main/scripts/run-e2e.mjs`、`node --check taoyuan-main/scripts/port-utils.mjs`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-ui-structure`、未过滤 `npm --prefix taoyuan-main run qa:mobile-ui-smoke` 均返回 0。
- 证据（2026-06-05，Agent A）：当前 HEAD 续跑 Agent A 目标组合 E2E `TAOYUAN_E2E_PORT=58220 TAOYUAN_DEV_SERVER_SETTLE_MS=5000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "(online festival room|online expedition room|standalone expedition room|online society festival square contribution unlocks festival room launch|built-in region map|locked region|blocked route|manual region expedition|region map mobile layout)" --reporter=line --workers=1 --timeout=120000` 返回 0，34 passed；未过滤全量 E2E 在端口 `58221` 返回 59 passed / 5 failed。
- 风险（2026-06-05，Agent A）：PR-F 继续保持 `[~]`；当前未过滤全量 E2E 剩余失败仍为炼丹、宠物、两条 NPC、共同庄园 tab alias，均非 Agent A 房间 / 节会 / 远征范围。除非非 Agent A 失败解除，否则不把 PR-F 全套 QA 验证条改为 `[x]`。
- 证据（2026-06-05，Agent A）：本轮续跑 PR-F 验证门：`node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`node --check taoyuan-main/scripts/run-e2e.mjs`、`node --check taoyuan-main/scripts/port-utils.mjs`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-ui-structure`、`npm --prefix taoyuan-main run qa:online-player-copy`、未过滤 `npm --prefix taoyuan-main run qa:mobile-ui-smoke` 均返回 0；`check` 仍仅保留既有 3 条 warning。
- 证据（2026-06-05，Agent A）：本轮 Agent A 目标组合 E2E `TAOYUAN_E2E_PORT=58230 TAOYUAN_DEV_SERVER_SETTLE_MS=5000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "(online festival room|online expedition room|standalone expedition room|online society festival square contribution unlocks festival room launch|built-in region map|locked region|blocked route|manual region expedition|region map mobile layout)" --reporter=line --workers=1 --timeout=120000` 返回 0，34 passed；未过滤全量 E2E `TAOYUAN_E2E_PORT=58231 TAOYUAN_DEV_SERVER_SETTLE_MS=5000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts --reporter=line --workers=1 --timeout=120000` 返回 1，59 passed / 5 failed。
- 风险（2026-06-05，Agent A）：PR-F 继续保持 `[~]`；本轮未过滤全量 E2E 剩余失败仍为 `processing workshop can start an alchemy furnace recipe`、`animal page can feed a pet special food`、`npc page can talk with a random visitor`、`npc family relation graph supports node selection`、`online cohabitation keeps legacy tab query aliases grouped`，均非 Agent A 房间 / 节会 / 远征范围；房间域、节庆广场入口和远征区域样例在目标组合与全量执行中均已跑过且未失败。
- 证据（2026-06-05，Agent A）：本轮再次复核 PR-F 当前门槛：`node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`node --check taoyuan-main/scripts/run-e2e.mjs`、`node --check taoyuan-main/scripts/port-utils.mjs`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-ui-structure`、`npm --prefix taoyuan-main run qa:online-player-copy`、未过滤 `npm --prefix taoyuan-main run qa:mobile-ui-smoke` 均返回 0；移动端 smoke 刷新 `docs/ui-smoke-2026-04-26/summary.json`，运行时仍有本地后端 `127.0.0.1:4013` proxy 拒连噪声。
- 风险（2026-06-05，Agent A）：本轮未过滤全量 E2E `TAOYUAN_E2E_PORT=58232 TAOYUAN_DEV_SERVER_SETTLE_MS=5000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts --reporter=line --workers=1 --timeout=120000` 仍返回 1，结果为 59 passed / 5 failed；失败仍为炼丹、宠物、两条 NPC、共同庄园 tab alias，均非 Agent A 房间 / 节会 / 远征范围。PR-F 继续保持 `[~]`，不关闭全套 QA 验证条。
- 风险（2026-06-05，Agent A）：阻塞审计已满足连续多轮同一阻断条件；Agent A 范围内房间流程、节会入口、远征区域样例、结构 QA、移动端 smoke 与玩家文案扫描均已有通过证据，但完整 PR-F 必需的未过滤全量 E2E 仍被非 Agent A 范围 5 个失败阻断。除非炼丹、宠物、NPC 与共同庄园 alias 失败解除，Agent A 无法继续推进 PR-F 关闭且不能按协作规则主动修改这些页面。
- 风险（2026-06-05，Agent A）：恢复后新一轮未过滤全量 E2E `TAOYUAN_E2E_PORT=58233 TAOYUAN_DEV_SERVER_SETTLE_MS=5000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts --reporter=line --workers=1 --timeout=120000` 仍返回 1，结果为 59 passed / 5 failed；失败仍为 `processing workshop can start an alchemy furnace recipe`、`animal page can feed a pet special food`、`npc page can talk with a random visitor`、`npc family relation graph supports node selection`、`online cohabitation keeps legacy tab query aliases grouped`，均非 Agent A 房间 / 节会 / 远征范围；Agent A 房间、节会入口和远征区域样例在全量执行中未失败。PR-F 继续保持 `[~]`。
- 风险（2026-06-05，Agent A）：本轮提权定向复跑上一轮 5 个阻断用例 `TAOYUAN_E2E_PORT=58234 TAOYUAN_DEV_SERVER_SETTLE_MS=5000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "(processing workshop can start an alchemy furnace recipe|animal page can feed a pet special food|npc page can talk with a random visitor|npc family relation graph supports node selection|online cohabitation keeps legacy tab query aliases grouped)" --reporter=line --workers=1 --timeout=120000` 返回 1，5 failed；失败仍分别停在炼丹页 `processing-view` 未出现、宠物特殊食物期望 `稻米` 但页面显示 `稻谷 · 饱腹`、两条 NPC 页 `npc-view` 未出现、共同庄园 `online-module-tab-visibility` 未被 legacy query 选中。沙箱内同命令因 Chromium `spawn EPERM` 跳过，提权结果为准。PR-F 继续保持 `[~]`。
- 证据（2026-06-05，Agent A）：本轮复验 Agent A 目标组合 E2E，沙箱内因 Chromium `spawn EPERM` 跳过；提权复跑 `TAOYUAN_E2E_PORT=58235 TAOYUAN_DEV_SERVER_SETTLE_MS=5000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "(online festival room|online expedition room|standalone expedition room|online society festival square contribution unlocks festival room launch|built-in region map|locked region|blocked route|manual region expedition|region map mobile layout)" --reporter=line --workers=1 --timeout=120000` 返回 0，34 passed；房间流程、节会入口和远征区域样例仍无回归。运行时保留本地后端 `127.0.0.1:4013` proxy 拒连噪声，未影响退出码。
- 风险（2026-06-05，Agent A）：本轮再次复核同一阻断，沙箱内 `TAOYUAN_E2E_PORT=58236 TAOYUAN_DEV_SERVER_SETTLE_MS=5000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "(processing workshop can start an alchemy furnace recipe|animal page can feed a pet special food|npc page can talk with a random visitor|npc family relation graph supports node selection|online cohabitation keeps legacy tab query aliases grouped)" --reporter=line --workers=1 --timeout=120000` 因 Chromium `spawn EPERM` 跳过；提权复跑返回 1，5 failed，失败仍为炼丹页 `processing-view` 未出现、宠物特殊食物期望 `稻米` 但显示 `稻谷 · 饱腹`、两条 NPC 页 `npc-view` 未出现、共同庄园 legacy tab query 未选中 `online-module-tab-visibility`。恢复后的连续阻塞审计已再次满足同一非 Agent A 阻断条件；PR-F 继续保持 `[~]`，Agent A 不能按协作规则主动修改这些页面。
- 证据（2026-06-05，Agent B）：收口共同庄园 legacy tab query 阻断后复验 PR-F 非房间门槛：`node --check taoyuan-main/e2e/game-smoke.spec.ts`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-ui-structure`、`npm --prefix taoyuan-main run qa:online-player-copy` 均返回 0；`check` 仍仅保留既有 3 条 warning。
- 证据（2026-06-05，Agent B）：提权未过滤全量 `TAOYUAN_E2E_PORT=58248 TAOYUAN_DEV_SERVER_SETTLE_MS=30000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts --reporter=line --workers=1 --timeout=120000` 返回 1，结果收窄为 60 passed / 4 failed；共同庄园 `online cohabitation keeps legacy tab query aliases grouped` 已通过。
- 风险（2026-06-05，Agent B）：PR-F 继续保持 `[~]`；完整 E2E 剩余 4 个失败为 `processing workshop can start an alchemy furnace recipe`、`animal page can feed a pet special food`、`npc page can talk with a random visitor`、`npc family relation graph supports node selection`，属于炼丹 / 宠物 / NPC 跨域页面，不在本轮 Agent B 在线页面收口范围。运行时仍有本地后端 `127.0.0.1:4013` proxy 拒连噪声，但未影响共同庄园目标用例退出码。
- 证据（2026-06-05，Agent B）：PR-F 非房间剩余阻断已清零：`taoyuan-main/src/router/index.ts` 补 `npc -> village`、`processing -> workshop` legacy 深链 redirect；`AnimalView.vue` 的宠物今日加餐状态优先使用特殊喂食短名，保持与按钮「稻米」一致，不改喂食扣减、时间推进、store action 或存档字段。定向提权 E2E `TAOYUAN_E2E_PORT=58249 TAOYUAN_DEV_SERVER_SETTLE_MS=30000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts -g "(processing workshop can start an alchemy furnace recipe|animal page can feed a pet special food|npc page can talk with a random visitor|npc family relation graph supports node selection)" --reporter=line --workers=1 --timeout=120000` 返回 0，4 passed。
- 证据（2026-06-05，Agent B）：本轮 PR-F 全量复验通过：`node --check taoyuan-main/e2e/game-smoke.spec.ts`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-ui-structure`、`npm --prefix taoyuan-main run qa:online-player-copy`、未过滤 `TAOYUAN_E2E_PORT=58250 TAOYUAN_DEV_SERVER_SETTLE_MS=30000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts --reporter=line --workers=1 --timeout=120000`、未过滤 `TAOYUAN_E2E_PORT=58251 TAOYUAN_DEV_SERVER_SETTLE_MS=30000 npm --prefix taoyuan-main run qa:mobile-ui-smoke` 均返回 0；完整 E2E 为 64 passed，移动端 smoke 刷新 `docs/ui-smoke-2026-04-26/summary.json`。
- 风险（2026-06-05，Agent B）：`check` 仍保留既有 3 条 warning（`onlineProfileApi.ts` import type、`CottageView.vue` / `FarmView.vue` floating promise）；E2E 与 mobile smoke 运行期仍有本地后端 `127.0.0.1:4013` 的 item icon、NPC portrait、online API / ws proxy 拒连噪声，但命令退出码均为 0，未影响本轮断言。跨域兼容改动仅限路由旧深链和宠物状态展示文案。
- 证据（2026-06-05，Agent A）：恢复后完成 PR-F 当前态复核：`node --check taoyuan-main/e2e/game-smoke.spec.ts`、`node --check taoyuan-main/scripts/qa-online-ui-structure.mjs`、`node --check taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`、`node --check taoyuan-main/scripts/run-e2e.mjs`、`npm --prefix taoyuan-main run check`、`npm --prefix taoyuan-main run qa:online-ui-structure`、`npm --prefix taoyuan-main run qa:online-player-copy` 均返回 0；`check` 仍仅保留既有 3 条 warning。
- 证据（2026-06-05，Agent A）：恢复后未过滤全量 E2E `TAOYUAN_E2E_PORT=58252 TAOYUAN_DEV_SERVER_SETTLE_MS=30000 npm --prefix taoyuan-main run test:e2e -- e2e/game-smoke.spec.ts --reporter=line --workers=1 --timeout=120000` 返回 0，64 passed；未过滤移动端 smoke `TAOYUAN_E2E_PORT=58253 TAOYUAN_DEV_SERVER_SETTLE_MS=30000 npm --prefix taoyuan-main run qa:mobile-ui-smoke` 返回 0，`taoyuan-main/test-results/.last-run.json` 为 `status: passed`、`failedTests: []`，并刷新 `docs/ui-smoke-2026-04-26/summary.json`。
- 风险（2026-06-05，Agent A）：`check` 仍保留既有 3 条 warning；E2E / mobile smoke 运行时仍有本地后端 `127.0.0.1:4013` proxy / ws 拒连、`item-icon-preferences` 请求中止等噪声，但命令退出码为 0，未影响断言。PR-F 当前可按文档状态视为收口。

## 13. 文件路径总览

通用组件：

- `taoyuan-main/src/components/game/online/OnlineActionDialog.vue`
- `taoyuan-main/src/components/game/online/OnlineBottomSheet.vue`
- `taoyuan-main/src/components/game/online/OnlineStickyActionBar.vue`
- `taoyuan-main/src/components/game/online/OnlineTechnicalDetails.vue`
- `taoyuan-main/src/components/game/online/OnlineConfirmActionDialog.vue`
- `taoyuan-main/src/components/game/online/OnlineEmptyState.vue`
- `taoyuan-main/src/components/game/online/OnlineStatusBanner.vue`
- `taoyuan-main/src/components/game/online/OnlineRoomWizard.vue`
- `taoyuan-main/src/components/game/online/OnlineInvitePanel.vue`
- `taoyuan-main/src/components/game/online/OnlineRoomLobbyDialog.vue`
- `taoyuan-main/src/components/game/online/OnlineOrderWizard.vue`

页面：

- `taoyuan-main/src/views/game/OnlineView.vue`
- `taoyuan-main/src/views/game/ExpeditionRoomView.vue`
- `taoyuan-main/src/views/game/online/OnlineFestivalView.vue`
- `taoyuan-main/src/views/game/online/OnlineCohabitationView.vue`
- `taoyuan-main/src/views/game/online/OnlineNeighborView.vue`
- `taoyuan-main/src/views/game/online/OnlineOrdersView.vue`
- `taoyuan-main/src/views/game/online/OnlineSocietyView.vue`
- `taoyuan-main/src/views/game/online/OnlineManorView.vue`

可视化：

- `taoyuan-main/src/components/game/online/OnlineVisualRoomShell.vue`
- `taoyuan-main/src/components/game/online/VisualMapBoard.vue`
- `taoyuan-main/src/components/game/online/VisualSceneBoard.vue`
- `taoyuan-main/src/components/game/online/VisualTrackBoard.vue`
- `taoyuan-main/src/components/game/online/AsyncCommunityBoard.vue`

QA：

- `taoyuan-main/scripts/qa-online-ui-structure.mjs`
- `taoyuan-main/scripts/qa-online-visual-feature-flags.mjs`
- `taoyuan-main/scripts/qa-online-player-copy.mjs`
- `taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`
- `taoyuan-main/e2e/game-smoke.spec.ts`
- `taoyuan-main/package.json`

样式：

- `taoyuan-main/src/app.css`

## 14. 验证命令清单

前端静态：

```bash
cd taoyuan-main
npm run qa:online-ui-structure
npm run qa:online-visual-feature-flags
npm run qa:online-player-copy
npm run check
```

前端浏览器：

```bash
cd taoyuan-main
npm run qa:mobile-ui-smoke
npm run test:e2e
```

服务端：

```bash
cd server
npm run qa:online-smoke
```

注意：服务端命令以 `server/package.json` 实际存在脚本为准。

## 15. 完成审计清单

- [x] `0531优化.md` 中 P0 组件均已拆成任务。
- [x] `0531优化.md` 中 P0 页面均已拆成任务。
- [x] `0531优化.md` 中移动端专项已拆成任务。
- [x] `0531优化.md` 中桌面端专项已拆成任务。
- [x] `0531优化.md` 中 QA 与验收已拆成任务。
- [x] `0531优化.md` 中文案闸门已拆成任务。
- [x] 每个任务都有文件、依赖、执行、验收、验证。
- [x] 每个阶段都有可交付结果。
- [~] 每个 PR 都能独立回滚或验证。
- [x] 不要求一次性改后端状态机。
- [x] 0.x AI 执行协议已被执行者阅读。
- [x] 已按任务元数据规范回填证据和风险。
- [x] 已按 AI 输出报告格式汇报本轮结果。
- 证据（2026-06-05，Agent B）：对照 `0531优化.md` 第 5、6、7、8、9、15、20、22、24、25、28、31、32 章，当前 `0531拆解todo.md` 已把通用组件、在线中心、共同庄园、邻里、委托、村社、庄园、可视化、移动端、桌面端、QA 与玩家文案闸门拆入 `OUI-000` 至 `OUI-QA-007`、PR-A 至 PR-F 和第 13-14 章文件 / 命令清单。
- 证据（2026-06-05，Agent B）：``rg -n '^## [0-9]+\.|^### OUI-|^### PR-|完成审计清单' 0531拆解todo.md`` 可读回 M0-M6、所有 `OUI-*` 任务、PR-A 至 PR-F、第 13-15 章；``rg -n '证据：未执行|风险：未评估|^- \[[ ~!]\] `P[0-2]`' 0531拆解todo.md`` 只命中 0.4 任务模板和第 1 章总体完成定义，未命中实际 `OUI-*` 任务缺证据 / 风险或未关闭状态。
- 证据（2026-06-05，Agent B）：当前验证 `npm --prefix taoyuan-main run qa:online-ui-structure` 返回 0；`npm --prefix taoyuan-main run qa:online-player-copy` 返回 0（67 files、14 denylist terms、1 allowlist entries）；本轮已重新读取 0.1-0.13 与 16.x，并按用户要求在最终回复使用“已完成项 / 验证结果 / 风险 / 当前自动切到的下一项”格式。
- 风险（2026-06-05，Agent B）：`npm --prefix taoyuan-main run check` 当前被并行改动的 `src/stores/useSaveStore.ts:1462` 阻断（`syncResult.invalidSlots` 可能为 `undefined`），非本轮第 15 章文档审计改动引入；PR-A 仍保留“当前集成线已接业务页面，无法作为纯组件独立 PR 边界”的历史风险，因此“每个 PR 都能独立回滚或验证”保持 `[~]`，等待类型检查和 PR-A 边界风险单独收口。

## 16. AI 可执行规格附录

本附录用于降低 AI 实现时的自由发挥。若任务描述没有更具体要求，按本附录实现。

### 16.1 通用组件规格

`OnlineActionDialog.vue`：

| 项 | 要求 |
| --- | --- |
| 用途 | 承载普通确认、信息确认、危险确认和简单表单确认 |
| Props | `open`、`title`、`description`、`tone`、`confirmLabel`、`cancelLabel`、`confirmDisabled`、`running`、`requireText`、`closeOnBackdrop` |
| Emits | `confirm`、`cancel`、`close` |
| Slots | `default`、`footer`、`details` |
| 必备 test id | `online-action-dialog`、`online-action-dialog-title`、`online-action-dialog-confirm`、`online-action-dialog-cancel` |
| 可访问性 | `role="dialog"`、`aria-modal="true"`、标题 `aria-labelledby`、说明 `aria-describedby` |
| 键盘 | Esc 关闭普通弹窗；危险弹窗 Esc 触发取消，不直接确认 |
| 焦点 | 打开后聚焦标题或首个可交互元素，关闭后返回触发按钮 |
| 移动端 | 面板宽度不超过视口，内容区可滚动，底部按钮不被安全区遮挡 |

`OnlineBottomSheet.vue`：

| 项 | 要求 |
| --- | --- |
| 用途 | 移动端详情、邀请、列表筛选、对象详情 |
| Props | `open`、`title`、`description`、`side`、`closeOnBackdrop`、`initialFocus` |
| Emits | `close`、`after-open`、`after-close` |
| Slots | `header`、`default`、`footer` |
| 必备 test id | `online-bottom-sheet`、`online-bottom-sheet-title`、`online-bottom-sheet-close` |
| 布局 | 小屏底部抽屉，大屏右侧抽屉或居中面板 |
| 滚动 | 头部和底部固定，内容区独立滚动 |
| 安全区 | 底部使用 `env(safe-area-inset-bottom)` |

`OnlineStickyActionBar.vue`：

| 项 | 要求 |
| --- | --- |
| 用途 | 移动端固定主行动，桌面端退化为普通行动区或隐藏 |
| Props | `statusLabel`、`primaryAction`、`secondaryActions`、`moreActions`、`disabledReason` |
| Emits | `primary`、`secondary`、`more` |
| 必备 test id | `online-sticky-action-bar`、`online-sticky-primary-action` |
| 尺寸 | 主按钮高度不低于 44px |
| 安全区 | 底部 padding 包含 `env(safe-area-inset-bottom)` |
| 页面配合 | 页面底部必须留 spacer，避免遮挡最后一张卡 |

`OnlineTechnicalDetails.vue`：

| 项 | 要求 |
| --- | --- |
| 用途 | 收纳凭证、审计、调试、备用操作、回滚信息 |
| Props | `title`、`summary`、`defaultOpen`、`tone`、`copyable` |
| Slots | `summary`、`default`、`actions` |
| 必备 test id | `online-technical-details`、`online-technical-details-toggle` |
| 默认状态 | 玩家路径默认折叠 |
| 文案 | 折叠标题用玩家可理解语言，展开后可以保留技术字段 |
| 复制 | 长 ID 默认截断，复制按钮可获取完整值 |

`OnlineConfirmActionDialog.vue`：

| 项 | 要求 |
| --- | --- |
| 用途 | 高风险动作二次确认 |
| Props | `open`、`title`、`impactItems`、`assetChanges`、`irreversible`、`requireText`、`confirmLabel`、`running` |
| Emits | `confirm`、`cancel`、`close` |
| 必备 test id | `online-confirm-action-dialog`、`online-confirm-impact-list`、`online-confirm-required-text` |
| 默认 tone | `danger` |
| 默认关闭 | `closeOnBackdrop=false` |
| 禁用规则 | 未输入确认文字、running、必要影响对象缺失时禁用确认 |

`OnlineEmptyState.vue`：

| 项 | 要求 |
| --- | --- |
| 用途 | 无房间、无邀请、无委托、无村社、无访问记录 |
| Props | `title`、`description`、`primaryLabel`、`secondaryLabel`、`illustration` |
| Emits | `primary`、`secondary` |
| 必备 test id | `online-empty-state` |
| 文案 | 说明下一步，不写泛泛的「暂无数据」 |

`OnlineStatusBanner.vue`：

| 项 | 要求 |
| --- | --- |
| 用途 | 加载、成功、警告、错误和信息反馈 |
| Props | `tone`、`title`、`description`、`actionLabel`、`dismissible` |
| Emits | `action`、`dismiss` |
| 必备 test id | `online-status-banner` |
| 可访问性 | 错误和成功状态使用 `aria-live` |

### 16.2 房间流程组件规格

`OnlineRoomWizard.vue`：

| 项 | 要求 |
| --- | --- |
| 用途 | 节会和远征创建房间统一向导 |
| Props | `open`、`domain`、`initialTemplateId`、`initialGameplayId`、`initialMemberLimit`、`initialTitle`、`initialVisibility` |
| Emits | `submit`、`cancel`、`close`、`draft-change` |
| 必备 test id | `online-room-wizard`、`online-room-wizard-next`、`online-room-wizard-back`、`online-room-wizard-submit` |
| 步骤 1 | 选择玩法，test id：`online-room-wizard-step-gameplay` |
| 步骤 2 | 配置房间，test id：`online-room-wizard-step-config` |
| 步骤 3 | 邀请成员，test id：`online-room-wizard-step-invite` |
| 步骤 4 | 确认创建，test id：`online-room-wizard-step-review` |
| 失败处理 | 创建失败停留当前步骤，保留草稿和邀请名单 |
| 移动端 | 使用 `OnlineBottomSheet` 或等价底部抽屉 |
| 桌面端 | 使用 `OnlineActionDialog` 或等价居中向导 |

节会向导字段：

| 字段 | 要求 |
| --- | --- |
| 玩法 | 上元灯会、龙舟赛道、腊八共灶、烟火留影 |
| 房间标题 | 必填，默认可由玩法名生成 |
| 人数上限 | 使用 store 当前允许范围，不写死未知上限 |
| 玩法模式 | 使用现有模板或 gameplay 字段 |
| 公开可见 | 布尔值或现有可见性字段 |
| Query 预填 | 支持 `template`、`gameplay` |

远征向导字段：

| 字段 | 要求 |
| --- | --- |
| 玩法 | 协作矿洞、护送抵运、海域共探、山林采集 |
| 队伍标题 | 必填，默认可由路线名生成 |
| 人数上限 | 使用 store 当前允许范围 |
| 风险等级 | 使用现有风险字段，不新增协议语义 |
| 路线模板 | 使用现有 route/template 字段 |
| 确认页 | 展示撤离规则和奖励预览 |

`OnlineInvitePanel.vue`：

| 项 | 要求 |
| --- | --- |
| 用途 | 单人邀请、批量邀请、失败重试 |
| Props | `open`、`domain`、`existingMembers`、`recentPlayers`、`busy` |
| Emits | `invite`、`retry`、`remove`、`close` |
| 必备 test id | `online-invite-panel`、`online-invite-input`、`online-invite-submit`、`online-invite-result-list` |
| 批量拆分 | 按空格、逗号、中文逗号、换行拆分 |
| 状态 | 已邀请、邀请中、邀请失败、已在房、不可邀请 |
| 失败处理 | 单项失败可重试，不清空全部输入 |

`OnlineRoomLobbyDialog.vue`：

| 项 | 要求 |
| --- | --- |
| 用途 | 房间准备、成员状态、邀请、倒计时、结算入口 |
| Props | `open`、`domain`、`room`、`currentUserId`、`busyAction`、`lastFeedback` |
| Emits | `invite`、`ready`、`unready`、`start-ready-check`、`start-countdown`、`settle`、`cancel-room`、`leave-room`、`close` |
| 必备 test id | `online-room-lobby`、`online-room-member-list`、`online-room-primary-action` |
| 成员状态 | 房主、已准备、未准备、离线、被邀请 |
| 按钮规则 | 每个状态只突出一个推荐主行动 |
| 禁用原因 | disabled 时必须展示原因 |
| 反馈 | 最近成功或失败反馈展示在大厅内 |

### 16.3 房间状态按钮矩阵

房主视角：

| 房间状态 | 主行动 | 次行动 | 危险行动 | 禁用原因示例 |
| --- | --- | --- | --- | --- |
| `created` | 邀请玩家 | 开始准备 | 取消房间 | 人数不足时不能开始准备 |
| `ready_check` | 开始倒计时 | 继续邀请 | 取消房间 | 有成员未准备时不能倒计时 |
| `countdown` | 查看倒计时 | 通知成员 | 取消房间 | 倒计时未结束不能结算 |
| `running` | 进入玩法 | 查看成员 | 关闭房间 | 玩法未完成不能结算 |
| `settling` | 查看结算 | 重试结算 | 关闭房间 | 正在结算时不能重复提交 |
| `settled` | 查看奖励 | 再开一局 | 关闭房间 | 已结算房间不能再次准备 |
| `closed` | 查看记录 | 返回大厅 | 无 | 房间已关闭 |

成员视角：

| 成员状态 | 主行动 | 次行动 | 危险行动 | 禁用原因示例 |
| --- | --- | --- | --- | --- |
| `invited` | 接受邀请 | 拒绝邀请 | 无 | 房间已满时不能加入 |
| `joined` | 我已准备 | 离开房间 | 无 | 房间状态变化后需要刷新 |
| `ready` | 取消准备 | 查看成员 | 离开房间 | 倒计时开始后不能取消准备 |
| `running` | 进入玩法 | 查看目标 | 离开房间 | 当前阶段不允许离开 |
| `disconnected` | 重新连接 | 查看说明 | 离开房间 | 网络未恢复 |
| `settled` | 查看奖励 | 返回大厅 | 无 | 奖励已记录 |

按钮呈现规则：

- 主行动使用高强调按钮。
- 次行动使用次级按钮或更多菜单。
- 危险行动必须进入 `OnlineConfirmActionDialog`。
- 禁用按钮旁边必须能看到原因，不能只设置 `disabled`。
- 状态未知时不展示危险行动，先展示刷新入口。

### 16.4 test id 命名规范

命名规则：

- 统一使用小写 kebab-case。
- 在线通用组件使用 `online-*` 前缀。
- 节会房间使用 `online-festival-*` 前缀。
- 远征房间使用 `online-expedition-*` 前缀。
- 共同庄园使用 `online-cohab-*` 前缀。
- 不因文案变化修改 test id。
- 旧 test id 第一轮必须保留，新 test id 可以并存。

P0 必备 test id：

| 场景 | test id |
| --- | --- |
| 在线中心主行动区 | `online-center-hero-actions` |
| 创建房间入口 | `online-room-create-trigger` |
| 房间向导 | `online-room-wizard` |
| 向导提交 | `online-room-wizard-submit` |
| 邀请面板 | `online-invite-panel` |
| 邀请输入 | `online-invite-input` |
| 邀请提交 | `online-invite-submit` |
| 准备大厅 | `online-room-lobby` |
| 大厅主行动 | `online-room-primary-action` |
| 结算确认 | `online-room-settle-confirm` |
| 关闭确认 | `online-room-close-confirm` |
| 技术详情 | `online-technical-details` |
| 移动端主操作栏 | `online-sticky-action-bar` |
| 底部抽屉 | `online-bottom-sheet` |

迁移策略：

- 旧测试查找按钮文案时，先补 `data-testid`，再改文案。
- 旧 test id 对应元素移入弹窗时，保留 test id。
- 如果旧 test id 语义不准确，新增 test id，不直接删除旧 test id。
- 删除旧 test id 前必须有 E2E 改动和本文件证据。

### 16.5 移动端布局规格

断点：

| 视口 | 目标 |
| --- | --- |
| 360x780 | Android 小屏主路径可用 |
| 390x844 | iPhone 常见尺寸主路径可用 |
| 768x1024 | 平板不出现过窄抽屉 |
| 1366x768 | 桌面主舞台和状态栏清晰 |

移动端要求：

- 首屏必须能看到状态摘要和一个主行动。
- 底部固定按钮不遮挡最后内容。
- 表单字段不超过一屏时可以直接展示，超过一屏必须拆步骤或抽屉。
- 抽屉打开后背景页面不滚动。
- 弹窗内容区可滚动，按钮区固定。
- 触控目标建议 44px，最低不能低于 36px。
- 横向滚动 tab 必须有明显选中态。
- 列表卡片每张只展示最关键 4 个信息以内。

桌面端要求：

- 房间页优先使用左列表 / 中舞台 / 右状态。
- 右侧状态栏宽度建议 320-380px。
- 在线中心最多突出 3 个主行动。
- 技术和治理信息不进入首屏主视觉。
- 不使用 5 列以上等权模块网格承载主路径。

### 16.6 QA 脚本规格

`qa-online-player-copy.mjs`：

| 项 | 要求 |
| --- | --- |
| 扫描范围 | `src/views/game`、`src/components/game/online` |
| 默认忽略 | `node_modules`、`dist`、测试 fixture、接口类型、注释中的历史说明 |
| denylist | 使用 0.9 中玩家主路径禁用词 |
| allowlist | `OnlineTechnicalDetails`、`data-testid`、API 字段、store 字段、E2E mock、测试数据 |
| 输出 | 文件、行号、命中词、建议替换 |
| 退出码 | 阶段 1 可只报警；接入 PR 后应对玩家路径命中返回非 0 |

`qa-online-ui-structure.mjs`：

| 项 | 要求 |
| --- | --- |
| 检查组件 | P0 通用组件文件存在 |
| 检查引用 | 房间页面引用 wizard、invite、lobby、confirm |
| 检查语义 | `OnlineModuleShell` tab 有 `role` 和 `aria-selected` |
| 检查移动端 | 页面存在 sticky action bar 或等价主行动 |
| 输出 | 缺失组件、缺失引用、缺失 test id |

`qa-mobile-ui-smoke.mjs`：

| 项 | 要求 |
| --- | --- |
| 视口 | 360x780、390x844 |
| 场景 | 在线中心、节会创建、邀请、准备大厅、结算确认、共同庄园确认 |
| 断言 | 主行动在 viewport 内，底部按钮未遮挡，弹窗可关闭 |
| 证据 | 保存截图路径或输出截图文件名 |

`game-smoke.spec.ts`：

| 场景 | 断言 |
| --- | --- |
| 房主创建节会房 | 打开向导、填写、提交、进入大厅 |
| 房主创建远征房 | 打开向导、填写、提交、进入大厅 |
| 邀请玩家 | 打开邀请面板、提交、看到反馈 |
| 准备流程 | 房主开始准备、成员准备、房主倒计时 |
| 结算确认 | 打开确认、看到奖励预览、确认后看到记录 |
| 关闭确认 | 打开危险确认、未填确认文字时禁用、填写后提交 |
| 键盘 | Esc 关闭普通弹窗，焦点返回触发按钮 |

### 16.7 任务证据质量标准

高质量证据包含：

- 任务 ID。
- 改动文件。
- 关键实现说明。
- 具体运行命令。
- 命令结果。
- 截图路径或人工检查视口。
- 未运行验证的原因。
- 剩余风险。
- 下一任务建议。

低质量证据示例：

- 「已完成」。
- 「测试通过」但没有命令。
- 「移动端正常」但没有视口。
- 「文案已优化」但没有扫描或命中说明。
- 「没有风险」但没有说明检查范围。

可接受的人工证据：

- `390x844` 下在线中心首屏显示主行动，底部无遮挡。
- `1366x768` 下节会页主舞台和房间状态分区清晰。
- 结算确认弹窗展示奖励、成员影响和失败恢复说明。
- 技术详情默认折叠，展开后能看到凭证字段。

不可接受的关闭条件：

- 只创建组件但没有任何页面引用或结构检查计划。
- 只改文案但没有文案扫描或人工搜索。
- 只改 CSS 但没有移动端视口检查。
- 只移动按钮但没有确认旧 action 仍被调用。
- 只让 E2E 通过但玩家主路径仍能看到开发态词。

### 16.8 AI 交接块模板

如果一次执行没有完成全部任务，必须在最终回复或文档证据中留下交接块。

```markdown
AI 交接：
当前任务：OUI-XXX-000
当前状态：`[~]`
已完成：
- 已新增 `OnlineActionDialog.vue`
- 已接入基础样式
未完成：
- 还未补焦点返回
- 还未更新 `qa-online-ui-structure.mjs`
验证：
- 已运行 `npm run check`，通过
- 未运行 `npm run test:e2e`，原因：任务尚未接页面
下一步：
- 补焦点返回
- 增加结构检查
- 回填证据后关闭任务
```

### 16.9 AI 自检问题

关闭任务前，AI 必须逐项回答。

- 这个任务的依赖是否已经完成或有替代证据。
- 本次是否保留旧 store action 和接口语义。
- 本次是否保留旧 test id 或写了迁移说明。
- 玩家主路径是否还出现开发态词。
- 移动端主行动是否可见。
- 高风险动作是否有确认。
- 技术详情是否默认折叠。
- 失败态是否可恢复。
- loading 态是否避免重复提交。
- 验证命令是否真实存在。
- 证据是否足够让下一个 AI 判断任务状态。
