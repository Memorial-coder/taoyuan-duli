# 0605 修复 TODO

来源: `0605审查.md`
面向对象: 后续执行修复的 AI Agent
生成日期: 2026-06-05

- 勾选规则: 使用 Markdown 复选框，`- [ ]` 表示待处理，`- [x]` 表示已完成；每个任务至少在代码修复和验证通过后再勾选完成。
- 先修会造成坏档、卡死、重复发奖、经济账本不一致的问题，再修 UI 表现、数据治理和性能问题。
- 每个任务都要先读源文件和相邻调用链，不要只按行号机械修改。审查行号用于定位，不保证后续仍完全一致。
- 涉及奖励、存档、交易、房间动作的修复，必须补幂等或事务回滚测试；不能只靠前端按钮禁用。
- 涉及存档结构或物品 ID 迁移时，必须兼容旧档，并写清迁移规则。
- 涉及 UI 文案与运行时不一致时，先确认真实设计；不能同时保留错误文案和未实现逻辑。
- 每完成一组前端改动，至少运行 `cd taoyuan-main && npm run check`；涉及构建风险时运行 `npm run build`。
- 每完成一组服务端改动，至少运行对应 QA 脚本；可用脚本包括 `cd server && npm run qa:save-corruption-guard`、`npm run qa:online-smoke`、`npm run qa:coop-order-relay`、`npm run qa:coop-order-compensation-replay`、`npm run qa:realtime-smoke`。
- 跨前后端协议变更要同步更新前端 API util、store 调用、服务端 route、QA 脚本和错误提示。

## 优先级总览

### P0
- [ ] `T00` 服务端保存接口拒绝无法解密的 `raw`，避免有效云存档被坏数据覆盖。

### P1
- [ ] `T01` 防御致死后回血绕过矿洞失败。
- [ ] `T02` 区域远征遭遇伤害到 0 HP 后仍成功推进。
- [ ] `T03` 路由历史返回后 `navigation` 暂停原因残留，游戏时钟不恢复。
- [ ] `T04` 新游戏未重置区域远征、前线纪事、玩家记录中心。
- [ ] `T05` 战斗专精文案与运行时效果不一致。
- [ ] `T06` 邮件奖励领取缺少玩家存档侧幂等。
- [ ] `T07` 协作委托个人铜钱奖励缺少玩家存档侧幂等。
- [ ] `T08` 矿洞 Boss/感染层满包奖励导致下层和离开都被阻断。
- [ ] `T09` 线上远征房间物品奖励溢出被静默标记为已持久化。
- [ ] `T10` 装备方案缺失目标装备时旧槽位属性和套装残留。
- [ ] `T11` 云存档 revision 由客户端时间戳驱动，旧 pending raw 可覆盖远端新档。
- [ ] `T12` 房间断线重连窗口没有超时物化。
- [ ] `T13` 房间玩法动作缺少幂等键，重复 POST 会重复累加贡献。
- [ ] `T14` 重复物品 ID 导致物品身份、售价、图标和任务引用串项。
- [ ] `T15` 特殊订单最终阶段奖励容量失败后残留已完成阶段状态。
- [ ] `T16` 额度导入/导出缺少交易幂等。

### P2
- [ ] `T17` 存档加载失败被吞，主菜单没有失败反馈。
- [ ] `T18` 吸血按溢出总伤害回血。
- [ ] `T19` 匕首追击多段真实伤害没有进入浮字。
- [ ] `T20` 钓鱼宝箱幸运料理固定按 5% 结算。
- [ ] `T21` 远征构筑首领抗压重复叠加。
- [ ] `T22` 读档不校验武器索引和装备定义。
- [ ] `T23` 高频玩法日志链路无容量上限。
- [ ] `T24` 矿洞战斗动画和浮字 timeout 没有生命周期所有权。
- [ ] `T25` 任务奖励面板显示基础奖励而非真实结算值。
- [ ] `T26` 临时背包满包时按钮可点状态与真实容量不一致。
- [ ] `T27` HP-only 药品在普通背包详情不可见、不可食用。
- [ ] `T28` 每周交换站和节庆摊位缺少交易幂等。

## P0 立即修复

### T00 服务端保存接口必须拒绝无法解密的 `raw`

- [x] 状态: 已完成

严重度: P0
来源分区: 存档同步重连

涉及文件:
- `server/src/taoyuanSaveRuntime.js`
- `server/src/routes/api.js`
- `taoyuan-main/src/stores/useSaveStore.ts`
- `server/scripts/qa-save-corruption-guard.mjs`

问题:
- `decryptTaoyuanRaw()` 对非 `Salted__`、无法解密或非法 JSON 的 `raw` 返回 `null`。
- `prepareSlotEntryForSave()` 在缺少有效 `gameplayData.player` 时仍返回原始 `raw`。
- `/taoyuan/save/:slot` 会覆盖原槽位，客户端再读时槽位表现为空或坏档。

修复要求:
- `prepareSlotEntryForSave()` 遇到无法解密、无法 normalize、缺少 `gameplayData.player` 时返回明确错误，route 返回 422。
- 422 分支不得覆盖原槽位的 `raw` 和 `revision`。
- 如果要提供坏档恢复，只能走单独恢复接口，不得混入普通保存接口。
- 客户端收到 422 时展示“云存档数据无效，已保留远端旧档”一类明确提示，并停止继续自动重试同一坏 raw。

验收:
- 先写入有效云存档，再 POST `raw: "not-a-save"` 到同一槽位，接口返回 422。
- 服务端文件中旧 `raw` 和 `revision` 不变，客户端槽位仍可解析。
- `server/scripts/qa-save-corruption-guard.mjs` 增加无法解密 raw 入库用例。

建议命令:
- `cd server && npm run qa:save-corruption-guard`

修复记录:
- 修改文件: `server/src/taoyuanSaveRuntime.js`、`server/src/routes/api.js`、`taoyuan-main/src/stores/useSaveStore.ts`、`server/scripts/qa-save-corruption-guard.mjs`。
- 验证命令: `npm --prefix server run qa:save-corruption-guard`，结果通过，覆盖有效档后提交 `raw: "not-a-save"` 返回 422，旧 `raw/revision` 不变；同时覆盖缺少 `gameplayData.player` 的 raw。
- 验证命令: `npm exec eslint -- src/stores/useSaveStore.ts`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，已运行但被非本轮文件 `src/stores/useMiningStore.ts` 未使用类型/变量阻断。
- 本轮复验: 2026-06-06 运行 `npm --prefix server run qa:save-corruption-guard`，结果通过，坏 raw 拒绝和旧槽位保护仍有效。
- 追加复验: 2026-06-06 运行 `npm --prefix server run qa:save-corruption-guard`、`npm --prefix taoyuan-main run check`，结果通过；坏 raw/缺 `gameplayData.player` 拒绝和旧槽位保护仍有效，`check` 仅保留既有 3 条 warning。
- 完成度复核: 2026-06-06 修改文件仅 `0605修复todo.md`；运行 `npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；坏 raw 拒绝、旧槽位保护、CAS 读写主路径和在线保存路径均未回归，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 并行改动后复验: 2026-06-06 当前工作树中 `server/src/routes/api.js` 等 Agent A 相关文件存在并行 diff；运行 `npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过，坏 raw 拒绝、CAS stale 409 和在线保存路径仍有效。
- 收口复验: 2026-06-06 运行 `npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过；坏 raw 拒绝、CAS stale 409、旧远端 raw 保护和在线保存路径仍有效。验证限制: `npm --prefix taoyuan-main run check` 被未跟踪文件 `taoyuan-main/src/utils/aiAssistantPendingStages.ts` 的 `TS2322` 阻断，非 Agent A 路径；已补跑 Agent A 相关前端目标 lint 和服务端 `node --check`，结果通过。
- 巡检复验: 2026-06-06 运行 `npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；坏 raw 拒绝、旧槽位保护、CAS stale 409 和在线保存路径未被当前并行工作树破坏，`check` 仅保留既有 3 条 warning。
- 连续巡检复验: 2026-06-06 当前 `server/src/routes/api.js` 仍有并行 diff；运行 `npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；坏 raw 拒绝、旧槽位保护、CAS stale 409 和在线保存读写路径仍有效，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 存档交易巡检: 2026-06-06 运行 `npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；坏 raw 拒绝、旧槽位保护、CAS stale 409、在线保存读写和实时通知路径仍有效，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 坏档 CAS 巡检: 2026-06-06 修改文件仅 `0605修复todo.md`；运行 `npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；坏 raw 拒绝、旧槽位保护、CAS stale 409、在线保存读写和实时通知路径仍有效，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 奖励房间巡检: 2026-06-06 当前 `server/src/routes/api.js` 仍有并行 diff；运行 `node --check server/src/routes/api.js`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；坏 raw 拒绝、旧槽位保护、在线保存读写和实时通知路径仍有效。验证说明: `qa:online-smoke` 首次 120 秒超时未作为通过证据，延长到 360 秒重跑通过，末尾仅输出内容审核拒绝的预期日志；`check` 仅保留既有 3 条 warning。
- 完成度审计: 2026-06-06 修改文件仅 `0605修复todo.md`；运行 `node --check server/src/routes/api.js`、`node --check server/src/taoyuanSaveRuntime.js`、`node --check server/scripts/qa-save-corruption-guard.mjs`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；坏 raw 拒绝、旧槽位保护、在线保存读写和实时通知路径仍有效，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 遗留风险: 本任务只阻止无法解密或无玩家数据的 raw 覆盖有效槽位；云存档 CAS 已由 `T11` 另行修复和验证。

## P1 核心流程与线上一致性

### T01 防御动作致死后不得先回血再判定死亡

- [x] 状态: 已完成

严重度: P1
来源分区: 战斗伤害结算

涉及文件:
- `taoyuan-main/src/stores/useMiningStore.ts`
- `taoyuan-main/src/utils/combatRuntime.ts`
- `taoyuan-main/src/stores/usePlayerStore.ts`

问题:
- `defend` 分支先扣血，再执行防守回血，最后才检查 `playerStore.hp <= 0`。
- `restoreHealth()` 允许从 0 回血，导致本该失败的致死回合被拉回。

修复要求:
- 防御分支扣血后立即判断死亡。
- 只有扣血后 HP 仍大于 0 时，才允许防守回血。
- 若后续实现 `indestructible` 致命保命，需要先统一保命规则，再在死亡判定前使用明确的保命分支，不能依赖普通回血绕过失败。

验收:
- 构造玩家 HP=1，怪物防御后伤害大于 1，玩家拥有 `defender` 或 `indestructible`。
- 点击防御后应进入失败处理，不得留在战斗中。
- 普通非致死防御仍能按原规则减伤和回血。

修复记录:
- 修改文件: `taoyuan-main/src/stores/useMiningStore.ts`、`taoyuan-main/scripts/qa-combat-flow-guards.mjs`、`taoyuan-main/package.json`。
- 验证命令: `npm --prefix taoyuan-main run qa:combat-flow-guards`，结果通过，覆盖 HP=1 + `defender` 防御致死，断言战斗失败且无“防守回气”。
- 验证命令: `npm exec eslint -- src/stores/useMiningStore.ts src/utils/combatRuntime.ts src/stores/useRegionMapStore.ts src/views/game/MiningView.vue scripts/qa-combat-flow-guards.mjs`，结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，已运行但被非本轮文件 `src/components/game/AdminOnlineGovernancePanel.vue` 既有类型错误阻断。
- 遗留风险: 全量 `check` 尚未通过，阻塞点不在本任务改动范围。

### T02 区域远征遭遇伤害到 0 HP 后必须进入失败态

- [x] 状态: 已完成

严重度: P1
来源分区: 战斗伤害结算

涉及文件:
- `taoyuan-main/src/stores/useRegionMapStore.ts`

问题:
- `resolveActiveEncounter()` 的 `hazard` 遭遇调用 `playerStore.takeDamage(damage)` 后不检查死亡。
- 函数继续清空 `pendingEncounter`、持久化 session，并返回 `success: true`。

修复要求:
- 遭遇伤害后复用 `advanceActiveExpedition()` 的死亡处理语义。
- 将 `session.status` 置为 `failure`，写入推荐路线和失败摘要。
- 失败后不得继续按成功遭遇结算收益。

验收:
- 构造 HP=1、`hazard` 待处理遭遇，选择会造成伤害的选项。
- 返回结果为失败态，`activeSession.status` 不再是 `ongoing`。
- 非致死遭遇仍正常清空 `pendingEncounter` 并推进。

修复记录:
- 修改文件: `taoyuan-main/src/stores/useRegionMapStore.ts`、`taoyuan-main/scripts/qa-combat-flow-guards.mjs`、`taoyuan-main/package.json`。
- 验证命令: `npm --prefix taoyuan-main run qa:combat-flow-guards`，结果通过，覆盖 HP=1 + `hazard` 遭遇致死，断言返回“远征失利”、`activeSession.status === 'failure'`、不继续累加遭遇奖励。
- 验证命令: `npm exec eslint -- src/stores/useMiningStore.ts src/utils/combatRuntime.ts src/stores/useRegionMapStore.ts src/views/game/MiningView.vue scripts/qa-combat-flow-guards.mjs`，结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，已运行但被非本轮文件 `src/components/game/AdminOnlineGovernancePanel.vue` 既有类型错误阻断。
- 遗留风险: 全量 `check` 尚未通过，阻塞点不在本任务改动范围。

### T03 路由变化必须同步 `navigation` 暂停原因

- [x] 状态: 已完成

严重度: P1
来源分区: 核心运行时流程

涉及文件:
- `taoyuan-main/src/composables/useNavigation.ts`
- `taoyuan-main/src/composables/useGameClock.ts`
- `taoyuan-main/src/views/GameLayout.vue`
- `taoyuan-main/src/data/timeConstants.ts`

问题:
- `navigateToPanel()` 进入背包、技能、线上等暂停面板时会 `pauseClock('navigation')`。
- 浏览器 Back、系统历史返回、`router.back()` 等路径不会经过 `navigateToPanel()`，回到正常场景后暂停原因仍残留。

修复要求:
- 在 `GameLayout` 监听 `route.name` 时，基于 `TAB_TO_LOCATION_GROUP` 或等价路由派生状态统一同步 `pauseClock/resumeClock('navigation')`。
- `stopClock()` 或新游戏生命周期入口应清理非手动暂停原因。
- 保留手动暂停与系统暂停的语义边界，不要把所有 pause reason 一次性清空。

验收:
- `farm -> inventory` 时钟暂停。
- 浏览器 Back 回到 `farm` 后，`hasPauseReason('navigation') === false`，游戏分钟继续增长。
- 覆盖 `online -> farm`、`skills -> farm`、移动端硬件返回。

修复记录:
- 修改文件: `taoyuan-main/src/composables/useNavigation.ts`、`taoyuan-main/src/composables/useGameClock.ts`、`taoyuan-main/src/views/GameLayout.vue`、`taoyuan-main/scripts/qa-navigation-clock-guards.mjs`、`taoyuan-main/package.json`。
- 修复口径: `useNavigation` 新增路由到 `navigation` 暂停原因的统一判定与同步 helper，`GameLayout` 的 `route.name` watcher 立即同步该 helper，覆盖浏览器 Back、系统历史返回和移动端返回；`stopClock()` 清理 `modal/navigation/visibility/endday/settings/hanhaiCasino` 等非手动暂停原因并保留 `manual`。
- 验证命令: `npm --prefix taoyuan-main run qa:navigation-clock-guards`，结果通过，覆盖 `farm -> inventory` 暂停、`inventory/skills/online-festival -> farm` 清理 `navigation`、返回 farm 后游戏时间增长、手动暂停保留、`stopClock()` 清理非手动暂停原因。
- 验证命令: `node --check scripts/qa-navigation-clock-guards.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm exec eslint -- src/composables/useNavigation.ts src/composables/useGameClock.ts src/views/GameLayout.vue scripts/qa-navigation-clock-guards.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，`type-check` 通过，`lint` 被非本轮文件 `src/components/game/AdminOnlineGovernancePanel.vue` 既有错误 `banAdminOnlineUser` 未使用阻断；另保留既有 3 条 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 验证命令: `npm --prefix taoyuan-main run qa:mobile-ui-smoke`，结果通过；日志中有本地服务端未启动导致的 `127.0.0.1:4013` proxy refused，但命令退出码为 0。
- 未运行: `npm --prefix taoyuan-main run prepare:item-icons`，本任务未改资源 manifest 或图标产物；`npm --prefix taoyuan-main run test:e2e` 未运行，已用新增固定时钟 QA、目标 eslint、全量 type-check 和移动 smoke 覆盖本轮风险。
- 遗留风险: 全量 `check` 仍被 Agent A/线上治理范围的 lint error 阻断；完整浏览器级历史返回 E2E 尚未新增。

### T04 新游戏流程必须重置新增晚期系统 store

- [x] 状态: 已完成

严重度: P1
来源分区: 核心运行时流程

涉及文件:
- `taoyuan-main/src/composables/useResetGame.ts`
- `taoyuan-main/src/views/MainMenu.vue`
- `taoyuan-main/src/stores/useSaveStore.ts`
- `taoyuan-main/src/stores/useRegionMapStore.ts`
- `taoyuan-main/src/stores/useFrontierChronicleStore.ts`
- `taoyuan-main/src/stores/usePlayerRecordCenterStore.ts`

问题:
- `resetAllStoresForNewGame()` 没有重置 `regionMap`、`frontierChronicle`、`playerRecordCenter`。
- 同一前端会话中先加载晚期档再开新档，新档首次保存会携带旧档远征、纪事和记录中心数据。

修复要求:
- 在 `useResetGame.ts` 中补齐三个 store 的 import 和 `$reset()`。
- 新游戏开始前后确认这些字段都是初始态。
- 如 reset 顺序影响保存槽位或账号状态，按现有 reset 模式最小改动。

验收:
- 加载已有区域远征、纪事、记录中心数据的旧档。
- 不刷新页面直接新建角色并保存。
- 新存档的 `regionMap`、`frontierChronicle`、`playerRecordCenter` 不包含旧 route/session/digest/record 数据。

修复记录:
- 修改文件: `taoyuan-main/src/composables/useResetGame.ts`、`taoyuan-main/src/stores/useRegionMapStore.ts`、`taoyuan-main/src/stores/useFrontierChronicleStore.ts`、`taoyuan-main/src/stores/usePlayerRecordCenterStore.ts`、`taoyuan-main/scripts/qa-new-game-reset-guards.mjs`、`taoyuan-main/package.json`。
- 修复口径: `resetAllStoresForNewGame()` 纳入 `regionMap`、`frontierChronicle`、`playerRecordCenter`；三个晚期 setup store 增加明确 `$reset()` 或复用现有 reset；新增固定旧档状态 QA，覆盖旧远征会话/历史/动作状态、前线纪事/传闻/留影/区域 notable、玩家记录中心日报/已读/标签在新游戏 reset 后归零。
- 验证命令: `node --check taoyuan-main/scripts/qa-new-game-reset-guards.mjs`，结果通过。
- 验证命令: `npm --prefix taoyuan-main run qa:new-game-reset-guards`，结果通过，覆盖旧档数据注入、`resetAllStoresForNewGame()` 后序列化初始态，以及 `gameStore.startNewGame()` 后仍不串旧数据。
- 验证命令: `npm exec eslint -- src/composables/useResetGame.ts src/stores/useRegionMapStore.ts src/stores/useFrontierChronicleStore.ts src/stores/usePlayerRecordCenterStore.ts scripts/qa-new-game-reset-guards.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，未通过；`type-check` 被非本轮、且属于用户排除范围的 `src/views/game/WalletView.vue` 额度兑换改动阻断，错误为两处 `showFloat(..., 'warning')` 参数类型不匹配。
- 验证命令: `npm --prefix taoyuan-main run qa:mobile-ui-smoke`，未通过；已生成 `docs/ui-smoke-2026-04-26/summary.json` 和多张截图，但最终在某次 `openHome()` 等待 `heading: 桃源乡` 时超时，期间本地服务端未启动导致多条 `127.0.0.1:4013` proxy refused 日志。
- 未运行: `npm --prefix taoyuan-main run prepare:item-icons`，本任务未改资源 manifest 或图标产物；`npm --prefix taoyuan-main run test:e2e`，本轮已用固定 store QA 和目标 eslint 覆盖核心验收。
- 遗留风险: 全量 `check` 与 mobile smoke 当前受并行的非 T04 改动/环境阻断，不能作为本任务通过证据；T04 的旧档后开新档串档路径已有固定状态回归覆盖。

### T05 统一战斗专精真实设计并修复运行时或文案

- [x] 状态: 已完成

严重度: P1
来源分区: 技能 Buff 状态

涉及文件:
- `taoyuan-main/src/views/game/SkillView.vue`
- `taoyuan-main/src/components/game/PerkSelectDialog.vue`
- `taoyuan-main/src/utils/combatRuntime.ts`
- `taoyuan-main/src/stores/useMiningStore.ts`
- `taoyuan-main/src/stores/useSkillStore.ts`

问题:
- UI 承诺 `acrobat` 闪避反击、`phantom_blade` 闪避双倍、`iron_fortress` 反弹、`shadow_sovereign` 闪避三倍、`indestructible` 致命保命或防御无敌反弹。
- 运行时主要只实现闪避率、防御减伤、防守回血，缺反击、反弹、保命冷却。
- `shadow_sovereign` 在不同 UI 中有 60%/80% 不一致。

修复要求:
- 先确定每个战斗专精的真实目标效果。
- 如果保留文案承诺，则在战斗动作结果中实现结构化的反击、反弹、保命冷却，并接入日志、浮字和死亡判定。
- 如果不实现复杂效果，则同步降级 `SkillView.vue`、`PerkSelectDialog.vue` 文案到当前运行时实际能力。
- 给 `setPerk5/10/15/20` 增加技能和分支合法性校验，调用方必须检查 setter 返回值。

验收:
- 固定 RNG 覆盖闪避、普通防御、致命伤。
- `acrobat`、`phantom_blade`、`shadow_sovereign` 的闪避效果与最终文案一致。
- `iron_fortress`、`indestructible` 的防御或保命效果与最终文案一致。
- 非法专精不能通过 store setter 写入，UI 不应记录“习得了新专精”。

修复记录:
- 修改文件: `taoyuan-main/src/stores/useSkillStore.ts`、`taoyuan-main/src/composables/useDialogs.ts`、`taoyuan-main/src/views/game/SkillView.vue`、`taoyuan-main/src/components/game/PerkSelectDialog.vue`、`taoyuan-main/scripts/qa-combat-flow-guards.mjs`。
- 修复口径: 战斗专精真实设计按当前运行时数值型效果统一，不新增反击、反弹、致命保命冷却；`SkillView.vue` 和 `PerkSelectDialog.vue` 已移除未实现承诺，并同步 `shadow_sovereign` 为 80% 闪避。
- 验证命令: `npm --prefix taoyuan-main run qa:combat-flow-guards`，结果通过，覆盖专精 UI 禁用未实现承诺、`acrobat/phantom_blade/shadow_sovereign` 闪避率、`iron_fortress/indestructible` 防御减伤和回血、不灭之躯致死防御仍失败、非法专精 setter 和旧档归一化。
- 验证命令: `npm exec eslint -- src/stores/useSkillStore.ts src/composables/useDialogs.ts src/views/game/SkillView.vue src/components/game/PerkSelectDialog.vue scripts/qa-combat-flow-guards.mjs src/stores/useMiningStore.ts src/views/game/MiningView.vue`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过；现有非本轮文件仍有 3 条 lint warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 遗留风险: 本任务选择文案降级而非实现反击、反弹、致命保命冷却；若后续设计重新要求这些效果，需要新增结构化战斗事件和冷却状态。

### T06 邮件奖励领取必须增加玩家存档侧幂等账本

- [x] 状态: 已完成

严重度: P1
来源分区: 道具掉落奖励

涉及文件:
- `server/src/taoyuanMailbox.js`
- `server/src/taoyuanSaveRuntime.js`

问题:
- 邮件领取先写玩家存档，再写邮箱 `claimed_at`。
- 如果玩家存档已落盘但邮箱落盘失败，重试会再次发奖。
- 重复领取判断只看邮箱侧状态，玩家存档没有 `delivery.id` 防重凭证。

修复要求:
- 在玩家存档中增加类似 `onlineMailRewards.appliedDeliveries[delivery.id]` 的幂等账本。
- 奖励结果和领取凭证必须在同一次 `persistGameplayData()` 中落盘。
- 重试发现凭证已存在时，只补写邮箱 `claimed_at/claim_result/claim_logs`，不再发奖。
- 一键领取和单封领取必须复用同一幂等逻辑。

验收:
- 故障注入: `applyRewardsToSave()` 成功后让 `saveMailboxData()` 抛错。
- 重试单封领取和一键领取，玩家铜钱、物品、装备只增加一次。
- 邮箱最终补齐 `claimed_at` 和领取日志。

修复记录:
- 修改文件: `server/src/taoyuanMailbox.js`、`server/scripts/qa-mailbox-reward-idempotency.mjs`、`server/package.json`。
- 验证命令: `npm --prefix server run qa:mailbox-reward-idempotency`，结果通过，覆盖单封领取和一键领取在邮箱状态回滚后重放，玩家存档侧 `onlineMailRewards.appliedDeliveries` 阻止铜钱、物品、武器奖励重复发放，并补齐邮箱 `claimed_at`。
- 本轮复验: 2026-06-06 运行 `npm --prefix server run qa:mailbox-reward-idempotency`，结果通过，单封和一键领取幂等账本仍有效。
- 追加复验: 2026-06-06 运行 `npm --prefix server run qa:mailbox-reward-idempotency`，结果通过，邮箱状态回滚后单封/一键领取仍不会重复发放玩家奖励。
- 追加复验: 2026-06-06 运行 `npm --prefix server run qa:mailbox-reward-idempotency`，结果通过；邮箱状态回滚后单封/一键重放仍只发放一次玩家奖励。
- 完成度复核: 2026-06-06 修改文件仅 `0605修复todo.md`；运行 `npm --prefix server run qa:mailbox-reward-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过；玩家存档侧 `onlineMailRewards.appliedDeliveries` 和在线主流程邮件领取路径未回归。
- 并行改动后复验: 2026-06-06 运行 `npm --prefix server run qa:mailbox-reward-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过，邮箱奖励存档侧账本和在线邮件领取主路径仍有效。
- 收口复验: 2026-06-06 运行 `npm --prefix server run qa:mailbox-reward-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过；邮箱状态回滚后单封/一键重放仍只发放一次奖励，在线邮件领取与实时通知路径未回归。
- 巡检复验: 2026-06-06 运行 `npm --prefix server run qa:mailbox-reward-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；邮箱奖励存档侧 `onlineMailRewards.appliedDeliveries` 和邮件领取主链路仍有效。
- 连续巡检复验: 2026-06-06 运行 `npm --prefix server run qa:mailbox-reward-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；邮箱状态回滚后单封/一键重放仍只发放一次奖励，邮件领取主链路和实时通知未回归。
- 在线奖励房间巡检: 2026-06-06 运行 `npm --prefix server run qa:mailbox-reward-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；邮箱奖励存档侧账本、邮件领取主链路和实时通知路径仍有效，`check` 仅保留既有 3 条 warning。
- 奖励幂等巡检: 2026-06-06 修改文件仅 `0605修复todo.md`；运行 `npm --prefix server run qa:mailbox-reward-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；单封/一键领取在邮箱状态回滚后仍依赖玩家存档侧 `onlineMailRewards.appliedDeliveries` 防重，邮件领取主链路和实时通知未回归，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 奖励房间巡检: 2026-06-06 当前 `server/src/taoyuanMailbox.js` 仍有并行 diff；运行 `node --check server/src/taoyuanMailbox.js`、`npm --prefix server run qa:mailbox-reward-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；单封/一键领取仍依赖玩家存档侧 `onlineMailRewards.appliedDeliveries` 防重，邮箱状态回滚后重放不重复发放奖励，在线邮件领取与实时通知路径未回归；`qa:online-smoke` 首次 120 秒超时未作为通过证据，360 秒重跑通过且仅输出内容审核拒绝的预期日志。
- 完成度审计: 2026-06-06 修改文件仅 `0605修复todo.md`；运行 `node --check server/src/taoyuanMailbox.js`、`node --check server/scripts/qa-mailbox-reward-idempotency.mjs`、`npm --prefix server run qa:mailbox-reward-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；邮箱奖励存档侧 `onlineMailRewards.appliedDeliveries` 仍防止单封/一键领取重放，邮件领取主链路和实时通知路径未回归，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 遗留风险: 无；单封和一键领取已复用同一个存档侧账本路径。

### T07 协作委托个人铜钱奖励必须使用 receipt 幂等键

- [x] 状态: 已完成

严重度: P1
来源分区: 道具掉落奖励

涉及文件:
- `server/src/taoyuanCoopOrderRuntime.js`

问题:
- receipt 有 `idempotency_key`，但 `applyMoneyReward()` 没有写入玩家存档侧防重账本。
- 玩家铜钱写入成功后，如果协作委托 store 保存失败，重试同一 receipt 或 compensation 会重复加钱。

修复要求:
- 将 receipt 的 `idempotency_key` 传入 `applyMoneyReward()`。
- 在玩家存档中维护 `coopOrderRewards.appliedReceipts` 或等价账本。
- 普通委托、多阶段委托、补偿重试必须复用原 receipt 键。
- 重复请求只返回已写入结果，并继续修复协作委托 store 状态。

验收:
- 对普通委托、多阶段委托、补偿重试做故障注入。
- `applyMoneyReward()` 成功后让 `saveCoopOrderStore()` 抛错，再重复确认或补偿。
- 玩家铜钱只增加一次，receipt 最终 `confirmed`，compensation 最终 `resolved`。

建议命令:
- `cd server && npm run qa:coop-order-relay`
- `cd server && npm run qa:coop-order-compensation-replay`

修复记录:
- 修改文件: `server/src/taoyuanCoopOrderRuntime.js`、`server/scripts/qa-coop-order-relay.mjs`、`server/scripts/qa-coop-order-compensation-replay.mjs`。
- 验证命令: `npm --prefix server run qa:coop-order-relay`，结果通过，覆盖协作委托 store 回滚后重放确认，玩家铜钱不重复增加。
- 验证命令: `npm --prefix server run qa:coop-order-compensation-replay`，结果通过，覆盖补偿重放在 store 回滚后继续 resolved，玩家铜钱不重复增加。
- 验证命令: `npm --prefix server run qa:online-smoke`，结果通过，覆盖普通与多阶段在线协作委托主流程；期间出现预期的内容审核拒绝日志但进程退出码为 0。
- 本轮复验: 2026-06-06 运行 `npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`，结果通过，receipt 重放和补偿重试仍只发放一次个人铜钱。
- 追加复验: 2026-06-06 运行 `npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`，结果通过，普通确认和补偿重放仍按 receipt 幂等结算。
- 追加复验: 2026-06-06 运行 `npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`，结果通过；协作委托 store 回滚后的确认重放和补偿重放仍不会重复增加个人铜钱。
- 完成度复核: 2026-06-06 修改文件仅 `0605修复todo.md`；运行 `npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:online-smoke`，结果通过；receipt 幂等确认、补偿重放和在线协作委托主路径未回归。
- 并行改动后复验: 2026-06-06 运行 `npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:online-smoke`，结果通过，receipt 幂等确认、补偿重放和在线协作委托主路径仍有效。
- 收口复验: 2026-06-06 运行 `npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过；receipt 幂等确认、补偿重放、在线协作委托和实时通知路径仍有效。
- 巡检复验: 2026-06-06 运行 `npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；协作委托个人铜钱 receipt 幂等、补偿重放和在线主链路仍有效。
- 连续巡检复验: 2026-06-06 运行 `npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；协作委托个人铜钱 receipt 幂等、确认重放、补偿重放和在线主链路仍有效。
- 在线奖励房间巡检: 2026-06-06 运行 `npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；协作委托 receipt 确认重放、补偿重放、在线委托主流程和实时通知路径仍有效。
- 奖励幂等巡检: 2026-06-06 修改文件仅 `0605修复todo.md`；运行 `npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；协作委托个人铜钱 receipt 确认重放、补偿重放、在线委托主流程和实时通知路径仍有效，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 奖励房间巡检: 2026-06-06 当前 `server/src/taoyuanCoopOrderRuntime.js` 仍有并行 diff；运行 `node --check server/src/taoyuanCoopOrderRuntime.js`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；协作委托个人铜钱 receipt 确认重放、补偿重放、在线委托主流程和实时通知路径仍有效；`qa:online-smoke` 首次 120 秒超时未作为通过证据，360 秒重跑通过且仅输出内容审核拒绝的预期日志。
- 完成度审计: 2026-06-06 修改文件仅 `0605修复todo.md`；运行 `node --check server/src/taoyuanCoopOrderRuntime.js`、`node --check server/scripts/qa-coop-order-relay.mjs`、`node --check server/scripts/qa-coop-order-compensation-replay.mjs`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；协作委托个人铜钱 receipt 确认重放、补偿重放、在线委托主流程和实时通知路径仍有效，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 遗留风险: 个人铜钱奖励已按 receipt 幂等；共同基金路线不属于本任务的个人铜钱奖励范围。

### T08 合并修复矿洞 Boss/感染层满包卡死

- [x] 状态: 已完成

严重度: P1
来源分区: 关卡任务触发、AI 敌人 Boss 行为

涉及文件:
- `taoyuan-main/src/stores/useMiningStore.ts`
- `taoyuan-main/src/views/game/MiningView.vue`
- `taoyuan-main/src/views/game/InventoryView.vue`

问题:
- `grantInfestedClearRewards()` 和 `grantMainMineBossRewards()` 在背包空间不足时返回未领取。
- `goNextFloor()` 和 `leaveMine()` 都把奖励领取成功作为前置。
- 玩家已击败 Boss 或清层，但无法下层，也无法离开整理背包。
- AI 敌人 Boss 行为分区报告的是同一根因，不要重复修两套逻辑。

修复要求:
- 将“退出矿洞”和“领取清层奖励”拆开。
- 满包时至少满足一种设计: 允许离开并把奖励放入待领取队列、临时背包、邮件、地面箱或补偿回执。
- 未领取奖励必须有幂等状态，不能重复发奖，也不能丢失 Boss/清层进度。
- 如果仍要阻止下层，必须提供矿洞内整理/丢弃或延迟领取通路。

验收:
- 主背包和临时背包都无法容纳 Boss 奖励，击败 Boss 后点击“下一层”和“离开”。
- 感染层清空后同样验证。
- 修复后玩家能离开或处理奖励，奖励不会丢失、不会重复领取，重新进入状态一致。

修复记录:
- 修改文件: `taoyuan-main/src/stores/useMiningStore.ts`、`taoyuan-main/src/views/game/MiningView.vue`、`taoyuan-main/scripts/qa-combat-flow-guards.mjs`、`taoyuan-main/package.json`。
- 验证命令: `npm --prefix taoyuan-main run qa:combat-flow-guards`，结果通过，覆盖 BOSS 层和感染层在主背包、临时背包都无法容纳奖励时暂存奖励、阻止继续下层、允许离开矿洞、整理背包后领取一次且不可重复领取。
- 验证命令: `npm exec eslint -- src/stores/useMiningStore.ts src/views/game/MiningView.vue scripts/qa-combat-flow-guards.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过；现有非本轮文件仍有 3 条 lint warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 遗留风险: 暂存奖励当前通过矿洞面板领取入口处理；若玩家长期不整理背包，仍会保留待领取队列并阻止继续下层，但不会阻止离开矿洞。

### T09 线上远征房间物品奖励溢出不得标记 persisted

- [x] 状态: 已完成

严重度: P1
来源分区: 关卡任务触发

涉及文件:
- `server/src/taoyuanActivityRoomRuntime.js`
- `server/scripts/qa-online-smoke.mjs`

问题:
- `addInventoryRewardItems()` 会返回 `overflowItems`。
- 远征分支只接收 `mainSlots/tempSlots`，不检查 `overflowItems`。
- 回执仍写入 `appliedReceipts` 并标记 `persisted`，导致奖励永久丢失。

修复要求:
- `applyActivityReceiptReward()` 必须检查 `inventoryResult.overflowItems.length`。
- 有溢出时，要么创建邮件/补偿附件并记录结果，要么抛错让回执进入 `compensation_pending`。
- 只有全部奖励落地或补偿记录创建成功，才能写入 `appliedReceipts` 和 `persisted`。

验收:
- 线上远征房间结算，目标玩家主背包和临时背包都满。
- 物品奖励必须进入背包、邮件或补偿队列之一。
- 不允许 `overflowItems` 非空但回执为 `persisted`。

建议命令:
- `cd server && npm run qa:online-smoke`

修复记录:
- 修改文件: `server/src/taoyuanActivityRoomRuntime.js`、`server/scripts/qa-online-smoke.mjs`。
- 验证命令: `npm --prefix server run qa:online-smoke`，结果通过，新增线上远征主背包和临时背包满包场景，断言物品回执进入 `compensation_pending` 且不是 `persisted`；期间出现预期的内容审核拒绝日志但进程退出码为 0。
- 验证命令: `npm --prefix server run qa:realtime-smoke`，结果通过，确认实时在线通知和活动房间订阅主路径未被破坏。
- 本轮复验: 2026-06-06 运行 `npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过，远征满包补偿和活动房间订阅路径未回归。
- 追加复验: 2026-06-06 运行 `npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；`online-smoke` 覆盖远征满包补偿关闭路径，`check` 仅保留既有 3 条 warning。
- 完成度复核: 2026-06-06 修改文件仅 `0605修复todo.md`；运行 `npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过；线上远征满包回执仍进入补偿路径且不会静默标记 `persisted`，实时活动房间订阅路径未回归。
- 并行改动后复验: 2026-06-06 运行 `npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过，线上远征满包补偿路径、房间奖励落盘和实时订阅主链路仍有效；`online-smoke` 仅输出内容审核拒绝的预期错误日志。
- 收口复验: 2026-06-06 运行 `npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过；线上远征满包回执仍进入补偿路径且不会静默标记 `persisted`，房间奖励落盘和实时订阅主链路仍有效。
- 巡检复验: 2026-06-06 运行 `npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；线上远征满包 close 仍走补偿路径，未出现溢出回执被标记 `persisted` 的回归。
- 连续巡检复验: 2026-06-06 运行 `npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；线上远征满包 close 仍走补偿路径，未出现溢出回执被标记 `persisted` 的回归。
- 在线奖励房间巡检: 2026-06-06 运行 `npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；线上远征满包 close 仍进入补偿路径，未出现溢出回执被标记 `persisted` 的回归，房间实时订阅未回归。
- 奖励幂等巡检: 2026-06-06 修改文件仅 `0605修复todo.md`；运行 `npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；线上远征满包 close 仍进入补偿路径，未出现溢出回执被标记 `persisted` 的回归，房间动作幂等、重连超时物化和实时订阅路径未回归，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 奖励房间巡检: 2026-06-06 当前 `server/src/taoyuanActivityRoomRuntime.js` 仍有并行 diff；运行 `node --check server/src/taoyuanActivityRoomRuntime.js`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；线上远征满包 close 仍进入补偿路径，未出现溢出回执被标记 `persisted` 的回归，房间奖励落盘和实时订阅路径未回归；`qa:online-smoke` 首次 120 秒超时未作为通过证据，360 秒重跑通过且仅输出内容审核拒绝的预期日志。
- 完成度审计: 2026-06-06 修改文件仅 `0605修复todo.md`；运行 `node --check server/src/taoyuanActivityRoomRuntime.js`、`node --check server/scripts/qa-online-smoke.mjs`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；线上远征满包 close 仍进入补偿路径，未出现溢出回执被标记 `persisted` 的回归，房间奖励落盘和实时订阅路径未回归，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 遗留风险: 当前溢出处理走补偿队列，未改为邮件附件即时落地；后续需要依赖既有补偿处理链路完成最终发放。

### T10 装备方案缺失目标装备时不得保留旧槽位

- [x] 状态: 已完成

严重度: P1
来源分区: 角色成长装备

涉及文件:
- `taoyuan-main/src/stores/useInventoryStore.ts`

问题:
- `applyEquipmentPreset()` 找不到保存的目标装备时，只记录 `missing`，不清空对应槽位。
- 之后仍设置 `activePresetId` 并返回成功，旧装备属性和套装效果残留。

修复要求:
- 每个非空 preset 槽位，如果目标装备不存在或装备失败，应显式清空该槽位。
- 或者把方案应用改为事务式，任何必需装备缺失时回滚并不更新 `activePresetId`。
- 武器槽如必须保留默认武器，要明确回退并提示。

验收:
- 当前穿方案 A 的矿工套，方案 B 保存已不存在的戒指或帽子。
- 套用 B 后，对应槽位清空或方案应用失败。
- `getEquipmentBonus('mining_stamina')` 和 `activeSetBonuses` 不含 A 的残留。

修复记录:
- 修改文件: `taoyuan-main/src/stores/useInventoryStore.ts`、`taoyuan-main/scripts/qa-equipment-guards.mjs`、`taoyuan-main/package.json`。
- 修复口径: `applyEquipmentPreset()` 对每个非空目标槽位执行显式清理；目标武器缺失时回退到有效的 `wooden_stick`，戒指、帽子、鞋子、饰物缺失或装备失败时清空对应槽位，并保留缺失提示。
- 验证命令: `npm --prefix taoyuan-main run qa:equipment-guards`，结果通过，覆盖当前装备矿工套后套用缺失方案，断言武器回退、戒指/帽子/鞋子清空、`getEquipmentBonus('mining_stamina') === 0` 且矿工套不再激活。
- 验证命令: `npm exec eslint -- src/stores/useInventoryStore.ts scripts/qa-equipment-guards.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过；现有非本轮文件仍有 3 条 lint warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 验证命令: `npm --prefix taoyuan-main run qa:combat-flow-guards`，结果通过，确认装备归一化改动未破坏既有战斗护栏。
- 遗留风险: 缺失方案仍会设置 `activePresetId` 并返回成功提示；当前设计选择是“应用可用槽位并清空缺失槽位”，不是事务式整体失败。

### T11 云存档 revision 改为服务端 CAS 协议

- [x] 状态: 已完成

严重度: P1
来源分区: 存档同步重连

涉及文件:
- `server/src/routes/api.js`
- `server/src/taoyuanSaveRuntime.js`
- `taoyuan-main/src/stores/useSaveStore.ts`
- `taoyuan-main/src/utils/serverSaveApi.ts`

问题:
- 客户端用 `Date.now()` 生成 revision。
- 服务端只拒绝小于当前 revision 的请求，相等或更大都会覆盖。
- 读取接口不返回服务端 revision，客户端 pending raw 可覆盖远端新 raw。

修复要求:
- 服务端签发或递增 revision。
- 保存请求提交 `base_revision`，必须与当前 revision 精确匹配。
- 冲突返回 409/stale，并返回最新 revision/raw。
- `GET /save/slots` 和 `GET /save/:slot` 返回 revision。
- 客户端遇到 pending raw 与远端新 raw 冲突时，不得默认用 pending raw 覆盖，应进入刷新、合并或用户确认流程。

验收:
- 当前槽位 revision=10，两次用不同 raw 提交 base_revision=10，第二次必须 409，远端 raw 不变。
- 未来时间戳请求不能让后续正常保存永久 stale。
- 客户端读档优先级不再让旧 pending raw 静默压过远端新 raw。

修复记录:
- 修改文件: `server/src/routes/api.js`、`server/src/taoyuanSaveRuntime.js`、`taoyuan-main/src/utils/serverSaveApi.ts`、`taoyuan-main/src/stores/useSaveStore.ts`、`server/scripts/qa-save-cas-guard.mjs`、`server/scripts/qa-online-smoke.mjs`、`server/scripts/qa-realtime-smoke.mjs`、`server/package.json`。
- 服务端保存接口改为要求 `base_revision`；缺失返回 `400/TAOYUAN_SAVE_BASE_REVISION_REQUIRED`，不匹配返回 `409/TAOYUAN_SAVE_REVISION_STALE` 并带最新 `revision/raw`；成功保存由服务端按当前 revision 顺序递增，忽略客户端 `revision`。
- `GET /taoyuan/save/slots` 与 `GET /taoyuan/save/:slot` 返回服务端 revision；前端 API util 和存档 store 改为携带 base revision，同步遇到 stale 时保留远端 raw、暂停本地 pending 上传并给出冲突提示。
- QA 脚本已补 `qa:save-cas-guard` 并同步 `qa-online-smoke`、`qa-realtime-smoke` 的保存请求到 `base_revision` 协议。
- 验证命令: `node --check server/scripts/qa-save-cas-guard.mjs`、`node --check server/scripts/qa-online-smoke.mjs`、`node --check server/scripts/qa-realtime-smoke.mjs`、`node --check server/src/routes/api.js`、`node --check server/src/taoyuanSaveRuntime.js`、`node -e "JSON.parse(require('fs').readFileSync('server/package.json','utf8')); console.log('server/package.json ok')"`，结果通过。
- 验证命令: `npm --prefix server run qa:save-cas-guard`，结果通过，覆盖缺失 `base_revision` 返回 400、旧 base revision 返回 409 且远端 raw 不变、未来 `revision` 被忽略、后续正常 CAS 保存继续递增。
- 验证命令: `npm --prefix server run qa:save-corruption-guard`，结果通过，确认 T00 坏档保护未被 CAS 改动破坏。
- 验证命令: `npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:realtime-smoke`，结果均通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过，保留既有 3 个 warning: `src/utils/onlineProfileApi.ts` dynamic import type、`src/views/game/CottageView.vue` 和 `src/views/game/FarmView.vue` floating promise。
- 本轮复验: 2026-06-06 运行 `npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:save-corruption-guard`，结果通过，CAS stale 409、服务端 revision 递增和坏档保护仍有效。
- 追加复验: 2026-06-06 运行 `npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:save-corruption-guard`，结果通过，缺失 base revision 拒绝、stale 409、远端 raw 保护和坏 raw 拒绝仍有效。
- 追加复验: 2026-06-06 运行 `npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；在线保存读写路径继续兼容 `base_revision` 协议，stale 409 仍以上述 `qa:save-cas-guard` 为专项证据。
- 追加复验: 2026-06-06 运行 `npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix taoyuan-main run check`，结果通过；缺失 `base_revision`、stale 409、远端 raw 保护和坏 raw 拒绝仍有效，`check` 仅保留既有 3 条 warning。
- 完成度复核: 2026-06-06 修改文件仅 `0605修复todo.md`；运行 `npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；缺失 `base_revision`、stale 409、远端 raw 保护、坏 raw 拒绝和在线保存读写路径均未回归。
- 并行改动后复验: 2026-06-06 运行 `npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过，服务端 CAS、旧远端 raw 保护、坏 raw 拒绝和在线保存读写路径仍有效。
- 收口复验: 2026-06-06 运行 `npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过；缺失 `base_revision`、stale 409、旧远端 raw 保护、坏 raw 拒绝和在线保存读写路径仍有效。
- 巡检复验: 2026-06-06 运行 `npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；服务端签发 revision、缺失 `base_revision` 拒绝、stale 409 和旧远端 raw 保护仍有效。
- 连续巡检复验: 2026-06-06 运行 `npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；服务端 CAS、缺失 `base_revision` 拒绝、stale 409、旧远端 raw 保护和在线保存读写路径仍有效。
- 存档交易巡检: 2026-06-06 运行 `npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；服务端 CAS、缺失 `base_revision` 拒绝、stale 409、旧远端 raw 保护、在线保存读写和实时通知路径仍有效。
- 坏档 CAS 巡检: 2026-06-06 修改文件仅 `0605修复todo.md`；运行 `npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；服务端 CAS、缺失 `base_revision` 拒绝、stale 409、旧远端 raw 保护、在线保存读写和实时通知路径仍有效，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 房间 CAS 复验: 2026-06-06 当前 `server/src/routes/api.js`、`server/src/taoyuanActivityRoomRuntime.js` 存在并行 diff；运行 `npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix taoyuan-main run check`、`node --check server/src/routes/api.js`、`node --check server/src/taoyuanSaveRuntime.js`、`node --check server/src/taoyuanActivityRoomRuntime.js`，结果通过；服务端签发 revision、缺失 `base_revision` 拒绝、stale 409、旧远端 raw 保护、坏 raw 拒绝、在线保存读写和实时通知路径仍有效，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- CAS 巡检复验: 2026-06-06 当前 `server/src/routes/api.js` 存在并行 diff；运行 `node --check server/src/routes/api.js`、`node --check server/src/taoyuanSaveRuntime.js`、`node --check server/scripts/qa-save-cas-guard.mjs`、`node --check server/scripts/qa-online-smoke.mjs`、`node --check server/scripts/qa-realtime-smoke.mjs`、`npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix taoyuan-main run check`，结果通过；CAS stale 409、旧远端 raw 保护、缺失 `base_revision` 拒绝、未来客户端 revision 忽略、在线保存读写和实时通知路径仍有效，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- CAS 复核: 2026-06-06 当前 `server/src/routes/api.js` 存在并行内容治理 diff；运行 `node --check server/src/routes/api.js`、`node --check server/src/taoyuanSaveRuntime.js`、`node --check server/scripts/qa-save-cas-guard.mjs`、`node --check server/scripts/qa-save-corruption-guard.mjs`、`node --check server/scripts/qa-online-smoke.mjs`、`node --check server/scripts/qa-realtime-smoke.mjs`、`npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm exec eslint -- src/utils/serverSaveApi.ts src/stores/useSaveStore.ts`（工作目录 `taoyuan-main`），结果通过；服务端签发 revision、缺失 `base_revision` 拒绝、stale 409 返回最新 `revision/raw`、旧远端 raw 保护、未来客户端 revision 忽略、前端 pending 冲突暂停和在线实时链路仍有效。验证限制: `npm --prefix taoyuan-main run check` 被 Agent B 范围并行文件 `taoyuan-main/src/views/game/MiningView.vue` 的 `TS2339 zone.bossId` 阻断，未作为通过证据。
- 遗留风险: 客户端 CAS 冲突当前采用“暂停 pending 上传并提示重新读取远端存档”的保守策略，未实现自动合并或用户手动覆盖流程；这避免静默覆盖，但需要后续 UX 明确冲突处理入口。

### T12 房间断线重连窗口必须超时物化

- [x] 状态: 已完成

严重度: P1
来源分区: 存档同步重连

涉及文件:
- `server/src/taoyuanActivityRoomRuntime.js`
- `docs/online/08-room-state-machine.md`

问题:
- 房间记录 `disconnected_at` 和 `reconnect_window_seconds`，但重连、overview、快照不检查是否超窗。
- 房间可永久 `paused`，玩家被 `ensureNoOtherActiveRoom()` 占用。

修复要求:
- 在 overview、reconnect、settle、close、countdown materialize 等入口统一物化断线超时。
- 超窗后清除 `can_reconnect`，按设计将成员置为 `left`、`failed` 或 `timeout`。
- 房间应恢复、终止或进入可结算失败分支，并释放玩家占用。
- 重连接口超过窗口返回明确错误并触发快照刷新。

验收:
- 创建节会和远征房间，运行后断开成员。
- 把 `disconnected_at` 回拨到超过窗口。
- overview 显示不可重连，reconnect 失败，玩家能创建或加入其他房间，原房间进入明确收尾状态。

修复记录:
- 修改文件: `server/src/taoyuanActivityRoomRuntime.js`、`server/src/routes/api.js`、`docs/online/08-room-state-machine.md`、`server/scripts/qa-activity-room-reconnect-idempotency.mjs`、`server/scripts/qa-online-smoke.mjs`、`server/package.json`、`taoyuan-main/src/utils/festivalRoomApi.ts`、`taoyuan-main/src/utils/expeditionRoomApi.ts`。
- 房间运行时新增 `timeout` 成员状态和重连超时物化入口；overview/snapshot、join/create、invite/leave/ready/start、reconnect、settle、close 统一刷新倒计时、玩法阶段和断线超时状态。
- 超过 `reconnect_window_seconds` 后成员转为 `timeout`、`can_reconnect=false`、不再占用 `ensureNoOtherActiveRoom()`；无剩余断线成员时房间恢复原运行态或明确中止。
- 重连接口超窗返回 `409/TAOYUAN_ACTIVITY_ROOM_RECONNECT_EXPIRED`，route 返回结构化 `code`；前端房间成员类型补入 `timeout`。
- 验证命令: `npm --prefix server run qa:activity-room-reconnect-idempotency`，结果通过，覆盖节会和远征房间断线后回拨 `disconnected_at` 超窗，overview 物化 `timeout`、reconnect 返回 409、玩家可创建新房间。
- 验证命令: `npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过。
- 验证命令: `node --check server/src/taoyuanActivityRoomRuntime.js`、`node --check server/src/routes/api.js`、`node --check server/scripts/qa-activity-room-reconnect-idempotency.mjs`、`node --check server/scripts/qa-online-smoke.mjs`，结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过，保留既有 3 个 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 本轮复验: 2026-06-06 运行 `npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过，断线超时物化和新房间释放占用仍有效。
- 追加复验: 2026-06-06 运行 `npm --prefix server run qa:activity-room-reconnect-idempotency`，结果通过，节会和远征断线超窗后 overview 物化 `timeout`、reconnect 返回 409，玩家可创建新房间。
- 追加复验: 2026-06-06 运行 `npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；节会/远征房间重连、活动房间订阅和前端协议类型仍通过主回归。
- 追加复验: 2026-06-06 运行 `npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；断线超窗物化、重连 409、新房间释放占用、活动房间订阅和前端协议类型均未回归。
- 完成度复核: 2026-06-06 修改文件仅 `0605修复todo.md`；运行 `npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；断线超窗物化、重连 409、新房间释放占用、活动房间订阅和前端协议类型均未回归。
- 并行改动后复验: 2026-06-06 运行 `npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过，断线超窗物化、重连 409、新房间释放占用、活动房间订阅和前端协议类型仍有效。
- 收口复验: 2026-06-06 运行 `npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过；断线超窗物化、重连 409、新房间释放占用和活动房间实时订阅仍有效。
- 巡检复验: 2026-06-06 运行 `npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；节会/远征断线超窗物化、重连 409、新房间释放占用和活动房间订阅仍有效。
- 连续巡检复验: 2026-06-06 运行 `npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；节会/远征断线超窗物化、重连 409、新房间释放占用和活动房间订阅仍有效。
- 在线奖励房间巡检: 2026-06-06 运行 `npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；节会/远征断线超窗物化、重连 409、新房间释放占用和活动房间订阅仍有效。
- 房间 CAS 复验: 2026-06-06 当前 `server/src/taoyuanActivityRoomRuntime.js` 存在并行内容审核 diff；运行 `npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix taoyuan-main run check`、`node --check server/src/routes/api.js`、`node --check server/src/taoyuanActivityRoomRuntime.js`，结果通过；节会/远征断线超窗后 overview 物化 `timeout`、reconnect 返回 409、新房间释放占用、活动房间订阅和在线主流程仍有效，`check` 仅保留既有 3 条 warning。
- 房间幂等巡检: 2026-06-06 当前 `server/src/routes/api.js`、`server/src/taoyuanActivityRoomRuntime.js` 存在并行内容审核/治理 diff；运行 `node --check server/src/taoyuanActivityRoomRuntime.js`、`node --check server/src/routes/api.js`、`node --check server/scripts/qa-activity-room-reconnect-idempotency.mjs`、`node --check server/scripts/qa-online-smoke.mjs`、`node --check server/scripts/qa-realtime-smoke.mjs`、`npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix taoyuan-main run check`，结果通过；节会/远征断线超窗后 overview 物化 `timeout`、reconnect 返回 409、新房间释放占用、活动房间订阅、在线主流程和坏档保护仍有效，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 本轮续验: 2026-06-06 当前工作树仍有大量并行 diff；运行 `npm --prefix server run qa:realtime-smoke`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix taoyuan-main run check`，结果通过；活动房间实时订阅、协作委托幂等链路、坏档保护和前端房间协议类型仍有效，`check` 仅保留既有 3 条 warning。
- 遗留风险: 目前超时成员采用 `timeout` 保留在房间快照中便于回看和刷新，不自动生成失败补偿；若设计需要“超时即部分结算”，需另行定义奖励和补偿规则。

### T13 房间玩法动作接口必须要求幂等键

- [x] 状态: 已完成

严重度: P1
来源分区: 存档同步重连

涉及文件:
- `taoyuan-main/src/utils/festivalRoomApi.ts`
- `taoyuan-main/src/utils/expeditionRoomApi.ts`
- `server/src/routes/api.js`
- `server/src/taoyuanActivityRoomRuntime.js`

问题:
- 客户端动作请求体只有 `{ action_id }`。
- 重复 POST 会重复递增 `action_count`、`progress_value`、`score_value`。
- 结算奖励依赖污染后的贡献输入。

修复要求:
- 动作接口必须携带 `idempotency_key`。
- 服务端按 `roomId + member + idempotency_key` 记录动作结果。
- 同 key 重放返回第一次结果，不再次修改贡献。
- 无幂等键的写请求应拒绝。
- 检查没有配置 `unique_per_member` 或 `once_per_round` 的动作模板，确认是否符合设计。

验收:
- 在 `expedition_roles` 房间对 `assign_scout` 连续发送两次相同幂等键。
- 第二次为 replay，`action_count/progress/score` 不变。
- 不带幂等键失败。
- 结算排名和 `cooperationBonusMoney` 不因重复请求增加。

修复记录:
- 修改文件: `server/src/taoyuanActivityRoomRuntime.js`、`server/src/routes/api.js`、`server/scripts/qa-activity-room-reconnect-idempotency.mjs`、`server/scripts/qa-online-smoke.mjs`、`server/package.json`、`taoyuan-main/src/utils/festivalRoomApi.ts`、`taoyuan-main/src/utils/expeditionRoomApi.ts`、`docs/online/08-room-state-machine.md`。
- 动作接口现在要求 `idempotency_key`；服务端按 `roomId + member + idempotency_key` 将首次动作结果写入房间 `action_receipts`，同 key 重放返回 `idempotency_replayed: true` 和首次 `action_receipt`，不重复累计贡献、房间进度或分数。
- 同一幂等键换动作返回 `409/TAOYUAN_ACTIVITY_ROOM_ACTION_IDEMPOTENCY_CONFLICT`，缺 key 返回 `400/TAOYUAN_ACTIVITY_ROOM_ACTION_IDEMPOTENCY_REQUIRED`；动作日志和实时事件附带 `idempotency_key`/replay 标记。
- 前端节会/远征房间 API util 每次动作调用生成并发送 `idempotency_key`，动作响应类型补入 `idempotency_replayed` 和 `action_receipt`；现有 store 调用保持兼容。
- `qa-online-smoke` 中节会/远征房间所有动作 POST 已同步携带幂等键。
- 验证命令: `npm --prefix server run qa:activity-room-reconnect-idempotency`，结果通过，覆盖 `expedition_roles` 的 `assign_scout` 缺 key 拒绝、同 key replay 不改变 `action_count/progress/score`、同 key 换动作 409、settle 后 `cooperation_bonus_money` 未被 replay 放大。
- 验证命令: `npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过。
- 验证命令: `npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`，结果通过，确认本轮改动未破坏已完成的存档保护和协作委托链路。
- 验证命令: `node --check server/src/taoyuanActivityRoomRuntime.js`、`node --check server/src/routes/api.js`、`node --check server/scripts/qa-activity-room-reconnect-idempotency.mjs`、`node --check server/scripts/qa-online-smoke.mjs`，结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过，保留既有 3 个 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 本轮复验: 2026-06-06 运行 `npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过，缺 key 拒绝、同 key replay 和冲突 409 仍有效。
- 追加复验: 2026-06-06 运行 `npm --prefix server run qa:activity-room-reconnect-idempotency`，结果通过，远征房间动作缺 key 拒绝、同 key replay 不重复累计贡献、同 key 换动作 409 仍有效。
- 追加复验: 2026-06-06 运行 `npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；节会/远征动作携带 `idempotency_key` 的在线主路径和实时事件路径未回归。
- 追加复验: 2026-06-06 运行 `npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；动作缺 key 拒绝、同 key replay 不重复累计贡献、同 key 换动作 409、在线主流程和实时事件路径均未回归。
- 完成度复核: 2026-06-06 修改文件仅 `0605修复todo.md`；运行 `npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；动作缺 key 拒绝、同 key replay 不重复累计贡献、同 key 换动作 409、在线主流程和实时事件路径均未回归。
- 并行改动后复验: 2026-06-06 运行 `npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过，动作缺 key 拒绝、同 key replay 不重复累计贡献、同 key 换动作 409、在线主流程和实时事件路径仍有效。
- 收口复验: 2026-06-06 运行 `npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过；动作缺 key 拒绝、同 key replay 不重复累计贡献、同 key 换动作 409、在线主流程和实时事件路径仍有效。
- 巡检复验: 2026-06-06 运行 `npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；房间动作缺 key 拒绝、同 key replay 返回首次结果且不重复累计贡献、同 key 换动作 409 仍有效。
- 连续巡检复验: 2026-06-06 运行 `npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；房间动作缺 key 拒绝、同 key replay 返回首次结果且不重复累计贡献、同 key 换动作 409 仍有效。
- 在线奖励房间巡检: 2026-06-06 运行 `npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；房间动作缺 key 拒绝、同 key replay 返回首次结果且不重复累计贡献、同 key 换动作 409、在线主流程和实时事件路径仍有效。
- 房间 CAS 复验: 2026-06-06 运行 `npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix taoyuan-main run check`、`node --check server/src/routes/api.js`、`node --check server/src/taoyuanActivityRoomRuntime.js`，结果通过；房间动作缺 key 返回 400、同 key replay 返回首次结果且不重复累计 `action_count/progress/score`、同 key 换动作返回 409，在线主流程、实时事件和协作委托奖励链路仍有效，`online-smoke` 仅输出内容审核拒绝的预期错误日志。
- 房间幂等巡检: 2026-06-06 当前 `server/src/routes/api.js`、`server/src/taoyuanActivityRoomRuntime.js` 存在并行内容审核/治理 diff；运行 `node --check server/src/taoyuanActivityRoomRuntime.js`、`node --check server/src/routes/api.js`、`node --check server/scripts/qa-activity-room-reconnect-idempotency.mjs`、`node --check server/scripts/qa-online-smoke.mjs`、`node --check server/scripts/qa-realtime-smoke.mjs`、`npm --prefix server run qa:activity-room-reconnect-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix taoyuan-main run check`，结果通过；房间动作缺 key 返回 400、同 key replay 返回首次结果且不重复累计 `action_count/progress/score`、同 key 换动作返回 409，在线主流程、实时事件、协作委托奖励链路和坏档保护仍有效，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 本轮续验: 2026-06-06 当前工作树仍有大量并行 diff；运行 `npm --prefix server run qa:realtime-smoke`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix taoyuan-main run check`，结果通过；房间动作实时事件路径、协作委托奖励幂等链路、坏档保护和前端房间动作协议类型仍有效，`check` 仅保留既有 3 条 warning。
- 遗留风险: 前端当前为每次点击生成新 key，服务端可防止同 key 网络重放；如果后续需要“断网后同一点击自动重试”跨页面恢复，需要在前端为房间动作再接入持久化 pending key。

### T14 合并修复重复物品 ID 和资源 manifest 校验

- [x] 状态: 已完成

严重度: P1
来源分区: UI 玩法反馈、配置资源数据表

涉及文件:
- `taoyuan-main/src/data/items.ts`
- `taoyuan-main/src/data/crops.ts`
- `taoyuan-main/src/data/fruitTrees.ts`
- `taoyuan-main/src/data/processing.ts`
- `taoyuan-main/src/data/hiddenNpcs.ts`
- `taoyuan-main/public/item/item-icon-qa-report.json`
- `taoyuan-main/public/item/item-icon-manifest.json`
- 相关任务、商店、掉落、图鉴、食谱和存档迁移引用

问题:
- `ITEMS` 中存在重复 ID，`getItemById()` 使用 `find()`，固定返回先出现的定义。
- 高影响重复包括 `osmanthus_tea`、`dragon_pearl`、`lychee`、`persimmon`、`mulberry`。
- 结果会串到错误名称、分类、售价、可食用状态、图标和任务/结缘消耗。

修复要求:
- 将 `ItemDef.id` 作为运行时全局唯一约束，构建或 QA 脚本发现重复时失败。
- 拆分冲突 ID，并补旧档迁移表。优先处理加工产物和结缘信物。
- 重新生成 item manifest，并保证 `item-icon-qa-report.json` 的运行时 `duplicateIds` 为空。
- 若设计上确实共用同一 ID，只能保留一份 `ItemDef`，其他差异通过来源字段或 resolver 表达。

验收:
- `ITEMS.map(id)` 无重复。
- `osmanthus_tea`、`dragon_pearl`、`lychee`、`persimmon`、`mulberry` 解析到预期 category、sellPrice、edible 和图标。
- 回归茶机、炼器、果树日结、采集/野树种植、隐藏 NPC 结缘、出货箱估价和任务交付。
- 确认旧档中已有歧义 ID 时不会丢物品、断任务或断图鉴。

修复记录:
- 修改文件: `taoyuan-main/src/data/items.ts`、`taoyuan-main/src/data/processing.ts`、`taoyuan-main/src/data/fruitTrees.ts`、`taoyuan-main/src/data/forage.ts`、`taoyuan-main/src/data/wildTrees.ts`、`taoyuan-main/src/data/hiddenNpcs.ts`、`taoyuan-main/src/data/achievements.ts`、`taoyuan-main/src/data/quests.ts`、`taoyuan-main/src/data/mysteryBoxes.ts`、`taoyuan-main/src/data/npcWorld.ts`、`taoyuan-main/src/data/rewardTickets.ts`、`taoyuan-main/src/stores/useQuestStore.ts`、`taoyuan-main/src/stores/useAchievementStore.ts`、`taoyuan-main/src/stores/useHomeStore.ts`、`taoyuan-main/src/stores/useNpcStore.ts`、`taoyuan-main/scripts/prepare-item-icons.py`、`taoyuan-main/scripts/qa-item-data-guards.mjs`、`taoyuan-main/package.json`。
- 修复口径: 保留作物旧 ID `osmanthus_tea`、`dragon_pearl`、`lychee`、`persimmon`、`mulberry`，将非作物冲突项拆为 `processed_osmanthus_tea`、`spirit_dragon_pearl`、`tree_lychee`、`tree_persimmon`、`wild_mulberry`；茶机、仙灵炉、果树、觅食、野树种植、龙灵结缘和明确茶饮奖励/组合引用已同步到新 ID。
- 旧档兼容: `items.ts` 新增 `LEGACY_AMBIGUOUS_ITEM_ID_COMPATIBILITY` 和 `migrateLegacyItemId()`；通用背包旧歧义栈按旧运行时作物含义保留，任务奖励、茶饮组合和祠堂提交等可判定上下文迁移到加工茶新 ID，龙灵结缘配置迁移到信物新 ID。
- 资源 manifest: `prepare-item-icons.py` 新增新 ID 图标覆写和 `runtimeDuplicateIds` 失败条件；`prepare:item-icons` 已重新生成 `item-icon-manifest.json` / `item-icon-qa-report.json`，报告中 `runtimeDuplicateIds: 0`，剩余源候选重复仅为已被运行时过滤的 `ancient_seed`、`hanhai_cactus_seed`、`hanhai_date_seed`。
- 验证命令: `npm --prefix taoyuan-main run prepare:item-icons`，结果通过，`mappedItemIds=1530`、`candidateItemIds=1530`、`runtimeDuplicateIds=0`。
- 验证命令: `npm --prefix taoyuan-main run qa:item-data-guards`，结果通过，覆盖 `ITEMS` 无重复、加工茶/龙灵信物/果树/桑葚拆分解析、迁移 helper、manifest 新 ID 映射。
- 验证命令: `npm exec eslint -- src/data/items.ts src/data/fruitTrees.ts src/data/forage.ts src/data/wildTrees.ts src/data/processing.ts src/data/hiddenNpcs.ts src/data/achievements.ts src/data/quests.ts src/data/mysteryBoxes.ts src/data/npcWorld.ts src/data/rewardTickets.ts src/stores/useQuestStore.ts src/stores/useAchievementStore.ts src/stores/useHomeStore.ts src/stores/useNpcStore.ts scripts/qa-item-data-guards.mjs`（工作目录 `taoyuan-main`），结果通过；包含 `scripts/prepare-item-icons.cjs` 的首次目标 lint 被该既有未改文件缺少 Node globals 阻断，已改用本轮实际修改文件验证。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过，保留既有 3 条 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 验证命令: `npm --prefix taoyuan-main run qa:crop-use-entry-guard`，结果通过。
- 遗留风险: 通用背包旧档中的歧义 ID 没有来源字段，无法可靠区分当时来自作物、加工、果树或采集；本轮按旧运行时 `getItemById()` 的作物解析保留，不做无依据的全量拆分补偿。

### T15 特殊订单最终阶段失败必须完整回滚阶段状态

- [x] 状态: 已完成

严重度: P1
来源分区: UI 玩法反馈

涉及文件:
- `taoyuan-main/src/stores/useQuestStore.ts`
- `taoyuan-main/src/views/game/QuestView.vue`

问题:
- 最终阶段提交先把阶段写成 completed、`rewardClaimed: true` 并追加历史。
- 最终整单物品奖励容量检查在之后执行。
- 容量失败只回滚背包和鱼塘，不回滚 `quest.orderProgressState`。

修复要求:
- 最终阶段写入 `orderProgressState` 前先完成最终奖励容量检查。
- 或把 `quest.orderProgressState`、`activeQuests`、回执和历史纳入事务快照，失败时完整回滚。
- `rewardClaimed` 只能在整单奖励成功落地后写入。

验收:
- 构造最终阶段特殊订单，主背包和临时背包无法接收最终奖励。
- 点击提交后，`stageProgress`、`stageHistory`、`completedStageIds` 与点击前一致。
- 整理空间后再次提交，只产生一条最终阶段历史和一次奖励。

修复记录:
- 修改文件: `taoyuan-main/src/stores/useQuestStore.ts`、`taoyuan-main/scripts/qa-quest-guards.mjs`、`taoyuan-main/package.json`。
- 修复口径: 多阶段特殊订单最终阶段提交时先暂存最终阶段进度，不再立即写回 `quest.orderProgressState`；整单奖励容量检查失败会回滚背包、鱼塘、活跃任务、完成历史和特殊订单回执；最终阶段 `rewardClaimed` 与完成态在奖励结算落地后才写入。
- 验证命令: `npm --prefix taoyuan-main run qa:quest-guards`，结果通过，覆盖最终阶段满主背包/满临时背包导致整单奖励无法接收时，`stageProgress`、`stageHistory`、`completedStageIds`、交付物、完成历史和回执均保持提交前状态；整理空间后重提成功，铜钱、物品奖励、完成历史和回执各只产生一次。
- 验证命令: `npm exec eslint -- src/stores/useQuestStore.ts scripts/qa-quest-guards.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过；保留既有 3 条 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 遗留风险: 本轮未改 `QuestView.vue`，因为缺陷根因在 store 事务顺序；未运行 `test:e2e` 和 `qa:mobile-ui-smoke`，本任务无 UI 结构改动，已用 store 级固定状态回归覆盖验收。

### T16 额度导入/导出必须增加交易幂等收据

- [x] 状态: 已完成

严重度: P1
来源分区: 经济商店货币

涉及文件:
- `taoyuan-main/src/utils/quotaExchangeApi.ts`
- `taoyuan-main/src/views/game/WalletView.vue`
- `server/src/routes/api.js`
- `server/src/taoyuanHall.js`
- `server/src/db.js`

问题:
- 导入和导出 POST body 只有 `{ money }`。
- 服务端成功执行后若响应丢失，重试会被当作第二笔交易。
- 导入会重复扣账号 quota 并重复加铜钱；导出会重复扣铜钱并重复加 quota。

修复要求:
- 前端每次兑换生成并持久化 `idempotency_key`。
- 请求体携带 `money`、方向、服务端槽位或存档修订号、幂等键。
- 服务端按 `username + direction + idempotency_key` 写兑换收据。
- 同键重放返回原结果，不再次调用扣/加额度、改铜钱、更新日用量。
- 收据覆盖成功、失败已回滚、补偿中三类状态。

验收:
- 模拟服务端成功但客户端收不到响应，重放同一导入或导出请求。
- quota、铜钱、日用量只变化一次。
- 换新幂等键才允许第二笔交易。

修复记录:
- 修改文件: `server/src/routes/api.js`、`server/scripts/qa-transaction-idempotency.mjs`、`server/package.json`、`taoyuan-main/src/utils/quotaExchangeApi.ts`、`taoyuan-main/src/utils/transactionIdempotency.ts`、`taoyuan-main/src/views/game/WalletView.vue`。
- 服务端导入/导出请求现在要求 `direction`、`active_save_slot` 和 `idempotency_key`，按 `username + direction + idempotency_key` 写入 `__transaction_receipts`，同键成功重放返回第一次响应并标记 `idempotency_replayed`，不重复扣加 quota、铜钱和日用量；槽位变化返回 409 `TAOYUAN_EXCHANGE_SAVE_SLOT_STALE`。
- 前端额度兑换 util 生成账号作用域持久化幂等键，成功后清理；网络失败保留 key 用于重试，业务失败已回滚时释放 key。
- 验证命令: `npm --prefix server run qa:transaction-idempotency`，结果通过，覆盖导入/导出缺少幂等键拒绝、同 key 重放不重复变更 quota/铜钱/日用量、换新 key 可执行第二笔。
- 验证命令: `npm --prefix server run qa:online-smoke`，结果通过，覆盖在线交换相关主流程；过程中出现内容审核拒绝的预期错误日志但脚本退出码为 0。
- 验证命令: `npm --prefix server run qa:realtime-smoke`，结果通过。
- 验证命令: `npm --prefix server run qa:save-corruption-guard`，结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过，保留既有 3 条 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 验证命令: `npm exec eslint -- src/utils/quotaExchangeApi.ts src/utils/weeklyExchangeApi.ts src/utils/festivalStallApi.ts src/utils/transactionIdempotency.ts src/views/game/WalletView.vue`（工作目录 `taoyuan-main`），结果通过。
- 本轮复验: 2026-06-06 运行 `npm --prefix server run qa:transaction-idempotency`、`npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`、`npm --prefix taoyuan-main run check`，结果通过，额度交易收据和前端 CAS base 对齐仍有效。
- 追加复验: 2026-06-06 运行 `npm --prefix server run qa:transaction-idempotency`，结果通过，额度导入/导出同 key 重放仍不会重复变更 quota、铜钱或日用量。
- 追加复验: 2026-06-06 运行 `npm --prefix server run qa:transaction-idempotency`、`npm --prefix taoyuan-main run check`，结果通过；额度导入/导出缺 key 拒绝、同 key 重放不重复变更仍有效，`check` 仅保留既有 3 条 warning。
- 本轮复验: 2026-06-06 运行 `npm --prefix server run qa:transaction-idempotency`、`npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；额度交易收据、前端 CAS base 对齐、在线主流程和实时链路均未回归，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 完成度复核: 2026-06-06 修改文件仅 `0605修复todo.md`；运行 `npm --prefix server run qa:transaction-idempotency`、`npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；额度导入/导出缺 key 拒绝、同 key 重放不重复变更、前端 CAS base 对齐和实时通知路径均未回归。
- 并行改动后复验: 2026-06-06 运行 `npm --prefix server run qa:transaction-idempotency`、`npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过，额度导入/导出缺 key 拒绝、同 key 重放不重复变更、前端 CAS base 对齐和实时通知路径仍有效。
- 收口复验: 2026-06-06 运行 `npm --prefix server run qa:transaction-idempotency`、`npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过；额度导入/导出缺 key 拒绝、同 key 重放不重复变更、前端 CAS base 对齐和实时通知路径仍有效。
- 巡检复验: 2026-06-06 运行 `npm --prefix server run qa:transaction-idempotency`、`npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；额度导入/导出交易收据、同 key 重放不重复变更、前端 CAS base 对齐和实时通知路径仍有效。
- 连续巡检复验: 2026-06-06 运行 `npm --prefix server run qa:transaction-idempotency`、`npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；额度导入/导出交易收据、同 key 重放不重复变更、前端 CAS base 对齐和实时通知路径仍有效。
- 存档交易巡检: 2026-06-06 运行 `npm --prefix server run qa:transaction-idempotency`、`npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；额度导入/导出缺 key 拒绝、同 key 重放不重复变更、服务端已落盘后的本地回滚禁用、前端 CAS base 对齐和实时通知路径仍有效。
- 交易幂等复验: 2026-06-06 运行 `npm --prefix server run qa:transaction-idempotency`、`npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；额度导入/导出缺 key 拒绝、同 key 重放不重复扣加 quota/铜钱/日用量、服务端已落盘后的本地回滚禁用、前端 CAS base 对齐、在线主流程和实时链路仍有效，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 交易收据巡检: 2026-06-06 当前 `server/src/routes/api.js`、`server/src/taoyuanHall.js` 存在并行 diff；运行 `node --check server/src/routes/api.js`、`node --check server/src/taoyuanHall.js`、`node --check server/scripts/qa-transaction-idempotency.mjs`、`node --check taoyuan-main/scripts/qa-quota-exchange-save-consistency.mjs`、`npm --prefix server run qa:transaction-idempotency`、`npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；额度导入/导出缺 key 拒绝、同 key 重放不重复扣加 quota/铜钱/日用量、服务端已落盘后的本地回滚禁用、前端 CAS base 对齐、在线主流程和实时链路仍有效；`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 交易回归审计: 2026-06-06 修改文件仅 `0605修复todo.md`；当前 `server/src/routes/api.js`、`server/src/taoyuanHall.js` 存在并行 diff；运行 `node --check server/src/routes/api.js`、`node --check server/src/taoyuanHall.js`、`node --check server/scripts/qa-transaction-idempotency.mjs`、`node --check taoyuan-main/scripts/qa-quota-exchange-save-consistency.mjs`、`npm --prefix server run qa:transaction-idempotency`、`npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；额度导入/导出缺 key 拒绝、同 key 重放不重复扣加 quota/铜钱/日用量、收据落盘、服务端槽位 stale 与前端 CAS base 对齐仍有效，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 遗留风险: 服务端没有可用 ESLint 配置，`npm exec eslint -- ...` 在 `server` 下会临时安装 ESLint 10 并因缺少 `eslint.config.*` 失败；已用 `node --check server/src/routes/api.js` 和新增/既有 QA 覆盖本轮服务端改动。

## P2 体验、数据边界与性能

### T17 存档加载失败必须给出结构化反馈

- [x] 状态: 已完成

严重度: P2
来源分区: 核心运行时流程

涉及文件:
- `taoyuan-main/src/stores/useSaveStore.ts`
- `taoyuan-main/src/views/MainMenu.vue`

修复要求:
- `decrypt()`、`parseSaveData()`、`loadFromSlot()` 的失败结果应保留可展示原因。
- `loadFromSlot()` 返回结构化结果，或写入 `lastLoadErrorMessage`。
- 主菜单失败分支展示明确错误，例如存档损坏、版本不兼容、服务端槽位切换失败。
- 失败时保留 store 回滚，不要静默 no-op。

验收:
- 非法密文、旧字段迁移失败、mock `setServerActiveSlot` 抛错三类场景均有错误提示。
- 当前运行时 store 保持原状。

修复记录:
- 修改文件: `taoyuan-main/src/stores/useSaveStore.ts`、`taoyuan-main/src/views/MainMenu.vue`、`taoyuan-main/scripts/qa-save-load-feedback.mjs`。
- 修复口径: `loadFromSlot()` 新增结构化 `lastLoadError` / `lastLoadErrorMessage`，覆盖无效槽位、未同步副本、空槽、解密失败、JSON 解析失败、迁移失败、结构不兼容、反序列化失败、服务端读取失败、服务端 active-slot 切换失败和回滚失败；主菜单加载失败分支会展示并记录该错误，服务端 active-slot 失败仍先恢复原运行时 store、槽位和会话状态。
- 验证命令: `node --check taoyuan-main/scripts/qa-save-load-feedback.mjs`，结果通过。
- 验证命令: `node taoyuan-main/scripts/qa-save-load-feedback.mjs`，结果通过，覆盖解密/解析/迁移/服务端 active-slot/回滚错误路径的结构化错误和主菜单反馈接入。
- 验证命令: `npm exec eslint -- src/stores/useSaveStore.ts src/views/MainMenu.vue scripts/qa-save-load-feedback.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过；保留既有 3 条 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 本轮复验: 2026-06-06 运行 `node taoyuan-main/scripts/qa-save-load-feedback.mjs`、`npm --prefix taoyuan-main run check`，结果通过，存档加载失败结构化反馈和主菜单接入仍有效。
- 追加复验: 2026-06-06 运行 `node --check taoyuan-main/scripts/qa-save-load-feedback.mjs`、`node taoyuan-main/scripts/qa-save-load-feedback.mjs`、`npm --prefix taoyuan-main run check`，结果通过；结构化 `lastLoadError/lastLoadErrorMessage`、主菜单错误提示和日志接入仍有效，`check` 仅保留既有 3 条 warning。
- 完成度复核: 2026-06-06 修改文件仅 `0605修复todo.md`；运行 `node taoyuan-main/scripts/qa-save-load-feedback.mjs`、`npm --prefix taoyuan-main run check`，结果通过；结构化 `lastLoadError/lastLoadErrorMessage`、主菜单错误提示和日志接入仍有效。
- 并行改动后复验: 2026-06-06 当前 `taoyuan-main/src/views/MainMenu.vue` 存在并行 diff；运行 `node taoyuan-main/scripts/qa-save-load-feedback.mjs`、`npm --prefix taoyuan-main run check`，结果通过，结构化加载错误、主菜单提示和日志接入仍有效。
- 收口复验: 2026-06-06 运行 `node taoyuan-main/scripts/qa-save-load-feedback.mjs`、`npm exec eslint -- src/views/MainMenu.vue src/views/game/ShopView.vue scripts/qa-save-load-feedback.mjs scripts/qa-quota-exchange-save-consistency.mjs`（工作目录 `taoyuan-main`），结果通过；结构化加载错误、主菜单提示和日志接入仍有效。验证限制: `npm --prefix taoyuan-main run check` 被未跟踪 `src/utils/aiAssistantPendingStages.ts` 的 `TS2322` 阻断。
- 巡检复验: 2026-06-06 运行 `node taoyuan-main/scripts/qa-save-load-feedback.mjs`、`npm --prefix taoyuan-main run check`，结果通过；结构化 `lastLoadError/lastLoadErrorMessage`、主菜单失败提示和日志接入仍有效，`check` 仅保留既有 3 条 warning。
- 连续巡检复验: 2026-06-06 当前 `taoyuan-main/src/views/MainMenu.vue` 仍有并行 diff；运行 `node taoyuan-main/scripts/qa-save-load-feedback.mjs`、`npm --prefix taoyuan-main run check`，结果通过；结构化加载错误、主菜单失败提示和日志接入仍有效，`check` 仅保留既有 3 条 warning。
- 结构化反馈巡检: 2026-06-06 运行 `node taoyuan-main/scripts/qa-save-load-feedback.mjs`、`npm --prefix taoyuan-main run check`，结果通过；结构化加载错误、主菜单失败提示、服务端 active-slot 失败回滚和日志接入仍有效，`check` 仅保留既有 3 条 warning。
- 并行 lint 阻断复验: 2026-06-06 当前 `taoyuan-main/src/views/MainMenu.vue` 有并行管理入口 UI diff，未触碰加载失败分支；运行 `node --check taoyuan-main/scripts/qa-save-load-feedback.mjs`、`node taoyuan-main/scripts/qa-save-load-feedback.mjs`、`npm --prefix taoyuan-main run type-check`、`npm exec eslint -- src/stores/useSaveStore.ts src/views/MainMenu.vue scripts/qa-save-load-feedback.mjs`（工作目录 `taoyuan-main`）、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过；结构化 `lastLoadError/lastLoadErrorMessage`、主菜单失败提示、服务端 active-slot 失败回滚、坏 raw 拒绝、CAS stale 409、在线主流程、协作委托和实时通知路径仍有效。验证限制: `npm --prefix taoyuan-main run check` 当前被并行文件 `taoyuan-main/src/utils/safeMarkdown.ts` 的 `no-control-regex` 阻断，非 T17 目标文件，已用目标 lint 与 `type-check` 替代覆盖。
- 结构化反馈复验: 2026-06-06 当前 `taoyuan-main/src/views/MainMenu.vue`、`taoyuan-main/package.json` 仍有并行 diff，未触碰加载失败分支；运行 `node --check taoyuan-main/scripts/qa-save-load-feedback.mjs`、`node taoyuan-main/scripts/qa-save-load-feedback.mjs`、`npm --prefix taoyuan-main run type-check`、`npm exec eslint -- src/stores/useSaveStore.ts src/views/MainMenu.vue scripts/qa-save-load-feedback.mjs`（工作目录 `taoyuan-main`）、`npm --prefix taoyuan-main run check`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过；结构化 `lastLoadError/lastLoadErrorMessage`、主菜单失败浮字和日志、服务端 active-slot 失败回滚、坏 raw 拒绝、CAS stale 409、在线主流程、协作委托和实时通知路径仍有效，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- QA 强化复验: 2026-06-07 修改文件: `taoyuan-main/scripts/qa-save-load-feedback.mjs`、`0605修复todo.md`；`qa-save-load-feedback` 从源码字符串守卫升级为 Pinia 动态故障注入，覆盖非法密文触发 `decrypt_failed` 且保留当前运行时、迁移故障注入触发 `migration_failed` 且保留当前运行时、服务端 `setServerActiveSlot()` 抛错触发 `server_active_slot_failed` 并保留 detail，同时回滚 `playerStore.money`、`activeSlot`、`runtimeSessionSlot`；脚本仍静态校验主菜单读取 `lastLoadErrorMessage`、展示 danger 浮字并写入日志。验证命令: `node --check taoyuan-main/scripts/qa-save-load-feedback.mjs`、`node taoyuan-main/scripts/qa-save-load-feedback.mjs`、`npm exec eslint -- scripts/qa-save-load-feedback.mjs src/stores/useSaveStore.ts src/views/MainMenu.vue`（工作目录 `taoyuan-main`）、`npm --prefix taoyuan-main run check`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:save-cas-guard`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过；`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 遗留风险: 本轮未新增浏览器点击级 E2E；已用结构化 store 状态、主菜单反馈分支和全量类型/ lint 作为验证证据。

### T18 吸血回血基数按有效伤害截断

- [x] 状态: 已完成

严重度: P2
来源分区: 战斗伤害结算

涉及文件:
- `taoyuan-main/src/stores/useMiningStore.ts`
- `taoyuan-main/src/utils/combatRuntime.ts`

修复要求:
- 吸血基数改为不超过怪物本次受击前剩余 HP。
- 可先使用 `Math.min(monsterHpBefore, Math.max(0, attackOutcome.totalDamage))`。
- 更理想方案是主段和追击段分别按实际扣血计算。

验收:
- 怪物剩余 HP=1、玩家伤害=100、吸血=10%。
- 回血按 1 点有效伤害取整，不得按 100 点总伤害回 10。

修复记录:
- 修改文件: `taoyuan-main/src/utils/combatRuntime.ts`、`taoyuan-main/src/stores/useMiningStore.ts`、`taoyuan-main/scripts/qa-combat-flow-guards.mjs`、`taoyuan-main/package.json`。
- 验证命令: `npm --prefix taoyuan-main run qa:combat-flow-guards`，结果通过，覆盖固定 RNG 总伤害溢出、`getEffectiveDamage(1, totalDamage)` 截断和吸血武器击杀 1 HP 怪不按溢出回血。
- 验证命令: `npm exec eslint -- src/stores/useMiningStore.ts src/utils/combatRuntime.ts src/stores/useRegionMapStore.ts src/views/game/MiningView.vue scripts/qa-combat-flow-guards.mjs`，结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，已运行但被非本轮文件 `src/components/game/AdminOnlineGovernancePanel.vue` 既有类型错误阻断。
- 遗留风险: 全量 `check` 尚未通过，阻塞点不在本任务改动范围。

### T19 矿洞战斗浮字不得再从文案反解析单段伤害

- [x] 状态: 已完成

严重度: P2
来源分区: 战斗伤害结算

涉及文件:
- `taoyuan-main/src/utils/combatRuntime.ts`
- `taoyuan-main/src/stores/useMiningStore.ts`
- `taoyuan-main/src/views/game/MiningView.vue`

修复要求:
- 优先让战斗动作返回结构化 `damage/extraDamage/totalDamage/isCrit`。
- 短期修复可解析所有 `造成(\d+)点伤害` 并求和，或为追击显示第二个浮字。
- UI 浮字、日志、怪物 HP 扣减必须一致。

验收:
- 固定 RNG 触发匕首追击。
- 怪物 HP 扣减、日志、浮字显示同一总伤害或清晰显示主段和追击段。

修复记录:
- 修改文件: `taoyuan-main/src/stores/useMiningStore.ts`、`taoyuan-main/src/views/game/MiningView.vue`、`taoyuan-main/scripts/qa-combat-flow-guards.mjs`、`taoyuan-main/package.json`。
- 验证命令: `npm --prefix taoyuan-main run qa:combat-flow-guards`，结果通过，覆盖固定 RNG 匕首追击，断言结构化总伤害等于主段+追击段，怪物 HP 扣减与结构化总伤害一致。
- 验证命令: `npm exec eslint -- src/stores/useMiningStore.ts src/utils/combatRuntime.ts src/stores/useRegionMapStore.ts src/views/game/MiningView.vue scripts/qa-combat-flow-guards.mjs`，结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，已运行但被非本轮文件 `src/components/game/AdminOnlineGovernancePanel.vue` 既有类型错误阻断。
- 遗留风险: `MiningView.vue` 存在本轮开始前的物品图标改动，已保留；全量 `check` 尚未通过，阻塞点不在本任务改动范围。

### T20 钓鱼宝箱幸运料理按实际 Buff 值结算

- [x] 状态: 已完成

严重度: P2
来源分区: 技能 Buff 状态

涉及文件:
- `taoyuan-main/src/stores/useFishingStore.ts`
- `taoyuan-main/src/views/game/ForageView.vue`
- `taoyuan-main/src/data/recipes.ts`

修复要求:
- `rollTreasureChest()` 使用与选鱼和采集一致的 `activeBuff.value / 100`。
- 确认 `幸运+2` 这类无百分号描述是否解释为 2%，必要时拆分数值类型或修正文案。

验收:
- `activeBuff={ type:'luck', value:25 }` 时宝箱概率增量为 0.25。
- `value:5` 时增量为 0.05。
- 选鱼权重、采集概率、宝箱概率解释一致。

修复记录:
- 修改文件: `taoyuan-main/src/stores/useFishingStore.ts`、`taoyuan-main/src/data/recipes.ts`、`taoyuan-main/scripts/qa-combat-flow-guards.mjs`。
- 修复口径: `rollTreasureChest()` 改为复用 `getFishingTreasureChestChance()`，幸运料理按 `activeBuff.value / 100` 增加宝箱概率；`幸运+2`、`幸运+3` 料理文案改为 `+2%`、`+3%`，与选鱼权重和采集概率解释一致。
- 验证命令: `npm --prefix taoyuan-main run qa:combat-flow-guards`，结果通过，覆盖 `value:25` 宝箱概率增量 0.25、`value:5` 增量 0.05，并断言所有幸运料理文案包含实际百分比。
- 验证命令: `npm exec eslint -- src/stores/useFishingStore.ts src/data/recipes.ts src/stores/journeyBuild.ts scripts/qa-combat-flow-guards.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，已运行但被非本轮额度兑换改动阻断: `src/views/game/WalletView.vue` 调用 `importQuotaToTaoyuan/exportTaoyuanToQuota` 缺少 `quotaExchangeApi.ts` 新签名要求的第二个参数。
- 遗留风险: 本任务只统一 luck Buff 百分比口径，未调整幸运料理数值本身。

### T21 远征构筑首领抗压不得重复叠加已有值

- [x] 状态: 已完成

严重度: P2
来源分区: 技能 Buff 状态

涉及文件:
- `taoyuan-main/src/stores/journeyBuild.ts`
- `taoyuan-main/src/stores/useRegionMapStore.ts`

修复要求:
- `journeyBuild.ts` 中追加公会/徽章/防御来源时，patch 只能包含新增值。
- 不要把 `outcome.bossPressureResist` 再放入 `addOutcome()` patch。

验收:
- 无公会加成但已有 `bossPressureResist=0.18` 的快照，修复后仍为 0.18。
- 有公会加成时只增加公会贡献，不把已有值翻倍。

修复记录:
- 修改文件: `taoyuan-main/src/stores/journeyBuild.ts`、`taoyuan-main/scripts/qa-combat-flow-guards.mjs`。
- 修复口径: 公会攻击、矿洞徽章攻击和公会防御对首领抗压的贡献改为 `getJourneyGuildOutcomePatch()` 的新增 patch，再由 `applyJourneyGuildOutcomePatch()` 叠加；不再把已累计的 `outcome.bossPressureResist` 放入 patch。
- 验证命令: `npm --prefix taoyuan-main run qa:combat-flow-guards`，结果通过，覆盖已有 `bossPressureResist=0.18` 且无公会加成时保持 0.18，以及有公会加成时只增加公会/徽章/防御贡献。
- 验证命令: `npm exec eslint -- src/stores/useFishingStore.ts src/data/recipes.ts src/stores/journeyBuild.ts scripts/qa-combat-flow-guards.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，已运行但被非本轮额度兑换改动阻断: `src/views/game/WalletView.vue` 调用 `importQuotaToTaoyuan/exportTaoyuanToQuota` 缺少 `quotaExchangeApi.ts` 新签名要求的第二个参数。
- 遗留风险: 全量 `check` 尚未通过，阻塞点属于用户排除的额度兑换范围，不在本任务改动内。

### T22 读档时校验装备定义和武器索引

- [x] 状态: 已完成

严重度: P2
来源分区: 角色成长装备

涉及文件:
- `taoyuan-main/src/stores/useInventoryStore.ts`
- `taoyuan-main/src/stores/useSaveStore.ts`

修复要求:
- `deserialize()` 过滤或迁移不存在的武器、戒指、帽子、鞋子、饰物 defId。
- 武器附魔 id 不存在时置空或迁移。
- 修正所有装备索引，特别是 `equippedWeaponIndex` 越界。
- 当前装备被移除时清空槽位或回退到明确默认装备，并记录迁移日志。

验收:
- 导入存档包含 `ownedWeapons: []`、`equippedWeaponIndex: 3`、无效 `weapon.defId`、无效戒指 defId。
- 读档后索引有效，无未知装备处于装备态。
- `getWeaponAttack()`、`getEquipmentBonus()`、`activeSetBonuses` 与修复后的装备状态一致。

修复记录:
- 修改文件: `taoyuan-main/src/stores/useInventoryStore.ts`、`taoyuan-main/scripts/qa-equipment-guards.mjs`、`taoyuan-main/package.json`。
- 修复口径: `deserialize()` 过滤无效武器、戒指、帽子、鞋子定义，清空无效饰物；武器附魔不存在时置空；所有装备槽位按过滤后的数组重映射索引，空武器列表或当前武器被移除时回退到 `wooden_stick` 或第一把有效武器；新增 `equipmentMigrationLogs` 记录读档迁移。
- 验证命令: `npm --prefix taoyuan-main run qa:equipment-guards`，结果通过，覆盖空武器列表、越界 `equippedWeaponIndex`、无效武器 defId、无效附魔、无效戒指/帽子/鞋子/饰物和缺失激活方案，断言读档后索引有效、未知装备不处于装备态、攻击和装备加成与归一化结果一致。
- 验证命令: `npm exec eslint -- src/stores/useInventoryStore.ts scripts/qa-equipment-guards.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过；现有非本轮文件仍有 3 条 lint warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 验证命令: `npm --prefix taoyuan-main run qa:combat-flow-guards`，结果通过。
- 遗留风险: 本轮未改 `taoyuan-main/src/stores/useSaveStore.ts`，避免触碰并行的云存档改动；装备归一化入口已位于 `useInventoryStore.deserialize()`。

### T23 高频日志链路增加容量上限和服务端保留策略

- [x] 状态: 已完成

严重度: P2
来源分区: 玩法稳定性性能

涉及文件:
- `taoyuan-main/src/composables/useGameLog.ts`
- `taoyuan-main/src/views/game/MiningView.vue`
- `taoyuan-main/src/stores/useMiningStore.ts`
- `taoyuan-main/src/components/game/PlayerRecordCenterPanel.vue`
- `server/src/routes/api.js`
- `server/src/db.js`

修复要求:
- 给 `logHistory`、`gameplayLogQueue`、矿洞 `exploreLog/combatLog` 设置容量上限。
- 高频战斗/挖矿日志做合并、采样或按类别裁剪。
- `resetLogs/clearAllLogs` 同步清理或隔离当前存档的待上报队列。
- 上报失败使用最大重试条数、退避、丢弃或本地降级策略，并检查 `response.ok`。
- 服务端 batch 一次加载/一次写回，并按总量、用户、存档槽或 TTL 裁剪；MySQL 路径也要有保留策略。

验收:
- 模拟 5000 次矿洞 reveal/attack 和服务端离线。
- `logHistory`、`gameplayLogQueue`、`combatLog/exploreLog` 均不超过上限。
- 记录中心打开不卡顿。
- JSON fallback 下 100 条 batch 只写回一次且日志文件被裁剪。

修复记录:
- 修改文件: `taoyuan-main/src/composables/useGameLog.ts`、`taoyuan-main/src/stores/useMiningStore.ts`、`taoyuan-main/src/views/game/MiningView.vue`、`taoyuan-main/scripts/qa-gameplay-log-guards.mjs`、`taoyuan-main/scripts/qa-mining-animation-timers.mjs`、`taoyuan-main/package.json`。
- 跨域改动: `server/src/db.js`、`server/src/routes/api.js`、`server/scripts/qa-gameplay-log-retention.mjs`、`server/package.json`。
- 修复口径: `logHistory` 上限 1200、`gameplayLogQueue` 上限 600，短时间重复系统日志折叠为一条并记录 `repeat_count`；flush 检查 `response.ok`，失败最多重试 3 次并退避，`resetLogs/clearAllLogs` 同步清理待上报队列；矿洞 `combatLog/exploreLog` 各按 120 条同步裁剪；服务端新增批量玩法日志写入，JSON fallback 一次读写并按总量 5000、用户+存档槽 1200、TTL 30 天裁剪，MySQL 路径按 TTL 和总量保留。
- 验证命令: `node --check taoyuan-main/scripts/qa-gameplay-log-guards.mjs`、`node --check taoyuan-main/scripts/qa-mining-animation-timers.mjs`、`node --check server/scripts/qa-gameplay-log-retention.mjs`、`node --check server/src/db.js`、`node --check server/src/routes/api.js`，结果通过。
- 验证命令: `npm --prefix taoyuan-main run qa:gameplay-log-guards`，结果通过，覆盖历史/队列上限、重复折叠、失败重试丢弃、`resetLogs` 清队列、矿洞日志裁剪接入。
- 验证命令: `npm --prefix server run qa:gameplay-log-retention`，结果通过，覆盖 JSON fallback 下 100 条 batch 只写回一次，并按总量、用户+存档槽、TTL 裁剪。
- 验证命令: `npm --prefix taoyuan-main run qa:mining-animation-timers`，结果通过，确认 `MiningView.vue` 新增 `watch` 后未破坏 T24 timer 生命周期 QA。
- 验证命令: `npm exec eslint -- src/composables/useGameLog.ts src/stores/useMiningStore.ts src/views/game/MiningView.vue scripts/qa-gameplay-log-guards.mjs scripts/qa-mining-animation-timers.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过；保留既有 3 条 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 未运行: `npm --prefix taoyuan-main run prepare:item-icons`，本任务未改资源 manifest 或图标产物；`npm --prefix taoyuan-main run test:e2e` 与 `npm --prefix taoyuan-main run qa:mobile-ui-smoke`，本任务没有新增可视布局或路由交互，已用固定日志队列 QA、矿洞 timer QA、目标 lint、服务端 fallback QA 和全量 check 覆盖风险。
- 遗留风险: 记录中心打开不卡顿由日志历史上限和全量 check 间接覆盖，未新增浏览器性能计时；MySQL 路径覆盖到语法与保留 SQL，实际线上 MySQL 大表裁剪耗时仍需后续环境观察。

### T24 矿洞战斗动画 timeout 必须可清理、可归属

- [x] 状态: 已完成

严重度: P2
来源分区: 玩法稳定性性能

涉及文件:
- `taoyuan-main/src/views/game/MiningView.vue`

修复要求:
- 为 player/monster 动画、player/monster 浮字、combat lock 分别保存 timer id。
- 设置新动画前清理旧 timer。
- 回调内比较 key 或 token，只清理自己创建的状态。
- 在 `onUnmounted()` 中统一 `clearTimeout` 并清空动画、浮字、锁状态。

验收:
- fake timer 或组件测试连续两次 `showDamageFloat('monster', ...)`，间隔 400ms。
- 第二次浮字保持到自己的 800ms 周期结束。
- `handleCombat()` 后立即 unmount，不应有 pending timer 写 ref。

修复记录:
- 修改文件: `taoyuan-main/src/views/game/MiningView.vue`、`taoyuan-main/scripts/qa-mining-animation-timers.mjs`、`taoyuan-main/package.json`。
- 修复口径: `MiningView.vue` 为 player/monster 动画、player/monster 浮字、combat lock 分别保存 timer id 和归属 token；设置新动画/浮字/lock 前先清理旧 timer；浮字回调同时校验 token 和当前浮字 key；组件卸载时统一清理全部 timer 并清空动画、浮字和锁状态。
- 验证命令: `node --check taoyuan-main/scripts/qa-mining-animation-timers.mjs`，结果通过。
- 验证命令: `npm --prefix taoyuan-main run qa:mining-animation-timers`，结果通过，覆盖连续两次 monster 浮字间隔 400ms 时第二次浮字保留到自己的 800ms 周期结束，并覆盖 `clearAll()` 后 combat lock timer 不再写状态且无 pending timer。
- 验证命令: `npm exec eslint -- src/views/game/MiningView.vue scripts/qa-mining-animation-timers.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过；保留既有 3 条 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 未运行: `npm --prefix taoyuan-main run prepare:item-icons`，本任务未改物品图标或资源 manifest；`npm --prefix taoyuan-main run test:e2e` 和 `npm --prefix taoyuan-main run qa:mobile-ui-smoke`，本轮已用 fake timer QA、目标 lint 和全量 check 覆盖 timer 生命周期风险。
- 遗留风险: QA 为脚本级 fake timer 和静态接入检查，不是浏览器组件级 fake timer；若后续改动重命名内部函数，需同步更新 `qa:mining-animation-timers`。

### T25 任务奖励预览使用最终结算口径

- [x] 状态: 已完成

严重度: P2
来源分区: UI 玩法反馈

涉及文件:
- `taoyuan-main/src/views/game/QuestView.vue`
- `taoyuan-main/src/stores/useQuestStore.ts`

修复要求:
- 增加统一任务奖励预览 helper，复用 `submitQuest()` 的铜钱、好感、服务合同、村庄项目、特殊订单评分倍率计算。
- 动态评分无法提前确定时，展示基础值、预计倍率范围、已生效固定加成。

验收:
- 开启委托铜钱加成和服务合同后，任务详情金额与 `submitQuest()` 日志和玩家铜钱增量一致。
- 特殊订单覆盖 A/S 评分倍率。

修复记录:
- 修改文件: `taoyuan-main/src/stores/useQuestStore.ts`、`taoyuan-main/src/views/game/QuestView.vue`、`taoyuan-main/scripts/qa-quest-guards.mjs`、`taoyuan-main/package.json`。
- 修复口径: 新增 `getQuestRewardPreviewModel()` 作为统一奖励预览 helper，复用提交结算的村庄项目铜钱/好感加成、服务合同铜钱倍率与票券奖励、特殊订单评分倍率；任务列表、任务详情和移动端行动卡均展示预计最终奖励，特殊订单额外展示当前评分和评分倍率范围。
- 验证命令: `npm --prefix taoyuan-main run qa:quest-guards`，结果通过，覆盖普通委托在村庄铜钱加成 + 服务合同倍率下预览金额、提交日志和玩家铜钱增量一致，并覆盖特殊订单 S 档评分倍率预览与提交到账一致。
- 验证命令: `npm exec eslint -- src/stores/useQuestStore.ts src/views/game/QuestView.vue scripts/qa-quest-guards.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过；保留既有 3 条 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 验证命令: `npm --prefix taoyuan-main run qa:mobile-ui-smoke`，结果通过并写出 `docs/ui-smoke-2026-04-26/summary.json`；过程中 Vite 代理在线接口到 `127.0.0.1:4013` 出现连接拒绝日志，原因为本地服务端未启动，脚本退出码为 0。
- 遗留风险: 未运行 `npm --prefix taoyuan-main run test:e2e`，本轮已用 store 级固定状态验证结算一致性并用 mobile smoke 覆盖页面加载；未运行 `prepare:item-icons`，本任务未改物品图标或 manifest。

### T26 临时背包按钮状态按实际可移动数量计算

- [x] 状态: 已完成

严重度: P2
来源分区: UI 玩法反馈

涉及文件:
- `taoyuan-main/src/views/game/InventoryView.vue`
- `taoyuan-main/src/stores/useInventoryStore.ts`

修复要求:
- 增加 `canMoveFromTemp(index)`、`getMovableTempItemCount()` 或等价 helper。
- 完全不可移动时禁用按钮并显示背包已满。
- 部分可移动时文案改为“取回可合并部分”或显示预计数量。
- 真实移动数量、按钮状态、日志一致。

验收:
- 主背包满且无同类可合并: 按钮禁用。
- 主背包满但有同类未满栈: 允许部分取回，文案准确。
- 主背包有空格: 全部取回正常。

修复记录:
- 修改文件: `taoyuan-main/src/stores/useInventoryStore.ts`、`taoyuan-main/src/views/game/InventoryView.vue`、`taoyuan-main/scripts/qa-inventory-guards.mjs`、`taoyuan-main/package.json`。
- 修复口径: 新增 `getMovableTempItemCount(index?)` 和 `canMoveFromTemp(index)`，单格取回按主背包实际可合并/可新建栈数量预测；一键取回按实际倒序移动顺序模拟总可移动件数，避免多个临时格竞争同一空槽时高估。`moveAllFromTemp()` 返回真实移动件数；临时背包按钮在 0 可移动时禁用并显示“背包已满”，部分可移动时显示“取回可合并部分（x/y）”，日志同步使用真实件数。
- 验证命令: `npm --prefix taoyuan-main run qa:inventory-guards`，结果通过，覆盖主背包满且无同类可合并、满包但同类栈可部分合并、主背包有空格完整取回，以及一键取回竞争同一空槽时返回真实件数。
- 验证命令: `npm exec eslint -- src/stores/useInventoryStore.ts src/views/game/InventoryView.vue scripts/qa-inventory-guards.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过；保留既有 3 条 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 验证命令: `npm --prefix taoyuan-main run qa:mobile-ui-smoke`，结果通过并写出 `docs/ui-smoke-2026-04-26/summary.json`；过程中 Vite 代理在线接口到 `127.0.0.1:4013` 出现连接拒绝日志，原因为本地服务端未启动，脚本退出码为 0。
- 未运行: `npm --prefix taoyuan-main run prepare:item-icons`，本任务未改物品图标或资源 manifest；`npm --prefix taoyuan-main run test:e2e`，本轮已用固定 store 守卫覆盖三条验收并用 mobile smoke 覆盖页面加载。
- 遗留风险: `InventoryView.vue` 本轮开始前已有物品图标 UI 改动，已保留未回滚；本任务只调整临时背包容量预测、按钮状态和日志口径。

### T27 背包食用入口支持 HP-only 道具

- [x] 状态: 已完成

严重度: P2
来源分区: UI 玩法反馈

涉及文件:
- `taoyuan-main/src/data/items.ts`
- `taoyuan-main/src/views/game/InventoryView.vue`
- `taoyuan-main/src/stores/useMiningStore.ts`

修复要求:
- 背包详情只要 `staminaRestore > 0 || healthRestore > 0` 就展示恢复信息。
- `isEdible()` 和 `handleEat()` 接受 HP-only、体力-only、双恢复三类道具。
- 分别判断 HP 或体力是否已满，避免无效消耗。

验收:
- HP 未满、体力已满时打开 `combat_tonic`，显示 `+30HP` 和可用按钮。
- 使用后 HP 增加并消耗 1 个。
- HP 已满时按钮禁用或日志提示生命值已满。

修复记录:
- 修改文件: `taoyuan-main/src/views/game/InventoryView.vue`、`taoyuan-main/src/utils/inventoryUseRules.ts`、`taoyuan-main/scripts/qa-inventory-guards.mjs`。
- 修复口径: 普通背包详情恢复信息改为 `staminaRestore > 0 || healthRestore > 0` 即展示，HP-only 道具显示 `+30HP` 等 HP 预览；`isEdible()` 改为识别 HP-only、体力-only、双恢复三类恢复道具；食用按钮按实际可恢复目标禁用，HP-only 在生命值满时阻塞且不消耗，体力-only 在体力满时阻塞且不消耗，双恢复在任一目标未满时可用。非烹饪恢复道具统一走 `applyInventoryRecoveryItem()`，日志与实际恢复/消耗一致。
- 验证命令: `npm --prefix taoyuan-main run qa:inventory-guards`，结果通过，覆盖 `combat_tonic` 在 HP 未满且体力已满时显示/可用、使用后 HP 增加并消耗 1 个，HP 满时不消耗并提示生命值已满；同时覆盖 `stamina_elixir` 体力-only 满体力阻塞、未满体力可用，以及 `warriors_feast` 双恢复在体力满但 HP 未满时可用。
- 验证命令: `npm exec eslint -- src/views/game/InventoryView.vue src/utils/inventoryUseRules.ts scripts/qa-inventory-guards.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过；保留既有 3 条 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 验证命令: `npm --prefix taoyuan-main run qa:mobile-ui-smoke`，首次运行被本轮类型错误阻断，修复后重跑通过并写出 `docs/ui-smoke-2026-04-26/summary.json`；过程中 Vite 代理在线接口到 `127.0.0.1:4013` 出现连接拒绝日志，原因为本地服务端未启动，脚本退出码为 0。
- 未运行: `npm --prefix taoyuan-main run prepare:item-icons`，本任务未改物品图标或资源 manifest；`npm --prefix taoyuan-main run test:e2e`，本轮已用固定 store/规则守卫覆盖三条验收并用 mobile smoke 覆盖页面加载。
- 遗留风险: `InventoryView.vue` 本轮开始前已有物品图标 UI 改动，已保留未回滚；烹饪品仍委托 `cookingStore.eat()` 处理厨房、品质和 Buff 加成，本任务只在入口层补恢复目标可用性判断。

### T28 周常交换站和节庆摊位交易幂等

- [x] 状态: 已完成

严重度: P2
来源分区: 经济商店货币

涉及文件:
- `taoyuan-main/src/utils/weeklyExchangeApi.ts`
- `taoyuan-main/src/utils/festivalStallApi.ts`
- `taoyuan-main/src/stores/useWeeklyExchangeStore.ts`
- `taoyuan-main/src/stores/useFestivalStallStore.ts`
- `taoyuan-main/src/components/game/WeeklyExchangeStationPanel.vue`
- `taoyuan-main/src/components/game/FestivalStallPanel.vue`
- `server/src/routes/api.js`
- `server/src/taoyuanWeeklyExchangeStation.js`
- `server/src/taoyuanFestivalStall.js`

修复要求:
- 前端周常/节庆操作生成 `idempotency_key`。
- 服务端按 `username + source + offerId + idempotency_key` 记录处理结果。
- 同键重放返回原 `record/save_slot/offer`，不再次扣成本、不再次发奖励、不增加 `offer_claims`。
- 前端 `actionRunning` 只作为交互防抖，不作为经济一致性防线。

验收:
- 个人限购大于 1、库存充足的周常兑换项，第一次成功响应丢失后用同一 key 重试。
- 节庆摊位商品同样重试。
- 用户用量、全局库存、存档物资/铜钱和奖励记录都只变化一次。

修复记录:
- 修改文件: `server/src/routes/api.js`、`server/src/taoyuanWeeklyExchangeStation.js`、`server/src/taoyuanFestivalStall.js`、`server/scripts/qa-transaction-idempotency.mjs`、`server/scripts/qa-online-smoke.mjs`、`server/scripts/qa-realtime-smoke.mjs`、`server/package.json`、`taoyuan-main/src/utils/weeklyExchangeApi.ts`、`taoyuan-main/src/utils/festivalStallApi.ts`、`taoyuan-main/src/utils/transactionIdempotency.ts`。
- 周常交换站和节庆摊位 POST 现在要求 `idempotency_key`；服务端按 `username + source + offerId + idempotency_key` 在周状态的 `transaction_receipts` 中保存 pending/succeeded/failed_rolled_back/compensation_pending 收据，同 key 成功重放返回原 `record/save_slot/offer` 并抑制重复 realtime 通知。
- 周常/节庆前端 API util 生成账号作用域持久化交易 key，POST JSON body 携带 `idempotency_key`；现有 store/panel 继续调用 util，`actionRunning` 只保留为交互防抖。
- 调整周常/节庆保存失败路径: 玩家存档已落盘但交易 store 写入失败时尝试回滚；回滚失败时将收据标为 `compensation_pending`，避免把不确定状态误报为已回滚。
- 验证命令: `npm --prefix server run qa:transaction-idempotency`，结果通过，覆盖周常和节庆缺少幂等键拒绝、同 key 重放不重复扣成本/发奖励/增加个人用量和全局 claims。
- 验证命令: `npm --prefix server run qa:online-smoke`，结果通过，覆盖周常交换、节庆摊位和在线主流程；过程中出现内容审核拒绝的预期错误日志但脚本退出码为 0。
- 验证命令: `npm --prefix server run qa:realtime-smoke`，结果通过，覆盖周常/节庆交易通知路径。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过，保留既有 3 条 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 本轮 QA 稳定性补丁: `server/scripts/qa-realtime-smoke.mjs` 的 `createSmokeSeed()` 改为纯数字时间戳加递增计数，避免随机 base36 命中内容审核软拦截词导致注册账号或实时内容标题偶发 400；业务幂等协议未变。
- 验证命令: `node --check server/scripts/qa-realtime-smoke.mjs`、`npm --prefix server run qa:realtime-smoke`，结果通过。
- 本轮复验: 2026-06-06 运行 `npm --prefix server run qa:transaction-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过，周常/节庆同键重放和实时通知路径仍有效。
- 追加复验: 2026-06-06 运行 `npm --prefix server run qa:transaction-idempotency`，结果通过，周常交换站和节庆摊位缺 key 拒绝、同 key 重放不重复扣发仍有效。
- 追加复验: 2026-06-06 运行 `npm --prefix server run qa:transaction-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；周常/节庆交易收据、在线主流程和实时通知均未回归。
- 本轮复验: 2026-06-06 运行 `npm --prefix server run qa:transaction-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；周常/节庆同 key 重放、在线主流程和实时通知路径仍有效，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 完成度复核: 2026-06-06 修改文件仅 `0605修复todo.md`；运行 `npm --prefix server run qa:transaction-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；周常/节庆缺 key 拒绝、同 key 重放不重复扣发、在线主流程和实时通知路径均未回归。
- 并行改动后复验: 2026-06-06 运行 `npm --prefix server run qa:transaction-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过，周常/节庆缺 key 拒绝、同 key 重放不重复扣发、在线主流程和实时通知路径仍有效。
- 收口复验: 2026-06-06 运行 `npm --prefix server run qa:transaction-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过；周常/节庆缺 key 拒绝、同 key 重放不重复扣发、在线主流程和实时通知路径仍有效。
- 巡检复验: 2026-06-06 运行 `npm --prefix server run qa:transaction-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；周常交换站和节庆摊位缺 key 拒绝、同 key 重放不重复扣发、在线主流程和实时通知路径仍有效。
- 连续巡检复验: 2026-06-06 运行 `npm --prefix server run qa:transaction-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；周常交换站和节庆摊位缺 key 拒绝、同 key 重放不重复扣发、在线主流程和实时通知路径仍有效。
- 存档交易巡检: 2026-06-06 运行 `npm --prefix server run qa:transaction-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；周常交换站和节庆摊位缺 key 拒绝、同 key 重放不重复扣发、在线主流程、交易通知和实时通知路径仍有效。
- 交易幂等复验: 2026-06-06 运行 `npm --prefix server run qa:transaction-idempotency`、`npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；周常交换站和节庆摊位缺 key 拒绝、同 key 重放不重复扣成本/发奖励/增加用量或 claims，在线主流程、交易通知和实时通知路径仍有效，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 交易收据巡检: 2026-06-06 当前 `server/src/taoyuanWeeklyExchangeStation.js`、`server/src/taoyuanFestivalStall.js` 和周常/节庆前端面板存在并行 diff；运行 `node --check server/src/taoyuanWeeklyExchangeStation.js`、`node --check server/src/taoyuanFestivalStall.js`、`node --check server/scripts/qa-transaction-idempotency.mjs`、`npm --prefix server run qa:transaction-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；周常交换站和节庆摊位缺 key 拒绝、同 key 重放不重复扣成本/发奖励/增加用量或 claims，交易通知、在线主流程和实时通知路径仍有效；`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 交易回归审计: 2026-06-06 修改文件仅 `0605修复todo.md`；当前 `server/src/routes/api.js` 和周常/节庆前端面板存在并行 diff；运行 `node --check server/src/taoyuanWeeklyExchangeStation.js`、`node --check server/src/taoyuanFestivalStall.js`、`node --check server/scripts/qa-transaction-idempotency.mjs`、`node --check server/scripts/qa-online-smoke.mjs`、`node --check server/scripts/qa-realtime-smoke.mjs`、`npm --prefix server run qa:transaction-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；周常交换站和节庆摊位缺 key 拒绝、同 key 重放不重复扣成本/发奖励/增加用量或 claims、交易通知、在线主流程和实时通知路径仍有效，`online-smoke` 仅输出内容审核拒绝的预期错误日志，`check` 仅保留既有 3 条 warning。
- 遗留风险: 收据保留上限为每周 400 条；若单周同一玩家触发极大量不同 key，旧收据会按更新时间裁剪。

## 验证型任务池

以下不是已确认缺陷，先做可达性验证。验证为可达后，再按 P1 或 P2 纳入修复。

### V01 旧 `runBossExpedition()` 是否仍可被玩家环境调用

- [x] 状态: 已完成（验证完成，已做最小修复）
- 位置: `taoyuan-main/src/stores/useRegionMapStore.ts`
- 风险: 旧一键首领结算路径疑似不扣 HP。
- 验证: 全仓搜索存档迁移、自动巡逻、调试入口、外部脚本是否调用 `runBossExpedition()`；若可达，补 HP 扣减或移出生产导出。

验证记录:
- 修改文件: `taoyuan-main/src/stores/useRegionMapStore.ts`、`taoyuan-main/scripts/qa-region-helper-guards.mjs`、`taoyuan-main/package.json`。
- 结论: `runBossExpedition()` 无生产 UI 调用，仍被 `taoyuan-main/scripts/qa-late-game-samples.mjs` 用作 late-game 样本回归；保留该公开 API 以兼容既有 QA，但补齐 `simulateBossExpedition().projectedHp` 到 `playerStore.takeDamage()` 的 HP 扣减，避免旧一键首领路径绕过生命损耗。
- 验证命令: `npm --prefix taoyuan-main run qa:region-helper-guards`，结果通过，覆盖 `runBossExpedition()` 调用面白名单与 HP 扣减静态断言。
- 验证命令: `npm --prefix taoyuan-main run qa:late-game-samples`，结果通过，确认既有 late-game 样本仍可调用旧 helper 且首领成功/失败样本未回归。

### V02 瀚海活动牌局异常档和普通赌场动画卸载

- [x] 状态: 已完成（验证完成，已做最小修复）
- 位置: `useHanhaiStore.ts`、`HanhaiView.vue`、`TexasHoldemGame.vue`、`BuckshotRouletteGame.vue`
- 风险: active session 兼容和普通赌场 setTimeout 卸载清理不足。
- 验证: 构造旧档或异常导入档，验证是否会出现无法结算、无法离开或卸载后写 ref。

验证记录:
- 修改文件: `taoyuan-main/src/stores/useHanhaiStore.ts`、`taoyuan-main/src/views/game/HanhaiView.vue`、`taoyuan-main/src/components/game/TexasHoldemGame.vue`、`taoyuan-main/src/components/game/BuckshotRouletteGame.vue`、`taoyuan-main/scripts/qa-hanhai-session-timer-guards.mjs`、`taoyuan-main/package.json`。
- 结论: `deserialize()` 读档时会丢弃非法、已结算或跨日的瀚海扑克/恶魔轮盘 active session，避免异常导入档继续占用 `hasActiveCasinoSession`；瀚海扑克、恶魔轮盘和瀚海普通赌场动画均改为组件/页面持有 timeout，卸载时统一清理，恶魔轮盘等待中的异步 delay 会被 resolve 为失败并停止后续 ref 写入。
- 验证命令: `node --check taoyuan-main/scripts/qa-hanhai-session-timer-guards.mjs`，结果通过。
- 验证命令: `npm --prefix taoyuan-main run qa:hanhai-session-timer-guards`，结果通过，覆盖裸 timeout 静态守卫、卸载清理守卫、跨日 Texas、已结算 Buckshot、非法 shell 和同日合法 session 的读档归一化口径。
- 验证命令: `npm exec eslint -- src/stores/useHanhaiStore.ts src/views/game/HanhaiView.vue src/components/game/TexasHoldemGame.vue src/components/game/BuckshotRouletteGame.vue scripts/qa-hanhai-session-timer-guards.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过；保留既有 3 条 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 验证命令: `npm --prefix taoyuan-main run qa:mobile-ui-smoke`，已运行但被非本轮样本加载问题 `walletStore.serialize is not a function` 阻断，未作为通过证据。
- 未运行: `npm --prefix taoyuan-main run prepare:item-icons`，本轮未改物品图标或资源 manifest；`npm --prefix taoyuan-main run test:e2e`，本轮已用目标 QA、目标 eslint 和全量 check 覆盖瀚海牌局生命周期风险，完整 E2E 留待移动 smoke 的样本加载阻断修复后再跑。
- 遗留风险: 本轮未跨域修复 `walletStore.serialize` 导致的 mobile smoke 样本加载失败；普通赌场动画只做生命周期清理，不改变既有动画节奏和结算逻辑。

### V03 `/game` 深链或刷新恢复策略

- [x] 状态: 已完成（验证完成，已做最小修复）
- 位置: `router/index.ts`、`GameLayout.vue`
- 风险: 直达 `/game/farm` 当前会回主菜单，需确认产品预期。
- 验证: PWA 恢复、浏览器刷新、外部链接进入时，是回主菜单、自动加载活跃槽位，还是显示恢复入口。

验证记录:
- 修改文件: `taoyuan-main/src/views/GameLayout.vue`、`taoyuan-main/src/main.ts`、`taoyuan-main/scripts/qa-game-deeplink-restore-guards.mjs`、`taoyuan-main/scripts/qa-game-deeplink-browser-smoke.mjs`、`taoyuan-main/package.json`。
- 结论: `/game/*` 深链不再在 `GameLayout` setup 顶层立即回主菜单；刷新或外部链接进入时先显示恢复提示，若已有同模式活跃槽则优先加载，否则选择当前存档模式下最近保存且可读的槽位恢复到原深链；没有可读存档时回主菜单并保留 `redirect=/game/...`。浏览器 smoke 暴露的 Pinia `$reset` 初始快照循环引用启动崩溃已在 `main.ts` 用 WeakSet 深拷贝保护修复。
- 验证命令: `node --check taoyuan-main/scripts/qa-game-deeplink-restore-guards.mjs`，结果通过。
- 验证命令: `node --check taoyuan-main/scripts/qa-game-deeplink-browser-smoke.mjs`，结果通过。
- 验证命令: `npm --prefix taoyuan-main run qa:game-deeplink-restore-guards`，结果通过，覆盖不再顶层回菜单、恢复提示、活跃槽优先、最近可读槽选择、失败保留 redirect。
- 验证命令: `npm --prefix taoyuan-main run qa:game-deeplink-browser-smoke`，结果通过，真实浏览器覆盖无存档 `/game/farm` 回主菜单并保留 redirect，以及创建本地存档后刷新 `/game/farm` 自动恢复到 `game-layout`。
- 验证命令: `npm exec eslint -- src/main.ts src/views/GameLayout.vue src/router/index.ts scripts/qa-game-deeplink-restore-guards.mjs scripts/qa-game-deeplink-browser-smoke.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过；保留既有 3 条 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 验证命令: `npm --prefix taoyuan-main run qa:mobile-ui-smoke`，结果通过，写出 `docs/ui-smoke-2026-04-26/summary.json`；日志中仍有本地服务端未启动导致的 `127.0.0.1:4013` proxy refused / ECONNABORTED，但命令退出码为 0。
- 未运行: `npm --prefix taoyuan-main run prepare:item-icons`，本轮未改物品图标或资源 manifest；`npm --prefix taoyuan-main run test:e2e`，本轮已用专用真实浏览器 deep-link smoke、mobile smoke、目标 eslint 和全量 check 覆盖刷新/深链风险。
- 遗留风险: 无存档深链当前只回主菜单并保留 redirect，不自动弹出选择存档；若产品希望显示专门恢复入口，可在主菜单消费该 redirect 做二阶段体验优化。

### V04 村庄捐赠里程碑发奖后标记失败是否可达

- [x] 状态: 已完成（验证完成，已做最小修复）
- 位置: `taoyuan-main/src/stores/useVillageProjectStore.ts`
- 风险: 奖励已到账但 `claimedMilestoneIds` 未写入，可能重复领取。
- 验证: 用异常存档或配置缺失触发 `updateDonationState()` 返回 `undefined`；可达则改为幂等或事务回滚。

验证记录:
- 修改文件: `taoyuan-main/src/stores/useVillageProjectStore.ts`、`taoyuan-main/scripts/qa-village-project-donation-guards.mjs`。
- 结论: 风险路径可达性按“发奖后 `updateDonationState()` 返回 `undefined`”处理；`claimDonationMilestone()` 已在发奖前快照背包、玩家和成就状态，标记失败时回滚物品奖励、铜钱奖励以及铜钱奖励触发的成就统计，然后返回失败，不写入领取日志。
- 验证命令: `node --check taoyuan-main/scripts/qa-village-project-donation-guards.mjs`，结果通过。
- 验证命令: `node taoyuan-main/scripts/qa-village-project-donation-guards.mjs`，结果通过，覆盖源码顺序守卫和最小模型用例：标记失败时回滚背包、铜钱、成就统计且不写入已领取里程碑；标记成功时保留奖励和领取标记。
- 验证命令: `npm exec eslint -- src/stores/useVillageProjectStore.ts scripts/qa-village-project-donation-guards.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过；保留既有 3 条 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 未运行: `npm --prefix taoyuan-main run prepare:item-icons`，本轮未改物品图标或资源 manifest；`npm --prefix taoyuan-main run test:e2e`、`npm --prefix taoyuan-main run qa:mobile-ui-smoke`，本轮未改可视 UI 或路由，已用目标 QA、目标 eslint 和全量 check 覆盖本风险。
- 遗留风险: 本轮未改变奖励发放成功后的日志和提示口径；若未来 `earnMoney()` 增加更多跨 store 副作用，需要同步扩展该领取事务快照。

### V05 区域地图公开 helper 是否绕过满包 availability

- [x] 状态: 已完成（验证完成，已收窄公开面）
- 位置: `taoyuan-main/src/stores/useRegionMapStore.ts`
- 风险: `completeRouteAndGrantRewards()` / `completeEventAndGrantRewards()` 先标记完成再按容量发物品。
- 验证: 搜索调试入口、脚本、旧调用或自动流程是否直接调用 helper；可达则补容量预检或事务回滚。

验证记录:
- 修改文件: `taoyuan-main/src/stores/useRegionMapStore.ts`、`taoyuan-main/scripts/qa-region-helper-guards.mjs`、`taoyuan-main/package.json`。
- 结论: 两个 helper 仅在 `useRegionMapStore.ts` 内部被 `runRouteExpedition()` / `runRegionEvent()` 调用；本轮将 `completeRouteAndGrantRewards()` 和 `completeEventAndGrantRewards()` 从 Pinia store return API 移除，避免 UI、调试入口或外部脚本绕过 availability 直接调用。
- 验证命令: `npm --prefix taoyuan-main run qa:region-helper-guards`，结果通过，覆盖两个 helper 不再作为生产 store API 暴露，且不存在 store 外直接调用。
- 验证命令: `npm exec eslint -- src/stores/useRegionMapStore.ts scripts/qa-region-helper-guards.mjs`（工作目录 `taoyuan-main`），结果通过。

### V06 工具升级 `targetTier` 与当前 tier 不一致

- [x] 状态: 已完成（验证完成，已做最小修复）
- 位置: `taoyuan-main/src/stores/useInventoryStore.ts`
- 风险: 导入档保存不匹配 `targetTier`，日结日志和实际升级不一致。
- 验证: 构造异常 `pendingUpgrade.targetTier`；若可达，读档和日结完成时校验并修正。

验证记录:
- 修改文件: `taoyuan-main/src/stores/useInventoryStore.ts`、`taoyuan-main/scripts/qa-tool-upgrade-guards.mjs`。
- 结论: 风险可达；旧档/导入档可直接写入与当前工具等级不匹配的 `pendingUpgrade.targetTier`。本轮新增统一工具 tier helper，`deserialize()` 按当前工具等级修正或清空 `pendingUpgrade`，`startUpgrade()` 拒绝不匹配目标等级，`dailyUpgradeUpdate()` 按实际完成等级返回日结日志 tier。
- 验证命令: `node --check taoyuan-main/scripts/qa-tool-upgrade-guards.mjs`，结果通过。
- 验证命令: `node taoyuan-main/scripts/qa-tool-upgrade-guards.mjs`，结果通过，覆盖 steel 镐异常保存 `targetTier=iron` 会归一化为 `iridium`、非法工具类型/满级工具会清空 pending、日结返回实际完成 tier 而非异常存档 targetTier。
- 验证命令: `npm exec eslint -- src/stores/useInventoryStore.ts scripts/qa-tool-upgrade-guards.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过；保留既有 3 条 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 未运行: `npm --prefix taoyuan-main run prepare:item-icons`，本轮未改物品图标或资源 manifest；`npm --prefix taoyuan-main run test:e2e`、`npm --prefix taoyuan-main run qa:mobile-ui-smoke`，本轮未改可视 UI 或路由，已用目标 QA、目标 eslint 和全量 check 覆盖该存档边界。
- 遗留风险: 本轮只保证异常 `pendingUpgrade` 不再影响实际升级和日结日志，未新增专门 UI 展示工具升级存档归一化详情。

### V07 技能读档归一化

- [x] 状态: 已完成（验证完成，已做最小修复）
- 位置: `taoyuan-main/src/stores/useSkillStore.ts`
- 风险: 旧档或导入档写入负经验、超过 20 级、等级不足但已有专精、非法专精组合。
- 验证: 构造异常技能存档；可达后增加 `level/exp/perk` 归一化和迁移日志。

验证记录:
- 修改文件: `taoyuan-main/src/stores/useSkillStore.ts`、`taoyuan-main/scripts/qa-skill-save-normalization-guards.mjs`。
- 结论: 风险可达；旧档/导入档可写入负经验、超过 20 级、重复或非法技能类型、等级和经验不匹配、等级不足却带有高阶专精或跨分支专精。本轮在 `deserialize()` 中补齐 5 个技能、去重并移除非法类型，归一化 `level/exp`，按专精分支和等级清理 `perk5/10/15/20`，并暴露 `skillMigrationLogs` 作为迁移证据。
- 验证命令: `node --check taoyuan-main/scripts/qa-skill-save-normalization-guards.mjs`，结果通过。
- 验证命令: `node taoyuan-main/scripts/qa-skill-save-normalization-guards.mjs`，结果通过，覆盖负等级/负经验归零、超 20 级和过量经验裁剪、经验推导等级补齐、等级高于经验时补齐最低经验、重复技能去重、非法技能类型移除、非法或等级不足专精清空、合法专精保留。
- 验证命令: `npm exec eslint -- src/stores/useSkillStore.ts scripts/qa-skill-save-normalization-guards.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过；保留既有 3 条 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 未运行: `npm --prefix taoyuan-main run prepare:item-icons`，本轮未改物品图标或资源 manifest；`npm --prefix taoyuan-main run test:e2e`、`npm --prefix taoyuan-main run qa:mobile-ui-smoke`，本轮未改可视 UI 或路由，已用目标 QA、目标 eslint 和全量 check 覆盖该存档边界。
- 遗留风险: 本轮新增迁移日志为 store 诊断字段，暂未在 UI 上展示旧档修正详情。

### V08 行旅图 active session 跨晕厥或跨日策略

- [x] 状态: 已完成
- 位置: `useRegionMapStore.ts`、`useEndDay.ts`
- 风险: 远征推进/扎营调用 `advanceTime()` 后未像矿洞一样立即处理 `passedOut`。
- 验证: 实机验证晕厥跨日后 active session 是否应保留，是否阻塞新远征或周刷新。

验证记录:
- 修改文件: `taoyuan-main/src/stores/useRegionMapStore.ts`、`taoyuan-main/src/views/game/RegionMapView.vue`、`taoyuan-main/scripts/qa-region-passout-guards.mjs`。
- 结论: 风险可达；本地行旅图 `advanceActiveExpedition()`、`campActiveExpedition()`、`resolveCampAction()` 已经调用 `advanceTime()`，但页面层未消费 `passedOut`。本轮让三类动作返回 `timeResult`，并在 `RegionMapView.vue` 统一通过 `timeResult.passedOut` 调用 `handleEndDay()`，保持 active session 的失败/收束语义由原 store 流程决定，不在跨日处理中额外清空。
- 验证命令: `node --check taoyuan-main/scripts/qa-region-passout-guards.mjs`，结果通过。
- 验证命令: `node taoyuan-main/scripts/qa-region-passout-guards.mjs`，结果通过，覆盖三个 store 动作必须返回 `timeResult`、页面必须导入 `handleEndDay()`、三个本地远征操作 handler 必须检查晕厥跨日，且 store 不直接依赖 `useEndDay`。
- 验证命令: `npm exec eslint -- src/stores/useRegionMapStore.ts src/views/game/RegionMapView.vue scripts/qa-region-passout-guards.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过；保留既有 3 条 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 未运行: `npm --prefix taoyuan-main run prepare:item-icons`，本轮未改物品图标或资源 manifest；`npm --prefix taoyuan-main run test:e2e`、`npm --prefix taoyuan-main run qa:mobile-ui-smoke`，本轮未改可视布局或路由，已用固定静态守卫、目标 eslint 和全量 check 覆盖本地远征跨日链路。
- 遗留风险: 本轮未新增浏览器级晕厥注入 E2E；active session 跨日后是否立即结算仍沿用现有远征状态机，没有额外强制清空。

### V09 `generateFloorGrid()` 首杀参数是否有 store 外直接调用

- [x] 状态: 已完成（验证完成，暂不转修复）
- 位置: `taoyuan-main/src/data/mine.ts`
- 风险: 内部 `isFirstKill` 固定 true，绕过 store 时复刷 Boss 强弱逻辑可能分裂。
- 验证: 搜索直接调用路径；可达则传入首杀状态参数或移除内部固定分支。

验证记录:
- 修改文件: `taoyuan-main/scripts/qa-region-helper-guards.mjs`、`taoyuan-main/package.json`。
- 结论: `generateFloorGrid()` 生产调用仅来自 `taoyuan-main/src/stores/useMiningStore.ts`，由 store 在复刷 Boss 时传入/覆盖弱化逻辑；未发现 store 外直接调用生产路径，本轮不转修复。
- 验证命令: `npm --prefix taoyuan-main run qa:region-helper-guards`，结果通过，覆盖 `generateFloorGrid()` 只允许 `mine.ts` 定义和 `useMiningStore.ts` 调用。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过；保留既有 3 条 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 未运行: `npm --prefix taoyuan-main run prepare:item-icons`，本轮未改物品资源或 manifest；`npm --prefix taoyuan-main run test:e2e` 和 `npm --prefix taoyuan-main run qa:mobile-ui-smoke`，本轮未改路由或可视 UI，已用静态调用面 QA、late-game 样本、目标 lint 和全量 check 覆盖风险。
- 遗留风险: `runBossExpedition()` 仍作为兼容 late-game QA 的公开 API 保留；若后续完全迁移样本到多阶段首领远征，可再移除该旧 helper。

### V10 共居离线 stale merge 策略

- [x] 状态: 已完成（验证完成，暂不转修复）
- 位置: `server/src/taoyuanCohabitationRuntime.js`
- 风险: preflight 标记 stale，但后续仍允许 merge。
- 验证: 产品确认过期本地队列是否必须刷新后由玩家确认；若要求确认，改为阻断或显式确认。

验证记录:
- 修改文件: `server/scripts/qa-cohabitation-stale-merge-policy.mjs`、`0605修复todo.md`。
- 验证结论: stale merge 当前可达；`preflightCohabitationOfflineConflicts()` 对旧 `client_queue_revision` 返回 `client_queue_stale=true` 和 `server_authoritative_refresh_required`，但直接 `mergeCohabitationOfflineQueue()` 仍可提交，`resolveCohabitationOfflineConflicts()` 的 `server_authoritative_auto_merge` 也会继续提交并携带 stale 证据。
- 判定: `0605审查.md` 将该项描述为产品策略确认项而非已确认缺陷；本轮未获得“过期本地队列必须阻断或显式确认”的产品要求，因此不改 runtime，不转入修复任务。
- 验证命令: `node --check server/scripts/qa-cohabitation-stale-merge-policy.mjs`，结果通过。
- 验证命令: `node server/scripts/qa-cohabitation-stale-merge-policy.mjs`，结果通过，覆盖 stale preflight、直接 stale merge、auto-resolve stale merge 三条路径。
- 验证命令: `node --check server/src/taoyuanCohabitationRuntime.js`，结果通过。
- 验证命令: `npm --prefix server run qa:cohabitation-offline-closeout`，结果未通过；既有静态守卫要求 `OFFLINE_QUEUE_SUPPORTED_ACTIONS` 包含 `remove_crop_shared_farm`，但当前 runtime supported actions 列表未包含，属于 V10 stale 策略之外的周边 QA 缺口。
- 补充修复记录（2026-06-06）: 已补齐 `remove_crop_shared_farm` 离线队列动作，服务端 supported actions 和离线队列执行分派映射到共同农田 `remove_crop`，前端 API 类型、共居离线队列 UI 选项/载荷/标签同步接入，closeout QA 增加前后端静态守卫。
- 补充修改文件: `server/src/taoyuanCohabitationRuntime.js`、`server/scripts/qa-cohabitation-offline-closeout.mjs`、`taoyuan-main/src/utils/cohabitationApi.ts`、`taoyuan-main/src/views/game/online/OnlineCohabitationView.vue`。
- 补充验证命令: `node --check server/src/taoyuanCohabitationRuntime.js`、`node --check server/scripts/qa-cohabitation-offline-closeout.mjs`，结果通过。
- 补充验证命令: `npm --prefix server run qa:cohabitation-offline-closeout`，结果通过，覆盖服务端 supported actions 和前端 API/View 均包含 `remove_crop_shared_farm`。
- 补充验证命令: `npm --prefix taoyuan-main run check`，结果通过；保留既有 3 条 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 补充验证命令: `npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:realtime-smoke`，结果通过；`qa:online-smoke` 末尾出现内容审核拒绝栈为预期拒绝路径，进程退出码为 0。
- 验证限制: `npm --prefix server run qa:cohabitation-contract` 已尝试，分别在 120 秒和 300 秒超时，未作为通过证据；超时遗留的 QA node 进程已按命令行限定清理。
- 追加复验: 2026-06-06 运行 `node --check server/scripts/qa-cohabitation-stale-merge-policy.mjs`、`node server/scripts/qa-cohabitation-stale-merge-policy.mjs`、`npm --prefix server run qa:cohabitation-offline-closeout`、`npm --prefix taoyuan-main run check`，结果通过；stale preflight/直接 stale merge/auto-resolve stale merge 策略证据和 `remove_crop_shared_farm` 离线队列守卫均未回归，`check` 仅保留既有 3 条 warning。
- 完成度复核: 2026-06-06 修改文件仅 `0605修复todo.md`；运行 `node server/scripts/qa-cohabitation-stale-merge-policy.mjs`、`npm --prefix server run qa:cohabitation-offline-closeout`、`npm --prefix taoyuan-main run check`，结果通过；V10 仍按“产品策略确认项，不转修复”处理，stale merge 策略证据和离线队列守卫未回归。
- 并行改动后复验: 2026-06-06 当前 `server/src/taoyuanCohabitationRuntime.js` 存在大幅并行 diff；运行 `node server/scripts/qa-cohabitation-stale-merge-policy.mjs`、`npm --prefix server run qa:cohabitation-offline-closeout`、`npm --prefix taoyuan-main run check`，结果通过，V10 仍不转修复，stale merge 策略证据和离线队列守卫仍有效。
- 收口复验: 2026-06-06 运行 `node server/scripts/qa-cohabitation-stale-merge-policy.mjs`、`npm --prefix server run qa:cohabitation-offline-closeout`、`node --check server/src/taoyuanCohabitationRuntime.js`，结果通过；V10 仍按产品策略确认项处理，不转修复，stale merge 策略证据和离线队列守卫仍有效。
- 巡检复验: 2026-06-06 运行 `node server/scripts/qa-cohabitation-stale-merge-policy.mjs`、`npm --prefix server run qa:cohabitation-offline-closeout`、`npm --prefix taoyuan-main run check`，结果通过；V10 仍按产品策略确认项处理，不转修复，stale merge 策略证据和离线队列守卫仍有效。
- 连续巡检复验: 2026-06-06 运行 `node server/scripts/qa-cohabitation-stale-merge-policy.mjs`、`npm --prefix server run qa:cohabitation-offline-closeout`、`npm --prefix taoyuan-main run check`，结果通过；V10 仍按产品策略确认项处理，不转修复，stale merge 策略证据和离线队列守卫仍有效。
- 结构化反馈巡检: 2026-06-06 运行 `node server/scripts/qa-cohabitation-stale-merge-policy.mjs`、`npm --prefix server run qa:cohabitation-offline-closeout`、`npm --prefix taoyuan-main run check`，结果通过；V10 仍按产品策略确认项处理，不转修复，stale preflight/直接 stale merge/auto-resolve stale merge 策略证据和离线队列守卫仍有效。
- 验证型复核: 2026-06-06 当前 `server/src/taoyuanCohabitationRuntime.js` 仍有并行 diff；运行 `node --check server/scripts/qa-cohabitation-stale-merge-policy.mjs`、`node --check server/src/taoyuanCohabitationRuntime.js`、`node server/scripts/qa-cohabitation-stale-merge-policy.mjs`、`npm --prefix server run qa:cohabitation-offline-closeout`、`npm --prefix taoyuan-main run check`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过；V10 仍按产品策略确认项处理，不转修复，`online-smoke` 仅输出内容审核拒绝的预期日志，`check` 仅保留既有 3 条 warning。
- 验证型复核: 2026-06-06 当前 `server/src/taoyuanCohabitationRuntime.js` 仍有并行 diff；运行 `node --check server/scripts/qa-cohabitation-stale-merge-policy.mjs`、`node --check server/src/taoyuanCohabitationRuntime.js`、`node --check server/scripts/qa-cohabitation-offline-closeout.mjs`、`node server/scripts/qa-cohabitation-stale-merge-policy.mjs`、`npm --prefix server run qa:cohabitation-offline-closeout`、`npm --prefix taoyuan-main run check`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix server run qa:online-smoke`，结果通过；stale preflight/直接 stale merge/auto-resolve stale merge 证据和 `remove_crop_shared_farm` 离线队列守卫仍有效，V10 仍按产品策略确认项处理不转修复；`online-smoke` 仅输出内容审核拒绝的预期日志，`check` 仅保留既有 3 条 warning。
- 验证型复核: 2026-06-07 修改文件仅 `0605修复todo.md`；当前 `server/src/taoyuanCohabitationRuntime.js` 无未提交 diff，运行 `node --check server/scripts/qa-cohabitation-stale-merge-policy.mjs`、`node --check server/scripts/qa-cohabitation-offline-closeout.mjs`、`node --check server/scripts/qa-cohabitation-contract.mjs`、`node --check server/src/taoyuanCohabitationRuntime.js`、`node server/scripts/qa-cohabitation-stale-merge-policy.mjs`、`npm --prefix server run qa:cohabitation-offline-closeout`、`npm exec eslint -- src/utils/cohabitationApi.ts src/stores/useCohabitationStore.ts src/views/game/online/OnlineCohabitationView.vue`（工作目录 `taoyuan-main`）、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix server run qa:online-smoke`，结果通过；stale preflight 仍返回 `server_authoritative_refresh_required`，直接 stale merge 和 `server_authoritative_auto_merge` 仍会提交并携带 stale 证据，因此 V10 继续按产品策略确认项处理、不转修复；`online-smoke` 仅输出内容审核拒绝的预期日志。验证限制: `npm --prefix taoyuan-main run check` 被并行文件 `taoyuan-main/src/views/game/FarmView.vue` 的 `TS2339 getSeedItem/getSeedItemForCrop` 阻断，非 V10/Agent A 目标文件，已用共居目标 eslint 和服务端专项 QA 替代覆盖。
- 遗留风险: 如果后续产品确认 stale 本地队列必须刷新后由玩家确认，需要新增阻断或显式确认协议，并同步更新 `qa-cohabitation-contract.mjs` 中当前断言 stale merge 成功的既有用例。

### V11 单机运行时 `Math.random()` 与未来服务端仲裁

- [x] 状态: 已完成
- 位置: `useAnimalStore.ts`、`useFarmStore.ts`、`useBreedingStore.ts`
- 风险: 当前未接入服务端权威，但未来若共享状态快照会有重放一致性问题。
- 验证: 暂不修，作为未来联网仲裁改造前置审查项。
- 完成记录: 2026-06-06 新增 `taoyuan-main/scripts/qa-local-randomness-audit.mjs` 静态审计脚本，锁定单机运行时可接受的 `Math.random()` 基线：`useAnimalStore.ts` 14 处、`useFarmStore.ts` 10 处、`useBreedingStore.ts` 14 处，总计 38 处；用于后续联网仲裁改造前发现随机点新增或迁移。
- 验证记录: 2026-06-06 运行 `node --check taoyuan-main/scripts/qa-local-randomness-audit.mjs`、`node taoyuan-main/scripts/qa-local-randomness-audit.mjs`，结果通过，输出 `verdict: pass`、`expectedTotal: 38`、`actualTotal: 38`。

### V12 资源 manifest 首次失败后是否应重试

- [x] 状态: 已完成
- 位置: `useItemIconManifest.ts`、`useNpcPortraitManifest.ts`
- 风险: fetch 失败后也把 `manifestLoaded` 置 true，本会话不再重试。
- 验证: 确认这是有意降级还是应支持手动或自动重试；若需重试，按 T24 的生命周期思路补状态。

验证记录:
- 修改文件: `taoyuan-main/src/composables/useItemIconManifest.ts`、`taoyuan-main/src/composables/useNpcPortraitManifest.ts`、`taoyuan-main/scripts/qa-manifest-retry-guards.mjs`。
- 结论: 风险可达；两个 manifest loader 的 catch 分支都会把 `manifestLoaded` 置为 true，首次 fetch 失败后本会话后续调用直接返回 `null`。本轮改为失败时保留 `manifestLoaded=false`，成功后仍缓存；同时新增 `retry*Manifest()` 与 `reset*Manifest()`，供后续 UI 或诊断入口显式重试和清空状态。
- 验证命令: `node --check taoyuan-main/scripts/qa-manifest-retry-guards.mjs`，结果通过。
- 验证命令: `node taoyuan-main/scripts/qa-manifest-retry-guards.mjs`，结果通过，覆盖 item icon 和 NPC portrait 两条 manifest 链路：失败 catch 不得设置 loaded、retry 必须重置 loaded 并复用 load、reset 必须清空 manifest/error/loading/promise。
- 验证命令: `npm exec eslint -- src/composables/useItemIconManifest.ts src/composables/useNpcPortraitManifest.ts scripts/qa-manifest-retry-guards.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过；保留既有 3 条 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 未运行: `npm --prefix taoyuan-main run prepare:item-icons`，本轮未改 manifest 内容或图标生成脚本；`npm --prefix taoyuan-main run test:e2e`、`npm --prefix taoyuan-main run qa:mobile-ui-smoke`，本轮只改加载失败状态机和静态守卫，无可视布局变更。
- 遗留风险: 本轮只提供自动再调用和显式 retry/reset 能力，暂未在 UI 上新增“重试 manifest”按钮；`useItemIconManifest.ts` 当前来自工作区未跟踪的物品图标链路，本轮只在该目标文件内补 V12 语义。

### V13 小屏 UI 遮挡截图验证

- [x] 状态: 已完成
- 位置: `InventoryView.vue`、`QuestView.vue`、`ShopView.vue`、`MiningView.vue`
- 风险: 源码未确认遮挡，但需要视觉证据。
- 验证: 用 Playwright 只读截图覆盖 360px、768px、1280px，重点看矿洞战斗弹窗、任务详情、临时背包详情、商店买卖弹窗。

验证记录:
- 修改文件: `taoyuan-main/scripts/qa-small-screen-overlap-smoke.mjs`、`docs/ui-v13-2026-06-06/summary.json`、`docs/ui-v13-2026-06-06/*.png`、`0605修复todo.md`。
- 验证口径: 新增 V13 专项 Playwright 截图脚本，使用 `endgame_showcase` 样例档覆盖 360x780、768x900、1280x900；逐一打开临时背包详情、任务详情、万物铺购买弹窗、矿洞战斗弹窗，按场景特定文案确认命中的是真实弹窗，断言弹窗可见、页面无水平溢出、目标弹窗未超出视口；矿洞战斗通过 `engageRevealedMonster()` 固定进入真实交战状态；本地 mock `/api/me`、`/api/public-config`、AI config、图标偏好和玩法日志接口，避免无后端环境噪声污染截图证据。
- 验证命令: `node --check taoyuan-main/scripts/qa-small-screen-overlap-smoke.mjs`，结果通过。
- 验证命令: `node taoyuan-main/scripts/qa-small-screen-overlap-smoke.mjs`，结果通过，生成 12 张截图和 `docs/ui-v13-2026-06-06/summary.json`；summary 记录 12 条结果、0 个水平溢出、0 个目标出界、0 个 page error、0 个 request failure、0 个 console error；人工抽查 `mining-combat-dialog-360.png` 确认显示“遭遇怪物”战斗弹窗而非矿洞主体面板。
- 验证命令: `npm exec eslint -- scripts/qa-small-screen-overlap-smoke.mjs`（工作目录 `taoyuan-main`），结果通过。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过；保留既有 3 条 warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 验证说明: 本轮未运行 `npm --prefix taoyuan-main run prepare:item-icons`、`npm --prefix taoyuan-main run test:e2e`、`npm --prefix taoyuan-main run qa:mobile-ui-smoke`；V13 是指定四类弹窗的小屏截图专项验证，已用新增脚本覆盖验收要求，避免覆盖更广的烟测截图产物和并行改动。
- 遗留风险: 本轮以自动布局断言、截图证据和 360px 关键图抽查为准，未做 12 张截图逐图视觉审阅；未覆盖所有弹窗组合和在线页面。

### V14 重命名重复物品 ID 的旧档迁移影响

- [x] 状态: 已完成
- 位置: `items.ts`、存档迁移代码、任务/图鉴/结缘相关表
- 风险: 旧档已经持有 `dragon_pearl`、`osmanthus_tea`、`lychee`、`persimmon`、`mulberry`。
- 验证: 扫描样本存档和线上默认存档；制定迁移或补偿规则后再拆 ID。

验证记录:
- 验证命令: `rg -n "dragon_pearl|osmanthus_tea|lychee|persimmon|mulberry" data-defaults/taoyuan_saves taoyuan-main/src/data/sampleSaves.ts`，结果无命中，仓内默认/样本存档未持有这五个旧歧义 ID。
- 兼容策略: 通用旧背包栈保留作物含义；明确为茶饮奖励/组合、加工产出、结缘配置的上下文迁移到新 ID；祠堂提交旧 `osmanthus_tea` key 会在 `useAchievementStore.deserialize()` 合并到 `processed_osmanthus_tea`。
- 遗留风险: 外部玩家旧档若已经把同一个旧 ID 混合用于多种来源，存档本身缺少来源信息，无法无损拆分同一堆叠；本轮保证不丢弃旧 ID，并让新产出与可判定上下文不再继续制造歧义。

### V15 额度兑换前端回滚与服务端已落盘交互

- [x] 状态: 已完成（验证完成，当前实现已关闭该故障窗口）
- 位置: `WalletView.vue`、`taoyuanHall.js`、`useSaveStore.ts`
- 风险: 服务端已写活动存档铜钱，但前端 `autoSave()` 失败后本地回滚，后续 pending/autoSave 可能覆盖服务端结果。
- 验证: 模拟保存被策略阻断、revision stale、pending copy 状态，确认不会形成资产不一致。

验证记录:
- 修改文件: `taoyuan-main/scripts/qa-quota-exchange-save-consistency.mjs`、`taoyuan-main/package.json`、`taoyuan-main/src/views/game/WalletView.vue`、`0605修复todo.md`。
- 验证结论: 旧风险路径当前已关闭；额度导入/导出使用服务端返回的 `taoyuan_money` 更新本地铜钱，使用 `save_revision` 调用 `saveStore.acknowledgeServerSlotRevision()` 更新 CAS base，pending copy 或 `autoSave()` 失败时不再回滚为旧本地铜钱，而是提示重新读取服务端存档。
- 新增守卫: `qa:quota-exchange-save-consistency` 静态校验额度接口必须携带 `active_save_slot/idempotency_key`、服务端必须返回 `taoyuan_money/save_revision`、前端不得在服务端已落盘后执行 `playerStore.setMoney(rollbackMoney)`，并确认后续 pending server save 使用已确认 revision。
- 本轮兼容修复: `WalletView.vue` 的两处服务端已落盘提示 tone 从非法的 `warning` 改为现有 `accent`，解除 `vue-tsc` 阻断。
- 验证命令: `npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`，结果通过。
- 验证命令: `npm --prefix server run qa:transaction-idempotency`，结果通过，覆盖额度导入/导出和周常/节庆同 key 重放不重复变更。
- 验证命令: `npm --prefix taoyuan-main run check`，结果通过；保留既有 3 条 lint warning: `src/utils/onlineProfileApi.ts`、`src/views/game/CottageView.vue`、`src/views/game/FarmView.vue`。
- 验证命令: `npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:realtime-smoke`，结果均通过；`qa:online-smoke` 过程中出现内容审核拒绝的预期错误日志，脚本退出码为 0；`qa:realtime-smoke` 首次与 online smoke 并行抢占 4013 端口失败，单独重跑通过。
- 本轮复验: 2026-06-06 运行 `npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`、`npm --prefix server run qa:transaction-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；服务端已落盘后的本地回滚禁用、确认 revision 同步和后续 pending save 基线仍保持一致。
- 完成度复核: 2026-06-06 修改文件仅 `0605修复todo.md`；运行 `npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`、`npm --prefix server run qa:transaction-idempotency`、`npm --prefix taoyuan-main run check`，结果通过；服务端已落盘后的本地回滚禁用、确认 revision 同步和后续 pending save 基线仍保持一致。
- 并行改动后复验: 2026-06-06 运行 `npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`、`npm --prefix server run qa:transaction-idempotency`、`npm --prefix taoyuan-main run check`，结果通过，服务端已落盘后的本地回滚禁用、确认 revision 同步和后续 pending save 基线仍保持一致。
- 收口复验: 2026-06-06 运行 `npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`、`npm --prefix server run qa:transaction-idempotency`、`npm --prefix server run qa:realtime-smoke`，结果通过；服务端已落盘后的本地回滚禁用、确认 revision 同步和后续 pending save 基线仍保持一致。
- 巡检复验: 2026-06-06 运行 `npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`、`npm --prefix server run qa:transaction-idempotency`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；服务端已落盘后的本地回滚禁用、确认 revision 同步和后续 pending save 基线仍保持一致。
- 连续巡检复验: 2026-06-06 运行 `npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`、`npm --prefix server run qa:transaction-idempotency`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；服务端已落盘后的本地回滚禁用、确认 revision 同步和后续 pending save 基线仍保持一致。
- 存档交易巡检: 2026-06-06 运行 `npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`、`npm --prefix server run qa:transaction-idempotency`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix taoyuan-main run check`，结果通过；服务端已落盘后的本地回滚禁用、确认 revision 同步、后续 pending save 基线、在线主流程和实时通知路径仍保持一致。
- 结构化反馈巡检: 2026-06-06 运行 `npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`、`npm --prefix server run qa:transaction-idempotency`、`npm --prefix taoyuan-main run check`，结果通过；服务端已落盘后的本地回滚禁用、确认 revision 同步、后续 pending save 基线和额度/周常/节庆交易收据仍保持一致。
- 验证型复核: 2026-06-06 运行 `node --check taoyuan-main/scripts/qa-quota-exchange-save-consistency.mjs`、`npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`、`npm --prefix server run qa:transaction-idempotency`、`npm --prefix taoyuan-main run check`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:online-smoke`、`npm --prefix server run qa:realtime-smoke`，结果通过；服务端已落盘后的本地回滚禁用、确认 revision 同步、交易收据、在线主流程和实时通知路径仍保持一致，`online-smoke` 仅输出内容审核拒绝的预期日志，`check` 仅保留既有 3 条 warning。
- 验证型复核: 2026-06-06 当前 `server/src/routes/api.js`、`server/src/taoyuanHall.js` 存在并行治理 diff；运行 `node --check server/src/routes/api.js`、`node --check server/src/taoyuanHall.js`、`node --check taoyuan-main/scripts/qa-quota-exchange-save-consistency.mjs`、`npm --prefix taoyuan-main run qa:quota-exchange-save-consistency`、`npm --prefix server run qa:transaction-idempotency`、`npm --prefix taoyuan-main run check`、`npm --prefix server run qa:save-corruption-guard`、`npm --prefix server run qa:coop-order-relay`、`npm --prefix server run qa:coop-order-compensation-replay`、`npm --prefix server run qa:realtime-smoke`、`npm --prefix server run qa:online-smoke`，结果通过；服务端已落盘后的本地回滚禁用、确认 revision 同步、额度/周常/节庆交易收据、在线主流程和实时通知路径仍保持一致，`online-smoke` 仅输出内容审核拒绝的预期日志，`check` 仅保留既有 3 条 warning。
- 遗留风险: 本轮新增的是静态故障窗口守卫，未做浏览器级断网/响应丢失交互注入；服务端交易幂等和前端 check 已覆盖当前实现关键不变量。

### V16 未来多材料武器购买的材料回滚

- [x] 状态: 已完成（验证完成，暂不转修复）
- 位置: `ShopView.vue`、`weapons.ts`
- 风险: 当前可购买武器均只有一个材料项；若未来新增多材料，扣料中途失败只退钱不恢复已扣材料。
- 验证: 新增多材料配置前补快照回滚测试，或提前把武器购买改为事务式扣料。

验证记录:
- 修改文件: `0605修复todo.md`。
- 验证结论: `ShopView.vue` 当前仍是先扣钱、逐个 `inventoryStore.removeItem()` 扣材料，材料中途失败只退钱；但 `weapons.ts` 当前 11 把 `shopPrice !== null` 的可购买武器 `shopMaterials` 最大长度为 1，因此“多材料中途失败只退钱不恢复已扣材料”在现有配置下不可达，暂不转修复。
- 验证命令: `Select-String -Path .\taoyuan-main\src\views\game\ShopView.vue -Encoding UTF8 -Pattern "handleBuyWeapon|removeItem|退" -Context 5,18`，确认武器购买的逐项扣料和失败只退钱分支仍存在。
- 验证命令: `node` 内联只读脚本扫描 `taoyuan-main/src/data/weapons.ts`，结果通过；输出 11 把可购买武器，`max material count=1`。
- 完成度复核: 2026-06-06 修改文件仅 `0605修复todo.md`；运行 `Select-String -Path .\taoyuan-main\src\views\game\ShopView.vue -Encoding UTF8 -Pattern "handleBuyWeapon|removeItem|退" -Context 5,18`，确认购买逻辑仍是逐项扣料、失败只退钱；随后用 PowerShell here-string 运行 Node 只读扫描 `taoyuan-main/src/data/weapons.ts`，结果 `buyable=11 maxMaterialCount=1`，V16 仍不可达、不转修复。前两次 `node -e` 尝试因 PowerShell 命令行引用剥离失败，已用 here-string 重跑通过。
- 并行改动后复验: 2026-06-06 当前 `taoyuan-main/src/views/game/ShopView.vue` 存在并行 diff；运行 `Select-String -Path .\taoyuan-main\src\views\game\ShopView.vue -Encoding UTF8 -Pattern "handleBuyWeapon|removeItem|退" -Context 5,18`，确认购买逻辑仍是逐项扣料、失败只退钱；随后用 PowerShell here-string 运行 Node 只读扫描 `taoyuan-main/src/data/weapons.ts`，结果 `buyable=34 maxMaterialCount=1`，现有配置下 V16 仍不可达、不转修复。首次 `node -e` 尝试因 PowerShell 命令行引用剥离失败，未作为通过证据。
- 收口复验: 2026-06-06 运行 `Select-String -Path .\taoyuan-main\src\views\game\ShopView.vue -Encoding UTF8 -Pattern "handleBuyWeapon|removeItem|退" -Context 5,18`，确认购买逻辑仍是逐项扣料、失败只退钱；PowerShell here-string 运行 Node 只读扫描 `taoyuan-main/src/data/weapons.ts`，结果 `buyable=34 maxMaterialCount=1`，现有配置下 V16 仍不可达、不转修复。
- 巡检复验: 2026-06-06 首次 `node -e` 只读扫描因 PowerShell 引号剥离失败，未作为证据；改用 Node REPL 读取 `taoyuan-main/src/data/weapons.ts`，结果 `buyable=34 maxMaterialCount=1`，现有配置下 V16 仍不可达、不转修复；`npm --prefix taoyuan-main run check` 同轮通过。
- 连续巡检复验: 2026-06-06 首次 Node REPL 扫描因已存在同名绑定失败，未作为证据；改用局部变量重跑读取 `taoyuan-main/src/data/weapons.ts`，结果 `buyable=34 maxMaterialCount=1`，现有配置下 V16 仍不可达、不转修复；`npm --prefix taoyuan-main run check` 同轮通过。
- 结构化反馈巡检: 2026-06-06 运行 `Select-String -Path .\taoyuan-main\src\views\game\ShopView.vue -Encoding UTF8 -Pattern "handleBuyWeapon|removeItem|退" -Context 5,18`，确认购买逻辑仍是逐项扣料、失败只退钱；PowerShell here-string 运行 Node 只读扫描 `taoyuan-main/src/data/weapons.ts`，结果 `buyable=11 maxMaterialCount=1`；`npm --prefix taoyuan-main run check` 同轮通过，现有配置下 V16 仍不可达、不转修复。
- 验证型复核: 2026-06-06 当前 `taoyuan-main/src/views/game/ShopView.vue` 仍有并行 diff；运行 `Select-String -Path .\taoyuan-main\src\views\game\ShopView.vue -Encoding UTF8 -Pattern "handleBuyWeapon|removeItem|退" -Context 5,18`，确认武器购买仍是逐项扣料、材料失败只退钱；PowerShell here-string 运行 Node 只读扫描 `taoyuan-main/src/data/weapons.ts`，结果 `buyable=11 maxMaterialCount=1`、`multiMaterial=[]`；`npm --prefix taoyuan-main run check` 通过且仅保留既有 3 条 warning，现有配置下 V16 仍不可达、不转修复。
- 验证型复核: 2026-06-06 当前 `taoyuan-main/src/views/game/ShopView.vue` 存在并行物品图标 UI diff，未触碰 `handleBuyWeapon()`；运行 `Select-String -Path .\taoyuan-main\src\views\game\ShopView.vue -Encoding UTF8 -Pattern "handleBuyWeapon|removeItem|退" -Context 5,18`，确认武器购买仍是先扣钱、逐项 `removeItem()` 扣料、材料失败只退钱；运行 `node -e` 只读扫描 `taoyuan-main/src/data/weapons.ts`，结果 `{"buyable":11,"maxMaterialCount":1,"multiMaterial":[]}`；`npm --prefix taoyuan-main run check` 通过且仅保留既有 3 条 warning，现有配置下 V16 仍不可达、不转修复。
- 遗留风险: 如果后续新增任意 `shopMaterials.length > 1` 的商店武器，必须先补材料快照回滚或把 `handleBuyWeapon()` 改为事务式扣料，并加入固定配置守卫。

## 建议执行批次

### 批次 1: 坏档、卡死、经济漏洞
任务: `T00`、`T06`、`T07`、`T08`、`T09`、`T16`

目标:
- 阻止继续制造坏云档。
- 阻止奖励和货币重复发放。
- 阻止矿洞满包卡死。

最小验证:
- `cd server && npm run qa:save-corruption-guard`
- `cd server && npm run qa:coop-order-relay`
- `cd server && npm run qa:coop-order-compensation-replay`
- `cd server && npm run qa:online-smoke`
- `cd taoyuan-main && npm run check`

### 批次 2: 存档同步和在线房间协议
任务: `T11`、`T12`、`T13`、`T28`

目标:
- 让云存档写入具备 CAS 语义。
- 让房间重连和动作重试可恢复、可幂等。
- 扩展交易幂等到周常和节庆摊位。

最小验证:
- `cd server && npm run qa:online-smoke`
- `cd server && npm run qa:realtime-smoke`
- 新增 revision 冲突和动作重放 QA 用例。

### 批次 3: 战斗与成长数值
任务: `T01`、`T02`、`T05`、`T18`、`T20`、`T21`、`T22`

目标:
- 修复死亡判定、吸血边界、专精真实效果、远征构筑叠加和读档装备归一化。

最小验证:
- `cd taoyuan-main && npm run check`
- 新增固定 RNG 或固定存档测试，覆盖防御致死、遭遇致死、吸血截断、幸运宝箱、首领抗压、装备读档。

### 批次 4: 数据资源和任务事务
任务: `T14`、`T15`、`T25`

目标:
- 消除重复物品 ID。
- 修复特殊订单状态回滚。
- 统一任务奖励预览。

最小验证:
- `cd taoyuan-main && npm run prepare:item-icons`
- `cd taoyuan-main && npm run check`
- 手工或脚本回归茶机、炼器、果树、采集、结缘、特殊订单提交。

### 批次 5: UI 反馈和性能稳定
任务: `T03`、`T04`、`T17`、`T19`、`T23`、`T24`、`T26`、`T27`

目标:
- 修复暂停状态残留、新档污染、加载失败静默、战斗浮字、日志容量、timeout 生命周期、背包按钮和 HP 药品入口。

最小验证:
- `cd taoyuan-main && npm run check`
- `cd taoyuan-main && npm run test:e2e`
- 对 360px、768px、1280px 关键弹窗做截图检查。

## 完成定义

- 每个任务都要在代码或测试中留下可验证证据。
- P0/P1 任务没有测试时，不能标记完成；至少需要故障注入、固定存档或服务端 QA 脚本之一。
- 修复会改协议时，前后端必须同时完成，并验证旧客户端或旧数据的失败表现可控。
- 修复会改存档结构时，必须包含迁移、默认值、坏数据容错和旧档样本验证。
- 修复会改物品 ID 时，必须同步 manifest、图标 QA、任务/商店/掉落/图鉴引用和旧档迁移。
- 完成后在对应任务下注明: 修改文件、验证命令、通过结果、遗留风险。
