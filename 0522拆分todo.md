# 0522 TODO：联机界面拆分与在线中心重组

> 目标：把当前联机系统从“多个长页平铺、从头滑到底”改成“模块优先的在线中心 + 独立子页”。  
> 方向：彻底拆页；首页优先展示模块，不把所有待办塞进第一屏。  
> 原则：不改后端 API，不重做联机业务逻辑，优先重组前端信息架构和入口路径。

---

## 0. 总执行原则

- [x] 联机入口统一收口到游戏内 `online` 面板。
- [x] `online` 首页只做模块分流，不承载所有细节操作。
- [x] 保留“庄园 / 邻里 / 委托 / 节会 / 村社”五条主线命名。
- [x] 旧入口先兼容，不直接删除玩家可能已经习惯的路由。
- [x] `/hall` 交流大厅保留为主菜单外社区入口，在线中心只提供跳转或提示。
- [x] 不修改服务端接口和结算规则。
- [x] 不把个人存档改成多人共享存档。
- [ ] 每拆完一个模块，都要能独立进入、独立刷新、独立完成原有核心操作。
- [ ] 每个阶段完成后更新 `CHANGELOG.md` 和 `taoyuan-main/CHANGELOG.md`。

---

## 1. 阶段 A：建立在线中心入口

> 目标：先解决“联机到底从哪里进”的问题，让玩家不用在地图里找散落入口。

### A0 新增 `online` 面板定义

- [x] 在 `taoyuan-main/src/composables/useNavigation.ts` 新增 `online` 面板 key。
- [x] 给 `online` 配置清晰图标和中文标签，建议标签为 `联机` 或 `在线`。
- [x] 在 `taoyuan-main/src/data/timeConstants.ts` 设置 `TAB_TO_LOCATION_GROUP.online = null`。
- [x] 确认进入 `online` 不消耗游戏内移动时间。
- [x] 确认进入 `online` 不触发店铺营业时间限制。

完成标准：

- [x] 玩家可从游戏内导航进入在线中心。
- [x] 切到在线中心不推进游戏时间。
- [x] 原有农场、村庄、野外、随身面板切换逻辑不受影响。

### A1 新增路由

- [x] 在 `taoyuan-main/src/router/index.ts` 新增 `/game/online`。
- [x] 新增 `OnlineView.vue` 作为在线中心首页。
- [x] 预留五个子路由或子页面入口：
  - [x] `/game/online/manor`
  - [x] `/game/online/neighbor`
  - [x] `/game/online/orders`
  - [x] `/game/online/festival`
  - [x] `/game/online/society`
- [x] 路由名称保持可读：`online`、`online-manor`、`online-neighbor`、`online-orders`、`online-festival`、`online-society`。

完成标准：

- [x] 直接访问 `/game/online` 不白屏。
- [x] 直接访问五个子入口不白屏。
- [x] 刷新页面后仍能回到对应子入口。

### A2 移动地图接入

- [x] 在 `MobileMapMenu.vue` 的“联机主导航”里新增在线中心入口。
- [x] 将当前散落的 `social / manor / quest / festival / society` 从一级联机组弱化为在线中心内的模块。
- [x] 保留必要快捷入口，但默认推荐先进入在线中心。
- [x] 确认 360px 宽度下入口文字不挤压、不换行失控。

完成标准：

- [x] 手机端一眼能看到“联机 / 在线中心”。
- [x] 五大模块不再在地图菜单里挤成一排主要入口。
- [x] 邮箱仍保留在常用工具，不并入在线中心。

---

## 2. 阶段 B：在线中心首页

> 目标：首页先回答“联机有哪些模块”，不是把所有操作堆出来。

### B0 首页结构

- [x] 首页顶部展示短标题和当前在线层说明。
- [x] 首页主体使用五张模块卡：
  - [x] 庄园：公开展示、来访、留言、主题周、收藏。
  - [x] 邻里：名片、好友、邻里组织、订阅。
  - [x] 委托：在线求助单、接单、交付、互助声望。
  - [x] 节会：世界事件、节会房间、远征房间、纪念。
  - [x] 村社：组织、成员、仓库、提案、公共建设。
- [x] 每张卡只显示摘要、状态、小统计和进入按钮。
- [x] 不在首页展开表单、长列表、历史记录。

完成标准：

- [x] 桌面首屏能完整看到五大模块或至少看到下一行提示。
- [x] 手机端打开后不需要从头滑很久才能理解入口结构。
- [x] 每张卡都有明确目的，不写长篇说明。

### B1 模块卡状态摘要

- [x] 庄园卡从 `useManorStore` 读取是否有快照、收藏/关注摘要。
- [x] 邻里卡从 `useSocialStore` 读取名片、邻里组、邀请/申请数量。
- [x] 委托卡从 `useCoopOrderStore` 读取我的订单、已接订单、可见订单数量。
- [x] 节会卡读取 `useFestivalRoomStore`、`useExpeditionRoomStore` 的我的房间和邀请数量。
- [x] 村社卡从 `useSocietyStore` 读取我的村社、申请/邀请、提案或工程摘要。
- [x] 如果某个 store 加载失败，卡片显示轻量错误，不影响其他卡片。

完成标准：

- [x] 一个模块接口失败时，其他模块仍可进入。
- [x] 首页不会因为一次性加载太多内容变成长页。
- [x] 卡片状态足够判断“这个模块有没有事可做”。

### B2 首页刷新策略

- 部分完成：在线中心首页已有总刷新、摘要级拉取、逐模块错误兜底和非遮挡刷新状态；“子页进入后再加载完整列表和表单”随 C-G 模块拆页继续落实。
- [x] 首页提供一个总刷新按钮。
- [x] 总刷新只拉取摘要所需数据。
- [ ] 子页进入后再加载完整列表和表单。
- [x] 刷新中状态不遮挡整个页面。

完成标准：

- [x] 首页刷新不会导致五个模块内容同时闪烁成大面积空白。
- [x] 刷新失败有错误提示，但不吞掉已加载内容。

---

## 3. 阶段 C：庄园拆页

> 目标：把 `ManorView.vue` 从一条长页拆成可分段进入的庄园模块。

### C0 建立庄园模块壳

- [x] 新增 `OnlineManorView.vue` 或在 `online/manor` 下建立庄园页。
- [x] 顶部保留当前庄园身份：自己的庄园 / 正在访问他人庄园。
- [x] 增加模块内标签页或二级导航：
  - [x] 概览
  - [x] 主题
  - [x] 留言
  - [x] 来访
  - [x] 导览
  - [x] 收藏
- [x] 默认进入“概览”，不默认展开所有内容。

完成标准：

- [x] 玩家不需要滚完整页就能找到留言或主题设置。
- [x] 访问他人庄园时，只显示可用操作。
- [x] 自己的庄园管理操作仍完整保留。

### C1 概览页

- [x] 迁移 `ManorPreviewCard` 到庄园概览页。
- [x] 展示当前主题、来访摘要、收藏关注摘要。
- [x] 提供跳转到主题、留言、来访的短按钮。

完成标准：

- [x] 打开庄园模块第一屏先看到庄园快照。
- [x] 概览页没有长表单。

### C2 主题页

- [x] 迁移封面上传、模板选择、主题保存。
- [x] 模板预览保留在主题页内。
- [x] 主题推荐和官方精选放在同页下半区。

完成标准：

- [x] 庄园主人可完成原有主题设置。
- [x] 访客只读主题展示，不看到无效编辑控件。

### C3 留言页

- [x] 迁移留言类型、快捷留言、留言输入。
- [x] 留言列表在独立滚动区域内展示。
- [x] 主人回复、置顶操作保留。

完成标准：

- [x] 留言输入和留言列表不再夹在庄园长页中间。
- [x] 留言为空、加载中、失败都有状态。

### C4 来访与导览页

- [x] 迁移来访目的、来访记录、反馈输入。
- [x] 迁移导览点新增、导览路线摘要、已设参观点列表。
- [x] 来访与导览可以做成两个标签，也可以做成同一页上下两块，但不能回到全页平铺。

完成标准：

- [x] 访客能快速记录来访。
- [x] 庄园主人能快速维护导览点。

---

## 4. 阶段 D：邻里拆页

> 目标：把 `SocialView.vue` 拆成名片、关系、邻里组织三类，不再混成一页。

### D0 建立邻里模块壳

- [x] 新增 `OnlineNeighborView.vue`。
- [x] 模块内二级导航：
  - [x] 名片
  - [x] 好友
  - [x] 邻里
  - [x] 订阅
- [x] 默认进入“名片”。

完成标准：

- [x] 公开名片设置不和邻里组织管理混在一起。
- [x] 好友主操作有清晰入口。

### D1 名片页

- [x] 迁移公开名片预览。
- [x] 迁移名片设置表单。
- [x] 头像上传、公开状态、标签选择保留。
- [x] 荣誉、纪念品、成就卡等长展示内容默认折叠或放到“展示档案”分段。

完成标准：

- [x] 名片编辑首屏可完成主要字段修改。
- [x] 荣誉长列表不把编辑区推到很后面。

### D2 好友页

- [x] 如果已有独立好友驿站，在线中心内只做入口跳转。
- [x] 不适用：当前版本已有独立好友驿站，好友搜索、申请、列表、黑名单继续由 `FriendStationView.vue` 承接。
- [x] 保留访问庄园、写信、送礼、邀请进房、协作跳转。

完成标准：

- [x] 好友相关主操作不再藏在地图或邻里长页里。
- [x] 目标玩家上下文传递不丢失。

### D3 邻里组织页

- [x] 迁移邻里任务卡、创建邻里、申请加入、邀请玩家。
- [x] 迁移成员、公告、角色调整。
- [x] 邻里公共组列表使用独立滚动区域。

完成标准：

- [x] 创建 / 加入 / 管理邻里的路径清楚。
- [x] 邻里页不再承载名片荣誉长列表。

---

## 5. 阶段 E：委托拆页

> 目标：把在线求助单从 `QuestView.vue` 拆出，但保留单人任务板原状。

### E0 建立在线委托页

- 部分完成：`/game/online/orders` 已建立在线委托模块壳，接入 `useCoopOrderStore` 摘要、独立刷新、目标玩家路由上下文和五个二级标签；完整发布、接单、交付、凭证与补偿写操作随 E1-E3 继续迁入，`QuestView.vue` 清理保留到迁移完成后处理。
- [x] 新增 `OnlineOrdersView.vue`。
- [x] 使用 `useCoopOrderStore` 作为主要数据源。
- [x] 模块内二级导航：
  - [x] 发布
  - [x] 可接
  - [x] 我的发布
  - [x] 我的接单
  - [x] 凭证与补偿
- [x] 默认进入“可接”或“我的接单”，但首页模块卡保持模块优先。

完成标准：

- [ ] 原在线求助单完整迁移。
- [ ] `QuestView.vue` 只保留单人主线、日常委托、特殊订单、经营提示。
- [ ] 任务页可提供“前往在线委托”的按钮。

### E1 发布页

- [x] 迁移标题、描述、类型、范围、奖励、截止时间。
- [x] 迁移单段 / 多段接力模式。
- [x] 多段阶段草稿使用清晰的卡片式编辑。

完成标准：

- [x] 可发布普通求助单。
- [x] 可发布多段接力单。
- [x] 表单在手机端不横向溢出。

### E2 接单与交付页

- [ ] 可接订单列表独立展示。
- [ ] 我的接单独立展示。
- [ ] 交付草稿输入、取消接单、确认交付保留。
- [ ] 声望摘要放在侧栏或顶部摘要，不挤占列表主路径。

完成标准：

- [ ] 玩家能快速找可接单。
- [ ] 玩家能快速处理自己已接的单。

### E3 凭证与补偿页

- [ ] 迁移我的 receipts。
- [ ] 迁移 compensation retry。
- [ ] 补偿失败原因清晰显示。

完成标准：

- [ ] 结算凭证不再混在发布和接单列表之间。
- [ ] 异常补偿有明确入口。

---

## 6. 阶段 F：节会拆页

> 目标：`FestivalView.vue` 目前承载世界事件、节会房间、远征房间等多块内容，需要拆成清晰子模块。

### F0 建立节会模块壳

- [ ] 新增 `OnlineFestivalView.vue`。
- [ ] 模块内二级导航：
  - [ ] 世界事件
  - [ ] 节会房间
  - [ ] 远征房间
  - [ ] 纪念记录
- [ ] 默认进入“世界事件”。

完成标准：

- [ ] 节会不再是一页里连续塞世界事件和房间系统。
- [ ] 远征房间可从节会模块内进入，也可保留原远征旧入口兼容。

### F1 世界事件页

- [ ] 迁移当前季节大事件。
- [ ] 迁移公共目标、世界事件列表、最近史册、世界纪年。
- [ ] 当前可贡献操作放在首屏。
- [ ] 长史册列表默认折叠或限制高度。

完成标准：

- [ ] 玩家能先看到当前世界事件和可提交贡献。
- [ ] 世界纪年不把当前操作推到很后面。

### F2 节会房间页

- [ ] 迁移节会房间创建、我的房间、邀请、可见房间、结算凭证。
- [ ] 房间创建表单和我的房间状态分栏展示。
- [ ] 手机端按“我的房间 -> 邀请 -> 创建 -> 可见房间”的顺序显示。

完成标准：

- [ ] 节会房间生命周期操作不丢。
- [ ] 房间列表不和世界事件混在一起。

### F3 远征房间页

- [ ] 将 `ExpeditionRoomView.vue` 纳入节会模块或在线模块子入口。
- [ ] 保留创建、邀请、ready、倒计时、断线、重连、结算、关闭。
- [ ] 保留事件卡、队伍资源、角色分工、回合日志。

完成标准：

- [ ] 原远征房间完整可用。
- [ ] 远征不再作为孤立散落入口。

---

## 7. 阶段 G：村社拆页

> 目标：把 `SocietyView.vue` 里的组织、成员、仓库、福利、史册、会议拆开。

### G0 建立村社模块壳

- [ ] 新增 `OnlineSocietyView.vue`。
- [ ] 模块内二级导航：
  - [ ] 总览
  - [ ] 成员
  - [ ] 仓库与福利
  - [ ] 公共建设
  - [ ] 提案
  - [ ] 史册
- [ ] 默认进入“总览”。

完成标准：

- [ ] 村社页不再从组织信息一路滑到会议提案。
- [ ] 村社未加入状态有清晰创建 / 申请入口。

### G1 总览页

- [ ] 展示我的村社摘要。
- [ ] 展示创建村社或加入村社入口。
- [ ] 展示公告摘要和当前身份。

完成标准：

- [ ] 未加入玩家知道下一步是创建还是申请。
- [ ] 已加入玩家知道当前村社状态。

### G2 成员页

- [ ] 迁移成员列表、邀请、申请处理、角色调整。
- [ ] 管理权限只在有权限时出现。

完成标准：

- [ ] 社长和管理者能完成成员治理。
- [ ] 普通成员不会看到无效管理控件。

### G3 仓库、建设、提案、史册页

- [ ] 仓库与福利独立页：公共仓库、福利等级、专属节会、装饰。
- [ ] 公共建设独立页：工程列表、贡献包、进度。
- [ ] 提案独立页：创建提案、投票、归档。
- [ ] 史册独立页：成立日期、职位历史、公共建设、节会参与、时间线。

完成标准：

- [ ] 高频治理操作不被史册长列表淹没。
- [ ] 史册可读，但不占据村社默认路径。

---

## 8. 阶段 H：旧入口兼容与迁移

> 目标：旧路由不让玩家迷路，但逐步引导到新在线中心。

### H0 旧路由处理

- [ ] `/game/social` 跳转或包装到 `/game/online/neighbor`。
- [ ] `/game/manor` 跳转或包装到 `/game/online/manor`。
- [ ] `/game/festival` 跳转或包装到 `/game/online/festival`。
- [ ] `/game/society` 跳转或包装到 `/game/online/society`。
- [ ] `/game/expedition` 跳转或包装到 `/game/online/festival?tab=expedition`。
- [ ] `/game/quest` 保留单人任务板，但在线委托区迁出后增加跳转入口。

完成标准：

- [ ] 旧链接不白屏。
- [ ] 旧入口不会再展示一整页旧长页。
- [ ] 玩家能感知功能迁移到了在线中心。

### H1 内部跳转上下文

- [ ] 好友访问庄园继续带 `target_username` 或 `target_save_id`。
- [ ] 邮箱写信、送礼、房间邀请、协作委托跳转继续带目标玩家上下文。
- [ ] 庄园访客模式继续能直接打开目标玩家庄园。
- [ ] 在线委托从好友入口跳转时保留 scope / target hints。

完成标准：

- [ ] 原“从好友条目跳到庄园 / 邮箱 / 房间 / 委托”的链路不丢参数。
- [ ] 拆页后不出现只能进入模块首页、不能进入具体目标的退化。

---

## 9. 阶段 I：通用 UI 与交互规则

> 目标：拆页不是换一批长页，必须形成可复用的在线模块结构。

### I0 在线模块壳组件

- [ ] 抽一个轻量在线模块布局组件，建议命名 `OnlineModuleShell.vue`。
- [ ] 支持标题、摘要、返回在线中心、刷新按钮、二级导航。
- [ ] 二级导航在桌面端横向排列，手机端允许横向滚动或换行。
- [ ] 不把模块内容包进多层卡片。

完成标准：

- [ ] 五个模块使用一致的导航和返回方式。
- [ ] 手机端标签不会遮挡内容。

### I1 在线模块卡组件

- [ ] 抽 `OnlineModuleCard.vue` 或同等组件。
- [ ] 支持模块名、摘要、状态、统计、主要按钮。
- [ ] 卡片高度稳定，避免不同模块文字多少导致布局跳动严重。

完成标准：

- [ ] 在线中心首页视觉稳定。
- [ ] 模块卡不写成长说明，不承担教程职能。

### I2 列表与表单收口

- [ ] 长列表使用独立滚动区或分页/折叠。
- [ ] 表单在对应功能页首屏出现，不夹在历史记录中间。
- [ ] 历史、史册、荣誉、纪念品类内容默认不是第一屏主角。
- [ ] 所有输入框继续复用已有 `online-input / online-select / online-textarea / online-action-row`。

完成标准：

- [ ] 手机端没有横向溢出。
- [ ] 主要操作按钮不被长列表挤到页面末尾。

---

## 10. 阶段 J：验证与回归

> 目标：拆页完成后，证明不是只换了路由，而是核心联机功能仍可用。

### J0 构建与静态检查

- [ ] 运行 `npm --prefix taoyuan-main run build`。
- [ ] 如果项目已有 type-check 脚本，运行对应 type-check。
- [ ] 检查是否有未使用 import、错误路由名、丢失组件。

完成标准：

- [ ] 构建通过。
- [ ] 没有明显 TS/Vue 模板错误。

### J1 手动页面验收

- [ ] 打开 `/game/online`。
- [ ] 从在线中心进入庄园、邻里、委托、节会、村社。
- [ ] 从每个模块返回在线中心。
- [ ] 直接访问五个子入口。
- [ ] 访问旧入口，确认跳转或包装正常。

完成标准：

- [ ] 所有入口不白屏。
- [ ] 返回路径清楚。
- [ ] 页面不再需要从头滑到底寻找模块。

### J2 核心功能验收

- [ ] 庄园：刷新快照、留言、回复、来访记录、主题保存。
- [ ] 邻里：名片保存、好友入口、邻里创建/申请/邀请。
- [ ] 委托：发布求助单、接单、交付、确认、补偿重试入口。
- [ ] 节会：世界事件贡献、节会房间创建/邀请/ready/结算。
- [ ] 远征：创建/邀请/ready/回合动作/结算。
- [ ] 村社：创建或加入、成员处理、仓库贡献、提案投票。

完成标准：

- [ ] 原有主路径功能没有因为拆页丢失。
- [ ] 失败状态和加载状态都能显示。

### J3 移动端验收

- [ ] 390x844 视口打开在线中心。
- [ ] 360x780 视口打开在线中心。
- [ ] 检查模块卡文字是否溢出。
- [ ] 检查二级导航是否可操作。
- [ ] 检查主要表单和按钮是否上下排列正常。

完成标准：

- [ ] 手机端首屏能理解五大模块。
- [ ] 没有横向滚动条。
- [ ] 重要按钮触达高度足够。

### J4 自动化建议

- [ ] 补充移动端 smoke：从移动地图进入在线中心。
- [ ] 补充路由 smoke：在线中心进入五大模块。
- [ ] 补充旧入口兼容 smoke：旧 `social / manor / festival / society / expedition` 入口能落到新结构。
- [ ] 保留现有联机 smoke，不把业务验证只替换成页面跳转验证。

完成标准：

- [ ] 新 smoke 覆盖“入口拆分”。
- [ ] 旧 smoke 覆盖“业务仍可用”。

---

## 11. 优先查看文件

- `taoyuan-main/src/router/index.ts`
- `taoyuan-main/src/composables/useNavigation.ts`
- `taoyuan-main/src/data/timeConstants.ts`
- `taoyuan-main/src/components/game/MobileMapMenu.vue`
- `taoyuan-main/src/views/game/SocialView.vue`
- `taoyuan-main/src/views/game/ManorView.vue`
- `taoyuan-main/src/views/game/QuestView.vue`
- `taoyuan-main/src/views/game/FestivalView.vue`
- `taoyuan-main/src/views/game/ExpeditionRoomView.vue`
- `taoyuan-main/src/views/game/SocietyView.vue`
- `taoyuan-main/src/stores/useSocialStore.ts`
- `taoyuan-main/src/stores/useManorStore.ts`
- `taoyuan-main/src/stores/useCoopOrderStore.ts`
- `taoyuan-main/src/stores/useFestivalRoomStore.ts`
- `taoyuan-main/src/stores/useExpeditionRoomStore.ts`
- `taoyuan-main/src/stores/useSocietyStore.ts`
- `taoyuan-main/src/app.css`
- `CHANGELOG.md`
- `taoyuan-main/CHANGELOG.md`

---

## 12. 不做项

- [ ] 本轮不改服务端 API。
- [ ] 本轮不改结算、奖励、补偿和审计规则。
- [ ] 本轮不把 `/hall` 交流大厅并入游戏内长页。
- [ ] 本轮不重做视觉主题，只沿用现有像素风、`game-panel`、`online-*` 表单类。
- [ ] 本轮不删除旧路由，先兼容迁移。
