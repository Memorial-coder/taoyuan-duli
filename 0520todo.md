# 0520 TODO：联机三条主线执行拆解

> 目标：把 `0520联机WebSocket.md`、`0520联机输入框UI修改计划.md`、`0520联机小游戏修改计划.md` 拆成可以逐步执行的任务清单。  
> 本轮不包含 `0520全量审查计划.md`。  
> 原则：先保留现有联机底座，再收口输入体验，最后做小游戏体感升级；每个大阶段都要有最小验证。

---

## 0. 总执行原则

- [ ] 每次开工前先阅读对应目标文档与当前实现。
- [ ] 不跳过依赖任务，除非任务正文明确允许并行。
- [ ] 不一次改全部联机模板。
- [ ] 不把 `0520全量审查计划.md` 纳入本轮执行范围。
- [ ] 每完成一个大阶段，至少做一轮最小验证。
- [ ] 如本轮涉及代码变更，回写 `CHANGELOG.md` 与 `taoyuan-main/CHANGELOG.md`。

---

## 1. 主线 A：联机 WebSocket + 好友系统

> 目标：先把“人”的身份、好友关系、在线状态、通知和邀请统一到一套底座里。

### A0 身份语义先对齐

- [x] 明确每个存档都有固定数字 ID。（完成：服务端存档槽已新增 `SaveIdentity` 注册表，`/api/taoyuan/save/slots` 与 `/api/taoyuan/save/:slot` 读取旧服务端存档时会补发 9 位数字 ID 并回写；本地档 / 导入档切到服务端保存或导入后，也会获得服务端公开 ID，并已由主菜单、存档管理器、好友驿站、`qa:online-smoke` 和 `qa:online-regression-live-smoke` 验证。产品语义已明确：纯本地未同步档不预生成公开数字 ID，只有进入服务端存档槽后才由服务端补发固定 ID。）
- [x] 明确昵称可改、ID 不可改。（完成：服务端按账号 + 槽位锁定数字 ID，保存时会覆盖客户端篡改的 `onlineIdentity.save_id`，昵称只作为 `nickname_snapshot` 更新；好友驿站和公开名片页已展示当前运行存档 ID，并在公开名片页提示昵称 / 公开名片可改、存档 ID 由服务端生成且保持固定；`qa:online-smoke` 覆盖保存响应嵌入身份、旧档读回补发身份和客户端篡改 ID 后仍保持原 ID。）
- [x] 明确好友关系按存档级，不再只按账号级理解。（完成：服务端申请记录与接受后生成的 `friendships` 已保存双方 `save_id / save_slot`，关系列表会按当前活动存档优先过滤存档级申请与好友；删除、拉黑、解除拉黑服务端链路已按活动存档身份校验，好友驿站前端主操作也已只暴露按 `target_save_id` 申请 / 拉黑 / 解除拉黑路径；远征 / 节会 / 村社邀请写路已能提交 `target_save_id`，邮箱玩家来信与礼物包裹写路会优先接收 `target_save_id`，在服务端解析到现有收件账号，并把 `target_save_id / target_save_slot` 持久化到投递记录；庄园目标页现在会读取路由 `target_save_id` 拉取公开庄园快照，留言 / 访问写路也会在服务端按存档 ID 解析到庄园主人账号，并把 `target_save_id / target_save_slot` 持久化到留言 / 访问记录；活动房间邀请现在也会持久化 `target_save_id / target_save_slot`，并在加入时校验当前活动存档必须匹配受邀存档；协作单创建页会读取路由 `target_save_id` 并在发布时提交给服务端，服务端按当前活动存档与目标存档的好友关系校验后生成定向协作单，目标存档玩家可见并可接单，非好友存档 ID 会被拒绝；村社申请、邀请、成员快照和角色履历也已写入存档 ID / 槽位，收到邀请 / 待处理申请会按当前活动存档过滤，自我接受邀请时会校验受邀存档匹配；职位调整和社长转移履历也会保留成员 / 继任者存档身份；村社当前成员判定与待处理去重会读取真实 active-slot，在当前活动存档 ID 存在时优先按 `save_id` 匹配，同账号 slot 1 不再继承 slot 0 村社身份；旧 username-only 村社记录只在没有多存档歧义或唯一可映射时兜底，避免历史数据跨存档串用，并由 `qa:online-smoke` 覆盖。）
- [x] 明确旧档第一次读档时自动补发 ID。（完成：旧服务端存档读回会自动注入 `onlineIdentity`，并已在 `server/scripts/qa-online-smoke.mjs` 加入补发与不可篡改回归；本地档 / 云存档 / 导入档进入服务端身份体系的 UI 闭环也已补齐，`qa:online-regression-live-smoke` 覆盖无 `onlineIdentity` 导入档写入服务端空槽、服务端 raw 回写 ID、按 ID 搜索和好友驿站显示同一 ID。）

### A1 拆出三类核心数据

- [x] `SaveIdentity`（完成：服务端已有 `taoyuan_save_identities.json` 注册表、读写注入和按数字 ID 搜索公开名片 API；主菜单、存档管理器、好友驿站和公开名片页已展示当前运行存档 ID；纯本地未同步档不预生成公开 ID，服务端槽位写入 / 读取旧档时才补发并回写固定 ID；`qa:online-smoke` 覆盖读回补发、不可篡改、旧 wrapped 服务端存档打开不丢数据和按数字 ID 搜索公开名片。）
- [x] `FriendGraph`（完成：好友申请记录与接受后生成的好友关系本体已保留 `from_save_id / to_save_id / from_save_slot / to_save_slot` 或对应的 `save_id_a / save_id_b` 字段，关系列表会回显双方存档身份；删除好友与拉黑 / 解除拉黑的服务端链路已按存档 ID 运行；前端 store / API 已移除 username-first 好友申请与拉黑入口，好友驿站以 `target_save_id` 为主路径；远征 / 节会 / 村社邀请路由会读取好友上下文里的 `target_save_id`，提交时也优先传给服务端解析；邮箱目标页会读取路由 `target_save_id`，玩家来信 / 礼物包裹提交时也会携带并持久化 `target_save_id / target_save_slot`；庄园目标页会用 `target_save_id` 读取快照并在留言 / 访问写路中继续传递该 ID，留言 / 访问记录也会持久化 `target_save_id / target_save_slot`；活动房间邀请会持久化 `target_save_id / target_save_slot`，加入时校验当前活动存档必须匹配受邀存档；协作单创建会保留目标存档 ID，服务端通过 `isSaveFriendWith()` 校验存档级好友关系后写入 `target_save_id / target_save_slot`，并已用 smoke 覆盖非好友目标拒绝；村社申请 / 邀请 / 接受 / 拒绝链路也已持久化并回显 `target_save_id / target_save_slot`，成员快照、入社履历、职位调整履历和社长转移履历会记录对应存档身份；村社成员归属 / 待处理去重已修复 active-slot 读取，当前活动存档存在固定 ID 时优先按存档 ID 隔离；旧 username-only 村社记录只在没有多存档歧义或唯一可映射时兜底。）
- [x] `RealtimeState`（完成：服务端已有内存态实时连接注册表，可按账号 / 当前活动存档记录连接、在线状态和连接快照；离线实时通知会持久写入 `taoyuan_realtime_notifications.json`，下次 WebSocket ready 后补发并等待客户端 ACK 清理；在线 / 离线 presence 会轻量持久写入 `taoyuan_realtime_presence.json`，记录账号、存档 ID、槽位、连接时间、最近活跃和最近离线时间；活动房间订阅范围会写入 `taoyuan_realtime_room_subscriptions.json`，记录 domain、room_id、订阅用户名、房间状态、成员数、待处理邀请数和最近动作；`/api/admin/taoyuan/realtime` 已接入管理端只读观测，可查看连接数、在线账号 / 存档数、连接身份摘要、最近活动时间、离线补发队列聚合、持久 presence 摘要和房间订阅摘要；`qa:realtime-smoke` 已覆盖在线 presence 持久可见、断开后 offline 状态持久可见、活动房间订阅快照持久可见，且不泄露通知 payload 或完整 room payload。）
- [x] 服务端成为好友关系与实时状态权威来源。（完成：好友关系由既有 HTTP 服务端路由权威写入，WebSocket 只广播事件；实时在线状态由服务端连接表生成，最近在线 / 离线 presence 已持久化为后台观测快照；活动房间邀请、建房、加入、离开、准备、倒计时、断线、重连、动作、结算和关闭的成功写路已由服务端广播 `activity.room.*` 事件，并记录房间订阅范围摘要；好友 / 房间实时通知离线时会进入服务端补发队列；管理端可通过 `/api/admin/taoyuan/realtime` 只读观察连接快照、持久 presence 摘要、房间订阅摘要和补发队列摘要。）
- [ ] 客户端只提交意图，不直接写最终结果。（部分完成：本轮 WebSocket 只接受 ping / presence.snapshot 等轻消息，好友申请 / 接受 / 拒绝 / 删除和活动房间邀请 / 状态变化仍走服务端 HTTP 权威结算；房间事件 payload 已标记 `refresh_required`，前端收到后只静默刷新节会 / 远征权威接口。剩余：后续更多联机页面接入 realtime 时还要继续保持这一边界。）

### A2 做好友基础链路

- [x] 按 ID 搜索玩家。（完成：服务端已新增 `/api/taoyuan/online/social/player-search?save_id=...` 并有 smoke 回归；前端 `onlineProfileApi / useSocialStore / FriendStationView.vue` 已接入存档 ID 搜索与结果名片。）
- [x] 发送好友申请。（完成：服务端 `/api/taoyuan/online/social/friend-requests` 已支持 `target_save_id`，好友驿站可从搜索结果按存档 ID 发送申请。）
- [x] 接受 / 拒绝好友申请。（完成：服务端接受申请时写入带双方存档 ID 的好友关系；好友驿站已接入收到申请的接受 / 拒绝按钮。）
- [x] 好友列表。（完成：`/api/taoyuan/online/social/relationships` 已回显存档身份字段；好友驿站已展示好友列表、存档 ID、最近互动和基础互动入口。）
- [x] 删除好友。（完成：服务端已有 `DELETE /api/taoyuan/online/social/friends/:friendshipId`；好友驿站已接入删除按钮与二次确认。）
- [x] 拉黑 / 解除拉黑。（完成：服务端拉黑 / 解除拉黑支持 `target_save_id`；好友驿站已接入搜索结果拉黑、好友拉黑和拉黑列表解除。）
- [x] 好友名片与互动入口。（完成：好友驿站已显示好友名片摘要，并提供访问庄园、写信、送礼、邀请进房、协作跳转、删除与拉黑入口；庄园 / 邮箱 / 礼物包裹 / 远征房间 / 协作单会接收目标玩家 username 和存档 ID 上下文，不自动提交动作。）

### A3 好友驿站独立为联机主导航入口

- [x] 好友功能不再放在地图页面里，改为独立路由 `friend-station`。（完成：`FriendStationView.vue` 已接管原好友驿站内容，`RegionMapView.vue` 不再承载这块 UI。）
- [x] 在 `taoyuan-main/src/views/game/FriendStationView.vue` 做独立好友页。（完成：好友驿站已从 `RegionMapView.vue` 抽出，保留原 `region-social-*` smoke 锚点与原业务逻辑。）
- [x] 联机主导航需要有清晰入口，例如移动端地图菜单里的独立按钮。（完成：`MobileMapMenu.vue` 已新增 `friend-station`，并放进“联机主导航”分组。）
- [x] 好友页至少包含：我的存档 ID、复制 ID、按 ID 搜索玩家、好友申请、好友列表、拉黑列表、最近互动。（完成：好友驿站页面已包含这些基础项；我的 ID 会优先取当前存档在线身份，并可从关系概览回退识别。）
- [x] 好友页里每个好友条目提供清晰互动入口：访问庄园、写信、送礼、邀请进房、协作跳转。（完成：好友条目已补齐五类互动入口；访问庄园会读取目标玩家公开庄园，写信 / 送礼会预填邮箱目标，邀请进房会预填远征房间邀请对象，协作跳转会切到好友范围并写入目标玩家上下文。）
- [x] `SocialView.vue` 后续只保留名片、邻里、公开资料等社会层内容，好友主操作迁移到好友驿站独立页。（完成：旧的用户名加好友、拉黑、申请接受 / 拒绝、好友列表操作和解除拉黑主操作已从社交页移除；社交页只保留公开名片、邻里、订阅内容和前往好友驿站的入口。）
- [x] 好友页移动端必须可用，不能依赖宽屏多列。（完成：页面已使用单列优先、按钮可换行、输入框最小高度 42px；好友申请处理、好友互动和解除拉黑按钮已补 `min-h-[32px]` 触控高度；`qa:mobile-ui-smoke` 已新增 390x844 / 360x780 好友驿站场景，覆盖存档 ID 搜索、申请入口、好友条目、送礼 / 邀请进房入口、最近互动、拉黑列表和横向溢出断言，并生成 `docs/ui-smoke-2026-04-26/22-region-social-friend-panel-mobile-390x844.png` 与 `23-region-social-friend-panel-mobile-360x780.png`。）

### A4 接入实时通讯

- [x] 先按原生 WebSocket 规划，不优先引入 `socket.io`。（完成：服务端已在 `/api/taoyuan/online/realtime` 接入原生 WebSocket upgrade，没有新增 `socket.io` 依赖，并复用签名 `taoyuan.sid` 与现有会话存储鉴权。）
- [ ] WebSocket 承担在线状态、好友申请推送、邀请、房间状态变化。（部分完成：服务端已投递 `realtime.ready`、`presence.online/offline/snapshot`、`realtime.heartbeat/pong`、好友关系事件，以及 `activity.room.invited / activity.room.updated`；房间事件覆盖远征 / 节会建房、邀请、加入、离开、准备、倒计时、断线、重连、动作、结算、关闭，并已由后端 realtime smoke 验证远征建房 / 邀请 / 加入推送；房间邀请 payload 现在会携带 `target_save_id / target_save_slot`，方便前端和审计端区分同账号多存档的受邀对象；房间订阅范围也会以摘要形式持久写入 `taoyuan_realtime_room_subscriptions.json`，供管理端排查。前端 realtime store 已在游戏布局生命周期内连接 WebSocket，收到好友事件会静默刷新关系概览，收到房间事件会按 `domain` 静默刷新节会 / 远征房间概览；协作单参与态变化、村社申请 / 邀请 / 审核结果、村社公共建设 / 公共仓共享进度、四季大事件贡献，以及邻里寄售挂单 / 购买 / 取消 / 回收，也已接入 `notification.created` + 静默刷新语义。离线好友 / 房间 / 协作单 / 村社 membership / 邻里寄售事件会持久排队、ready 后补发、客户端 ACK 后清理；四季大事件贡献通知目前只投递当前在线连接，避免全服事件刷满离线队列；邻里寄售通知会投递同邻里成员，前端再静默重读寄售概览、交换账本和官方调控快照；真实浏览器 smoke 已验证远征邀请无需手动刷新即可出现在邀请列表。剩余：其他联机页面的实时消费还未整体收口。）
- [ ] SSE / 轮询只保留轻通知与刷新。（部分完成：本轮未新增 SSE，好友写路仍以 HTTP 为权威，WebSocket 只做投递；好友驿站、协作单列表、四季大事件概览、邻里寄售概览、交换账本和官方调控概览已能由 WS 事件推动静默刷新。剩余：其他联机页面的轮询 / 手动刷新策略还未整体收口。）

### A5 断线、未读、回写

- [x] 断线重连后恢复在线状态与好友通知。（完成：前端 realtime store 已加入退避重连、客户端 ping、重连 ready 后重新拉取 presence snapshot 并静默刷新好友关系；服务端会把离线好友 / 房间事件持久排队，下次连接 ready 后补发，客户端收到带 `queued_event_id` 的补发帧后回 ACK 清理；`qa:realtime-smoke` 已覆盖离线好友申请补发、ACK 清理和再次连接不重复补发；`qa:region-friend-panel-live-smoke` 现在会在真实浏览器中主动关闭 realtime socket，等待自动重连后验证 `realtime.ready`、`presence.snapshot` 和断线窗口内好友申请刷新。）
- [ ] 未读通知可补发。（部分完成：好友申请、接受、拒绝、删除、活动房间邀请 / 状态变化、协作单定向创建与参与态变化、村社公告与村社申请 / 邀请 / 审核结果、玩家来信、礼物包裹、系统邮件、后台即时邮件、定时 campaign 到期发送、大厅回复通知、大厅官方公告帖通知，已支持离线入队、ready 补发和 ACK 清理；庄园留言墙的留言 / 回复通知也已接入 `notification.created`、离线补发和 ACK 清理，且只投递摘要不投递正文；庄园留言通知现在即使写入请求只携带 `target_save_id`，也会用解析后的庄园主人账号投递在线通知和离线补发；定向协作单创建通知会投递给目标存档账号，`qa:realtime-smoke` 已覆盖在线目标好友收到 `order_created` 摘要，以及离线目标好友 ready 补发、ACK 清理和重连不重复补发；玩家来信已由真实浏览器 smoke 验证收件人停在邮箱页时无需手动刷新即可看到新邮件，玩家来信 / 礼物包裹现在即使请求只携带 `target_save_id` 也会用解析出的收件账号投递 realtime 通知；大厅回复也已在 realtime smoke 与真实浏览器回归里验证；邻里寄售挂单 / 成交摘要现在会投递给同邻里成员，在线即时收到、离线进入补发队列，并已由 `qa:realtime-smoke` 覆盖离线补发、ACK 清理和重连不重复补发；定时 campaign、官方公告帖、村社公告、村社 membership、协作单参与态变化、庄园留言和邻里寄售已由 `qa:realtime-smoke` 验证离线补发、ACK 清理和重连不重复补发。村社公共建设 / 公共仓共享进度本轮先只做在线摘要通知，不进入离线补发队列，避免高频共建动作把 queue 刷满。管理端现在可通过 `/api/admin/taoyuan/realtime` 观察补发队列总量、按玩家聚合的 pending 数和事件类型分布，且不泄露通知正文 payload。剩余：更多高频共建类通知是否进入补发队列仍需按频率和队列压力逐项判断。）
- [ ] 事件只负责投递，不负责最终结算。（部分完成：当前好友事件只触发前端刷新服务端权威关系概览，不直接修改好友结算态；房间邀请和房间状态事件也只在 HTTP 写路成功后投递 `refresh_required` 通知，前端消费时只调用节会 / 远征概览接口静默刷新，不直接套用 payload 结算；邮箱与大厅 `notification.created` 也只触发权威列表 / 详情重读，不直接改邮件、回复、公告或奖励状态；四季大事件贡献通知只带事件 / 贡献摘要，前端收到后静默重读世界事件概览，不直接套用贡献结果；村社公共建设 / 公共仓通知也只带村社、项目 / 仓库和最近一次动作摘要，前端仍旧静默重读 `/api/taoyuan/online/societies` 权威概览；邻里寄售通知也只带挂单状态与邻里范围摘要，前端收到后静默重读 `/api/taoyuan/exchange-station/neighbors/consignments`、`/ledger` 和 `/governance` 三个权威接口，不直接套用挂单、成交或资金结果；离线补发队列只重放事件通知并由客户端 ACK 清理，不执行跨玩家结算。系统邮件、后台即时邮件和定时 campaign 通知也只在邮件保存后投递新增 delivery，大厅官方公告也只在帖子保存后投递帖子摘要，不改奖励、领取或跨玩家结算逻辑。剩余：后续把更多通知类型接入队列时仍需保持 delivery-only 边界。）
- [x] 旧档兼容本地档、云存档、导入档。（完成：服务端存档读 / 写路径已经会给旧 payload 自动注入固定数字 ID；`POST /api/taoyuan/save/:slot` 会返回注入身份后的权威 raw，前端服务端同步成功后会用该 raw 刷新当前运行时 `currentOnlineIdentity`，并在主菜单和存档管理器里明确提示本地 / 导入档切到服务端后会获得公开 ID。`qa:online-regression-live-smoke` 已新增真实浏览器回归：通过主菜单把不含 `onlineIdentity` 的导入档写入服务端空槽，验证服务端 raw 已回写 ID、`player-search` 可按该 ID 找到对应槽位，且载入导入槽后行旅图好友驿站显示同一个 ID。纯本地档在未同步到服务端前不生成公开数字 ID，作为本地模式的设计约束保留。）
- [x] 可写时把补发结果回写到存档元数据。（完成：服务端可写槽位会把补发后的 `onlineIdentity` 写回加密存档 raw，并在保存响应中返回权威 raw；`useSaveStore` 会在当前运行会话对应服务端槽位时从权威 raw 更新运行时身份，主菜单和存档管理器也会据此提示公开 ID 已随服务端保存写回。`qa:online-smoke` 已验证保存响应包含嵌入的身份，且后续覆盖客户端篡改时 ID 不变；`qa:online-regression-live-smoke` 进一步验证导入档写入服务端空槽后，后端 raw、搜索接口和浏览器好友驿站三处使用同一服务端补发 ID。）

### A6 入口嵌入

- [ ] 把好友系统接进庄园、邮箱、节会、村社、协作单。（部分完成：好友驿站已能把好友上下文带入庄园 / 邮箱 / 礼物包裹 / 远征房间 / 节会 / 村社 / 协作单；远征 / 节会 / 村社页面会读取路由里的 `target_save_id` 并在邀请提交时优先发送存档 ID，username 仅作为兼容预填和旧结构落点，不自动提交动作；村社申请 / 邀请 / 接受 / 拒绝链路会把目标存档 ID / 槽位写入申请单、成员快照和角色履历，并在收到邀请 / 待处理申请概览里按当前活动存档过滤；邮箱写信 / 送礼会读取路由里的 `target_save_id` 并在提交玩家来信 / 礼物包裹时传给服务端解析，realtime 通知也使用解析后的收件账号；庄园页会读取路由 `target_save_id` 获取目标快照，留言 / 访问提交也会携带存档 ID 并由服务端解析到庄园主人账号；协作单创建页会读取并展示目标存档 ID，发布时携带 `target_save_id`，服务端按存档级好友关系校验后落定向协作单，目标存档玩家可见并可接单。协作单参与态变化也已接入 realtime 通知和列表静默刷新。剩余：更多联机玩法仍需继续复用同一身份与通知底座。）
- [ ] 让现有联机玩法复用同一套身份与通知底座。（部分完成：远征 / 节会房间已接入 realtime 房间事件投递和静默刷新；地图页好友入口已把庄园、邮箱、远征、节会、村社、协作单串到同一套好友身份上下文，庄园留言墙也已接入 realtime 通知和离线补发；邮箱玩家来信、礼物包裹、系统邮件、后台即时邮件、定时 campaign、大厅回复、大厅官方公告帖、村社申请 / 邀请 / 审核结果、村社公共建设 / 公共仓共享进度、四季大事件贡献、邻里寄售挂单 / 成交摘要，以及协作单定向创建 / 参与态变化都已接入实时通知；邮箱玩家来信 / 礼物包裹、庄园留言 / 访问、村社申请 / 邀请和协作单定向发布写路已从好友存档 ID 解析到既有账号结构，其中邮箱玩家来信 / 礼物包裹、庄园留言 / 访问和村社 membership 申请单会持久化目标存档 ID / 槽位。剩余：更多非邮箱通知和旧轮询策略还未整体收口。）

### A7 验收

- [x] 旧档可直接打开，不丢数据。（完成：`qa:online-smoke` 已新增旧 wrapped 服务端存档读取回归，脚本会直接写入无 `onlineIdentity` 的旧槽位，再通过 `/api/taoyuan/save/1` 打开，验证自动补发固定存档 ID、玩家金钱、背包物品、旧自定义字段和 meta 均保留；同时验证 `/api/taoyuan/save/slots` 能看到回写后的旧槽位，旧槽位可设为当前槽并再恢复到原槽。验证通过 `node --check server/scripts/qa-online-smoke.mjs` 与 `npm --prefix server run qa:online-smoke`。）
- [x] 能通过数字 ID 搜索并加好友。（完成：服务端 smoke、前端 type-check / build、移动端浏览器 smoke 均已覆盖基础链路；新增 `qa:region-friend-panel-live-smoke` 已在真实后端会话下通过浏览器点击存档 ID 搜索结果并提交好友申请。）
- [x] 好友申请、通过 / 拒绝、删除都可用。（完成：新增 `qa:region-friend-panel-live-smoke` 已在真实后端会话下点击接受申请、拒绝申请和删除好友，并校验后端关系列表同步更新。）
- [x] 联机主导航里存在独立好友驿站，玩家能从移动端菜单直接进入。
- [x] 好友面板能完成搜索、申请、处理申请、查看列表、拉黑、删除、互动跳转。（完成：代码与构建验证、移动端浏览器 smoke 已覆盖基础可用性；新增真实后端浏览器 smoke 已逐按钮点击搜索、申请、接受、拒绝、删除好友、拉黑现有好友、搜索后拉黑、解除拉黑，并验证庄园 / 写信 / 送礼 / 远征邀请 / 节会邀请 / 村社邀请 / 协作跳转携带目标上下文，其中节会和村社邀请输入会预填目标用户名。）
- [x] WebSocket 断线重连后能恢复通知与在线状态。（完成：前端已有退避重连、ready 后 presence snapshot、好友关系静默刷新和房间事件后的权威概览刷新；服务端 realtime smoke 已验证离线好友通知补发、ACK 清理和重连不重复补发；真实浏览器 smoke 已验证主动关闭 WebSocket 后页面会重连、重新收到 ready / presence snapshot，并刷新断线窗口内产生的好友申请。）
- [ ] 现有云存档、邮箱、大厅、单机玩法不被破坏。（部分完成：已通过 `type-check`、`build`、`qa:realtime-smoke`、`qa:region-friend-panel-live-smoke`、`qa:online-smoke` 与 `qa:online-regression-live-smoke`；其中在线回归烟测覆盖服务端存档载入、导入档写入服务端后补发公开 ID、realtime WebSocket、服务端快速保存、邮箱奖励领取入账、玩家来信实时到达后邮箱页自动刷新、大厅发帖与回复以及大厅回复实时通知刷新；`qa:online-smoke` 现在也覆盖玩家来信 / 礼物包裹按目标存档 ID 写入、投递记录持久化目标存档字段、收件箱 / 发件箱回显目标存档字段、庄园快照、庄园留言和庄园访问按目标存档 ID 写入并返回解析后的账号，协作单按目标存档 ID 发布、强制 friends scope、目标存档可见、目标字段回显、目标存档接单和非好友目标拒绝，同账号不同服务端存档不会继承村社成员身份的 active-slot 隔离，以及同账号多存档时旧 username-only 村社记录不会被任意存档继承；`qa:realtime-smoke` 也覆盖按目标存档 ID 发信并在通知摘要里携带目标存档字段、庄园留言、定向协作单创建、四季大事件贡献、邻里寄售挂单 / 成交，以及村社公共建设 / 公共仓共享进度时的在线通知，并覆盖邮件离线补发与定向协作单创建通知离线补发、ACK 清理和重连不重复补发；本轮旧村社 username 兜底收口已复跑 `node --check server/src/taoyuanSocietyRuntime.js`、`node --check server/scripts/qa-online-smoke.mjs`、`npm --prefix server run qa:online-smoke` 与 `npm --prefix server run qa:realtime-smoke` 验证。剩余：单机玩法与更广的云存档 / 邮箱 / 大厅组合路径还未做全量浏览器回归。）

### A8 优先看这些文件

  - `server/src/index.js`
  - `server/src/routes/api.js`
  - `server/src/`
  - `taoyuan-main/src/views/GameLayout.vue`
  - `taoyuan-main/src/components/game/MobileMapMenu.vue`
  - `taoyuan-main/src/views/game/FriendStationView.vue`
  - `taoyuan-main/src/views/game/RegionMapView.vue`
  - `taoyuan-main/src/stores/useSocialStore.ts`
  - `taoyuan-main/src/utils/onlineProfileApi.ts`
  - `taoyuan-main/src/utils/`
  - `taoyuan-main/src/stores/`
  - `taoyuan-main/src/views/game/`

---

## 2. 主线 B：联机输入框 UI 修改

> 目标：把联机相关输入框从“挤、窄、扁”收口成统一、舒展、可反复使用的表单体验。

### B0 先定统一样式口径

- [x] 在 `taoyuan-main/src/app.css` 补统一联机表单类。（完成：已在 `taoyuan-main/src/app.css` 新增 `online-field` 等基础类，沿用现有 `--color-accent / --color-bg / --color-panel / --color-text / --color-muted / --color-danger / --font-game` 设计 token，不引入新主题色或新依赖。）
- [x] 统一 `online-field` / `online-field-label` / `online-input` / `online-select` / `online-textarea` / `online-action-row` / `online-action-buttons`。（完成：上述类全部到位，并补 `online-field--inline / online-field-hint / online-field-error / online-action-btn / online-action-btn--primary / online-action-btn--danger / online-action-btn--icon / online-action-btn--compact / online-action-buttons--compact` 辅助类，覆盖 42px 主操作与 32px 紧凑面板两档高度。）
- [x] 普通输入框最小高度定到 42px。（完成：`.online-input, .online-select` 与 `.online-action-btn` 默认 `min-height: 42px`，便于和 textarea 区分。）
- [x] 高频输入框与按钮高度对齐。（完成：`.online-action-buttons > .online-action-btn` 与 `.online-input` 同为 42px，桌面端排成同一基线。）
- [x] textarea 默认高度定到 96px。（完成：`.online-textarea` 默认 `min-height: 96px` 并允许垂直 resize。）
- [x] 桌面端输入框最小可用宽度不低于 240px。（完成：≥768px 媒体查询下 `.online-input / .online-select / .online-textarea` 至少 240px，避免被三列结构压扁；`.online-action-row > .online-input/.online-select` 使用 `flex: 1 1 240px`。）
- [x] 移动端操作行自动换行。（完成：≤639px 时 `.online-action-row` 切回纵向、按钮和输入框各占满宽，`.online-action-buttons` 也按需 100% 铺满；中间宽度由 `flex-wrap: wrap` 自然换行。）

### B1 先改高频入口

- [x] `FriendStationView.vue`：好友驿站里的 ID 搜索、加好友、拉黑、好友申请处理。（完成：好友驿站的刷新、复制 ID、搜索结果申请 / 拉黑、收到申请接受 / 拒绝、好友条目庄园 / 写信 / 送礼 / 邀请进房 / 节会 / 村社 / 协作、删除 / 拉黑，以及拉黑列表解除都已迁移到 `online-action-btn--compact`，保留原有事件、禁用条件和 `data-testid`，不改业务逻辑。）
- [x] `SocialView.vue`：邻里邀请、名片设置。（完成：名片设置的庄园名、公开称号、邻里身份、展示主题、公开状态和公开介绍已迁到 `online-input / online-select / online-textarea`，邻里邀请输入也改成 `online-action-row + online-input`，并把邀请 / 创建邻里按钮收进统一按钮类。）
- [x] `ExpeditionRoomView.vue`：房间标题、邀请玩家。（完成：创建房间区的模板选择、玩法模板选择、房间标题和创建按钮已迁到统一表单 / 按钮类；邀请玩家行改为 `online-action-row + online-input + online-action-btn`，窄屏按统一规则换行，不改房间创建或邀请逻辑。）
- [x] `FestivalView.vue`：节会房间标题、邀请输入。（完成：创建节会房间区的房型选择、玩法模板选择、房间标题和创建按钮已迁到统一表单 / 按钮类；邀请玩家行改为 `online-action-row + online-input + online-action-btn`，和远征房间保持同一套响应式规格。）
- [x] `SocietyView.vue`：村社邀请、提案标题、归档备注。（完成：村社邀请输入行已迁到 `online-action-row + online-input + online-action-btn`；提案标题、类型、摘要分别迁到 `online-input / online-select / online-textarea`；归档备注输入和归档按钮也改用统一联机表单 / 按钮类。本轮只改 UI class，不改村社邀请、提案发起、投票或归档业务逻辑。）

### B2 再改中频表单

- [x] `QuestView.vue`：在线求助单创建区与 textarea。（完成：在线求助单创建区的标题、类别、可见范围、协作模式、截止时间、回报类型、回报数值、回报说明，以及多段接力阶段草稿输入都已迁到 `online-input / online-select`；求助内容与阶段说明 textarea 迁到 `online-textarea`；发布、增加阶段和删除阶段按钮改用统一按钮类。本轮只改创建草稿 UI class，不改求助单发布、接单、交付、确认或补偿逻辑。）
- [x] `SocialView.vue`：名片设置四个输入框与公开介绍 textarea。（完成：庄园名、公开称号、邻里身份、展示主题、公开状态、头像说明和公开介绍已迁到统一联机表单类；剩余更深的订阅 / 邻里细项不属于本子项。）
- [x] `ManorView.vue`：庄园主题名保存操作行。（完成：庄园主题名输入行已迁到 `online-action-row + online-input + online-action-btn`，窄屏会按统一规则换行；本轮只改主题名保存操作行 UI class，不改庄园主题周、导览或来访记录逻辑。）

### B3 收口管理端联机配置

- [x] `AdminOnlineGovernancePanel.vue` 的模板输入框不再在中等屏宽下强行三列挤压。（完成：联机发布控制里的三个 beta template 输入从 `md:grid-cols-3` 改为 `md:grid-cols-2 xl:grid-cols-3`，中宽屏不再硬塞三列。）
- [x] release notes textarea 保持更高的默认高度。（完成：发布说明的 5 个 release notes textarea 从 3 行提升到 5 行，并迁到 `online-textarea`。）
- [x] 管理端和玩家端输入高度保持同级。（完成：白名单 textarea、beta template 输入和 release notes textarea 已分别迁到 `online-textarea / online-input`，复用玩家端统一联机表单高度与焦点样式。）

### B4 验收

- [x] 桌面端输入框不再互相抢宽。（完成：`online-input / online-select / online-textarea` 有 240px 桌面最小宽度，`online-action-row` 允许 wrap；管理端 beta template 中宽布局已从 3 列改为 2 列。）
- [x] 手机端“输入 + 按钮”能自动上下排列。（完成：≤639px 时 `.online-action-row` 切为纵向，按钮和输入框各自 100% 宽；本轮 B1-B3 的邀请、保存和发布操作行都复用该规则。）
- [x] 输入框和按钮高度一致。（完成：普通 `online-input / online-select` 与 `online-action-btn` 默认 42px；紧凑按钮只用于好友驿站等密集操作，并保留 `online-action-btn--compact`。）
- [x] 长中文 placeholder 不导致布局变形。（完成：本轮长 placeholder 所在输入 / textarea 均使用 `online-*` 宽度与换行规则，textarea 有 96px 默认高度，release notes 提升到 5 行。）
- [x] 不引入新的 UI 库，不改业务逻辑。（完成：本轮 B1-B3 均只替换样式 class / 响应式布局和文档记录；每轮已通过 `type-check`、`build`、`git diff --check`，没有 package 依赖变更。）

### B5 优先看这些文件

- `taoyuan-main/src/app.css`
- `taoyuan-main/src/views/game/RegionMapView.vue`
- `taoyuan-main/src/views/game/SocialView.vue`
- `taoyuan-main/src/views/game/ExpeditionRoomView.vue`
- `taoyuan-main/src/views/game/FestivalView.vue`
- `taoyuan-main/src/views/game/SocietyView.vue`
- `taoyuan-main/src/views/game/QuestView.vue`
- `taoyuan-main/src/views/game/ManorView.vue`
- `taoyuan-main/src/components/game/AdminOnlineGovernancePanel.vue`

---

## 3. 主线 C：联机小游戏修改

> 目标：把联机小游戏从“点按钮推进数值”改成“短回合协作 + 事件 + 角色 + 风险反馈”。

### C0 先做样板间

- [x] 优先选择 `expedition_cavern` 做第一版样板。（完成：本轮只增强 `expedition_cavern`，没有迁移节会或其它远征模板。）
- [x] 保留房间创建、邀请、加入、准备、倒计时、运行、结算、关闭链路。（完成：L81 双人 / 三人 / 四人矿洞专项回归仍完整跑过创建、邀请、加入、ready、倒计时、动作、结算、关闭与写回。）
- [x] 不一次改全部小游戏模板。（完成：其它 `expedition_roles / supply / gathering / escort / sea` 仍走原共享进度动作逻辑。）

### C1 补回合与事件信息

- [x] 当前回合号。（完成：`gameplay.cavern_state.round_number / round_text` 已进入房间快照。）
- [x] 当前事件。（完成：`gameplay.cavern_state.current_event` 提供事件标题、说明、风险提示和资源提示。）
- [x] 风险值。（完成：`risk_value / risk_max / risk_text` 已随矿洞动作和回合推进变化。）
- [x] 队伍资源。（完成：补给、灯火、绳索、路标以 `team_resources` 进入快照，并会随动作消耗或补回。）
- [x] 角色分配。（完成：按成员顺序生成领队、探路、支护、采集职责，并在前端展示我的职责和队伍分工。）
- [x] 每回合动作记录。（完成：`round_log` 记录回合、事件、动作、角色、风险 / 资源变化和行动摘要。）

### C2 抽共通动作协议

- [x] `required_role`（完成：矿洞动作带职责要求，领队可兜底。）
- [x] `once_per_round`（完成：矿洞动作支持每回合动作约束，避免同一成员在同一回合重复刷同类动作。）
- [x] `risk_delta`（完成：动作和事件组合会改变风险值。）
- [x] `resource_delta`（完成：动作会消耗或补回补给、灯火、绳索、路标。）
- [x] `combo_tags`（完成：动作标签与当前事件标签命中时会触发额外收益。）
- [x] `round_effect`（完成：前端动作区会展示本动作对本回合的意义。）
- [x] 先让一个模板活起来，再抽共通结构。（完成：字段已通过 `available_actions` 通用快照下发；后续迁移其它模板时复用这一结构。）

### C3 前端从按钮列表改成事件面板

- [x] `ExpeditionRoomView.vue` 改成事件卡、队伍状态板、角色提示、回合动作区、回合日志、最近一次行动反馈。（完成：仅在 `cavern_state` 存在时显示增强事件面板，旧动作按钮与结算 / 关闭按钮保留。）
- [x] 玩家一眼能看出现在发生了什么、自己该干什么、这一步为什么重要。（完成：事件卡显示当前事件、风险提示和资源提示；动作区显示职责、风险 / 资源变化和 `round_effect`。）

### C4 再迁移节会模板

- [x] `FestivalView.vue`（完成：节会房间已接入事件卡、职责提示、场面压力、队伍资源、角色分工、回合日志和最近反馈，动作卡也能显示职责、每回合约束、压力 / 资源变化和回合效果。）
- [x] `useFestivalRoomStore.ts`（完成：已补 `myFestivalState` 以承接服务端回合快照，仍保持原有房间生命周期和结算调用方式。）
- [x] `festivalRoomApi.ts`（完成：已补节会回合快照、事件、资源、角色和日志等类型，前端可直接消费服务端权威结构。）
- [x] 把守岁、灯会、赛舟、赏月等模板接进回合事件系统。（完成：节会模板现在会按房型生成回合事件、职责分工、场面压力、队伍资源和回合日志，动作执行由服务端 runtime 权威推进。）

### C5 验收

- [x] 至少能跑出 3 轮有差异的回合。（完成：`qa:online-smoke` 的 L81 双人 / 三人 / 四人矿洞专项回归断言 `round_number >= 3`。）
- [x] 动作会影响进度之外的内容，例如风险或资源。（完成：矿洞动作会改变风险、补给、灯火、绳索、路标，并写入 `round_log`。）
- [x] 玩家之间的选择会互相影响。（完成：节会动作现在会受职责、每回合一次、事件 combo 和场面压力共同影响，且会推进共享回合状态。）
- [x] 结算凭证仍然能正常写回个人存档。（完成：`qa:online-smoke` 继续覆盖 L81 矿洞结算、关闭、奖励凭证持久化和个人存档写回。）
- [x] 玩家能说清楚自己这一轮在做什么。（完成：UI 已显示当前事件、我的职责、队伍资源、回合日志、最近反馈和动作效果。）

### C6 优先看这些文件

- `server/src/taoyuanActivityRoomRuntime.js`
- `taoyuan-main/src/utils/expeditionRoomApi.ts`
- `taoyuan-main/src/stores/useExpeditionRoomStore.ts`
- `taoyuan-main/src/views/game/ExpeditionRoomView.vue`
- `taoyuan-main/src/views/game/FestivalView.vue`
- `taoyuan-main/src/stores/useFestivalRoomStore.ts`
- `taoyuan-main/src/utils/festivalRoomApi.ts`

---

## 4. 推荐推进顺序

- [ ] Phase 1：WebSocket + 好友底座
- [x] Phase 2：联机输入框 UI 收口
- [x] Phase 3：联机小游戏样板间
- [ ] Phase 4：抽共通结构并迁移到更多模板
