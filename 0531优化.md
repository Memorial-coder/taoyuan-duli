# 0531 联机页面可读性优化计划

日期：2026-05-31  
范围：`taoyuan-main/src/views/game/OnlineView.vue`、`taoyuan-main/src/views/game/online/*`、`taoyuan-main/src/components/game/online/*`、`taoyuan-main/src/app.css`、联机 QA 脚本。

## 1. 结论

当前联机页面的底层能力已经很完整，但 UI 把「玩家当下要做什么」「房主房间流程」「审计/回执/降级/安全阀」全部铺在一个大页面里。移动端和桌面端虽然有响应式网格，但核心体验仍是长滚动大页：房主开房、邀请、准备、倒计时、结算、关闭都在同屏按钮区完成，没有弹窗、抽屉或步骤引导；玩家会先看到大量开发态和治理态语言，而不是游戏里的行动路线。

优化方向不是删掉联机能力，而是做三层收口：

- 玩家层：只展示当前身份、当前任务、下一步行动、结果反馈。
- 进阶层：把列表、记录、凭证、活动细则放进折叠区、详情抽屉或二级页。
- 治理层：把 `服务端`、`降级`、`hash`、`ledger`、`幂等`、`安全阀`、`主态`、`record-only` 等保留给后台、QA 或调试折叠区。

## 2. 代码阅读证据

- `OnlineView.vue:64` 之后同时展示「可视化活动」「功能开关」「活动排期」「奖励与投放控制」，联机中心首屏承担了入口、排期、灰度、奖励治理四种职责。
- `OnlineModuleShell.vue:35` 仅提供横向标签和刷新/返回按钮，没有二级导航、主行动区、弹窗或移动端底部动作区。
- `OnlineFestivalView.vue:266` 的节会房间页把我的房间、邀请、创建房间、可见房间铺在同一标签内；`OnlineFestivalView.vue:311` 到 `OnlineFestivalView.vue:364` 直接渲染开准备、我已准备、倒计时、模拟断线、结算、关闭、离开等按钮。
- `OnlineFestivalView.vue:578` 附近邀请玩家是同屏输入框；`OnlineFestivalView.vue:630` 附近创建节会房间是右侧长表单，不是开房流程弹窗。
- `OnlineFestivalView.vue:1250` 附近远征房间也同样把 ready、邀请、模拟断线、结算、关闭放在页面内。
- `OnlineCohabitationView.vue:3109` 定义了 12 个标签；`OnlineCohabitationView.vue:2707` 的家族节会席位中，`OnlineCohabitationView.vue:2731` 到 `OnlineCohabitationView.vue:2755` 将锁席、开房、供品、结算并列成按钮，没有确认层。
- `OnlineCohabitationView.vue:8201` 到 `OnlineCohabitationView.vue:8246` 等多处玩家可见文案混入 `revision`、`ledger`、`record_high_risk_receipt`、主态、安全阀等开发/治理语言。
- `OnlineNeighborView.vue:582`、`OnlineSocietyView.vue:129`、`OnlineOrdersView.vue:39` 等表单都以页面区块形式直接展开，适合功能验证，但不适合正式玩家阅读。
- `app.css:155` 已有 `game-modal-overlay` 基础样式，说明项目已有弹窗视觉底座；但联机页面搜索不到 `game-modal-overlay`、`Teleport`、`role="dialog"` 或 `aria-modal`。
- `qa-online-ui-structure.mjs` 和 `qa-online-visual-feature-flags.mjs` 目前会校验不少「降级」「服务端」「fallback」相关结构，后续改文案和信息层级时必须同步更新 QA。

## 3. 核心问题

### P0 问题

- 房主流程没有弹窗：创建房间、邀请成员、准备确认、开始倒计时、结算和关闭都是页面按钮，风险动作缺少确认和上下文。
- 页面层级过平：每个标签内继续堆多个 `game-panel-muted`、表单、日志、列表、凭证，用户必须滚动查找主行动。
- 开发态语言外露：`服务端`、`降级入口`、`缺失`、`模拟断线`、`ready`、`receipt`、`ledger`、`hash`、`幂等`、`record-only`、`主态`、`安全阀` 会破坏游戏沉浸。
- 移动端只是窄屏堆叠：已有网格断点和滚动边界，但缺少移动端「底部主操作」「抽屉详情」「首屏摘要」。

### P1 问题

- 联机中心承担过多治理信息，首页不像玩家的联机入口，更像 QA 总览。
- 各子页共用壳只统一了样式，没有统一「主任务 / 次任务 / 详情」的信息架构。
- 房间状态和可视化内容重复展示：`OnlineVisualRoomShell` 已经有成员、奖励、状态、降级读回，但页面下方又继续铺旧动作和凭证。
- 高风险资产、结算、关闭等动作缺少统一确认弹窗规范。

### P2 问题

- 视觉密度过高，`text-[10px]` 和大量细边框导致桌面端也像日志面板。
- 可访问性已有 `aria-live`、`aria-label`，但没有对话框焦点陷阱、Esc 关闭、返回焦点等完整弹窗行为。
- QA 目前偏结构存在性，缺少「首屏可读」「移动端主行动可达」「玩家文案无开发词」类校验。

## 4. 总体 UX 方案

### 4.1 信息架构

把所有联机页统一为四块：

- 页头：模块身份、当前状态、刷新、返回在线中心。
- 今日行动：最多 3 个主 CTA，例如「继续房间」「处理邀请」「创建房间」。
- 内容区：当前标签的核心内容，最多双栏或三栏，不再同时展示所有表单和日志。
- 详情层：弹窗、抽屉、折叠面板承接创建、邀请、凭证、审计、技术解释。

### 4.2 房主流程

房主开房间改为完整的弹窗/抽屉流程：

1. 选择活动：灯会、龙舟、远征等，以卡片说明人数、时长、奖励。
2. 配置房间：房型、玩法模板、人数上限、标题。
3. 邀请成员：输入用户名、最近好友、批量邀请、邀请结果。
4. 准备大厅：成员状态、房主提示、我已准备、开倒计时。
5. 结算确认：展示收益、未完成成员、关闭条件，再执行结算或关闭。

桌面端使用居中弹窗或右侧抽屉；移动端使用底部抽屉，保留 44px 以上触控按钮。

### 4.3 玩家文案策略

玩家页面只说「发生了什么」「为什么不能做」「下一步怎么办」。

技术词替换建议：

| 当前文案 | 玩家态替换 | 放置位置 |
| --- | --- | --- |
| 服务端 | 系统已确认 / 记录已同步 | 玩家可见 |
| 降级入口 | 备用入口 / 旧版入口 | 折叠说明 |
| 缺失 | 资料未完整 / 该内容暂未开放 | 玩家可见 |
| fallback | 备用路径 | 仅代码或 QA |
| ready | 准备 | 玩家可见 |
| 模拟断线 | 调试：断开连接 | 调试折叠或后台 |
| ledger | 流水 / 记录编号 | 玩家可见时只保留中文 |
| receipt | 结算凭证 | 玩家可见 |
| hash | 校验码 | 默认隐藏 |
| idempotency / 幂等 | 重复提交保护 | 默认隐藏 |
| record-only | 仅记录，不改资产 | 管理/审计区 |
| 主态 / 安全阀 | 存档保护 / 执行保护 | 管理/审计区 |

## 5. 组件改造计划

### P0 组件

- 新增 `OnlineActionDialog.vue`：复用 `game-modal-overlay`，支持标题、说明、主按钮、危险按钮、取消按钮、`role="dialog"`、`aria-modal`、Esc 关闭、焦点返回。
- 新增 `OnlineBottomSheet.vue`：移动端使用底部抽屉，桌面端可退化为右侧抽屉。
- 新增 `OnlineTechnicalDetails.vue`：统一收纳服务端、凭证、hash、ledger、回滚、补偿、降级说明，默认折叠。
- 新增 `OnlineStickyActionBar.vue`：移动端固定当前主行动，避免按钮散落在长页面里。
- 增强 `OnlineModuleShell.vue`：加入主行动插槽、标签说明可折叠、移动端 sticky tab、`tablist/tab/tabpanel` 语义。

### P1 组件

- 新增 `OnlineRoomWizard.vue`：创建房间步骤流，服务节会和远征。
- 新增 `OnlineRoomLobbyDialog.vue`：邀请、准备、倒计时、成员状态统一弹窗。
- 新增 `OnlineInvitePanel.vue`：统一玩家邀请、批量输入、近期好友、错误提示。
- 新增 `OnlineConfirmActionDialog.vue`：结算、关闭、取消、供品消耗、基金支出等危险动作统一确认。
- 新增 `OnlineEmptyState.vue` 和 `OnlineStatusBanner.vue`：统一空态、阻塞原因、成功反馈。

### P2 组件

- 新增 `OnlineCopyGuard` 或文案规则脚本：扫描玩家态 Vue 模板中的禁用词。
- 新增截图回归入口：固定 390x844、360x780、1366x768、1920x1080 四类视口。

## 6. 页面改造计划

### 6.1 在线中心 `OnlineView.vue`

P0：

- 首屏改为「继续联机」「待处理邀请」「可加入房间」「我的村社/邻里」四张行动卡。
- 将「可视化功能开关」「缺失」「降级入口」「奖励与投放控制」移动到「调试与治理信息」折叠区，默认不展开。
- 可视化活动保留，但只显示玩家语言：活动名、是否可参加、下一步。

P1：

- 增加「我该去哪」推荐：根据 `myRoom`、邀请数量、待处理订单、村社提案给出 1 个主 CTA。
- 首页模块卡从 6 张平铺改为「主要行动 + 其他入口」。

### 6.2 节会/远征 `OnlineFestivalView.vue`

P0：

- 创建节会房间、创建远征房间改为 `OnlineRoomWizard` 弹窗。
- 邀请玩家改为 `OnlineInvitePanel` 弹窗，入口放在房间操作区。
- 「开准备 / 我已准备 / 开倒计时」进入 `OnlineRoomLobbyDialog`，成员准备状态固定可见。
- 「取消房间 / 正式关闭 / 撤离并结算」必须走确认弹窗。
- 「模拟断线」移入调试折叠区或仅后台可见。
- `开始 ready` 改为「开始准备」。

P1：

- 合并 `OnlineVisualRoomShell` 与旧动作区：可视化模式正常时，只保留一个主行动区；旧动作作为「备用操作」折叠。
- 可见房间和待处理邀请改为列表 + 详情抽屉，避免和我的房间抢首屏。
- 房主和成员分别显示不同主按钮：房主看到「邀请 / 开始准备 / 开始倒计时」，成员看到「我已准备 / 离开」。

P2：

- 给灯会、龙舟、矿洞做更强的首屏视觉区，减少纯文本格子和重复说明。

### 6.3 共同庄园 `OnlineCohabitationView.vue`

P0：

- 12 个标签重组为 4 组：总览、共同经营、家庭/节会、资产与治理。
- 默认只显示总览、地图、仓库、基金、节会；建筑、公开、离线、权限、声望等作为「更多」或二级页。
- 家族节会席位中的「锁席 / 开房 / 供品 / 结算」全部改为确认弹窗，展示影响对象、资产变化、失败原因。
- `ledger`、`hash`、`record-only`、`主态`、`安全阀`、`revision` 等移入 `OnlineTechnicalDetails`。

P1：

- 共同农田、仓库、基金、建筑都改为「列表 + 详情抽屉 + 主行动」结构。
- 大额基金、高风险回执、真实拆除等高风险流程统一使用 `OnlineConfirmActionDialog`，要求明确确认文字。
- 将审计日志默认折叠，并提供搜索/筛选，不再把所有日志直接铺在页面里。

P2：

- 将超长常量和文案拆到 `src/data/onlineCohabitationCopy.ts` 或同类配置，降低单文件维护成本。

### 6.4 邻里 `OnlineNeighborView.vue`

P0：

- 创建邻里、邀请成员、申请/邀请处理改为弹窗或抽屉。
- 好友驿站跳转、邻里组织、公开名片在首屏只保留摘要和主 CTA。

P1：

- 公开邻里列表点击后打开详情抽屉，再执行申请加入。
- 名片编辑改为「编辑名片」弹窗，默认展示公开卡片。

### 6.5 在线委托 `OnlineOrdersView.vue`

P0：

- 发布委托从长表单改为步骤弹窗：基础信息、协作模式、回报、确认发布。
- 接单/交付/确认补偿用确认弹窗。

P1：

- 可接委托列表支持详情抽屉；列表卡只显示标题、需求、奖励、剩余时间、主按钮。
- 凭证与补偿默认收进技术详情，玩家只看状态和处理建议。

### 6.6 在线村社 `OnlineSocietyView.vue`

P0：

- 创建村社、邀请玩家、申请处理、发起提案改为弹窗。
- 「创建房间」类入口接到节会/房间向导，不直接成为页面内按钮。

P1：

- 村社公共建设、提案、史册分别改为卡片列表 + 详情抽屉。
- 管理动作和普通成员动作分层展示。

### 6.7 在线庄园 `OnlineManorView.vue`

P0：

- 上传主图、保存主题、留言、协作护理房动作使用弹窗或底部抽屉。
- 概览页只保留庄园预览、主题、留言/照料/访客三个主入口。

P1：

- 访客记录、轻采凭证、照料记录改为详情抽屉。
- 主人态和访客态使用不同默认行动，不再共享同一长页面。

## 7. 移动端专项

P0 验收目标：

- 360x780 和 390x844 下，进入任一联机子页，首屏必须看到模块标题、当前状态、1 个主行动。
- 房主开房、邀请、准备不再需要滚动到页面中段。
- 底部主操作区固定显示，不遮挡安全区。
- 所有主按钮高度不低于 44px；紧凑按钮只用于次要操作。

移动端布局建议：

- 顶部：状态摘要。
- 中部：当前卡片或列表。
- 底部：主按钮 + 次按钮。
- 详情：底部抽屉。
- 技术/审计：折叠区，默认关闭。

## 8. 桌面端专项

桌面端不要继续把所有内容变成 2 列平铺，应改为：

- 左侧：列表或导航。
- 中间：当前核心内容。
- 右侧：详情 / 成员 / 状态 / 审计折叠。

节会房间、远征房间优先采用「房间舞台 + 右侧成员/操作栏」；创建、邀请、结算仍走弹窗，避免右栏持续膨胀。

## 9. QA 与验收

必须同步更新：

- `taoyuan-main/scripts/qa-online-ui-structure.mjs`：增加弹窗组件存在性、主行动入口、移动端 sticky action、玩家文案扫描。
- `taoyuan-main/scripts/qa-online-visual-feature-flags.mjs`：不再要求玩家页直接展示「降级」「缺失」「服务端」等治理文案；改为要求这些内容进入技术详情或后台。
- `taoyuan-main/scripts/qa-mobile-ui-smoke.mjs`：新增房主开房、邀请、准备、结算弹窗路径，覆盖 390x844 和 360x780。
- `taoyuan-main/e2e/game-smoke.spec.ts`：新增桌面端弹窗路径、Esc 关闭、确认后反馈、焦点返回。

建议验证命令：

```bash
cd taoyuan-main
npm run qa:online-ui-structure
npm run qa:online-visual-feature-flags
npm run qa:mobile-ui-smoke
npm run check
```

验收标准：

- 联机页面玩家可见区域不再出现 `fallback`、`hash`、`idempotency`、`record-only`、`personal_save_changed`。
- 「服务端」「降级」「缺失」「安全阀」「主态」「ledger」「revision」只允许出现在技术详情、调试、后台或 QA 文档里。
- 房主开房、邀请、准备、倒计时、结算、关闭都有弹窗/抽屉承接。
- 移动端首屏不再是纯长列表，主操作始终可达。
- 关闭/结算/供品/基金等风险动作都有确认和影响说明。

## 10. 执行顺序

### 第一阶段：P0，先把玩家从大页里解放出来

1. 新增 `OnlineActionDialog.vue`、`OnlineBottomSheet.vue`、`OnlineStickyActionBar.vue`、`OnlineTechnicalDetails.vue`。
2. 改 `OnlineFestivalView.vue`：创建、邀请、准备、结算、关闭全部弹窗化。
3. 改 `OnlineCohabitationView.vue`：家族节会席位弹窗化，技术词折叠。
4. 改 `OnlineView.vue`：治理/功能开关/奖励控制折叠，首屏改成玩家行动。
5. 更新 QA 脚本，确保不会因为移走旧文案而误报。

### 第二阶段：P1，统一所有联机子页结构

1. `OnlineModuleShell.vue` 增强主行动区、tab 语义和移动端 sticky。
2. 邻里、委托、村社、庄园的创建/邀请/发布/留言动作弹窗化。
3. 共同庄园从 12 标签改为分组导航和详情抽屉。
4. 房间页合并重复状态区，旧动作统一进入「备用操作」折叠。

### 第三阶段：P2，视觉与回归

1. 做联机页面视觉密度优化：字号层级、留白、重点色、列表卡片信息裁剪。
2. 增加截图回归和玩家文案 lint。
3. 将共居超长文案/配置拆出，降低维护成本。

## 11. 风险与注意事项

- 不改房间状态机和结算语义，只改前端呈现层；`docs/online/08-room-state-machine.md` 仍是房间生命周期边界。
- 不删除审计、补偿、回滚、凭证能力，只把它们从玩家主路径移到技术详情或后台。
- QA 目前依赖很多现有文案和 test id，改 UI 时必须同步迁移测试断言。
- 部分按钮虽然像调试功能，例如「模拟断线」，可能被现有 smoke 使用；需要改成调试入口后更新脚本。

## 12. 最小可落地任务清单

- `P0-1`：实现 `OnlineActionDialog.vue`，并在节会房间关闭/结算上先接入。
- `P0-2`：实现节会/远征创建房间向导。
- `P0-3`：实现邀请弹窗，替换节会、远征、邻里、村社邀请输入区。
- `P0-4`：实现房间准备大厅弹窗，成员准备状态、房主开准备、成员准备都在同一处完成。
- `P0-5`：在线中心隐藏治理信息，改为玩家行动首页。
- `P0-6`：共居家族节会席位四个按钮弹窗化。
- `P0-7`：玩家文案替换第一轮，清除最明显开发态语言。
- `P0-8`：更新 QA 脚本并跑移动端 smoke。

## 13. 首屏落地草图

### 13.1 在线中心首屏

目标是让玩家进入在线中心 3 秒内知道下一步，而不是先读完功能开关。

```text
┌ 在线中心 ───────────────────────────────┐
│ 当前联机状态：你有 1 条节会邀请 · 村社有 2 个待处理事项 │
│ [继续当前房间] [处理邀请] [创建房间]                 │
├ 今日推荐 ───────────────────────────────┤
│ 1. 上元灯会邀请等待处理                               │
│ 2. 村社节庆广场可继续布景                             │
│ 3. 好友庄园有新的护理房记录                           │
├ 常用入口 ───────────────────────────────┤
│ 节会房间 | 村社 | 委托 | 庄园 | 共同庄园 | 邻里        │
└ 调试与治理信息（默认折叠）                            │
```

保留现有 `OnlineModuleCard`，但把它从首屏主角降为入口矩阵；可视化功能开关、奖励控制和排期进入折叠详情。

### 13.2 房间页首屏

房间页应该像一个「房间大厅」，不是所有工具按钮的集合。

```text
┌ 房间舞台：上元灯会 · 准备中 ───────────────┐
│ 成员 3/4 · 已准备 2/3 · 倒计时未开始                    │
│ [邀请玩家] [开始准备] [更多]                            │
├ 现场画面 / 地图 / 轨道 ───────────────────┤
│ 主灯、灯谜、人群、留影点，或远征地图节点                 │
├ 右侧 / 抽屉：成员与奖励 ──────────────────┤
│ 成员准备、奖励预览、最近反馈                             │
└ 技术详情（默认折叠）                                    │
```

移动端把「邀请玩家 / 我已准备 / 开始倒计时」放到底部 sticky action bar；成员、奖励、技术详情放入底部抽屉。

## 14. 房间动作矩阵

| 动作 | 角色 | 当前问题 | 新承载 | 确认要求 |
| --- | --- | --- | --- | --- |
| 创建房间 | 房主 | 页面长表单平铺 | `OnlineRoomWizard` | 最后一步确认模板、人数、标题 |
| 邀请玩家 | 房主 / 管理员 | 输入框散落在页面中段 | `OnlineInvitePanel` | 邀请结果即时反馈，可批量重试 |
| 开始准备 | 房主 | `开始 ready` 文案开发态 | `OnlineRoomLobbyDialog` | 显示未加入成员和准备规则 |
| 我已准备 | 成员 | 按钮混在旧动作区 | 大厅弹窗 + 移动端底部按钮 | 不需要二次确认 |
| 取消准备 | 成员 | 缺少影响说明 | 大厅弹窗次按钮 | 显示会影响倒计时 |
| 开始倒计时 | 房主 | 与结算/关闭并列 | 大厅弹窗主按钮 | 显示剩余未准备成员 |
| 模拟断线 | QA | 玩家可见破坏沉浸 | 调试详情 | 仅调试态可见 |
| 结算 | 房主 | 高风险动作直接按钮 | `OnlineConfirmActionDialog` | 展示奖励、未完成成员、凭证结果 |
| 关闭/取消房间 | 房主 | 缺少损失预期 | 危险确认弹窗 | 必须说明房间状态和影响 |
| 离开房间 | 成员 | 与房主动作混排 | 成员菜单 / 更多 | 房间进行中时二次确认 |

## 15. 玩家态文案闸门

建议新增一份玩家可见模板扫描规则，先以静态脚本形式落地，不要求一次性完美。

禁用词第一批：

- 英文技术词：`fallback`、`idempotency`、`record-only`、`hash`、`ledger`、`revision`、`mock`。
- 开发流程词：`模拟断线`、`开始 ready`、`缺失配置`、`降级入口`。
- 治理词：`主态`、`安全阀`、`服务端状态冲突`、`只读回看`。

允许出现的位置：

- `OnlineTechnicalDetails.vue`。
- 后台管理页和 QA 脚本。
- `data-testid`、类型、API 字段、测试 mock 数据。

玩家态替换原则：

- 把技术原因改成行动建议，例如「服务端状态冲突」改成「房间信息有更新，请刷新后继续」。
- 把治理名词改成结果反馈，例如「服务端落账」改成「奖励已记录」。
- 把降级机制改成备用说明，例如「旧按钮降级入口」改成「备用操作」。

## 16. PR 切分建议

为了降低回归风险，建议不要把所有联机页一次性大改，而是按可回滚的 PR 切片推进。

1. `PR-A`：只新增通用组件，不接业务。包含 `OnlineActionDialog`、`OnlineBottomSheet`、`OnlineTechnicalDetails`、`OnlineStickyActionBar`，并给组件写最小静态测试。
2. `PR-B`：节会/远征房间 P0。把创建、邀请、准备、结算、关闭接入弹窗，保留旧 test id 的兼容别名或在 QA 中一次性迁移。
3. `PR-C`：在线中心 P0。治理信息折叠，首页改成玩家行动入口，同时更新 `qa-online-ui-structure.mjs`。
4. `PR-D`：共同庄园 P0。先重组 12 个标签和家族节会高风险动作，再做资产/审计详情折叠。
5. `PR-E`：邻里、委托、村社、庄园弹窗化。复用前面组件，不再新增交互范式。
6. `PR-F`：文案 lint、移动端截图回归和桌面布局 polish。

每个 PR 的合入标准：

- 当前页主路径在 390x844、360x780、1366x768 下可完成。
- 不引入横向溢出。
- 房主开房、邀请、准备不需要滚到页面中段。
- 玩家可见区域不出现第一批禁用词。
- 原有服务端状态机、结算、审计、补偿接口不变。

## 17. 第二版深化目标

这次优化不只是「加几个弹窗」，而是把联机系统从功能验证页改造成正式可玩的联机大厅。成功标准要从玩家感受、工程可维护性、QA 可验证性三条线同时定义。

### 17.1 玩家体验目标

- 进入在线中心后 3 秒内知道自己应该点哪里。
- 房主开房、邀请、准备、倒计时、结算、关闭都有明确流程，不再散落在长页面里。
- 成员加入房间后只看到和自己身份有关的主操作，不被房主按钮、调试按钮、凭证细节干扰。
- 移动端单手操作可完成房间创建、接受邀请、准备、结算回看。
- 桌面端利用横向空间展示舞台、成员和详情，不再把所有信息两列平铺到底。

### 17.2 工程目标

- 新增弹窗、抽屉、技术详情、主行动栏等通用组件后，各在线页只组合组件，不重复写交互范式。
- 保持现有 store、API、状态机、结算接口不变，第一轮只调整呈现层和前端交互层。
- 保留关键 `data-testid` 或提供迁移别名，减少一次性重写全部 E2E 的风险。
- 把共同庄园、在线节会等超长模板拆成更清晰的子组件或配置，降低后续维护成本。

### 17.3 QA 目标

- 新增静态检查，阻止玩家态模板出现第一批开发态词。
- 新增房主流程 E2E，覆盖创建、邀请、准备、倒计时、结算、关闭。
- 新增移动端 smoke，验证 360x780、390x844 下主行动可见、无横向溢出、底部按钮不遮挡安全区。
- 新增弹窗可访问性检查，覆盖 `role="dialog"`、`aria-modal`、Esc 关闭、焦点返回。

## 18. 用户角色与任务地图

### 18.1 角色拆分

| 角色 | 主要目标 | 当前阻塞 | 优化后入口 |
| --- | --- | --- | --- |
| 新玩家 | 找到联机能做什么 | 首页像功能总表，信息过载 | 在线中心「今日推荐」和「常用入口」 |
| 房主 | 快速开房、拉人、开始 | 创建/邀请/准备按钮散落 | 房间向导 + 房间大厅 |
| 被邀请成员 | 接受邀请并准备 | 邀请列表和房间信息混在一起 | 待处理邀请卡 + 详情抽屉 |
| 已在房成员 | 看状态、准备、行动 | 房主/成员/调试按钮混排 | 身份化主行动栏 |
| 村社管理员 | 管理申请、邀请、提案 | 管理表单直接铺满 | 管理动作弹窗 + 批处理列表 |
| 共同庄园成员 | 共同经营、处理资产风险 | 12 个标签过多，治理语言外露 | 总览推荐 + 分组导航 |
| 庄园访客 | 留言、照料、轻采 | 主人态/访客态混杂 | 访客主入口和照料详情 |
| QA / 管理员 | 看凭证、降级、审计 | 和玩家主路径混在一起 | 技术详情折叠 + 后台入口 |

### 18.2 任务优先级

P0 必须优先支持：

- 房主创建节会房间。
- 房主创建远征房间。
- 房主邀请成员。
- 成员接受邀请。
- 房主开始准备。
- 成员准备 / 取消准备。
- 房主开始倒计时。
- 房主结算。
- 房主关闭或取消房间。
- 移动端完成上述路径。

P1 继续支持：

- 村社创建、邀请、申请处理、提案。
- 在线委托发布、接单、交付、确认。
- 共同庄园基金、仓库、家族节会、建筑高风险动作。
- 在线庄园留言、照料、轻采、护理房。

P2 打磨：

- 视觉主题、动效、空态、插画感活动卡。
- 复杂审计筛选和凭证搜索。
- 玩家引导、首次使用提示和轻量教程。

## 19. 全局信息架构细化

### 19.1 页面四层结构

每个联机页面都按同一层级组织：

| 层级 | 内容 | 默认可见性 | 示例 |
| --- | --- | --- | --- |
| 身份层 | 我是谁、当前身份、模块状态 | 总是可见 | 房主、成员、村社管理员、访客 |
| 行动层 | 当前最重要的 1-3 个按钮 | 总是可见 | 创建房间、处理邀请、我已准备 |
| 内容层 | 当前标签的主要内容 | 总是可见 | 房间舞台、委托列表、村社工程 |
| 详情层 | 表单、成员、凭证、日志、技术说明 | 按需展开 | 弹窗、抽屉、折叠区 |

### 19.2 导航结构

在线中心：

- 今日行动。
- 我的联机。
- 常用入口。
- 活动日历。
- 调试与治理。

节会 / 远征：

- 我的房间。
- 待处理邀请。
- 可加入房间。
- 纪念与回看。
- 备用 / 技术详情。

共同庄园：

- 总览。
- 共同经营。
- 家庭与节会。
- 资产与治理。
- 更多。

村社：

- 总览。
- 成员。
- 共建。
- 仓廪。
- 提案。
- 史册。
- 管理。

委托：

- 推荐可接。
- 我的发布。
- 我的接单。
- 公共接力。
- 补偿与记录。

庄园：

- 概览。
- 留言。
- 照料。
- 访客。
- 主题。
- 记录。

邻里：

- 名片。
- 好友。
- 邻里组织。
- 邀请 / 申请。
- 订阅。

### 19.3 信息收口规则

- 当前页只展示玩家完成任务必须知道的信息。
- 任何凭证 ID、hash、ledger、revision、idempotency 默认不在主路径出现。
- 长表单默认不出现在页面主体，必须进入弹窗、抽屉或步骤流。
- 日志列表默认只展示最近 3 条，其余进入「查看全部」抽屉。
- 高风险动作不能只是按钮，必须有确认、影响说明、失败提示和完成反馈。

## 20. 通用组件详细规格

### 20.1 `OnlineActionDialog.vue`

用途：承接单步确认、风险确认、简单表单提交。

核心 props：

- `open: boolean`。
- `title: string`。
- `description?: string`。
- `tone?: 'default' | 'success' | 'warning' | 'danger'`。
- `confirmLabel?: string`。
- `cancelLabel?: string`。
- `confirmDisabled?: boolean`。
- `running?: boolean`。
- `requireText?: string`，用于高风险确认文字。
- `closeOnBackdrop?: boolean`，高风险默认 `false`。

事件：

- `confirm`。
- `cancel`。
- `close`。

可访问性：

- 根节点使用 `role="dialog"`。
- 高风险弹窗使用 `aria-modal="true"`。
- 打开后聚焦标题或第一个可交互元素。
- Esc 可关闭普通弹窗，高风险弹窗 Esc 只触发取消确认。
- 关闭后返回触发按钮焦点。

测试钩子：

- `online-action-dialog`。
- `online-action-dialog-title`。
- `online-action-dialog-confirm`。
- `online-action-dialog-cancel`。
- `online-action-dialog-required-text`。

### 20.2 `OnlineBottomSheet.vue`

用途：移动端详情、成员、邀请、列表详情；桌面端可切换为右侧抽屉。

核心行为：

- 小屏：底部上滑抽屉，最大高度 `min(80vh, 720px)`。
- 中大屏：右侧抽屉或居中面板。
- 支持头部固定、底部 action 固定、内容区独立滚动。
- 支持安全区 `env(safe-area-inset-bottom)`。

### 20.3 `OnlineStickyActionBar.vue`

用途：移动端固定当前主操作，避免滚动找按钮。

结构：

- 主按钮 1 个。
- 次按钮最多 2 个。
- 更多按钮打开动作菜单。
- 显示当前状态简写，例如「准备中 · 2/4」。

规则：

- 只在 `sm` 以下默认固定；桌面端由页面右栏承接。
- 主按钮高度至少 44px。
- 底部 padding 加安全区。
- 不遮挡页面最后内容，页面底部增加相同高度 spacer。

### 20.4 `OnlineTechnicalDetails.vue`

用途：统一收纳治理、凭证、降级、审计、回滚说明。

默认标题：

- 「技术详情」。
- 「结算凭证」。
- 「审计记录」。
- 「备用操作」。

显示规则：

- 默认折叠。
- 玩家态只展示中文摘要。
- QA / 管理态可展开字段明细。
- 支持复制凭证号，但不在主页面裸露长 ID。

### 20.5 `OnlineRoomWizard.vue`

用途：节会、远征、村社完工房间等统一开房。

步骤：

1. 选择玩法。
2. 配置房间。
3. 邀请成员。
4. 确认创建。

必须支持：

- 从 URL query 预填 `template`、`gameplay`、`memberLimit`。
- 房主取消时保留草稿。
- 创建成功后跳转或刷新到「我的房间」。
- 创建失败时停留当前步骤并展示失败原因。

### 20.6 `OnlineRoomLobbyDialog.vue`

用途：准备、邀请、倒计时、成员状态集中管理。

显示内容：

- 房间名、玩法、人数、状态。
- 成员列表：房主、已准备、未准备、离线、被邀请。
- 房主提示：还差谁、是否能开始倒计时。
- 成员提示：自己是否已准备、是否可取消。
- 最近反馈：邀请成功、准备成功、倒计时开始。

房主按钮：

- 邀请玩家。
- 开始准备。
- 开始倒计时。
- 取消房间。

成员按钮：

- 接受邀请。
- 我已准备。
- 取消准备。
- 离开房间。

### 20.7 `OnlineInvitePanel.vue`

用途：统一邀请输入体验。

能力：

- 单个用户名输入。
- 多个用户名粘贴，用空格、逗号、换行拆分。
- 最近好友 / 村社成员快捷选择。
- 展示已邀请、邀请中、邀请失败、已在房、不可邀请。
- 批量重试失败项。

### 20.8 `OnlineEntityListWithDrawer.vue`

用途：解决所有列表平铺问题。

适用：

- 可见房间列表。
- 待处理邀请。
- 委托列表。
- 村社公开列表。
- 访客记录。
- 审计记录。

行为：

- 列表卡只展示标题、状态、最重要的 2-3 个摘要、主按钮。
- 点击卡片打开详情抽屉。
- 详情抽屉展示完整信息、次要按钮、技术详情。

## 21. 房主开房全流程细化

### 21.1 流程状态

```text
无房间
  -> 打开创建向导
  -> 选择玩法
  -> 配置房间
  -> 邀请成员
  -> 创建成功
  -> 准备大厅
  -> 开始准备
  -> 成员准备
  -> 倒计时
  -> 进行中
  -> 结算确认
  -> 结算完成
  -> 关闭房间
```

异常分支：

- 创建失败：停留向导当前步骤，保留草稿。
- 邀请失败：保留失败项，允许重试。
- 成员未准备：倒计时按钮禁用并说明原因。
- 房间状态冲突：提示「房间信息有更新，请刷新后继续」。
- 结算失败：保留房间，提示可重试。
- 关闭失败：提示当前状态不允许关闭，提供刷新。

### 21.2 节会创建向导字段

步骤 1：选择玩法。

- 上元灯会。
- 龙舟赛道。
- 腊八共灶。
- 烟火留影。
- 其他节会模板。

步骤 2：配置房间。

- 房间标题。
- 人数上限：2 / 4 / 6 / 8。
- 玩法模式：共建、竞速、协作、纪念。
- 是否允许可见房间列表展示。

步骤 3：邀请成员。

- 用户名。
- 最近好友。
- 村社成员。
- 跳过邀请，稍后再邀请。

步骤 4：确认。

- 展示模板、人数、奖励口径、房主责任。
- 主按钮「创建房间」。

### 21.3 远征创建向导字段

步骤 1：选择路线。

- 协作矿洞。
- 护送抵运。
- 海域共探。
- 山林采集。

步骤 2：配置队伍。

- 队伍标题。
- 人数上限。
- 难度 / 风险等级。
- 目标节点或路线模板。

步骤 3：邀请成员。

- 推荐邀请最近远征伙伴。
- 支持直接复制房间口令。

步骤 4：确认。

- 展示风险、撤离规则、奖励预览。
- 主按钮「创建远征队伍」。

## 22. 页面级详细改造清单

### 22.1 `OnlineView.vue`

现状重点：

- 首屏先展示模块卡和可视化活动。
- 功能开关、活动排期、奖励控制直接铺在玩家中心页。
- 玩家行动和治理信息没有分层。

P0 改造：

- 新增 `online-center-hero-actions` 区域。
- `heroActions` computed 根据 store 派生：继续房间、处理邀请、创建房间、进入村社。
- 可视化功能开关迁入 `OnlineTechnicalDetails`，标题改为「调试与治理信息」。
- 奖励与投放控制迁入折叠区。
- 可视化活动卡文案去掉「降级」「缺失」「服务端凭证」。

推荐首屏 CTA 派生规则：

| 条件 | 主 CTA | 次 CTA |
| --- | --- | --- |
| 有节会/远征 `myRoom` | 继续当前房间 | 邀请玩家 / 查看回看 |
| 有待处理邀请 | 处理邀请 | 创建房间 |
| 有村社提案 / 公共工程 | 查看村社待办 | 创建房间 |
| 有可接公共订单 | 接力委托 | 发布委托 |
| 无在线状态 | 创建房间 | 查看可加入房间 |

P1 改造：

- 模块卡合并为「我的在线生活」区域，每张卡只保留一个主入口。
- 活动日历只展示本周 3 个重点活动，其余进入抽屉。
- 加入「最近联机」：最近房间、最近委托、最近村社动作。

验收：

- 390x844 首屏能看到标题、状态摘要、至少 1 个主 CTA。
- 玩家态首屏不出现 `降级`、`缺失`、`服务端凭证`。
- `online-visual-feature-flag-panel` 仍可在技术详情中被 QA 找到。

### 22.2 `OnlineFestivalView.vue`

现状重点：

- 节会房间、远征房间、纪念回看塞在一个大视图。
- `开始 ready`、`模拟断线`、结算、关闭都在房间壳操作区。
- 邀请和创建为页面内输入框 / 表单。

P0 改造：

- 页面顶部新增当前房间摘要：房间名、状态、成员、下一步。
- 创建节会房间入口改为 `openFestivalRoomWizard()`。
- 创建远征房间入口改为 `openExpeditionRoomWizard()`。
- 邀请入口改为 `openInvitePanel(domain)`。
- 准备入口改为 `openRoomLobby(domain)`。
- 结算、关闭改为 `OnlineConfirmActionDialog`。
- `模拟断线` 放入 `OnlineTechnicalDetails`。
- `开始 ready` 改为「开始准备」。

房主主按钮规则：

| 房间状态 | 主按钮 | 次按钮 |
| --- | --- | --- |
| invited / created | 邀请玩家 | 取消房间 |
| ready_check | 开始倒计时 | 邀请玩家 |
| countdown | 查看倒计时 | 取消房间 |
| running | 结算房间 | 查看成员 |
| settling | 正式关闭 | 查看凭证 |
| closed | 查看回看 | 创建新房 |

成员主按钮规则：

| 成员状态 | 主按钮 | 次按钮 |
| --- | --- | --- |
| invited | 接受邀请 | 拒绝 / 稍后 |
| joined | 我已准备 | 离开房间 |
| ready | 取消准备 | 查看成员 |
| running | 进入现场 | 离开房间 |
| disconnected | 重新连接 | 刷新 |
| settled | 查看回看 | 离开 |

P1 改造：

- 我的房间与可见房间不要同屏竞争；可见房间改为列表 + 详情抽屉。
- 纪念回看默认展示最近 3 条，其余抽屉查看。
- `OnlineVisualRoomShell` 只保留房间状态和舞台，旧动作面板进「备用操作」折叠。

验收：

- 房主开房、邀请、准备、结算、关闭全走弹窗或抽屉。
- `online-festival-room-shell-ready-check-submit` 文案不再出现 `ready`。
- 移动端不滚动即可触发「邀请 / 准备 / 我已准备」。

### 22.3 `ExpeditionRoomView.vue`

现状重点：

- 独立远征页也有页面内创建、邀请、ready、结算、关闭。
- 文案中出现「降级玩法动作」「per-member receipt」「服务端状态冲突」。

P0 改造：

- 与 `OnlineFestivalView.vue` 共用 `OnlineRoomWizard`、`OnlineRoomLobbyDialog`、`OnlineInvitePanel`。
- 独立页顶部改为「远征大厅」摘要。
- 远征地图或轨道作为主舞台。
- 降级动作进入 `OnlineTechnicalDetails` 的「备用操作」。
- 最近 receipt 改成「最近结算」卡片，凭证 ID 默认隐藏。

验收：

- 独立远征页和在线节会远征标签行为一致。
- 390x844 首屏能看到远征状态、成员准备、主行动。

### 22.4 `OnlineCohabitationView.vue`

现状重点：

- 12 个标签过多。
- 家族节会席位、基金、仓库、建筑、分居等高风险动作直接平铺。
- 文案和函数里大量 `ledger`、`revision`、`receipt`、主态、安全阀。

P0 改造：

- 导航重组为 4 组，并保留旧 query 到新组的映射。
- 总览页只展示：契约状态、共同基金摘要、仓库摘要、今日建议、风险待办。
- 家族节会席位四个动作接入确认弹窗。
- 大额基金、仓库高价值取用、高风险回执统一接入 `OnlineConfirmActionDialog`。
- `formatSharedAuditDetail` 输出拆分为玩家摘要和技术详情两套。

旧标签映射建议：

| 旧 tab | 新分组 | 默认展示 |
| --- | --- | --- |
| overview | 总览 | 是 |
| map | 共同经营 | 是 |
| warehouse | 共同经营 | 是 |
| fund | 资产与治理 | 是 |
| permissions | 资产与治理 | 否，更多 |
| offline | 资产与治理 | 否，更多 |
| family | 家庭与节会 | 是 |
| festival | 家庭与节会 | 是 |
| buildings | 共同经营 | 否，更多 |
| public | 总览 | 否，更多 |
| reputation | 家庭与节会 | 否，更多 |
| separation | 资产与治理 | 高风险入口 |

玩家摘要示例：

- 「共同仓库已产出 3 份料理，个人资产未变化」。
- 「房间信息有更新，请刷新后继续」。
- 「高风险支出已记录，等待另一位成员确认」。

技术详情保留：

- `warehouse_ledger_ids`。
- `client_queue_revision`。
- `server_queue_revision`。
- `idempotent`。
- `receipt_ref`。

P1 改造：

- 拆子组件：`CohabitationOverviewPanel`、`CohabitationSharedMapPanel`、`CohabitationWarehousePanel`、`CohabitationFundPanel`、`CohabitationFamilyFestivalPanel`、`CohabitationGovernancePanel`。
- 审计日志改为列表 + 抽屉，默认显示最近 3 条。
- 高风险流程使用确认文字，例如输入「确认消耗共同资产」。

验收：

- 总览首屏不超过 4 张主卡。
- 玩家态不出现 `ledger`、`revision`、`record_high_risk_receipt`。
- 高风险动作都能说明影响对象、资产变化、可撤销性。

### 22.5 `OnlineNeighborView.vue`

P0 改造：

- 名片编辑进入弹窗。
- 创建邻里进入步骤弹窗。
- 邀请成员进入 `OnlineInvitePanel`。
- 申请 / 邀请处理进入详情抽屉。
- 首页默认展示我的名片、好友入口、邻里状态和待办。

P1 改造：

- 公开邻里卡片只展示名称、人数、招募状态、主按钮。
- 详情抽屉展示公告、加入条件、成员摘要和申请表单。

验收：

- 移动端不会先看到长表单。
- 创建邻里失败后保留草稿。

### 22.6 `OnlineOrdersView.vue`

P0 改造：

- 发布委托进入 `OnlineOrderWizard`。
- 接单、交付、确认交付、补偿确认进入确认弹窗。
- 公共订单接力路线保留视觉组件，但阶段详情进入抽屉。
- 凭证 ID 和补偿细节进入技术详情。

发布委托步骤：

1. 选择委托类型。
2. 填写需求和数量。
3. 设置协作模式和阶段。
4. 设置奖励。
5. 确认发布。

列表卡字段：

- 标题。
- 需求摘要。
- 奖励。
- 剩余时间或状态。
- 主按钮。

验收：

- 发布委托不再占据首屏。
- 可接委托列表一屏至少能看到 3 张卡。

### 22.7 `OnlineSocietyView.vue`

P0 改造：

- 创建村社进入向导。
- 邀请玩家使用 `OnlineInvitePanel`。
- 申请处理进入详情抽屉。
- 发起提案进入弹窗。
- 公共工程贡献保持主按钮，但阶段详情和历史进入抽屉。
- 「创建房间」类入口接 `OnlineRoomWizard` 预填模板，而不是只跳到节会页。

P1 改造：

- 成员管理拆为普通成员列表和管理抽屉。
- 仓廪入仓 / 消耗使用确认弹窗，说明只扣公共仓。
- 史册默认按时间线展示，长记录分页或抽屉。

验收：

- 非管理员看不到管理主按钮。
- 管理员首屏看到待处理申请和邀请数量。

### 22.8 `OnlineManorView.vue`

P0 改造：

- 主图上传、主题保存、留言、护理房创建进入弹窗。
- 主人态默认展示「管理展示、查看留言、处理照料」。
- 访客态默认展示「留言、照料、轻采」。
- 照料记录、轻采凭证、访客记录进入详情抽屉。

P1 改造：

- 护理房独立成 `ManorCareRoomPanel`。
- 轻采风险、反刷审计、主人保留比例进入可折叠规则说明。

验收：

- 访客进入庄园首屏能看到庄园预览和访客主行动。
- 主人进入庄园首屏能看到管理主行动和最新留言 / 照料摘要。

### 22.9 可视化组件

`VisualMapBoard.vue`、`VisualSceneBoard.vue`、`VisualTrackBoard.vue` 当前已经承担舞台能力，不建议重写。

P0 只做：

- 失败原因改玩家语言。
- 动作结果固定回读到房间顶部反馈。
- 移动端选中对象后打开底部详情，不把详情挤在舞台下方。

P1 再做：

- 对象详情抽屉化。
- 轨道 / 场景操作按钮统一大小。
- 热区触控目标不低于 44px。

## 23. 状态、空态、错误态规范

### 23.1 状态类型

| 状态 | 展示方式 | 示例文案 |
| --- | --- | --- |
| loading | 骨架 / 轻提示 | 正在同步房间信息 |
| empty | 空态卡 + 主按钮 | 现在还没有房间，可以创建一个 |
| blocked | 警告条 + 解决按钮 | 房间信息有更新，请刷新后继续 |
| success | 成功条 + 下一步 | 邀请已发送，等待对方加入 |
| danger | 确认弹窗 | 关闭后成员将无法继续行动 |
| technical | 折叠详情 | 查看凭证与审计信息 |

### 23.2 空态模板

无房间：

- 标题：「还没有进行中的房间」。
- 说明：「可以创建一个房间，或从可加入房间里找队伍」。
- 主按钮：「创建房间」。
- 次按钮：「查看可加入房间」。

无邀请：

- 标题：「暂无待处理邀请」。
- 说明：「收到好友或村社邀请后会在这里出现」。
- 主按钮：「创建自己的房间」。

无可见房间：

- 标题：「暂时没有可加入房间」。
- 说明：「可以稍后刷新，或自己开一个房间邀请朋友」。
- 主按钮：「刷新」。
- 次按钮：「创建房间」。

### 23.3 错误态模板

网络失败：

- 「暂时连不上在线服务，请检查网络后重试」。

状态冲突：

- 「房间信息有更新，请刷新后继续」。

权限不足：

- 「当前身份不能执行这个操作」。

高风险缺确认：

- 「请先确认本次操作会影响的共同资产」。

结算失败：

- 「结算暂未完成，房间仍保留，可以稍后重试」。

## 24. 文案体系与替换清单

### 24.1 写作规则

- 按玩家视角写，不按系统实现写。
- 优先说下一步，不先说技术原因。
- 中文优先，不混用英文状态名。
- 风险动作必须写清影响范围。
- 成功反馈必须告诉玩家结果去了哪里。

### 24.2 第一批替换

| 当前 | 替换 |
| --- | --- |
| 开始 ready | 开始准备 |
| ready | 准备 |
| 服务端房间状态存在冲突 | 房间信息有更新，请刷新后继续 |
| 服务端落账 | 奖励已记录 |
| 服务端凭证已生成 | 结算记录已生成 |
| 降级入口 | 备用操作 |
| 旧按钮降级入口 | 备用操作面板 |
| 缺失配置 | 内容暂未完整 |
| fallback | 备用路径 |
| per-member receipt | 成员结算记录 |
| ledger | 流水记录 |
| hash | 校验码 |
| idempotency / 幂等 | 重复提交保护 |
| record-only | 只记录，不改资产 |
| 主态 | 主存档状态 |
| 安全阀 | 保护机制 |
| 模拟断线 | 调试：断开连接 |

### 24.3 示例改写

当前：

```text
服务端节会房间状态存在冲突，请刷新后再继续提交。
```

建议：

```text
房间信息有更新，请刷新后继续。
```

当前：

```text
旧节会按钮和结算入口仍在房间下方，移动端与键盘用户可继续从降级入口操作。
```

建议：

```text
如果现场画面暂时不可用，可以展开备用操作继续参与。
```

当前：

```text
服务端落账：80 铜钱、1 张奖券。
```

建议：

```text
奖励已记录：80 铜钱、1 张奖券。
```

## 25. 响应式布局详细规则

### 25.1 移动端 360-430px

布局：

- 顶部：模块标题 + 状态摘要。
- 中部：当前主卡 / 舞台 / 列表。
- 底部：sticky action bar。
- 详情：bottom sheet。
- 技术详情：折叠区，默认关闭。

限制：

- 主按钮高度至少 44px。
- 输入框高度至少 40px。
- 弹窗底部必须预留安全区。
- 页面主体底部必须预留 sticky bar 高度。
- 不允许横向滚动，除非是明确的 tab 或舞台轨道。

### 25.2 平板 768-1024px

布局：

- 顶部状态区。
- 双栏内容：左侧主内容，右侧状态 / 成员 / 详情。
- 弹窗可居中，也可右侧抽屉。
- Tab 可横向滚动但要有选中态。

### 25.3 桌面 1280px 以上

布局：

- 左侧：导航 / 列表。
- 中间：主舞台 / 主表格。
- 右侧：成员 / 状态 / 摘要 / 技术详情。

限制：

- 不把所有卡片用 `xl:grid-cols-6` 平铺成密集表盘。
- 主舞台宽度优先，右侧栏宽度固定在 320-380px。
- 弹窗最大宽度按任务区分：普通 448px，向导 672px，复杂确认 576px。

## 26. 可访问性与交互细节

弹窗：

- 打开时焦点进入弹窗。
- 关闭时焦点返回原按钮。
- 背景不可被屏幕阅读器重复读到。
- Esc 行为一致。
- 危险操作不允许误点遮罩关闭。

Tab：

- `OnlineModuleShell` 使用 `tablist`、`tab`、`tabpanel`。
- 选中 tab 有 `aria-selected`。
- 键盘左右方向可切换，至少不阻断 Tab 顺序。

按钮：

- 图标按钮必须有 `aria-label`。
- 禁用按钮旁边或 tooltip 中说明原因。
- 风险按钮统一 danger 样式。

动态反馈：

- 行动成功、失败、状态变化使用 `aria-live="polite"`。
- 冲突和失败使用 `aria-live="assertive"`。

## 27. 数据与状态接入原则

### 27.1 不改后端语义

第一阶段不改：

- 房间状态机。
- 结算接口。
- 审计接口。
- 补偿接口。
- store action 名称。
- API payload 字段。

只改：

- 触发入口。
- 弹窗状态。
- 文案展示。
- 详情折叠。
- 测试断言。

### 27.2 弹窗状态管理

建议每个页面先用轻量本地状态：

```ts
type OnlineDialogState =
  | { kind: 'none' }
  | { kind: 'create-room'; domain: 'festival' | 'expedition' }
  | { kind: 'invite'; domain: 'festival' | 'expedition' | 'society' | 'neighbor'; roomId?: string }
  | { kind: 'lobby'; domain: 'festival' | 'expedition'; roomId: string }
  | { kind: 'confirm'; action: string; targetId: string }
```

等节会 / 远征 P0 稳定后，再抽到 composable：

- `useOnlineDialogState()`。
- `useOnlineConfirmAction()`。
- `useOnlineInviteDraft()`。

### 27.3 测试 ID 迁移

策略：

- 第一轮保留旧按钮 test id，但按钮可以移动到弹窗内。
- 新增弹窗 test id 用于新 E2E。
- 待 E2E 迁移完成后再清理旧 test id。

示例：

- 旧：`online-festival-room-settle-submit`。
- 新：`online-room-confirm-settle-submit`。
- 兼容：结算弹窗主按钮短期同时带两者中的旧关键 test id 或新增 alias wrapper。

## 28. QA 详细计划

### 28.1 静态结构检查

更新 `qa-online-ui-structure.mjs`：

- 检查新增通用组件文件存在。
- 检查在线房间页引用 `OnlineRoomWizard`。
- 检查在线房间页引用 `OnlineRoomLobbyDialog`。
- 检查在线房间页引用 `OnlineInvitePanel`。
- 检查高风险按钮通过 `OnlineConfirmActionDialog`。
- 检查玩家模板禁用词不出现在主路径。
- 检查 `OnlineModuleShell` 有 `tablist` 语义。

### 28.2 文案检查

新增 `qa-online-player-copy.mjs`：

- 扫描 `src/views/game/OnlineView.vue`。
- 扫描 `src/views/game/ExpeditionRoomView.vue`。
- 扫描 `src/views/game/online/*.vue`。
- 扫描 `src/components/game/online/*.vue`。
- 排除 `data-testid`、类型字段、技术详情组件、脚本注释。
- 输出文件、行号、命中词、建议替换。

### 28.3 移动端 smoke

更新 `qa-mobile-ui-smoke.mjs`：

- `online-center-action-mobile-390x844`。
- `online-center-action-mobile-360x780`。
- `online-festival-create-room-wizard-mobile-390x844`。
- `online-festival-invite-panel-mobile-390x844`。
- `online-festival-lobby-ready-mobile-390x844`。
- `online-expedition-create-room-wizard-mobile-390x844`。
- `online-room-settle-confirm-mobile-390x844`。
- `online-cohabitation-family-festival-confirm-mobile-390x844`。

断言：

- 主按钮在首屏。
- 弹窗或抽屉可见。
- 底部按钮不超出视口。
- 无横向溢出。
- 技术词不在主路径可见。

### 28.4 E2E

更新 `game-smoke.spec.ts`：

- 房主创建节会房间完整路径。
- 房主创建远征房间完整路径。
- 成员接受邀请并准备。
- 房主开始准备和倒计时。
- 房主结算确认。
- 房主关闭确认。
- Esc 关闭普通弹窗。
- 危险弹窗需要确认文字。
- 焦点返回触发按钮。

### 28.5 验证命令

前端：

```bash
cd taoyuan-main
npm run qa:online-ui-structure
npm run qa:online-visual-feature-flags
npm run qa:mobile-ui-smoke
npm run test:e2e
npm run check
```

服务端保持：

```bash
cd server
npm run qa:online-smoke
```

如果服务端脚本名与当前 `server/package.json` 不一致，以实际 package script 为准。

## 29. 实施排期建议

### 第 0 天：准备与保护

- 确认当前主分支测试基线。
- 给 `0531优化.md` 对应工作开分支。
- 先新增文案扫描脚本的 allowlist 草案，不启用阻断。
- 确认现有 E2E 哪些断言依赖旧文案。

### 第 1-2 天：通用组件

- 实现 `OnlineActionDialog.vue`。
- 实现 `OnlineBottomSheet.vue`。
- 实现 `OnlineStickyActionBar.vue`。
- 实现 `OnlineTechnicalDetails.vue`。
- 给组件加基础样式和 test id。
- 更新 `app.css` 支持抽屉、安全区、滚动锁定。

验收：

- 组件可单独渲染。
- Esc、点击遮罩、焦点返回可用。
- 移动端底部抽屉不遮挡安全区。

### 第 3-5 天：房间核心流程

- 实现 `OnlineRoomWizard.vue`。
- 实现 `OnlineInvitePanel.vue`。
- 实现 `OnlineRoomLobbyDialog.vue`。
- 接入 `OnlineFestivalView.vue` 节会房。
- 接入 `OnlineFestivalView.vue` 远征房。
- 接入 `ExpeditionRoomView.vue` 独立远征页。

验收：

- 房主完整路径跑通。
- 成员准备路径跑通。
- 结算 / 关闭确认跑通。

### 第 6-7 天：在线中心与文案

- 在线中心首页改为行动导向。
- 治理信息折叠。
- 第一批开发态文案替换。
- 技术详情承接凭证 / 降级 / 开关。

验收：

- 在线中心首屏不再像 QA 面板。
- 玩家主路径不出现禁用词。

### 第 8-10 天：共同庄园 P0

- 标签分组。
- 家族节会席位弹窗化。
- 资产高风险确认弹窗化。
- 审计摘要 / 技术详情拆分。

验收：

- 12 标签不再全部平铺给玩家。
- 高风险动作都有确认说明。

### 第 11-14 天：其他在线页

- 邻里创建 / 邀请弹窗化。
- 委托发布 / 接单 / 交付弹窗化。
- 村社创建 / 邀请 / 提案弹窗化。
- 庄园留言 / 上传 / 护理房弹窗化。

验收：

- 各页面首屏都有明确主行动。
- 长表单不直接出现在首屏。

### 第 15-17 天：QA 与回归

- 更新静态结构检查。
- 更新移动端 smoke。
- 更新 E2E。
- 跑 `npm run check`。
- 记录剩余风险和已知问题。

## 30. 风险、回滚与兼容

### 30.1 主要风险

- E2E 大量依赖旧 test id 和旧文案，迁移成本高。
- 联机页面状态复杂，弹窗移动按钮后可能遗漏某些状态下的入口。
- 共同庄园单文件过长，拆分时容易引入响应式或类型回归。
- 技术详情折叠后，QA 仍需要能稳定定位凭证和降级说明。

### 30.2 缓解

- 第一轮保留旧 test id。
- 每个状态都写主按钮矩阵。
- 先只迁移展示层，不改 store action。
- 技术详情保留稳定 test id。
- 每页先做 P0 主路径，不同时重构视觉细节。

### 30.3 回滚策略

- 通用组件独立 PR，可单独回滚。
- 页面接入以 feature flag 或局部开关保护，例如 `useNewOnlineRoomFlow`。
- 如果新弹窗路径异常，保留「备用操作」折叠入口临时恢复旧按钮。
- 测试迁移分批进行，不一次删除旧断言。

## 31. 最终验收清单

产品验收：

- 在线中心像玩家入口，不像调试总览。
- 房主开房有步骤。
- 邀请和准备有大厅。
- 结算和关闭有确认。
- 共同庄园不再 12 标签全部压给玩家。
- 移动端不用滚动半页找主按钮。
- 桌面端不再把所有信息两列平铺。

工程验收：

- 通用弹窗 / 抽屉组件复用。
- 玩家态文案扫描通过。
- 原 API 和 store 语义不变。
- 关键 E2E 通过。
- 移动端 smoke 通过。

QA 验收：

- 360x780、390x844、1366x768、1920x1080 都有证据。
- 主路径截图可读。
- 高风险动作截图包含影响说明。
- 技术详情仍可找到凭证和审计信息。

## 32. 建议先做的 10 个具体任务

1. 新建 `OnlineActionDialog.vue`，把节会结算接进去。
2. 新建 `OnlineInvitePanel.vue`，替换节会房间邀请输入框。
3. 新建 `OnlineRoomLobbyDialog.vue`，替换节会房 `开始 ready / 我已准备 / 倒计时`。
4. 新建 `OnlineRoomWizard.vue`，替换节会创建表单。
5. 把远征房复用同一套 wizard / lobby / invite。
6. 把 `模拟断线` 移进技术详情。
7. 在线中心把功能开关、奖励控制折叠进「调试与治理信息」。
8. 共同庄园家族节会四个高风险按钮先弹窗化。
9. 新增玩家态文案扫描脚本。
10. 更新移动端 smoke 覆盖房主开房、邀请、准备、结算。

