# 0520 联机 WebSocket 规划

## 0. 文档定位

这份文档不是“再加一个联机入口”，而是把 **好友系统 + 实时通讯 + 房间协作** 合并成一套联机底座。

本版默认前提：
- 好友关系按 **存档级** 实现。
- 每个存档拥有一个 **固定数字 ID**。
- 存档 ID 可公开搜索，**昵称可改，ID 不可改**。
- 好友系统和 WebSocket 是并列主线，不把好友当附属功能。
- 旧档必须兼容，本地档、云存档、导入档都不能要求玩家手动重开。

## 1. 当前问题

现有联机已经有账号、云存档、公开名片、大厅、邮箱、庄园、节会、村社等能力，但好友关系仍偏“账号级”语义，和后续的联机房间、邀请、在线状态并没有统一到同一套身份体系里。

所以这版要先解决两件事：
- 把“人”从账号层下沉到 **存档身份**。
- 把“消息/状态/邀请/房间动作”统一到 **实时通讯层**。

## 2. 目标

- 每个存档都有自己的数字 ID，可复制、可搜索、可加好友。
- 好友关系以存档为准，同账号不同存档彼此独立。
- 好友申请、接受/拒绝、删除好友、最近互动、互动入口都能用。
- WebSocket 负责强实时；SSE/轮询只负责轻实时通知与刷新。
- 旧档自动补发存档 ID，不做手动迁移。

## 3. 存档 ID 规则

- ID 在服务端生成并持久化。
- 同一账号下的不同存档可以拥有不同 ID。
- ID 只负责身份，不承载存档内容。
- 玩家可以改昵称、公开名片，但不能改 ID。
- ID 搜索只返回名片级公开信息，不泄露存档数据。

## 4. 好友系统范围

第一版至少包含：
- 按 ID 搜索玩家。
- 发送好友申请。
- 接受 / 拒绝好友申请。
- 好友列表。
- 删除好友。
- 拉黑 / 解除拉黑。
- 好友互动入口：庄园访问、书信、送礼、房间邀请、协作跳转。

## 5. 旧档兼容

兼容范围：
- 本地存档
- 云存档
- 导入档

兼容原则：
- 不改坏旧 payload。
- 旧档第一次读到联机身份时自动补发存档 ID。
- 如果旧档没有好友数据，默认初始化为空图谱。
- 现有账号级好友数据作为迁移来源，默认映射到当前补发 ID 的存档。
- 读档后若当前存档可写，则把补发结果回写到存档元数据中。

## 6. 数据与服务端权威

服务端必须是好友关系与实时状态的权威来源，客户端只提交意图。

建议拆成三类数据：
- `SaveIdentity`：存档 ID、账号、槽位、昵称快照、创建/更新时间。
- `FriendGraph`：好友关系、好友申请、拉黑关系。
- `RealtimeState`：在线状态、订阅连接、未读通知、房间订阅。

## 7. 实时通讯方案

先按 **原生 WebSocket** 规划，不优先引入 `socket.io`。

分层建议：
- `SSE / 轮询`：好友提醒、轻量通知、榜单刷新、活动进度。
- `WebSocket`：在线状态、好友申请推送、邀请、房间状态、房间动作。

事件只做投递，不做最终结算。结算仍以服务端落盘为准。

## 8. 事件清单

建议首批事件：
- `friend.request.created`
- `friend.request.accepted`
- `friend.request.rejected`
- `friend.removed`
- `presence.online`
- `presence.offline`
- `notification.created`
- `room.invite.created`
- `room.state.changed`

## 9. 分阶段推进

### Phase 1
- 存档 ID 生成与回写
- 旧档自动补发
- 存档级好友关系图谱

### Phase 2
- 好友申请、列表、拉黑、删除
- 好友名片与互动入口
- 好友数据从账号级迁移到存档级

### Phase 3
- WebSocket 在线状态与实时通知
- 邀请与房间状态同步
- 断线重连与未读补发

### Phase 4
- 把好友系统嵌进庄园、邮箱、节会、村社、协作单
- 让现有联机玩法都能复用同一套身份与通知底座

## 10. 验收标准

- 旧档可直接打开，不丢数据。
- 旧档首次读档后能自动生成存档 ID。
- 能通过数字 ID 搜索并加好友。
- 好友申请、通过/拒绝、删除都可用。
- WebSocket 断线重连后能恢复好友通知与在线状态。
- 现有云存档、邮箱、大厅、单机玩法不被破坏。

