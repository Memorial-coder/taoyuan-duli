# 0603 后台加强 TODO（AI 接手版）

> 目标：增强管理端日志留存、违法有害信息过滤屏蔽工作，加强违法有害和不良信息的防范、发现、拦截和处置能力。

## 接手提示

- 先读现有实现，不要另起一套后台：当前项目已经有文本审核、图片审核、后台审计、在线审计和举报处置雏形。
- 优先复用 `server/src/taoyuanTextModeration.js`、`server/src/taoyuanImageModeration.js`、`server/src/taoyuanOnlineAudit.js`、`server/src/db.js` 的现有存储和 helper。
- 管理端入口主要在 `taoyuan-main/src/views/UserAdminView.vue`、`taoyuan-main/src/views/TaoyuanAdminView.vue`、`taoyuan-main/src/views/HallView.vue`。
- 服务端路由主要在 `server/src/routes/api.js`。先用 `rg -n` 查相关 action，不要凭记忆改。
- 不要把敏感词规则下发到前端；规则和命中详情只能在服务端、管理端权限页面、审计日志里可见。
- 不要记录明文 IP、UA、设备指纹等可识别信息；如需追踪，使用服务端盐值做 hash，并记录截断后的摘要。

## 现有事实源

- 文本过滤：`server/src/taoyuanTextModeration.js`
  - 已有硬拦截词、软拦截词、长度/换行校验。
  - 已在账号名、展示名、邮件、部分社交/庄园/社团等服务端模块中复用。
- 图片审核：`server/src/taoyuanImageModeration.js`
  - 已有上传登记、图片举报、图片隐藏、图片黑名单、上传大小限制和可见性检查。
- 后台审计：`server/src/db.js`
  - `recordAdminAuditLog`、`listAdminAuditLogs` 已存在。
  - MySQL 与 JSON fallback 都有审计存储路径。
- 在线审计：`server/src/taoyuanOnlineAudit.js`
  - 当前 `ONLINE_AUDIT_LOG_LIMIT = 5000`，有被高频日志顶掉证据的风险。
  - `server/src/routes/api.js` 已通过 `ONLINE_AUDIT_ROUTE_RULES` 记录部分关键在线写操作。
- 举报/处置入口：`server/src/routes/api.js`
  - 大厅帖子/回复举报：`/admin/taoyuan/hall/reports`
  - 图片举报：`/admin/taoyuan/hall/image-reports`
  - 图片隐藏、图片黑名单、帖子隐藏、回复删除等动作已存在，但需要逐一确认是否都写入后台审计。
- 合规基线参考：
  - 《中华人民共和国网络安全法》要求相关网络日志留存不少于六个月，并要求对禁止发布或传输的信息采取处置措施、保存有关记录。
  - 《网络信息内容生态治理规定》要求平台建立内容审核、投诉举报、应急处置等机制，处置违法和不良信息。

## 总体交付目标

- 让后台形成完整闭环：发现 -> 拦截 -> 待审 -> 处置 -> 留痕 -> 复核/恢复 -> 统计。
- 所有用户生成内容入口都经过统一服务端审核。
- 所有管理员治理动作都可追溯。
- 日志按时间保留，不被简单数量上限挤掉。
- 管理端能看到待处理内容、已处置内容、重复违规用户、规则命中趋势。

## P0：必须先做

### P0-1 统一日志留存策略

- [x] 把 `taoyuanOnlineAudit.js` 的固定 5000 条上限升级为按天归档或按月份归档。
- [x] 设置默认保留期：
  - 普通后台审计、在线关键操作、鉴权失败、限流拦截：不少于 180 天。
  - 内容治理处置证据、封禁/解封、严重违规举报：建议不少于 365 天。
- [x] 新增可配置项，建议放在服务端 env 或 `data-defaults/sys_config.json` 兼容结构：
  - `ADMIN_AUDIT_RETENTION_DAYS=180`
  - `CONTENT_MODERATION_RETENTION_DAYS=365`
  - `ONLINE_AUDIT_RETENTION_DAYS=180`
- [x] 每条审计日志补齐最小字段：
  - `id`
  - `request_id`
  - `created_at`
  - `actor_username`
  - `actor_role`
  - `target_username`
  - `target_type`
  - `target_id`
  - `action`
  - `outcome`
  - `reason`
  - `rule_version`
  - `ip_hash`
  - `ua_hash`
  - `detail`
- [x] 写一个清理函数，只清理超过保留期且非重大处置证据的日志。
- [x] 不要在请求链路里同步做重型清理；可以在服务启动、管理端手动触发或轻量定期触发。

验收：

- [x] 连续写入超过 5000 条在线审计后，最近 180 天日志不会丢。
- [x] 管理端能按时间、用户、动作、结果查询日志。
- [x] 超期清理不会删除封禁、违法有害处置、申诉恢复等重大证据。

本轮证据（2026-06-05）：

- `server/src/taoyuanOnlineAudit.js` 移除固定 `ONLINE_AUDIT_LOG_LIMIT = 5000` 截断，改用 `ONLINE_AUDIT_RETENTION_DAYS` / `online_audit_retention_days`，并新增 `pruneOnlineAudits()`；普通写入不再同步执行重型清理。
- `server/src/db.js` 新增 `pruneAdminAuditLogs()`，`server/src/taoyuanContentModerationAudit.js` 新增 `pruneContentModerationEvents()`；`/admin/taoyuan/audit-retention/prune` 可手动清理后台审计、在线审计和内容审核事件，并写入后台审计。
- `server/src/config.js`、`data-defaults/sys_config.json` 已追加 `admin_audit_retention_days=180`、`content_moderation_retention_days=365`、`online_audit_retention_days=180`；env 仍优先生效。
- `server/scripts/qa-admin-audit-retention.mjs` 验证 5010 条在线审计不会被 5000 上限截断，且超期清理保留 `ban_user_for_image` / critical 内容审核事件等重大证据。
- `server/src/db.js` 的 `recordAdminAuditLog()` 已保留调用方传入的 `created_at`，`listAdminAuditLogs()` 支持 `created_from` / `created_to`、目标用户、操作者、动作、结果组合过滤。
- `server/src/taoyuanOnlineAudit.js` 的 `listOnlineAudits()` 支持 `created_from` / `created_to` 时间范围过滤；`/admin/audit-logs`、`/admin/taoyuan/audit-logs`、`/admin/taoyuan/online-audit` 已透传时间、用户、动作、结果等查询参数。
- `server/scripts/qa-admin-audit-retention.mjs` 新增在线审计时间范围断言、后台审计时间/用户/操作者/动作/结果组合过滤断言，并验证超期普通后台审计清理不影响重大证据。
- `server/src/db.js` 新增后台审计最小字段标准化：`recordAdminAuditLog()` 写入时补齐 `request_id`、`actor_username`、`actor_role`、`target_username`、`target_type`、`target_id`、`action`、`outcome`、`reason`、`rule_version`、`ip_hash`、`ua_hash`；`listAdminAuditLogs()` 在 MySQL 与 JSON fallback 返回时把这些字段提升为顶层字段，同时保留 `detail`。
- `server/scripts/qa-admin-audit-retention.mjs` 新增最小字段断言，验证后台审计响应包含 `id`、`created_at`、`request_id`、`actor_username`、`actor_role`、`target_username`、`target_type`、`target_id`、`action`、`outcome`、`reason`、`rule_version`、`ip_hash`、`ua_hash`、`detail`，且缺省 `outcome` 会归一为 `completed`。
- 风险：历史已落盘的旧后台审计文件不会物理回填 detail 字段，但查询返回层会补默认值；MySQL 表结构仍未拆出 `request_id`、`outcome`、`ip_hash`、`ua_hash` 等新列，MySQL 侧结果过滤仍依赖 detail JSON 字符串匹配，精确度低于 JSON fallback 的结构化解析。

### P0-2 建立内容审核事件流水

- [x] 新增服务端模块，建议：`server/src/taoyuanContentModerationAudit.js`。
- [x] 新增存储文件或表，建议：
  - JSON fallback：`data/taoyuan_content_moderation_events.json`
  - MySQL 表：`content_moderation_events`
- [x] 审核事件字段建议：
  - `id`
  - `request_id`
  - `scene`
  - `field`
  - `username`
  - `content_type`
  - `content_id`
  - `action`
  - `severity`
  - `matched_category`
  - `matched_term_hash`
  - `rule_version`
  - `content_hash`
  - `content_excerpt`
  - `outcome`
  - `created_at`
- [x] 修改 `moderateText`：命中硬拦截或软拦截时，允许调用方传入 `auditContext`，由服务端记录事件。
- [x] `content_excerpt` 只保留短摘要，不保存完整敏感内容；完整内容如确需保留，只能进入重大证据存储并加权限。

验收：

- [x] 用户提交违规文本被拦截时，前端只看到通用提示，服务端有审核事件。
- [x] 管理端能看到命中类别、场景、用户、时间、处置结果。
- [x] 日志里不暴露完整敏感词表。

本轮证据（2026-06-05）：

- 新增 `server/src/taoyuanContentModerationAudit.js`，JSON fallback 写入 `data/taoyuan_content_moderation_events.json`，字段包含 `request_id`、`scene`、`field`、`username`、`content_type`、`content_id`、`action`、`severity`、`matched_category`、`matched_term_hash`、`rule_version`、`content_hash`、`content_excerpt`、`outcome`、`created_at`。
- `server/src/taoyuanTextModeration.js` 支持 `auditContext`，硬拦截、软拦截和长度/换行拒绝都会记录事件；错误对象只保留 `hit_term_hash`，不再暴露 `hit_term` 明文。
- 新增 `/admin/taoyuan/content-moderation/events`，`/admin/taoyuan/overview` 和 `/admin/taoyuan/hall/overview` 也返回最近审核事件。
- `server/scripts/qa-content-moderation-guard.mjs` 覆盖正常文本、硬拦截、软拦截、字段长度、事件字段，以及注册、玩家邮件/礼物、后台邮件、庄园、社交/邻里、社团、协作委托等入口的命中事件；确认事件只有 hash 与短摘要。
- `taoyuan-main/src/components/game/AdminOnlineGovernancePanel.vue` 的内容治理闭环面板已拉取 `/admin/taoyuan/content-moderation/events`，并展示审核事件的场景、用户、时间、命中类别、处置结果、规则版本、字段和短摘要；支持按场景、动作、时间、处置结果筛选。
- `taoyuan-main/scripts/qa-online-ui-structure.mjs` 新增结构断言，验证管理端内容治理面板包含 `event.scene`、`event.username`、`formatTime(event.created_at)`、`event.matched_category`、`event.outcome` 和 `eventOutcomeFilter`。
- 风险：本轮未新增 MySQL `content_moderation_events` 表；内容治理面板展示的是脱敏短摘要和 hash 关联信息，不展示完整原文或敏感词明文。

### P0-3 补齐所有治理动作的后台审计

逐一检查 `server/src/routes/api.js`，凡是管理员改变内容可见性、举报状态或用户状态，都必须调用 `appendAdminAuditLog`。

重点检查：

- [x] `/admin/taoyuan/hall/reports/:id/status`
- [x] `/admin/taoyuan/hall/image-reports/:id/status`
- [x] `/admin/taoyuan/hall/image-reports/:id/hide`
- [x] `/admin/taoyuan/hall/image-assets/hide`
- [x] `/admin/taoyuan/hall/posts/:id/hide`
- [x] `/admin/taoyuan/hall/posts/:id/replies/:replyId`
- [x] `/admin/taoyuan/hall/posts/:id/pin`
- [x] `/admin/taoyuan/hall/posts/:id/feature`
- [x] `/admin/taoyuan/image-blacklist/:username`
- [x] 用户封禁、解封、删除、重置密码、额度调整、存档导出、存档迁移等已有动作也要确认字段完整。

审计 detail 至少包含：

- [x] 处置原因
- [x] 处置前状态
- [x] 处置后状态
- [x] 被处置内容 ID
- [x] 举报 ID 或图片 ID
- [x] 是否由多人举报触发
- [x] 管理员备注

验收：

- [x] 对帖子、回复、图片、用户做任何后台治理动作后，`/admin/audit-logs` 和管理端审计页都能查到记录。
- [x] 可以从举报记录反查最终处置动作。
- [x] 可以从用户详情反查最近治理记录。

本轮证据（2026-06-05）：

- `server/src/routes/api.js` 的 9 个大厅/图片/黑名单治理路由均已在处置成功后调用 `appendAdminAuditLog`，detail 包含 `request_id`、`actor_role`、`target_type`、`target_id`、`before_status`、`after_status`、`reason`、`report_id` / `image_url` / `post_id` / `reply_id`、`admin_note`。
- `appendAdminAuditLog` 统一补 `ip_hash`、`ua_hash`，不记录明文 IP / UA；黑名单、封禁、隐藏、删除回复等动作被 `pruneAdminAuditLogs()` 视为重大证据，不会被普通超期清理删除。
- `server/src/routes/api.js` 已补齐用户额度、重置密码、状态封禁/解封、单个删除、批量删除、存档导出、存档迁移、内容治理面板解封的审计 detail；字段包含 `target_type`、`target_id`、`before_status`、`after_status`、`before_quota` / `after_quota`、原因、管理员备注、重大证据标记和存档摘要。
- `server/src/db.js` 的 `listAdminAuditLogs()` 新增 `targetUsername` / `target_username` 过滤；`/admin/users/:username` 现在返回 `recent_governance_logs`，可从用户详情反查最近封禁、解封、额度、重置密码、删除、存档导出/迁移等治理记录。
- `server/scripts/qa-user-governance-audit.mjs` 启动临时服务端覆盖额度调整、重置密码、封禁、内容治理解封、存档导出、存档迁移、单删、批删和用户详情反查；断言审计 detail 有前后状态/目标/原因/备注/存档摘要，且不包含明文 IP、UA、密码或存档正文。
- `server/src/routes/api.js` 新增多人举报审计上下文推导：管理员处置同一帖子/回复/图片的任一相关举报时，都会从同目标举报集合里反查 `auto_action`，并在审计 detail 中写入 `multi_report_triggered`、`auto_action`、`auto_action_at`、`multi_report_report_count`、`multi_report_reporter_count`、`multi_report_report_ids`。
- `server/scripts/qa-report-disposition-flow.mjs` 新增真实大厅多人举报场景：第三个举报自动临时隐藏帖子后，管理员处置第一条举报，后台审计仍能记录 `multi_report_triggered=true` 和 `auto_hide_hall_post`。
- `server/scripts/qa-image-moderation-flow.mjs` 新增真实图片多人举报场景：第三个图片举报自动临时隐藏图片后，管理员按第一条举报执行隐藏处置，后台审计仍能记录 `multi_report_triggered=true` 和 `auto_hide_image_asset`。
- 风险：用户治理审计的 MySQL `targetUsername` 过滤对 detail 使用 `LIKE` 兜底，精确度低于 JSON 查询，但 JSON fallback 和用户详情 QA 已覆盖主要链路；多人举报阈值仍为服务端常量，尚未外置配置。

### P0-4 统一 UGC 文本入口

检查所有用户可提交文本的服务端入口，必须走 `moderateText` 或 `moderateCompositeText`。

优先入口：

- [x] 注册用户名、展示名
- [x] 大厅发帖标题、正文、富文本 blocks、回复、举报原因
- [x] 图片 alt、图片举报原因
- [x] 玩家邮件/礼物留言
- [x] 庄园留言、封面说明、访问策略文案
- [x] 社团名称、简介、公告、仓库说明
- [x] 好友/邻居/黑名单备注
- [x] AI 助理提问内容
- [x] 后台首页内容、系统邮件
- [x] 活动公告等其他后台发布内容

验收：

- [x] 任意入口提交硬拦截内容，服务端拒绝并记录审核事件。
- [x] 任意入口提交软风险内容，按场景进入待审或记录风险。
- [x] 前端不能绕过服务端审核。

本轮证据（2026-06-05）：

- `server/src/taoyuanHall.js` 已对大厅标题、正文文本 blocks、图片 alt、回复、帖子/回复举报原因、上传图片说明执行 `moderateText`。
- `server/src/taoyuanImageModeration.js` 已对图片举报原因执行 `moderateText`，并通过路由传入 `auditContext`。
- `server/src/db.js` 的注册用户名、展示名已支持 `auditContext`；`/register` 传入 request 级上下文，`qa-content-moderation-guard.mjs` 覆盖注册违规命中事件。
- `server/src/taoyuanMailbox.js` 已对玩家书信标题/正文/图片说明、礼物包裹标题/附言、系统/后台邮件 campaign 标题/正文执行 `moderateText` 并记录场景；QA 覆盖玩家书信、礼物包裹、后台邮件 campaign 违规事件。
- `server/src/taoyuanManorRuntime.js` 已对庄园访问摘要/反馈、留言、留言回复、主题周标题、封面说明、偷菜留言接入审核事件；访问策略当前仅枚举模式，无用户自由文案。
- `server/src/taoyuanSocialRuntime.js` 已对公开名片、头像说明、邻里名称/简介/公告、订阅标签接入审核事件；本轮复核 `requestFriendship`、好友关系和 `blockPlayer` / `unblockPlayer` 数据结构，当前好友申请、好友关系、黑名单关系没有备注/留言自由文本字段，不再列为现有审核缺口。
- `server/src/taoyuanSocietyRuntime.js` 已对村社名称/简介/公告/入社说明、公告更新、提案标题/摘要、提案归档说明接入审核事件；公共仓库当前仅使用预设 deposit/consume id，无用户自由文案说明。
- `server/src/taoyuanCoopOrderRuntime.js` 已对协作委托标题/说明、阶段标题/说明、普通/阶段交付说明接入审核事件。
- `server/src/taoyuanActivityRoomRuntime.js` 新增活动房间审核上下文，对节会/远征房间标题、来源说明、来源标签执行 `moderateText`；`/taoyuan/online/festival/rooms`、`/taoyuan/online/expedition/rooms` 改传 `getSessionActor(req)`，命中事件可记录 request_id、scene、field、content_id。
- `server/src/taoyuanCohabitationRuntime.js` 新增同居/家族审核上下文，对同居契约标题、家族订单标题/备注、家族节会房间标题/备注、共同家庭心愿标题/备注、恢复申诉说明、分居预览原因、分居执行失败说明/备注接入审核事件；相关裸 actor 路由已改传 `getSessionActor(req)`。
- `server/src/taoyuanCohabitationRuntime.js` 继续补齐共同基金冻结/解冻原因与备注、同居安全版本回滚原因、家族订单接取/交付/结算备注、家族声望发放/奖励领取备注、家族公开设置/回滚备注、家族节会席位/供品/结算备注的服务端审核入口。
- `server/src/taoyuanCohabitationRuntime.js` 继续补齐共同农田浇水/管护/种植/施肥/收获备注、共同动物喂食/购买命名/抚摸/产物收集/出售备注、共同宠物照料备注、共同工坊加工备注的服务端审核入口；对应 POST 路由改传 `getSessionActor(req)`，保留 request_id 审核上下文。
- `server/src/taoyuanCohabitationRuntime.js` 继续补齐共同仓库高价值取出草案/确认/执行/回滚原因、补偿复核/处理/预检/自动补偿阻断/执行/人工申诉/票据审计说明、共同仓库治理申诉/恢复原因与备注的服务端审核入口；高价值仓库 POST 路由改传 `getSessionActor(req)`，治理申诉保留 `getCohabitationGovernanceActor(req)` 以带入风险 hash 元数据。
- `server/src/taoyuanCohabitationRuntime.js` 继续补齐离线自动收益领取备注、离线冲突预检/解决备注、共同装修移动备注、同居权限变更/默认恢复备注、共同孩子照料备注的服务端审核入口；相关 POST 路由改传 `getSessionActor(req)`，补齐 request_id 审核上下文。
- `server/src/taoyuanCohabitationRuntime.js` 继续补齐家族建筑补偿重放、真实拆除复核/驳回/批准/执行请求、个人存档写回、主状态预览/映射/安全阀/执行、精确目标绑定/执行/解析/适配器和共同装修拆除主状态变更的 `reason` 服务端审核入口；对应真实拆除 POST 路由改传 `getSessionActor(req)`，并修复 `/real-demolition/reject` 与 `/permissions/default-restore` 的路由嵌套异常。
- `server/src/taoyuanCohabitationRuntime.js` 继续补齐共同基金大额草案执行/高风险回执备注，以及家族建筑真实落账、材料消耗、回滚、基金退款、材料恢复的 `memo` / `reason` 服务端审核入口；相关家族建筑 POST 路由改传 `getSessionActor(req)`，保留 request_id 审核上下文。
- `server/src/taoyuanCohabitationRuntime.js` 继续补齐分居预览确认、执行请求、资产返还、个人田区写回、共同基金差额确认/返还、共同仓库返还、装饰建筑拆分、剧情拆分/演出、个人剧情回执、孩子安排、个人家庭回执的 `memo` / `confirmation_text` 服务端审核入口；相关分居 POST 路由改传 `getSessionActor(req)`，保留 request_id 审核上下文。
- `server/src/taoyuanCohabitationRuntime.js` 继续补齐共同庄园日结备注、家族职位变更备注的服务端审核入口；`/daily-settle` 与 `/roles` 路由改传 `getSessionActor(req)`，保留 request_id 审核上下文。
- `/taoyuan/ai/ask` 和 `/admin/content/homepage-about` 已在路由层进入 `moderateText`；新增 `server/scripts/qa-content-moderation-route-guard.mjs` 启动临时服务端验证两条真实路由硬拦截返回 400，并确认 `ai_question` / `admin_content` 事件仅保存 hash 与短摘要。
- `server/src/routes/api.js` 已补后台 AI 知识库入口：`/admin/taoyuan/ai/knowledge` 创建/更新审核标题、正文、关键词，`/source-draft` 审核管理员输入的问题，`/:id/publish` 发布旧草稿前复核当前条目；事件场景为 `admin_ai_knowledge` / `admin_ai_knowledge_source_draft`。
- `server/src/routes/api.js` 已补后台发布配置文案：安卓发布配置的 `releaseNotes` / `forceUpdateMessage`，联机发布配置的灰度模板、功能说明、可见变化、玩家公告、已知问题、回滚说明均在保存前执行服务端审核，事件场景为 `admin_android_release_config` / `admin_online_release_config`。
- `server/src/routes/api.js` 已补 AI 助手后台配置发布审核：`/admin/taoyuan/ai/config` 的助手名称、欢迎语、署名文案、系统提示词、拦截主题说明在保存前执行服务端审核，事件场景为 `admin_ai_config`，并复用现有 `update_ai_config` 脱敏审计，不记录 API key 明文。
- `server/src/routes/api.js` 已补 official-control 官方云控发布审核：`/admin/official-control/config/publish` 对 `MANAGED_KEYS` 中的 AI 欢迎语/署名/名称、首页关于标题/正文执行服务端审核；发布成功会写入 `publish_official_control_config` 后台审计，detail 仅保留变更字段、长度和 hash，不保存原文。
- `server/scripts/qa-content-moderation-route-guard.mjs` 新增真实官方大厅活动公告路由覆盖：注册临时账号拿 session / CSRF 后，携带 `X-Admin-Token` 调用 `/taoyuan/hall/posts` 发布 `event_announcement`，确认违规正文返回 400，并写入 `admin_hall_post` / `blocks.text` / `hall_post` 脱敏审核事件。
- `server/src/taoyuanTextModeration.js` 将文本匹配窗口从 4000 提升到 20000，避免后台长正文在 4000 字后出现硬拦截内容时绕过命中。
- `server/src/taoyuanCohabitationRuntime.js` 新增离线队列通用 `content_moderation_rejected` 收敛：任一 supported action 在业务函数中触发 `moderateText` / `moderateCohabitationText` 时，`mergeCohabitationOfflineQueue()` 会返回 rejected operation，拒绝条目只保留 code、field、scene、类别和规则版本，不保存原文或命中词。
- `server/scripts/qa-content-moderation-guard.mjs` 扩展到活动房间、同居/家族、共同仓库高价值治理、离线冲突、权限、共同装修、儿童照护、共同基金大额执行/高风险回执、家族建筑落账链路、分居低层执行链路、日结/家族职位备注、家族建筑补偿重放、真实拆除主状态链路和离线队列 merge 真实契约夹具；当前分页验证 128 个拒绝场景均写入审核事件，且事件只有 hash 与短摘要。
- `server/scripts/qa-content-moderation-guard.mjs` 通过临时好友关系 + active 同居契约夹具逐一覆盖 `OFFLINE_QUEUE_SUPPORTED_ACTIONS` 的 24 个 action，包括共同农田、共同动物、共同宠物、共同工坊、共同装修移动、稀有物/家庭重大事件/限定装饰/装修拆除回执、日结和离线自动收益；确认审核拒绝被收敛为 rejected operation 且仍写入内容审核事件。
- `server/scripts/qa-content-moderation-route-guard.mjs` 新增动态软规则真实路由覆盖：不经过前端、直接 HTTP 调用 `/api/taoyuan/ai/ask` 时，软风险内容由服务端拒绝并写入 `soft_block` 脱敏审核事件；`/admin/taoyuan/content-moderation/events?action=soft_block` 可按动作反查场景、字段、规则版本、命中类别、hash 和 `outcome=rejected`，响应不暴露命中词明文。
- P0-4 验收闭环：硬拦截由真实 AI/后台内容/后台 AI 配置/AI 知识库/发布配置/官方大厅公告/official-control 路由和 128 个模块入口事件覆盖；软风险由模块级入口与真实 AI 路由 `soft_block` 事件覆盖；route guard 均为直打服务端 API，证明前端无法绕过服务端审核。
- 风险：活动公告等其他后台发布入口已补 AI 配置、AI 知识库、安卓发布说明、联机发布公告/模板、官方大厅活动公告和 official-control `MANAGED_KEYS` 专项 QA；official-control 非 managed 字段当前由实现忽略，不转化为玩家可见文案。好友/黑名单如未来新增备注字段，需要按本轮同样方式接入 `moderateText` 和审核事件。

## P1：后台能力补强

### P1-1 规则配置外置

- [x] 新增默认规则文件：`data-defaults/taoyuan_content_moderation_rules.json`。
- [x] 运行时读取：`data/taoyuan_content_moderation_rules.json`，不存在时用 defaults。
- [x] 规则结构建议：

```json
{
  "version": "2026.0603.1",
  "hard_block": [
    { "category": "illegal_harmful", "terms": [] },
    { "category": "pornography", "terms": [] },
    { "category": "gambling_drugs", "terms": [] },
    { "category": "terror_extremism", "terms": [] },
    { "category": "fraud_black_market", "terms": [] }
  ],
  "soft_block": [
    { "category": "spam_promotion", "terms": [] },
    { "category": "harassment_abuse", "terms": [] },
    { "category": "minor_risk", "terms": [] }
  ],
  "scene_policy": {
    "hall_post": "reject_hard_review_soft",
    "hall_reply": "reject_hard_review_soft",
    "admin_content": "warn_hard_review_soft"
  }
}
```

- [x] 规则变更必须写入后台审计，记录 `old_version`、`new_version`、操作者和摘要。
- [x] 管理端可以查看规则版本和最近更新时间，但不要展示完整敏感词清单给普通管理员。

本轮证据（2026-06-05）：

- 新增 `data-defaults/taoyuan_content_moderation_rules.json`，按 `hard_block` / `soft_block` / `scene_policy` 外置默认规则；`server/src/taoyuanTextModeration.js` 运行时优先读取 `data/taoyuan_content_moderation_rules.json`，不存在时回退 defaults，文件损坏或缺失时才使用代码内 emergency fallback。
- `moderateText()` 命中事件继续只记录 `matched_term_hash`、`content_hash`、短摘要和 `rule_version`；规则版本来自当前加载规则，默认文件版本为 `2026.0603.1`。
- 新增 `/admin/taoyuan/content-moderation/rules`：`GET` 只返回版本、更新时间、来源、分类数量、词条数量和场景策略，不返回词条明文；`POST` 仅 `adminAuth` 可更新运行时规则，响应仍只返回 metadata。
- `saveContentModerationRules()` 更新运行时规则时写入后台审计 `update_content_moderation_rules`，detail 包含 `old_version`、`new_version`、操作者、自动摘要和分类/词条计数，不写入敏感词明文；该 action 已加入 `server/src/db.js` 重大后台审计保留集合，避免普通清理误删。
- `server/scripts/qa-content-moderation-route-guard.mjs` 已验证 admin 规则 metadata 不暴露默认词条、规则更新响应不暴露新词条、审计 detail 不暴露新词条，且 AI 路由按动态规则版本 `qa.route.rules.2` 拦截并写入脱敏审核事件。
- 风险：前端内容治理页尚未接入规则 metadata 展示；规则变更接口当前支持整体替换运行时规则，尚未做字段级差异预览/审批流。

### P1-2 自动发现和临时处置

- [x] 同一用户短时间多次命中硬拦截，自动进入风险队列。
- [x] 同一帖子/回复/图片被多人举报，自动临时隐藏，等待管理员复核。
- [x] 同一图片 hash 被处置后，再次上传自动拒绝或进入待审。
- [x] 同一 IP hash 多账号高频发布，记录异常，不自动封禁，避免误伤。
- [x] 建立 `risk_score`，只作为排序和提示，不直接替代人工判断。

本轮证据（2026-06-05）：

- `server/src/taoyuanContentModerationAudit.js` 新增 `taoyuan_content_risk_queue.json` 风险队列；同一用户 10 分钟内达到 3 次 `hard_block` 自动生成 `repeat_hard_block` 信号，信号只保留事件 ID、内容 hash、命中类别、规则版本、计数和 `risk_score`，不保存原文或命中词。
- `server/src/taoyuanHall.js` 对同一帖子/回复达到 3 个不同 pending 举报人时自动临时隐藏；帖子从公开列表消失，回复在公开详情中只返回隐藏占位和原因，不返回原回复内容；同时写入 `multi_report_auto_hide` 风险信号。
- `server/src/taoyuanImageModeration.js` 对同一图片达到 3 个不同 pending 举报人时自动临时隐藏图片资产，并写入 `multi_report_auto_hide` 风险信号；信号只保留图片 hash 前缀、举报 ID、计数和 `risk_score`。
- `server/src/taoyuanImageModeration.js` 在上传登记时检测同 hash 已隐藏/处置图片；再次上传同一 hash 会返回 `IMAGE_HASH_PREVIOUSLY_DISPOSED` 拒绝，并写入 `duplicate_image_hash_reuse` 风险信号，只保存 hash 前缀、账号、处置资产 ID 和计数。
- `server/src/routes/api.js` 为在线写操作生成 `ip_hash` 并写入在线审计 detail；`server/src/taoyuanContentModerationAudit.js` 基于同一 `ip_hash` 10 分钟内 3 个账号、6 次写操作生成 `multi_account_ip_publish` 风险信号，只记录 `ip_hash`、用户名集合、路由 key、request_id 和 `risk_score`，不记录明文 IP/UA/设备指纹，也不自动封禁。
- `/admin/taoyuan/content-moderation/risk-signals`、`/admin/taoyuan/overview`、`/admin/taoyuan/hall/overview` 已返回风险信号；`risk_score` 仅用于排序和提示，没有自动封禁或删除。
- 管理员后续处置自动隐藏举报时，`buildReportDispositionAuditDetail()` 会把 `multi_report_triggered`、`auto_action`、`auto_action_at` 写入审计 detail。
- 风险：多人举报阈值、重复硬拦截阈值、同 IP hash 多账号阈值当前仍为服务端常量，尚未外置配置；IP hash 信号当前只做异常记录和排序提示，不直接替代人工判断。

### P1-3 管理端“内容治理”页

建议在现有大厅举报管理基础上扩展，不要重复造一个孤立页面。

- [x] 待处理举报列表：文本、图片、用户、系统入口统一展示。
- [x] 风险命中列表：按严重度、场景、用户、时间筛选。
- [x] 处置按钮：隐藏、恢复、驳回举报、封禁上传、封禁账号、加入观察、备注。
- [x] 证据面板：展示内容摘要、举报原因、命中类别、历史处置、相关用户风险。
- [x] 复核面板：支持误伤恢复，恢复也要写审计。
- [x] 统计卡片：今日命中、待审数量、自动临时隐藏、重复违规用户、误伤恢复。

本轮证据（2026-06-05）：

- `taoyuan-main/src/components/game/AdminOnlineGovernancePanel.vue` 在既有“联机治理”页新增 `admin-content-governance-panel`，集中展示待处理大厅举报、图片举报、风险信号、审核事件、规则 metadata、隐藏帖子/图片、图片黑名单和封禁账号恢复候选；未新增孤立后台页。
- 内容治理面板支持用户/ID 筛选、风险状态/类型/场景/严重度/时间筛选、审核事件场景/动作/时间筛选；风险信号展示 `risk_score`、场景、命中类别、事件/举报计数、图片 hash 前缀，不展示敏感词明文。
- 待处理举报卡片提供隐藏帖子、删除回复、隐藏图片、封禁上传、已处置、驳回；调用 `taoyuanHallApi.ts` 时带入 `report_id` 和处置原因，便于后台审计从举报反查最终动作。
- 复核恢复面板提供帖子恢复、图片恢复、图片上传限制解除、账号恢复；对应后端已有 `restore_hall_post`、`restore_hall_image_asset`、`remove_image_blacklist`、`unban_user` 审计路径。
- `server/src/taoyuanContentModerationAudit.js` 新增 `updateContentModerationRiskSignalStatus()`；`server/src/routes/api.js` 新增 `/admin/taoyuan/content-moderation/risk-signals/:id/status`，支持 `pending/reviewing/resolved/dismissed`，并写入 `update_content_risk_signal_status` 后台审计。
- `taoyuan-main/src/utils/adminOnlineApi.ts` 和 `taoyuan-main/src/types/adminOnline.ts` 补齐审核事件、风险信号、规则 metadata、风险状态更新的 API 与类型；`taoyuan-main/scripts/qa-online-ui-structure.mjs` 追加治理面板结构断言。
- `taoyuan-main/src/components/game/AdminOnlineGovernancePanel.vue` 新增待处理举报、风险信号、误伤恢复候选的独立备注输入；隐藏帖子、删除回复、隐藏图片、封禁上传、举报已处置/驳回、风险加入观察/标记处置/误伤关闭、帖子/图片/上传限制/账号恢复均会把 `admin_note` 透传到已有后台审计。
- `taoyuan-main/src/utils/taoyuanHallApi.ts` 为举报状态、图片举报状态、按举报隐藏图片、图片可见性、图片黑名单、帖子隐藏、回复删除补齐 `admin_note` payload；`taoyuan-main/src/utils/adminOnlineApi.ts` 为风险信号状态更新和账号恢复补齐 `admin_note` payload。
- `taoyuan-main/scripts/qa-online-ui-structure.mjs` 新增结构断言，覆盖 `governanceNotes` 输入、举报/风险/恢复备注 key，以及 `admin_note: options.adminNote` API 透传。
- `taoyuan-main/src/components/game/AdminOnlineGovernancePanel.vue` 的待处理举报卡片新增 `governance-report-evidence` 证据区，集中展示脱敏内容摘要、举报原因、命中类别、历史处置和相关风险；历史处置来自 `/api/admin/audit-logs` 后台治理审计，审核事件来自内容审核事件流水，风险来自内容风险信号。
- `taoyuan-main/src/utils/adminOnlineApi.ts` 新增 `fetchAdminGovernanceAuditLogs()` 读取后台治理审计日志；证据区按 report/post/reply/image id 和目标用户关联，不展示敏感词明文、IP、UA 或完整原文。
- `taoyuan-main/scripts/qa-online-ui-structure.mjs` 新增结构断言，覆盖后台审计日志读取、`governance-report-evidence`、`reportEvidenceSummary()`、`reportEvidenceCategories()`、`reportGovernanceHistoryLabels()`、`reportRiskEvidenceLabels()` 和短摘要 helper。
- `server/src/taoyuanContentModerationAudit.js` 为风险信号补齐 `scene` 字段、pending 信号合并保留场景，并支持 `/admin/taoyuan/content-moderation/risk-signals?scene=...&created_from=...&created_to=...` 查询；重复硬拦截、多人举报自动隐藏、重复图片 hash、同 IP hash 多账号发布均会写入风险场景。
- `taoyuan-main/src/components/game/AdminOnlineGovernancePanel.vue` 的风险命中列表新增场景和时间筛选；`taoyuan-main/src/utils/adminOnlineApi.ts` / `taoyuan-main/src/types/adminOnline.ts` 同步支持风险场景与时间查询参数。
- `server/scripts/qa-content-risk-discovery.mjs` 验证风险信号 scene 落库、scene 查询、时间查询、hash/IP 脱敏；`taoyuan-main/scripts/qa-online-ui-structure.mjs` 验证管理端风险场景/时间筛选、API 参数和类型字段。
- `taoyuan-main/src/components/game/AdminOnlineGovernancePanel.vue` 的待处理举报卡片新增“封禁账号”，风险信号卡片对明确单账号风险新增“封禁账号”；封禁前二次确认，复用独立处置/复核备注。
- `taoyuan-main/src/utils/adminOnlineApi.ts` 新增 `banAdminOnlineUser()`，复用现有 `/api/admin/users/:username/status`，提交 `status=banned`、`reason`、`admin_note`；后端既有 `set_user_status` 审计会记录封禁前后状态、原因、备注，并把封禁作为重大证据保留。
- `taoyuan-main/scripts/qa-online-ui-structure.mjs` 新增结构断言，覆盖举报/风险封禁按钮、封禁 API、`banned` 状态提交和备注透传；`server/scripts/qa-user-governance-audit.mjs` 已验证该封禁路由写入 `set_user_status` 审计且不泄露明文 IP/UA/密码/存档正文。
- `taoyuan-main/src/components/game/AdminOnlineGovernancePanel.vue` 的统一待处理列表新增 `pendingModerationEventReports` 和 `pendingRiskSignalReports`：大厅帖子/回复举报、图片举报、非大厅/图片的软拦截或待审审核事件、单用户 pending 风险信号、系统 pending 风险信号统一按时间展示。
- 统一列表新增 `system_moderation_event`、`user_risk_signal`、`system_risk_signal` 类型；大厅/图片处置按钮只对原举报类型开放，风险信号派生项可直接加入观察、标记处置或误伤关闭，系统审核事件以脱敏短摘要进入待复核列表。
- `taoyuan-main/scripts/qa-online-ui-structure.mjs` 新增结构断言，覆盖审核事件独立来源、用户/系统风险信号来源、统一列表类型区分，以及新类型不会误走大厅/图片举报接口。
- 风险：历史已落盘的旧风险信号不会自动回填 `scene`，需要结合审核事件/后台审计继续追溯；系统审核事件目前来自事件流水的软拦截/待审状态，本身没有单独状态更新接口，处置闭环需要通过关联风险信号、封禁账号或专门审计查询继续完成；证据区按当前已加载的最近后台审计/审核事件/风险信号关联，超出分页范围的历史证据需要进入专门审计查询页继续追溯。

## P2：质量和发布保障

### P2-1 QA 脚本

新增或扩展脚本，建议放到 `server/scripts/`：

- [x] `qa-content-moderation-guard.mjs`
  - 覆盖硬拦截、软拦截、正常文本、字段长度、换行限制。
- [x] `qa-content-moderation-route-guard.mjs`
  - 覆盖 AI 提问、后台首页内容、后台 AI 配置、后台 AI 知识库、AI 知识源码草稿、安卓发布配置、联机发布配置、官方大厅活动公告、official-control 云控发布等真实路由的服务端硬拦截和脱敏审核事件。
- [x] `qa-admin-audit-retention.mjs`
  - 覆盖日志写入、查询、归档、过期清理。
- [x] `qa-content-risk-discovery.mjs`
  - 覆盖短时硬拦截风险队列、帖子/回复/图片多人举报自动临时隐藏、重复处置图片 hash 拒绝、同 IP hash 多账号发布异常、风险信号脱敏和 `risk_score` 边界。
- [x] `qa-report-disposition-flow.mjs`
  - 覆盖举报 -> 后台处置 -> 审计 -> 恢复。
  - 2026-06-05：通过真实服务端路由覆盖大厅帖子/回复举报、隐藏帖子、删除回复、举报状态 resolved、恢复帖子和后台审计；审计 detail 验证 `report_id`、处置前后状态、短摘要、`ip_hash` / `ua_hash`，并确认不写入明文 IP / UA。
- [x] `qa-image-moderation-flow.mjs`
  - 覆盖上传登记、举报、隐藏、hash 重复、黑名单。
  - 2026-06-05：通过真实服务端路由覆盖图片上传登记、发图、图片举报、按举报隐藏图片、重复已处置 hash 上传 409 拒绝、`duplicate_image_hash_reuse` 风险信号、误伤恢复、图片上传黑名单/解除和后台审计脱敏。
  - 2026-06-05：`server/src/taoyuanHall.js` 的真实上传入口已把图片 `sha256` 写入 `registerUploadedImage()`，避免 QA 只在模块夹具里验证重复 hash，真实 `/taoyuan/hall/upload-image` 也能触发 hash 复用拒绝。
- [x] `qa-user-governance-audit.mjs`
  - 覆盖额度调整、重置密码、封禁/解封、删除、批量删除、存档导出、存档迁移、用户详情最近治理记录和审计脱敏。

### P2-2 回归范围

至少跑：

```powershell
node --check server/src/taoyuanTextModeration.js
node --check server/src/taoyuanImageModeration.js
node --check server/src/taoyuanOnlineAudit.js
node --check server/src/routes/api.js
node server/scripts/qa-online-release-guard.mjs
node server/scripts/qa-online-smoke.mjs
```

如果改了前端管理页，再跑：

```powershell
npm --prefix taoyuan-main run build
```

如果本机依赖缺失，记录原因，不要把“未跑”写成“通过”。

本轮验证（2026-06-05）：

- 通过：`node --check server/src/taoyuanTextModeration.js`
- 通过：`node --check server/src/taoyuanImageModeration.js`
- 通过：`node --check server/src/taoyuanOnlineAudit.js`
- 通过：`node --check server/src/routes/api.js`
- 通过：`node --check server/src/db.js`
- 通过：`node --check server/src/taoyuanMailbox.js`
- 通过：`node --check server/src/taoyuanManorRuntime.js`
- 通过：`node --check server/src/taoyuanSocialRuntime.js`
- 通过：`node --check server/src/taoyuanSocietyRuntime.js`
- 通过：`node --check server/src/taoyuanCoopOrderRuntime.js`
- 通过：`node --check server/src/taoyuanHall.js`
- 通过：`node --check server/src/taoyuanActivityRoomRuntime.js`
- 通过：`node --check server/src/taoyuanCohabitationRuntime.js`
- 通过：`node --check server/src/taoyuanContentModerationAudit.js`
- 通过：`node --check server/src/config.js`
- 通过：`node --check server/scripts/qa-content-moderation-guard.mjs`
- 通过：`node --check server/scripts/qa-content-moderation-route-guard.mjs`
- 通过：`node --check server/scripts/qa-content-risk-discovery.mjs`
- 通过：`node server/scripts/qa-content-moderation-guard.mjs`（扩展后覆盖活动房间、同居/家族、共同仓库高价值治理、离线冲突、权限、共同装修、儿童照护、共同基金大额执行/高风险回执、家族建筑落账链路、分居低层执行链路、日结/家族职位备注、家族建筑补偿重放、真实拆除主状态链路和离线队列 merge 真实契约夹具 108 个拒绝事件）
- 通过：`npm --prefix server run qa:content-moderation-route-guard`（含 AI/后台内容硬拦截、规则 metadata 不泄露词条、规则更新审计、动态规则版本命中事件）
- 通过：`npm --prefix server run qa:content-risk-discovery`
- 通过：`node server/scripts/qa-admin-audit-retention.mjs`
- 通过：`node server/scripts/qa-online-release-guard.mjs`
- 通过：`node server/scripts/qa-online-smoke.mjs`（并行批次曾因 120s 超时被工具终止；单独 300s 重跑通过。脚本中注册审核拒绝路径会输出预期 `TEXT_BANNED_TERM` 错误栈，进程退出码为 0）
- 通过：`npm --prefix taoyuan-main run qa:online-player-copy`
- 通过：`node --check server/src/taoyuanTextModeration.js`
- 通过：`node --check server/src/taoyuanImageModeration.js`
- 通过：`node --check server/src/taoyuanOnlineAudit.js`
- 通过：`node --check server/src/taoyuanContentModerationAudit.js`
- 通过：`node --check server/src/routes/api.js`
- 通过：`npm --prefix taoyuan-main run qa:online-ui-structure`（新增内容治理面板、风险信号、审核事件、规则 metadata、举报审计上下文结构断言）
- 通过：`npm --prefix taoyuan-main run check`（退出码 0；保留既有 3 条 warning：`onlineProfileApi.ts` consistent-type-imports、`CottageView.vue` / `FarmView.vue` no-floating-promises）
- 通过：临时 `DB_STORAGE=D:\taoyuan-latest\taoyuan-duli\tmp\taoyuan-risk-status-qa\db.json` 的 `node -e` 模块验证，确认 `updateContentModerationRiskSignalStatus()` 可将 pending 风险信号更新为 reviewing，且 outcome 变为 `under_observation`；`C:\tmp` 临时路径被当前沙箱拒绝创建目录，已改用工作区 `tmp`。
- 通过：冲突标记扫描 `rg -n "^<<<<<<<|^=======|^>>>>>>>" ...` 无匹配；`git diff --check -- ...` 仅报告现有 LF/CRLF 转换 warning。
- 通过：`node --check server/src/taoyuanHall.js`
- 通过：`node --check server/src/taoyuanTextModeration.js`
- 通过：`node --check server/src/taoyuanImageModeration.js`
- 通过：`node --check server/src/taoyuanOnlineAudit.js`
- 通过：`node --check server/src/routes/api.js`
- 通过：`node --check server/scripts/qa-report-disposition-flow.mjs`
- 通过：`node --check server/scripts/qa-image-moderation-flow.mjs`
- 通过：`npm --prefix server run qa:report-disposition-flow`
- 通过：`npm --prefix server run qa:image-moderation-flow`
- 通过：`npm --prefix server run qa:content-risk-discovery`
- 通过：冲突标记扫描 `rg -n "^<<<<<<<|^=======|^>>>>>>>" 0603后台加强.md server/src/taoyuanHall.js server/package.json server/scripts/qa-report-disposition-flow.mjs server/scripts/qa-image-moderation-flow.mjs` 无匹配。
- 通过：`git diff --check -- 0603后台加强.md server/src/taoyuanHall.js server/package.json server/scripts/qa-report-disposition-flow.mjs server/scripts/qa-image-moderation-flow.mjs` 退出码 0；仅提示 `server/package.json`、`server/src/taoyuanHall.js` 的 LF/CRLF 转换 warning。
- 通过：`node --check server/src/routes/api.js`
- 通过：`node --check server/src/db.js`
- 通过：`node --check server/scripts/qa-user-governance-audit.mjs`
- 通过：`node --check server/src/taoyuanTextModeration.js`
- 通过：`node --check server/src/taoyuanImageModeration.js`
- 通过：`node --check server/src/taoyuanOnlineAudit.js`
- 通过：`npm --prefix server run qa:user-governance-audit`
- 通过：`npm --prefix server run qa:admin-audit-retention`
- 通过：冲突标记扫描 `rg -n "^<<<<<<<|^=======|^>>>>>>>" 0603后台加强.md server/src/routes/api.js server/src/db.js server/package.json server/scripts/qa-user-governance-audit.mjs` 无匹配。
- 通过：`git diff --check -- 0603后台加强.md server/src/routes/api.js server/src/db.js server/package.json server/scripts/qa-user-governance-audit.mjs` 退出码 0；仅提示 `server/package.json`、`server/src/db.js`、`server/src/routes/api.js` 的 LF/CRLF 转换 warning。
- 通过：`node --check server/src/routes/api.js`
- 通过：`node --check server/src/taoyuanTextModeration.js`
- 通过：`node --check server/src/taoyuanContentModerationAudit.js`
- 通过：`node --check server/src/taoyuanAiAssistant.js`
- 通过：`node --check server/src/taoyuanImageModeration.js`
- 通过：`node --check server/src/taoyuanOnlineAudit.js`
- 通过：`node --check server/scripts/qa-content-moderation-route-guard.mjs`
- 通过：`npm --prefix server run qa:content-moderation-route-guard`（新增覆盖后台 AI 知识库正文、4000 字后长正文命中、AI 知识源码草稿问题、安卓发布说明、联机发布玩家公告）
- 通过：`npm --prefix server run qa:content-moderation-guard`
- 通过：`node --check server/src/routes/api.js`
- 通过：`node --check server/src/taoyuanTextModeration.js`
- 通过：`node --check server/src/taoyuanImageModeration.js`
- 通过：`node --check server/src/taoyuanOnlineAudit.js`
- 通过：`node --check server/src/taoyuanContentModerationAudit.js`
- 通过：`node --check server/scripts/qa-content-moderation-route-guard.mjs`
- 通过：`npm --prefix server run qa:content-moderation-route-guard`（新增覆盖 AI 配置欢迎语与 official-control 首页关于正文；测试中按服务端 `.env` 加载顺序解析云控二次密码，并用 raw Host 请求验证真实路由）
- 通过：`npm --prefix server run qa:content-moderation-guard`
- 通过：`node server/scripts/qa-ai-assistant-security.mjs`
- 通过：冲突标记扫描 `rg -n "^<<<<<<<|^=======|^>>>>>>>" 0603后台加强.md server/src/routes/api.js server/scripts/qa-content-moderation-route-guard.mjs server/src/taoyuanTextModeration.js` 无匹配。
- 通过：`git diff --check -- server/src/routes/api.js server/scripts/qa-content-moderation-route-guard.mjs server/src/taoyuanTextModeration.js` 退出码 0；仅提示 `server/src/routes/api.js`、`server/src/taoyuanTextModeration.js` 的 LF/CRLF 转换 warning。
- 通过：`node --check server/src/taoyuanCohabitationRuntime.js`
- 通过：`node --check server/src/taoyuanSocialRuntime.js`
- 通过：`node --check server/scripts/qa-content-moderation-guard.mjs`
- 通过：`node --check server/src/taoyuanTextModeration.js`
- 通过：`node --check server/src/taoyuanImageModeration.js`
- 通过：`node --check server/src/taoyuanOnlineAudit.js`
- 通过：`node --check server/src/routes/api.js`
- 通过：`node --check server/src/taoyuanContentModerationAudit.js`
- 通过：`npm --prefix server run qa:content-moderation-guard`（新增逐一覆盖 `OFFLINE_QUEUE_SUPPORTED_ACTIONS` 24 个离线队列 action，当前 128 个拒绝场景均有分页可读审核事件。）
- 通过：`node --check server/scripts/qa-content-moderation-route-guard.mjs`
- 通过：`node --check server/src/routes/api.js`
- 通过：`npm --prefix server run qa:content-moderation-route-guard`（新增覆盖官方大厅活动公告 `event_announcement` 真实路由：session / CSRF / 管理员 token 均成立时，违规正文被服务端拒绝并写入脱敏审核事件。）
- 通过：`node --check server/src/db.js`
- 通过：`node --check server/src/taoyuanOnlineAudit.js`
- 通过：`node --check server/src/routes/api.js`
- 通过：`node --check server/src/taoyuanTextModeration.js`
- 通过：`node --check server/src/taoyuanImageModeration.js`
- 通过：`node --check server/scripts/qa-admin-audit-retention.mjs`
- 通过：`npm --prefix server run qa:admin-audit-retention`（新增覆盖后台审计与在线审计的时间范围、用户、操作者、动作、结果组合查询。）
- 通过：`node --check server/src/db.js`
- 通过：`node --check server/src/taoyuanOnlineAudit.js`
- 通过：`node --check server/src/routes/api.js`
- 通过：`node --check server/src/taoyuanTextModeration.js`
- 通过：`node --check server/src/taoyuanImageModeration.js`
- 通过：`node --check server/scripts/qa-admin-audit-retention.mjs`
- 通过：`npm --prefix server run qa:admin-audit-retention`（新增覆盖后台审计最小字段顶层返回、detail 持久化和默认 `outcome=completed`。）
- 通过：`node --check server/src/routes/api.js`
- 通过：`node --check server/src/taoyuanHall.js`
- 通过：`node --check server/src/taoyuanImageModeration.js`
- 通过：`node --check server/src/taoyuanTextModeration.js`
- 通过：`node --check server/src/taoyuanOnlineAudit.js`
- 通过：`node --check server/scripts/qa-report-disposition-flow.mjs`
- 通过：`node --check server/scripts/qa-image-moderation-flow.mjs`
- 通过：`npm --prefix server run qa:report-disposition-flow`（新增覆盖大厅多人举报自动隐藏后处置非触发举报的 `multi_report_triggered` 审计字段。）
- 通过：`npm --prefix server run qa:image-moderation-flow`（新增覆盖图片多人举报自动隐藏后按非触发举报隐藏图片的 `multi_report_triggered` 审计字段。）
- 通过：`npm --prefix server run qa:content-risk-discovery`
- 通过：`npm --prefix taoyuan-main run qa:online-ui-structure`（新增覆盖管理端审核事件的场景、用户、时间、命中类别、处置结果展示和处置结果筛选。）
- 通过：`npm --prefix taoyuan-main run check`（退出码 0；保留既有 3 条 warning：`onlineProfileApi.ts` consistent-type-imports、`CottageView.vue` / `FarmView.vue` no-floating-promises）
- 通过：`node --check server/src/taoyuanTextModeration.js`
- 通过：`node --check server/src/taoyuanImageModeration.js`
- 通过：`node --check server/src/taoyuanOnlineAudit.js`
- 通过：`node --check server/src/routes/api.js`
- 通过：`node --check server/src/taoyuanContentModerationAudit.js`
- 通过：`node --check server/scripts/qa-content-moderation-route-guard.mjs`
- 通过：`node --check server/scripts/qa-content-moderation-guard.mjs`
- 通过：`npm --prefix server run qa:content-moderation-route-guard`（新增覆盖动态软规则真实 AI 路由、后台 `soft_block` 事件查询和响应不暴露命中词明文。）
- 通过：`npm --prefix server run qa:content-moderation-guard`
- 通过：`npm --prefix taoyuan-main run qa:online-ui-structure`（新增覆盖内容治理独立处置/复核/恢复备注输入和 `admin_note` API 透传。）
- 先前阻断：`npm --prefix taoyuan-main run check` 曾被 `src/views/game/InventoryView.vue(1520,7)` 的 `ItemDef | undefined` 到 `RecoverableItemDef` 类型错误阻断；本轮后续已重跑通过，见下方记录。
- 通过：`npm --prefix taoyuan-main run lint`（退出码 0；保留既有 3 条 warning：`onlineProfileApi.ts` consistent-type-imports、`CottageView.vue` / `FarmView.vue` no-floating-promises）
- 通过：`npm --prefix taoyuan-main run qa:online-ui-structure`（新增覆盖内容治理证据面板、后台治理审计读取、内容摘要、命中类别、历史处置、相关风险和短摘要 helper。）
- 通过：`npm --prefix taoyuan-main run check`（退出码 0；保留既有 3 条 warning：`onlineProfileApi.ts` consistent-type-imports、`CottageView.vue` / `FarmView.vue` no-floating-promises）

## 建议实施顺序

1. 先补审计：找出所有管理员治理路由，补 `appendAdminAuditLog`。
2. 再补内容审核事件：让 `moderateText` 命中时有事件流水。
3. 再改日志留存：避免证据被 5000 条上限顶掉。
4. 再做规则外置：降低后续维护成本。
5. 最后做管理端治理看板和自动发现。

## 完成定义

- [x] 所有 UGC 服务端入口都有审核。
- [x] 所有治理动作都有后台审计。
- [x] 内容审核命中有独立事件流水。
- [x] 日志默认不少于 180 天留存。
- [x] 严重处置证据不会被普通轮转清掉。
- [x] 管理端能完成举报查看、处置、恢复、审计追踪。
- [x] QA 脚本覆盖主要拦截和处置链路。
- [x] 文档更新：把最终实现写回本文件或追加到 `CHANGELOG.md`。

最终收口证据（2026-06-05）：

- 本轮复跑语法检查：`node --check server/src/taoyuanTextModeration.js`、`server/src/taoyuanImageModeration.js`、`server/src/taoyuanOnlineAudit.js`、`server/src/routes/api.js`、`server/src/taoyuanContentModerationAudit.js`、`server/src/db.js` 均通过。
- 本轮复跑服务端 QA：`npm --prefix server run qa:content-moderation-guard`、`qa:content-moderation-route-guard`、`qa:content-risk-discovery`、`qa:admin-audit-retention`、`qa:report-disposition-flow`、`qa:image-moderation-flow`、`qa:user-governance-audit` 均通过。
- 本轮复跑前端/发布验证：`npm --prefix taoyuan-main run qa:online-ui-structure`、`qa:online-player-copy`、`check`、`build` 均通过；`check` 仍保留既有 3 条 warning，`build` 仍保留 Vite 大 chunk warning。
- `data-defaults/taoyuan_content_moderation_rules.json` 已具备 `version`、`hard_block`、`soft_block`、`scene_policy` 结构；管理端规则接口仅返回 metadata，不返回词条明文。

收口风险（2026-06-05）：

- MySQL 侧仍未新增物理 `content_moderation_events` 表，当前以 JSON fallback 路径完成审核事件流水；MySQL 后台审计部分过滤仍依赖 detail JSON/LIKE 兜底。
- 历史旧风险信号不会自动回填 `scene`；需要结合审核事件、后台审计和专门查询页追溯。
- 系统审核事件没有独立状态更新接口，当前通过关联风险信号、治理动作、封禁/恢复和审计查询完成处置闭环。

## 风险提醒

- 规则误伤：不要直接扩大硬拦截范围，先用软拦截和待审观察。
- 隐私风险：不要保存完整 IP、UA、设备指纹或大段敏感原文。
- 前端绕过：所有审核必须在服务端完成，前端提示只能做体验优化。
- 证据丢失：不要继续只依赖固定条数截断的在线审计。
- 权限扩大：普通管理员可以处理举报，但规则配置、日志导出、批量封禁建议只给超级管理员。
- 编码问题：仓库里部分中文在终端显示可能乱码，编辑文件时保持 UTF-8，不要因为 PowerShell 显示乱码而重写大量无关文本。
